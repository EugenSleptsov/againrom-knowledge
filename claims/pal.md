# Claim registry — PAL (.pal palette)

Level 2 ledger. Index: [registry.md](registry.md) · spec: [`formats/pal/format.md`](../formats/pal/format.md). Legend: `✔ promoted` (also in the spec) · `● active` · `✖ retracted`. IDs are permanent.

> `.pal` is not a container of its own: it is a **BMP** whose 256-entry colour table the engine
> reads and whose pixel data it never opens. It matters because it is the whole of the per-tier
> and per-owner recolour — there is no arithmetic anywhere in that path.

| ID | Claim | Confidence | Status | Evidence |
|----|-------|-----------|--------|----------|
| PAL-FILE-001 | A `.pal` the unit loader opens is a **BMP**: the engine seeks `0x36` and reads exactly `0x400` bytes — a 256-entry `[B,G,R,0]` colour table, the same layout as a `.256`'s own leading 1024 bytes (`SPR256-PAL-003`/`011`). The pixel data after it is never read. Shipped sizes 17462 B = 54 + 1024 + 128·128 (26678 for `dragon`, 160·160). The **one exception is `graphics\units\humans\human.pal`**, which is opened with **no seek** and read `0x4000` bytes whole: 16 raw 1024-byte tables, not a BMP (its first two bytes are `00 00`, not `BM`) | High (`0046c161` open → `0046c168 PUSH 0x36` seek → `0046c176 PUSH 0x400` malloc → `0046c197` read, against `0046c889` open → `0046c88e PUSH 0x4000` read with no seek in between; the sizes are a census over 82 `.pal` nodes) | ● active | [EXP-0089](../experiments/EXP-0089-tier-hue/) |
| PAL-KEY-002 | `class+0x98` is the `units.reg` key **`Palette`** — the literal at `0x005bda8c`, read at `0046bfed` with the parent's own `+0x98` as the default and **0** when there is no parent (`0046bfe3`). It is the length of the `class+0x9c` shade-object array and of the parallel `class+0xac` palette-buffer array, both filled by one loop `0046c036…0046c1c8`. Over the 34 shipped classes: **0 on 18** (every human/hero class), **1 on 3** (`Catapult 1`, `Catapult 2`, `Death Star`), **4 on 13** (every monster class that ships tiers) | High (the key literal is a `PUSH` in the loader's own listing; the array identity is the same loop that allocates `+0x9c[i]` and `+0xac[i]`; the census is `evidence/units-palette-summary.txt`) | ● active | [EXP-0089](../experiments/EXP-0089-tier-hue/) |
| PAL-NAME-003 | The per-class palette file's name is **built, never stored**: `dirname(Files[class.File]) + "\palette" + (i ? str(i+1) : "") + ".pal"`, i.e. `palette.pal`, `palette2.pal`, `palette3.pal`, `palette4.pal` in the sheet's own directory. The digit is struck off for `i == 0` by an explicit `buf[len-1] = 0` (`0046c0bc TEST EBX,EBX` / `0046c0d1`), so tier 1's file is `palette.pal`. Corpus: **52/52** nodes named this way exist in `graphics.res` | High (the format literals `"\palette%d"` `0x005bda80` and `".pal"` `0x005bda78` sit in the loader's own `PUSH`es; the truncation is a named instruction; existence is a census) | ● active | [EXP-0089](../experiments/EXP-0089-tier-hue/) |
| PAL-TIER-004 | **The tier's colour treatment is that shipped palette and nothing else.** The unit body draw selects `class+0x9c[j]` with `j = (unit+0x24) − 1` when `Palette > 1` (`0045b94a…0045b97c`), lazily building it as `FUN_00427df0(class+0xac[j], nLevels=0x10, mode=2, useTint=1)` — the **same** builder, arguments and blit lookup every other sprite uses (`TERR-LIGHT-059`/`060`/`064`). No arithmetic distinguishes a tier: only which 1024 bytes went in | High (every argument is a `PUSH` at a named address; the subscript is `0045b954` + `0045b95a DEC EBP`; `class+0x9c` and `class+0xac` are `0x10` apart, so each array is four dwords and `Palette ≤ 4` is a struct-width fact) | ● active | [EXP-0089](../experiments/EXP-0089-tier-hue/) |
| PAL-FACE-005 | **`unit+0x24` is the actor's `face`** — `Data.bin` Units/Humans column 30/17, streamed to `actor+0x4b` (`004f58c3`, `UNIT-STREAM-001` slot 30), carried in the client state message as the **second** of two bytes under sync mask bit `0x4000` (`004111b2` type id, `004111e4` face), landing at `unit+0x24` through `FUN_0045f850`'s only argument (`0045f883`/`0045f8cf`/`0045f8fa`) or directly at `00411eff`. The identification is not nominal: `FUN_0045f850` (`0045f928`) and the prototype-cache builder `FUN_00477b10` (`00477b62`) compute the **same** expression `typeID*4 + X` into the **same** array `0x5efa34` — one from this byte, one from the `Data.bin` row's own `face` column (`UNIT-PROTO-008`). The accessor `FUN_0051cbd0` masks the field `& 0x3f`; `FUN_0045f850`'s human arm masks `& 0x7f` and branches on bit 7 | High (each hop is a named instruction, and the shared subscript expression discriminates against every rival label the byte could carry — an owner, a group, a placement index would not index a `(typeID, face)` cache) / **Instrument**: `EnumRefs disp:24` and `disp:4b`, whole image, 0 orphan hits; **blind spot**: a `disp:` sweep cannot see a wholesale `memcpy` of the containing structure. `CUnit` is absent from the 28-record serializable-class table (`ANIM-CLOCK-001`), so no `Serialize` path restores it either | ● active | [EXP-0089](../experiments/EXP-0089-tier-hue/) |
| PAL-XFORM-006 | **No closed form reproduces a tier palette from tier 1.** Four rivals, each fitted to its own optimum over the 39 shipped (class, tier ≥ 2) pairs, error in 8-bit channel units after round-and-clamp: per-channel affine `c' = a·c + b` best **18**, worst 118; general 3×3 matrix + offset — which contains every hue rotation, saturation scale and channel swap — best **4**, worst 80; HSV hue rotation with S,V preserved gives an inter-quartile hue spread up to **357.9°**; an index remap is impossible because at most **191** of 256 tier colours occur anywhere in tier 1 (median 32). A consumer must read the file. Two self-checks: **tier 1 is byte-identical to the sheet's own embedded palette, 13/13 classes, all 256 entries** — so `j = 0` is "no recolour" — and **palette entry 0 is identical across every tier of every class, 39/39**, which is the transparent key (`SPR256-PAL-012`) and the one index a reproduction must not touch | High (a fitted rival that cannot reach zero error on its own best parameters is excluded, not merely unsupported; the two identities are exact byte comparisons) | ● active | [EXP-0089](../experiments/EXP-0089-tier-hue/) |
| PAL-OWN-007 | **The `Palette == 0` arm is team colouring, and it is the other branch of the same `if`.** `0045b8fa` takes `[0x005eb65c][k]` with `k = (unit+0x14)->+0x08`, from 16 shade objects the `units.reg` loader builds at `0046c8b0…0046ca92` over `graphics\units\humans\human.pal` at stride `0x400`, each `(0x10, mode 2, useTint 1)`; a **17th**, `[0x005eb65c][16]` (read as `[[0x005eb65c]+0x40]` at `0045b9ab`), is built `(0x10, mode 5, useTint 0)` from sub-palette 0 — mode 5 is greyscale. The 16 sub-palettes differ from sub-palette 0 on **exactly the same 55 indices** for all fifteen k (`4,10,13,15-17,21,23-24,28,36,41-42,44-45,55,81,89,104,106,109,114,116,118-120,123-125,127,141,144-147,150-151,162-164,168-174,176-179,181,193,211,225`), and index 164 reads as eight hues at two brightness levels with k=7 and k=15 neutral grey | High (the build loop, its stride and the mode/level arguments are named `PUSH`es; the 55-index band and the eight-hue structure are exact byte measurements) / ~~**Medium** (that k *is* the owner's colour slot: `unit+0x14` is written at `00411718` from `[[view+0x9a4] + idx*4]` under sync mask bit `0x40`, but no writer of that record's `+0x08` was located, so the reading rests on the palettes' shape rather than on a store)~~ — *[EXP-0180]: the writer is located and this clause is raised to **High**. The record is the interface `CPlayer` (`UNIT-VPLAYER-022`); its `+0x08` has exactly two writers, both constructor stores — `0048da11` from `FUN_0048d9d0`'s second argument and `0048d976` in the copy constructor — and the value arrives on session message `0x96` from either the ALM type-5 record's `+0x00` word or the session slot (`PAL-SHADE-012`, `PAL-SHADE-013`, `ALM-PLAYER-069`). The reading rests on a store* | ● active (amended) | [EXP-0089](../experiments/EXP-0089-tier-hue/), [EXP-0180](../experiments/EXP-0180-shade-select/) |
| PAL-CORP-008 | Every palette node this ledger names is **byte-identical across all three roots** — the live GOG install, `gameversions\en` and `gameversions\ru` — over the 52 per-class tier tables (1024 B at `0x36`) and `units/humans/human.pal` (16384 B, `ed8f0e6d…`). Every `Data.bin` row whose `typeID` resolves to a `units.reg` class (**262** rows across Units and Humans) carries a `face` inside `[1, Palette]` on every class that owns palettes: **0 violations** | High (a hash census over three roots, and a corpus check with a single failure mode) | ● active | [EXP-0089](../experiments/EXP-0089-tier-hue/) |
| PAL-LIMIT-009 | The tier limit and what lifting it costs. **4 tiers per class is a compiled constant** — `class+0x9c` and `class+0xac` are `0x10` apart, so `Palette > 4` overruns one array into the other at the loader's own stride-4 store `0046c05b`; widening the struct is free for us and changes no shipped byte. **`face` on the wire is one byte masked `& 0x3f`**, so 63 values are already reachable and 4 are used. What is *not* free: each tier is a **shipped file** (`paletteN.pal`), so a new tier adds a `graphics.res` node, and giving tiers to a class that had none also edits `units.reg` (`Palette`) and `Data.bin` (a new Units row). And `typeID*4 + face` into `0x5efa34` is a **semantic coupling**: the stride 4 is the tier count and it is spelled in two routines (`0045f928`, `00477b62`) that must agree | High (each classification is a named instruction or a named file; the array width is a structure-geometry fact) | ● active | [EXP-0089](../experiments/EXP-0089-tier-hue/) |
| PAL-MODE4-010 | **(rom.exe) Mode 4 is the `.16a` table and it is the one mode that ignores the day/night tint; its `/18` sibling is gated on installed RAM, not on the display.** `FUN_00427df0`'s six-arm switch is documented for modes 2, 3 and 5 by this ledger and by `TERR-LIGHT-019`; arm 4 is the one `.16a` construction asks for (`FUN_00428ad0(0x10, 4, tint)` -- `SPR16A-PIX-011`, and `FUN_0046dd90` at `0046dff4` for projectiles). It builds `for level = 1..16, for i = 0..255: chan = clamp((pal[i].chan * level) >> 4, 0, 255)` packed to the framebuffer's widths and shifts, and **the three tint locals the other arms add are not referenced on this arm** -- so `useTint` has no effect on a `.16a` sprite whatever the caller passes, and cast art does not change colour with time of day the way a unit does. The alternative branch inside the arm divides by **18** instead of shifting by 4, and it is selected by `DAT_005eb570`, whose only writer is `FUN_0044ba10`: `GlobalMemoryStatus(&ms); DAT_005eb570 = (ms.dwTotalPhys < 24000000)`. The same flag halves the companion destination table from 65 536 entries a row to 8 192 and makes the blitter quantize the old pixel by `>> 3`. `formats/spr16a/format.md` had this pair recorded as a per-display-mode residual; it is a **low-memory** fallback and is unreachable on any machine that can run the install. **G2:** engine-side and not elastic -- the 16 rows are fixed by the 4-bit level field, and the memory gate is a constant in the image, so lifting either edits `rom.exe` rather than a data file | High (the arm read as instructions with every immediate cited, the tint's absence read as an absence inside one bounded switch arm, and the gate read at its only writer, which `ScanField wr:` returns as a single hit for all three globals) | ● active | [EXP-0140](../experiments/EXP-0140-cast-art-drawn/) |
| PAL-PROJ-011 | **(rom.exe) `projectiles.pal` and `projectile_.pal` are not sprite palettes at all -- each becomes a 16-level mode-2 shade table with the tint OFF, and only the first of the two is ever read by a blit.** `FUN_0046e0f0`, outside the registry loop, opens each with this ledger's shape-A idiom (`open`, `seek 0x36`, `read 0x400`), news a **12-byte** object through `FUN_00427d20` (`*this = &PTR_FUN_00597378; this[2] = 0` -- the shade-table constructor, not a sprite), stores it in `DAT_005efa24` / `DAT_005efa28`, and calls `FUN_00427df0(table, 0x10, 2, 0)` -- mode 2, **`useTint = 0`**, unlike every `units.reg` palette, which is built with `useTint = 1`. So the shared projectile tables are fixed at build and do not move with the daylight cycle. `ScanField rd:` finds `005efa24` read at exactly two sites -- `0046e524` in the teardown `FUN_0046e4e0` and `0046236e` in the draw `FUN_00461fc0`, where it is pushed as the external-table argument of `vt+0x14` -- and `005efa28` read at exactly **one**, `0046e547`, the teardown. **`projectile_.pal` is loaded, parsed, built into a table and never drawn with.** Both files are 5 174-byte BMPs (54 + 1 024 + 64x64) on both preserved roots and their colour tables differ on **255 of 256** entries, maximum per-channel delta 232, the second being largely a grey ramp -- so the choice is not cosmetic, and a consumer that picks the wrong one gets desaturated arrows. Neither name occurs as a byte string anywhere in the install's other executable (`Map Editor.exe`: 0 hits each, against 1 each in `rom.exe`), so the dead file is not the editor's either. **G2:** both are data files and free -- replacing either changes only that file's bytes -- but only one of them has any effect, which is itself the customisation limit worth knowing | High (the construction read instruction by instruction, and the reader sets from a reference-manager scan whose blind spot -- orphan code -- is named in `docs/INSTRUMENT.md` and does not apply to a global read through an absolute address; the render **cannot** discriminate the two tables and is not what grades this row: drawing the palette-less arrows through `projectile_.pal` still yields recognisable arrows, only paler) | ● active | [EXP-0140](../experiments/EXP-0140-cast-art-drawn/) |
| PAL-SHADE-012 | **(rom.exe) Within the vptr-stamped `CPlayer` constructor family, the shade-selector field `+0x08` has exactly two writers, both constructor stores.** `0045b903 MOV EAX,dword ptr [EDX + 0x8]` reads `k` off the object at `drawable+0x14`, which `UNIT-VPLAYER-022` identifies as a `CPlayer`, RTTI object size `0x48`, vtable `0x0059a560`. Three instructions stamp that vptr: `0048d891` in `FUN_0048d850` leaves `+0x08` alone, `0048d95c` in copy constructor `FUN_0048d920` copies it at `0048d976`, and `0048da1e` in `FUN_0048d9d0` writes its second argument at `0048da11`. The actual vtable surface is **six dwords** — five function entries plus a null at `0x0059a574`; `0x0059a578` begins the next class (`ALM-CPLAYER-090`). The earlier 16-slot dump crossed that boundary, and the 13-function `0048d800..0048e000` neighbourhood likewise includes next-class routines. The value reaches the drawable at `00411718` from the array at `view+0x9a4`, closing `PAL-OWN-007`'s stated writer gap | **High** for the two stores inside the bounded constructor family: the three vptr sites and six-dword boundary are independently enumerated / **Medium** for "no aliased instruction elsewhere writes this field": `disp:8` returns 14 921 hits over 4 468 owners (55 orphan/undisassembled), with a further 568 hits at displacements 9/a/b, so a computed or aliased store remains outside the construction census | ● active (amended) | [EXP-0180](../experiments/EXP-0180-shade-select/), [EXP-0297](../experiments/EXP-0297-roster-scalar-reader/) |
| PAL-SHADE-013 | **(rom.exe) The slot value arrives on session message `0x96` and is produced by three instructions under two rules, whose data origins are the map's own per-player word and the session slot.** The interface builds the array at `view+0x9a4` in the message handler `FUN_004104e8`, arm `0041592f`; the dispatch is read as data on three roots — `[0x0041879b + (0x96-3)] = 0x1d` and `[0x004186e3 + 29*4] = 0x0041592f`. The arm takes the index from `0041593e MOV EAX,[EDX + 0xa]` / `00415941 AND EAX,0xffff` and the slot from `00415a0d MOV EDX,[ECX + 0xa]` / `00415a10 SAR EDX,0x10` / `00415a13 AND EDX,0xff`, that is the byte at message `+0x0c`, and passes them to `FUN_0048d9d0` at `00415a27`. Exactly two instructions in the image store `0x96` at a message's `+0x09`. `004e9fee` in the catch-up sender `FUN_004e9fa0`, which announces every existing player to one recipient, always computes `004ea011 MOVSX ECX,word ptr [EAX + 0x4]` / `004ea015 AND ECX,0xf` / `004ea01b MOV byte ptr [EDX + 0xc],CL`. `004d2e62` in the join sender `FUN_004d27dd` branches on `004d2e82 MOV CL,byte ptr [EAX + 0x44]` and writes `004d2e97 MOV byte ptr [ECX + 0xc],AL` with `AL = Player+0x44 - 1` when that byte is non-zero, otherwise the same `& 0x0f` fallback at `004d2ea9`. `Player+0x44` is `0` from the constructor (`004fab45 MOV byte ptr [EDX + 0x44],0x0` in `FUN_004faa81`) and is written non-zero at one instruction, `004e243e MOV byte ptr [ECX + 0x44],AL` in `FUN_004e2327`, from `CPlayer+0x08 + 1` — the ALM type-5 record's `+0x00` word (`ALM-PLAYER-069`). The fallback's `Player+0x04` is the lowest free value in 1..0x1f, allocated by `FUN_004fb312` at `004fb3d1`. `FUN_004d303e`, the mission-entry handler, builds no `0x96` message and holds no `+0x44` reference; it calls the catch-up sender at `004d33e6` | High (each producer is a named store, all 61 cited instructions re-read from raw image bytes on three roots with 0 mismatches, and the two senders are the complete population of `0x96` builders over a `disp:9` sweep of 216 hits filtered to byte writes) / **Medium → answered by `PAL-RULE-021`** (which of the two rules a shipped single-player mission takes. The clause was framed as an ordering question — whether the map load, and therefore `Player+0x44`, precedes the join. The ordering is not load-bearing: the join allocates a fresh `Player` at `004d2d69`, whose `+0x44` is 0 from the constructor, so a join always takes the `& 0x0f` fallback, and a faction the map created never joins at all. A shipped single-player mission takes `Player+0x04 & 0x0f`) | ● active (amended) | [EXP-0180](../experiments/EXP-0180-shade-select/) |
| PAL-FIGURE-014 | **(rom.exe) The equipment-layer compositor applies no owner shade: it names no palette global and can receive none, so the character doll and the dialogue speaker carry the layers' own colours.** `FUN_0045ed10` (`DLG-FIGURE-020`, `HERO-APPEAR-051`, `UNIT-FIGURE-032`) is `RET 0xc` with three parameters, all surfaces, so no shade object can enter it as an argument. Over `0045ed10..0045f850` it contains **0** references to `0x005eb65c`, the 17 shade objects, and **0** to `0x005eb674`, the `units.reg` class table whose `+0x9c` array holds the tier palettes. Two instruments with disjoint blind spots agree: the reference-manager sweep `refto:5eb65c` returns 23 hits over 6 owners with 0 in orphan code, and an independent byte-by-byte scan of `.text` for the little-endian dword returns the same 23 hits attributed to the same 6 owners on all three roots — `FUN_0045a800` (1), `FUN_0045b3f0` (2), `FUN_0045ce00` (1), the loader `FUN_0046ba80` (14), the teardown `FUN_0046cc90` (3) and `FUN_0046f1e0` (2). The raw scan reads bytes no disassembler visited, which is the reference sweep's only blind spot for an absolute-address operand. Its 31 layer blits through vtable slot `+0x18` push five arguments each — seventeen push five literal zeros, fourteen push `EBX` five times, `EBX` being zeroed at `0045f01a` — but that detail is not load-bearing: no shade object is in scope to push. The selection is therefore in the **world unit body draw** and nowhere else: `FUN_0045b3f0` hands the object it selected at `0045b8fa` to a sprite blit — `0045bad8 MOV EDX,dword ptr [ESP + 0x18]` (the saved slot, one push down), `0045bade PUSH EDX`, `0045baeb CALL dword ptr [EAX + 0x34]`. **`PAL-BLIT-024` extends this**: `FUN_0045b3f0` has **five** blit call sites, not one — `0045baeb`, `0045bb0e`, `0045bbaa`, `0045bcd5`, `0045bd13` — and every one of them pushes that same stack slot in the same position, on both sides of the `drawable+0x18c & 1` branch. The sentence above names the first of the five | High (the parameter count is the routine's own `RET` immediate; the two absence instruments have disjoint blind spots and agree exactly; the counts are identical on three roots) | ● active (amended) | [EXP-0180](../experiments/EXP-0180-shade-select/) |
| PAL-SLOT-015 | **The sixteen owner slots are seven hues and a grey, each at two brightness levels, and the pairing is `k` with `k+8`.** Measured over the 55 band indices of `PAL-OWN-007` in `graphics\units\humans\human.pal` (16 384 B, sha256 `ed8f0e6d…c501cdf0`, byte-identical on all three roots; 16 flat 1024-byte `[B,G,R,0]` tables, byte 3 zero in all 4 096 entries). Saturation-weighted circular mean hue in degrees, then mean saturation and value: `k=0` 235.8 blue, `k=1` 112.1 lime-green, `k=2` 354.5 red, `k=3` 22.0 red-orange, `k=4` 287.7 violet-magenta, `k=5` 39.0 orange-yellow, `k=6` 174.5 cyan, `k=7` grey; all seven chromatic slots at saturation 0.510 and value 0.573. `k=8..15` repeat the same seven hues within 1.0 degree and the grey, at saturation 0.561 and value **0.423**, and `k=15`'s grey is darker still at value 0.303. Every one of `k=1..15` differs from `k=0` on all 55 band indices, and the union of differing indices over the fifteen is exactly the published 55-index set, re-measured here rather than assumed. Hue names are the nearest of twenty-four names spaced 15 degrees apart, a mechanical mapping from the measured angle. Per-entry colours are in `evidence/shade-slots.csv`, 2 640 rows a root | High (exact byte measurements over a file that is byte-identical on three roots, with the band re-derived rather than taken from the earlier row; the hue names are a stated mechanical mapping and carry no confidence of their own) | ● active | [EXP-0180](../experiments/EXP-0180-shade-select/) |
| PAL-BAND-016 | **The band recolours the sleeves of a fighter and the hood and mantle of a mage, and it reaches only the 18 `Palette == 0` classes.** Complete decode of both sheets on three roots, every frame tiling exactly with no RLE failure: `units/humans/swordsman/swordsman.256` 152 frames, 56 503 opaque pixels, **7 374 in the band = 13.05 %**; `units/humans/mage/sprites.256` 144 frames, 80 809 opaque pixels, **36 419 in the band = 45.07 %**. Band pixels as a percentage of opaque pixels per tenth of the figure's own bounding box, head to feet — swordsman `2.8 4.0 9.8 12.6 16.0 22.3 21.7 15.7 5.3 1.1`, mage `67.9 58.7 40.9 46.4 52.1 49.6 46.3 41.3 26.9 4.9`. The two classes differ in how much of the figure the owner colour claims and in where it sits: on the fighter it is a mid-height band, and the helmet, breastplate, gloves, belt, boots and weapon are outside it; on the mage it is heaviest at the head. Only classes whose `units.reg` `Palette` is 0 reach the selection at `0045b8fa`, which is 18 of 34 — every human and hero class, and no monster, catapult or Death Star class (`PAL-KEY-002`) | High (the counts and the profile are a complete decode with a per-frame tiling invariant that would fail loudly, identical on three roots) / **Medium** for the words "sleeves", "hood" and "mantle", which name what a render of the marked pixels shows rather than what a byte says | ● active | [EXP-0180](../experiments/EXP-0180-shade-select/) |
| PAL-LIMIT-017 | **What the owner-colour path lets an author customise, and what lifting each limit costs.** Decisive test: does lifting it change the bytes of a file the original wrote? **Free, data only.** (1) Which of the 256 palette indices recolour, and to what: the 55-index band and every slot's colours are `human.pal`'s own content, and the loader reads 16 tables at stride `0x400` out of a 16 384-byte file whatever they contain. (2) Which classes are owner-shaded: the `units.reg` `Palette` key, 0 on 18 of 34 classes. (3) Which slot a faction takes: the ALM type-5 record's `+0x00` word (`ALM-PLAYER-069`). (4) The eight-hue, two-brightness structure is authoring and not code — nothing in the image reads it. **Not free, engine-side.** (5) **Sixteen team slots is a compiled constant**: the build loop `0046c8b0…0046ca92` fills 16 objects and the 17th, `[0x005eb65c][16]` read at `0045b9ab`, is the greyscale one, so a 17th team slot collides with it. (6) **`k` is read unmasked**: `0045b903 MOV EAX,dword ptr [EDX + 0x8]` then `0045b906 MOV EDX,dword ptr [ECX + EAX*0x4]`, with no bound test between them, so an authored word above 16 indexes past the allocated table. The wire carries a full byte (`00415a13 AND EDX,0xff`), so 0..255 reach the array. The safe authored range is **0..15**. Shipped content stays inside it: over 357 type-5 records on three roots the maximum is 13, and slots 11, 12, 14 and 15 never occur | High (each classification is a named instruction, a named data key or a named shipped file; the unmasked read is two adjacent instructions with nothing between them; the corpus bound is a complete census) | ● active | [EXP-0180](../experiments/EXP-0180-shade-select/) |
| PAL-RULE-021 | **(rom.exe) On a shipped single-player campaign mission a faction's owner shade slot is its 1-based ordinal in the ALM type-5 roster, not the `+0x00` word that roster authors.** `PAL-SHADE-013`'s rule A, `Player+0x44 - 1`, needs `Player+0x44` non-zero. The session `Player` constructor sets it to 0 (`004fab45` in `FUN_004faa81`) and exactly one instruction in the image writes it non-zero — `004e243e` in `FUN_004e2327`, inside the map load. `FUN_004d27dd` allocates a fresh `Player` at `004d2d69` on the path that reaches the rule test at `004d2e82`, with no call into `FUN_004e2327` between them, so a join reads 0 and takes the fallback `004d2ea9`, `Player+0x04 & 0x0f`. A faction the map created never joins, so the only sender that announces it is the catch-up sender `FUN_004e9fa0`, which has one rule (`004ea011`/`004ea015`/`004ea01b`, the same `& 0x0f`). `FUN_004fb312` allocates the lowest free index in `1..0x1f` and `FUN_004e2327` walks the type-5 records in file order, so the *n*-th record's index is *n*: the first reuses the one existing `Player` when the multiplayer flag is clear and the player count is 1 (`004e238f`, `004e2395`, `004e23a1`, `004e23b1`), every later one allocates. The ordering the question was framed around is not load-bearing, because neither order lets a join read a non-zero `+0x44` | High (each step is a named instruction, all re-read from raw image bytes on three roots with 0 mismatches; the fresh-`Player` allocation and the constructor default together rule out rule A on the join path) / **Medium** (that a *shipped* mission takes rule B: the step from "no path in this reading sets `+0x44` first" to "no path exists" rests on the completeness of the `+0x44` writer enumeration and on `FUN_00477c00` being the only mission start that loads a map, and a virtual dispatch bounds both. [EXP-0181](../experiments/EXP-0181-owner-colour-rule/) states the prediction a running original refutes) | ● active | [EXP-0181](../experiments/EXP-0181-owner-colour-rule/) |
| PAL-FIRST-022 | **(rom.exe) The interface `CPlayer` array entry is created by the first `0x96` for a given index and is never re-slotted.** Arm `0041592f` of `FUN_004104e8` tests the array cell before constructing — `00415999 CMP dword ptr [EAX + ECX*0x4],0x0` — and on a non-zero cell takes the reuse path at `004159dc MOV EAX,dword ptr [ECX + EDX*0x4]`, which contains no store to `+0x08`. The construction path takes the slot from message `+0x0c` (`00415a0d MOV EDX,[ECX + 0xa]`, `00415a10 SAR EDX,0x10`, `00415a13 AND EDX,0xff`) and calls `FUN_0048d9d0` at `00415a27`. `PAL-SHADE-012` established that `+0x08` has exactly two writers, both constructor stores. The one other creator is `FUN_004023c6` at `00402baa`, the index-0 entry. Consequence: a second `0x96` naming the same index with a different slot byte cannot change the colour of an already-created entry, so the catch-up sender's announcement is decisive only where it is first | High (the reuse branch and the absence of a `+0x08` store on it are read in the arm's own listing, and the two-writer enumeration is `PAL-SHADE-012`'s construction census, not a displacement sweep) | ● active | [EXP-0181](../experiments/EXP-0181-owner-colour-rule/) |
| PAL-JOIN-023 | **(rom.exe) `PAL-SHADE-013`'s rule A is the reconnect rule.** `FUN_004d27dd` has two paths to the `0x96` build. One matches the joining name against an existing session `Player` and reuses it; the other allocates a fresh `Player` at `004d2d69 CALL 0x004d35e6`. Only the first can present a non-zero `Player+0x44` at `004d2e82`, because `+0x44`'s sole non-zero writer is in the map load and the constructor's default is 0. So `Player+0x44 - 1` — the ALM type-5 `+0x00` word — reaches the wire only when a session `Player` built by the map load is re-adopted by an arriving connection under a matching name. Its message is a broadcast (`004d2e69 MOV word ptr [EDX + 0x7],0x0`), unlike the catch-up sender's, which is addressed to one recipient (`004e9ffc`) | High (both paths and the allocation are named instructions in one routine; the constructor default and the single non-zero writer are separately cited) / **Medium** (that the matched path is specifically a *reconnect* rather than some other re-adoption: the name comparison is read, the surrounding session state is not) | ● active | [EXP-0181](../experiments/EXP-0181-owner-colour-rule/) |
| PAL-BLIT-024 | **(rom.exe) The world body draw supplies the selected palette object to every one of its five sprite blits, on both sides of the `drawable+0x18c & 1` branch — so the branch selects which sprite is drawn, not whether the owner shade is applied.** `FUN_0045b3f0` writes the selection into one stack slot, `[ESP+0x14]` at its own baseline, from four sites and nowhere else: the owner shade `[0x005eb65c][k]` at `0045b909` when the class `Palette` key is 0, `class+0x9c[0]` at `0045b944` when it is 1, `class+0x9c[face-1]` at `0045b987` when it is above 1, and `[0x005eb65c]+0x40` at `0045b9ae` as an override taken when `FUN_004599d0(0x30) >= 0` and `drawable+0x15a <= 2`. `0045ba01 TEST byte ptr [ESI+0x18c],0x1` / `0045ba08 JZ 0x0045bbb2` then splits the routine: the set side blits `drawable+0x194` (`0045baeb` through vtable `+0x34`, `0045bb0e` through `+0x14`) and then `drawable+0x198` (`0045bbaa`), the clear side blits an object from the global table `[0x005ef6ac]` (`0045bcd5`, `0045bd13`). All five push six arguments, and in all five the second push is a read of `[ESP+0x18]` taken after exactly one push, i.e. the same baseline `[ESP+0x14]`: `0045bad8`, `0045bafb`, `0045bb9b`, `0045bcc6`, `0045bd04`. This extends `PAL-FIGURE-014`, which cited the first of the five | High (the four stores and the five reads are named instructions at one displacement, all re-read from raw image bytes on three roots with 0 mismatches; the alternative that five different locals share a displacement is excluded by there being no other store to it in the routine's disassembled body) / **Unknown** (what `[0x005ef6ac]` holds, and therefore whether the sprite blitted on the clear side is an equipment-composed figure or a class sheet) | ● active | [EXP-0181](../experiments/EXP-0181-owner-colour-rule/) |
