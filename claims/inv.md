# Claim registry — INV — corpus & signatures (cross-format)

Level 2 ledger. Index: [registry.md](registry.md). Legend: `✔ promoted` (also in the spec) · `● active` · `✖ retracted`. IDs are permanent.

| ID | Claim | Confidence | Status | Evidence |
|----|-------|-----------|--------|----------|
| INV-CORPUS-001 | Install = 120 files (this GOG install, as inventoried by EXP-0000); authoritative list w/ size+sha256 in the manifest. *(Corrected from 121 — the original run counted a `.claude/settings.local.json` that a prior mis-rooted tooling session had written into the read-only install; not a game file. Excluded, install cleaned, inventory regenerated — all 120 hashes unchanged. See EXP-0000 "Correction".)* | High (an enumeration with hashes — a listing of what is there, with no model interposed and therefore no second model to exclude; the one error it ever carried was a *scope* error, corrected in place) | ● active | [EXP-0000](../experiments/EXP-0000-inventory/) |
| INV-SIG-002 | All 11 `*.res` + `KIDS.LM` begin `26 59 41 31` ("&YA1") — one container family | High (a direct byte observation over every such file in the install; "one container family" is now backed from the binary too — one magic, validated by `CMP` in the two readers, `RES-CODE-020`) | ● active | [EXP-0000](../experiments/EXP-0000-inventory/) |
| INV-SIG-003 | All 10 maps begin `4d 37 52 00` ("M7R␀") then constant `u32=20` | High (a direct byte observation over every map in the install, and the constant is no longer bare: `ALM-FRAME-031` shows `20` is the file header's own **length**, read and used by `FUN_00484bd0` — so this row's "constant" is a live field, not a version number) | ● active | [EXP-0000](../experiments/EXP-0000-inventory/) |
| INV-SIG-004 | Saves begin `41 73 67 26` ("Asg&"); next 2 bytes vary per save — *"meaning unknown" is stale: `SAV-PTR-003` identified them as the low half of the descriptor offset at `+0x04`, which scales with body size* | High (the magic — a direct observation over all 4 saves) / Medium (the two varying bytes, via `SAV-PTR-003`, which is itself Medium on 4 samples of one map) | ● active | [EXP-0000](../experiments/EXP-0000-inventory/) |
| INV-SCOPE-005 | `Map Editor.opt` = OLE2 compound doc; `unins000.dat` = Inno Setup log — out of scope | High (both identified by their own self-describing signatures; nothing is claimed about their contents) | ● active | [EXP-0000](../experiments/EXP-0000-inventory/) |
