# Claim registry — SPR256 (.256 sprite)

Level 2 ledger. Index: [registry.md](registry.md) · spec: [`formats/spr256/format.md`](../formats/spr256/format.md). Legend: `✔ promoted` (also in the spec) · `● active` · `✖ retracted`. IDs are permanent.

> **Confidence re-graded by the standing sweep (2026-07-25).** Re-judged against the evidence
> already cited. Most of this ledger's EXP-0002…0005 rows were *retro-fitted with a discriminator*
> by EXP-0016, which read the shipped `CSprite256` loader — so they keep High, but the row now
> says so. Two inferences were demoted and one row raised. No new bytes were read.

| ID | Claim | Confidence | Status | Evidence |
|----|-------|-----------|--------|----------|
| SPR256-CURSOR-046 | **The minimap's five mode cursors are five single-frame 16x16 `.256` payloads:** `cursors/smove.256`, `sattack.256`, `sdefend.256`, `spatrol.256`, `scast.256`. Every payload has a palette, walks whole to trailer count 1 and is byte-identical across EN/RU. `FUN_0046d3f0` registers each at hotspot `(0,0)` with the same large period argument used by static cursors. Their names are corroboration only: construction at `campaign+0xd8`, the overview draw and the physical handler establish that this is the minimap set. **G2:** pixels/geometry/count are resource bytes; registration order and hotspot are executable constants | High (five complete file walks on two roots joined to five constructor calls and the independently identified minimap consumer) | ● active | [EXP-0190](../experiments/EXP-0190-combat-controls/) |
| SPR256-STRUCT-001 | Layout `[1024B palette][frames][4B trailer]`; frame = `[u32 w][u32 h][u32 dataSize][data]` | High (the palette block and the trailer are read by the shipped loader — it gates the 1024-byte palette read on the trailer's bit 31, `SPR256-TRLR-021`, and its constructor indexes exactly `frameCount` frames, `SPR256-EXC-020`; and the `w`-before-`h` order is not a convention but a discriminated fact, since `SPR256-RLE-008`'s row/column closure would fail on non-square frames under the swap) | ● active | [EXP-0002](../experiments/EXP-0002-spr256-frames/), [EXP-0016](../experiments/EXP-0016-rom-spr256-decoder/) |
| SPR256-COUNT-002 | Frame count = records before the 4-byte trailer (walk-derived); `u32@0` is palette entry 0, not a count. **Refined by SPR256-TRLR-016**: the count is *also* stored — in the trailer's low `u16` (the earlier "not stored anywhere" is superseded; the walk remains a valid independent derivation and agrees exactly) | High (the loader computes `frameCount = trailer & 0x7FFFFFFF` — `SPR256-TRLR-021`; the walk is the corroboration, not the proof) | ● active | [EXP-0002](../experiments/EXP-0002-spr256-frames/), [EXP-0013](../experiments/EXP-0013-spr256-residual/) |
| SPR256-PAL-003 | Leading 1024 B = 256-entry palette × 4 bytes (768 fits zero files) | High (the loader reads exactly 1024 bytes when the has-palette bit is set — `SPR256-TRLR-021`; the 3-byte alternative is excluded both by that length and by 0 files fitting it) | ● active | [EXP-0002](../experiments/EXP-0002-spr256-frames/) |
| SPR256-VAR-004 | Variant (6 projectile arrows): no embedded palette, frames from offset 0 | **High — raised by this sweep** (it was Medium as a corpus observation; `SPR256-TRLR-021` since supplied the mechanism: the loader gates the palette read on trailer bit 31, which is 0 in exactly these 6 files, so "no palette, frames from 0" is what the shipped code does rather than what the bytes allow) | ● active | [EXP-0002](../experiments/EXP-0002-spr256-frames/) |
| SPR256-EXC-005 | 8 sprites: one frame + trailing non-record section (structure unknown). **Refined by SPR256-EXC-017**: the section is bracketed `0x80000001 … 0x80000001` (8/8) | High/Unknown | ● active | [EXP-0002](../experiments/EXP-0002-spr256-frames/), [EXP-0013](../experiments/EXP-0013-spr256-residual/) |
| SPR256-CORPUS-006 | Corpus maxima **over the 1376 palette-bearing `.256` of this install** (Bucket-B and the 6 no-palette arrows excluded — `SPR256-RLE-019` counts the wider corpus at 23 895 frames, and the two figures are not interchangeable): 23 791 frames; max W=640, H=480, 256 frames/sprite (falsifies the carried-over hypothesis of a 512 width cap) → RT-12 | High (a census, not a model — the maxima are what the files contain, and the width-cap hypothesis is refuted by a single counterexample) | ● active | [EXP-0002](../experiments/EXP-0002-spr256-frames/) |
| SPR256-RLE-007 | Frame `data` = RLE stream of `[2-bit op][6-bit count]` bytes: `00`=literal-n (n index bytes follow), `01`=n transparent rows, `10`=n transparent px; cursor wraps at width | High (confirmed byte-for-byte against the shipped decoder — `SPR256-RLE-022`; the corpus closure alone would have been Medium, and the fourth quadrant shows why: only the code says what `0xC0` means, `SPR256-RLE-020`) | ● active | [EXP-0003](../experiments/EXP-0003-spr256-rle/) |
| SPR256-RLE-008 | Decode is exact/lossless: every row's tokens sum to exactly `width`, run yields exactly `height` rows, stream consumed fully; `Σ lit+skip+blankRows·w ≡ Σ w·h` (100 % of 23 791 frames — the 1376 palette-bearing sprites) | High (a measurement of exact closure, and here closure *is* discriminating in one direction: it fails under a swapped `w`/`h` or a shifted opcode map. The grammar it closes over is the loader's own — `SPR256-RLE-022`) | ● active | [EXP-0003](../experiments/EXP-0003-spr256-rle/) |
| SPR256-RLE-009 | The `0b11` control quadrant (`0xC0–0xFF`) is never used in ROM1; observed counts: literal 0–63, skip ≤63, blank-rows 1–63 | High (an absence measured exhaustively, superseded in scope by `SPR256-RLE-019`'s full-corpus count and *explained* by `SPR256-RLE-020`: the quadrant is code-defined as an alias, so its absence in data is a fact about the art, not about the format) | ● active | [EXP-0003](../experiments/EXP-0003-spr256-rle/) |
| SPR256-RLE-010 | Grammar is corpus-universal: all 14 non-standard payloads' frames decode by the same rules; a Bucket-B trailing section follows a cleanly-decoded frame | High (universality is a measurement over all 14; the grammar it applies is the loader's — `SPR256-RLE-022`) | ● active | [EXP-0003](../experiments/EXP-0003-spr256-rle/) |
| SPR256-PAL-011 | Palette entry = `[B,G,R,0]` (BGR + reserved 4th byte). Order pinned by `cursors/default.256` = canonical IBM VGA palette (matches only under BGR); 4th byte = 0 in 99.96 % of 350 720 entries (= 1370 palette-bearing sprites × 256; the 138 exceptions are two files, `SPR256-PAL-018`) | High **on the corpus alone, and it earns it**: the competing model (RGB) is named and excluded by a *known-answer* test — one shipped file's palette is the canonical IBM VGA palette, which matches under BGR and does not match under RGB, so the two orders are not both consistent with the bytes. This is the shape the confidence scale allows; note it is not corroborated by any `rom.exe` read, so a loader that swapped channels on upload would not have been caught | ● active | [EXP-0004](../experiments/EXP-0004-spr256-palette/) |
| SPR256-PAL-012 | Palette index 0 = reserved transparent/background key — 0 literal occurrences in 9 541 025 literal pixels (the 1376 palette-bearing `.256`) | High (the **absence** — an exhaustive count over every literal byte in the corpus, which cannot look different if true) / **Medium** (that index 0 is *the transparent key* rather than merely an index the artists never used: the `.256` blit path reaches transparency through the RLE opcodes, not through a colour-key test, so nothing in `SPR256-RLE-020/022` tests for 0. The nearest instruction-level support is a **different consumer** — `FUN_0044c390`'s per-byte zero test on the terrain dirt overlay, `TERR-DIRT-017`). **[EXP-0140] read both sprite pixel loops whole and the Medium half does not survive them**: `FUN_0044db00` and `FUN_00451ae0` contain no compare and no branch on the source value at all, so no sprite blit reaches transparency by testing index 0 -- `SPR256-KEY-044`. The census is untouched and so is the word *reserved*; what falls is *key*) | ● active (amended) | [EXP-0004](../experiments/EXP-0004-spr256-palette/) |
| SPR256-PAL-013 | 181 sprites carry a zeroed low palette: `spritesb.256` "b"-variants are sparse overlay layers (borrow sibling palette); `cursors/attack.256`+`pickup.256` decode to dense banded non-image content (open anomaly) | Medium | ● active | [EXP-0004](../experiments/EXP-0004-spr256-palette/) |
| SPR256-OVL-014 | `spritesb.256` = overlay layer: 1 frame per base `sprites.256` frame (180/180 match), ~12× sparser (density 0.022 vs 0.266); literal indices are zero-in-own-palette but coloured-in-base (179/180 ≥99 %, median 100 %) → composites over base using base palette | High (the measurements: the 180/180 frame pairing, the density ratio, and the zero-in-own-palette/coloured-in-base test) / **Medium** (the conclusion "composites over base using base palette" — an inference from those three agreeing measurements, with **no `rom.exe` path read**: nothing has been shown to load the pair together or to blend them, and `AGENTS.md` still carries the `spritesb` **blend rule** as an open residual. Restored to High by reading whichever `CSprite256` consumer resolves the `b` sibling). **[EXP-0053] narrows that to two pixel loops and confirms the pair IS drawn together.** The `b` sibling is the second sprite object at the table entry's `+0x08`, and it is drawn by `vt+0x34` = `FUN_00428fc0` — the same six-argument lit signature as the base pass `vt+0x14`, so **the overlay is level-shaded through the same table row as the base** — whose two pixel loops `FUN_0044dde0` (plain) and `FUN_0044eca0` (mirrored) read three operands where the base pass reads one: the source index through the shading row, the **destination** pixel (`0044dea0 MOV DX,word ptr [EDI]`), and the global `[0x005eb578]` (`0044ddee`). That is the blend, unread. It is also not object-only — the unit body pass issues it at `0045bcd5` and `0045bdff` | ● active (amended) | [EXP-0005](../experiments/EXP-0005-spr256-overlay/), [EXP-0053](../experiments/EXP-0053-sprite-lighting/) |
| SPR256-OVL-015 | `cursors/attack.256`+`pickup.256` are the only zero-low-palette sprites with no base sibling; own partial palette (low 16 zero), dense, decode to non-image bands — cause open | Low | ● active | [EXP-0005](../experiments/EXP-0005-spr256-overlay/) |
| SPR256-TRLR-016 | The 4-byte trailer = `[u16 frameCount][u16 flag]` (LE). Low half **equals the walk-derived frame count** (standard 1370/1370; no-palette arrows 6/6; Bucket-B frame-terminator = 1); `k` range `[1..256]` = the frame-count domain (`k==count±1`: 0/1370). High half = `0x8000` for palette-bearing sprites (bit 31 ⇒ impossible width ⇒ the **end-of-frames sentinel** that stops a frame-walker) / `0x0000` for the no-palette arrows (plain `u32` count). So the frame count **is** stored (refines COUNT-002) and the terminator is statically characterised. **Refined by SPR256-TRLR-021**: bit 31 is specifically the loader's *has-palette* flag; the count field is 31-bit | High (count) / High (high word = has-palette flag) | ● active | [EXP-0013](../experiments/EXP-0013-spr256-residual/), [EXP-0016](../experiments/EXP-0016-rom-spr256-decoder/) |
| SPR256-EXC-017 | The 8 Bucket-B files = a standard 1-frame sprite (frame + count-trailer `0x80000001`) followed by an **appended secondary section bracketed `0x80000001 … 0x80000001`** (8/8). The inner (block − 8 marker bytes) is opaque: **not** a frame (first `u32` has bit 31), **not** a same-**width** RLE image (0/8 close at the frame width; a different width is not excluded), and the large inners use bytes across the full `0x00-0xFF` range. Two size regimes — 4–16 B (small UI/projectile) vs 303–2521 B dense (world-map tiles + shop frame). A consistent sub-format, not junk. **Resolved by SPR256-EXC-020**: the shipped loader loads it but never indexes/decodes it — runtime-inert (authoring origin still open, but non-runtime). Refines EXC-005 | High (framing) / Resolved (runtime role) | ● active | [EXP-0013](../experiments/EXP-0013-spr256-residual/), [EXP-0016](../experiments/EXP-0016-rom-spr256-decoder/) |
| SPR256-PAL-018 | The palette entry's reserved 4th byte is non-zero **only** in `cursors/pickup.256` (95) + `cursors/attack.256` (43) = 138/350 720 standard entries; all other 1376 palette-bearing `.256` have an all-zero reserved column. Values scattered `1..255`, no constant, 106 distinct indices → these two files' leading 1024 B is not a standard `[B,G,R,0]` palette. Ties the 4th-byte read to the OVL-015 anomaly | High (measured) / Unknown (meaning) | ● active | [EXP-0013](../experiments/EXP-0013-spr256-residual/) |
| SPR256-RLE-019 | The `0xC0-0xFF` (`0b11`) control quadrant occurs **0** times across the full `.256` corpus — 23 895 frames incl. Bucket-B + no-palette, 3 669 184 control bytes (literal 1 341 762 / blank-row 106 664 / transparent 2 220 758). ~~Whether the loader aliases `0xC0` to `0x80` or gives it a 4th meaning is unobservable statically — needs `rom.exe`.~~ **Resolved by SPR256-RLE-020**: the loader aliases `0xC0` to `0x80` (shared `else`; no 4th opcode). The absence measurement stands. Extends RLE-009 | High (an exhaustive count over every control byte in the corpus, with the sub-totals printed so the total is checkable — and the question the count could **not** settle was handed to `rom.exe` rather than guessed, which is why the row survives the sweep intact) | ● active | [EXP-0013](../experiments/EXP-0013-spr256-residual/), [EXP-0016](../experiments/EXP-0016-rom-spr256-decoder/) |
| SPR256-RLE-020 | **(rom.exe)** The SPR256 RLE decoder — a family of ~42 DirectDraw blit routines (`0x0044c660`‥`0x00452xxx`; 14 carry the decode), verified in full in `FUN_0x0044dd40` (stencil) and `FUN_0x0044dde0` (colour) — dispatches on `control & 0xC0` via `op = (CONCAT11(b,b) & 0xc03f) >> 8` and a three-way `if(0x00)/else if(0x40)/else`. The `else` handles **both `0x80` and `0xC0`** as a transparent skip of `control & 0x3F` px. `0xC0` is an **alias of `0x80`, not a fourth opcode** — data-unused (RLE-019) but code-defined. Resolves the `rom.exe` flag on RLE-019 | High (a three-way branch read in two routines; the fourth quadrant's meaning is **only** decidable this way, since it never occurs in the data — the clearest case in the repo of code answering what no corpus could). **Re-checked by [EXP-0040]** on a repaired function table (the vtable blind spot closed, 607 functions created): the repair adds 3 functions inside `0044c660..00453000`, all of them 3–4 instruction MSVC SEH unwind funclets carrying no RLE opcode constant — no blit routine was hidden — **confirmed**. Note for family censuses: a raw function count over that range (66) now includes funclets and is not the same measurement as "~42 blit routines" | ● active | [EXP-0016](../experiments/EXP-0016-rom-spr256-decoder/) |
| SPR256-RLE-022 | **(rom.exe)** The loader confirms the EXP-0003 opcode assignment byte-for-byte: `0x00` = opaque run of `n` source palette-indices, `0x40` = skip `n` rows, `0x80` = transparent skip of `n` px (`n = control & 0x3F`); a row ends when the column counter reaches the frame width, and `0x40` ends the frame if `n` exceeds the rows remaining. Corroborates RLE-007…010 from the shipped decoder | High (the opcode assignment read off the decoder, including the row-end and frame-end conditions — this row is what upgrades `SPR256-RLE-007` from a grammar that fits to the grammar the game runs) | ● active | [EXP-0016](../experiments/EXP-0016-rom-spr256-decoder/) |
| SPR256-EXC-020 | **(rom.exe)** The `CSprite256` constructor (`FUN_0x00428c50`) indexes **exactly `frameCount`** frames; for the 8 Bucket-B files `frameCount = 1` (from the final `0x80000001` trailer), so the appended `0x80000001 … 0x80000001` inner block is read into the load buffer **but never indexed, decoded, or blitted** by any `CSprite256` method — **runtime-inert**. Resolves the `rom.exe` flag on EXC-017 (what the loader does with the inner = nothing) | High (the loop bound is the trailer count and the class's methods were enumerated — "never indexed" is an absence over the whole `CSprite256` surface, not an observation that it was not seen). **Re-checked by [EXP-0040]** on a repaired function table (the vtable blind spot closed, 607 functions created): the `CSprite256` vtable is `0x597418` (installed at `00428c89`), 44 slots / 35 distinct method targets, and **every one already had a function** — the surface enumeration was never blind — **confirmed** | ● active | [EXP-0016](../experiments/EXP-0016-rom-spr256-decoder/) |
| SPR256-TRLR-021 | **(rom.exe)** The 4-byte trailer is `[31-bit frameCount][bit31 = has-palette]`: the loader computes `frameCount = trailer & 0x7FFFFFFF` and gates the 1024-byte palette read on `trailer & 0x80000000`. Matches EXP-0013's split with no exception (1370 palette sprites bit31=1; 6 no-palette arrows bit31=0). Refines TRLR-016 (the high half is a **1-bit palette flag**, not a `0x8000` word; the count field is 31-bit) and explains the no-palette variant (SPR256-VAR-004) as the same format with the flag off | High (mask and gate both quoted; and it earns extra credit by *explaining* a previously separate observation instead of merely agreeing with it — the 1370/6 split falls out of the flag rather than being fitted to it) | ● active | [EXP-0013](../experiments/EXP-0013-spr256-residual/), [EXP-0016](../experiments/EXP-0016-rom-spr256-decoder/) |
| SPR256-FRAME-023 | **Frames inside one `.256` are not all the same size, so a consumer must not cache one frame's bounds for the sheet.** Sweeping every `.256` node in the install (1384 parsed, 8 unparsable): **1253 carry a single `(w,h)` across all frames and 131 do not** — up to 5 distinct sizes in one sheet. Two of them are object sheets whose per-frame differences are load-bearing: `objects/stones/sprites.256` (9 frames, `64×80` and `64×64`) and `objects/pointer/sprites.256` (6 frames, `16×32` and `24×24`), because nine and six `objects.reg` classes respectively select different frames of them by `Index` (`TERR-SPR-042`). This is what makes the engine's own two anchor computations disagree — the body pass measures the drawn frame, the shadow pass measures frame 0 (`TERR-SPR-043`) — and it is why `TERR-SPR-040`'s "a frame of a different size is centred inside the `Width × Height` canvas first" is a live case rather than a hypothetical | High (a measurement over every `.256` in the install with no model interposed — the frame record's `[w][h][size]` header is `SPR256-STRUCT-001`, and the count is `SPR256-TRLR-021`'s trailer, both already pinned) | ● active | [EXP-0038](../experiments/EXP-0038-object-frame/) |
| SPR256-UNIT-024 | **A unit's `.256` is a block-addressed sheet, and the block bases are arithmetic on the class's own phase counts — not stored anywhere.** `FUN_0045bf00` (CUnit `vt+0x2c`, `TERR-SPR-048`) selects a frame with two hard-coded constant pairs chosen by the class's `Flip`: **`S = 16, D = 8` when `Flip == 0`, `S = 9, D = 5` otherwise** (`REG-UNITS-051`), where `S` is the standing block's length and `D` the number of directions the sheet **stores**. Writing `MB, MV, AT, DY, BN, ID` for `MoveBeginPhases, MovePhases, AttackPhases, DyingPhases, BonePhases, IdlePhases`, the blocks tile the sheet in this order and at these absolute bases: **`0` stand, `S` frames** (index = the facing itself); **`S` move, `D×(MB+MV)` frames** — move-begin occupies the first `MB` of each direction's slot and the move loop the remaining `MV`, since the arm adds `MoveBeginPhases` (`0045c1ae`/`0045c1cb`); **`S + D·(MB+MV)` attack, `D×AT`**; **`S + D·(MB+MV+AT)` dying, `D×DY`**; **`S + D·(MB+MV+AT+DY)` bone, `D×BN`** and **idle, `D×ID`, at the *same* base** — the two never collide because no shipped class carries both (0 of 34). Within a block the index is `dir × <that block's phase count> + k`, `k` being `moveTL[phase mod len]`, `attackTL[phase]`, `idleTL[phase]`, `phase/2` (dying) or `stage−2` (bone). The dying and bone blocks are read out of **`classes[Dying]`'s** sheet with *its* phase counts and *its* `Flip` (`REG-UNITS-050`). Corpus: the predicted total `S + D·(MB+MV+AT+DY+max(BN,ID))` equals the sheet's own frame count on **33 of 34** classes exactly, with **0** sheets longer than predicted; the 34th (`Unit33`, `ID 2`) is a registry defect, not a layout exception — it spells `File = 21` (a `Flip = 1`, 129-frame sheet) but inherits `Flip = 0` from its `Parent`, so it addresses that sheet as a 16/8 layout and reaches index 207. Also measured here: **0 of the 34 unit sheets have uniformly-sized frames**, so `SPR256-FRAME-023`'s warning is universal on this path, and the anchor must be built from the drawn frame (`TERR-SPR-043`) | High (every base, stride and immediate is a named instruction in a function read end to end — `evidence/rom-unit-frame-excerpt.md` §3 — and the tiling **discriminates**: every rival `S`, `D` or block order tried misses on all 34 classes, where the published one misses on the one class whose own data is inconsistent) / Medium (the 33/34 figure itself — a corpus measurement over one registry) | ● active | [EXP-0039](../experiments/EXP-0039-unit-frame/) |
| SPR256-STR-040 | **(rom.exe)** A `structures.reg` sheet is neither the unit sheet's `*Phases` blocks (`SPR256-UNIT-024`) nor an object's `Index`-addressed list (`TERR-SPR-042`): it is a **row-major grid of tile-sized frames, `TileWidth` columns × `FullHeight` rows**, and the class opens **two** of them — `graphics\structures\<File>.256` and `<File>b.256`, built as `CSprite256` objects at class `+0x04` / `+0x08` by `FUN_0046e720` (`0046e761`, `0046e7d4`) and drawn by the same `vt+0x18` / `vt+0x34` pair with the *same* frame index, the second gated on `[0x005eb520]` (`0045a203`) — the `spritesb` overlay class `SPR256-OVL-014` describes, here per structure. Frame `(k, c)` is index `k·TileWidth + c` and is blitted at the top-left of one map cell (`TERR-STRUCT-100`). Corpus: **130 of 130** structure sheets in `graphics.res` carry frames that are uniformly **32×32** — the one sheet family in this install for which `SPR256-FRAME-023`'s warning does not bite, and the reason no anchor pixel is needed | High (the grid stride `k·TileWidth + c` is `0045a163`'s `IMUL` + `ADD` read at instruction level, the two sheet names are two `PUSH`es of `".256"`/`"b.256"` in one routine, and 130/130 uniform 32×32 is a census with no model interposed. A failure would have looked like: any structure sheet with a non-32×32 frame, or a stride other than `TileWidth`) | ● active | [EXP-0092](../experiments/EXP-0092-structure-art/) |
| SPR256-STR-041 | **(rom.exe)** The sheet holds **three blocks**, and the draw picks between them with two tests (`FUN_0045a0a0`, `0045a172`…`0045a1da`): **base** `[0, TW·FH)` — frame = the cell index; **animation** `[TW·FH, TW·FH + maxAnimFrame·live)` — frame = `TW·FH + (phaseFrame−1)·live + rank(cell)`, where `phaseFrame` is the class timeline's current value, `live` = `AnimMask`'s non-`'-'` count and `rank` counts live cells before this one, a `'-'` cell falling back to its base frame; **ruin** `[frames − TW·FH, frames)` — frame = `frames − TW·FH + cellIndex`, taken when `health <= 0` **and** `Indestructible == 0`. So the ruin block is addressed **from the end of the file**, not from a stored base. Corpus, re-executing the loader's own `AnimTime`/`AnimFrame` expansion: `frames == k·TW·FH + maxAnimFrame·live` with `k ∈ {1,2}` on **64 of 66** classes (`k = 2` on 39, `k = 1` on 25); the two exceptions are `bridge1v` (9 frames) and `bridge2` (14), whose drawable is a C++ subclass with its own selector (`TERR-STRUCT-105`) and whose `1×1` grid is meaningless. Where `k = 1` no ruin art ships and `frames − TW·FH` is 0, so a destroyed such structure re-draws its intact art rather than reading out of range. `Indestructible` gates the *draw*, not the sheet: 4 of the 39 `k = 2` classes are `Indestructible` and ship an unreachable ruin grid | High (all three bases and both gates are named instructions; the identity re-executes over the shipped registry and sheets at 64/66 with the two misses predicted in advance by the reading, and the `-mode art` montages show the base grid over the ruin grid as one coherent picture per class. A failure would have looked like: a residual on a class the reading does *not* exempt, or a montage whose two halves are not the same building intact and wrecked) | ● active | [EXP-0092](../experiments/EXP-0092-structure-art/) |
| SPR256-EQUIP-042 | **(corpus)** The `graphics\equipment` tree is a **portrait** tree, not a world-sprite tree: four figure directories (`ffighter`, `fmage`, `mfighter`, `mmage`) matching `rom.exe`'s four literals exactly, two layer directories under each (`primary`, `secondary`) matching its two, and **987 file nodes** on both roots with identical path sets. Of those, 928 are the layer sheets — 784 `primary` + 144 `secondary` — and the rest are face sheets named `1.256`..`31.256` directly under a figure directory (31 for `mfighter`, 13 for `mmage`, 10 for `ffighter`, 5 for `fmage`). Walked by the verified `.256` reader, **every one of the 924 that parses whole holds exactly one frame, and every frame 0 is 160x240**; four `primary` sheets yield 0 frames. Leaf names are exactly seven digits and each is produced by exactly one `u16` through `HERO-APPEAR-049`'s formatter (928 of 928, 0 unproduced, 0 ambiguous, over the whole 65 536-value domain). Contrast `SPR256-UNIT-024`'s unit sheets, whose sixteen hero bodies carry 129 to 216 frames of 24x40 to 40x48 | High (exact archive measurements and an exhaustive frame walk with the verified reader, reproduced independently on both roots, plus a total inversion rather than a sample) / **Unknown** what the four zero-frame `primary` sheets contain | ● active | [EXP-0115](../experiments/EXP-0115-held-item/) |
| SPR256-PICT-043 | **(corpus)** The `graphics\infowindow` tree is where a non-composed actor's picture lives, and it is **not** `.256` at all: **86 file nodes on the EN root and 81 on the RU**, every one a Windows BMP with `planes=1`, `bpp=24`, `compression=0`, `dataOff=54`, `nClrs=0` and **`w=160`, `h=240`** — 86 of 86 and 81 of 81, walked rather than sampled. That is the same 160x240 as `SPR256-EQUIP-042`'s equipment face and layer sheets, so the two ways of getting a picture out of this engine land on one geometry; unlike them it carries no palette and no run stream. Payload sizes: **85 nodes are 115256 bytes with the BMP header's own `imgSize` field 0**, which is 2 bytes past `54 + 160*240*3 = 115254`, and one node (`mage.bmp`) is exactly 115254 with `imgSize=115200`. The two roots' 81 shared paths agree exactly in size; the five EN-only nodes are `magic.bmp root.bmp ruins.bmp sphinx.bmp teleport.bmp` and there are **no** RU-only nodes | High (exact archive measurements and a header walk over every node on both roots) / **Unknown** what the two extra bytes on 85 of 86 nodes are | ● active | [EXP-0138](../experiments/EXP-0138-actor-pictures/) |
| SPR256-KEY-044 | **(rom.exe) Neither sprite blitter tests the source value: there is no colour key in the `.256` or `.16a` draw, and index 0 is written like any other index.** `SPR256-PAL-012` measures, exhaustively and correctly, that index 0 never occurs as a literal in the 1 376 palette-bearing `.256`; its own confidence cell grades the *interpretation* -- that 0 is "the transparent key" -- only Medium, and names the risk. The interpretation is now read out rather than inferred. `FUN_0044db00`, the plain `.256` pixel loop, decodes the control byte as `AL; AH = AL; EAX &= 0xc03f`, dispatches on `AH` being 0 / 0x40 / else, and on the literal arm does `0044dbf8 MOV AX,word ptr [EBX + EAX*0x2]` followed by an **unconditional** `MOV dword ptr [EDI + -0x4],EDX` -- no `CMP`, no `TEST`, no branch on the byte anywhere between the fetch and the store. `FUN_00451ae0`, the `.16a` loop, is the same shape: the only tests in it are `TEST AX,0x4000` and `TEST AX,0x8000` on the *control* word. So transparency in both formats is entirely the RLE's *skip* and *blank-rows* opcodes, and for `.16a` additionally the per-pixel alpha of `SPR16A-ALPHA-025` -- which is a continuum, so a binary mask cannot express a `.16a` sheet at all. What survives of `SPR256-PAL-012` unchanged: the census, and that index 0 is *reserved* in the sense that no artist emitted it. What does not: that any sprite blit reaches transparency by testing it. The nearest instruction-level support the older row could point at, `FUN_0044c390`'s per-byte zero test, is a **different consumer** (the terrain dirt overlay, `TERR-DIRT-017`) and remains that. **G2:** a consumer must not implement a colour-key test for these two formats -- doing so changes nothing on shipped art, because index 0 never occurs, and diverges immediately on any art a customisation adds | High (two pixel loops read instruction by instruction over their whole bodies; the claim is an absence inside a bounded routine, which is the one shape an absence can be read directly rather than swept for) | ● active | [EXP-0140](../experiments/EXP-0140-cast-art-drawn/) |
| SPR256-DOLL-045 | **The `.256` equipment tree and the `.16a` icon tree are ONE name space, and the doll set is exactly the icon set minus the magic items.** `SPR256-EQUIP-042` measured 928 layer sheets under `equipment/<figure>/<layer>/` and `ITEM-APPEAR-024` measured 416 `inventory/` icons, and left the relation between the two sets unmeasured. Taken as sets of leaf names on both roots: the 928 sheets carry **367 distinct** names — a figure/layer directory repeats a name rather than owning it — and **367 of 367 have an icon**, **0 doll names lack one**, while **49 of the 416** icons lack a doll sheet. Those 49 are exactly the class-14 rows, the `MagicItems` collection, which are consumables and quest items and are never worn: `367 = 416 - 49`. So a consumer needs one addressing scheme, not two, and the presence of a doll sheet is decided entirely by whether the item's kind nibble is 14. The 59 non-layer `.256` nodes `SPR256-EQUIP-042` counts separately (`1.256`..`31.256` directly under a figure directory) are face sheets and are not in this name space at all | High (a set comparison over two complete subtrees on both roots, with the leaf names inverted through `FUN_00483c80` rather than pattern-matched) | ● active | **[EXP-0144](../experiments/EXP-0144-item-pictures/)** |
