# Claim registry — SPR16A (.16a / .16 sprite)

Level 2 ledger. Index: [registry.md](registry.md) · spec: [`formats/spr16a/format.md`](../formats/spr16a/format.md). Legend: `✔ promoted` (also in the spec) · `● active` · `✖ retracted`. IDs are permanent.

> **Confidence re-graded by the standing sweep (2026-07-25).** This ledger already paid for the
> confidence rule — four of the repo's overturns are its retracted rows. What survived is
> EXP-0015's instruction-level work, which also retro-fits a discriminator to `SPR16A-RLE-002`.
> One demotion, and it is a *repeat*: `SPR16A-STRUCT-001` still says the frame count is "not
> stored", which is what `.256`'s ledger said before EXP-0013 found it in the 4-byte trailer.
>
> **2026-07-28 — the flag was right (EXP-0042).** The count *is* stored, in the same place
> `.256` stores it: `SPR16A-TRLR-012`, 545/545. The "not stored" clause is withdrawn from
> `SPR16A-STRUCT-001` and recorded in [`retracted.md`](retracted.md). EXP-0042 also closed the
> `.16` font glyph grammar and found that **five** font atlases ship, not three. It had no
> `rom.exe` access (a parallel experiment held the Ghidra project), so its interpretive clauses
> are Medium by the confidence rule and each names the reader-side check that would lift it.
>
> **2026-07-29 (later the same day) — the install moved under the ledger, and the high half is
> read (EXP-0050).** A GOG update replaced the EN data with the **Russian release** at 16:41 and
> the owner reinstalled EN at 16:56 (the reinstalled `graphics.res` re-hashes exactly to the
> `3ec2f34c…` pin; the RU state is preserved at `gameversions\ru`). Every corpus figure in this
> ledger recorded before that date describes the EN archive — which for the SPR16A domain turns
> out to matter for **one node only**: `font2.16` (`SPR16A-FONT-022`). The headline: the atlases'
> reachable records 96..223 carry **the whole Russian alphabet in a hybrid arrangement**
> (А..Я `0xB0..0xCF`, а..п `0xD0..0xDF`, р..я `0xF0..0xFF`, beside CP437's accented Latin at
> CP437's own positions) — in the **English** build's bytes too (`SPR16A-FONT-020`) — and the
> dead tails are older in-place-overwrite layers of the same font (`SPR16A-FONT-021`).
>
> **2026-07-29 — the reader was read (EXP-0047), and every check EXP-0042 named came back.**
> Four of those Mediums are lifted and one Unknown is answered against the analogy that would
> have been assumed. The headline is structural: **there are two loaders, not one.** `.16a` is
> parsed by the *same constructor as `.256`* (`FUN_00428c50`) — so `SPR256-TRLR-021`'s `AND
> 0x7fffffff` and its bit-31 palette gate are, instruction for instruction, the `.16a`'s — while
> `.16` has its own (`FUN_004284e0`), which takes the trailing `u32` **raw** and can never read a
> palette. That is what the 542/3 corpus partition could not cut and the binary does.

| ID | Claim | Confidence | Status | Evidence |
|----|-------|-----------|--------|----------|
| SPR16A-CURSOR-046 | **The cursor registry names 23 `.16a` sheets and fixes each hotspot and period argument in `.text`.** The complete join is `cursor-construction.tsv`; every sheet walks to its stored trailer count and has uniform frame geometry. Command-relevant counts are default 1, move 5, swarm 5, attack 10, defend 8, select 1, patrol 8, cast 14 and pickup 15; the eight edge arrows and small-default are one frame each; cantput/town/backpack are one, dice 15 and wait 10. Dimensions are 32x32 except swarm 44x44, small-default 16x16 and cantput 64x64. All 23 payload hashes agree across EN/RU. **G2:** count, pixels and geometry are file bytes; hotspot and period arguments are executable constants. A larger sheet is structurally accepted by the existing trailer field, but no code evidence establishes whether the cursor animator visits frames beyond its registered count | High for registry paths/hotspots/arguments and file walks (constructor read whole; both resources measured); Medium for the implied animation use, which is intentionally not claimed beyond the passed argument | ● active | [EXP-0190](../experiments/EXP-0190-combat-controls/) |
| SPR16A-STRUCT-001 | `.16a` layout `[1024B block][frames][4B trailer]`; frame = `[u32 w][u32 h][u32 dataSize][data]` — the SPR256 *shape*. 542/542 walk clean at start 1024; residual tail is exactly 4. Corpus (graphics.res): 542 `.16a` + 3 `.16`, 2727 `.16a` frames. **Amended by EXP-0042:** the "frame count walk-derived (**not stored**)" clause is **withdrawn** — the count is in the trailer (`SPR16A-TRLR-012`), and the walk is the corroboration rather than the only derivation; the two agree exactly on all 545. The corpus figures are re-measured and reproduce (`wmax 552 hmax 128 framesMax 224`, now stated with their distributions in `SPR16A-BOUND-016`) | High (the layout — the walk closes 542/542 at start 1024 with the residual exactly 4, and the leading block's role is settled from `rom.exe` by `SPR16A-PIX-011`) | ● active (amended) | [EXP-0008](../experiments/EXP-0008-spr16a/), [EXP-0042](../experiments/EXP-0042-spr16a-residuals/) |
| SPR16A-RLE-002 | `data` is a run-length stream of u16 LE control words `[2-bit op][14-bit count]`: `00`=literal-n (next n u16 words are 16-bit pixels), `01`=n blank rows, `10`=n transparent-skip px; cursor wraps at width. The 16-bit word-based analogue of the SPR256 byte grammar. **Amended by EXP-0047: the `0b11` Unknown is answered, and the `.256` analogy would have got it backwards** | High (**not** by analogy with `.256`, which would be Medium: EXP-0015's H2 anchored the decoder in `rom.exe` and confirmed it consumes u16 controls as `op = cw>>14`, `count = cw & 0x3fff`, advancing literals two bytes, with three opcode classes — `SPR16A-PIX-011`) / **High** (the fourth quadrant `0b11` decodes as **blank rows**, not as a skip. The dispatch is two sequential bit tests, not a switch, and bit **14** is tested first: `TEST AX,0x4000 / JNZ` at `00451b6e` reaches the blank-rows arm, and only a word with bit 14 clear reaches `TEST AX,0x8000 / JNZ` at `00451b78`. A word with both bits set therefore takes the first branch. The same two tests in the same order at all four dispatch sites of the two u16 blitters `FUN_00451ae0`/`FUN_00451e50` (`00451b6e/78`, `00451cc6/d0`, `00451ee7/ef1`, `0045204c/056`). So `.16a` and `.256` alias their unused quadrant to **different** opcodes — `SPR256-RLE-020`'s shared-`else`-to-skip reading is right for `.256` and for the `.16` byte blitter (`SPR16A-FONT-019`) and wrong here. Data-unused either way: 0 occurrences corpus-wide, `SPR16A-RLE-003`) | ● active | [EXP-0008](../experiments/EXP-0008-spr16a/), [EXP-0047](../experiments/EXP-0047-spr16-reader/) |
| SPR16A-RLE-003 | Decode is exact/lossless: every row's tokens sum to width, run yields exactly height rows, stream consumed fully; `Σ lit+skip+blankRows·w ≡ Σ w·h` (2727/2727 = 100%; closure 34 639 040 == 34 639 040). The `0b11` opcode quadrant (0xC000+) is never used; count maxima literal ≤137, skip ≤551, blank ≤128 | High (an exhaustive closure measurement over the whole `.16a` corpus — 2727 frames, both sides of the identity printed — against a grammar `rom.exe` supplies rather than one this measurement had to infer) | ● active | [EXP-0008](../experiments/EXP-0008-spr16a/) |
| SPR16A-PIX-004 | ~~Literal pixels are 16-bit **direct colour** (12-bit RGB444 in bits 1..12), *not* palette-related.~~ **✖ RETRACTED.** The byte observation stands (`OR=0x1FFE`; bits 13–15 and 0 zero), but EXP-0015 shows the word is a runtime-LUT offset, not direct RGB. | — | ✖ retracted | [EXP-0008](../experiments/EXP-0008-spr16a/) |
| SPR16A-PIX-005 | ~~Channel assignment for the direct-colour pixels — first "green = high nibble", then corrected to R-B-G (R = bits 12–9, B = 8–5, G = 4–1).~~ **✖ RETRACTED** — *both* direct-colour readings were wrong. "green = high nibble" rendered every sprite green; the mean-nibble "R-B-G" fix rendered fire/freeze/gold plausibly but portraits came out pink and freeze blue-purple (not cyan). The direct-colour channel model is refuted; EXP-0015 traces a runtime palette/blend LUT instead. Kept to record the evolution. | — | ✖ retracted | [EXP-0008](../experiments/EXP-0008-spr16a/) |
| SPR16A-PAL-006 | ~~The 1024-byte leading block is `[B,G,R,0]`-palette-shaped but is **not indexed** by the pixels; role open.~~ **✖ RETRACTED / corrected by `SPR16A-PAL-008`** — the block *is* the palette and the pixels *do* index it. | — | ✖ retracted | [EXP-0008](../experiments/EXP-0008-spr16a/) |
| SPR16A-PAL-008 | The leading 1024-byte block is a genuine **per-file 256-entry `[B,G,R,0]` palette**: **497 distinct blocks** across the 542 `.16a` (each sprite has its own), and they are meaningful gradient ramps matching the sprite — `fireball` = black→dark-red→bright-orange, `freeze` = black→cyan (R low, G≈B rising), `money` = gold/tan, `inn/unit1` = brown/skin (≈255 nonzero entries each) | High (that the block is the palette **and is indexed** — `SPR16A-PIX-011` traces `rom.exe` building a mode-4 LUT from it and the blitter indexing that LUT; the 497-distinct-blocks count is a measurement) / High (the `[B,G,R,0]` channel order — this looked at first like the sweep's clearest demotion, since "the ramps look right for fire/freeze/gold" is the very evidence that endorsed the **retracted** `SPR16A-PIX-005`. It survives because the repo has better evidence elsewhere and had not connected it: the LUT is built by the shared builder `FUN_00427df0`, whose palette source order `TERR-LIGHT-019` read off the instructions as the RGBQUAD `[B,G,R,x]`. Colour plausibility is corroboration here, not the grade) | ● active | [EXP-0008](../experiments/EXP-0008-spr16a/) |
| SPR16A-PIX-009 | ~~The high byte alone is the final palette index; the low byte is only anti-alias/sub-pixel coverage; flat `colour=pal[index]` plus CRT gamma reproduces the correct colours.~~ **✖ RETRACTED** — owner visual comparison failed; EXP-0015 traces a different 4+8 runtime-LUT operation. | — | ✖ retracted | [EXP-0008](../experiments/EXP-0008-spr16a/) |
| SPR16A-PIX-010 | ~~Words split as a 5-bit palette-related `base=(word>>8)&31` plus `aux7=(word>>1)&127`.~~ **✖ RETRACTED** — all H8/H9 render variants failed owner comparison, and instruction tracing in EXP-0015 proves the executable uses the word as an even byte offset into a `[16][256]u16` LUT, forcing a 4+8 split. | — | ✖ retracted | [EXP-0008](../experiments/EXP-0008-spr16a/) |
| SPR16A-PIX-011 | **PROMOTED — the resolved `.16a` pixel model** (supersedes retracted PIX-004/005/009/010). In the owned `rom.exe`, `.16a` construction calls LUT mode 4 with 16 levels; the verified u16-RLE blitter uses each literal word as an even byte offset into the generated `[16][256]u16` source LUT. This forces `paletteIndex=(word>>1)&255` (bits 1–8), `level=(word>>9)&15` (bits 9–12), and source multiplier `level+1`; a level-selected lookup of the old framebuffer pixel is added before the u16 write. Instruction data flow closes across `0x00457b15 → 0x00428ad0 → 0x00427df0` and `0x0042b970 → 0x00451ae0`; 2727/2727 frames and all 2,535,421 literals pass, all 256 indices occur, altered masks/stride fail, and the executable-derived render **matches the game on the anchor sprites** (fire warm, freeze cyan, portraits legible/skin, mage purple, coins gold). Residual for `rom.exe`: exact per-display-mode framebuffer packing/quantization. | High (data flow closed across named addresses, every mask and stride an immediate, altered masks/stride falsified against the corpus, and the render checked against the running game on anchor sprites — the combination the scale asks for, and the one that overturned four earlier readings of these same bytes) | ● active | [EXP-0015](../experiments/EXP-0015-spr16a-blit/) |
| SPR16A-FONT-007 | The 3 `.16` files are font glyph atlases using a byte-based control variant (odd dataSizes): `font3` = 64 glyphs (8×6); `font1`/`font2` = 224 glyph records (chars 32–255) + a large trailing pool (17 720 / 3 103 B). Distinct from `.16a`. **Amended by EXP-0042** on both open clauses: the pixel grammar is no longer deferred (`SPR16A-FONT-013`), the "trailing pool" is a chain of further record sections (`SPR16A-FONT-014`), and "the 3 `.16` files are font glyph atlases" is **incomplete** — five atlases ship, two of them as `.16a` (`SPR16A-FONT-015`). The record counts and cell sizes reproduce exactly | Medium (structure) | ● active (amended) | [EXP-0008](../experiments/EXP-0008-spr16a/), [EXP-0042](../experiments/EXP-0042-spr16a-residuals/) |
| SPR16A-TRLR-012 | **The frame count IS stored, and the two extensions read the same 4 bytes differently. Amended by EXP-0047 — all three deferred clauses lifted.** The trailing `u32`'s low 31 bits equal the independently walk-derived record count in **545/545** payloads (±1 control 0/545; 21 distinct counts over 1..224); bit 31 is set on exactly the 542 payloads carrying a 1024-byte leading block. No other quantity comes close: first `u32` 0/545, `u32` at end−8 0/545, size mod 256 2/545. **What the loaders do** (`evidence/disasm-loader.txt`): a **`.16a`** is constructed through `FUN_0042b920 → FUN_00428c50` — *the `.256` constructor itself* — which seeks to EOF−4, reads **4 bytes as one dword**, and computes `count = trailer & 0x7fffffff` (`00428d4e`) and `flag = trailer & 0x80000000` (`00428d54`), then `TEST EDI,EDI / JZ` at `00428d71..76` gates a `Read(palette, 0x400)`. A **`.16`** goes through `FUN_00428b90 → FUN_004284e0`, which reads the same 4 bytes and uses them **unmasked** as the count (`00428616`, no `AND` anywhere in the function) and stores `palette = NULL` unconditionally (`004285ec`) — it has no palette read to gate. So the trailer is `[31-bit count][bit31 = has-palette]` **for `.16a`** and a plain `u32` count **for `.16`**, and bit 31 clear on all three shipped `.16` is *required* by the code (set, it would ask for ~2.1 billion records) rather than a coincidence of the partition. Supersedes `SPR16A-STRUCT-001`'s "not stored" | **High** (the count identity — the walk and the trailer are two independent readings agreeing on every payload) / **High** (the `.16a` field split and the palette gate — the mask and the gate are both quoted immediates in the constructor, and the `[u16 count][u16 flag]` rival is refuted by the single 32-bit read: there is no `MOVZX …,word` of the trailer in either loader) / **High** (that this is the format's count field: `FUN_0042b8f0`-equivalent walk at `00428dc4..dd8` indexes exactly `count` records) / **High** (the `.16` raw-`u32` reading — a second loader, quoted, with the mask absent) | ● active (amended) | [EXP-0042](../experiments/EXP-0042-spr16a-residuals/), [EXP-0047](../experiments/EXP-0047-spr16-reader/) |
| SPR16A-FONT-013 | **The `.16` glyph pixel grammar. Amended by EXP-0047: the blitter is read, so the grammar is High, and the 4-bit value's Unknown is answered — against the hypothesis the row named.** Byte control `[2-bit op][6-bit count]`: `00` = literal *n* **bytes**, each carrying **two 4-bit pixels, low nibble = the left-hand pixel**, with a trailing high nibble of `0` as the **odd-run pad**; `01` = *n* blank rows; `10` = *n* skipped pixels. Exact: **687/687** glyph records over all sections of all three `.16` close with the stream consumed, the last row flush, exactly `h` rows and **zero slack**. Eight competing models fail: 1 px/byte 122/687, hi-nibble-first 286/687, no-odd-tail-with-clamp 642/687. Pixel values observed: `{4,5,6,7,8,9,11,13,15}`. The blitter is **`FUN_0044d890`**, reached only as `vt+0x34` of the `.16` sprite class: `AND EAX,0xc03f` / `OR AH,AH / JZ` (literal) / `CMP AH,0x40 / JNZ` (skip) at `0044d90c..18`; the literal arm loads the low nibble first (`AND EAX,0xf`, `0044d947`), then `SHR EAX,0x4` (`0044d958`), and a **zero high nibble ends the run** (`JZ 0044d96e`) and decrements the column count by one (`0044d972`) — the pad, exactly | **High** (the grammar — every opcode, count mask, nibble order and the pad decrement is a quoted instruction, and the corpus closure above is the corroboration rather than the evidence) / **High** (the `0xC0-0xFF` quadrant occurs **0** times in 14 845 control bytes; what the code does with it is now also known — see `SPR16A-FONT-019`) / **High** (the pad census: over 24 689 literal bytes a zero nibble occurs **only** as the high nibble of a run's final byte — 2 896 times, 0 mid-run, 0 in any low nibble) / **High, and the hypothesis was wrong** (what the 4-bit value means. It is **not** a level into the mode-4 LUT `SPR16A-PIX-011` builds — a `.16` object never calls that builder and its palette pointer is NULL — and **not** a palette index. It is an index into a **16-entry `u16` table the caller passes as the blit's 6th argument**: `MOV EBX,[EBP+0x1c]` then `MOV AX,[EBX + EAX*0x2]`, stride 2, so the whole table is 32 bytes. `FUN_00457c40` builds **thirteen** such tables, each 16 entries (`ESI += 2` per pass, guard `0x8ac` falling `0x94` per pass ⇒ 16 passes), each entry a `base*k/15` gradient packed to the active framebuffer channel masks. So the value is an **intensity level of a text colour the caller chooses**, file-independent — EXP-0042's render drew it as coverage as a display convention, and coverage is what it is. The write is opaque: unlike the u16 blitter there is no destination read and no add. Note a consumer must not treat 0 as transparent — the **low** nibble has no zero test, so value 0 is a written pixel; 0 shipped low-nibble zeros makes this unobservable in data) | ● active | [EXP-0042](../experiments/EXP-0042-spr16a-residuals/), [EXP-0047](../experiments/EXP-0047-spr16-reader/) |
| SPR16A-FONT-014 | **`font1.16`/`font2.16` are a chain of record sections, not one.** After the count trailer each continues into further `[records][4-byte trailer]` sections, every trailer an identical copy of the first (`0x000000e0`): font1 = 224 + 11 + 115 records (16×15), font2 = 224 + 49 (8×10), both ending exactly at EOF with no leftover. The appended records are glyph art (font1's 11) and then **solid cells of the single value `0xd`** (115 and 49); none is byte-identical to a section-0 record, and every one decodes exactly under `SPR16A-FONT-013`. **This is `SPR256-EXC-017`'s Bucket-B shape** — a normal payload plus an appended section bracketed by a copy of its own trailer — reappearing in a second format | **Medium** (the chain and its record contents are measured; the framing is the interpretation) / **High, and it settles the Unknown by dissolving it — amended by EXP-0047** (the `.16` loader **does** stop at the count trailer: `FUN_004284e0` builds a table of exactly `count` pointers, `cursor += 12 + dataSize`, and the loop's only exit test is `CMP EDX,EDI / JL` against that count — nothing compares the cursor to the buffer end, and the class has no other reader of the load buffer. Walking `font1.16`'s 224 records ends at byte **15 212** of 32 932 and `font2.16`'s at **10 789** of 13 892, leaving **17 720** and **3 103** bytes — the same two figures this row measured as the "trailing pool", reached from the opposite direction. So every appended section, the solid-`0xd` cells and the 26/12/57 boundary runs alike, is **unreachable by any code path**, and the boundary runs need no framing: they are dead bytes in a file the engine reads only the front of). **Amended by EXP-0050:** the chain is explained — the sections are **older build layers of the same font**, each boundary run the byte-suffix of a record cut by the next build's end (`SPR16A-FONT-021`); the 11 appended records are **х..я** at positions 213..223; and the figures above are the **EN** `font2.16` — the RU build's replacement carries a 28-byte cut suffix + trailer copy instead (`SPR16A-FONT-022`) | ● active | [EXP-0042](../experiments/EXP-0042-spr16a-residuals/), [EXP-0047](../experiments/EXP-0047-spr16-reader/), [EXP-0050](../experiments/EXP-0050-font-cyr/) |
| SPR16A-FONT-015 | **Five font atlases ship, not three.** `font1.16` 224 glyphs 16×15, `font2.16` 224 glyphs 8×10, `font3.16` 64 glyphs 8×6 (byte-control form) — and **`font4.16a` 224 frames all 16×16, `font5.16a` 224 frames all 24×24**, ordinary palette-bearing `.16a` sheets already decodable by `SPR16A-PIX-011` since EXP-0015. All five have record 0 empty (one blank-rows control), and **record `k` is character `32+k`** — pinned by the render, which draws `font1` records 32..62 as `@ A B C … ^` and `font4` records 33..63 as `A B … _`, rather than inferred from 224 = 256−32. **Amended by EXP-0047:** five ship, but only **four are loaded** — `font5.16a` is named by nothing in either executable (`SPR16A-FONT-018`), and the `32+k` origin now has a second, independent attestation, since `DrawText` subtracts `0x20` from every character before indexing (`SUB AL,0x20` at `00457899` and `00457bde`). **Amended by EXP-0050:** what those 224 records *hold* past ASCII is now read — CP437 accents plus the whole Russian alphabet in a hybrid arrangement (`SPR16A-FONT-020`), byte-identical between EN and RU for every font node except `font2.16` (`SPR16A-FONT-022`) | **High** — the alternatives are named and ruled out: "they are not fonts" fails because the archive's own node names are `font4/font4.16a` and `font5/font5.16a` (attested by the container, `RES-*`), because the frame count 224 is identical to font1/font2's glyph count and the cell size is constant across all 224 frames (unlike every other `.16a` sheet, whose frames vary in content but here are a fixed grid), and because the render draws frames 33..63 of each as a legible A..Z under the *already-verified* decoder. "They are three fonts and two sprite sheets" is what this refutes | ● active | [EXP-0042](../experiments/EXP-0042-spr16a-residuals/) |
| SPR16A-RDR-017 | **(rom.exe) The two loaders, and the fact a consumer most needs: nothing validates a frame header.** `.16a` and `.16` are parsed by **different** routines — `.16a` by `FUN_00428c50`, *the `.256` constructor* (`FUN_0042b920` calls it and then swaps the vptr to `0x5974f8`), and `.16` by `FUN_004284e0` (via `FUN_00428b90`, vptr `0x5973e0`) — so the two formats share a container because they share a parser, and the `.16` differences are the raw count and the absent palette (`SPR16A-TRLR-012`). Both then do the same thing: `malloc(fileLen)`, one sequential read (the `.16a` palette read lands at **`buf + fileLen − 0x400`**, the *tail* of the allocation, and the frame region at `buf+0`, so **file offset 0x400 is buffer offset 0**), a `malloc(count*4)` pointer table, and a walk `table[i] = cursor; cursor += 12 + *(cursor+8)`. **Between the `dataSize` load and the cursor advance there is no compare at all** — no width or height read, no size threshold, no bound against the buffer end or the file length; the loop's single exit test is against the frame count. There is therefore **no validity threshold, no rejection path, and no "null frame" semantics** anywhere in the reader: a corrupt `dataSize` walks the table into arbitrary memory, and a `dataSize == 0` record is simply a 12-byte header whose successor begins immediately. `w`/`h` are never read by the loader — they are fetched later by the accessors `vt+0x20`/`vt+0x24` (`FUN_00428b00`/`FUN_00428b10` return `frame[0]`/`frame[1]`). One artefact worth knowing: the palette pointer `this+0x20` is stored **before** the bit-31 branch, so a flag-clear `.16a`/`.256` carries a non-NULL pointer into its own frame bytes, while the `.16` loader sets it to NULL outright | **High** (every clause is a quoted instruction in `evidence/disasm-loader.txt`; the absence of validation is an absence *within one basic block*, which is the one shape where "no instruction does X" needs no enumeration) / **High** (that these are the only two loaders — instrument: `EnumRefs callto:` over the committed `RepairVtables`-repaired project; `FUN_004284e0` has **1** call site and `FUN_00428b90` **1**, all hits attributed, 0 in orphan or undisassembled code, and no `.rdata` slot holds either constructor. **Blind spot:** bytes never disassembled, which this instrument cannot see) / **High** (the walk base — 542/542 shipped `.16a` walked as disassembled end exactly at EOF−4) | ● active | [EXP-0047](../experiments/EXP-0047-spr16-reader/) |
| SPR16A-FONT-018 | **(rom.exe) A font is two nodes, and the glyph cell is not the advance.** Each font object loads `<base>.16`/`.16a` into `this+4` *and* **`<base>.dat` into `this+8`**, with `this+0xc` = the letter spacing (`2`, passed by the constructor site). `DrawText` advances `x += dat[glyph] + spacing` (`FUN_004577f0` at `00457938..49`: `MOV ECX,[ESI+8]` / `MOV EDX,[ECX + EDI*4]` / `ADD EDX,[ESI+0xc]`), and glyph 0 (space) additionally adds `GetHeight(0) >> 1`. The sidecar is `u32` per glyph and matches the sheet's record count on all five atlases (896 B = 224 for font1/2/4/5, 256 B = 64 for font3); **the cell width is never the advance** — `dat[g] <` the record width on **224/224, 224/224, 64/64, 224/224, 224/224** glyphs, ranges 0..14 / 0..7 / 0..3 / 0..15 / 0..20 against cells 16 / 8 / 8 / 16 / 24. So the `.16` and `.16a` glyph records are fixed cells and the proportional metrics live outside the sprite. **Four fonts are constructed and `font5.16a` is loaded by nothing:** `FUN_00458780` is the only construction site and names `graphics\font1\font1` … `graphics\font4\font4` as whole string literals — font1/2/3 through the `.16` class, font4 through the `.16a` class. **Amended by EXP-0097 on one clause and confirmed on the rest.** The advance rule, the two-node load, the `this+0xc` spacing of `2` and the space's extra `GetHeight(0) >> 1` all stand — `vt+0x24` is `FUN_00428b10`, which returns the frame record's `+0x4`, so it really is the **height**. What is incomplete is *"`DrawText` subtracts `0x20` from every character before indexing"*: it subtracts `0x20` from **the result of `FUN_004562f0`**, a code-page converter called on every byte at `0045788e`/`004578a7`/`00457bd6`, which this row and `SPR16A-FONT-015` both read past (`TEXT-CONV-001`). The four globals the construction site fills are named: font1 `[0x005e88b8]`, font2 `[0x005e92f8]`, font3 `[0x005e9c28]`, font4 `[0x005e9bc0]` (`TEXT-API-007`) | **High** (the advance rule and the two-node load — quoted instructions, plus the sidecar sizes and the 960/960 strictly-less census, which is what discriminates "advance" from "cell width") / **High** (`font5` is loaded by nothing. Two instruments, and the second is immune to the first's blind spot: `EnumRefs callto:00457640`/`callto:00457990` over the repaired table finds **3** and **1** call sites, all inside `FUN_00458780`, none orphan; and the ASCII run `font5` **does not occur in `rom.exe` or `Map Editor.exe` at all**, while no font path is composed from a format string — the image's only `%s%d` literals are `%s%d.bmp` and `%s%d.256`. A whole-image string scan does not depend on disassembly, so the undisassembled-bytes blind spot cannot hide a caller that must name the path) | ● active (amended) | [EXP-0047](../experiments/EXP-0047-spr16-reader/), [EXP-0097](../experiments/EXP-0097-ru-text/) |
| SPR16A-FONT-019 | **(rom.exe) The `.16` byte blitter aliases its unused quadrant to *skip*.** `FUN_0044d890` dispatches on `AND EAX,0xc03f` with two sequential tests — `OR AH,AH / JZ` → literal (`0044d911`), then `CMP AH,0x40 / JNZ` → skip (`0044d915..18`) — so the `else` catches **both `0x80` and `0xC0`** and only `0x40` reaches the blank-rows arm. This is `SPR256-RLE-020`'s shape exactly, and it is the **opposite** of the sibling u16 blitter, which tests the blank-rows bit first and so decodes `0b11` as blank rows (`SPR16A-RLE-002`). Two blitters in one binary, the same two control bits, opposite aliases — which is why the analogy had to be tested rather than transplanted | **High** (both tests quoted, in both the unclipped and the clipped path of the same routine) — but note it is **code-defined and data-unused**: the quadrant occurs 0 times in 14 845 shipped control bytes (`SPR16A-FONT-013`), so no shipped glyph exercises either alias | ● active | [EXP-0047](../experiments/EXP-0047-spr16-reader/) |
| SPR16A-FONT-020 | **The 224-record atlases' high half: CP437's letters plus the whole Russian alphabet in a hybrid arrangement.** Records 96..223 under `char = record+32` hold the CP437 accented-Latin letters at CP437's own positions (`0x80..0x9A`, `0xA0..0xA5`, ß at `0xE1`; its currency/punctuation/Greek dropped) and **Cyrillic on CP437's box/math regions: А..Я `0xB0..0xCF`, а..п `0xD0..0xDF`, р..я `0xF0..0xFF`** — an arrangement that is none of CP866/CP1251/KOI8-R (each needs letters where these fonts are blank). Per font: font1 ✔, font2 ✔ (both versions), font4 ✔, font5 ✔ (own art style), font3 n/a (64 records). Mechanical check: of 18 Latin/Cyrillic homoglyph pairs (A/А…x/х), **17/18 decode pixel-identical at exactly the hybrid positions** in font1 and font2 (М differs by 2 px), 5/18 in font4, vs 0/18 at CP866/KOI8-R positions and 4/18 at CP1251's — those four being the `0xF0/F1/F3/F5` positions CP1251 shares with the hybrid. **EN↔RU: font1/font3/font4/font5 and all five `.dat` are byte-identical (sha256 equal)** — the English release always shipped these Cyrillic-bearing fonts | **High, raised by EXP-0097, which discharged this cell's own named lift** (the arrangement is no longer a map without an explanation: it is **exactly the image of CP866's Cyrillic under the engine's converter** `FUN_004562f0`, `0x80..0xAF → +0x30` and `0xE0..0xEF → +0x10` — which is why it matches no standard page, since the *file* encoding is CP866 and this is simply where the engine puts it. The lift asked for "runtime observation of the RU game **or a shipped string shown to reach `DrawText` with high bytes**"; the second was delivered, and the fit is two-sided and exact — the converter's 64-cell image carries ink on **64/64** in font1/font2/font4/font5 on both roots, the RU `font2` has **0** ink outside it, and all **1 952** high bytes of the RU text corpus fall inside the converter's domain, `TEXT-CONV-001`/`TEXT-FIT-004`. The *letter identities* within the arrangement remain render-and-homoglyph evidence, now corroborated by a render that draws the whole alphabet in order) / Medium (arrangement maps, render identification and homoglyph identity are corpus evidence. The hash identities and censuses within are exact measurements) | ● active (amended) | [EXP-0050](../experiments/EXP-0050-font-cyr/), [EXP-0097](../experiments/EXP-0097-ru-text/) |
| SPR16A-FONT-021 | **The appended tails are older in-place-overwrite layers of the same font** (the `SPR256-EXC-017` Bucket-B mechanism, now with the cut shown byte-exactly). Every boundary run is the **suffix of a record cut by the next-newer build's end**: font1's 12-byte run == the last 12 bytes of a full solid-`0xd` 16×15 record byte-for-byte, EN font2's 57-byte run == the last 57 of a solid 8×10 record (3 cut `h` bytes + `dataSize`=50 + all data), font1's 26-byte and RU font2's 28-byte runs parse as valid `SPR16A-FONT-013` stream suffixes; every remnant's records share the host's cell; the header rival fails (`w,h ≈ 2.4·10⁹`). Backward from each remnant's own trailer (all say 224): font1 sec#1's **11 art records are positions 213..223 = chars `0xF5..0xFF` = х ц ч ш щ ъ ы ь э ю я under the live arrangement** — confirmed by render, same letters as live records 213..223 in a different drawing style (pixel diffs 32..65, 0 identical) — sec#2's 115 solid-`0xd` placeholders are positions 109..223, EN font2's 49 are 175..223, and RU font2's cut fell inside position 223 (я) leaving 0 whole records. Layer stratigraphy of `font1.16`: solid placeholders → bolder draw (х..я survive) → the current draw | Medium (the suffix identities, cell mirrors and position arithmetic are exact; "older layers of the same font" is the interpretation they all point at, with no live rival — H2b refuted — but no build instrument to attest it) | ● active | [EXP-0050](../experiments/EXP-0050-font-cyr/) |
| SPR16A-FONT-022 | **The 2026-07-29 install base move, measured for this ledger's domain.** A GOG update replaced the EN install with the **Russian release** at 16:41 local; the owner reinstalled EN at 16:56; the RU state is preserved read-only at `gameversions\ru` (MANIFEST-RU.md5). `graphics.res`: EN 61 394 716 B `3ec2f34c…f30adf` (the reinstall re-hashes **exactly** to the pin every prior experiment recorded), RU 59 584 832 B `1dd52652…7d6002`. `ROM.EXE` byte-identical in both (`e9c4ceaa` md5) — every `rom.exe` claim applies to both releases. SPR16A corpus recount on the RU archive: **542 `.16a` / 3 `.16`, 542/542 walk exactly to EOF−4** — unchanged counts. Among font nodes only **`font2.16` differs**: 13 892 → 9 436 B, exactly **35 sec#0 records blanked** (96..122, 128..133, 193, 207 — the CP437 accents, ß and `0xEF`; art → `dataSize 1`), the Cyrillic rows row-identical, the 49-solid tail replaced by a 28-byte cut suffix + trailer copy. Install-wide file diff: 22 same / 19 changed / 79 only-EN (Map Editor, Help/, Hints/, 4 root maps) / 1 only-RU (`Patch 1.03.txt`) | High (every clause is a hash or a census quoted in `EXP-0050/evidence/` — fingerprints.txt, install-diff.txt, glyph-census row diff 350/350 font1, 64/64 font3, 189/273 font2) | ● active | [EXP-0050](../experiments/EXP-0050-font-cyr/) |
| SPR16A-TXT-023 | **The RU release's text bytes use three different systems, and none is the fonts'.** RU `README.TXT` reads as Russian under **CP1251**; RU `MAIN.RES` UI strings read as Russian under **CP866** («Дом гоблинов», «Мельница», «Пещера»… — byte-searched: 0 occurrences in EN main.res); the shipped atlases hold Cyrillic at the **hybrid** positions (`SPR16A-FONT-020`). Under `char−32` with no conversion anywhere (`SUB AL,0x20` — SPR16A-FONT-018; no code-page pass — RES-NAME/EXP-0034), a CP866 string through these atlases lands on accent glyphs and blank cells. ~~**How the RU game displays Russian text is open**~~ — **ANSWERED by EXP-0097, and this row's premise was wrong**: an unidentified path *does* convert, which was the first of the three disjuncts this row offered. `FUN_004562f0` maps `0x80..0xAF → +0x30` and `0xE0..0xEF → +0x10` on every byte of every drawn string when `[0x005eb57c] == 1`, and the `SUB AL,0x20` cited above is applied to **its result** — the instruction two before that `SUB` is `0045788e CALL 0x004562f0` (`TEXT-CONV-001`). The three encodings this row measured are all correct and all load-bearing: the data strings are CP866, the `README.TXT` is CP1251, and a **second** converter `FUN_00468a90` maps the latter onto the former on the input side (`TEXT-IN-005`). The selector is **data**: `rom.exe` is byte-identical across the roots and the language is the last character of the `main\id` resource, `"english 0"` vs `"russian 1"` (`TEXT-LANG-002`). The `.256` census stands and was never the question. **The clause that failed is the negative** — "no code-page pass" — inherited from `EXP-0034`, which established it for archive *entry names*, not for glyph indexing; see [`retracted.md`](retracted.md) | Medium **for the encoding identifications, which all survived** (corpus readings of unambiguous Russian words with the byte runs quoted) / **the "no conversion anywhere" clause is REFUTED**, and with it the open question it framed. The row's own named lift — "needs runtime observation of the RU game" — was itself wrong: no runtime observation was required, only a listing of the routine the row already cited | ● partially retracted (EXP-0097) | [EXP-0050](../experiments/EXP-0050-font-cyr/), [EXP-0097](../experiments/EXP-0097-ru-text/) |
| SPR16A-BOUND-016 | **The bounds a `.16a` decoder can point at a measurement for** (whole shipped corpus, 542 sheets / 2727 frames / 0 decode failures; census in `evidence/bounds.txt` + `frame-census.csv`). Measured maxima: width **552**, height **128**, `w·h` **50 784**, `dataSize` **20 892**, frames per sheet **224**, payload **206 328 B**; minima width/height **12**, `dataSize` **2**. RLE run maxima: literal **137** words, skip **551** px, blank **128** rows (the `.16a` count field is 14 bits and allows 16 383). Structural invariants: `dataSize` is **even in 2727/2727** frames (it is a `u16` stream); **145** frames are wholly empty (`dataSize == 2`); `(word & 0xE001) == 0` for all **2 535 421** literal words; all **256** palette indices occur but the level field is **never 0** (values 1..15 only); and **0 of 542 sheets mix frame sizes** — the opposite of `.256`, where 131 of 1384 do (`SPR256-FRAME-023`), so a `.16a` consumer *may* cache one frame's bounds for the sheet | **High** (every figure is a direct census of the shipped corpus with no model interposed) — but note what it is *not*: these are the bounds of what shipped, **not** of what the format admits. The geometry fields are `u32` and the format itself bounds nothing below `2^32−1`. Any cap is a **decision**; the spec records ours (`formats/spr16a/format.md` → *Defensible limits*) as a decision, with the measurement it clears | ● active | [EXP-0042](../experiments/EXP-0042-spr16a-residuals/) |
| SPR16A-PROJ-024 | **The projectile sheet corpus, walked: 24 of the 31 rows are `.16a` and the frame count is `Phases * facings` on 29 of 31.** `tools/spellart -mode sheets` resolves every `projectiles.reg` `File` under both extensions and walks the sheet frame by frame on `SPR16A-STRUCT-001` and `SPR256-STRUCT-001`, never sampling. Result, identical on both roots: **24 `.16a` sheets** (`A16 = 1`) and **7 `.256`** (`A16 = 0`, the four arrows and the three catapult rows, two of which share one file). Frame geometry is uniform inside every sheet, and the walked count equals the trailer count in all 31. The law the draw implies (`ANIM-PROJ-026`: `frame = Phases * facing + phase`, facings folded to 9 when `Flip` is non-zero) predicts `Phases * 9` frames with `Flip` and `Phases * RotationPhases` without; **29 of 31 hold exactly that**, including the two whose `RotationPhases` is 16 — `firebolt` and `fireball`, 4 `Phases` times 9 folded facings = **36 frames each**, which is what makes `Flip` a halving of the sheet rather than a flag on the art (`REG-UNITS-051` from the unit side). The two exceptions are `healing` — 7 `Phases`, **8** frames, one frame the draw can never reach — and `goblin\arrow`, one frame followed by an 8-byte residue before the count trailer, the `SPR256-EXC-017` shape. Frame sizes run 12x12 to 128x128 and equal the registry's `Width` and `Height` on all 23 rows that state them, the 8 that do not falling back to the loader's `0x40` default against a real 12x12 or 64x96 frame — so **`Width` and `Height` are the draw's centring offsets, not the art's dimensions**, and disagreeing with the art shifts the sprite rather than clipping it. **G2:** frame count is a file-format limit — adding a facing or a phase changes the sheet's bytes — while `Width` and `Height` are registry values and free | High (every sheet is walked to its trailer and the walk agrees with the stored count 31 of 31; the 29-of-31 law is a prediction made from the draw's instructions and then measured, with its two failures named) | ● active | [EXP-0139](../experiments/EXP-0139-spell-pictures/) |
| SPR16A-ALPHA-025 | **(rom.exe) The `.16a` literal word's level field is the pixel's ALPHA, and the write is an exact 16-step linear blend whose two weights sum to 16.** `SPR16A-PIX-011` established the word as an even byte offset into a `[16][256]u16` source table and that "a level-selected lookup of the old framebuffer pixel is added"; what that lookup *holds* was never read, so the model stopped one step short of a compositing rule. Both tables are now read. Source, `FUN_00427df0` case 4: for `level = 1..16` and every index, `chan = clamp((pal[i].chan * level) >> 4, 0, 255)` packed to the framebuffer's own widths -- **no tint term appears on this arm at all**, on either of its two branches. Destination, `FUN_0044ba10`, the only writer of `[0x005e8420]`, `[0x005e42f0]` and `[0x005eb570]`: it allocates `entries * 0x22` bytes = **17 rows** and fills row `k` with every representable pixel scaled by `m/16` where `m = 16 - k`. The blitter `FUN_00451ae0` ties them together in six instructions: `00451baf ADD ECX,[EBP+0x1c]` makes the source `*(u16*)(lutBase + word)`; `00451ba0 MOV BX,CX` / `SHL EBX,0x8` / `AND EBX,0x1e0000` is `level << 17` = `level` rows of `0x20000` bytes; `[EBP-8]` is the table base **plus one row** (`00451af6 LEA EDX,[EAX + ECX*0x2]`), so the destination row is `1 + level` and its multiplier is `15 - level`; `00451bc1 ADD BX,DX` then `00451bc9 MOV word ptr [EDI + -0x2],BX`. Hence `out = pal[(word>>1)&0xFF] * (level+1)/16 + dst * (15-level)/16` with `level = (word>>9)&0xF`, and `(level+1) + (15-level) = 16` exactly. Level **0 does not occur** in any shipped `.16a` (`SPR16A-PROJ-026` re-measures it over the projectile subset), so alpha runs 2/16..16/16 and level 15 is the only opaque value -- its destination row is row 16, `m = 0`, all zeros. The `/18` variant of the source table and the 8 192-entry destination row are **not a display mode**: `FUN_0044ba10` opens `GlobalMemoryStatus(&ms); DAT_005eb570 = (ms.dwTotalPhys < 24000000)`, so both are the under-24-MB arm and neither can be reached on a machine that runs this install. **G2:** the alpha resolution is a **file-format** limit -- 4 bits, 15 usable steps, one step per pixel -- and widening it changes a sheet's bytes; the two tables are engine-side and their *shape* (16 levels, 17 destination rows) is fixed by the field width, so nothing about them is elastic without editing art | High (both tables read as instructions, every mask and shift an immediate, the complementarity arithmetic rather than fitted, and the memory gate read at its only writer; the competing model -- destination row indexed by `level` rather than `1+level` -- is excluded by the `LEA` that skips one row, and the competing model "opaque, level is brightness only" is excluded by the destination read at `00451b98`) | ● active | [EXP-0140](../experiments/EXP-0140-cast-art-drawn/) |
| SPR16A-PROJ-026 | **The level census over the 24 `.16a` projectile sheets: the alpha field, not the palette, is what most effect art is drawn with.** `tools/castart -mode levels` decodes every frame of every sheet and histograms `(word>>9)&0xF` over lit pixels; identical on both preserved roots and the live install. **Level 0 occurs 0 times** in 247 883 lit pixels. Share of pixels at level 15 (the only fully opaque value): **0.0000** on `firewall`, `freeze`, `poison`, `steam`, `acid`, `teleport` and `shield`; 0.0392 `bless`; 0.0946 `lightnin` and `chain`; 0.1035 `healing`; 0.1036 `Curse`; 0.1170 the four `p_*`; 0.1612 `Meteor`; 0.3937 `firebolt`; 0.4300 `smallxpl`; 0.4301 `fireexpl`; 0.4381 `poison_d`; 0.4772 `Drain`; 0.5477 `fireball`; 0.8867 `wall`. Three sheets carry **one distinct palette index each** -- `freeze`, `poison` and `shield` -- so every pixel of them differs only in alpha, and a decode that discards the level field renders them as a single flat colour. Two families share a pixel stream and differ only in their embedded palette: `steam` and `acid` (9 415 lit pixels, identical histograms, 60 vs 48 distinct indices), and `p_fire`/`p_water`/`p_air`/`p_earth` (265 lit pixels each, identical histograms, 23/30/24/35 indices). **G2:** every figure here is a property of shipped art, not of the engine -- changing any of it changes a sheet's bytes and nothing else | Medium (a corpus census; it is a measurement of what shipped and discriminates nothing about the engine on its own. It is published because it is what makes `SPR16A-ALPHA-025` visible: the three single-index sheets are a known-answer test for any renderer, which can only produce a cloud if it reads the level field) | ● active | [EXP-0140](../experiments/EXP-0140-cast-art-drawn/) |
| SPR16A-ICON-027 | **The item-icon subtree is the most regular `.16a` population in the archive: 416 nodes, every one a single 80x80 frame with its own palette, and byte-identical across the two releases.** Walked with the `SPR16A-TRLR-012` reader — trailer `& 0x7fffffff` for the count, bit 31 for the 1024-byte leading palette — the whole `inventory/` tree parses whole: **416 of 416**, **0** parse failures, **0** zero-frame sheets, **0** sheets with more than one frame, **0** with bit 31 clear, and frame 0 is `80x80` on **all 416**. That is the opposite shape from the two populations already published beside it: `SPR256-EQUIP-042`'s 928 equipment sheets have four that yield 0 frames, and `SPR16A-BOUND-016`'s wider `.16a` corpus mixes geometries freely. The per-pixel level field runs **1..15** over every literal word in the tree — level 0 occurs in none of them, which is `SPR16A-PROJ-026`'s measurement holding on a second subset, and level 15 does occur, so under `SPR16A-ALPHA-025` the alpha runs 2/16..16/16 and the fully opaque value is used. sha256 per node on both roots: **416 identical, 0 different, 0 present on one root only** — so nothing in the item art is localised | High (an exhaustive walk of a closed subtree with the verified reader, hashed and compared root against root; the geometry and frame counts are read from the records rather than inferred from the walk terminating) | ● active | **[EXP-0144](../experiments/EXP-0144-item-pictures/)** |
| SPR16A-CAST-028 | **The art a cast can actually reach, joined row by row: 15 cast sheets, 8 burst sheets, 7 spells with no sheet at either parity, 2 sheets that rotate, and one shipped row nothing names.** `tools/castflight -mode map` joins the 28 `Data.bin` spell ids to `projectiles.reg` through `MAGIC-PIC-026`'s `2*id + 8` and `2*id + 9` and walks each resolved sheet, on both preserved roots, with identical results. **Cast parity (`+8`), 15 defined rows:** `firebolt`(10) `fireball`(12) `p_fire`(18) `healing`(20) `poison_d`(24) `p_water`(28) `Drain`(30) `lightnin`(34) `chain`(36) `p_air`(40) `shield`(44) `p_earth`(52) `bless`(54) `teleport`(60) `Curse`(62). **Burst parity (`+9`), 8 defined rows:** `fireexpl`(13) `firewall`(15) `smallxpl`(17) `freeze`(23) `poison`(25) `acid`(27) `wall`(47) `Meteor`(51). Only `fire_ball`(2) and `poison_cloud`(8) own a row at both parities. **Orientation:** exactly **2** of the 23 spell-reachable rows carry `RotationPhases = 16` with `Flip = 1` -- `firebolt` and `fireball`, both `Phases = 4`, both walking **36** frames, which is `Phases * 9` under `ANIM-PROJ-026`'s mirrored-facing law; every other spell-reachable row is `RotationPhases = 1`, so its frame index is the phase alone and it has no per-direction row. **Frame law:** the walked count equals `Phases * RotationPhases` on 22 of 23, the exception being `healing` at 8 walked against `Phases = 7`, which is `SPR16A-PROJ-024`'s already-known unreachable frame. **Registry `Width` / `Height` are not the art dimensions:** `healing` is `16x16` in the registry and `12x12` in the sheet, and `lightnin` `16x16` against `16x16` -- they are the draw's centring halves (`SPR16A-PROJ-024`). **Reachability:** of the 31 shipped rows, one -- `steam`(8) -- is named by no spell id at either parity and by no `units.reg` `Projectile` key, so it is defined art that no shipped consumer addresses. **Release parity:** the 23 spell-reachable sheets, the two `smoke%d` sheets the draw uses, and `projectiles.reg` itself are **26 of 26 byte-identical** across the two preserved roots, by payload sha256 (`evidence/sheet-parity.csv`). **G2:** every figure here is a property of shipped data -- adding a facing, a phase or a row changes a `.16a` or `projectiles.reg`'s bytes and nothing else; what is not elastic is which id a spell computes, which `MAGIC-PIC-026` already fixes in `.text` | Medium (a corpus census over two roots; the frame law and the centring clause are re-measurements of `SPR16A-PROJ-024` on this experiment's own join, and the reachability clause is bounded by the four consumers `REG-PROJ-086` enumerates, not by a fresh sweep) | ● active | [EXP-0167](../experiments/EXP-0167-spell-art/) |
| SPR16A-MARK-029 | **The ten `projectiles.reg` rows an actor-bound mark can reach, and what each row's declared `Phases` is worth: eight of the ten are used exactly and two carry frames no mark can show.** `MAGIC-MARK-061` fixes the ten record indices a mark can carry, and `MAGIC-MARK-059` fixes that the record's `+0x07` byte is the frame passed to the sprite blit. The frame set each arm can produce is an engine immediate: `countdown mod 6` for the four Protections and `poison_cloud`, so 0..5; `4 - step/18` over five fixed steps for `bless` and `curse`, so 0..4; `4` minus a quotient by 9 for `shield`; and a carried frame incremented while below 7 for `heal` and `drain_life`, so 0..6. Joined against `projectiles.reg` measured by `tools/spellart -mode proj`, identical on the install default and both preserved roots (`evidence/mark-sheet-join.csv`): all ten rows have `RotationPhases = 1`, so under `ANIM-PROJ-026` the frame index is the phase alone and there is no per-direction row. Declared `Phases` equals the frames used on eight rows -- `p_fire` `p_water` `p_air` `p_earth` at 6, `shield` `bless` `Curse` at 5, `healing` at 7. Two exceed it: `poison_d` declares 8 against 6 used and `Drain` declares 9 against 7 used, so each carries two frames no actor mark reaches. `healing`'s 7 also settles which count is the used one for `SPR16A-PROJ-024`'s named exception, whose walked sheet has 8 frames against `Phases = 7`. Nine of the ten rows are 12x12 in the registry and `healing` is 16x16; under `SPR16A-PROJ-024` those are the draw's centring halves, and `MAGIC-MARK-059`'s anchor subtracts exactly `Width/2` and `Height/2`. **G2:** the frame moduli are `.text` immediates, so a sheet with more phases than its arm indexes gains nothing without editing `ROM.EXE`; `Phases`, `Width`, `Height` and the sheet itself are data and are free. **✖ HEAL/DRAIN FRAME CLAUSE REFUTED by `SPR16A-031` (EXP-0189).** A carried phase-6 record is incremented and then drawn at phase 7, so both marks reach 0–7. Heal uses all eight walked frames; Drain leaves only frame 8 unused. The other eight mark rows and all centring observations stand.  | High for the frame sets, which are read from asserted immediates in `tools/effectmark` and reproduce on both roots, and for the registry columns, which `tools/spellart` measures on both roots and the install default with identical results. The join itself is arithmetic over those two measurements. | ✖ partially retracted (EXP-0189) | [EXP-0177](../experiments/EXP-0177-effect-marks/), [EXP-0189](../experiments/EXP-0189-spell-visual-algorithms/) |
| SPR16A-PART-030 | **The 23 spell-reachable sheets sort into four consumers, and two sheets serve two consumers each.** `SPR16A-CAST-028` enumerated 15 rows at cast parity `2*id + 8` and 8 at burst parity `2*id + 9` without saying which code draws each. `MAGIC-MARK-061` and `MAGIC-OVERLAYART-051` supply that. Of the 15 even rows, **ten are actor-bound marks** -- `p_fire`(18) `healing`(20) `poison_d`(24) `p_water`(28) `Drain`(30) `p_air`(40) `shield`(44) `p_earth`(52) `bless`(54) `Curse`(62) -- and the remaining **five are moving cast objects** -- `firebolt`(10) `fireball`(12) `lightnin`(34) `chain`(36) `teleport`(60) -- which are five of the seven picture ids `MAGIC-CASTSPAWN-033` gives a non-zero flight length. The even parity is therefore **not** a clean partition: the other two of those seven, `healing`(20) and `Drain`(30), get a one-tick flight and are also mark rows, so those two sheets serve both consumers. Of the 8 odd rows, **four are also painted on ground cells** by `FUN_0040cd70` -- `firewall`(15) `freeze`(23) `poison`(25) `wall`(47) -- and the other four are burst-only -- `fireexpl`(13) `smallxpl`(17) `acid`(27) `Meteor`(51). The two sheets that rotate, `firebolt` and `fireball`, are both in the cast-object group, and every row in the mark group and the ground group has `RotationPhases = 1`. Every one of the 23 rows lands in at least one group; `healing` and `Drain` land in two on the even side, and the four ground rows are drawn by the burst arm as well as by `FUN_0040cd70` on the odd side. **G2:** the assignment of a sheet to a consumer is `.text` -- the flight-length switch, the mark dispatch table and the four ground-overlay immediates -- so re-pointing a sheet at a different consumer means editing `ROM.EXE`; the art and its geometry are data. | Medium. The three consumer sets are each read from instructions and are individually High, but the partition is an inference over three separate enumerations rather than one sweep, and it inherits `SPR16A-CAST-028`'s Medium reachability bound: it states which of the four known consumers draws each row, not that no fifth consumer exists. | ✔ promoted | [EXP-0177](../experiments/EXP-0177-effect-marks/) |
| SPR16A-031 | **The four reviewed spell visuals join to embedded-palette `.16a` sheets with these exact reachable frame sets.** Heal record 20 declares `Phases=7`, registry 16x16, while the sheet walks as eight 12x12 frames and its mark reaches all frames 0–7. Drain record 30 is 12x12 with nine frames and its mark reaches 0–7, leaving only frame 8 unused. Shield record 44 is 12x12 with five frames and reaches 0–4; at Shield `p=0` Component B emits no record rather than indexing `-1`. Meteor record 51 is 64x64 with nine frames: frame 8 is used for all eight falling offsets and frames 0–7 for impact. All four have `Palette=1`; the common blend is `palette[index]*(level+1)/16 + destination*(15-level)/16`, so record duplication and ordering matter. **G2:** art, registry geometry and palettes are data; reachable phase laws are engine code. | High for the both-root registry/sheet joins and frame reachability, independently reviewed and byte-identical across the install and preserved roots. | ✔ promoted | [EXP-0189](../experiments/EXP-0189-spell-visual-algorithms/) |
| SPR16A-CURSOR-061 | **The cursor constructor's frame-count and period arguments are consumed by a `timeGetTime`-paced frame counter belonging to a single manager object, and the wrap is exact: a caller never sees a stored frame index at or above the count `SPR16A-CURSOR-046` publishes per sheet.** `SPR16A-CURSOR-046` names each sheet's frame count and leaves open "whether the cursor animator visits frames beyond its registered count." `AI-CURSOR-172` (`EXP-0213`) traces the consumer: setting a cursor copies the constructor's `+0x10` frame-count and `+0x14` period arguments into a single global manager object (`0x005cd790`, `+0x28`/`+0x2c`), zeroing the manager's own frame index and last-tick fields on every call; a paired refcount helper then calls `WINMM.dll timeGetTime` (the image's one call site for that import) and, once elapsed time since the last tick exceeds the stored period, increments the frame index and resets it to `0` before it can be read externally if it would reach the stored count. **G2:** the frame count and period are executable constants, one pair per constructor call — as `SPR16A-CURSOR-046` already states — and are unaffected by any `.16a` sheet's own bytes; a larger sheet does not change how many frames the counter can address, and the counter's own mechanism (the manager object, the `timeGetTime` source, the wrap) is engine code, not sheet data | High for the mechanism and the wrap (traced end to end, `AI-CURSOR-172`); this rules out the sheet's own trailer frame count being read a second time by some other, undiscovered consumer that could exceed it — the wrap happens inside the one routine that increments the index, before any other code can observe the pre-wrap value. **Medium**, inherited from `AI-CURSOR-172`, for whether the counter is ever observed to advance past frame index 1 during play: the 11 callers of the increment helper outside the reset-then-advance call chain were not traced | ● active | [EXP-0190](../experiments/EXP-0190-combat-controls/), [EXP-0213](../experiments/EXP-0213-cursor-lifecycle/) |
| SPR16A-CURSOR-067 | **The 28 cursor registry slots are the 28 dwords at `0x005ef9b4 + 4n`, and slot order is construction order.** `SPR16A-CURSOR-046` names the 28 registrations and their art, hotspots and period arguments but not the address each is stored at, so no published row lets a reader turn a slot address in a listing into a cursor name. Joining the table addresses against that row's `cursor-construction.tsv` in construction order gives: 0 `default` `0x005ef9b4`, 1 `move` `0x005ef9b8`, 2 `swarm` `0x005ef9bc`, 3 `attack` `0x005ef9c0`, 4 `defend` `0x005ef9c4`, 5 `select` `0x005ef9c8`, 6 `patrol` `0x005ef9cc`, 7 `cast` `0x005ef9d0`, 8 `pickup` `0x005ef9d4`, 9 `arrow0` `0x005ef9d8`, 10 `arrow4` `0x005ef9dc`, 11 `arrow6` `0x005ef9e0`, 12 `arrow2` `0x005ef9e4`, 13 `arrow7` `0x005ef9e8`, 14 `arrow5` `0x005ef9ec`, 15 `arrow1` `0x005ef9f0`, 16 `arrow3` `0x005ef9f4`, 17 `sdefault` `0x005ef9f8`, 18 `smove` `0x005ef9fc`, 19 `sattack` `0x005efa00`, 20 `sdefend` `0x005efa04`, 21 `spatrol` `0x005efa08`, 22 `scast` `0x005efa0c`, 23 `cantput` `0x005efa10`, 24 `town` `0x005efa14`, 25 `dice` `0x005efa18`, 26 `wait` `0x005efa1c`, 27 `backpack` `0x005efa20`. **The eight arrow slots are not in arrow-number order**, which is why a reader indexing by arrow number reaches the wrong slot | High. The registry constructor `FUN_0046d3f0` stores to each of the 28 addresses exactly once (28 of the 212 references in `AI-CURSOR-188`, one per slot) and the destructor `FUN_0046dac0` reads each exactly once, so the join is one-to-one with no free choice. **The decisive fact is that the 28 stores ascend monotonically in both code address and slot address with no gaps** -- `0046d44e` to `0x005ef9b4` through `0046da98` to `0x005efa20`, evenly spaced -- so construction order and slot order are the same sequence by inspection rather than by inference. The live alternative excluded is that slot order is the arrow numbering or some other declared order: the eight arrow slots come out as 0, 4, 6, 2, 7, 5, 1, 3 in address order, which no declared ordering produces. It is confirmed independently by a measurement in the shipped art that no code reading enters: `EXP-0190`'s `cursor-construction.tsv` gives the eight arrows' registered hotspots as a compass rose -- (15,5), (22,8), (25,15), (23,22), (16,25), (8,23), (6,16), (8,9) for `arrow0`..`arrow7` -- which matches the direction `AI-CURSOR-190` derives for each slot from the screen-edge tests. Citing `AI-CURSOR-190` alone would be mutual support, since that row cites this one; the hotspot table is outside both | ● active | [EXP-0214](../experiments/EXP-0214-cursor-surfaces/) |

`EXP-0213` was allocated `SPR16A-CURSOR-061`..`SPR16A-CURSOR-066` (6 ids) and spent
`SPR16A-CURSOR-061`, 1 id. **`SPR16A-CURSOR-062`..`SPR16A-CURSOR-066` (5 ids) are returned
unused**, none ever reissued. The next free `spr16a.md` id is therefore `SPR16A-CURSOR-067`.

`EXP-0214` was allocated `SPR16A-CURSOR-067`..`SPR16A-CURSOR-069` (3 ids) and spent
`SPR16A-CURSOR-067`, 1 id. **`SPR16A-CURSOR-068`..`SPR16A-CURSOR-069` (2 ids) are returned
unused**, neither ever reissued. The next free `spr16a.md` id is therefore
`SPR16A-CURSOR-070`.
