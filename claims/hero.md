# Claim registry — HERO (character generation and the derived-stat graph)

Level 2 ledger. Index: [registry.md](registry.md) · spec: [`formats/hero/format.md`](../formats/hero/format.md). Legend: `✔ promoted` (also in the spec) · `● active` · `✖ retracted`. IDs are permanent.

Not a file format — the **simulation area** that turns a player's four numbers into a hero. It sits on
top of the placeable-definition database (`DAT-ACT-006` gives the actor its `Data.bin` row and the
field map `+0x84…+0x134`) and feeds the mover (`TERR-MOVE-056`). Opened by
[EXP-0063](../experiments/EXP-0063-hero-stats/), which read every routine below at instruction level.
The **spell** side of the same actor is [`magic.md`](magic.md); two rows here
(`HERO-REGEN-021`, `HERO-DAMAGE-022`) came out of that round and stayed in this ledger because their
subject is the actor's own fields.

Vocabulary is the **engine's own** wherever the image supplies a name: `Body` / `Reaction` / `Mind` /
`Spirit` are `scenario\npc.reg` key literals, the six skills are `Data.bin` column titles. Where the
image supplies no name the field is described, not christened.

Two integer conventions hold throughout and are stated once here rather than in every row:
`__ftol` (`0055458c`) sets x87 rounding-control to **11, truncate toward zero**, and `IDIV` and the
`CDQ`/`SUB`/`SAR 1` idiom truncate toward zero as well. The **only** rounding-to-nearest in the whole
area is `FUN_004d3718`'s `+0.5` before its truncate.

| ID | Claim | Confidence | Status | Evidence |
|----|-------|------------|--------|----------|
| HERO-DEFEAT-136 | **The six-slot XP penalty belongs to one conditional repair operation, not to every death or campaign reload.** `004f7a53` has one direct caller `004d3755@004d37ac`. The accepter's existing-primary arm zero-extends stage byte `actor+0x13c` (`XOR ECX,ECX; MOV CL,[...]`) and charges only when it is nonzero; it then restores HP/MP and the loss leaf clears stage through `004f4a87`. The leaf multiplies each of six signed-dword XP values by 9, divides by 10 with truncation, recomputes their total and inverse-derived levels, and snapshots bases 1..5. The accepter has exactly three direct sites and no additional stored-address candidate in the PE scan: reporter `004d8a67` (mode nonzero plus latch, entry-active and HP gates), session-space opcode `0x48@004d7573` (resolved player, no mode/latch predicate; existing staged primary is chargeable if such a command is delivered), and `0xbe@004d7fed` (reachable only inside `player+0x34 == 0`, hence fresh creation, not repair of an existing dead primary). The observed `0x48` producer `0041fd61` is reached by character acceptance `0047bd50`; no failure-menu replay of it was found. Direct heal wrapper `004f49d7`, retained-character teardown `004d05b2` and imported-character continuation `004cfa20` clear stage/restore without the XP leaf. Campaign exit therefore does not establish deferred penalty on next entry. | High for complete discovered call-site set, opcode table and distinct local gates; producer exclusivity beyond the stated scan and delivery of a staged-primary `0x48` from ordinary UI remain Unknown. No runtime XP loss is claimed | ● active | [EXP-0274](../experiments/EXP-0274-defeat-modes/) |
| HERO-STAT-001 | *(amended by EXP-0066: **two further independent witnesses of the same crossed order**, neither of them the chargen panel. The object serialiser pair carries the four stats over the wire as **single bytes** — `FUN_004e7de3` reads `byte [actor+0x88]` at `004e84a7` and `byte [actor+0x8a]` at `004e84cb` under the dirty bit `0x20000000`, and `FUN_004e26bb` reads them back `MOVZX` from stream bytes `+0x28 → +0x84 Body`, `+0x2b → +0x86 Reaction`, **`+0x29 → +0x88 Mind`**, **`+0x2a → +0x8a Spirit`** (`004e2cad`/`004e2ccb`/`004e2ce9`/`004e2d07`), each write skipped when the byte is 0. And `FUN_0045f850` writes the campaign record from the live actor in the same crossing — `+0x84→+0x138`, `+0x88→+0x139`, `+0x86→+0x13b`, `+0x8a→+0x13a` (`0045f935`…`0045f959`). So the binding now rests on the registry key literals, the effect dispatch's kind names (`HERO-EFFECT-019`), the serialiser's field order and the record copy — four directions. The **byte width of the wire** is a second ceiling on a stat independent of `HERO-CAP-015`'s.)* **A hero has exactly four primary stats, and their names come from the game's own registry keys.** `FUN_0047c590` reads one of `scenario\npc.reg`'s four archetype sections — `MaleFighter` / `MaleMage` / `FemaleFighter` / `FemaleMage`, selected by `screen+0x74 & 0xc0` — and the key literals bind the slots: `Body → screen+0x90` (`0047c674`), `Reaction → +0x94` (`0047c69f`), `Mind → +0x98` (`0047c6ba`), `Spirit → +0x9c` (`0047c6c5`), plus `Skill → +0xa0` and `Face → +0xa4`. `FUN_0047bf40` carries those four words into the `Human` actor at **`+0x84 / +0x86 / +0x88 / +0x8a` (u16)** and, truncated to a byte, into the campaign character record at **`+0x138 / +0x13b / +0x139 / +0x13a`** (`0047c1c6…0047c22c`, four `MOV`-pair sequences). The chargen panel reads them back in **display order** `0x138, 0x13b, 0x139, 0x13a` → rows 0..3 (`FUN_0042c2b0`), so the screen's Сила / Ловкость / Разум / Дух are **Body / Reaction / Mind / Spirit** in that order. Four and only four: the panel loop bound is `0042c8fd CMP EBX,0x4`, the record bytes are contiguous `0x138..0x13b`, and the acceptance test charges exactly four costs | High (every hop is a named instruction and the naming rests on the registry's own key strings, not on order or on testimony) | ● active | [EXP-0063](../experiments/EXP-0063-hero-stats/) |
| HERO-COST-002 | **One cost function serves the whole point-buy, and it is cumulative, not per-step.** `FUN_004d3718(n) = ftol(0.349 × pow(1.15, n−1) + 0.5)` — `1.15` is the inline `PUSH 0x3ff26666`/`PUSH 0x66666666` and is `pow`'s **first** argument, i.e. the base; `0.349` is `[0x0059bc40]` (`004d3740 FMUL`), `0.5` is `[0x0059bc48]` (`004d3746 FADD`), and `0055458c` truncates. It returns the **total** cost of one stat standing at `n`. A `+` click charges the first difference `T(v+1) − T(v)` (`FUN_0042d050`) and a `−` click returns `T(v) − T(v−1)` (`FUN_0042d080`), so **the refund is exactly symmetric by construction**: `refund(v) == cost(v−1)` on every `v ∈ [16..45]` (`evidence/point-buy.txt`). The cost **escalates**: 1 point per step through the low twenties, 22 points for the last step 44 → 45. Instrument: `EnumRefs callto:4d3718` on the repaired table — **8 hits, 3 owners, 0 in orphan/undisassembled code**, covering `.rdata` slots, so no second cost function exists on any enumerated route; blind to a call through a register Ghidra resolved to no target | High (the routine is read instruction by instruction, the two callers are read, and the symmetry is an algebraic consequence of the shape rather than an observation) | ● active | [EXP-0063](../experiments/EXP-0063-hero-stats/) |
| HERO-BUY-003 | **What a click does, and the four bounds.** `FUN_0042ce40(panel,row)`: read `v = panel[0x1d0 + 4·row]`, compute `cost`, **refuse if `panel+0x1f0 < cost`** (`0042ce55`/`0042ce5b`), **refuse if `v ≥ 0x2d = 45`** (`0042ce68`/`0042ce6b`), else `v += 1` and `pool -= cost`. `FUN_0042cf50(panel,row)`: compute `refund`, **refuse if `v ≤ 0x0f = 15`** (`0042cf6c`/`0042cf6f`), else `v -= 1` and `pool += refund`. Both then call `FUN_0047bec0`, which writes the four values back to `screen+0x90…+0x9c` and rebuilds the preview actor. **The counter on screen counts what remains**, not what was spent — the same word `panel+0x1f0` that both handlers move is the one `FUN_0042c330` draws. The start is `FUN_0042fb20`: **all four stats 25** (`0042fb32…0042fb53 MOV …,0x19`) and **pool 100** (`0042fb28`); it is the only writer of a nonzero pool in the image. `FUN_0042c7f0` greys the buttons on the same two tests, so the disabled state and the refusal are one rule. **Nothing here depends on class, race, or the other three stats** | High (both handlers and the initialiser are read whole; the bound immediates are the `CMP` operands themselves) | ● active | [EXP-0063](../experiments/EXP-0063-hero-stats/) |
| HERO-BUDGET-004 | **The budget is the literal 140, and it is `ΣT(stat) ≤ 140`.** The accepting side `FUN_004d3755` loads `0x8c` (`004d3c38`), subtracts `T()` of each of the four submitted bytes (`004d3c47`, `004d3c61`, `004d3c7b`, `004d3c95`), and **forces all four stats to `0x19 = 25` if the remainder is negative** (`004d3ca6 CMP …,0 ; JL 004d3cea`). The client's pool is the same number: `4 × T(25) + 100 = 40 + 100 = 140`, so `pool = 140 − ΣT(stat)` identically. No registry key, class table or race modifier is read on either side. **Consequences, from `evidence/point-buy.txt`:** one stat rises alone to **42** with the others left at 25 and to **43** with the others floored at 15; all four together reach **34**; and the click-time cap **45 is unreachable in character generation**, since `T(45) = 164` exceeds the 134 that remain after flooring the other three. **Unknown:** the accepting side tests the budget and *no bound at all* — no `CMP` against 15 or 45 appears between `004d3c38` and `004d3d1a` — so whether an out-of-range stat can reach it by another route was not established | High (the constant, the four subtractions and the clamp are named instructions; the `4·T(25) + 100 = 140` identity is arithmetic on two independently read constants) | ● active | [EXP-0063](../experiments/EXP-0063-hero-stats/) |
| HERO-HP-005 | **Health maximum, three steps.** In `FUN_004f7dfc` (the actor's `vt+0x50`): `h = body × (fighter ? 2 : 1)` (`004f7f4d IMUL`), **and if the `Data.bin` `HealthMax` column is 0 the whole arm is skipped** (`004f7f66 JZ`); then `h = ftol(h + log₁.₁(XP/5000 + 1) × (fighter ? 2 : 1))` (`004f7f82` FILD `actor+0x130`, `004f7f88` FDIV `[0x59bc98] = 5000`, `004f7f8e` FADD `1`, `004f7f9a` CALL the log, `004f7fb4` FADD, `004f7fb7` ftol); then `h = ftol(h × (pow(1.1, body)/100 + 1))` (`004f7fe4`, `004f7fec` FDIV `[0x59bc60] = 100`, `004f7ff2` FADD `1`, `004f7ff8` FMUL, `004f7ffb` ftol) → `actor+0x96`. **Every intermediate is truncated and stored as a `word`, so the truncations compound.** The current health `+0x94` is clamped to it at `004f84c4` and is live state, not a derived value | High (read instruction by instruction; the two class multipliers are the same `SETNZ`/`INC` idiom on the `FUN_0051cb70` result, whose identity is `HERO-CLASS-013`) | ● partially retracted | [EXP-0063](../experiments/EXP-0063-hero-stats/) |
| HERO-MP-006 | **Mana maximum, the same three steps on a different stat and the opposite class multiplier.** `m = spirit × 2` (`004f8026 SHL EDX,0x1`), gated on the `Data.bin` `ManaMax` column being nonzero — **and if it is zero the current mana `+0x9a` is set to 0 instead** (`004f80d2`); then `m = ftol(m + log₁.₁(XP/5000 + 1) × (fighter ? 1 : 2))` — note `NEG`/`SBB`/`ADD 2` at `004f8068` where health used `SETNZ`/`INC`, which is what inverts the multiplier; then `m = ftol(m × (pow(1.1, spirit)/100 + 1))` → `actor+0x9c`. **The base `spirit × 2` does not depend on class**; only the experience term does. `actor+0xa0` is a further product `manaMax × [actor+0x14]+0x58 / 100` (`004f85a9…004f85ba`) — *(amended by EXP-0072: **`+0xa0` is the AI's mana floor.** The same two arms are the whole body of the non-hero `vt+0x50` `FUN_004f5946` (`UNIT-DERIVE-003`), and an `EnumRefs disp:a0` sweep — a `u16` field, so every byte/word-wide hit is a candidate and all were read — leaves three actor-module readers: the deserializer `FUN_00510518`, `FUN_0052d600`, and `FUN_0052eb60`, the heal AI, which returns unless the actor has a spellbook, has nonzero current mana, and **`mana > +0xa0`** (`0052eb98` MOVSX, `0052eb9f CMP ECX,EAX ; JLE`), before fetching spell 6 from the book. So the column-free product is a per-player reserve the caster will not spend below. `FUN_0052d600` was located, not read.)* | High (the arithmetic and both multipliers are named instructions; the gate that consumes `+0xa0` is a named `CMP`/`JLE` and the reader enumeration states its instrument) | ● active | [EXP-0063](../experiments/EXP-0063-hero-stats/) |
| HERO-SIGHT-007 | **Sight and carry capacity.** Sight: `ftol(((mind + reaction)/25 + 4) × 256)` → `actor+0xa4` as a **u16** (`004f80e1` ADD, `004f80fa` FDIV `[0x59bca0] = 25`, `004f8100` FADD `[0x59bc50] = 4`, `004f8106` FMUL `[0x59bca8] = 256`, `004f810c` ftol, `004f8114` MOV word). The `× 256` is the sub-cell unit `MOVE-STEP-010` names, so the whole-cell radius is the **high byte** — which is also the byte `DAT-ACT-006` records the `Data.bin` `ScanRange` column landing in (`+0xa5`); the recompute overwrites both. Carry capacity: `body × 10 + 1` → `actor+0x92` (`004f8125 IMUL EAX,EAX,0xa`, `004f8128 ADD EAX,0x1`). Carried load: own weight `actor+0x8e`, plus **half the container's running weight sum** `[actor+0x7c]+0x20` (*amended by EXP-0079: this row said “half the carried money”. `[actor+0x7c]` is the inventory container and `+0x20` is `Σ (weight × count)` over its elements, maintained by four `IMUL` sites inside the container class; money is `Player+0x38` and is never an item — `ITEM-LOAD-005`, `ITEM-CONT-004`, retracted.md. The arithmetic below is unchanged.*) when that is `< 0xfa00 = 64000`, else the load is set to `0x7d00 = 32000` (`004f81b3…004f8206`); the halving is `CDQ`/`SUB`/`SAR 1`, truncating toward zero | High (named instructions throughout; the field's identity is now carried by the four writers inside the container class, `ITEM-CONT-004`) / **High** for sight's own divisor (amended by EXP-0209: the sheet's row formatter reads the full `actor+0xa4` u16 — not the high byte alone — and prints `whole.frac` with `whole = raw >> 8`, `frac = ((raw & 0xff) × 10) >> 8`, one digit truncated toward zero, via a literal `%d.%d` — `HERO-104`) / **Unknown**, narrowed (whether carried load is ever rendered as a decimal at all: EXP-0209 read the character-screen module `FUN_0047bf40` whole and found no *direct* access to `actor+0x8e`, `+0x90` or `+0x92` anywhere in its body — the function does make one indirect call, `0047c236 call [eax+0x50]`, into `FUN_004f7dfc`, the same routine cited above for sight and carried load, so the fields are reached transitively even though the fill routine's own body never touches them; if the sheet's Weight row has a fractional form its source is still unlocated) | ● active (amended, contested) | [EXP-0063](../experiments/EXP-0063-hero-stats/) |
| HERO-SPEED-008 | **Speed, and the overload penalty that is the only place load touches anything.** `speed = reaction` when `reaction < 12`, else `speed = reaction/5 + 12` (`004f8138 CMP EAX,0xc`, `004f8165 MOV ECX,0x5 ; IDIV`, `004f816c ADD EAX,0xc`); `+10` when `typeID (actor+0xe) == 0x13 or 0x15` (`004f8182`/`004f8190`). Then, **only if `load ≥ capacity`** (`004f8220`): `speed -= load/capacity` (`004f8239 IDIV`) and `speed = max(speed, 6)` (`004f825c CMP ECX,0x6`). The result is finally copied as a **byte** into the mover at `[actor+0x154]+0xa` (`004f85e3`/`004f85e9`) — the same field `TERR-MOVE-056` reads for the per-step duration, which is how a stat reaches the movement system at all. **Unknown:** no human `typeID` is in `{0x13, 0x15}` (`HERO-CLASS-013` puts humans at `0x21..0x24`, and `FUN_004f72d7` bounds the family at `0x21..0x3f`), so which class takes the `+10` arm is not established | High (every branch and constant is a named instruction) / **Unknown** (the `+10` arm's reachability for a human) | ● active | [EXP-0063](../experiments/EXP-0063-hero-stats/) |
| HERO-SKILL-009 | *(amended by EXP-0065: the mage half of the five shared slots now has a consumer — `MAGIC-POWER-004` reads `skill[Sphere]` off this array, so the dual naming is not decorative.)* **Six skill slots, not five and not ten — one shared set of five that the class renames, plus a `General` slot the sheet does not show.** Storage is `actor+0xa8 + 2i` for `i = 0..5`, ending at `+0xb3`; `+0xb4` is written as the damage byte by the same routine, so **ten slots would not fit**. `Data.bin`'s own column titles for the six are `Skill.General`, `Skill.Blade (Fire)`, `Skill.Axe (Water)`, `Skill.Bludgen (Air)`, `Skill.Pike (Earth)`, `Skill.Shooting (Astral)` (`EXP-0049/evidence/databin-schema.csv`, group `F humans`, indices 11..16) — **the game's own text gives each of the five its warrior name and its mage name in one string**, and the chargen screen loads `chrgen\fighter\{sword,axe,Mace,Pike,Bow}` or `chrgen\mag\{fire,water,air,earth,astral}` for the same five positions off the one class bit (`FUN_0042e250`, arm selected by `param_2 = record+0x18c & 2`). Slots 1..5 are restored from a base copy at `+0x116 + 2i`, have a bonus `+0xe8 + 2i` added, and are clamped to `[0,100]` **twice** (`004f834d`, `004f8894`); **slot 0 is in neither loop** but is in the experience sum. Character generation sets exactly one slot: `FUN_004f992c` zeroes 1..5 (`004f99c4`) then writes one (`004f99e4`), with the value **20** when `[0x005eb5a4] == 2` and **10** otherwise (`0047c1b1`/`0047c1bc`; the same pair at `004d3d2f`/`004d3d3f`), and a value above 10 also runs the starting-equipment arm (`004f9a00`). *Origin note:* the owner's «5 навыков воина, 5 навыков мага» and their hedged «возможно это всегда просто 5 полей» both aimed this reading; the record decides it, and the second is the one the record agrees with | High (the slot count is bounded by the next field's writer, the loop bounds are the `CMP` operands, and the dual naming is the shipped file's own column titles) | ● active | [EXP-0063](../experiments/EXP-0063-hero-stats/) |
| HERO-XP-010 | **RETRACTED twice over by EXP-0135.** The headline it carried — *"Experience is a function of the six skill levels, not an accumulated counter"* — is false for a hero in play, and so is EXP-0069's repair of it, *"the field is the **monster's** accumulator and the hero's is still recomputed"*. `actor+0x130` is incremented on a **hero** in the very routine this row named as the gain, at `004f7439`/`004f743f`/`004f7445` (book arm) and `004f75db`/`004f75e1`/`004f75e7` (weapon arm), and a third time in `FUN_004f7cd7` at `004f7da2`/`004f7da8`/`004f7dae`; all six loads and stores are plain `MOV`s at displacement `0x130` and the `ADD` between them carries none, which is `docs/INSTRUMENT.md` rule 5 and this field is the rule's own example. What is true instead is `HERO-XP-077`. The two tables `S(n)` and its inverse, the Mind multiplier and the type band survive unchanged and are restated there | **retracted** — it carried High, and so did the repair, which was written at the end of a round with the same instrument that produced the error | ✖ retracted | [EXP-0063](../experiments/EXP-0063-hero-stats/), [EXP-0069](../experiments/EXP-0069-combat-loop/), **[EXP-0135](../experiments/EXP-0135-skill-raise/)** |
| HERO-COMBAT-011 | *(absorption and the damage spread: source published in `HERO-EQUIP-017`/`HERO-ARMOUR-018`, EXP-0064. **The "degenerate range" clause is RETRACTED by EXP-0065** — `actor+0xb5` is the roll's spread, not its maximum, so an unequipped hero rolls `[d, 2d]`; see `HERO-DAMAGE-022` and retracted.md. Everything else below stands.)* **Attack, damage and defence — and two of the three are barely derived at all.** `toHit = ftol((pow(1.1, body) + pow(1.1, reaction)) / 5)` → `actor+0xa6` (`004f82c9`, `004f82df`, `004f82e7` FADD, `004f82ea` FDIV `[0x59bc58] = 5`). Bare damage: `ftol(pow(1.1, body) / 20)` → byte `actor+0xb5`, **and `actor+0xb4` is copied from it** (`004f82b2`) — ~~so an unequipped hero's damage is a degenerate range~~ *(retracted: the pair is `(base, spread)`, so the roll is `[d, 2d]`)*. The **active skill** `actor+0xb6`, when nonzero, then adds `3 × skill[active]` to toHit (`004f83d3 IMUL EDX,EDX,0x3`) and `skill[active] / 5` to the damage **base only** (`004f8404 IDIV 5`) — a term that has **no counterpart for a spell**, whose school skill buys `MAGIC-POWER-004`'s power instead. Defence: `reaction / 3` → `actor+0xbe` (`004f845f`, `MOV ECX,3 ; IDIV`), floored at 0 later (`004f85ef`). **Absorption (`actor+0xc0`, the sheet's `БРОНЯ`) is derived from nothing**: `FUN_004fa4b1` is `memset(actor+0xbe, 0, 0x16)` (`004f844a`/`005542b0`), zeroing it, and no instruction in `FUN_004f7dfc` writes it again. So `БРОНЯ` and `ЗАЩИТА` are two separate fields with two different sources, and the damage spread a player sees is the weapon's | High (each formula is a named instruction sequence; the "derived from nothing" clause is bounded by the routine, not by the image) | ● partially retracted | [EXP-0063](../experiments/EXP-0063-hero-stats/) |
| HERO-RESIST-012 | **The five elemental resistances share one source, and it is Spirit halved.** `FUN_004f7dfc` writes `prot[i] = spirit / 2` — `CDQ`/`SUB`/`SAR 1`, truncating toward zero — into `actor+0xc2 + 2i` for `i = 1..5`, i.e. `+0xc4 … +0xcc`, the five `u16`s `DAT-ACT-006` records the units table's `prot Fire..Astral` columns landing in (`004f8489…004f84a6`, one loop body, five identical stores). Later each is clamped `prot[i] = clamp(min(spirit/2 + 70, prot[i]), 0, 100)` (`004f86b0…004f8873`, `0x46` = 70, `0x64` = 100). Because the equipment pass `FUN_004f54f8` runs between the two, the clamp is what an item bonus is bounded by. The five damage-kind resistances `+0xcf..+0xd3` fall inside the same `memset` and **are never re-derived for a human**. Prediction the owner's sheet then satisfied: on an unequipped character all five read the same number | High (one loop, one expression, five stores; the shared source is the loop body itself rather than an inference from equal values) | ● active | [EXP-0063](../experiments/EXP-0063-hero-stats/) |
| HERO-CLASS-013 | **The derive has exactly two class-conditional edges, and `actor+0x4c` bit 2 is the fighter/mage discriminator.** `FUN_0051cb70` returns that bit; `FUN_0051cb90` tests `typeID ∈ {0x22,0x24}`. In non-zero player-character constructor mode, the typeID overwrite is `gender+0x23` for a mage and `gender+0x21` otherwise, so gender is the addend and class the base; map Humans constructed in zero mode retain their table typeID and are outside that four-value sex test (`PARTY-M20-031`). The chargen screen independently uses two bits for the four sex/class combinations. Bit 2 is derived from positive streamed mana, not copied from Data.bin (`HERO-HP-071`), and `FUN_004f6b11` also sets it with a spellbook and non-zero mana. The two derive edges are fighter-double health and mage-double mana; fifteen effect edges lie outside it (`HERO-CLASS-020`) | High for the class bit and the conditional addend/base split; mission 20 original saves discriminate the player-character mode from every-Human wording | ● active (corrected) | [EXP-0063](../experiments/EXP-0063-hero-stats/), [EXP-0133](../experiments/EXP-0133-person-health/), [EXP-0192](../experiments/EXP-0192-mission20-party-boundary/) |
| HERO-ORDER-014 | *(the cap's own value and its writer are `HERO-CAP-015`; what `FUN_004f54f8` does is `HERO-MOD-016`, EXP-0064.)* **The recompute is one virtual method, it is reached only virtually, and its order is load-bearing.** `FUN_004f7dfc` is `vt+0x50` of the two human classes — `EnumRefs callto:4f7dfc` on the repaired table returns **2 hits, both `.rdata` slots (`0x0059c498`, `0x0059c520`), 0 direct callers, 0 orphan** — so a reader looking for call sites finds none, and the `0x0059c3c0` base family's `vt+0x50` is a different body. Order matters at four points and a consumer that reorders them gets different numbers: the **stat caps run first**, so every later formula sees the capped value; **health and mana are computed before the skills are restored**, so a skill change reaches them only through the next recompute; the **`memset(actor+0xbe, 0, 0x16)` runs after defence's inputs are read but before defence is written**, so defence, absorption, the five protections and the five damage-kind resistances all start from zero each time; and the **equipment pass `FUN_004f54f8(actor+0xd4, actor)` runs at `004f84b5`, between the bare values and the clamps**, which is why the clamps bound equipment rather than the stats. The current health and mana are clamped to their maxima at the end (`004f84c4`, `004f8501`) — they are live state, and only the maxima are derived | High (the enumeration states its instrument and covers `.rdata` slots; the ordering claims are the instruction addresses themselves) | ● active | [EXP-0063](../experiments/EXP-0063-hero-stats/) |
| HERO-CAP-015 | *(amended by EXP-0066: the row's `disp:d5` enumeration is **reproduced field for field on Mind's and Spirit's own modifiers** — `EnumRefs disp:d6` and `disp:d7` each return **4 hits / 2 owners / 0 in orphan or undisassembled code**, the same two owners, `FUN_004f7dfc` reading twice (`004f7eac`/`004f7ec7`; `004f7ef4`/`004f7f0f`) and `FUN_00501a22` reading and writing (`00501c49`/`00501c55`; `00501cdb`/`00501ce7`). The `CMP EAX,0x64` this row cites for Body at `00501b41` exists identically in all four arms: Reaction `00501bd3`, **Mind `00501c65`**, **Spirit `00501cf7`**. Two consequences the row implies and did not state: **100 is a stat's hard ceiling in any reachable state**, and because the cap is step 0 of the recompute and every effect arm ends in a full recompute, **an untouched stat is 50 whenever anything reads it** — which is what bounds Mind's contribution to spell power to 20 points, `MAGIC-CEIL-013`.)* **What bounds a stat after character generation: `stat = min(stat, 50 + (int8)actor[0xd4+i])`, the 50 hardcoded, the modifier written by effects alone, and the same for both classes.** Step 0 of `FUN_004f7dfc` is four instruction-for-instruction identical blocks — `MOVSX` the **signed byte** `actor+0xd4+i`, `ADD …,0x32` (`004f7e23` Body / `004f7e6b` Reaction / `004f7eb3` Mind / `004f7efb` Spirit), `CMP`/`JGE` against the stat, and **write the smaller back into the stat** (`004f7e5a` and siblings), so the clamp is destructive. No registry key, `Data.bin` column or level term is read: the cap is those two operands. **Not class-conditional** — the class flag is computed into `EBP-0xc` at `004f7e09…004f7e16` and first *read* at `004f7f45`, past all four blocks, which is what discriminates "no branch" from "the flag is not available yet". The modifier's **only writer** is the effect dispatch's four stat arms, which add the same magnitude to stat and modifier (byte-wide, unclamped) unless `effect+0x3d & [0x0059bc14] = 0x08` is set, and clamp **the stat alone to 100** (`00501b41 CMP EAX,0x64`). So: **untouched by any effect a stat is capped at 50; the ceiling in principle is 100; both figures are identical for fighters and mages.** Instrument for the modifier's writer set: `EnumRefs disp:d5` (a `disp:` sweep, so structure displacements are covered), whole image, **4 hits / 2 owners / 0 in orphan or undisassembled code** — `FUN_004f7dfc` reads it twice, `FUN_00501a22` reads and writes it; blind only to bytes never disassembled. Consequences of the arithmetic, in `EXP-0064/evidence/stat-caps.txt`: the gated form's raise is undone by the next recompute, and the ungated form's byte modifier wraps at +127, taking the cap to −78..−76 with it. *Origin note:* `EXP-0063/evidence/worked-examples.txt` case F labels 50 "the storage cap"; that was a chosen sweep input with no instruction and no ledger row behind it, and it happens to be right — this row rests on `004f7e23`, not on it | High (four named immediates, a `disp:` enumeration that states its instrument and its blind spot, and the class question settled on the flag's first *read* rather than on an absence) | ● active | [EXP-0064](../experiments/EXP-0064-hero-equipment/) |
| HERO-MOD-016 | *(amended by EXP-0065: the layout's damage bytes are `[dmgBase][dmgSpread]`, not `[dmgMin][dmgMax]` — `HERO-DAMAGE-022`.)* **`FUN_004f54f8` is not "the equipment pass" — it is a 27-instruction fold of a 0x40-byte modifier block, and the actor carries three parallel copies of one layout.** `FUN_004f5438(actor+0xd4)` memsets `0x40` bytes (`004f5455 PUSH 0x40`), so the block is exactly `actor+0xd4 … +0x113`; all five constructors of the `0x0059c3c0` actor family build it beside `actor+0xa6`, `actor+0xbe` and `actor+0x114` (`004f2c12`, `004f2d14`, `004f2dff`, `004f2eec`, `004f2ffa`). The layout repeats three times: **live** `+0xa6` / **modifier** `+0xe6` / **base** `+0x114` for `[toHit u16][6 skill u16][dmgBase u8][dmgSpread u8][active u8][5 u8]`, the last five being `[?][?][elemBase u8][elemSpread u8][elemKind u8]`, and **live** `+0xbe` / **modifier** `+0xfe` for the 0x16-byte `[defence u16][absorption u16][6 protection u16][6 damage-kind u8]`. The fold does five scalar adds — `+0x8c += +0xd8` speed, `+0x92 += +0xda` capacity, `+0x96 += +0xdc` healthMax, `+0x9c += +0xe0` manaMax, `+0xa4 += +0xe4` sight — then `FUN_004fa4eb(actor+0xbe, actor+0xfe)` and `FUN_004fa818(actor+0xa6, actor+0xe6)`. `FUN_004fa593` is `FUN_004fa4eb` with `SUB` at all four sites. Two quirks are instructions, not readings: a resulting **negative speed zeroes the modifier `actor+0xd8`, not the speed** (`004f552b`), and `FUN_004fa818`'s last field is **assigned, not added** (`004fa8a3`). The six skill words are *not* in `FUN_004fa818`; the derive adds `actor+0xe8+2i` itself at `004f8361`/`004f838d`. Instrument: `callto:4f54f8` on the repaired table — 2 hits / 2 owners / 0 orphan (`004f6a7a` in the spawn `FUN_004f59de`, `004f84b5` in the derive); `callto:4fa818` — 1 hit | High (the routine is read whole; the block size is its own `memset` argument; the three copies are the constructors' own four calls) | ● active | [EXP-0064](../experiments/EXP-0064-hero-equipment/) |
| HERO-EQUIP-017 | *(amended by EXP-0065: the fill's second damage byte **subtracts** the first — `0050db5d MOV DL,byte ptr [ECX + 0x60]` / `0050db66 FSUBP`, so `w+0x61 = round(physicalMax × shape × material) − w+0x60`, a spread. `HERO-DAMAGE-022`.)* **Equipment has separate attach and removal routines; Armor and Shield add into both defensive copies, while Weapon changes modifiers and invokes the derive.** `Armor::Equip FUN_0050c8d0` (string `"Illegal armor"`) takes the item's own slot byte `item+0x50`, evicts `actor+0x198 + 4·slot` through that item's `vt+0x3c`, stores itself there, and adds `item+0x52` into **both** `actor+0xfe` and `actor+0xbe`. `Shield::Equip FUN_0050d2de` (`"Invalid shield"`) is the same with the block at `shield+0x50`, the single slot `actor+0x78`, and a two-handed test (`def column 0xe == 2`) that first drops the weapon in `actor+0x74`. `Weapon::Equip FUN_0050def2` branches on `def column 5` — the shipped title `@.attackType`: **melee (`< 10`)** does `actor+0xf4 += w+0x60`, `actor+0xf5 += w+0x61`, `actor+0xfe += w+0x6a`, `actor+0xe6 += w+0x52`, assigns `actor+0xf9/+0xfa/+0xfb` from `w+0x65/+0x66/+0x67` and sets **`actor+0xb6` — the active skill — to the low byte of definition parameter5 cached in `[EBP-4]`** (`0050df76..0050df88`, `0050e0d9/0050e0dc`), not the serialized Weapon byte `+0x50`; **ranged (`0xb`/`0xc`)** puts the damage in `actor+0xf9/+0xfa`, writes `actor+0xfb = 1` or `2`, sets `actor+0xe6 = actor+0xa8` and `actor+0xb6 = 0`. `FUN_0050c9de` / `FUN_0050d3f3` / `FUN_0050e1b5` are removal routines, not universal mathematical inverses: the ranged equip assigns `+0xe6` from General, but weapon removal subtracts `w+0x52` from that word (`SAV-HUMEQUIP-447`), the last also restoring the bare-hands pair `actor+0x134 = 8`, `actor+0x135 = 4`. The six accepted item-class paths call load refresh `FUN_004f36f7` with signed item weight (negated on removal) and an Effect walker, but not in one common order. Weapon removal runs `FUN_005088ab` before its direct fields and derives; Armor/Shield refresh negative weight before their block subtraction, with different slot-clear placement around Effects. `FUN_00508860/005088ab` walk item `+0x20` calling Effect `+0x40/+0x44` respectively (`SAV-EQUIPORDER-552`, `SAV-EQUIPEFFECT-553`). **The retained defensive modifier copy survives the later live-block clear; Weapon calls the derive at `0050e0ea` after its direct modifier stores.** The weapon's three numbers are built by `FUN_0050dadc` as `ftol(column × shape × material)` — `w+0x60` from column 6, `w+0x61` from 7, `w+0x52` from 8, `w+0x6a` from 9, `w+0x4a` from 3 — and since the shipped titles put `@.physicalMin`/`@.physicalMax`/`@.toHit`/`#.deIrnce`/`weight` at 7/8/9/10/4, **runtime column *i* is title *i+1***, the same 1-based skip `DAT-GRAM-003` found in the entry arrays | High (each routine is read whole and each field pair is a named `ADD`/`SUB`; the class of each routine is fixed by the error string it prints, not by its address) | ● active (weapon inverse, direct-write scope and selector provenance corrected) | [EXP-0064](../experiments/EXP-0064-hero-equipment/), [EXP-0288](../experiments/EXP-0288-equipment-event-order/) |
| HERO-ARMOUR-018 | *(amended by EXP-0104: `FUN_004fa4eb` adds **fourteen** fields, not the two named below — six `u16` at `+0x04 + 2i` and six `u8` at `+0x10 + i` before `+0x00` and `+0x02` — so the six damage-kind bytes `+0xce…+0xd3` have an unclamped 8-bit adder. Nothing this row concludes changes; what it used the routine for is defence and absorption. `HERO-FOLD-033`.)* *(**the `УРОН` half of the reconciliation is RETRACTED by EXP-0065** — it fitted `actor+0xf5 = 16` on the reading that the second damage byte is a maximum, which `HERO-DAMAGE-022` refutes; under the read fill the same sheet requires `+0xf5 = 8` **if** the client prints `[base, base+spread]`, and that display routine is still unread, so the row now bounds nothing on that line. `ЗАЩИТА` and `БРОНЯ` are untouched. retracted.md.)* **`БРОНЯ` is `actor+0xc0` and its source is armour, through the second word of the 0x16 block; the sheet's two EXP-0063 mismatches reproduce exactly.** `FUN_004fa4eb` adds `src+0` into `dst+0` and `src+2` into `dst+2` (`004fa56b`, `004fa57d`), so with `dst = actor+0xbe` the armour/shield block's first word is **defence** and its second is **absorption** — the shipped titles `#.deIrnce` and `#.absorbtion`, adjacent, in that order. On the owner's sheet (mage 19/23/30/42, no armour): `ЗАЩИТА = reaction/3 + actor+0xfe = 7 + 10 = 17`; `УРОН = [ftol(1.1^body/20) + skill[active]/5 + actor+0xf4, ftol(1.1^body/20) + actor+0xf5] = [0+0+8, 0+16] = 8–16`; `БРОНЯ = 0 + actor+0x100 = 0`. **Bounded:** which item the owner carried is not recorded, so those three modifier values are what the sheet requires rather than what a decoded item was measured to hold — what is shown is that the arithmetic needs exactly one word and two bytes of the modifier block and nothing else. Corpus, EN `Data.bin`: `#.deIrnce` is nonzero on four of the 27 weapon rows (6, 10, 10, 99) and on all 9 shield and all 30 armour rows, so a `+10` defence is an ordinary value; `#.absorbtion` is 0 on 22 of the 30 armour rows, which is why an armoured character can read `БРОНЯ 0` | High for the field identification and the arithmetic (named adds, the block's own two stores) / **Medium** for the sheet reconciliation, which is a fit of three unknown item values to three known sheet values rather than a measurement of the item | ● partially retracted (amended) | [EXP-0064](../experiments/EXP-0064-hero-equipment/), **[EXP-0104](../experiments/EXP-0104-combat-columns/)** |
| HERO-EFFECT-019 | *(amended by EXP-0065: the three `+0x3d` gate constants are the game's own duration words — `[0x0059bc08] = 1` duration, `[0x0059bc0c] = 2` continuous, `[0x0059bc10] = 4` charges, `[0x0059bc14] = 8` single use, parsed by `FUN_00503890` (`MAGIC-EFFMODE-009`); `teachSpell`'s container is `MAGIC-BOOK-002`; and **`castSpell` is dead here because it is never applied — it is read as data by `FUN_005089a7`** (`MAGIC-ITEM-007`), so "dead arm" must not be read as "unused kind".)* **An effect is one typed increment, and the game's own data file names all 49 kinds.** `FUN_00501a22(effect, target, magnitude)` — reached **only** virtually (`callto:501a22`: 2 hits, both `.rdata` slots `0059c6d0`/`0059c728`, 0 direct callers) — computes `v = param × effect+0x40` (read `(i16)` when `effect+0x3d` meets any of `[0x0059bc08] = 1`, `[0x0059bc10] = 4`, `[0x0059bc0c] = 2`, else `(i32)`), takes `kind = effect+0x3c` bounded `0..0x31` (`00501ac7`), and jumps through a **50-entry table at `0x00502846`** (46 distinct arms). Every arm ends in `CALL [target->vt+0x50]` — one increment triggers a full recompute. The table's slot *i* corresponds one-for-one to `Data.bin`'s **Magic** row *i*, whose name column is the kind's own name: `price`, `body`/`mind`/`reaction`/`spirit`, `health`/`healthMax`/`healthRegeneration`, `mana`/`manaMax`/`manaRegeneration`, `toHit`, `damageMin`/`damageMax`, `defence`, `absorbtion`, `speed`, `rotationSpeed`, `scanRange`, `protection0`+five elements, `fighterSkill0`+five, `mageSkill0`+five, `itemLore`/`magicLore`/`creatureLore`, `castSpell`/`teachSpell`, `damage`+five elements, `damageBonus` — and the arms match field for field, **including the crossed pair `3 mind → +0x88`, `4 reaction → +0x86`**, which corroborates `HERO-STAT-001`'s binding from a second, independent direction. Three structural facts: **fifteen arms branch on `target->vt+0x30()`**, writing the *modifier* copy when the target recomputes and the *live* field when it does not, so one dispatch serves heroes and non-recomputing actors; **`itemLore`/`magicLore`/`creatureLore` are dead** (all three point at the arm printing `"Effect affects not implemented yet parameter"`) and so is `castSpell` (a bare `JMP` to the epilogue), while `teachSpell` is what adds to the spellbook `actor+0x140`; and **`actor+0xe8+2i` has no other writer** — `EnumRefs disp:e8`, whole image, 148 hits / 68 owners / 0 orphan, of which the actor family is eight lines: two reads in `FUN_004f72d7`, two in `FUN_004f7dfc`, two read/write pairs here. Equipment reaches the skill bonuses only through an item's effect list. `evidence/effect-kinds.txt` reads the 50 slots out of the image rather than off the listing, so the mapping is not a memory-order assumption | High (the table is read from the image, the arms are read at instruction level, and the kind↔name alignment is checked on a case where the two orders disagree) | ● active | [EXP-0064](../experiments/EXP-0064-hero-equipment/) |
| HERO-CLASS-020 | **The class bit reaches fifteen more edges than the derive's two, all of them in the effect dispatch — and `FUN_00523ab0` is its exact negation.** `FUN_0051cb70` returns `actor+0x4c & 4`; `FUN_00523ab0` returns `NEG`/`SBB`/`INC` of the same expression, i.e. 1 exactly when the bit is clear. `HERO-HP-005`'s health arm doubles when `FUN_0051cb70` returns 0, so **`FUN_0051cb70 != 0` is the mage and `FUN_00523ab0 != 0` is the fighter**. In `FUN_00501a22`: **six fighter-gated arms** (`0050233b`, `00502364`, `005023a5`, `005023e6`, `00502427`, `00502468`) and **six mage-gated arms** (`005024a9`, `005024d2`, `00502513`, `00502554`, `00502595`, `005025d6`) write **the same six fields** `actor+0xe8 … +0xf2` — the two names of one skill set that `HERO-SKILL-009` found in `Data.bin`'s column titles, now with a code path each, so an effect naming `skillBlade` does nothing to a mage and one naming `skillFire` does nothing to a fighter. Three further arms are mage-only: `manaMax` (`00501e6a`) and `manaRegeneration` (`00501ec3`) — and the manaMax arm is the only place where a mage-only gate gates a *current* value as well as a modifier. `HERO-CLASS-013`'s count is unchanged where it is scoped: **two** class-conditional edges *inside* `FUN_004f7dfc` | High (both predicates are three-instruction bodies; the polarity is fixed by `HERO-HP-005`'s already-read multiplier rather than by naming) | ● active | [EXP-0064](../experiments/EXP-0064-hero-equipment/) |
| HERO-REGEN-021 | **Arithmetic/store amendment (EXP-0284):** the formulas below are ordinary-range shorthand only. Operands are signed words except zero-extended remainder bytes; intermediate products and accumulators wrap at32 bits. Remainder low byte is stored first, quotient low word second, then the stored signed word is upper-bounded without a lower clamp or remainder reset. `SAV-REGENWIDTH-528` through `SAV-REGENWIRE-532` give the exact arithmetic, guard, fault and serialization limits. The regeneration route requires positive health; a callback lies between its health stores and mana reads, so composed runtime effects remain Unknown. *(amended by EXP-0066: **no stat is read by the tick.** `EnumRefs disp:88` and `disp:8a`, whole image, place no hit in `FUN_004f4204`, so Spirit reaches mana regeneration only as a **proportional factor through `manaMax`** (`HERO-MP-006`) and Mind not at all; the rate modifier `actor+0xe2`, the period `actor+0x9e` and `FUN_004f41e8`'s `> 0x50` test are stat-free. `MAGIC-SPIRIT-011`.)* **What ticks health and mana: the actor's own `vt+0x14`, with a 1/100 remainder byte each, and the two regeneration modifiers enter as a percentage.** `FUN_004f4204` is reached only virtually — `EnumRefs callto:4f4204` on the repaired table returns **3 hits, all `.rdata` slots (`0059c3d4`, `0059c45c`, `0059c4e4`), 0 direct callers, 0 orphan** — the `+0x14` of the same three vtables whose `+0x50` is `HERO-ORDER-014`'s `FUN_004f7dfc`. It returns at once when `actor+0x54 == 0x10`. `rate = 3` when `FUN_004f41e8(actor) > 0x50`, else `1` (`004f4237`). Health, gated on `health < healthMax`, `actor+0x98 != 0` **and `(*[0x005cd758] % 4) == 0`**: `acc = health×100 + actor[0xa2] + healthMax×2 × (actor+0xde + 100) × rate / actor+0x98`, then `actor[0xa2] = acc % 100` and `health = min(acc / 100, healthMax)` (`004f4291`…`004f4349`). Mana, gated **only** on `mana < manaMax`: the same shape on `manaMax × 1` and `actor+0xe2`, dividing by `actor+0x9e` and keeping its remainder in `actor[0xa3]` (`004f4388`…`004f4434`). `actor+0xde` and `actor+0xe2` are exactly the two modifier fields `HERO-EFFECT-019`'s `healthRegeneration` / `manaRegeneration` arms write, and `+ 100` before the multiply makes each a **percentage of the base rate, not an addend**. Three asymmetries are instructions, not readings: health is doubled and mana is not (`004f42b8 SHL EAX,0x1`); health advances on one tick in four and mana on every tick; and a dying actor (`health < 0`) loses one point per four ticks (`004f447f`). `actor+0x98` and `actor+0x9e` are the two periods and are divisors; health has the stated nonzero guard, while mana does not. *(amended by EXP-0072: **what fills them is answered.** The base constructor `FUN_004f3317` sets `+0x98 = 100` and `+0x9e = 50` (`004f33cf`, `004f3407`), and the **Units** table overrides them per class from its `HP regeneraton` / `MP regeneraton` columns, slots 5 and 7 (`004f5692`, `004f56de`) — the **Humans** table has no such column, so a hero's class-template initialization carries 100 / 50; the serializer can restore other period values. Why the EXP-0065 sweep could not see it: every template store goes through a helper that takes a pointer (`FUN_005233c0` `MOV word ptr [ECX],AX`), so it carries **no displacement** and no `disp:98`/`disp:9e` sweep can contain it — the same shape as this ledger's own `HERO-XP-010` lesson, one level further out. Re-swept: `disp:9e` is 6 hits image-wide, 0 orphan, and the only actor-module ones are the constructor default, this tick and the deserializer. The zero-divisor hazard is real and unexercised: 50 of 56 unit classes ship `MP regeneraton = 0` and the mana arm has no `!= 0` gate, but **0 of 56** pair that with a nonzero `manaMax`, and the two classes with `HP regeneraton = 0` are covered by the health arm's own `+0x98 != 0` gate.)* ~~**Unknown:** what `FUN_004f41e8` measures, and what advances `[0x005cd758]`'s counter.~~ — **both closed by [EXP-0076]** (`SESS-TICK-004`, `SESS-TICK-006`, `SESS-CLOCK-005`). `FUN_004f41e8` has eleven instructions, with a four-instruction value-bearing slice, and returns `[0x005cd758]+0x04 − actor+0x138` (`004f41ef`…`004f41fa`) — **sub-ticks since the actor's current action run was due to end** — so `> 0x50` is *idle for more than 80 sub-ticks*, i.e. five full ticks ≈ 5 s at the shipped default speed, and the rate-3 arm is an out-of-action bonus with no stat input, consistent with this row's own "stat-free" amendment. `[0x005cd758]+0x00` is the **full-tick** counter: advanced by one instruction sequence, `004d2606`…`004d260e` in `FUN_004d2551`, gated on the **sub-tick** counter `+0x04 % 16 == 15`; `+0x04` itself is advanced at `004d8927` in `FUN_004d891a`, once per paced sub-tick of `1000/R` ms with `R = 16` by default, so a full tick ≈ 992 ms. The cadence of *this* routine is now named from the other side as well: `FUN_004f4204` is `vt+0x14` and its caller — Unknown when this row was written — is `FUN_0050fed6`, which the server tick runs **once per full tick** (`server+0x04 % 16 == 12`). So "health advances on one tick in four and mana on every tick" is right as written with *tick* = **full** tick ≈ 1 s at default speed. This closes EXP-0064's open item 3 *(amended by EXP-0122: the last of those three asymmetries — *"a dying actor (`health < 0`) loses one point per four ticks (`004f447f`)"* — is the whole of what happens to a corpse before the teardown, and it is what `HERO-DWELL-065` needed and did not have. Read out: `004f4227 JLE` enters the arm on `health <= 0`, `004f445b JGE` leaves it on `health >= 0`, so **0 is admitted and untouched**; the `AND EAX,0x3` filter is on the **full**-tick counter `[0x005cd758]+0x00`. `HERO-DECAY-069`, `HERO-ZERO-070`.)* | High (the routine is read whole; both accumulators, both periods and the `% 4` filter are named instructions, and the enumeration states its instrument; the two fills are named stores in the constructor and the streamer) / High for the two former Unknowns ([EXP-0076]: three routines read whole, both increments named instructions, the rate table re-read out of the PE through its own section table) | ● active (partially retracted/amended) | [EXP-0065](../experiments/EXP-0065-magic/), **[EXP-0076](../experiments/EXP-0076-session-lifecycle/)**, **[EXP-0122](../experiments/EXP-0122-corpse-clock/)**, **[EXP-0284](../experiments/EXP-0284-regen-store-semantics/)** |
| HERO-DAMAGE-022 | *(amended by EXP-0104 on two points, neither of them a correction of what this row states: the absorption subtraction is **gated** — `004fbe4f JNZ 004fbe63` jumps past it whenever the attacker carries `+0x4c & 0x10`, and a null attacker takes the same exit (`HERO-AUTOHIT-031`) — and it is followed **immediately by a clamp at 0**, `004fbe63` / `004fbe67` / `004fbe69`, so an over-absorbed physical blow contributes exactly 0 and never a negative (`HERO-CLAMP-030`). This row's silence about that clamp, beside `HERO-DMG2-029`'s explicit one, is what blocked an implementation story; the resolver also returns `max(total, 0)` at `004fc202`.)* **The unit-target resolver uses a `(base, spread)` damage pair, not `(min, max)`.** The former universal headline is narrowed by EXP-0270: Building targets use `FUN_005046dc` instead (`UNIT-STRUCTDAMAGE-064`), not this unit resolver. `FUN_004fbc92` is the actor's `vt+0x4c` (`EnumRefs callto:4fbc92`: **3 hits, all `.rdata` slots `0059c40c`/`0059c494`/`0059c51c`, 0 direct callers, 0 orphan**), signature `(this = target, A = a 0x16-byte combat block, attacker)`. The melee strike `FUN_004fba0e` passes `attacker+0xa6`, the live block, at `004fba9a ADD EAX,0xa6` / `004fbab2 CALL dword ptr [EDX + 0x4c]`, and subtracts the result from `target+0x94`. In order: `dmg = A+0x0e + U[0, A+0x0f]` (`004fbdad`, `FUN_00504003(n)` being inclusive, `rand()×(n+1) >> 15`); **hit iff `A+0x00 + U[−100,100] > target+0xbe`, or `attacker+0x4c & 0x10`, or the roll `>= 90`** (`004fbdf9`, `004fbe09`, `004fbe1b` — the `roll < −90` arm at `004fbe2f` assigns 0 to a variable already 0 and is inert); then `dmg −= target+0xc0` **absorption, flat and physical-only**; then `dmg = ftol(dmg × (100 − target[0xce + A+0x10])/100 + 0.75)`, so the six bytes this ledger's `HERO-RESIST-012` calls "damage-kind resistances, never re-derived for a human" are **resistance indexed by the attacker's own active skill slot** — blade/axe/bludgeon/pike/shooting. **Bless and Curse are the roll**: `FUN_004f4c25(attacker, n)` is `(1 << n) & actor+0x144`, a bitmask of active spells by id, and on `rand(100) < the effect's +0x40` spell 23 takes the maximum damage without rolling (`004fbd3b` adds the whole spread) while spell 27 takes the minimum. The routine's last act is `target+0x40 = the attacker` and `target+0x48 = A+0x15` when the attacker is a mage. **The pair's second byte is a spread**, which is why `HERO-COMBAT-011`'s "degenerate range" is retracted: both fills subtract the first from the second — `0050db66 FSUBP` in the Weapon fill, `004fe369 FSUBP` in the spell fill (`MAGIC-DMG-005`) — and the corpus test at spell power 0 reproduces `Data.bin`'s own `damageMin … damageMax` columns on 9/9 damage spells under that reading and 0/9 under the rival | High (the routine is read whole and its second argument is fixed by the call site rather than by inference; the (base, spread) reading is discriminated by two independent fills and a corpus re-execution) | ● active (amended) | [EXP-0065](../experiments/EXP-0065-magic/), **[EXP-0104](../experiments/EXP-0104-combat-columns/)**, [EXP-0270](../experiments/EXP-0270-structure-attacks/) |

| HERO-CADENCE-023 | *(amended by EXP-0072: equipped weapons can override the template charge and relax columns. Amended by EXP-0234: the old complete-period formula and its three worked intervals are retracted; `HERO-CADENCE-112` and `HERO-CADENCE-113` add the omitted recovery and re-entry terms.)* **An attack uses a per-actor three-phase countdown, but `attackChargeTime` and `attackRelaxTime` seed rather than equal its whole repeated interval.** `FUN_004f37be`, actor `vt+0x18`, holds phase at `actor+0x58` and a byte countdown at `actor+0x6c`: phase 0 calls `FUN_004fb853`, loads charge `actor+0x134` plus the ranged extra and enters phase 5; phase 5 decrements once per actor tick and calls the single strike path when the byte reaches exactly zero; phase 7 loads relax `actor+0x135`, inclusive `U[0,3]`, and for an equipped Humanoid the weight/Reaction penalty of `HERO-CADENCE-113`. At recovery zero it sets phase 0 and completion byte `actor+0x136`; the order machine consumes that byte on the next tick and can re-arm the retained order one tick later. The uninterrupted physical application interval is therefore `charge + rangedExtra + relax + U[0,3] + humanoidPenalty + 2 actor ticks`, not the former `charge + relax + U[0,3] + extra`. The two base bytes come from Units params 17/18 and Humans params 19/20, with equipped-item slots 12/13 overriding non-`-1` values. Simulation does not wait for the client animation: both attack notifications send charge+relax, but the client sizes the visible run from registry art and does not consume that byte as the animation length. Global speed changes tick duration, not these stored tick counts | High for the phase machine, arithmetic through recovery and two-tick order boundary: the routines are read whole, the application path has one caller and EXP-0234 re-reads the decisive spans as raw bytes on both byte-identical executables. Medium for shipped population and speed-family corpus clauses | ● active (amended; complete-period formula and worked intervals retracted) | [EXP-0069](../experiments/EXP-0069-combat-loop/), [EXP-0072](../experiments/EXP-0072-unit-stats/), [EXP-0234](../experiments/EXP-0234-action-cadence/) |
| HERO-TARGET-024 | *(amended by EXP-0074: **`FUN_005327d0` is not the acquisition rule and this row's Medium is refuted, not lifted.** The other fourteen callers of `FUN_0052ab60` were read. The filter is a **diplomacy predicate with the wanted bit as a parameter** — it unlinks an element when `(diplo & 1) == wantBit`, so "unlinks everything whose byte is 0" describes the `wantBit = 0` sites only; `FUN_0052eb60` and `FUN_0052ed30` pass **1** and heal what survives (`AI-FILTER-001`). Twelve of the fifteen sites filter an occupancy block, not the world. And the route an **unordered** unit actually runs is `FUN_0052e110` — 13 callers against `FUN_005327d0`'s one — which builds its candidate set from what the unit can **see** (`FUN_0052dea0`, `actor+0xa5`) and only then discards everything past reach (`AI-ACQUIRE-002`, `AI-SIGHT-006`). `FUN_005327d0` is the **player-order** variant. Two mechanism corrections inside this row's own text: it does **not** take the nearest — both routes break the distance tie on turn cost `FUN_0054f090(dir, mover[0])`, and `FUN_005327d0`'s list keeps every candidate that was `<=` the running minimum when it was seen, so a farther candidate can win on facing; and the `(actor+0x4c & 4) && actor+0x12c < 2 && player+0x28 == 0` clause fires **after** `order+0xc` and `order+8 = 6` are written, leaving the target pointer behind and the order idle (`005329f3`, `005329fc`, `00532a20`), rather than "first". `world+0xa4554` is answered by `AI-LIST-003`: the global on-map actor list `[0x00609558]`, walked linearly. See `claims/ai.md`.)* **The combat target is a second field, `actor+0x5c`, and the one auto-acquisition route read here uses the actor's own reach as its radius — not sight.** The movement destination lives in the mover `actor+0x154` and the order block `actor+0x158`; the strike reads only `+0x5c` (`004f3bbd`). Writers of `+0x5c` in the actor module (`EnumRefs text:0x5c],E`, whole image): the deserializer `FUN_00510518` (`00510d0f`), `FUN_005310e0` (`005312eb`, `00531431`, `005314b3`), `FUN_00531b10` (`00531b38`), `FUN_00531d30` (`00531d6f`) — each storing `order+0xc` and setting `actor+0x54 = 3`. A **player order** takes `FUN_005310e0`'s attack arms, gated on `FUN_00548ea0(mover, actor, target, actor+0x12c)`, which itself requires the actor to be **already facing** the target (`00548ec1 CMP byte ptr [ECX],AL` against `FUN_0054a680`'s 16-way direction) and the surface distance `FUN_0054a960` to be within the reach. **Auto-acquisition** is `FUN_005327d0`, whose only call site is `FUN_005310e0` @`0053179c` (`callto:5327d0`: 1 hit), taken when the mover raises an event (`mover+0x98 != 0`) with `actor+0x50` not in {1, 0xa, 0x17}: it walks the world list at `+0xa4554`, keeps candidates whose whole-cell Chebyshev distance `FUN_00544760` is `<= actor+0x12c` (`00532837`), filters them through `FUN_0052ab60` — which unlinks everything whose diplomacy byte `ai[0xa9c4 + 50·myPlayer + hisPlayer] & 1` is 0, and unlinks an invisible candidate (`+0x144 & 0x8000`) unless a group member is inside that member's `order+0x71` (`See invisible`) — takes the nearest, writes `order+0xc` and `order+0x8 = 6`, and either attacks (`FUN_00531b10`, state 3) or walks to it (`FUN_00532bb0`, state 1). One clause fires first: `(actor+0x4c & 4) && actor+0x12c < 2 && player+0x28 == 0` clears the order and returns (`00532a00`…`00532a2b`). `scanRange` is **not** this radius — the streamers put it at `actor+0xa5` (`004f571a`, `004f981f`; ctor default 5 at `004f3374`) and its eight readers all sit in the AI module (`0052d445`, `0052d57f`, `0052e413`, `0053331a`, `005334a7`, `00546d81`, `0054e22a`, write `00532e8c`), none on this route | High (the field's identity and the two order-machine roads: the store shape was enumerated image-wide and every hit read) / ✖ (that this is *the* acquisition rule — **refuted** by EXP-0074, which read the other fourteen callers and `world+0xa4554`: acquisition happens in three places and this is the least used of them, `AI-ACQUIRE-002`) / ✖ ("takes the nearest", and the ordering of the stand-down clause — both corrected above) | ● active (amended, contested) | [EXP-0069](../experiments/EXP-0069-combat-loop/), [EXP-0074](../experiments/EXP-0074-target-acquisition/) |
| HERO-REACH-025 | **Reach is footprint-aware and lives in `actor+0x12c`; out of range the actor paths toward the target and leaves the attack state.** The strike's own gate is `reach = (u8)attacker+0x12c` against `FUN_004fb702(attacker, target)` (`004fba5f` MOV, `004fba6f` CALL, `004fba7a CMP ESI,EDX ; JGE`). That function computes `d = max(\|Δx\|, \|Δy\|)` in 1/256-cell units (`FUN_004f280d`/`FUN_004f2849` per axis, `FUN_005545c0` the abs), subtracts `((attacker->vt+0x1c() + target->vt+0x1c()) << 7) − 0x100` (`004fb7a3 SHL EBX,0x7`, `004fb7a6 SUB EBX,0x100`) and returns **1** for `d <= 0x180`, else `(d + 0x40) >> 8` (`004fb7b4`…`004fb7c9`). `vt+0x1c` is `FUN_00523210`, returning `actor+0x49` — the `tokenSize` column (`004f58cb`, `004f990a`). `+0x12c` is **1** by construction (`004f3427 MOV byte ptr [EDX + 0x12c],0x1`) and neither streamer writes it, so a unit's reach comes from its weapon (`HERO-EQUIP-017`, `weapon[0x50] − 1`) — which closes this ledger's open item 9. *(amended by EXP-0072: the weapon's `+0x50` is `Data.bin` **slot `0x0b`, the `@.range` column**, or 1 when that cell is `−1` (`0050dc3c`/`0050dc44`), and `Weapon::Equip` **adds** rather than assigns — `0050e161 MOV CL,[EAX+0x50]` / `0050e164 SUB ECX,0x1` / `0050e170 ADD AL,CL` / `0050e175` store — with `FUN_0050e1b5` subtracting the same term. A whole-image `disp:12c` sweep on the repaired table returns 130 hits / 81 owners / 1 orphan; the field is a byte, so every byte-wide hit is a candidate and the four **writes** are the complete population: the constructor, that pair, and `00532e92` in `FUN_00532e60`, which sets reach **and** scanRange to 5 for an actor whose `+0x0e` is `0x18` — a typeID no shipped Units row carries, so what reaches it is Unknown. Blind spot, stated: `+0x12c` lies outside all three `HERO-MOD-016` blocks, so no wholesale fold can touch it.)* When `FUN_00548ea0` fails, `FUN_00532100` issues a move-to-**actor** order (`FUN_005492a0`) and sets `actor+0x54 = 1`, so the approach tracks a live target and is re-evaluated at the mover's next event, on top of the per-cell dynamic re-plan `claims/move.md` records; the strike sub-phases run only under state 3 | High (the range function and both gates are read at instruction level, and the footprint term is an instruction rather than an inference) / **Medium** on the re-path cadence — `FUN_005492a0`'s own body was not read here | ● active | [EXP-0069](../experiments/EXP-0069-combat-loop/) |
| HERO-DEATH-026 | **Death is a three-stage arm of the actor's own tick; the corpse keeps its cells until the dying countdown expires *and* its health is at or below −10, and `MOVE-ID-016`'s id release is confirmed from the death path's own instructions.** On `(s16)actor+0x94 <= 0` (`004f384b`) the tick takes its death arm. **First tick**: `actor+0x13c == 0` → set it to 1, interrupt the cast (`FUN_004f4c97`), **halve the defence** `actor+0xbe` (`004f389d SAR EAX,0x1`), load `actor+0x6c = vt+0x6c() − 1` — the `dyingTime` column (`FUN_004f36da` slot `0x21` for units; `FUN_004f700e` slot `0x17` for humans, **defaulting to 8** when the column is −1) — and broadcast 0x409. **Each tick after**: decrement while positive (`004f38e4`). **When it expires**: `if ((u8)actor+0x4a > 1) actor+0x94 = −1000` (`004f3904`; `+0x4a` is `movementType`, above 1 on exactly Ghost/Bee = 2 and Bat_Sonic/Dragon = 3, so those four leave no corpse), then `if (actor+0x94 <= −10)` set `actor+0x54 = 0x10` and call the teardown `FUN_004f4f5d` (`004f391f`, its only caller). **Occupancy is released there and nowhere earlier**: `FUN_005476d0(world, actor)` loops the `vt+0x1c() × vt+0x1c()` footprint into `FUN_00545230`, which finds the cell record in the hash table at `world+0x540b4`, clears the mover's slot by the block class `vt+0x20()` and, when no occupant is left, writes the static block bytes back into the two planes; then it frees the **reserved next cell** `mover+0xa6` through `FUN_0054af40` and zeroes `mover+0xa6`/`+0x80`/`+0x76` (`00547741`…`0054778b`). `FUN_00548e10` snaps the position's sub-cell offsets to `0x80`. `FUN_004f4f5d` ends in `FUN_004f52ee`, the decay machine: `−1` health on every tick whose global counter `[0x005cd758]` has its low bit clear (`004f531a`…`004f5343`), stage `actor+0x13c` = 2/3/4 as health passes −10/−20/−40, and **below −600** stage 5, health pinned to −10001 *(narrowed 2026-08-12 by [EXP-0147]: a save disagrees. Of the eight stage-5 records in the corpus (23 files at the landing; the eight added there carry none) four read −10001 and four read −10007, each stably - the same identity key carries the same value across saves minutes apart. The stage ladder, the −600 threshold and the id release are intact and `+0x13c == 5` coincides with `+0x04 == 0` 8/8; what falls is that −10001 is what a stage-5 corpse carries in a file. `SAV-DEATH-051`, retracted.md.)*, `FUN_004d9fac(actor+0x4)` freeing the id bitmap bit and `actor+0x4 = 0` (`004f53a6`…`004f5401`); every stage change is broadcast as message 9. *(amended by [EXP-0076]: **the counter is named and so is the decay's cadence.** `[0x005cd758]+0x00` is the **full-tick** counter, advanced once per **16** sub-ticks (`SESS-TICK-004`), so "every tick whose low bit is clear" is every second full tick ≈ 2 s at the shipped default speed. And `FUN_004f52ee`'s caller — not stated here — is `FUN_0050fed6`, which the server tick runs on the sub-tick phase `% 16 == 12`, i.e. **once per full tick**, over the dead list at `[0x005cd758]+0x14 → +0x0c`. So the corpse loses 1 health every two full ticks and reaches −600 in ≈ 20 minutes at default speed.)* *(amended by [EXP-0121](../experiments/EXP-0121-death-row/): **"when it expires" is a floor, not a moment.** `actor+0x6c` is decremented only while positive (`004f38e2 JLE 004f38f7`) and nothing else writes it while the actor is dying, so once it reaches 0 it stays 0 and the tick re-runs `004f38f7`…`004f391d` — the `movementType` arm and the `<= −10` test — on **every** invocation. The teardown fires on the first tick at which health is at or below −10, which may be the tick the countdown expires or any tick after it. `HERO-DWELL-065`. Health can still move because the strike does not require a live target (`HERO-FINISH-066`), and the countdown is in **sub-ticks**, not full ticks (`HERO-DYETICK-067`). "its only caller" now rests on a raw dword scan as well as `callto:` — `FUN_004f4f5d` and `FUN_004f52ee` each appear **zero** times in the image as data, so no pointer table reaches either. A corpse is still a **live actor** and a heal reverses this arm's first tick — the stage byte is cleared and the halved defence doubled back (`HERO-REVIVE-068`), which also corrects one address above: the halving's `SAR EAX,0x1` is at `004f389f`, and the `004f389d` cited here is the `SUB EAX,EDX` of the `CDQ`/`SUB`/`SAR 1` idiom, one instruction earlier.)* | High (every hop is a listed instruction, `FUN_004f4f5d` has a single caller, and the id release is re-derived here rather than cited) / High for the cadence ([EXP-0076], three routines read whole) / High for the corrected dwell ([EXP-0121](../experiments/EXP-0121-death-row/), the countdown's complete writer set plus the branch's own `rel8`) | ● active (amended) | [EXP-0069](../experiments/EXP-0069-combat-loop/), **[EXP-0076](../experiments/EXP-0076-session-lifecycle/)**, **[EXP-0121](../experiments/EXP-0121-death-row/)** |
| HERO-KILL-027 | **Experience is paid per landed blow in proportion to the health removed, only to a human class, and a kill also pays gold from three template columns.** `FUN_004fba0e`'s last act is `attacker->vt+0x64(target, dmg, 0)` (`004fbbb8 CALL dword ptr [EDX + 0x64]`), gated on `dmg > 0`, the target having been **alive before the blow** (the flag at `EBP−0x8`, set at `004fba8d` from the pre-hit health) and `target+0x94 > −10` (`004fbb8e`…`004fbba7`). On the base actor class `vt+0x64` is `FUN_005232a0` — three instructions and `RET 0xc`, so a **monster gains nothing**; on both human classes it is `FUN_004f78cc`, which computes `xp = ftol(target+0x1c × 0.5 × dmg / (s16)target+0x96 + 1)` (`004f7945 FILD [ECX + 0x1c]`, `004f7948 FMUL [0x59bc48] = 0.5`, `004f794e FIMUL [EBP + 0xc]`, `004f795e FILD`, `004f7961 FDIVP`, `004f7963 FADD [0x59bc80] = 1`, `004f7969 ftol`; the constants are read out of the image, `evidence/rom-constants.txt`) and passes it to `vt+0x5c` = `FUN_004f72d7`. `target+0x1c` is the units table's **`XPvalue`** (`004f5931`, the streamer's last store) and `+0x96` is `healthMax`, so a kill pays `XPvalue/2` spread across the blows by the fraction of maximum health each removed, plus 1 per blow — before the `Mind/30 + 0.25` scaling (`HERO-SKILLGATE-074`; `HERO-XP-010`, cited here when this row was written, is **retracted** and replaced by `HERO-XP-077`). Two refusals sit in `FUN_004f72d7`: attacker and target sharing a player (`004f7347`), and the diplomacy byte's bit 1 (`004f737d`). Gold: `FUN_004f4f5d`, for `actor+0xe > 0x40` (`004f50dd`), pays `treasureMin.1 + U[0, treasureMax.1]` when `treasure.1 Gold` exceeds `U[0,100]` (params `0x26`/`0x27`/`0x28` = titles 39/40/41, `004f50e2`…`004f5133`) and builds the sack via `FUN_004f4f30` when that or the corpse's inventory is non-empty | High (both vtable slots are dumped and read, the guard is the strike's own compares, and the two field identifications ride `HERO-CADENCE-023`'s offset law) / ~~**Unknown** which skill absorbs the gain~~ — **closed by [EXP-0135]** (`HERO-SKILLGATE-074`): with the bit **set** the slot is the spell's `Sphere` and a blow, which carries spell id 0, is dropped outright; with it **clear** the slot is `actor+0xb6`, the weapon's own attack type. So this row's per-blow experience reaches a **weapon** skill for a fighter and reaches **nothing at all** for a spellbook carrier | ● active (amended) | **[EXP-0069](../experiments/EXP-0069-combat-loop/)**, **[EXP-0135](../experiments/EXP-0135-skill-raise/)** |
| HERO-AGGRO-028 | *(amended by EXP-0074: **the flip is not unconditional — `0053d9f3 TEST AL,0x3` guards it, so a cell whose bit 0 *or* **bit 1** is already set is left alone.** Bit 1 is the scenario's "allied, and it stays that way": the `.alm` loader writes it from the type-5 row and forces it on the diagonal, and **37 of the 866 ordered off-diagonal pairs in the shipped corpus carry it** — on those, one blow turns nobody hostile (`AI-DIPLO-004`, `AI-DIPLO-005`). "Symmetrically" also needs a bound: the *flip* writes both directions, but the authored relation the flip acts on is **not** symmetric — 102 of those 866 pairs disagree with their mirror on bit 0.)* **Being struck does not give the victim a target — it makes the two *players* mutually hostile, and acquisition does the rest.** `FUN_004fbc92`'s tail stores the attacker in `target+0x40` (`004fc1ad`), copies `A+0x15` into `target+0x48` when the attacker has `+0x4c & 4` and zeroes it otherwise (`004fc1b3`…`004fc1cd`), and — when both actors have a player — calls `FUN_00531ac0([0x005f21c4], attacker, target)` (`004fc1f7`). That routine sets the victim's `order+0x54 = 1`, records the attacker's cell in `order+0x58` and clears `order+0x5a` (`00531ad3`…`00531afa`), then calls `FUN_0053d9b0(ai + 0xa9bc, attacker, target)`, which indexes the **same diplomacy matrix the acquisition filter reads** at `[attackerPlayer][victimPlayer]` and at the mirrored `[victimPlayer][attackerPlayer]` and, for each cell whose low two bits are clear, **sets bit 0** and notifies a human player (`0053d9ef`…`0053da42`). Since `FUN_0052ab60` keeps exactly the candidates whose byte has bit 0 set, one blow on a neutral turns his whole faction hostile, symmetrically and permanently | High (the hook, its arguments and the matrix arithmetic are read at instruction level, and the matrix address `ai+0xa9c4` is the one the acquisition filter indexes) / **Medium** that this is the *only* retaliation mechanism — the victim's own AI arms were not enumerated, the same limit `HERO-TARGET-024` carries / ✖ ("for each cell whose low two bits are clear, **sets bit 0**" was right about the instruction and wrong about the consequence stated beside it: "one blow on a neutral turns his whole faction hostile" holds only where bit 1 is clear) | ● active (amended) | [EXP-0069](../experiments/EXP-0069-combat-loop/), [EXP-0074](../experiments/EXP-0074-target-acquisition/) |
| HERO-DMG2-029 | *(re-tested by EXP-0072 with the Units schema decoded, and it holds — on **two** shipped roots, EN and RU, whose `Data.bin` files differ: `attackKind` is `−1` on 48 classes and `3` on eight, never 1 or 2, and the Units collection is byte-for-byte identical between the roots. One mechanism the row asserted implicitly is now an instruction: `−1` does **not** reach arm 0 by being `≤ 0`, because the helper `FUN_00523460` skips the store entirely on `−1` — it reaches arm 0 because the local is **pre-set to 0** at `004f571a`, before the read. A consumer that treats an absent `attackKind` as anything but 0 diverges.)* **The `+0xb7`/`+0xb8` pair is a class's `physicalMin`/spread routed by the `attackKind` column; it is reduced by `target+0xc6` and never by absorption — and no shipped class takes that arm.** In `FUN_004fbc92` the second component is gated only on `A+0x11 + A+0x12 > 0` (`004fbeca`), rolled `A+0x11 + U[0, A+0x12]` (`004fbed7`), reduced by `ftol(v × (100 − target+0xc6)/100 + 0.75)` clamped at 0 (`004fbefa`…`004fbf36`) and added to the total; the absorption subtraction `SUB ECX, target+0xc0` at `004fbe5e` is inside the physical block only, and no hit-roll gate stands on this path. A third component follows — `A+0x13`/`A+0x14`, with `A+0x15` selecting a protection through a five-way jump table at `004fc216` (1→`+0xc4`, 2→`+0xca`, 3→`+0xc8`, 4→`+0xc6`, 5→`+0xcc`; anything else takes the error string at `004fc162`). The **writer** is `FUN_004f5604`'s `attackKind` switch: `<= 0` → `+0xb4`/`+0xb5`; `== 1` → `+0xb7`/`+0xb8` (`004f577a`/`004f5786`); `== 2` → `+0xb9`/`+0xba` **negated**; `== 3` → `+0xb4`/`+0xb5` **and `actor+0x4c \|= 0x10`** (`004f57be OR AL,0x10`) — which identifies `HERO-DAMAGE-022`'s auto-hit override as that arm. The shipped population of the column is exactly `−1` on 48 unit classes and `3` on eight (Bat_Sonic ×4, Bee ×4, which therefore **never miss**), and **1 and 2 on none**, so on a shipped map the second component is zero and its own gate skips it. Instrument: `EnumRefs disp:b7`, whole image on a repaired table — **3 hits, 0 orphan**; the field is a byte, so every byte-wide hit is a candidate and the whole population is listed (a read in `FUN_004e7de3`, the writer above, and `FUN_004f7dfc`'s zeroing store at `004f8440`). `disp:b8` adds only those same two stores inside the actor module; `disp:bb` (`A+0x15`) returns **0 hits anywhere in the image**. **Blind spot, stated:** `+0xb7` is offset 0x11 inside the 0x40-byte live block at `actor+0xa6` that `HERO-MOD-016` shows being folded wholesale from a modifier copy, and a store through that base carries no displacement — so "one writer" is a claim about displacement-addressed writers only | High (the resolver's three components are read whole, the switch is read whole, and the population is a corpus census of the column) / **Medium** on "never non-zero in play", which the modifier-fold blind spot bounds | ● active | [EXP-0069](../experiments/EXP-0069-combat-loop/) |
| HERO-CLAMP-030 | **The blow's three components carry three different clamps, and only the physical one can leave its block negative.** `FUN_004fbc92`, read whole. **Physical**: `dmg = A+0x0e + U[0, A+0x0f]`, the hit test, then `dmg -= target+0xc0` (`004fbe5e`) — and **immediately a clamp at 0**: `004fbe63 CMP dword ptr [EBP + -0x1c],0x0` / `004fbe67 JGE 004fbe70` / `004fbe69 MOV dword ptr [EBP + -0x1c],0x0`. So an over-absorbed physical blow contributes exactly **0**, and `HERO-DAMAGE-022`'s silence beside `HERO-DMG2-029`'s explicit clamp is filled. The clamp sits **before** the damage-kind multiply and there is **none after it**, while components two and three clamp **after** theirs with the branchless `XOR`/`CMP`/`SETL`/`DEC`/`AND` idiom (`004fbf29`…`004fbf36`, and the same five times over in the jump-table arms at `004fc00b`, `004fc05d`, `004fc0af`, `004fc101`, `004fc150`). The asymmetry is load-bearing because the physical divisor is read **unsigned** — `XOR EDX,EDX` then `004fbe7d MOV DL,byte ptr [ECX + EAX*0x1 + 0xce]` — while the other two read theirs `MOVSX` (`004fbeef`, `004fbfd1`): a damage-kind byte above 100 makes `(100 − res)` negative, and the physical component is added to the total **unconditionally** at `004fbeb2` while the third is gated `> 0` at `004fc17d`. **Two rivals killed by instructions, not by argument.** The clamp is not redundant against the multiply, because `004fbe87`/`004fbe8b` skip the multiply entirely when the resistance byte is 0 — the shipped majority, 42…54 of 56 classes per column — and a negative would pass through untouched; and it is not redundant against the return clamp `004fc202`/`004fc206`/`004fc208` (`return max(total, 0)`), because a negative physical component cancels a positive elemental one and `max` cannot restore that. Reachability, censused on **both** roots: **0 of 56** classes carry a `res.*` cell above 100 (max 80) and **0** a protection above 100 (max 100), so no shipped *template* reaches the negative path — but `HERO-FOLD-033`'s fold adds the six damage-kind bytes 8-bit with no clamp, so the live field is not bounded by that census | High (the routine is read whole, each clamp is its own named instruction sequence, and both live rivals are refuted by cited instructions) / **Medium** on "no blow in play reaches the negative path", which is a template census the modifier fold bounds | ● active | [EXP-0104](../experiments/EXP-0104-combat-columns/) |
| HERO-AUTOHIT-031 | **The auto-hit bit does two things, and the second one is published nowhere: it skips absorption.** `004fbe3c CMP dword ptr [EBP + 0xc],0x0` / `004fbe40 JZ 004fbe63` / `004fbe47 MOV CL,byte ptr [EAX + 0x4c]` / `004fbe4a AND ECX,0x10` / `004fbe4f JNZ 004fbe63` — and the jump target is **past** the subtraction at `004fbe5e`, so an attacker carrying `actor+0x4c & 0x10` is reduced by no absorption at all, on top of never missing (`004fbe04`…`004fbe11`). A **null** attacker takes the same exit, so a source-less blow is also unabsorbed. `HERO-DMG2-029` fixes the bit's writer as the units streamer's `attackKind == 3` arm (`004f57be OR AL,0x10`), whose shipped population is **8 of 56** — `Bat_Sonic` ×4 and `Bee` ×4, identical on both roots — so those eight classes ignore armour *and* never miss. A consumer that applies absorption unconditionally over-protects every target of those eight, and `Bee` is 11 of mission 1's 21 hostile placements (`UNIT-M10-018`) | High for the skip (one `JNZ` whose target address is the whole of the claim) / **Medium** that `attackKind == 3` is the only writer of the bit: the sweep covered the `OR AL,0x10` / `OR CL,0x10` / `OR DL,0x10` forms — 11 / 5 / 4 hits, 0 in orphan code, exactly one of them in the actor module at `004f57be` — and is blind to `OR byte ptr [reg+0x4c],0x10` and to a wholesale copy of the containing block | ● active | [EXP-0104](../experiments/EXP-0104-combat-columns/) |
| HERO-HEALTH-032 | **What reaches the health field, read at the write: `max(total, 0)`, subtracted 16-bit, with no guard on either side.** The resolver's last act is `004fc202 CMP dword ptr [EBP + -0x24],0x0` / `004fc206 JG 004fc20c` / `004fc208 XOR EAX,EAX`, so it returns 0 for any non-positive total. The melee strike then does `004fbabb MOV CX,word ptr [EAX + 0x94]` / `004fbac2 SUB CX,word ptr [EBP + -0xc]` / `004fbac9 MOV word ptr [EDX + 0x94],CX` — a **16-bit** read/subtract/store, with no floor at 0, no cap at `healthMax` and no re-read. Three consequences a consumer must carry. A blow **can never raise** health, which is why both readings of the physical clamp agree at the write and why the clamp's real effect is *inside* the total rather than at the field. Health goes arbitrarily negative, which is exactly what `HERO-DEATH-026`'s decay machine and its `−1000` / `−10001` pins consume. And the subtraction is modulo 2^16 — unreachable on shipped numbers, since three components of at most `255 + 255` cannot reach 32 768, but a real width. `FUN_004fba0e` is the **only** `vt+0x4c` call site in the actor module: `re:CALL dword ptr \[E.. \+ 0x4c\]` over the whole image returns **34 hits / 26 owners / 1 in orphan code**, and `004fbab2` is the sole one in `004f….`; blind to a call through a register Ghidra resolved to no target | High (every step is a named instruction and the return clamp is read at the return, not inferred from the arithmetic). **This row does not claim the strike is the only writer of `+0x94`**: `disp:94` word-wide names `FUN_004feadb`, `FUN_00502c51`, `FUN_0050177e`, `FUN_00501a22`, `FUN_0053cb30` and `FUN_005492a0` as further stores, none of them read here. *(amended by EXP-0122: all six are read, and the complete word-wide writer set of `+0x94` is **36 stores**, each attributed to what causes it — six of them the server clock's and thirty needing a blow, a spell, an effect, a heal, a removal from the map, a constructor, a template, a load or a script action. Two instruments reduced independently agree on the same 36 with nothing on either side. `HERO-ZERO-070`.)* | ● active (amended) | [EXP-0104](../experiments/EXP-0104-combat-columns/), **[EXP-0122](../experiments/EXP-0122-corpse-clock/)** |
| HERO-FOLD-033 | **`FUN_004fa4eb` folds fourteen fields, not two — the six damage-kind bytes among them, 8-bit and unclamped.** Read whole, `004fa4eb`…`004fa590`: a first loop `i = 0..5` adds `src[+0x04 + 2i]` into `dst[+0x04 + 2i]` as `u16` (`004fa519` / `004fa51e` / `004fa529`, bound `004fa507 CMP …,0x5`); a second loop `i = 0..5` adds `src[+0x10 + i]` into `dst[+0x10 + i]` as `u8` (`004fa554 MOV CL,byte ptr [EDX + 0x10]` / `004fa557 ADD CL,byte ptr [EAX + 0x10]` / `004fa560`); then `+0x00` and `+0x02` as `u16` (`004fa56b`…`004fa588`). With `dst = actor+0xbe` that is defence, absorption, the **six** protections `+0xc2…+0xcc` — index 0 at `+0xc2` filled by no column, exactly like `+0xce` — and the **six** damage-kind bytes `+0xce…+0xd3`: `2 + 2 + 12 + 6 = 0x16`, the block's own `memset` size. `HERO-ARMOUR-018` describes this routine as adding `src+0` into `dst+0` and `src+2` into `dst+2`; those are two of the fourteen adds, and that row is amended rather than retracted because what it used the routine for — identifying defence and absorption — is untouched. What is new is the consequence: the damage-kind resistance `HERO-CLAMP-030`'s physical block reads **unsigned** has an unclamped 8-bit adder, so an item or effect modifier can carry the live field past 100 — and past 255, wrapping | High (the routine is read end to end, both loop bounds are its own `CMP …,0x5`, and the block size is the `memset` argument `HERO-MOD-016` already cites) | ● active | [EXP-0104](../experiments/EXP-0104-combat-columns/) |
| HERO-DERIVE-034 | **A hero's melee pair and its to-hit sibling are built by one routine out of additive terms, and the two ends of the pair do not carry the same terms.** `FUN_004f7dfc` (the actor's `vt+0x50`) writes, in this order: `actor+0xb5 = ftol(1.1^Body / 20)` at `004f82a6`, then `actor+0xb4 = actor+0xb5` at `004f82b8` — the **spread is computed and the base copied from it**, so the bare pair is equal; then `actor+0xa6 = ftol((1.1^Body + 1.1^Reaction) / 5)` at `004f82f8`; then, gated on `actor+0xb6 > 0` by one `JLE` at `004f83bb`, `actor+0xa6 += 3 x skill[actor+0xb6]` (`004f83e6`) and `actor+0xb4 += skill[actor+0xb6] / 5` (`004f8419`, `IDIV` by 5, truncating); then the modifier fold at `004f84b5`. So `base = ftol(1.1^Body/20) + skill/5 + mod(+0xf4)` and `spread = ftol(1.1^Body/20) + mod(+0xf5)`, and since the roll is `base + U[0,spread]` the **Body term enters the minimum once and the maximum twice** while the **skill term reaches the base alone**. The three constants are read from the shipped bytes through the PE section table rather than from a decompiler: `[0x0059bcb0] = 20` (`004f8298`), `[0x0059bc58] = 5` (`004f82ea`), and `FUN_004f7233`'s base is `0x3FF199999999999A` = 1.1, assembled by the two `PUSH`es at `004f723f`/`004f7244`. `__ftol` (`0055458c`) sets `RC=11` before `FISTP`, so it truncates toward zero and every `x + 0.5` in this chain is round-half-up. Both bytes are **u8, wrapping, never clamped** — the store is `MOV byte ptr [..],AL` off a 32-bit `ftol`, and the resolver reads both ends unsigned (`004fbcba XOR ECX,ECX` then `MOV CL,byte ptr [EAX + 0xe]`; `004fbda7` the same for `+0xf`), so a Body above 110 wraps the term. `Weapon::Equip` calls `vt+0x50` itself at `0050e0ea`, which is why a sheet is right the instant an item is equipped | High (every term is a named store inside the one routine that writes these fields for a hero; the constants are read out of the image; and the two live rivals — a multiplicative stat factor, and a term symmetric across the pair — are each killed by an instruction rather than by absence, see `HERO-FOLD-035`) | ● active | [EXP-0105](../experiments/EXP-0105-damage-fold/) |
| HERO-FOLD-035 | *(The ADD and whole-body instruction counts are corrected by SAV-HUMFOLD-446; the seven destinations and assignment below stand.)* **The equipment fold into the `+0xa6` block is seven plain `ADD`s and one assignment — there is no multiply anywhere on the path from an item to a damage number.** `FUN_004f54f8(this = actor+0xd4, arg = actor)` dispatches to two block folds: `FUN_005233a0` to `FUN_004fa4eb` for the `+0xbe` block (`HERO-FOLD-033`), and **`FUN_004fa818(dst = actor+0xa6, src = actor+0xe6)`** for this one. That routine is 53 instructions, read end to end over `004fa818`…`004fa8ac`: `dst+0x00 += src+0x00` as u16 (to-hit), then `dst+0x0e`, `+0x0f`, `+0x11`, `+0x12`, `+0x13`, `+0x14` each `+=` as u8, then `dst+0x15 = src+0x15` **assigned**. It contains no `IMUL`, no `FMUL` and no shift. `dst+0x10`, the attack kind, is **not** folded — `Weapon::Equip` writes the live field directly at `0050e0dc`. The consequence a consumer needs is that every contribution to a damage number is an integer addition, so the only non-linearity in the whole chain is `1.1^Body` and two truncations. **Instrument and blind spot, stated.** The complete byte-wide writer set of `actor+0xb4`/`+0xb5` is `EnumRefs disp:b4 disp:b5` on a repaired table — 159 and 8 hits, of which 18 and 8 are byte-wide, 0 in orphan code — and it is `FUN_004f7dfc` (this row), `FUN_004f5604` (the **Units** streamer, `HERO-DMG2-029`, which a hero never reaches) and `FUN_004f6ded` (the constructor defaults, 1 and 1). **This fold is invisible to that sweep**, because it stores through a pointer rebased to `actor+0xa6` and therefore carries displacement `0x0e`/`0x0f`; it is enumerated here positively instead, by reading `FUN_004f54f8`'s two calls, which is the blind spot `HERO-DMG2-029` named and did not close. A wholesale `memcpy` of the actor would carry no displacement at all and no displacement sweep can see one. The modifier pair `+0xf4`/`+0xf5` has exactly three contributors — `FUN_0050def2` (Weapon::Equip, `ADD`), `FUN_0050e1b5` (Weapon::Unequip, `SUB`) and `FUN_00501a22` (the effect apply/remove pair) — and **no armour or shield routine is among them**, so a shield or a breastplate can never move a damage number | High (the fold is read whole, and its absence of a multiply is a property of 53 transcribed instructions rather than of a search; the writer sets are complete populations under the field's own width, with the one blind spot named and then closed by direct reading) | ● active (instruction counts corrected) | [EXP-0105](../experiments/EXP-0105-damage-fold/) |
| HERO-STATDMG-036 | **Of the four stats, only Body reaches damage and only Body and Reaction reach to-hit — established by an exhaustive read census inside the sole writer, not by failing to find one.** In `FUN_004f7dfc`, the only routine that writes `actor+0xb4`/`+0xb5`/`+0xa6` for a hero, the stat words are read as follows. **Mind** (`+0x88`): **four** reads — `004f7eb9`, `004f7ed9`, `004f7eea`, its own cap clamp, and `004f80e1`, the sight term. **Spirit** (`+0x8a`): its cap clamp (`004f7f01`/`004f7f21`/`004f7f32`), mana (`004f801f`, `004f80a2`), the five protections (`004f848c`) and their clamps in the tail. Neither appears on the damage arm or the to-hit arm at all. **Body** (`+0x84`): eight reads, among them the damage term at `004f8288` and the to-hit term at `004f82c1`. **Reaction** (`+0x86`): nine reads — cap, sight, speed, defence, and the to-hit term at `004f82d7` — and **none of them is the damage arm**. So no stat enters multiplicatively, through a table, or through a threshold: the only stat expressions on these two paths are `ftol(1.1^Body/20)` and `ftol((1.1^Body + 1.1^Reaction)/5)`. That is why the two outputs move together in play, Body raising both, while the pair's own ends do not — Body is added to base and to spread, the skill only to base. **The census is over one routine rather than over the image**, which is what makes it decisive: the fields' complete writer set is fixed separately by `HERO-FOLD-035`, so no other routine can introduce a stat term for a hero | High (a positive enumeration inside the only writer, every read attributed to a named output, rather than an absence inferred from a whole-image sweep) | ● active | [EXP-0105](../experiments/EXP-0105-damage-fold/) |
| HERO-BARE-037 | **The unarmed hero is the same routine with one gate closed, and below Body 32 he does literally zero damage.** `Weapon::Unequip FUN_0050e1b5` subtracts the three modifier fields its inverse added and writes **`actor+0xb6 = 0`** at `0050e2f4`, then calls `vt+0x50` at `0050e303`. With `actor+0xb6 = 0` the `JLE` at `004f83bb` skips **both** skill terms, and with the modifier pair back at `(0,0)` the fold adds nothing — so `base = spread = ftol(1.1^Body / 20)` and the roll is `[d, 2d]`, which is the reading `HERO-DAMAGE-022` retracted `HERO-COMBAT-011`'s degenerate range in favour of. `1.1^Body >= 20` first at **Body 32**, so a bare-handed hero rolls `0-0` at Body 15, `1-2` at 32, `2-4` at 39 and `3-6` at 43. There is **no hidden bare-hands weapon**: `Data.bin` does ship a Weapons row `BareHands` (`@.physicalMin` 1, `@.physicalMax` 2, `@.attackType` 3) which would have made this row wrong, and a `StrDump range:400000:5f0000` ASCII census over the whole mapped image finds **0** occurrences of that string, so no instruction can name it — the row is reachable only from a Units `EquipItem` column, i.e. a monster template. Instrument: a string census over the image's mapped range, which is immune to the function-table blind spots a `callto:`/`disp:` sweep has; what it cannot see is a name assembled at runtime from fragments | High (both halves are instructions — the `0xb6` clear and the gate it closes — and the one live alternative, an implicit `BareHands` equip, is excluded by a whole-image string census rather than by not having found it) | ● active | [EXP-0105](../experiments/EXP-0105-damage-fold/) |
| HERO-SHEET-038 | **The character sheet prints `[base, base + spread]`, which closes `HERO-ARMOUR-018`'s open display clause and makes every figure in this ledger directly comparable with what the owner reads on screen.** `FUN_00460480` formats the damage line through the literal `%d-%d` at `0x005bcbd8` (`EnumRefs refto:5bcbd8`: **2 hits, 1 owner, 0 orphan**). At `00460aa1`…`00460ac2` it zero-extends both bytes and **adds** them before pushing: `XOR EAX,EAX` / `XOR ECX,ECX` / `MOV AL,byte ptr [EBP + 0x148]` / `MOV CL,byte ptr [EBP + 0x149]` / `ADD ECX,EAX` / `PUSH ECX` / `PUSH EAX` / `PUSH 0x5bcbd8`. The same shape repeats at `004609bc`…`004609dd` over an adjacent pair of stack bytes. `+0x148`/`+0x149` are the block `FUN_0047bf40` fills from `actor+0xb4`/`actor+0xb5` at `0047c27f`/`0047c28b`, after calling `vt+0x50` at `0047c236`. So the first number on the sheet is the base and the second is base+spread — **exactly the roll's own bounds** — and this is a third, independent kill of the `(min, max)` reading, alongside the resolver and the weapon fill that `HERO-DAMAGE-022` used. `HERO-ARMOUR-018`'s residual, *under the read fill the same sheet requires `+0xf5 = 8` if the client prints `[base, base+spread]`, and that display routine is still unread*, is discharged: it does. *Origin:* the owner's 8–16 sheet reading is consistent with a weapon whose scaled pair is `(8,8)`, of which `Data.bin` admits several; which item he carried is still not recorded, so that row's Medium on the reconciliation is unchanged and nothing here rests on it | High (the format literal's reference set is complete at 2 hits, and the arithmetic is three transcribed instructions ending in the `PUSH` order) | ● active | [EXP-0105](../experiments/EXP-0105-damage-fold/) |
| HERO-START-039 | **A new campaign hero is handed exactly one weapon, chosen from ten string literals by a jump table on his one trained skill — and which of the two sets he gets is a conjunction of two tests, neither of which a shipped campaign is here shown to satisfy.** `FUN_004f992c(slot, value)` zeroes skill slots 1..5 (`004f99c4`), writes `skill[slot] = value` (`004f99e4`), recomputes, then `CMP ECX,0xa` at `004f9a00` picks between two arms. The **ordinary** arm (`value <= 10`) dispatches on `slot - 1` through a five-entry jump table at `0x004fa435` (`004fa087`) and `new(0x84)`s a `Weapon` from that slot's literal: **`Iron Short Sword`** (`0x005c7ed4`, slot 1 Blade), `Uncommon Bronze Axe`, `Uncommon Bronze Mace`, `Bronze Pike`, `Uncommon Wood Short Bow`, plus `Wood Staff {castSpell=Fire_Arrow:10}` on the mage arm. The **high** arm (`value > 10`) gives `Uncommon Steel Two Handed Sword`, `Uncommon Steel Axe`, `Uncommon Steel Mace`, `Uncommon Steel Pike`, `Uncommon Magic Wood Short Bow` and the `Fire_Arrow:20` staff. All ten resolve **10/10** through `FUN_0050d670`'s two lookups on both roots (`ITEM-SCALE-017`). The value is chosen at `0047c191`…`0047c1bf` by a **conjunction**: `[0x005eb5a4] == 2` **and** `campaign+0x6bc == 2` push `0x14`; anything else pushes `0x0a`. So `HERO-SKILL-009`'s *20 when `[0x005eb5a4] == 2`* is amended with the second condition it was missing. `EnumRefs refto:5eb5a4` returns **seven writes and not one of them is the immediate 2**; the only two writers that could deposit it, `FUN_00470f35` and `FUN_004713f5`, are reachable solely as `.rdata` slots `005a8144`/`005a8154`, i.e. MFC message-map handlers. Instrument blind spot: `refto:` is reference-manager based and would miss a write from undisassembled code or through a computed address | High for the dispatch, the ten literals and the conjunction, each read at instruction level / **Medium** for *a shipped single-player campaign takes the skill-10 arm*, which rests on the absence of an immediate-2 writer rather than on reading the menu chain that sets the mode | ● active | [EXP-0105](../experiments/EXP-0105-damage-fold/) |
| HERO-APPEAR-040 | **What the wire carries about a hero's appearance is the actor's `typeID` and a face byte, under one mask bit — and the class accessor is nine instructions with no arithmetic in it.** `FUN_004e7de3`, the actor serializer, gates two pushes on bit `0x4000` of the field mask at `[EBP+0x10]`: `FUN_00521890(actor)` at `004e8236`, then `byte [actor+0x4b]` at `004e8259`, each handed to the byte writer `FUN_004f0dfc` with `0x4000` as its tag. `FUN_00521890` is the whole of the class computation and it is `MOV AX, word ptr [EAX + 0xe]` (`0052189a`) — the `typeID` `HERO-SPEED-008` already reads at that displacement — so **nothing is derived on the producing side**. The client decodes the pair into two globals, `[0x005cd5c8]` and `[0x005cd5cc]`, at `004111b2` and `004111e4`, both gated on the same `0x4000`, and `EnumRefs refto:5cd5c8` returns **8 hits, 1 owner, 0 orphan**: that one write and seven reads, all inside `FUN_004104e8` | High (the mask test, both pushes and the accessor's single load are named instructions; the class global's writer set is a whole-image reference enumeration with its instrument and its 0-orphan count stated) | ● active | [EXP-0113](../experiments/EXP-0113-hero-appearance/) |
| HERO-APPEAR-041 | **The id range `[0x20, 0x40)` is the hero range, and for it the client throws the shipped class away: it banks two bits and forces the drawn class to `1`.** `drawable+0x20` is assigned the wire class byte at `00411746` and is the subscript the frame selector uses on the `units.reg` class array (`0045bf3a` `MOV EAX,[ESI+0x20]`, `0045bf44` `MOV EBX,[ECX + EAX*0x4]`). `FUN_0045f850`, called one instruction later at `00411755`, splits on it: `CMP EDX,0x40 ; JGE` (`0045f872`) and `CMP EDX,0x20 ; JL` (`0045f87a`) send everything outside the range to `0045f8c8` — both branch targets fixed by their own `rel8`, `0x51` and `0x4c`, so the hero block is exactly the 76 bytes `0045f87c..0045f8c8`. Inside it: `LEA ECX,[EDX + -0x21]` (`0045f880`), then `TEST CL,0x1` → `OR AL,0x4` and `TEST CL,0x2` → `OR AL,0x2` into `drawable+0x18c` (with bits 0 and 3 set unconditionally by `OR AL,0x9`), then `MOV dword ptr [ESI + 0x20],0x1` (`0045f8bc`). By `HERO-CLASS-013`'s constructor arithmetic `typeID - 0x21` is `gender` for a fighter and `gender + 2` for a mage, so `+0x18c` **bit 2 is the sex axis and bit 1 the fighter/mage axis**; `1` is `units.reg` `ID 1`, `Unarmed Fighter`. The shipped roster occupies `1..27` and `64..80` only, so `[0x20,0x40)` is a range no class ever holds and the discard costs nothing | High (every branch, immediate and store is a named instruction, and the two range tests are settled by branch displacements in their own listing rather than by a second decode; the bit-to-axis mapping is `HERO-CLASS-013`'s already-published addend/base split applied to a `LEA`) | ● active | [EXP-0113](../experiments/EXP-0113-hero-appearance/) |
| HERO-APPEAR-042 | **A player's character is drawn as whatever its visible equipment says, through a seventeen-arm name chain — the drawn class is DERIVED, and the derivation is a string comparison.** `FUN_0045fb00` (`00411760`, the instruction after `FUN_0045f850`) returns immediately unless `drawable+0x18c & 1` (`0045fb2b`), the bit the hero arm sets. It then builds a body name: slot 0 of a twelve-entry array at `drawable+0x15c` gives `(byte[slot+6] & 0x1f) - 1` (`0045fbc2`…`0045fbc8`), which indexes a registry string through `FUN_004687f0` on the section object `0x5ea220` (`0045fbca`); a non-null slot 1 (`+0x160`, `0045fbf2`) appends `"_"`; and when `+0x18c & 2` (mage) turns the name out as `"unarmed"` it is replaced by `"mage"` (`0045fc78`), or by `"mage_st"` when the action code `+0x84` is `6`, the dying action (`0045fb3c`, `0045fbae`). The name then runs a chain of seventeen compare-and-store arms over `0045ff64..00460440`, each `MOV dword ptr [EAX+0x20], imm32`, mapping `unarmed→1 unarmed_→2 swordsman→3 swordsman_→4 swordsman2h→5 axeman→7 axeman_→8 axeman2h→9 clubman→10 clubman_→11 pikeman→12 pikeman_→13 archer→14 bowman→14 xbowman→15 mage_st→24 mage→23` — **sixteen distinct `units.reg` ids from seventeen names**. Both roots, reproduced by a second instrument that reads the `BE`/`BF` literal loads and the `C7 40 20` stores as raw bytes through the PE section table with no disassembler in the path (`tools/heroappear -mode image`) | High (the gate, the two slot expressions, the three special cases and all seventeen arms are named instructions, and the arm table is reproduced independently from the image's own bytes on both roots) / **Medium** for reading slots 0 and 1 as *weapon* and *shield*: what is established is that slot 0 supplies the name and slot 1 the `"_"`, and the labels come from `UNIT-EQUIP-005` and from the sheet names, which are not evidence | ● active | [EXP-0113](../experiments/EXP-0113-hero-appearance/) |
| HERO-APPEAR-043 | **A hero's pixels do not come through `units.reg`'s `File` at all: the sheet is loaded by a composed path, and the directory is an armour material's `Path` out of `material.reg`.** `FUN_0045fb00` assembles `graphics\units\` (`0045fd28`) + a directory + a separator + the body name + `\sprites.256` (`0045fe7a`), and the same with `\spritesb.256` (`0045fea6`), then `operator new(0x24)` — `CGameBitmap`, `UNIT-RTTI-024` — twice through the `.256`/`.16a` constructor `FUN_00428c50` (`SPR16A-RDR-017`) into `drawable+0x194` and `+0x198` (`0045ff08`, `0045ff39`). The directory is a three-way: a mage always takes `heroes` (`0045fd56 TEST AL,0x2`, `0045fdca`); a fighter with no object in slot 7 (`+0x178`) takes `heroes_l` (`0045fd64 JZ`, `rel8 = 0x4f`, target fixed at `0045fdb5`); otherwise `stringPool[[0x005eb4dc] + (word[slot+6] >> 12)]` (`0045fd66`…`0045fd78`), the top four bits of slot 7's `u16`. `0x005eb4dc` has **1 reference image-wide** and is BSS, so its table is not readable statically; it is `units/material.reg`, whose sixteen `Material0..15` blocks (`REG-MAT-042`) carry `Path` values that are **`heroes` for 0–7 and 14 and `heroes_l` for 8–13 and 15**, byte-identical on both roots. `+0x19c` caches the last name and `+0x1ac` the last material index, and the routine returns before releasing anything when neither changed (`0045fd00`). Corpus, both roots: `graphics.res` ships **64** `units/heroes*` nodes over exactly two directories and sixteen body names; the cross product leaves **8 of 68** combinations without a complete sheet pair, and they are exactly `bowman` (no sheet anywhere, an arm sharing `archer`'s id) and `mage`/`mage_st` under `heroes_l`, the directory a mage can never take. No shipped body directory lacks an arm; the two roots' node sets are identical | High (the path segments, the three-way and the shift are named instructions, one branch target fixed by its own displacement; `material.reg`'s content is read with the published framing on both roots; the cross product is an exact set comparison whose two gaps are each explained by an instruction) | ● active | [EXP-0113](../experiments/EXP-0113-hero-appearance/) |
| HERO-APPEAR-044 | **It changes while the game runs: the appearance is recomputed on the ordinary per-actor state message, not only at creation, and the server picks the carrying opcode on the same hero range.** `EnumRefs callto:45fb00` returns **10 hits, 5 owners, 0 orphan**. Six are inside `FUN_004104e8`, and attributed against the dispatcher's own two tables (`ANIM-MSG-005`'s instrument, re-run) four fall in arm `0x00410e0e` — opcodes **`0x6c`, `0x6e`, `0x6f`, `0x70`**, the field-masked state sync that also carries health, mana, facing, position and the corpse stage — one in arm `0x00412cf7` (**`0x9c`**) and one in arm `0x00412fe3` (**`0x76`**). Those last two are the arms that rebuild the slot array: `0x9c` loops twelve slots (`00412e40 CMP …,0xc`), releases each old entry, allocates `0x24` bytes per non-zero `u16` at `msg+0xc+2i` and stores it at `slot+0x6` (`00412f46`), sets `+0x18c` bit 3 and calls the recompute. The four remaining owners are character-screen routines — `FUN_0047aee0`, `FUN_004922f0`, `FUN_00492410` and `FUN_0047c590`, the last being the routine `HERO-CLASS-013` names as the setter of the chargen bits. On the producing side `FUN_004e873b` chooses between the two opcodes with the **same** range test, `CMP EAX,0x21 ; JL` (`004e87dd`) and `CMP EAX,0x40 ; JL` (`004e87ef`) over `FUN_00521890`'s result: `0x76` when the typeID is in `[0x21,0x40)` or when the receiving player owns the actor (`004e8816`), `0x9c` otherwise. It is called from the actor serializer `FUN_004e7de3` (`004e864e`), recursively over participants (`004e879e`), and twice from `FUN_00539be0` | High for the call enumeration, the opcode attribution and both range tests (the attribution uses static table decoding with no disassembler in the path, and the enumeration states its instrument and 0-orphan count) / ~~**Unknown** what `FUN_00539be0` is: it was located as the non-broadcast trigger and not read, so *which player action* schedules the send is not established here~~ — *discharged twice. [EXP-0114] read it: it is `TRIG-ACT-004`'s 34-arm instant dispatcher, so **no player action schedules a re-send — a script action does**. [EXP-0119] then bounded it from the corpus (`HERO-FIGURE-064`): the two arms are opcodes **14** and **15**, they are the only arms that reach `FUN_004e873b`, and **neither is authored on either root** — so on shipped content the equipment message is only ever produced by the actor serializer* | ● active (amended) | [EXP-0113](../experiments/EXP-0113-hero-appearance/) |
| HERO-APPEAR-045 | **Of `HERO-CLASS-013`'s two axes only one reaches the drawing: the sex bit is put on the drawable and neither routine that turns a drawable into pixels reads it.** `FUN_0045f850` writes `+0x18c` bit 2 from `typeID - 0x21` bit 0 (`0045f896`/`0045f8a1`). `FUN_0045fb00` reads `+0x18c` at `0045fb2b`, `0045fb65`, `0045fc39` and `0045fd56` and tests **only bits 0 and 1**; `FUN_0045bf00` reads `+0xe0`, `+0x6c`, `+0x20` and `+0x74` and does not read `+0x18c` at all, the one `+0x18c` test in the draw path being `REG-UNITS-061`'s bit `0x80` in `CUnit::vt+0x38`. Both routines were read end to end. So the body name, the directory, the sheet pair and the drawn class id are all **independent of the character's sex**, and the four archetypes reduce to two for drawing purposes. Which of `FUN_0051cb90`'s two values is male is not established and is not needed | High for the scoped statement — an exhaustive read of the two named routines, each in full, rather than an absence in a sweep. **The unscoped form is deliberately not claimed:** `disp:18c` returns 232 hits over 81 owners across several classes and cannot discriminate, so whether bit 2 is read anywhere else in the image is open | ● active | [EXP-0113](../experiments/EXP-0113-hero-appearance/) |
| HERO-APPEAR-046 | **The name-to-class mapping is confirmed by a quantity neither side of it was fitted to: every hero body sheet holds exactly the frame count its assigned class record predicts, and the two that fall short fall short by exactly the blocks a hero can never reach.** For each of the sixteen shipped `units/heroes*/<name>/sprites.256`, the frame count walked by the verified `.256` reader is compared against `SPR256-UNIT-024`'s block arithmetic applied to the `units.reg` record `HERO-APPEAR-042`'s chain assigns that name — a number computed in EXP-0039 from the class's own `*Phases` scalars, years of experiments before this mapping existed. **Fourteen of sixteen are exact**: `unarmed` 129, `swordsman`/`swordsman2h`/`axeman`/`axeman_`/`axeman2h`/`clubman`/`clubman_`/`unarmed_`… 152, `pikeman`/`pikeman_` 136, `archer`/`xbowman` 168, `mage` 144, `mage_st` 216. The two exceptions are `unarmed_` (id 2) and `swordsman_` (id 4), each **56 short of its predicted 208** — and `8 × DyingPhases(4) + 8 × BonePhases(3) = 56` on both, `Flip = 0` giving eight stored directions (`REG-UNITS-051`). Those two ids are exactly the classes `REG-UNITS-050` makes the *corpse* sheets for the whole human family, and a hero never dies in either body: `FUN_0045fb00` forces the name to `"unarmed"` while the action code is 6, or to `"mage_st"` for a mage (`0045fb3c`, `0045fbae`). So the shipped art carries the dying and bone blocks in precisely the two bodies a dying hero is forced into and omits them from precisely the two it is forced out of. This is the strongest single check in the experiment because **neither number was chosen to fit the other**: the mapping comes from seventeen `MOV` immediates in `rom.exe`, the prediction from a registry's scalars, and the measurement from an archive's bytes | High (three independent measurements — an image's instruction immediates, a registry's `*Phases` fields through already-published block arithmetic, and a walked sprite payload — agreeing on 14 of 16 with both residuals equal to an independently derived constant; the alternative that the agreement is coincidental is excluded by the residual being *exactly* `56` on both and by the two exceptions being the two classes an unrelated claim already singles out) / **Medium** for the causal gloss that the art was *authored* to the dying rule rather than the rule written to the art — the arithmetic is symmetric and this experiment cannot order them | ● active | [EXP-0113](../experiments/EXP-0113-hero-appearance/) |
| HERO-APPEAR-047 | **Only a humanoid actor ever ships visible equipment, the sender fans out one message per player, and the wire slot is the equipment slot minus one.** `FUN_004e873b` returns at `004e87cb` without sending anything unless `actor->vt+0x30()` is true at `004e87c4` — `ITEM-EQUIP-006`'s humanoid predicate, `FUN_005232d0` (returns 0) on the base actor class and `FUN_00523530` (returns 1) on both human classes. With no named recipient (`arg2 == 0`) it walks the player list at `[0x00609544]` and **calls itself** once per player (`004e879e`). Both of its arms then visit the same fields in the same order: `actor+0x74`, `actor+0x78`, and `actor+0x198+4i` for a loop whose induction variable runs **3..12** (`004e88af` sets 3, `004e88c1 CMP …,0xc / JG`; the `0x76` arm repeats it at `004e8a4e`/`004e8a60`). The `0x9c` arm writes them to `msg+0x0c`, `msg+0x0e` and `msg+0xa+2i` (`004e887b`, `004e88a0`, `004e88ee`), so the twelve wire slots `k = 0..11` sit at `msg+0xc+2k` and carry equipment slot `k+1` in the numbering the take-off arm of command `0x22` uses — `slot == 1` is `actor+0x74`, `slot == 2` is `actor+0x78`, `3 <= slot <= 12` is `[actor + slot*4 + 0x198]`. An empty slot is sent as `0` rather than omitted (`004e8884`, `004e88a9`, `004e88f5`) | High (the gate, the two loop bounds, every store and every source displacement are named instructions in a routine read end to end; `EnumRefs callto:4e873b` = 4 hits / 3 owners / 0 orphan) | ● active | [EXP-0115](../experiments/EXP-0115-held-item/) |
| HERO-APPEAR-048 | **The two equipment opcodes do not carry the same thing: `0x9c` carries twelve bare `u16`, `0x76` carries twelve serialised item records.** `HERO-APPEAR-044` describes the `0x9c` shape; the `0x76` arm was read here and differs. It writes `msg+0x9 = 0x76`, `msg+0xc = 1`, and then calls the item record writer `FUN_00509229` once per slot (`004e8996`, `004e89ef`, `004e8a87`); an **empty** slot is not omitted but filled by constructing a throwaway `Item` on the stack (`FUN_00507f42`), serialising it and destroying it (`004e89a4`/`004e89c8`/`004e89d7`). The record is `7 + len` bytes: `rec+0` is `FUN_00525da0(item)`, nine instructions returning `word[item+0x40]` — the same value the `0x9c` arm sends; `rec+2` is `item+0x42`, the stack count; `rec+4` bit 7 is `item+0x14 != 0` and bit 6 is `item+0x8 != 0`; `rec+6` is a length and `rec+7..` a blob. The client arm at `00412fe3` starts at `msg+0x13` (`004139a1 ADD EDX,0x13`), loops twelve (`004139c5`), and maps the record onto the slot object: `rec+0` to `slot+0x6` (`00413ac7`), `rec+2` to `slot+0x10`, `rec+4`/`rec+5`/`rec+6` to `slot+0x8`/`+0x9`/`+0xa`, and `malloc(len)` + `memcpy` of `rec+7` into `slot+0xc` (`00413b3e`, `00413b6f`), advancing by `7 + len` (`00413c5d`). A `rec+0` of zero means the slot is empty (`00413ab2`). **A consumer that decodes `0x76` as twelve `u16` at `msg+0xc` decodes garbage** | High (both arms read end to end at instruction level, producer and consumer, with every field's source and destination a named instruction) | ● active | [EXP-0115](../experiments/EXP-0115-held-item/) |
| HERO-APPEAR-049 | **The visible-equipment `u16` has four fields and every bit of it is consumed.** `FUN_00483c80` reads `word[slot+0x6]` at `00483c98` and formats it two ways. When `(v & 0x0f00) == 0x0e00` (`00483ca7`/`00483cad`) it uses `"%02d%02d%03d"` (`0x5beefc`) on `v>>12`, `(v>>8)&0xf` and `v & 0xff`; otherwise `"%02d%02d%1d%02d"` (`0x5beeec`) on `v>>12`, `(v>>8)&0xf`, `(v>>5)&7` and `v & 0x1f` (`00483ced`…`00483d01`). Both forms produce exactly seven digits. Calling the fields **A** = bits 15..12, **B** = 11..8, **C** = 7..5, **D** = 4..0: A is `HERO-APPEAR-043`'s `material.reg` index, read from slot 7 by `FUN_0045fb00` as `word[slot+6] >> 12`, and D is the same five bits that routine reads from slot 0 as `(byte[slot+6] & 0x1f) - 1`. So `HERO-APPEAR-042`'s nine consumed bits are D of one slot and A of another, and the other seven bits of each are consumed here. The value is assembled by `FUN_00525d60`, twenty-one instructions: `((arg3 & 0xff) << 12) \| ((arg1 & 0xff) << 8) \| ((arg2 & 0xff) << 5) \| (arg4 & 0xff)` | High (the formatter and the builder were each read whole; the inversion of all 928 shipped leaf names through the formatter over the entire 65 536-value domain produced 928, with 0 unproduced and 0 ambiguous, so a layout off by one bit anywhere is excluded) | ● active | [EXP-0115](../experiments/EXP-0115-held-item/) |
| HERO-APPEAR-050 | **Field B is the equipment slot number, and the corpus discriminates that against every neighbouring offset.** The figure compositor `FUN_0045ed10` builds a `secondary` sheet only for drawable slots 3, 8 and 9 (`0045f054`, `0045f05d`, `0045f062`) and for slot 7 when `drawable+0x18c & 2` (`0045f067`, `0045f070`); drawable slot `k` is equipment slot `k+1` (`HERO-APPEAR-047`), so under `B = equipment slot` the shipped `secondary` sheets must occupy `B` in `{4, 8, 9, 10}` exactly. They do, on both roots. The probe prints the predicted set at five offsets, `-2..+2`, and only `+1` matches. The two sides were not fitted to each other: one is a chain of `CMP EBP,<n>` in `rom.exe`, the other a directory listing in `graphics.res`. Over `primary` sheets B occupies `{1, 2, 4, 5, 6, 7, 8, 9, 10, 12}` — **ten** of the twelve slots carry shipped content and slots 3 and 11 carry none. B is written from the item's own kind: the literal `1` in the `Weapon` builder `FUN_0050d8e8` (which also stores it at `item+0x50`), `2` in `FUN_0050cfa2`, `1` in `FUN_0050d5d4`, `byte[item+0x50]` in `FUN_0050c53a` and the caller's argument in the base `Item` builder `FUN_005081f1` | High (a discriminating corpus test against an instruction-level rule, reproduced on both roots, where four of the five candidate offsets are excluded) / **Unknown** what distinguishes the two builders that both push the literal 1 | ● active | [EXP-0115](../experiments/EXP-0115-held-item/) |
| HERO-APPEAR-051 | **All twelve visible-equipment slots are read — by a routine `callto:` cannot see — and each occupied one contributes a per-item sheet under one of four figure directories.** `FUN_0045ed10` loops `EBP = 0..11` over `drawable+0x15c+4*EBP` (`0045ee58`, `0045ef69`), skipping an empty slot (`0045ee61`), and for each occupied one composes `graphics\equipment\` (`0x5bcad0`) + a figure directory + `primary\` (`0x5bca9c`) + `FUN_00483c80(slot)` + `.256`, allocates `0x24` bytes and hands the path to the `.256` constructor `FUN_00428c50` (`0045efc3`, `0045f00e`, `0045f031`). The figure is one of four literals behind `JMP dword ptr [EAX*4 + 0x45f834]` on `(drawable+0x18c & 6)` (`0045edda`…`0045ede8`): `mfighter\`, `mmage\`, `ffighter\`, `fmage\` — **the mage bit and the sex bit together**. A `secondary\` sheet (`0x5bca90`) is built only at `EBP` 3, 8, 9 and at 7 for a mage. After the loop it composes `"%s%d.256"` (`0x5bca7c`) from the figure directory and `drawable+0x24`, the face byte, and for a drawn class in `[0x11, 0x15]` (`0045f14a`/`0045f14f`) it loads `graphics\infowindow\horse.bmp`. `0045f0c9 JL` carries `rel32 = 0xfffffd7d` from `0045f0cf`, fixing the loop body at exactly `0045ee4c..0045f0cf` | High for the routine (read at instruction level, its loop bound fixed by its own `rel32`). The caller enumeration needed a **second instrument**: `EnumRefs callto:45ed10` returns 2 hits / **0 distinct owners**, both `RDATA-SLOT` (`0x00599468`, `0x005994f0`) — the routine is virtual slot `+0x80` of two vtables and `callto:` is structurally blind to an indirect call. A regex over `CALL dword ptr [reg + 0x80]` returns 55 hits / 41 owners / 0 orphan; only four were identified | ● active | [EXP-0115](../experiments/EXP-0115-held-item/) |
| HERO-APPEAR-052 | **The ordered list slot 0 indexes is a shipped text file — `main\text\heropicture.txt`, 25 lines — and it is why one arm of the name chain is unreachable.** `HERO-APPEAR-042` left the order Unknown because the object at `0x5ea220` is BSS-backed and built at run time. It is built **from a file**: `FUN_004709e0` executes `PUSH 0x5be2cc` (`004711ab`, the string `"main\text\heropicture.txt"`), `MOV ECX,0x5ea220` (`004711b0`), `CALL 0x00468550` (`004711b5`) — a `__thiscall` with one stack argument, in the middle of three consecutive triples of the same shape loading `main.txt` into `0x5eb500`, this file into `0x5ea220`, and `stats.txt` into `0x5ea230`. The payload is 248 bytes with the same content on both roots, 25 non-blank lines, and `FUN_0045fb00` indexes it with `D - 1`: `0 unarmed`, `1..4 swordsman`, `5 swordsman2h`, `6..9 clubman`, `10 axeman`, `11 axeman2h`, `12..13 mage_st`, `14..16 pikeman`, `17 axeman`, `18 axeman2h`, `19..20 archer`, `21 xbowman`, `22 Sonic Beam`, `23 Flame Thrower`, `24 swordsman`. **`bowman` never occurs**, which is why `HERO-APPEAR-042`'s seventeenth arm is dead and why EXP-0113's cross product found a sheetless arm: nothing can produce the word. Two lines (`Sonic Beam`, `Flame Thrower`) name no shipped body directory, and the list being 25 long while D is five bits wide means a slot-0 item with `D > 25` indexes past its end | High (the load is three named instructions whose pairing is fixed by the calling convention and repeated three times over in the same block; the file is a shipped payload read by this repo's own reader on both roots) / **Unknown** what the reader does with an index past the end | ● active | [EXP-0115](../experiments/EXP-0115-held-item/) |
| HERO-APPEAR-053 | **Nothing is composited over the world sprite: a held weapon is visible because the whole body sheet changes.** The `graphics\equipment` sheets `HERO-APPEAR-051` loads are, walked by the verified `.256` reader on both roots, **928 sheets of exactly one frame each, every frame 160x240** (780 of 784 `primary` and 144 of 144 `secondary` parse whole; the four that do not yield 0 frames). The sixteen hero body sheets under `units/heroes*` are 129, 136, 144, 152, 168 or 216 frames of 24x40 to 40x48 — one frame per direction and phase, as `SPR256-UNIT-024`'s block arithmetic requires. A world-space overlay would have to carry the body's frame count at the body's scale; none of the 928 does. So the per-item sheets are the **info window's** figure — of the compositor's callers, `FUN_0047d4f0` and `FUN_00491930` carry the literals `graphics\infowindow\%s.bmp` and `allods%d.$$$` — and what makes a weapon visible in the world is the body name `HERO-APPEAR-052`'s list yields: `swordsman`, `axeman`, `clubman`, `pikeman`, `archer`, `xbowman` and the two-handed variants are weapon words and they are the whole figure. **A character holds a visible weapon exactly when slot 0 is occupied and its D names a body other than `unarmed`**; with slot 0 empty the index is 0, which is `unarmed` | High for the world half — an exhaustive frame-count measurement over both trees with the same verified reader, and a separation of 129..216 frames against 1 that no reading was fitted to / **Medium** for "the compositor is the info window": the caller set is a regex over one indirect-call form with four of 41 owners identified by their string operands | ● active | [EXP-0115](../experiments/EXP-0115-held-item/) |
| HERO-APPEAR-054 | **The client's slot array is twelve wide, its entries are `0x24`-byte objects, and nothing outside the two message arms edits it.** The constructor `FUN_0045ae30` clears twelve dwords at `drawable+0x15c` (`MOV ECX,0xc` at `0045ae3b`, `LEA EDI,[ESI+0x15c]`, `STOSD.REP`) and nulls `+0x194`/`+0x198`; the destructor `FUN_0045b170` releases twelve through `vt+0x04` (`MOV EDI,0xc` at `0045b1a3`); both stamp the same vptr `0x5993e8`, which fixes the owning class. The entry's constructor is `FUN_00483b50`, sixteen instructions: vptr `0x59a250`, `+0x4` from its argument, `+0x10 = 1`, `+0x1c = +0x20 = -1`, the rest zero; `+0x6` is the appearance `u16`, `+0xc` a heap blob of `+0xa` bytes. Sweeping all twelve displacements and keeping the hits whose owner lies in the class's own method extent `0045a000..00461000`, plus `FUN_004104e8`: the **writers** are the four message stores (`00412f94`, `00412fa9`, `00413b89`, `00413cbb`) and the constructor and destructor; the **readers** are `FUN_0045fb00` (slots 0, 1, 7), `FUN_0045ed10` (all twelve, indexed), `FUN_0045e890` and `FUN_00460480` (slot 0 only), and `FUN_0045aed0`'s copy path. There is no client-local edit: an equipment change reaches the drawable only by a re-send | High for the array width, the entry layout and the classified hit list (each is a named instruction and the extent is fixed by the vptr the constructor and destructor both store) / **Medium** for "nothing outside the two arms edits it": a displacement sweep cannot see a wholesale `REP MOVSD` of the containing object, and the twelve raw counts (86/77/74/70/47/48/36/33/45/39/45/90 hits) are shared with unrelated object families and with stack frames, so only the extent-filtered sub-list was read | ● active | [EXP-0115](../experiments/EXP-0115-held-item/) |
| HERO-APPEAR-055 | **Slot 0's presence and the sex bit together select one of eight 48-byte blocks.** `FUN_0045e890`, whole function, computes `EAX = ((drawable+0x18c) & 4) != 0` (`0045e89d`…`0045e8a3`), scales it by 48 (`LEA EAX,[EAX + EAX*2]` then `SHL EAX,4`) and adds one of four bases: `0x5f1b20` when `+0x18c & 2` (mage), else `0x5f1bf8` when `+0x18c & 1` (hero-shaped), else `0x5f1c58` when `drawable+0x15c` is non-null (`0045e8c9`), else `0x5f1b98`. Its callers are six routines in the same class, `FUN_0045e700` (three times), `FUN_0045e8f0`, `FUN_0045e9c0`, `FUN_0045ea70`, `FUN_0045eb10`, `FUN_0045ebb0` — 8 hits / 6 owners / 0 orphan. This **bounds the natural reading of `HERO-APPEAR-045`**, which is correct as scoped: the sex bit is unread by the sheet builder and the frame selector, but two further routines read it — this one, and `FUN_0045ed10`'s figure-directory jump table (`HERO-APPEAR-051`) | High for the selector (the routine is nine effective instructions, read whole, and its caller enumeration states its instrument and 0-orphan count) / **Unknown** what the four 96-byte regions hold — they were not read | ● active | [EXP-0115](../experiments/EXP-0115-held-item/) |
| HERO-APPEAR-056 | **`spritesb.256` is drawn in the world, by the frame selector, at the same frame index as `sprites.256`.** `HERO-APPEAR-043` established that `FUN_0045fb00` builds both sheets into `drawable+0x194` and `+0x198`; EXP-0113 did not trace the second one's consumer. `FUN_0045bf00` blits both, behind one gate `TEST byte ptr [ESI+0x18c],0x1` (`0045c2d9`): `+0x194` through `vt+0x3c` at `0045c44c`, then `+0x198` through `vt+0x3c` at `0045c4fd`, with the same frame index in `EDI`. The second blit derives its own centre from the sheet's own width and height (`vt+0x20` at `0045c464`, `vt+0x24` at `0045c493`) and the `units.reg` class record's `+0x30`/`+0x38` (`0045c49d`, `0045c4a3`), and takes two arguments the first does not: the constant `0x26` (`0045c499`) and the global `[0x005eb4a0]` (`0045c4ee`). **⚠ The `0x26` clause is WRONG and is corrected by `HERO-FIGURE-062`: `0x26` is not an argument of the blit at all — it is the single argument of `FUN_004599d0` at `0045c4c5`, an effect-list lookup whose result at `0045c4cc JGE` decides whether the second blit runs.** `[0x005eb4a0]` is the blit's fourth argument and is `TERR-SPR-066`'s shroud level, which does place the second sheet in the silhouette family | High for the fact that it is blitted, the gate, the shared frame index and the `[0x005eb4a0]` operand (all named instructions in one routine) / **the `0x26` operand is retracted** — an argument attributed to the wrong call, corrected by `HERO-FIGURE-062` from the raw bytes on both roots | ✖ partially retracted (the `0x26` operand; the rest stands) | [EXP-0115](../experiments/EXP-0115-held-item/) |
| HERO-FIGURE-057 | **The figure compositor renders the character TWICE, and the second rendering is not a picture.** `FUN_0045ed10` takes two surfaces (`[ESP+0x8a0]`, `[ESP+0x8a4]`), binds each with `vt+0x28` and runs a complete pass of layer calls into it: the first through `vt+0x18` with five zero arguments, the second through `vt+0x40` with `(0,0,0,n)`. The two are different blitters of the same layer class (vtable `0x00597418`, constructor `FUN_00428c50`). `vt+0x18` = `FUN_00428e60` (`RET 0x14`) reaches `FUN_0044ea40`/`FUN_0044db00` passing `this+0x1c + (arg4 << 9)`, a 512-byte palette row. `vt+0x40` = `FUN_00429190` (`RET 0x10`; `00429197 MOV EDX,[ESP + 0x10]`, `0042919e PUSH EDX`) passes `n` as the **sixth** argument of `FUN_0044dd40` — and `FUN_0044dd40` walks the same `.256` run stream (`0044dd70 AND EAX,0xc03f`) but for an opaque run copies **nothing**: `0044dda2 MOV AL,byte ptr [EBP + 0x1c]` / `0044dda8 MOV byte ptr [EDI + -0x1],AL`, one constant byte per pixel into `[0x005e43bc]` at stride `[0x005e43a8]`. So the second surface is a byte-per-pixel **stencil** and `n` is a value painted into it — not a z-order key and not a selector, because an ordering key or an index would have to be *read* somewhere and this one never is | High (both methods and both blitters read end to end, to the store that consumes the argument rather than to the call that passes it; the live rival is killed by `0044dda8` rather than by inference) | ● active | [EXP-0119](../experiments/EXP-0119-figure-compositor/) |
| HERO-FIGURE-058 | **The byte the stencil paints is the EQUIPMENT SLOT NUMBER, so the second surface is a hit map.** Over all **28** `CALL [reg+0x40]` sites in `FUN_0045ed10` the value is the drawable array index **plus one**, the head layer taking **0**; `primary[i]` and `secondary[i]` paint the same `i+1`. Read from raw PE bytes on **both roots** by `tools/figurecomp -mode layers`, a scanner over a fixed instruction subset with **no disassembler in the path**, which takes the two arrays' displacements from the routine's own zero-init stores (`0045ee50 MOV [ESP + EBP*0x4 + 0x28],EBX` primary, `0045ee54 …+0x5c` secondary) rather than assuming them, and prints an `UNRESOLVED` row rather than dropping a hit it cannot classify — **28 sites, 0 unresolved**. This is an **independent confirmation of `HERO-APPEAR-050`**: that row derived wire slot `k` = equipment slot `k+1` from the secondary-sheet rule against shipped sheet names, and this derives the same `+1` from a chain of `CMP EBP,<n>` and thirteen `PUSH imm8` in a routine that round did not read | High (a second instrument, on both roots, with its blind spot named and its unresolved count printed beside the table; a one-slot error would break the identity at 27 of the 28 sites, not at one) | ● active | [EXP-0119](../experiments/EXP-0119-figure-compositor/) |
| HERO-FIGURE-059 | **The draw order is program order, it differs between a mage and everything else, and the difference is three separate things.** The split is `0045f1fd TEST byte ptr [ESI + 0x18c],0x2`, the non-mage half at `0045f44c` read from the `JZ`'s own `rel32`. Mage: `p7 head p11 p9 s9 p3 s3 p6 p4 p8 p0 p5 s7`. Non-mage: `p11 p10 p6 p3 p4 p8 p9 p0 p7 s3 p5 s8 s9`, then `p1` **or** `p0` (`HERO-FIGURE-060`). Three asymmetries, each a presence or absence in `evidence/layer-table.txt`: **(a)** drawable index **2** (equipment slot 3) is drawn by **neither** half, and `HERO-APPEAR-050`'s corpus ships no primary sheet for it — dead in both directions; **(b)** index **10** (slot 11) is drawn only in the non-mage half and ships no sheet — a live call with no content; **(c)** index **8** (slot 9) is **tagged but never colour-blitted in the mage half**, so on a mage figure whatever occupies that slot is clickable and invisible. (c) cannot be seen in the tag pass alone, which is why both passes are printed | High for the order and for all three (both passes reproduced from raw bytes on both roots; the mage colour pass carries 12 objects against the tag pass's 13 and the one it lacks is `primary[8]`) | ● active | [EXP-0119](../experiments/EXP-0119-figure-compositor/) |
| HERO-FIGURE-060 | **Which of the two held layers is painted last is decided by a six-name predicate on the body the weapon selects.** `0045f1e6` passes visible-equipment slot 0 to `FUN_00483e70`, which takes `byte[slot + 6] & 0x1f` — field D (`HERO-APPEAR-049`) — less one, indexes the shipped `heropicture` list through `FUN_004687f0`, and compares the name against six literals, returning 1 on any match: `bowman` (`0x5bcaec`), `archer` (`0x5bcaf4`), `xbowman` (`0x5bcae4`), `axeman2h` (`0x5bcb24`), `swordsman2h` (`0x5bcb40`), `mage_st` (`0x5bcbc0`). The non-mage tail then paints **drawable index 0 last** when the predicate is 1 (`0045f706 PUSH 0x1`) and **index 1 last** when it is 0 (`0045f6f6 PUSH 0x2`). Those six are every two-handed and ranged body in `HERO-APPEAR-042`'s seventeen-arm chain except `mage`, which this code is never reached with. `bowman` is a **second** dead reference to a word `HERO-APPEAR-052`'s shipped list never yields — the chain's dead arm and this one are dead for the same reason, in two different routines. **`EXP-0115` graded index 1 = *shield* Medium on an argument; this is the instruction that argument wanted** | High for the predicate, its six literals and the swap (one routine read end to end, both `PUSH`es verified against raw bytes on both roots) / **Medium** for the words *weapon* and *shield*, which the swap makes mechanically certain and lexically no more certain than `EXP-0115` left them | ● active | [EXP-0119](../experiments/EXP-0119-figure-compositor/) |
| HERO-FIGURE-061 | **The eight 48-byte blocks `HERO-APPEAR-055` selects between are VOICE BANKS, not geometry — and visible-equipment slot 0 is one of the four selectors.** `FUN_004af540`, a C++ static initializer, fills them in a two-iteration loop whose prefix char is `0x6d` less `BL*7` (`004af82c MOV DL,0x6d`, `004af828 MOV CL,0x7`) — `'m'` then `'f'` — concatenated with four literals, each followed by the bank constructor `FUN_004afa60`: `"f_hero"` → `0x5f1bf8`, `"f_merc"` → `0x5f1c58`, `"_mage"` → `0x5f1b20`, `"_peasant"` → `0x5f1b98`; the bound `004af94a CMP EBP,0x5f1b80` from `0x5f1b20` at step `0x30` fixes it at exactly two iterations. `FUN_004afa60` fills each block from `sfx\%s\select%d.wav`, `sfx\%s\command%d.wav` and `sfx\<name>\` + `retreat` / `defend` / `idle` / `easy` / `hard` / `die` `.wav`. **Corpus, both roots: 80 of 80 (bank, leaf) pairs present, 0 absent**, and those eight are exactly the human directories in `sfx.res` — the rest are `monsters/*`, `town/*`, `units/*`, `magic`, `ambient`, `chrgen`. The blocks are not file-backed content: the image holds their addresses, the run time their values. Consumers: `callto:45e890` = **8 hits / 6 owners / 0 orphan** — `FUN_0045e700` (a `0x5dc`-ms-throttled emitter), `FUN_0045e8f0`, `FUN_0045e9c0`, `FUN_0045ea70`, `FUN_0045eb10`, `FUN_0045ebb0` — and the compositor is not among them | High (the initializer and the selector are read end to end, every literal and immediate is verified against raw bytes on both roots, and the corpus half is an exact set comparison between two artifacts never fitted to each other — a one-character error in the prefix would have missed all eight) | ● active | [EXP-0119](../experiments/EXP-0119-figure-compositor/) |
| HERO-FIGURE-062 | **The second world blit is GATED, and `0x26` is the gate's key rather than the blit's argument — correcting `HERO-APPEAR-056`.** `0045c499 PUSH 0x26` is followed by no further push before `0045c4c5 CALL 0x004599d0`, so it is that routine's single argument. `FUN_004599d0` is six instructions of linear search over `[this + 0x128]`, count `[this + 0x12c]`, matching `dword >> 16` (`004599ea SHR EDI,0x10`) and returning the index or **−1** (`004599f9 OR EAX,0xffffffff`); the caller's `0045c4cc JGE 0x0045c90d` **skips the second blit when the key is found**, and a second gate `0045c4d2 MOV EAX,[0x005eb520]` / `JZ` follows. The blit's own six arguments are, last-push-first, `dstX` (EBP), `dstY` (EBX), frame (EDI), `[0x005eb4a0]`, sun shear, mirror flag — exactly `TERR-SPR-038`'s push list, so the fourth is `TERR-SPR-066`'s **shroud level** and `spritesb.256` belongs to the silhouette family, as `TERR-STRUCT-103` already reports of the structure `b`-overlay through the same global. The first blit passes `[0x005eb49c]` on its ordinary arm and `[0x005eb4a0]` on its alternate one | High (every push and both gates transcribed from raw bytes on both roots, and `FUN_004599d0` read whole) / **Unknown** what the keys `0x26` and `0x30` name — the list at `+0x128` was read as a structure and its id space was not cross-walked | ● active (corrects `HERO-APPEAR-056`) | [EXP-0119](../experiments/EXP-0119-figure-compositor/) |
| HERO-FIGURE-063 | **The shipped name list is indexed with no bound in either direction, by both of its readers.** `FUN_004687f0` is `MOV EAX,[ECX + 0xc]` / `ADD EAX,i` / `MOV EAX,[[0x005eb3d4] + EAX*0x4]` / `RET 0x4` — six instructions, **no test**. Both callers pass `(byte[slot + 6] & 0x1f) − 1`: `FUN_0045fb00` (`HERO-APPEAR-042`) and `FUN_00483e70` (`HERO-FIGURE-060`). `HERO-APPEAR-052` already notes that the list is 25 long while field D is five bits wide, so `D ≥ 26` reads past the end; the other end is new — `FUN_00483e70` runs only when slot 0 is **occupied** (`0045f1f0 CMP ECX,EBX` / `JZ`), so an occupied slot whose `D` is **0** indexes at **−1**, one dword before the first entry. A consumer must clamp; the engine does not | High for the accessor and both call sites (each a named instruction, verified against raw bytes on both roots) / **Unknown** whether a shipped definition row index of 0 or ≥ 26 occurs — that is `item+0x0c`'s `Data.bin` source, which this round does not open | ● active | [EXP-0119](../experiments/EXP-0119-figure-compositor/) |
| HERO-FIGURE-064 | **Two of the thirty-four trigger arms are the only path to the equipment send outside the actor serializer, and NEITHER IS AUTHORED on either root.** The table at `0x0053a178` was read out of the PE as data by `tools/figurecomp -mode trigger`, each arm scanned only to its own `JMP 0x0053a15b` terminator: **arm 13 (opcode 14) → `0x00539e52`** and **arm 14 (opcode 15) → `0x00539e6a`**, calling `FUN_004e873b` at `00539e60` and `00539e78`; no other arm reaches it. `callto:4e873b` = **4 hits / 3 owners / 0 orphan** — those two, the actor serializer `FUN_004e7de3` (`004e864e`), and the sender's own per-player recursion (`004e879e`). The two arms are semantically identical and differ only in instruction scheduling; both pass `(actor, actor+0x14)`, the owning participant, so a script send is aimed at the owner. **Corpus, both roots** (`tools/almrt -mode vocab`, EXP-0080's instrument, re-run per root): EN **797** action nodes over 38 maps, RU **792** over 34, **27** distinct opcodes each and the same 27 — **14 and 15 in neither**. So on shipped content the equipment message is only ever produced by the actor serializer. This discharges `HERO-APPEAR-044`'s Unknown from the corpus side, as `EXP-0114` discharged it from the image side | High for the table, the two arms and the caller enumeration (the table is data read through the PE section table, each arm is bounded by its own terminator, and the read reproduces `TRIG-ACT-004`'s dead arm 9 and `TRIG-DIPLO-019`'s arm 10 without having been built from them) / **Medium** for the negative, a corpus histogram over the `.alm` maps: `TRIG-INI-012` establishes that mission `.ini` overlays do not ship, but a save file's own trigger state was not examined | ● active | [EXP-0119](../experiments/EXP-0119-figure-compositor/) |
| HERO-DWELL-065 | **The dying countdown is a floor on the dwell, not the whole of it: the teardown fires on the first tick at which health is at or below −10, which may be the tick the countdown expires or any tick after it.** `actor+0x6c` is decremented **only while positive** — `004f38dc MOVSX ECX,byte ptr [EAX + 0x6c]` / `004f38e0 TEST ECX,ECX` / `004f38e2 JLE 004f38f7` — and nothing else writes it while the actor is dying. `EnumRefs disp:6c` + `imm:6c` over the whole image, restricted to the simulation module, returns **eight byte-wide stores** and all eight are inside `FUN_004f37be`: two in the death arm (`004f38bc`, loading `vt+0x6c() − 1`; `004f38ef`, the decrement) and six in the attack arms (`004f3b0d`, `004f3b3a`, `004f3b4e`, `004f3e02`, `004f3e3e`, `004f3e4f`), which a dying actor never reaches because `004f3857 JG` on `health > 0` fails forever. The only other byte-wide hit is a **read**, `0051068f` in the deserializer; every remaining `+0x6c` hit in the module is `dword`-wide and belongs to a different object. So once the byte reaches 0 it stays 0 and `004f38f7`…`004f391d` — the `movementType` arm and the `<= −10` test — **re-runs on every invocation of the tick, indefinitely**. The guard itself is `004f3913 MOVSX EAX,word ptr [EDX + 0x94]` / `004f391a CMP EAX,-0xa` (`83 f8 f6`, an `imm8` sign-extended) / `004f391d JG` (`7f 17`), whose target `004f3936` is fixed **by the branch displacement itself**, so the 0x17 bytes it skips are checked by the encoding rather than by a second decode; both arms jump to the same address, so the guard's entire effect is the `actor+0x54 = 0x10` and `CALL FUN_004f4f5d` between them. The dwell is `dyingTime − 1` invocations **and then a standing condition**, and on a body felled at −1…−9 that nothing strikes again it never ends: cells, reserved next cell and id are held for the rest of the mission at corpse stage 1. Amends `HERO-DEATH-026`. *(amended by EXP-0122: **the standing condition does end by itself, and the value it never ends at is 0 rather than −1…−9.** The complete writer set this row establishes is of `+0x6c`; the guard tests `+0x94`, and nothing enumerated here bears on what moves health afterwards — a complete enumeration of the wrong field. `vt+0x14`'s dying arm at `004f444f` removes one point every fourth full tick while health is **strictly** negative, and it reaches the actor because a body that has not been torn down is still on the live list phase 12 walks (`HERO-DECAY-069`). So a ground unit felled at −1…−9 is carried to −10 in at most **36 full ticks ≈ 36 s** and torn down then. The rest-of-the-mission case is real and is `health == 0` exactly (`HERO-ZERO-070`).)* | High (the countdown's complete writer set at the field's own width, plus the branch's own `rel8`, every cited byte re-read from the raw file on both roots) / the completeness clause is bounded in one direction only (`docs/INSTRUMENT.md` rule 8): a `REP MOVSD` of the containing structure, or a store through a pointer the sweep cannot attribute, would not appear | ● active (amended) | [EXP-0121](../experiments/EXP-0121-death-row/), **[EXP-0122](../experiments/EXP-0122-corpse-clock/)** |
| HERO-FINISH-066 | **A corpse can still be struck, and −10 is where the image stops treating a body as a thing being fought — three sites say so, and the death arm's guard is the third.** The only aliveness test in the strike `FUN_004fba0e` is on `[EBP + 0x8]`, the **attacker** (`004fba4a MOVSX EAX,word ptr [EDX + 0x94]`, `004fba51 TEST`, `004fba53 JG`, else `JMP 004fbbbe`) — the same operand whose reach `004fba5f MOV DL,byte ptr [ECX + 0x12c]` is read (`HERO-REACH-025`). The **target**'s health is read once, at `004fba86`, and only into the flag `wasAlreadyDead` (`004fba91 SETLE DL`, `004fba94` store); the subtraction that follows is **unconditional** — `004fbabb MOV CX,word ptr [EAX + 0x94]` / `004fbac2 SUB CX,word ptr [EBP + -0xc]` / `004fbac9 MOV word ptr [EDX + 0x94],CX`. The tick's attack arm adds no test of its own: `FUN_004f37be` touches `+0x94` three times and all three are on the actor itself (`004f384e`, `004f3907`, `004f3913`). The other two `-0xa` compares are in the strike and each suppresses something once the body is past the line: the damage numeral `004fbb89 CALL 004e9da2` (`004fbb77 CMP EAX,-0xa` / `7e12 JLE`, under `wasAlreadyDead`) and the experience/retaliation call `004fbbbb CALL dword ptr [EDX + 0x64]` (`004fbba4 CMP ECX,-0xa` / `7e15 JLE`). This is what dissolves `HERO-DEATH-026`'s apparent deadlock — health is **not** fixed between the killing blow and the teardown, so the guard was never a proposition about the killing blow | High (three compares and the unconditional subtraction read at instruction level in one routine, and the tick's own health reads enumerated) / **Unknown** how often a body is in fact finished off: whether an attacker retains a dead actor as its `+0x5c` is decided in the order/AI layer, which this round did not read | ● active | [EXP-0121](../experiments/EXP-0121-death-row/) |
| HERO-DYETICK-067 | **`dyingTime` counts sub-ticks, not full ticks — the routine that decrements it is `vt+0x18` on three sibling vtables, and `vt+0x18` is what the server dispatches once per sub-tick.** A raw little-endian **dword scan of the whole image** for each routine address — the instrument `docs/INSTRUMENT.md` rule 7 asks for, because `callto:` cannot see a pointer table — puts `FUN_004f37be` at exactly three `.rdata` sites, each carrying the same neighbour quadruple `0x00401980, FUN_004f4204, FUN_004f37be, 0x00523210`. Two of the three place that pair at `+0x14`/`+0x18` of a 33-slot table outright; the third's run detection merged it with the table above it, so its base is `0x0059c3d8 − 0x18 = 0x0059c3c0` and it is 33 slots as well. `FUN_004f4204` at `vt+0x14` is fixed **independently** — `HERO-REGEN-021` identifies it, `SESS-TICK-006` reads `0050ff2a CALL dword ptr [EDX + 0x14]` — so the entry four bytes above it is `vt+0x18`, and `vt+0x18` is what `FUN_0050fca9` calls at `0050fd1b`, once per sub-tick (`SESS-TICK-004`/`006`). At the shipped default speed index 4 a sub-tick is `1000/16 = 62.5` ms, so the human default `dyingTime = 8` (`DAT-HUMANS-008`) loads `actor+0x6c = 7` and floors the dwell at **≈ 437 ms** — not the ≈ 7 s a full-tick reading gives. The same scan returns **0** occurrences of `FUN_004f4f5d` and of `FUN_004f52ee` anywhere in the image as data, so no vtable, static-init table, jump table or callback array reaches either: `HERO-DEATH-026`'s *"its only caller"* (`callto:4f4f5d`, 1 hit) and the decay machine's two callers (`callto:4f52ee`: `004f52da` in the teardown, `0050ff7b` in `FUN_0050fed6`) are complete sets rather than `callto:` artefacts | High (the slot is pinned by an independently identified neighbour, and the scan is over raw bytes on both roots — three hits each, identical) | ● active | [EXP-0121](../experiments/EXP-0121-death-row/) |
| HERO-REVIVE-068 | **A body that has not been torn down is still a live actor with non-positive health, and the heal path is written to expect exactly that.** `FUN_004f49d7` captures `wasDead = health <= 0` **before** it adds (`004f4a03 SETLE DL`, then `004f4a13 ADD CX,word ptr [EBP + 0x8]` and the `+0x96` cap), and on a crossing back to `health > 0` calls `FUN_004f4a87` (`004f4a77 JLE 004f4a81` / `004f4a7c CALL`), which clears the corpse stage (`004f4ab6 MOV byte ptr [ECX + 0x13c],0x0`), **doubles** the defence the death arm halved (`004f4adb SHL DX,0x1` against the death arm's `SAR EAX,0x1`) and floors health at 1 (`004f4b00 JGE`, `004f4b1f MOV word ptr [ECX + 0x94],DX`). The death arm's first-tick effects are therefore written as **reversible**, and the window in which they can be reversed is exactly `HERO-DWELL-065`'s standing condition. One address correction while this row is next to it: that `SAR` sits at **`004f389f`**, and the `004f389d` `HERO-DEATH-026` cites is the `SUB EAX,EDX` of the `CDQ`/`SUB`/`SAR 1` idiom this ledger's header names, one instruction earlier — `8b 55 8c · 0f bf 82 be 00 00 00 · 99 · 2b c2 · d1 f8` from `004f3892`, identical on both roots | High (both halves read at instruction level, and the halve/double pair is one shift against another) / the *consumer* side — which spell or item reaches `FUN_004f49d7` with a dead target — is not enumerated here | ● active | [EXP-0121](../experiments/EXP-0121-death-row/) |
| HERO-DECAY-069 | **A corpse that has not been torn down is still on the live list, and the regeneration tick is what decays it: `vt+0x14` removes one health point every fourth full tick while health is strictly negative.** `FUN_004f4204`'s entry test `004f421e MOVSX EDX,word ptr [ECX + 0x94]` / `004f4225 TEST EDX,EDX` / `004f4227 JLE 0x004f444f` sends every **non-positive** health to an arm at `004f444f`, whose own first branch `004f4459 TEST EAX,EAX` / `004f445b JGE 0x004f44a4` acts only on `health < 0`. The arm then filters on `[0x005cd758]+0x00` — the **full**-tick counter (`SESS-CLOCK-005`), not the `+0x04` sub-tick counter the dwell is measured in — with `004f446a AND EAX,0x3` / `004f4473 JNZ`, and does `004f447f SUB AX,0x1` / `004f4486 MOV word ptr [ECX + 0x94],AX`, notifying at `004f449f`. It reaches the actor through the **live** list: phase 12 of the full tick (`004d25a5 AND EAX,0xf` / `004d25ac CMP EAX,0xc`) calls `FUN_0050fed6(M)` at `004d25b7`, and that routine is **two loops over two different lists** — the first walks `M+4`, which `FUN_0050fca9` removes an actor from only after the teardown sets `actor+0x54 = 0x10`, calling `vt+0x14` at `0050ff2a`; the second walks the dead list and calls the decay machine at `0050ff7b`. They cannot overlap, because `FUN_004f4204` returns at `004f4216` whenever `actor+0x54 == 0x10`. So a ground unit felled to `−h` for `1 <= h <= 9` is carried to −10 in `(10 − h) × 4` full ticks — at most **36 full ticks ≈ 36 s** at the shipped default speed — at which point `HERO-DWELL-065`'s standing guard fires, and the dead-list ladder continues at one point per **two** full ticks. Two clocks and two rates on one field, and the corpse stage byte does not move in the first of them. Amends `HERO-DWELL-065`; promotes `HERO-REGEN-021`'s dying-arm parenthesis to the mechanism it is | High (the arm is read whole at instruction level, both its branch displacements are `rel8` onto the same epilogue at `004f44a4` so the skipped blocks are fixed by the encoding (`docs/INSTRUMENT.md` rule 6), and phase 12's two loops are read with the list each is handed; 148/148 cited bytes re-read from the raw file on both roots) | ● active | [EXP-0122](../experiments/EXP-0122-corpse-clock/) |
| HERO-ZERO-070 | **Health exactly 0 is a fixed point of the entire tick: a ground unit reduced to it is a corpse that never decays, is never torn down and never returns its runtime id.** `004f4227 JLE` admits `health <= 0` to the dying arm and `004f445b JGE` rejects `health >= 0` inside it, so **0 is the one value that enters and does nothing** — re-entered every fourth full tick, indefinitely. The sub-tick arm cannot move it either: `004f391a CMP EAX,-0xa` / `004f391d JG` is false at 0, and the only health store in `FUN_004f37be` is `004f3907 MOV word ptr [ECX + 0x94],0xfc18` under `004f38fc MOV AL,byte ptr [EDX + 0x4a]` / `004f38ff CMP EAX,0x1` / `004f3902 JLE` — `movementType > 1` — which is why this is a **ground-unit** state and the four flying classes `ANIM-DEATH-007` names never reach it. The decay machine is unreachable, being called only from phase 12's dead-list loop (`HERO-DECAY-069`). And 0 is reachable: the strike's subtraction is unconditional with no floor (`HERO-HEALTH-032`), so a blow whose resolved total equals the target's remaining health lands exactly on it. **The enumeration behind the negative**, two instruments reduced independently: `EnumRefs disp:94` returns 419 hits / 191 distinct owners / 4 in orphan code, of which **36** are word-wide stores; and a raw-encoding scan of `.text` for every ModRM destination whose `disp32` overlaps the field — `go run ./tools/corpseclock -mode stores`, which never disassembles and so has no blind spot for code no pass reached — returns 147 stores, of which **the same 36** are word-wide, with nothing on either side. `disp:93` and `disp:95` are **0 hits**; `disp:92`'s 10 hits are all word-wide at `+0x92` and none reaches `+0x94`. The four orphan hits are named rather than counted: two `[ESP + 0x94]` stack accesses and two word-wide `CMP` **reads**. All **49** `LEA`s taking the field's address lie outside the actor module, so nothing there holds a pointer a displacement-free store could arrive through. Of the 36, six belong to the clock and thirty need a blow, a spell, an effect, a heal, a removal from the map, a constructor, a template, a load or a script action (`evidence/writer-attribution.md`). Closes the open clause `HERO-HEALTH-032` states about itself | High (two instruments that fail differently, agreeing exactly, with every unnamed hit named) / **bounded twice, and neither bound is closed here**: a wholesale `REP MOVSD`/`memcpy` of the containing structure carries no displacement at all and neither instrument can see it; and what re-runs `FUN_00501a22` for an effect whose duration word is `continuous` was not read, so an attached continuous `health` effect is outside this claim | ● active | [EXP-0122](../experiments/EXP-0122-corpse-clock/) |
| HERO-HP-071 | **`HERO-HP-005`'s gate clause is a misreading, and the `Data.bin HealthMax` column does not survive creation at all on the `Humans` arm — it is neither the value nor a gate.** In `FUN_004f7dfc` the store and the test are two instructions apart in one routine: `004f7f3c MOVSX EDX,word ptr [ECX + 0x84]`, `004f7f4d IMUL EDX,EAX`, **`004f7f53 MOV word ptr [ECX + 0x96],DX`**, then `004f7f5d MOVSX EAX,word ptr [EDX + 0x96]`, `004f7f64 TEST EAX,EAX`, `004f7f66 JZ 0x004f800a` — both operands are the same field of the same `this` (`[EBP-0x14]`), so the `JZ` tests **`body × multiplier`**, and the streamed column was destroyed one instruction earlier. The **mana** arm nine instructions later is the shape the clause describes and is genuinely different: `004f800d MOVSX EAX,word ptr [EDX + 0x9c]` / `004f8014 TEST` / `004f8016 JZ` reads the streamed `ManaMax` column **before** `004f8028 MOV word ptr [EAX + 0x9c],DX` overwrites it, so the mana column *is* a gate and the health column is not — the clause reads one arm's shape onto the other. Nothing recovers the column afterwards: the clamp `004f84c4` computes `actor+0x94 = min(+0x94, +0x96)` while `+0x94` still holds it, and `004f966b` overwrites that with `+0x96` before the constructor returns. **The corpus discriminates rather than agreeing**: over the 215 shipped `Humans` rows, **6 pairs** hold identical Body, ManaMax and all six skill columns while differing in the `HealthMax` column, and **0** of those pairs derive a different maximum; derived equals the column on **1** row of 215 (128 above, 86 below, both roots). Also corrected: `HERO-CLASS-013` calls `004f94c8`/`004f94d0` *"copied verbatim out of the `Data.bin` definition"* — those two addresses are the read and the write of `004f94cb OR AL,0x6`, a read-modify-write gated on `004f94bd TEST` / `004f94bf JLE` over the streamed `ManaMax` column, and the streamer's 23-slot map has no `+0x4c` destination at all | High (the store/reload pair is two instructions apart in one listing read end to end; the mana arm supplies the second model and the ordering of `TEST` and store discriminates between them; the corpus test is a positive same-inputs/different-column experiment with no split, not an absence) | ● active | [EXP-0133](../experiments/EXP-0133-person-health/) |
| HERO-HP-072 | **What a map-placed person's health maximum actually is, and that the derivation runs at creation — twice.** `FUN_004f9065` calls it itself: `004f964e CALL FUN_004f7c28`, `004f965b CALL dword ptr [EDX + 0x50]`, then `004f9664`/`004f966b` `actor+0x94 := actor+0x96`; the spawner calls it again at `004e2d28` after its stat overrides (`UNIT-PLACE-034`). So `ITEM-EQUIP-006`'s load-bucket call is not the only invocation, and `UNIT-DERIVE-003`'s *"reached only virtually"* is about the linkage, not the frequency — `EnumRefs callto:4f7dfc` is 2 hits, both `.rdata` slots `0059c498`/`0059c520`, 0 owners, and slot `+0x50` of `0059c448` **and** `0059c4d0` is `FUN_004f7dfc` while `0059c3c0`'s is `FUN_004f5946` (all 22 slots of all three tables resolve to functions on a repaired table). The number, in order: `body = min(bodyColumn, (s8)actor+0xd4 + 50)` (`004f7e19`…`004f7e5a`, and the same three-instruction idiom for `+0x86`/`+0x88`/`+0x8a`); `mult = 2` iff `actor+0x4c & 4` is **clear** — the routine's first act is `004f7e0c CALL FUN_0051cb70` / `004f7e11 NEG` / `004f7e13 SBB` / `004f7e15 INC`, a logical NOT into `[EBP-0xc]`, which `004f7f45 CMP`/`SETNZ`/`INC` then reads, so the doubling belongs to a row with **no** mana and `HERO-CLASS-013`'s `health × (fighter ? 2 : 1)` is right as written; the bit itself is set on this path by `004f94d0` exactly when the `ManaMax` column is positive and by nothing else — the base constructor's `004f30ee` writes 0 and `EnumRefs disp:4c` places no third writer on the path; `xp = Σ ftol((1.1^skill − 1) × 1000)` over the six skill columns (`FUN_004f7c28` at `004f7c57`…`004f7c94`, term `FUN_004f72b5`); `h = (word)(body × mult)`; and if `h ≠ 0`, `h = (word)ftol(h + log₁.₁(xp/5000 + 1) × mult)` then `h = (word)ftol(h × (1.1^body/100 + 1))`; finally `h += modifier+0x08` at `004f5560`, reached from `004f84b5` **inside** the derive rather than once at the end of creation as on the units arm. The `0x40`-byte modifier block is `memset` to 0 by `FUN_004f5438` and no placement-path instruction writes it (`imm:d4`/`disp:d4`: five family constructors, `FUN_004f2bb4`, the two fold sites, two in the magic module), so at spawn the stat cap is a flat **50** and the health addend **0**. Constants read out of the PE at their cited addresses and censused whole-file: `5000` occurs **once** in 1 977 344 bytes, `1000` twice, `100` three times, and the `1.1` base is a `PUSH`-pair immediate occurring **zero** times as an f64. The `-1` default is also not `UNIT-CTOR-004`'s: a human runs a **second** defaults routine, `FUN_004f6ded` from `FUN_004f6d3f`, which raises Mind and Spirit 20 → 30, healthMax and health 30 → **50** (`004f6e7f`, `004f6e8b`) and speed 10 → 16. Consequence for the shipped table: the sum of the 215 derived maxima is 31 828 against 20 809 of column (EN), the largest single ratio is **20.2×** (`M151_SuperAxemen`, 607 against 30), a caster loses the doubling instead of gaining it (`ManMage_Unarmed`, 10 against a column of 50), and EN and RU differ on exactly one row, `M120_BrigandChief` (654 against 604), because its skill columns differ | High (the vtable slot, the whole body, the two call sites and the complete `[reg+0x96]` writer set are each read at instruction level; the constant census is over the file rather than a code sweep) / Medium (that `actor+0x4c` bit 2 *means* one archetype rather than the other — this row states the condition under which it is set, and the direction of the multiplier, without depending on the name) | ● active | [EXP-0133](../experiments/EXP-0133-person-health/) |
| HERO-SKILLUP-073 | **A skill level does move in play, it moves by exactly one, and the routine that moves it is `FUN_004f72d7` — `vt+0x5c` on both hero vtables (`0x0059c4a4`, `0x0059c52c`).** The level is its own `u16` at `+0xa8 + 2i`, not a display of the experience: the raise is `ADD DX,0x1` on the live word (`004f74c5` book arm, `004f7687` weapon arm) with no loop, so **no single event can carry a slot up two levels**, and it fires only when `xp[slot] > S(skill[slot])` — a strict `SETG` at `004f7471`/`004f7625` against `FUN_004f72b5` of the **current** level. The slot must already be under 100 (`004f73f2`/`004f757c` `CMP …,0x64 ; JGE` to `004f7544`/`004f76f4`), and that test runs **before** the award, so a slot at 100 banks nothing either. Two consequences of shape rather than of any one instruction: the award is capped at one level's worth (`HERO-SKILLGATE-074`), and `FUN_004f7c28` sets `xp[i] = S(skill[i])` **exactly** at creation while the test is strict, so **the first award of one point or more into any slot always raises it**, once per slot per character. On a raise the routine re-snapshots the base copy `+0x116 + 2i` for `i = 1..5` and calls `vt+0x50`, the whole derive (`004f7702`), so health, mana, sight, damage, to-hit, defence and the five protections move with it; on the book arm it also runs `FUN_00500a9d`, which hands every non-null spellbook entry to `FUN_004fe13b` and rewrites its cached power as `clamp(skill[Sphere] + Mind − 30, 0, 100)` (`004fe171` `LEA ECX,[ECX + EAX − 0x1e]`), so `MAGIC-POWER-004`'s expression is a **cache maintained on a raise**, not a cast-time computation. Where nothing was raised the tail instead folds the modifier copy back, `skill[i] = min(base[i] + mod[i], 100)` for `i = 1..5` (`004f774f`…`004f77ae`) | High (the routine is read end to end, both arms and the shared tail; every cited address is byte-verified against the shipped file and the two arm entries are settled by the `rel32` displacements in the same bytes, `evidence/code-identity.txt`; the rival *recompute-from-experience* model is ruled out at the write and by the inverse having exactly one caller in the image, `callto:4f7285` = 1 hit / 1 owner / 0 orphan) | ● active | [EXP-0135](../experiments/EXP-0135-skill-raise/) |
| HERO-SKILLGATE-074 | **Skill gain first requires `actor+0x0e` in `[0x21,0x3f]`; this admits player-character constructor mode, not every Human.** Mission 20's transferred Humans retain `0x17/0x0a` and fail this gate as well as the boundary culls (`PARTY-M20-031`). Once admitted, the amount is `ftol(amount × (mind/30 + 0.25))`. A live source then refuses same-owner and treaty-protected awards; an already-dead source bypasses both. One award is capped at the next-level delta, or one fifth in the multi-participant mode. The class bit and spell id select the skill slot: a spellbook mage uses the spell Sphere when positive; otherwise the active weapon skill must be positive. The three feeds remain kill, hit and cast, while the plain actor vtable's three stubs award nothing | High for the gate order, slot selection and feeds; the narrowed population is directly witnessed by mission-20 save typeWords | ● active (corrected) | [EXP-0135](../experiments/EXP-0135-skill-raise/), [EXP-0192](../experiments/EXP-0192-mission20-party-boundary/) |
| HERO-SKILLLOSS-075 | **An admitted repair charges all six experience slots; this is not a campaign defeat/next-entry rule.** `FUN_004f7a53` zeroes `+0x130`, then for **`i = 0..5`** (`004f7a91 CMP …,0x6` — `Skill.General` included, unlike every other loop in the area) does `xp[i] = xp[i] × 9 / 10` (`004f7aa4 IMUL …,0x9` then `IDIV 0xa`, truncating), re-accumulates the total, and writes back `skill[i] = FUN_004f7285(xp[i]) = ftol(log₁.₁(xp/1000 + 1))` (`004f7ae9`/`004f7af7`) before rebuilding the base copy for `i = 1..5`. It has **one caller**: `FUN_004d3755` at `004d37ac`, gated on the **zero-extended byte** `actor+0x13c != 0` (`004d379b`/`004d37a3`) — the byte the dying tick `FUN_004f37be` (`vt+0x18`, `HERO-DYETICK-067`) sets to 1 at `004f3883` and which `FUN_004f4a87`, called from the loss's own tail, clears. The same arm then sets `health = healthMax` and `mana = manaMax` (`004d37be`/`004d37d2`). So this helper's penalty and restore are one act, not an operation of the dying tick itself. **EXP-0274 refutes unqualified next-entry timing:** automatic reporter repair needs actual `server+0x0c != 0`, participant `+0x3f != 0`, latch at least 2 and primary HP below -53. The other call sites are session opcode `0x48` (no mode restriction inside its handler; an existing staged primary can be charged if delivered) and `0xbe` (the call requires a null primary, therefore creates without penalty). Campaign failure exit instead reaches teardown, restoring retained actors and clearing their stage without this XP leaf (`HERO-DEFEAT-136`). Its size is not a tenth of a level: because a raise leaves the level one ahead of the experience, a slot at level *n* holds `xp ∈ (S(n−1), S(n)]`, and re-deriving from nine tenths of that costs **1–2 levels below 26 and 2–3 from 26 up** — the boundary being algebraic and rounding-independent, `0.9 × S(n) < S(n−1)` exactly when `1.1ⁿ > 10`, i.e. from level 25 (`evidence/skill-ladder.txt`). *(partially retracted by EXP-0187: the concluding claim that General “can only fall” was false. This play-award worker cannot raise slot 0, but opcode `0x3d` can raise live General when supplied slot 0; the shipped school cannot supply it. The arithmetic and all-six-slot helper mutation stand; the generic timing was separately refuted by EXP-0274.)* | High for arithmetic, corrected zero-extended gate and the three enumerated call sites, not universal next-entry timing; High for the algebraic threshold / **Medium** for the 1–3 band at its edges: the image evaluates `pow` and `log` through the CRT and the probe through Go's, and the truncation in `S` makes the boundary cases FP-dependent — the band's interior does not depend on that, its edges might | ● partially retracted (amended) | [EXP-0135](../experiments/EXP-0135-skill-raise/), [EXP-0187](../experiments/EXP-0187-general-skill/), [EXP-0274](../experiments/EXP-0274-defeat-modes/) |
| HERO-SKILLBUY-076 | **A level can also be bought, for gold, on one command — and this is the only shipped server operation that can raise `Skill.General`.** `FUN_004f7cd7(slot)` restores `1..5` from their base copies, increments the selected level with no test against 100, sets `xp[slot] = S(new level) + 1`, updates `+0x130`, snapshots base slots `1..5` and derives. Server opcode 61 (`0x3d`) is single-participant only, accepts `0 ≤ slot ≤ 5`, checks the purse, charges and broadcasts the refresh. *(amended by EXP-0187: both price arms index `actor+0x116+2*slot`, but every base-maintenance loop starts at 1. Thus `ftol(1.1^base[slot] × 200)` is exact only for slots 1..5; slot 0 uses a serialized shadow word not maintained from live General. The purchase raises live General and its experience without refreshing its next price input. The first crossing of each fixed-width stage, ordered by numeric level/base input, is: per-slot XP signed-`i32` at new level 153 and `u32` wrap at 161; price signed-`i32` at maintained base 170 and `u32` wrap at 178; XP and price `FISTP qword` input range at 386 and 403; level signed-`i16` interpretation at `0x8000`; and `u16` storage wrap at `0xffff + 1`. The six-slot total can cross its dword boundaries earlier depending on the other five slots.)* The school Train widget is the sole direct opcode-61 producer, but its five-choice mapping yields only slots `1,2,4,3,5`. General is accepted by the server operation and unreachable through the shipped school UI | High for the operation, opcode, bounds, price source, the eight fixed-stage boundary encodings and their order, and completed school chain; Unknown for downstream runtime consequences after any unexecuted transition, including the exact original conversion result beyond signed-qword range | ● active (amended) | [EXP-0135](../experiments/EXP-0135-skill-raise/), [EXP-0187](../experiments/EXP-0187-general-skill/) |
| HERO-XP-077 | **`actor+0x130` is `Σ xp[i]` over the six per-slot experiences, maintained incrementally, and it equals `Σ S(skill[i])` only at creation.** Replaces `HERO-XP-010`, whose headline and whose EXP-0069 repair are both retracted. `S(n) = FUN_004f72b5 = ftol((pow(1.1, n) − 1) × 1000)` (`004f72bc` pow, `004f72c4` FSUB `[0x59bc80] = 1`, `004f72ca` FMUL `[0x59bc78] = 1000`, `004f72d0` ftol) and `FUN_004f7285` is its inverse. **Four routines write the pair and every one preserves `+0x130 == Σ xp[i]`**: `FUN_004f7c28` zeroes the total and sets `xp[i] = S(skill[i])` for `i = 0..5` (`004f7c74`, `004f7c8a`), and it runs at creation and nowhere else — `callto:4f7c28` = 2 hits / 2 owners / 0 orphan, the chargen setter `FUN_004f992c` and the placement builder `FUN_004f9065`; the gain `FUN_004f72d7` and the purchase `FUN_004f7cd7` add one amount to one slot **and** to the total; the loss `FUN_004f7a53` rebuilds both from the cut slots. So a hero's experience is a running counter of what six slots have been paid, and it stops being a function of the six *levels* the moment the first raise lands, because the raise moves the level and leaves the experience where it is (`HERO-SKILLUP-073`) — a slot at level *n* holds `xp ∈ (S(n−1), S(n)]`. Every consumer of `+0x130`, `HERO-HP-005`'s health term among them, reads the counter and not the level sum | High (the four writers are read whole, the invariant is arithmetic on their own stores, and the three increments the retracted row denied are byte-verified against the shipped file — `evidence/code-identity.txt`, 114 cited instructions, 0 mismatches) | ● active | [EXP-0135](../experiments/EXP-0135-skill-raise/) |

| HERO-DOLL-078 | **The doll's face leaf is `graphics\equipment\<figure>\<face>.256`, and both halves of it are read off the drawable rather than out of any registry.** `FUN_0045ed10` — virtual `+0x80` of both drawable vtables `0x005993e8` and `0x00599470` (`UNIT-FIGURE-032`) — loads `0x005bcad0` `"graphics\equipment\"` (`0045edad MOV EDI,0x5bcad0`), then `0045edda MOV EAX,[ECX+0x18c] ; AND EAX,0x6 ; CMP EAX,0x6 ; JA 0045ee4a ; 0045ede8 JMP dword ptr [EAX*0x4 + 0x0045f834]`. The table at `0x0045f834` reads `45edef 45ee4a 45edfd 45ee4a 45ee0b 45ee4a 45ee19`, and each live target is one `MOV EDI,imm32`: **0 → `mfighter\` (`0x5bcac4`), 2 → `mmage\` (`0x5bcabc`), 4 → `ffighter\` (`0x5bcab0`), 6 → `fmage\` (`0x5bcaa8`)** — bit 1 the fighter/mage axis and bit 2 the sex axis, exactly `HERO-APPEAR-041`'s reading of those two bits, now with the four directory names they select. The leaf is then `0045f0da MOV EAX,[EDX+0x24]` (the face byte) and `0045f0e6 PUSH 0x5bca7c` `"%s%d.256"`, i.e. `sprintf(buf, "%s%d.256", dirPath, face)` — which is exactly the shape `SPR256-EQUIP-042` measured as face sheets named `1.256`..`31.256` directly under a figure directory, now with its selector. **And one node of the *portrait* tree is a doll layer**: `0045f147 MOV EAX,[EDX+0x20] ; CMP EAX,0x11 ; JL ; CMP EAX,0x15 ; JG` guards `0045f171 PUSH 0x5bca5c` `"graphics\infowindow\horse.bmp"`, and the only shipped ids in `[0x11,0x15]` are 19 and 21, whose own `DescText` reads `Hero of the Sword` and `Hero of the Lance`. `HERO-APPEAR-045` is untouched: it scopes its "neither routine reads the sex bit" to `FUN_0045fb00` and `FUN_0045bf00`, and this is a third routine | High (the jump table is read as four dwords out of the raw image through the section table, each live target one named `MOV`; the formatter, the range test and the face load are named instructions; the corpus shape this leaf produces was measured independently by `SPR256-EQUIP-042` before the selector was read) | ● active | [EXP-0138](../experiments/EXP-0138-actor-pictures/) |
| HERO-START-081 | *(amended by EXP-0221: the broad statement that character generation cannot supply the access item is withdrawn. The packed inputs and equipment paths remain negative; a separate gated helper is called during live-hero construction.)* **The character-generation command, selected templates, positional equipment cells and direct starting-weapon branch do not encode the document access item or change the purse.** The packed command carries five stat bytes and one appearance/class/sex byte, while the name is a separate `CString`. The four combinations select `PC_Danath`, `PC_Naira`, `PC_Fergard`, or `PC_Reniesta`. Their equipment cells are interpreted positionally as Weapon, Shield, and Armor, and the direct `FUN_004f992c` branch constructs one of 14 weapon literals. All 909 non-empty `Humans` equipment cells in each root contain no `Quest` string. No measured branch writes `Player+0x38` or calls the purse delta helper. After these paths, `FUN_004d3755` calls the conditional string producer `ITEM-DOC-069`; whether its `[0x005eb5a4] != 2` gate passes in a fresh campaign remains Unknown | High for the packed command, template/equipment population, direct weapon branch and scoped purse negative / **Unknown** for actual fresh-campaign access-item presence | ✔ promoted (amended) | [EXP-0154](../experiments/EXP-0154-campaign-start/), [EXP-0221](../experiments/EXP-0221-valuable-documents/) |

## Open items

1. **The magnitude's provenance** — `effect+0x3c` / `+0x3d` / `+0x40` are read by `HERO-EFFECT-019`
   but not sourced. `Data.bin`'s Magic row carries `affectMin` / `affectMax` / `UsableBy` and a
   per-slot column set, and `SHOP-MAGIC-007`'s enchantment stage builds the effects; neither hop
   was followed.
2. **`target->vt+0x30()`** — the predicate fifteen effect arms and every equip path branch on.
   *Narrowed by EXP-0069*: it is `FUN_005232d0` (returns 0) on the base actor class and
   `FUN_00523530` (returns 1) on both human classes, so it is a constant class discriminator, not
   state; what it is *called* is still unnamed.
3. ~~**Regeneration**~~ — *closed by EXP-0065*: the tick is the actor's `vt+0x14`, `FUN_004f4204`,
   and the two modifiers enter as percentages of `healthMax × 2` / `manaMax` (`HERO-REGEN-021`).
   What is left of it is narrower: what fills the two periods `actor+0x98` / `actor+0x9e`, what
   `FUN_004f41e8` measures against `0x50`, and what advances `[0x005cd758]`'s counter.
4. **The shape/material factor block** at `element+0x20` — `FUN_0050dadc` multiplies by doubles at
   `+0x38`, `+0x40`, `+0x48`, `+0x50`, `+0x60`, so an item's numbers cannot yet be predicted from
   its template row, which is why `HERO-ARMOUR-018`'s reconciliation is a fit.
5. ~~**The school's skill training**~~ — *closed by EXP-0187*: selection sends opcode `0x40` and
   displays the server price; enabled Train sends opcode `0x3d`, whose purchase raises the selected
   live level and sets its experience to `S(new level) + 1`. The five shipped choices map to slots
   `1,2,4,3,5`, so they cannot select General (`TOWN-GENERAL-107`, `TOWN-GENERAL-108`,
   `HERO-SKILLBUY-076`).
6. **The experience distribution** — `FUN_004f72d7`'s two class arms at `004f73cd`.
6a. **Both stat sweeps are now on record** and neither found a hop this ledger did not already
    own: `MAGIC-MIND-010` (`disp:88`, 461/235/4) and `MAGIC-SPIRIT-011` (`disp:8a`, 36/13/0). What
    they leave open for *this* ledger is `actor+0x13c`, the byte `Control Spirit` selects a victim
    by, and the blind spot both share — a wholesale actor copy carries no displacement.
7. **The character sheet's display divisors** for load and sight, and `actor+0xa0` — now with a
   sharper case: `FUN_004e7de3` ships `actor+0xb4`/`+0xb5` over the wire raw, so whether the sheet
   prints `[base, base+spread]` is a client question, and settling it closes rather than bounds
   `HERO-ARMOUR-018`'s `УРОН` line.
8. **`typeID 0x13 / 0x15`**, `HERO-SPEED-008`'s `+10` arm: no human typeID lies in that range.
9. ~~**`actor+0x12c`**~~ — *closed by EXP-0069* (`HERO-REACH-025`): it is the reach, the ctor
   default is 1 (`004f3427`), neither actor streamer writes it, and `FUN_004fb702` compares it
   against a footprint-adjusted Chebyshev distance. It is also the radius of the one
   auto-acquisition route read so far (`HERO-TARGET-024`).
10. ~~**The `+0xb7`/`+0xb8` damage pair**~~ — *closed by EXP-0069* (`HERO-DMG2-029`): the writer
    is `FUN_004f5604`'s `attackKind == 1` arm, the component is not gated on the hit roll, no
    absorption touches it, and no shipped class takes that arm. What is left of it is one
    bounded question: whether any **modifier block** sets its offset 0x11 through
    `HERO-MOD-016`'s wholesale fold, which a `disp:` sweep cannot see.
11. ~~**`actor+0x4c` bit 2** — no located writer~~ — *the writer is located by EXP-0133*
    (`HERO-HP-071`): on the placement path `004f94d0` ORs it in, gated on the streamed `ManaMax`
    column being positive (`004f94b6`…`004f94bf`), and the base constructor's `004f30ee` writes 0.
    So the bit is **derived from the mana column, not authored per row**, and it cannot be set
    independently of mana on that path. What is still Unknown is which way the archetype name runs
    on the **chargen** path, where a different object supplies it (`FUN_0047c590`), and whether a
    chargen actor can carry the bit with a zero mana maximum. The four consumers are unchanged.
12. **`world+0xa4554`** — the actor list `FUN_005327d0` scans. Written by the sim world
    constructor, read by fourteen routines; what determines its membership is unread, so
    `HERO-TARGET-024`'s candidate set is bounded only by its radius test.
| HERO-NAME-079 | **`actor+0x80` is a `CString` and it is the actor's name — every actor has one, monster included, and it is per-instance rather than per-class.** The field is constructed and destroyed in all six routines `UNIT-DERIVE-003` enumerated for the base actor vptr store — `004f2be4` (`FUN_004f2bb4`), `004f2ce6` (`FUN_004f2cb8`, the `Units` arm's `new(0x198)`), `004f2dd1`, `004f2ebe`, `004f2fcc` and `004f36b2` (the destructor-shaped sixth) — plus `004f32c2` on the human path, each `MOV ECX,[ebp-slot] / ADD ECX,0x80` into an MFC `CString` constructor or destructor (`EnumRefs re:ADD E.X,0x80$`, 75 hits / 42 owners; the seven above are exactly the actor family). **Four writers, and they are the whole authoring surface.** (1) `FUN_004f9065` at `004f9230` assigns the literal `"Unknown"` (`0x5c7d84`, `EnumRefs refto:5c7d84` = 1 hit) and at `004f9255`/`004f9276` an element of a default-name array (`SESS-DEFNAME-032`). (2) `FUN_004d3755` at `004d3d80`..`004d3d90` — `MOV ECX,[EBP+0x8] / ADD ECX,0x18 / PUSH ECX / MOV ECX,[EBP-0x18] / ADD ECX,0x80 / CALL 0x00572b03` — copies a `CString` from the request object's `+0x18`. (3) the `.alm` spawner `FUN_004e26bb` at `004e292d`, immediately after the `Humans` constructor `FUN_004f8e78`. (4) `FUN_004e0f26` at `004e10cf`, on the key literal `"Name"` (`0x5c6c88`). **One consumer is known and it uses the field as a presence test, not as a lookup key:** `FUN_004e32c2` walks an actor list and at `004e3362`/`004e340a` calls `GetLength(actor+0x80)`, skipping every actor whose name is empty before evaluating a sex/class predicate (`FUN_0048c980`, whose own literals are `Female`, `MySex`, `MyClass`, `Mage`). So an unnamed actor is invisible to that scan | **High** that the field is a `CString` at `+0x80` on the actor and that the seven ctor/dtor sites are the actor family (each is a cited two-instruction idiom, and the owner set is identical to a set published independently for the vptr store) / **High** for the four writers (each disassembled, not decompiled) / **Medium** for "these four are the whole authoring surface": the instrument is a regex over `ADD E.X,0x80`, which cannot see a write through a pointer already advanced past `+0x80`, and no such write was hunted separately | ● active | [EXP-0143](../experiments/EXP-0143-actor-names/) |
| HERO-TYPED-080 | **The player's typed character name does not travel in the six-byte `0x48` command: it arrives beside it, as a `CString` at the request object's `+0x18`.** `SESS-HERO-014` reads the command builder `FUN_0041fd61` as five bytes into `cmd+0x0a..0x0e` and one packed byte into `cmd+0x0f`, and the accepting routine `FUN_004d3755` as taking six bytes. The name is not among them. What `FUN_004d3755` does with the name is `004d3d80 MOV ECX,[EBP + 0x8] / ADD ECX,0x18` — the **first argument**, not the byte block — pushed into `CString::operator=` on `actor+0x80`, three instructions after the last stat store (`004d3d79 MOV word ptr [ECX + 0x9a],AX`) and immediately before the `+0x130`/`+0x8c` autohit test. So on the campaign path the actor's name is **exactly** what the field held, with no fixup, no case change and no truncation beyond the ten bytes `TEXT-NAMEIN-024`'s control already refused to store; and the default-name arrays are not consulted at all, because `FUN_004f9065`'s gate (`SESS-NAMEGATE-033`) fails on a `PC_<name>.<sex><face>` template. The G2 limits that follow, stated as limits: **length** 10 bytes, enforced at the keystroke; **charset** whatever the input converter can emit, which is `0x20..0xFF` minus the unreachable stored values `0xC0..0xDF` and `0xF0..0xFF` under selector 1 (`TEXT-COLL-025`); **storage** an MFC `CString` on the actor, so no fixed buffer to overflow; and **key-ness** — the name gates `FUN_004e32c2`'s scan by being non-empty (`HERO-NAME-079`), so an empty name is not merely cosmetic | **High** for the assignment and its position (the whole window is disassembled, and `[EBP+0x8]` is the argument the frame receives) / **Medium** that the `+0x18` `CString` is the *typed* name rather than a name the accepting side composed: the producer side of that request object was not read this round, and what discriminates it is only that no other writer of `actor+0x80` is on the campaign path and the field is a `CString` at a fixed offset of the same argument the six bytes ride in | ● active | [EXP-0143](../experiments/EXP-0143-actor-names/) |
| HERO-CHARGEN-082 | **Reset preserves the selected skill and every identity choice, but reconstructs the whole preview selected by them.** `FUN_0042fb20` writes pool `100` and four stats `25`, reads `skillPanel+0x134`, adds one and passes that same index to `FUN_0047bec0`. With `this = main-frame+0x420`, the setter writes the four stats at `main-frame+0x4b0..+0x4bc` and the unchanged skill at `+0x4c0`, then `FUN_0047bf40` creates a fresh one of the four archetype actors, zeroes the five class skills and sets the chosen one, derives all combat mirrors, releases all twelve preview equipment objects and rebuilds the starting objects. No instruction in the handler or reconstruction writes name `+0x430`, difficulty `+0x65c`, class/sex `+0x494` or appearance `+0x4c4`; no live actor or `Player+0x34` is installed | **High.** The direct handler is read whole, its only direct caller is enumerated with zero orphan, and the reconstruction routine is read to its return. The rival “only four scalar writes” is killed by the twelve-object release/rebuild loop; the rival “full draft reset” is killed by the current-index read and the disjoint identity fields | ● active | [EXP-0158](../experiments/EXP-0158-chargen-controls/) |
| HERO-CHARGEN-083 | **Detailed Back is a return to pre-create, not a cancel of the campaign draft; stat and skill edits are discarded one transition later.** Closing `main-frame+0x358` with `0x446` calls `FUN_00479f50`, which copies name `+0x430`, difficulty `+0x65c` and class/sex `+0x494` back into pre-create `+0x360` and opens it. It does not destroy `server+0x3f54` or write stats `+0x4b0..+0x4bc`, skill `+0x4c0` or appearance `+0x4c4`. But the next pre-create Forward unconditionally calls `FUN_0047c590`, reloading archetype defaults into those stat/skill fields before reopening detailed generation. Pre-create Back is a different arm: it destroys the temporary record and opens the participant/lobby screen at `main-frame+0x35c` | **High** for the object transitions and write set (both close arms and both openers read whole; the two models are separated by the temporary-record destructor appearing only on pre-create Back) / **Unknown** for the user-visible label of `+0x35c`, which the image does not name | ● active | [EXP-0158](../experiments/EXP-0158-chargen-controls/) |
| HERO-CHARGEN-084 | **Name validation occurs at final Play's participant join, not at pre-create Forward: positive length, an exact two-name forbidden table, then duplicate and returning-character gates.** The ten-byte control remains `TEXT-NAMEIN-024`'s only upper bound. Join command 2 carries the field at packet `+0x16`; `FUN_004d27dd` first requires `CString::GetLength > 0` (error 1), then loops exactly two pointers at `0x005c2320`, the image strings `Self` and `Computer`, through case-insensitive `FUN_00555d80` (error 2), then compares each `Player+0x18` byte-exact through `FUN_00553d80`, permitting only the same returning connection (error 3 otherwise), and rejects a returning participant already bound to a character (error 12). The local-server `Self` sentinel is substituted with the first participant's name before the forbidden loop. Thus, apart from that sentinel arm, the shipped keyboard accepts every non-empty 1..10-byte sequence its converter can store except ASCII-case variants of those two whole strings and a byte-exact conflicting participant name. Client `FUN_0040fed4` selects localized `stringTable[192+error]`, closing the former open consumers at indices 193/194/195 | **High** for the ordered predicates, table extent and comparison semantics (the join routine, both compare bodies and the one table reference are read; its pointers resolve to two strings and no third slot is indexed) / **Medium** for the complete visible refusal experience because no original window was driven after an error | ● active | [EXP-0158](../experiments/EXP-0158-chargen-controls/) |
| HERO-CHARGEN-085 | **The typed field becomes a live actor only in successful command `0x48`, after participant validation; a rejected Play has already consumed UI and preview state.** Producer chain: pre-create Forward copies its input `CString` to `main-frame+0x430`; `FUN_0041023e` copies that to join packet `+0x16`; `FUN_004d27dd` passes it into `FUN_004d35e6`, which stores the participant name at `Player+0x18`; the sole caller `FUN_0047bd50` then sends `0x48`, whose dispatcher resolves that Player and whose `FUN_004d3755` copies `Player+0x18` unchanged to `actor+0x80` while constructing and installing the `Human`. This promotes `HERO-TYPED-080`'s producer half from Medium. Before an invalid name is rejected, detailed chargen has closed, the ready bit is set, networking is reset and `server+0x3f54` is released; the rejection calls neither participant factory nor actor constructor. The generic close latches screen `+0x5c` to zero before posting the transition, so repeat Back/Play while inactive cannot repeat it; Reset remains active but converges to the same reconstructed draft | **High** for producer, commit point, object non-creation and close idempotence (each hop is a named copy/call, `FUN_0041fd61` and `FUN_004d35e6` each have one direct owner on the relevant path, and actor installation is after all join rejects) / **Unknown** for the visible screen after a dismissed rejection because the main handler ignores the Boolean return and runtime was not driven | ● active | [EXP-0158](../experiments/EXP-0158-chargen-controls/) |
| HERO-GENERAL-086 | **`Skill.General` is slot 0 of the six-word hero skill vector: live `actor+0xa8`, experience `+0x1cc`; it has no maintained base copy.** The `Humans` loader reads the six `Data.bin` titles in order into `+0xa8+2i`; creation writes `S(level[i])` to `+0x1cc+4i` for `i=0..5`. Both roots carry the same title order. The loader then copies live General to the preceding to-hit word `+0xa6`, excluding that field as the stored skill. The word at `+0x116` is serialized and both price arms index it for slot 0, but all twelve base restore/snapshot/fold loops initialize their index to 1 and end at 5: five in `FUN_004f72d7`, two each in `FUN_004f7cd7` and `FUN_004f7dfc`, and one each in `FUN_004f974d`, `FUN_004f7c28`, and `FUN_004f7a53`. It is a decoupled shadow, not a live-General base maintained like `+0x118..+0x120` | High (two independent six-slot loops fix the live/xp alignment; all twelve loop initializers and bounds are raw-byte assertions that refute the rival base-copy model) | ● active (corrected after two independent falsifications) | [EXP-0187](../experiments/EXP-0187-general-skill/) |
| HERO-GENERAL-087 | **The shipped `Humans` corpus uses General as authored data, not a constant or reserved column.** In each root, 210 rows have a full parameter block; General is 0 on 128 and nonzero on 82: 10×4, 20×12, 30×7, 40×14, 50×6, 60×8, 80×21, 99×10. Starting templates 26..29 all store 0 | Medium (exhaustive dual-root corpus measurement; distribution alone does not identify behaviour) | ● active | [EXP-0187](../experiments/EXP-0187-general-skill/) |
| HERO-GENERAL-088 | **General's shipped hero producers are initialization, death loss, opcode `0x3d`, and archive load; no play award raises it.** The `Humans` load initializes live slot 0 and creation initializes `xp[0] = S(level)`; death reduces all six live/xp slots; purchase accepts selected slot 0..5 and raises both live level and experience. Kill, hit and cast awards exclude slot 0. `Unit::Serialize` sends fixed 24-byte blocks beginning `+0xa6` and `+0x114` through the same helper in both archive directions, while the human tail preserves six experience dwords (`SAV-HEROSKILL-064`); this preserves live General and the separate `+0x116` price shadow, not a maintained General base. Two generic seams are unexercised by shipped inputs: `FUN_004d1f42` calls `FUN_004fe11e`, whose complete body reads byte 0 of Spell-row parameter 2 (`Sphere`); the caller masks it to a byte, then `004d207a` writes `live[Sphere]`, so custom Sphere 0 touches General; the chargen helper can write its caller's slot although every shipped caller supplies 1..5 | High for the named routines, the complete Sphere-accessor-to-indexed-store chain, and fixed archive blocks; Medium for image-wide completeness because a wholesale actor copy is the stated field-sweep blind spot | ● active (corrected after two independent falsifications) | [EXP-0187](../experiments/EXP-0187-general-skill/) |
| HERO-GENERAL-089 | **The shipped pre-create, detailed generation, character information, and school paths do not display or select General.** Pre-create stores identity choices only. Detailed generation loads and updates five class icons beginning at slot 1, and every shipped caller of its selected-slot helper supplies 1..5. The information streamer and renderer transfer five words `+0xaa..+0xb2`. The school exposes five indices and maps them to `1,2,4,3,5` for either class. The whole-image `disp:a8` result is 260 hits / 118 owners / 1 unrelated orphan; all fifteen actor-family owners are classified, and none is another UI owner | High for the four complete read paths / Medium for the image-wide negative because a pointer advanced outside its owner remains invisible to the displacement sweep | ● active (expanded after independent falsification) | [EXP-0187](../experiments/EXP-0187-general-skill/) |
| HERO-GENERAL-090 | **General is the ranged-weapon accuracy skill.** The ranged arms of `Weapon::Equip` copy `actor+0xa8` to to-hit modifier `actor+0xe6` (`0050e026`/`0050e02d`) and set active skill `+0xb6` to 0. The modifier fold adds `+0xe6` to live to-hit `+0xa6`. Melee instead selects slots 1..5 through the weapon attack type | High (the equip arm and modifier fold are read at instruction level; the two weapon models write different fields) | ● active | [EXP-0187](../experiments/EXP-0187-general-skill/) |
| HERO-GENERAL-091 | **General experience also contributes indirectly to maximum health and mana.** Creation and every experience writer preserve `actor+0x130 = sum(xp[0..5])`; `FUN_004f7dfc` reads `+0x130` in both maximum-stat formulas. General does not contribute to the victim XP value, whose reducer sums slots 1..5 only | High (the six-slot sum, both derived reads, and the five-slot control are named instruction loops) | ● active | [EXP-0187](../experiments/EXP-0187-general-skill/) |
| HERO-GENERAL-092 | **The two General bonus effect kinds are inert on a recomputing human, and there is no alternate live-General arm.** The dispatch has two contiguous six-arm groups, `fighterSkill0..5` and `mageSkill0..5`. Their slot-0 arms at `0050233b` and `005024a9` use the opposite class predicates and add only to modifier `actor+0xe8`; the other ten arms are controls using `+0xea..+0xf2`. The human derive restores and folds only indices 1..5, so it never reads `+0xe8`. Contrary to the first EXP-0187 candidate, these arms do not contain a non-recomputing-target branch that writes live `+0xa8` | High (both six-arm groups and the derive loop bounds are re-read at instruction level) | ● active (corrected after independent falsification) | [EXP-0187](../experiments/EXP-0187-general-skill/) |
| HERO-GENERAL-093 | **G2 has eight fixed-stage numeric boundaries, plus a dynamic aggregate boundary, fixed six-slot storage, a decoupled slot-0 price word, and data-indexed Sphere 0.** Levels are six `u16`; experiences are six `i32`; save helpers copy fixed 24-byte blocks beginning `+0xa6` and `+0x114`, while the experience tail has six dwords. General has no live clamp and purchase has no 100 test. Ordered by numeric level/base input, the first fixed-stage crossings are: **(1)** per-slot XP signed-`i32` at new level 153, storing `S(153)+1 = 0x8056f100`, signed `-2,141,785,856`; **(2)** per-slot XP `u32` at 161, wrapping `4,615,536,784` to `0x131b8090`; **(3)** price signed-`i32` at maintained base 170, comparing `0x81bce163`, signed `-2,118,327,965`; **(4)** price `u32` at 178, wrapping `4,665,819,714` to `0x161ac242`; **(5–6)** XP and price first leave the signed-qword input range of `FISTP` at 386 and 403; **(7)** new level `0x8000`, where `MOVSX` passes `-32768` to `S`; **(8)** old level `0xffff`, where `ADD AX,1` stores `0x0000`. Live General and its serialized price shadow are independent inputs, so this ordering is not one execution timeline. The six-slot total is another signed dword and can cross its sign and wrap boundaries earlier depending on the other five values. The full `__ftol` body truncates through `FISTP qword` and returns `EDX:EAX`; XP and price callers use only EAX. Signedness changes are engine-policy changes within existing widths. Lifting the per-slot/total dword wrap requires wider actor and save fields but no `Humans` level-schema change; lifting the price wrap requires wider price, purse, and message state; extending the conversion range requires a conversion-policy change; lifting the level wrap requires wider actor, `Humans`, and save fields. Exact downstream behaviour after these unexecuted transitions, including the original conversion result beyond signed-qword range, remains Unknown. Adding a seventh slot changes the `Humans` schema, overlaps following actor fields and changes save bytes. Making `+0x116` a maintained General base fits the existing save width and requires no shipped data-file rewrite, but changes price behaviour and newly written saves. Shipped spells have 0/28 Sphere 0 on both roots, yet the complete `FUN_004fe11e` parameter-2 accessor and `004d207a` writer, plus `FUN_004f5998`, `FUN_004fe13b`, and `FUN_004feadb`, accept index 0: custom Sphere 0 changes only the authored spell row, not the schema or save width. Adding General to school is a code/UI mapping change, not a data-format lift | High for widths, all eight fixed-stage boundary encodings and their order, the dynamic total-width classification, all loop bounds, and raw indexed seams; the exact boundary values are a reproducible IEEE-754 probe whose wide margins make the ordering insensitive to last-bit `pow` differences; Unknown for downstream runtime behaviour after any unexecuted transition | ● active (corrected after four independent falsifications) | [EXP-0187](../experiments/EXP-0187-general-skill/) |

| HERO-ITEMSKILL-096 | **Item-spell experience belongs to the actor class arm, not to an item proficiency: plain Units receive none, and only player-character-band Humanoids can receive it.** Damage and kill caller gates apply before the sink: damage requires a victim owner with `+0x28 != 0`, while kill omits that participant test; both require a victim owner and refuse owner `+0x5c != 0` in multiplayer. Plain Unit vtable `0x0059c3c0` maps kill, damage and cast award slots to three empty stubs; the shipped Catapult and Ballista can release their rider but never train. The sink first requires `actor+0x0e` in `[0x21,0x3f]`; transferred low-type Humans train nothing. After that gate, `actor+0x4c & 4` set maps a nonzero spell id through `Spells[id].Sphere`; with it clear, the damage routine passes slot zero and the sink substitutes `actor+0xb6`, the currently equipped weapon's attack skill. The sink scales the incoming signed amount by `trunc(amount*(Mind/30+0.25))`; it has no zero or negative refusal, and a negative value below the positive cap reduces slot and aggregate XP without lowering the level. It refuses live same-owner and protected-relation sources, caps one award at `S(n+1)-S(n)` or one fifth in a multi-participant session, and refuses a slot already at 100. It writes `Humanoid+0x1cc+4*slot` and aggregate `Unit+0x130`; a strict threshold crossing raises `Unit+0xa8+2*slot` exactly once and re-derives the actor. The six levels, six per-skill values and aggregate are restored directly by the save reader. On a fighter route, admitted spell-side awards run before the physical XP test and all use the current weapon skill; the physical award is then absent if the spell moved target health to `-10` or below. An admitted caster's item spell changes its school only through damage or pseudo-damage, never for release alone. | **High.** The EXP-0191 raw probe asserts the recipient band, class selection, slot and skill ceilings, both cap calls, the school-arm writes, rider-before-physical order and the serializer fields. `HERO-SKILLUP-073` and narrowed `HERO-SKILLGATE-074` independently establish the Mind, ownership, relation and participant gates plus both arms' complete write paths. A separate caller census excludes an item-specific writer on the complete item release graph. Save persistence is independently established by `SAV-HEROXP-063` and `SAV-HEROSKILL-064`. | ● active | [EXP-0191](../experiments/EXP-0191-itemcast-training/) |
| HERO-104 | **The sheet's row formatter, its two fractional conventions, and what each reads.** `FUN_00460480` (`0x00460480`..`0x004610b0`, one `ret` at `004610ad`, confirmed both by its own SEH prologue and by the next function's SEH prologue starting exactly at `004610b0`) is the character sheet's row-by-row string builder. Five `sprintf`-style literals are referenced from inside this function, at 18 push sites total, each confirmed by a whole-image reference count and an in-function range count (`evidence/refs-range-format-literals-in-0460480.txt`): `%d` (`0x005b8e1c`, 37 whole-image refs, 11 inside this function, not traced to source fields), `%d.%d` (`0x005bcbd0`, 2 whole-image, 2 inside), `%d-%d` (`0x005bcbd8`, 2 whole-image, 2 inside, `HERO-SHEET-038`'s damage line), `%d/%d` (`0x005bcbec`, 5 whole-image, 2 inside), and `#%s: %d-%d` (`0x005bcbe0`, 6 whole-image, 1 inside at `00460983`, pushing `[edx+0x1d8]` off a global pointer loaded from `0x005eb3d4` — not traced to an actor field). Both `%d/%d` sites push the drawable's health/healthMax (`[EBP+0xfc]`/`[EBP+0x100]`) and mana/manaMax (`[EBP+0x13c]`/`[EBP+0x13e]`) pairs — `panel-fields.csv`'s own offsets — printing current before max, plain integers. The first `%d.%d` site (`00460c7f`) reads `[EBP+0x102]`, the drawable's **whole** sight u16 copied verbatim from `actor+0xa4` (not the high byte alone), and prints `whole = raw >> 8` then `frac = ((raw & 0xff) × 10) >> 8` — one decimal digit, truncated toward zero. This closes `HERO-SIGHT-007`'s divisor Unknown for sight: the sub-cell scale it already names (`× 256`) is exactly this display's divisor. The second `%d.%d` site (`00460f2f`) reads `[EBP+0x106]` and prints `raw / 10` then `raw % 10` — decimal tenths, a different convention from the first, so the sheet does not format every fractional row through one rule. `[EBP+0x106]` is not a field `panel-fields.csv` names. The character-screen fill `FUN_0047bf40` writes it to a literal 0 (`0047c2c1`, word) immediately after filling sight and speed, together with the following dword `+0x108` (`0047c2c8`), rather than sourcing either from an actor field on the pass this round read. Besides that zero-fill, one other writer of `drawable+0x106` is confirmed: a propagating copier at `00458e47`/`00458e4e` reads `[ebx+0x106]` off one object and stores it into `[ebp+0x106]` of another, and a third writer sits at `00411a30` (`mov word [eax+0x106], dx`, a non-zero source) inside the network-record filler `FUN_004104e8`, the third cache-filling routine `UNIT-PANEL-011` already names — not traced further this round. That site's true source field remains unidentified. Weight is drawn by neither identified fractional site. `FUN_0047bf40` (`0x0047bf40`..its one `ret` at `0047c4ae`) was read whole: no instruction *directly* resolves `actor+0x8e`, `actor+0x90` or `actor+0x92` (`ITEM-LOAD-005`'s own weight, load and capacity fields) off a base register holding the actor `this`. A raw byte-pattern search for the disp32 encoding of each of the three offsets, range-filtered to the function's real span (`evidence/refs-range-0x8e-0x90-0x92-in-0047bf40.txt`), finds zero occurrences of `0x8e` or `0x92` and exactly one of `0x90` (`0047c1c9`, resolving to `[ebp+0x90]`, an unrelated local written into `actor+0x84`). This is one occurrence, not two: an earlier, unbounded sweep's second `0x90` hit (`[ebx+0x90]`, a caption-pointer array element, disp32 at `0047c676`) sits past this function's own `ret`, inside the *next* function's body — that function's SEH prologue begins at `0047c590`, after `FUN_0047bf40`'s own jump table (`0047c4b0`..`0047c4c4`, five case targets) and 193-byte index table (`0047c4c4`..`0047c584`). Because `0x8e`, `0x90` and `0x92` all exceed 127, no `mod=01` (disp8) encoding can carry them as a displacement; every base-plus-displacement form that can (`mod=10` disp32, or the SIB-less absolute `mod=00,rm=101` case) writes the four displacement bytes literally into the instruction stream regardless of which base register is used, so the range-bounded scan is exhaustive over every possible base register for these three offsets, not only `esi`. The function does make one indirect call reaching those fields transitively: `0047c236`, `call [eax+0x50]`, with `eax` the actor's own vtable pointer (loaded at `0047c21b`, `mov eax,[esi]`) and `ecx = esi` (the actor `this`) set immediately before the call. `UNIT-DERIVE-003` gives the human vtable's `+0x50` slot as `FUN_004f7dfc`, and `HERO-SIGHT-007` already cites that same routine for both sight (`actor+0xa4`, `004f80e1`..`004f8114`) and carried load (`actor+0x8e`/`0x90`, `004f81b3`..`004f8206`) — so this call reaches all three excluded fields one level down, even though `FUN_0047bf40`'s own body never touches them directly; what the recompute does with them, and whether either fractional site's value passes through it, was not traced this round. The tag-list appender (`FUN_00484af0`, `ITEM-PANEL-022`) is called from this function with tags including `0x0d`/`0x0e` (the weapon's own damage pair, matching `ITEM-PANEL-022`) and `0x26`/`0x29`/`0x32` (a weapon-range byte and a two-byte property pair read from an object a call on the equipped weapon returns, not the actor); none of the tags this pass located reads a load- or capacity-shaped field. `UNIT-DERIVE-003`'s "load/encumbrance routine" independently confirmed at instruction level, not carried forward from either prior transcription: `FUN_004f36f7` is `actor+0x8e += param` (a signed word), `actor+0x90 = actor+0x8e`, then, if `actor+0x7c` is non-null, `actor+0x90 += (actor+0x7c)+0x20 / 2` under the identical `0xfa00`/`0x7d00` guard `ITEM-LOAD-005` gives for the full derive, followed by a load/capacity bucket-change test that conditionally calls `this->vt+0x50()` — the same formula as `FUN_004f7dfc`'s, reproduced by a second routine rather than assumed from its callers | High for the row table, the sight divisor, the corrected addresses and the five-literal/18-site count (every cited site is a named literal with its whole-image and in-function reference counts given, both fractional sources are read at instruction level, and every address in this row was independently re-derived from a raw byte-pattern search against the shipped executable, not carried from a single hand transcription) / High for the `FUN_0047bf40` direct-access absence: the function's real span was read, the range-bounded scan is exhaustive for these three displacements under any base register, and the one raw `0x90` hit inside the true function bounds was individually resolved by base register / Medium that the field is unreachable at all: the `vt+0x50` call demonstrates one indirect path from `FUN_0047bf40` into `FUN_004f7dfc`, a routine independently cited as writing exactly these fields, so carried load, sight and capacity are all reachable from this function through that one call — whether either fractional site's printed value passes through that call's result was not traced / **Unknown**: where, or whether, Weight is drawn as its own row on the sheet, and the true source of the second `%d.%d` site's field. Plain `%d`-only format sites (11 inside this function, none paired with a divisor) were not traced to their source fields and could carry Weight as an unadorned integer — not searched | ● active | [EXP-0209](../experiments/EXP-0209-carried-weight/) |

| HERO-CADENCE-112 | **A retained physical order applies again after `charge + rangedExtra + relax + U[0,3] + humanoidPenalty + 2` actor ticks; the final two ticks are scheduler boundary, not a stored cooldown.** `FUN_004f37be` calls `FUN_005310e0` before dispatching the actor state. Phase 5 applies when countdown byte `actor+0x6c` reaches zero, then phase 7 loads recovery. At recovery zero `004f3e43`..`004f3e6a` writes phase 0 and completion byte `actor+0x136 = 1`, after the order machine has already run. On the following tick progress arm 1 sees the completion byte and `005311df`/`005311e3` clears progress and actor state. On the next tick pending order 2 re-arms state 3, and phase 0 can load charge. This ordering excludes zero or one boundary tick. The fields are per actor; the client animation clock and global tick counter do not release them | High (both routines are read whole; the decisive order-before-action, completion store and shared progress tail are raw-byte anchors on both byte-identical executables, and the rival zero/one-tick boundaries cannot satisfy their order) | ● active | [EXP-0234](../experiments/EXP-0234-action-cadence/) |
| HERO-CADENCE-113 | **An equipped Humanoid adds a clamped weapon-weight/Reaction penalty to every attack or cast recovery.** Behind `actor+0x74 != 0` and the Humanoid `vt+0x30` predicate, `004f3cef`..`004f3ddc` computes `clamp(IDIV(runtimeWeaponWeight + 5*(30 - Reaction), 12), 0, 12)` and adds it before relax and `U[0,3]`; signed `IDIV` truncates toward zero. Non-Humanoids and unarmed Humanoids add zero. Each shipped root has 210 Human rows: all 156 non-empty primary-weapon strings resolve, 54 are empty, and the authored-Reaction/runtime-weight result over all 210 is `0:147, 1:13, 2:24, 3:8, 4:13, 5:1, 7:2, 10:2`. Reaction effects can therefore change later cadence; Haste and Slow write the separate speed effect and do not enter this arithmetic | High for the formula and gates (the complete repeated expression, both clamps and the predicate call are named instructions and raw-byte anchors) / Medium for the dual-root population and effect reachability (exhaustive shipped data plus prior effect/derive claims) | ● active | [EXP-0234](../experiments/EXP-0234-action-cadence/) |
| HERO-CADENCE-114 | **Player and AI ownership does not select a second attack/cast executor after an order is armed, and the two weapon-spell routes keep different cadence identities.** Pending order 2 installs progress 1, actor state 3 and target `actor+0x5c`; pending orders 8 and 9 install progress 2 and state `0x0d`/`0x0e`. `FUN_005310e0` and the common action routine `FUN_004f37be` contain no ownership fork on those arms. Upstream player and AI code can fill different orders, but the armed bytes converge here. A caster with a Spell-bearing weapon is diverted from state 3 into state `0x0d` before the physical strike and takes the cast-state base recovery. Application then clears the item and Spell pointers at `004f3ca4`..`004f3cb7`, before the complication gate, so this route adds no `Complication Level`. A non-caster's weapon Spell is a rider inside the physical strike and does not replace that physical action's cadence | High for the shared executor and route distinction (the relevant order arms and action routine are read whole; the caster diversion, pointer clears and complication gate are read in their execution order, and the physical strike has one caller) / Medium for the ownership-negative wording beyond the two complete executor routines, because upstream order selection remains source-specific | ● active | [EXP-0234](../experiments/EXP-0234-action-cadence/) |
| HERO-CADENCE-115 | **Death cancels an in-flight action before dispatch; physical attack target validity is rechecked at start and application, but target death alone does not cancel the strike.** `FUN_004f37be` tests health before calling the order machine, clears actor/action state and enters death processing on health `<= 0`, so neither wind-up application nor recovery dispatch runs afterward. `FUN_004fb853` and `FUN_004fba0e` each refuse null or unlinked actor/target and a dead attacker. **Corrected by EXP-0270: only application (`FUN_004fba0e`) rejects a target outside current reach; start (`FUN_004fb853`) computes distance for extra delay without comparing it to reach**, agreeing with `AI-FACE-066` and `UNIT-STRUCTREACH-063`. The application path has no target-health refusal, agreeing with `HERO-FINISH-066`; the group routine instead cancels the retained attack when the target reaches teardown state `0x10` and can reacquire. This row does not generalise those target rules to every Spell arm and does not claim what every external command writer does to progress 1 or 2 | High for death-before-dispatch and the named physical checks (complete routines and raw-byte anchors) / Unknown for mid-action external order replacement and spell-specific target loss, explicitly outside the completed search | ● active (start/reach clause corrected) | [EXP-0234](../experiments/EXP-0234-action-cadence/), [EXP-0270](../experiments/EXP-0270-structure-attacks/) |

| HERO-JOIN-120 | **The shipped persistent-companion population has five producer origins and ten conditional source variants.** The complete 28-node instant-19/22 campaign census reduces to four `Hero` npc records whose constructor-overwritten type is in `[0x21,0x40)`: mission 40 unit 6/group 14/player 2 `npc25`; mission 70 unit 151/group 105/player 6 `npc23`; mission 100 unit 245/group 113/player 7 `npc24`; and mission 140 unit 443/group 16/player 5 `npc26`. The separate shipped town population has one `AddHero` entry, mission 30 value 22. Fixed ids select rows 42 and 43. For `DataBinID=26`, `FUN_0048cbe0` selects `26 + sex/class selector + 4*floor(mission/40)`, producing two town, two mission-70 and four mission-100 variants. | **High** for the four map records, selector formula and sole shipped `AddHero` entry (complete parsed data populations plus the selector, spawner, constructor and cull instruction paths) / **Medium** for image-wide producer completeness because a direct constructor reachable only through an unclassified callback or containing-object copy is outside the reference searches; no additional shipped data producer was found | ● active | [EXP-0242](../experiments/EXP-0242-joined-hero-payload/) |
| HERO-JOIN-121 | **Brian's source is mission-40 unit 6 `npc25`, Humans row/server id 42 `PC_Paladin`, not a campaign `AddHero` grant.** The source actor is owned by player 2 in group 14. It is a male fighter, actor type `0x21`, definition type 5, face 1, figure `mfighter`, weapon body `swordsman2h`, geometry id 5. Its localized instance name is EN `Brian` (`427269616e`) and RU `Бриан` (`81e0a8a0ad`). | **High.** The complete map source record, fixed `DataBinID`, constructor mode, localized-name lookup and both shipped data rows agree; the separate town producer excludes the `AddHero` model | ● active | [EXP-0242](../experiments/EXP-0242-joined-hero-payload/) |
| HERO-JOIN-122 | **Mission-70 Naira exists only for a male primary and uses Humans row/server id 31 `PC_Naira_2`.** `npc23` applies `!Mage,!MySex,Start`; the male-primary selector is 1, and the mission tier adds 4. Unit 151 begins under player 6 in group 105. The selected actor is a female fighter, actor type `0x22`, definition type 14, face 1, figure `ffighter`, weapon body `archer`, geometry id 14. Her instance name is EN `Naira` (`4e61697261`) and RU `Найра` (`8da0a9e0a0`). A female primary instead selects row 30 `PC_Danath_2`. | **High.** The raw selector instructions discriminate row 31 from base `PC_Naira` and later saves; both roots supply the same map, registry and Humans values | ● active | [EXP-0242](../experiments/EXP-0242-joined-hero-payload/) |
| HERO-JOIN-123 | **Brian's six source levels are `[0,25,3,0,0,1]`, with per-skill XP `[0,9834,331,0,0,100]` and aggregate 10265. Mission-70 Naira's are `[0,24,5,0,0,38]`, `[0,8849,610,0,0,36404]`, and aggregate 45863.** Slot order is General, Blade/Fire, Axe/Water, Bludgeon/Air, Pike/Earth, Shooting/Astral. There is no separate overall hero-level scalar in this payload. The aggregate is the sum of the six exact `S(level)` values at construction. Naira's known-spell mask is zero. The value 1593 belongs to the base mission-30 mage templates selected by `AddHero`, not to mission-70 Naira. | **High.** The selected source rows carry all six levels; the complete six-slot construction loop fixes each XP value and aggregate, and the selector path excludes the row whose aggregate is 1593 | ● active | [EXP-0242](../experiments/EXP-0242-joined-hero-payload/) |
| HERO-JOIN-124 | **Brian's source-initial derived payload is B/R/M/S `41/39/25/21`, max HP/MP `157/0`, speed 18, sight 1679 raw 1/256-cell, capacity/load `411/457`, to-hit/defence/absorption `97/76/5`, physical base/spread `20/15`, and protection `0/10/10/10/10/10`. Naira's is `39/41/22/26`, `177/0`, 20, 1669, `391/179`, `177/88/1`, `17/11`, and `0/32/13/29/13/13`.** Protection order is physical, fire, water, air, earth, astral. The initial current pools equal the maxima. A map join does not restore those current pools: Brian is an authored attack target before transfer, so current HP at his transfer is combat-dependent. | **High** for source-initial values and the transfer-preservation boundary (exact selected inputs through the instruction-level Human derive; complete transfer routine has no reset) / **Unknown** for one constant live current-HP value at every transfer because such a constant does not exist | ● active | [EXP-0242](../experiments/EXP-0242-joined-hero-payload/) |
| HERO-JOIN-125 | **Brian joins with zero carried and seven worn count-1 instances:** slots `1:0201106` Uncommon Steel Two Handed Sword, `6:0206110` Plate Helm, `7:0207116` Chain Mail, `8:0208119` Plate Cuirass, `9:0209122` Plate Bracers, `10:0210126` Scale Gauntlets, `12:0212130` Plate Boots; all have no magic effect and stored prices `4800,2700,1020,4200,1800,1200,2400`. **Naira joins with zero carried and nine worn count-1 instances:** `1:0901220` Rare Magic Wood Short Bow (to-hit +22, stored 91488), `4:0504201` Rare Mithrill Ring (air +16, 37245), `5:0505202` Amulet (3200), `6:0506208` Chain Helm (4480), `7:0207116` Chain Mail (1020), `8:0508218` Cuirass (8000), `9:0509220` Bracers (3200), `10:1110124` Uncommon Hard Leather Soft Gauntlets (16), `12:0212129` Chain Boots (fire +19, 52822).** Empty slots are the complement through slot 12. The evidence table also records every weight, defence, absorption, damage, intrinsic hit, base price and destination offset. | **High.** The selected Humans positional cells resolve through the established item grammar and exact shipped definition/material/shape/effect rows on both roots; all non-empty cells resolve and all empty cells are enumerated | ● active | [EXP-0242](../experiments/EXP-0242-joined-hero-payload/) |
| HERO-JOIN-126 | **Every shipped join variant becomes a full permanent Human hero owned by player 1, but not the primary hero.** Map actions transfer the same actor object: `FUN_004d1e14` removes it from the old owner/group, rewrites ownership, appends it to player 1 and a new group, and does not reconstruct the Human or write `Player+0x34`. The town path constructs and inserts a full Human. Types `0x21..0x24` pass the persistence cull. Inventory `+0x7c`, all worn slots, progression, spellbook capability and serializer remain the ordinary Human fields. Fighters' empty spellbooks are source state, not producer loss. | **High.** Both complete insertion paths, the complete transfer write set, constructor type overwrite and persistence band exclude reduced-actor, reconstruction and primary-replacement models | ● active | [EXP-0242](../experiments/EXP-0242-joined-hero-payload/) |
| HERO-JOIN-127 | **Only the town producer has an explicit consumed campaign grant: mission 30 `AddHero=22`, cleared after activation. The four map transfers contain no destination-roster identity guard.** Ordinary mission progress and once-only trigger state prevent normal replay. Map transfer preserves the allocator-dependent runtime actor id; save `Token` values remain file-internal object keys, not durable hero ids. Forced replay is not refused by name, source row or actor identity inside the complete transfer routine. | **High** for the town key/clear, lack of a guard inside the complete transfer and identity-field meanings / **Medium** for the final forced-replay roster because the running original was not forced into that state | ● active | [EXP-0232](../experiments/EXP-0232-sav-campaign-state/), [EXP-0242](../experiments/EXP-0242-joined-hero-payload/) |
| HERO-JOIN-128 | **EN and RU select the same ten conditional source rows and produce identical numerical, progression, spell, item, price, presentation-selector and ownership payloads; only localized display-name bytes differ.** The ten variants are two mission-30 town rows, Brian, two mission-70 rows, four mission-100 rows and Rood Glaen. `payloads.tsv` and `items.tsv` enumerate every value and instance. | **High** for equality of the measured inputs and outputs (field-by-field comparison of both preserved roots, plus byte-identical executables) / release agreement is not used as evidence for selector or transfer semantics | ● active | [EXP-0242](../experiments/EXP-0242-joined-hero-payload/) |

`HERO-105`..`HERO-111` were allocated to EXP-0209 and are returned unused. The allocation
section in `claims/registry.md` is authoritative.

`HERO-GENERAL-094`..`HERO-GENERAL-095` were allocated to EXP-0187 and are returned unused. The
allocation section in `claims/registry.md` is authoritative.
