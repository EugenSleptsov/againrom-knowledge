# Claim registry — RES / LM container

Level 2 ledger. Index: [registry.md](registry.md) · spec: [`formats/res/format.md`](../formats/res/format.md). Legend: `✔ promoted` (also in the spec) · `● active` · `✖ retracted`. IDs are permanent.

> **Confidence re-graded by the standing sweep (2026-07-25).** The EXP-0001-era rows here are
> the oldest claims in the repo and the sweep expected them to be the weakest; they are not.
> EXP-0017/0032/0034 read the reader, the lookup, the writer and the node-insert, and between
> them they cover **every** field this ledger names — the header *is* a node record, so the
> `.reg` attestations carry the `.res` layout too. Two clauses were demoted. No new bytes read.

| ID | Claim | Confidence | Status | Evidence |
|----|-------|-----------|--------|----------|
| RES-MAGIC-001 | Magic = `26 59 41 31` ("&YA1"); LE u32 = `0x31415926` = π's first 8 digits (arithmetic only, no intent claimed) | High (**the case the scale's carve-out is written for**: a magic constant has no second model — and it is not even corpus-only here, since `RES-CODE-020` finds the immediate `0x31415926` in exactly four `rom.exe` functions, two `CMP` readers and two `MOV` writers. The one alternative worth naming, "these four bytes are data rather than a signature", is what those two `CMP`s exclude. The π reading is stated as arithmetic and claims nothing) | ✔ promoted | [EXP-0001](../experiments/EXP-0001-res-container/) |
| RES-HDR-002 | Header is 24 bytes; payload data begins at offset 24 | High (not a fit: `RES-NODE-016` shows the header **is** a node record, read field-by-field into the object's node slots by `FUN_004c90a0`, and the tail open takes the payload base from it) | ✔ promoted | [EXP-0001](../experiments/EXP-0001-res-container/) |
| RES-HDR-003 | `u32@16` = registry offset; registry `[@16,EOF)` length ≡ 0 (mod 32). **Second clause narrowed by EXP-0051:** the mod-32 identity is an **EN-corpus packer fact**, not a format rule — RU `MAIN.RES` ships `(EOF−@16) = 16151 ≡ 23`, and the engine never computes that length at all (RES-OPEN-026); the node array is still 32-stride (RES-GEOM-028) | High (offset semantics: the tail open seeks by this word; the 32-byte *stride* is pinned by the `SHL 0x5` in the shared lookup — `REG-REC-032`) / the region-length ≡ 0 clause: **EN corpus only** (see retracted.md) | ✔ promoted | [EXP-0001](../experiments/EXP-0001-res-container/), [EXP-0051](../experiments/EXP-0051-res-ru/) |
| RES-HDR-004 | `u32@20` = node count; the engine's only size input (alloc + bulk read length). **`= regLen/32` narrowed by EXP-0051:** the identity holds on EN 12/12 but fails on RU `MAIN.RES` (`504 ≠ 16151/32`); a reader deriving the count from region length rejects a file the engine opens (RES-OPEN-026, RES-ACCEPT-031) | High (count semantics — read as the allocation/read length by the tail open) / the `regLen/32` identity: **EN corpus only** (see retracted.md) | ✔ promoted | [EXP-0001](../experiments/EXP-0001-res-container/), [EXP-0051](../experiments/EXP-0051-res-ru/) |
| RES-HDR-005 | `u32@8` = root count (top-level / unowned nodes) | High (it is the root node's own `size` field — the child count a directory node carries, iterated as the loop bound by `FUN_004ce8e0`; `RES-NODE-016`/`REG-REC-032`) | ✔ promoted | [EXP-0001](../experiments/EXP-0001-res-container/) |
| RES-HDR-006 | `u32@4` and `u32@12` — **meaning resolved in EXP-0017** (they are the root directory node's `off` and `type` fields; see RES-HDR-017 / RES-HDR-018). Value laws pinned on the widened corpus in RES-HDR-012 (`@4`) / RES-HDR-013 (`@12`) | Resolved (meaning) | ● active | [EXP-0001](../experiments/EXP-0001-res-container/), [EXP-0014](../experiments/EXP-0014-res-opaque/), [EXP-0017](../experiments/EXP-0017-res-open/) |
| RES-NODE-007 | Node = 32 bytes `{u32, u32 off, u32 size, u32 type, char[16] name}`; name 0xCD-padded | High (the same record `REG-REC-032` attests four ways in `rom.exe` — stride from `SHL 0x5`, `name @+0x10` fixed from the **writer**'s 15-character clamp. The `0xCD` padding is a measurement, and it is the CRT's uninitialised-fill byte, i.e. a fact about the packer, not a format constant) | ✔ promoted | [EXP-0001](../experiments/EXP-0001-res-container/) |
| RES-NODE-008 | `type` 0=file (byte range in `[24,regOff)`), 1=dir (first-child index + count) | High (`RES-HDR-018` reads the flag word instruction-level — `1` = directory, bit 4 = sorted — and `REG-REC-032` shows the directory case computes `&records[node->off]` and iterates `node->size`, which is what "first-child index + count" asserts) | ✔ promoted | [EXP-0001](../experiments/EXP-0001-res-container/) |
| RES-TREE-009 | Nodes form a tree; corpus-wide all reachable, no cycles, file ranges tile `[24,regOff)` exactly (60 blobs / 4592 nodes, 0 violations) | High **as a measurement** — and it is worth saying what that does and does not buy: exact tiling is the invariant EXP-0030 showed can hold under two framings at once, so it corroborates the layout rather than pinning it. What pins it is the reader (`RES-NODE-016`, `RES-HDR-017/018`). This row claims only the measurement, and the measurement is exhaustive | ✔ promoted | [EXP-0001](../experiments/EXP-0001-res-container/) |
| RES-SCOPE-010 | `.LM` = same container (empty archive valid); `Allods/*.RES` = identical container | High (a measurement over every such file in this install — and the extension plays no part in the binary either: one reader validates the magic, `RES-CODE-020`) | ✔ promoted | [EXP-0001](../experiments/EXP-0001-res-container/) |
| RES-NODE-011 | Node `u32@0` — observed 0 in standalone archives; widened + hash/id refuted in RES-NODE-014; **meaning resolved in EXP-0017** (node record's reserved word, never dereferenced by lookup/descent/sort — RES-NODE-019) | Resolved (meaning) | ● active | [EXP-0001](../experiments/EXP-0001-res-container/), [EXP-0014](../experiments/EXP-0014-res-opaque/), [EXP-0017](../experiments/EXP-0017-res-open/) |
| RES-HDR-012 | Header `@0x04` ∈ `{0, non-root-node count}` over the 60-blob widened corpus (12 standalone + 44 nested `.reg` + 4 save state stores): `0` in 57 blobs (all 48 inline REG stores, the empty archive, 8/11 tail archives), `= nNodes−roots` (Σ dir child-counts) in exactly graphics(3120)/main(491)/sfx(274). **Not** recomputed (VIDEO4/VIDEO8/movies/speech/world have 30–359 non-root nodes yet store 0), **not** a checksum (`blob_sum32` unrelated), and the populate-vs-zero choice has **no** structural predictor (depth/dirs/nesting/contains-REG all falsified). Which archives populate it → `rom.exe` writer routine. **Resolved by RES-HDR-017 [EXP-0017]:** `@0x04` = the root directory node's `off` (index of the first top-level node); `{0, ownedNodes}` is the packer's roots-first vs roots-last layout order, not a recomputed count. **Scope narrowed by RES-HDR-029 [EXP-0051]:** the `{0, nNodes−roots}` law is EN-corpus only — RU `SFX.RES` stores a mid-table `186` | High (value law, **EN corpus**) / Resolved (meaning, EXP-0017) | ● active | [EXP-0014](../experiments/EXP-0014-res-opaque/), [EXP-0017](../experiments/EXP-0017-res-open/) |
| RES-HDR-013 | Header `@0x0C` is a per-file constant ∈ `{1, 17}` with no third value in 60 blobs: `17` in every inline REG-style store (48/48) + `graphics.res` + `main.res`; `1` in the other 9 tail archives + the empty archive. `17 ⟺ contains-REG` / `⟺ inline-flavor` / `⟺ has-dirs` all falsified (main`@17` has 0 nested; VIDEO4`@1` has 18; graphics/main are tail; VIDEO4 has 15 dirs `@1`). Bit 4 (`0x10`) is the discriminator; a format-variant / writer-generation flag. **Resolved by RES-HDR-018 [EXP-0017]:** `@0x0C` = the root directory node's `type/flags` (`1`=directory); **bit 4 = children-sorted flag** the reader tests to pick binary vs linear search (`17`=dir+sorted), set by the writer's finalize-sort. **Association narrowed by RES-HDR-030 [EXP-0051]:** RU ships `1` on all 11 archives incl. graphics/main — "17 on graphics+main" was EN packing; domain `{1,17}` holds over all 23 | High (domain + inline⟹17 invariant) / Resolved (meaning, EXP-0017) | ● active | [EXP-0014](../experiments/EXP-0014-res-opaque/), [EXP-0017](../experiments/EXP-0017-res-open/) |
| RES-NODE-014 | Tail-registry node `@0x00` = `0` for **every** node of **every** reachable tail-registry `&YA1` (4592 nodes, 11 archives); the per-node hash/id/checksum candidate is **refuted** (a hash would vary; the word is exactly 0). Reserved / always-zero. The inline REG record `@0x00` is the separate `A` field (REG-FMT-017), not this word. Narrows RES-NODE-011. **Resolved by RES-NODE-019 [EXP-0017]:** it is the node record's reserved word (aligned with the header's magic), never dereferenced by any lookup/descent/sort instruction | High (always-zero) / Resolved (meaning, EXP-0017) | ● active | [EXP-0014](../experiments/EXP-0014-res-opaque/), [EXP-0017](../experiments/EXP-0017-res-open/) |
| RES-SCOPE-015 | The `&YA1` magic labels **two** structurally distinct formats: the tail-registry archive (`u32@0x10` = registry byte offset; EXP-0001) and the inline REG-style record store (`u32@0x10` = record count `R`; EXP-0006/0011). Across the 60 reachable blobs, **every nested `&YA1`** (inside an archive or a save) is the inline flavor; the tail-registry flavor occurs **only** as a standalone top-level `.res`/`.LM` | High (that there are **two** flavours: `rom.exe` has two distinct validators for the same magic — tail open `FUN_004c90a0` and inline read `FUN_004cae80`, `RES-CODE-020` — so the distinction is in the code, not only in the bytes) / **Medium** (the *distribution* "every nested is inline, tail-registry only at top level" — corpus-only over this install's 60 blobs; nothing read in the binary forbids a nested tail-registry archive, so this is a fact about the shipped data and must not be relied on as a structural rule) | ● active | [EXP-0014](../experiments/EXP-0014-res-opaque/) |
| RES-NODE-016 | **(rom.exe)** The 24-byte `&YA1` header is a **directory-node record**: header and node share their first 16 bytes `[+0 reserved][+4 off][+8 size][+0xc type/flags]`; the header is the tree's virtual root node (its `off`=`@0x04`, `size`=`@0x08` rootCount, `type`=`@0x0C`), and `@0x10`/`@0x14` (regOffset/nodeCount) occupy the slot where a real node keeps its 16-byte name. Derived from the tail open `FUN_004c90a0` (header words → object node-slots; base filename copied into `this+0x10`) and the serializer `FUN_004cafe0 @0x004cafe7` passing the object as **both** `this` and the root node to the tree-sort | High (the identification rests on one instruction — the serializer handing the same pointer as object and as root node — which is what turns "the header looks like a node" into "the header is the node") | ● active | [EXP-0017](../experiments/EXP-0017-res-open/) |
| RES-HDR-017 | **(rom.exe)** Header `@0x04` = the **root directory node's `off` = the array index of the first top-level node**. The name lookup dereferences it: `FUN_004ce8e0 @0x004ce8fb` `MOV ESI,[node+4]; SHL 5; ADD [this+0x28]` → `&firstChild`. So `@0x04` is **read and used**, not recomputed, not a checksum; EXP-0014's `{0, nNodes−roots}` law = the packer's roots-first (`0`) vs roots-last (`ownedNodes`, e.g. graphics/main/sfx) serialization order. Resolves the `rom.exe`/Unknown flag on RES-HDR-012 (value law intact) | High (the dereference is quoted — `MOV ESI,[node+4]; SHL 5; ADD` — so the field is read and used; the corpus law it replaces had two candidate explanations and no way to choose) | ● active | [EXP-0017](../experiments/EXP-0017-res-open/) |
| RES-HDR-018 | **(rom.exe)** Header `@0x0C` = the **root directory node's `type/flags`**: `1` = directory; **bit 4 (`0x10`) = children-sorted flag**. The reader **branches** on it — `FUN_004ce8e0 @0x004ce909 TEST AL,0x10; JZ` → binary search over sorted children (`bsearch`, cmp `0x4ceaf0`) when set, else a linear `strncmp` scan — so `{1,17} = {dir, dir+sorted}`. The writer sets it in the finalize-sort `FUN_004ce660 @0x004ce6c3 OR AL,0x10; MOV [node+0xc]`. Bit 4 is EXP-0014's discriminator; (bit 31, tested at `FUN_004c90a0 @0x004c92a8` to gate a seek, is clear in all shipped archives). Resolves the meaning flag on RES-HDR-013 (domain + inline⟹17 intact) | High (the reader **branches** on the bit and the writer **sets** it, both quoted — read and write sides, which is the strongest form available for a flag) | ● active | [EXP-0017](../experiments/EXP-0017-res-open/) |
| RES-NODE-019 | **(rom.exe)** Node `@0x00` = the node record's **reserved word** (aligned with the header's magic slot). **No** name-lookup, path-descent, or finalize-sort instruction dereferences `[node+0]` — scanned `FUN_004ce8e0` (reads `+4/+8/+0xc/+0x10`), `FUN_004ce800`/`FUN_004ce9e0` (`name+0x10`, `type+0xc`), `FUN_004ce660` (`+4/+8/+0xc`). Reserved/ignored, confirmed at instruction level (not merely "0 in the corpus"). Resolves the meaning flag on RES-NODE-014 | High (an **absence**, established by enumerating every dereference in the four functions that touch a node — the row says so itself, and the distinction it draws is exactly the one the confidence scale turns on) | ● active | [EXP-0017](../experiments/EXP-0017-res-open/) |
| RES-CODE-020 | **(rom.exe)** Address map of the `&YA1` reader/lookup/writer in `rom.exe` (sha256 `942e9b72…7d367d03`): tail open+validate `FUN_004c90a0` (magic `CMP` `@0x004c91f8`), inline read+validate `FUN_004cae80` (`@0x004caeaf`), shared name lookup `FUN_004ce8e0`, path walk `FUN_004ce800`/`FUN_004ce9e0`, file-resolve `FUN_004c9320`, finalize-sort `FUN_004ce660` (writer-only; sole caller `FUN_004cafe0`), serializer `FUN_004cafe0`, empty ctor `FUN_004cb0a0`, INI→REG compiler `FUN_004cb340`, record comparator `0x4ceaf0`. **Extended by EXP-0032** with the value-accessor family and the node-insert path: `FUN_004cc670` GetString, `FUN_004ccc20` GetInt, `FUN_004ccda0` GetDouble, `FUN_004cd240`/`004cd130` int-array getters, `FUN_004ccad0`/`004cccb0`/`004cd910`/`004cda60`/`004cdbb0`/`004cdd10`/`004ce020` setters, `FUN_004cd370` create-subkey, `FUN_004ce6d0` **node insert** (fixes `value`/`size` as block-start/count and the 15-char name clamp), `FUN_004cb160` Open. The magic-immediate `0x31415926` occurs in exactly these four functions (two `CMP` readers, two `MOV` writers). **Extended by EXP-0034** with the path-resolve wrapper `FUN_004ce8c0`, the linear branch's comparator `FUN_00557030` (`_strnicmp`) and the sorted branch's search driver `FUN_00557140` (`bsearch` — **not a comparator; [EXP-0040] read it, the comparison arrives as a function-pointer argument**), and `FUN_004c9b10` (the sole caller of `FUN_004c9320`). **Extended by [EXP-0051](../experiments/EXP-0051-res-ru/)** with the tail open's stream class: ctor+open `FUN_00573b09` (vtable `0x0059d4a4`), read `FUN_00573dba` (raw `ReadFile`, vt+0x3c), seek `FUN_00573e3f` (raw `SetFilePointer`, vt+0x30), throw helpers `FUN_00579973`/`FUN_0057988e`, allocator `FUN_00554390 → FUN_005543b0` (CRT malloc-retry), and the length family `FUN_00579c7d/cff/d81 → FUN_00579db0` = the image's **sole** `GetFileSize` call site. **Extended by [EXP-0052](../experiments/EXP-0052-res-resolution/)** with the archive **manager** and its search: singleton `0x005f2140` (archive `CObArray` `+0x00`/vt `0x59bb00`, directory `CStringArray` `+0x14`/vt `0x59bae8`, loose-file `CObArray` `+0x28`/vt `0x59bad0`), ctor `FUN_004c93b0` + static-init/destructor thunks `FUN_004c9380`/`ORPHAN[004c9370..004c937a]`/`ORPHAN[004c93a0..004c93aa]`, free registrars `FUN_004c9910` AddArchive / `FUN_004c9940` AddDirectory / `FUN_004c9a20` update.lst-loader, methods `FUN_004c95e0` / `FUN_004c9970` / `FUN_004c9ad0` (node mask), the path fold `FUN_004c9580` + CRT `tolower` `FUN_005562d0` + gate `0x005c1d00`, the resolver `FUN_004c9b10`, the public open `FUN_004c9f10` (manager vt slot `0x0059bb40`), the directory-existence check `FUN_004ca300`, and the startup `FUN_004709e0` with its catch funclets `FUN_00470e0d`/`FUN_00470f35`/`FUN_004710c7`; the on-demand `World.res` site `FUN_004db20d`. Archive-name string pool: `0x005be34c..0x005be434`, plus `World.res` `0x005c6934` and the default extension `".res"` `0x005c1d04` | High (an address map of a **named binary** — pinned to its sha256, so it is checkable rather than believable; the "exactly four functions" claim is a whole-image immediate scan, not a sample) | ● active (amended) | [EXP-0017](../experiments/EXP-0017-res-open/), **[EXP-0034](../experiments/EXP-0034-res-name-codec/)** |
| RES-TEXT-021 | **(rom.exe)** `rom.exe` applies **no** byte-to-character conversion when reading `.res` archive entry names. The tail-open routine `FUN_004c90a0` reads the entire node table — every 16-byte name field included — via **one** verbatim bulk stream `Read()` call, with no per-name loop or transform between the allocation and the read; no code-page API (`MultiByteToWideChar`/`WideCharToMultiByte`/`CharToOemA`/`OemToCharA`/`GetACP`/`GetOEMCP`/`LCMapStringA`) has any caller in `0x004c9000-0x004cf000` (whole-image reachability sweep). The one name-shaped operation in the function — deriving the archive's own base filename from its OS open-path — is also a raw byte copy with no fold. **This is the same conclusion `REG-TEXT-036` reached for `.reg`, independently re-derived here from `.res`'s own distinct reader, not assumed to carry over.** CP866 (our shipped decoding) is therefore a display convention of ours, unbacked by the binary, exactly as CP866/CP1251 are for `.reg` text | High. **Re-checked by [EXP-0040]** on a repaired function table (the vtable blind spot closed, 607 functions created): **0** code-page references inside `0x004c9000–0x004cf000` and, in fact, **0 anywhere below `0x550000`** — not one reference from the game's own code at all, which is stronger than this row claims; 0 in orphan code — **confirmed**. Precision fix: the caller range is `0x0055xxxx–0x0057xxxx`. Original rationale: an absence over a whole-image reachability sweep, plus the single bulk `Read` that leaves no room for a per-name transform — and deliberately **re-derived** from `.res`'s own reader rather than inherited from `REG-TEXT-036`, which is the discipline this ledger should be judged by) | ● active | [EXP-0034](../experiments/EXP-0034-res-name-codec/), **[EXP-0040](../experiments/EXP-0040-enumeration-audit/)** |
| RES-TEXT-022 | Corpus fact, with its parameters: sweeping all 12 standalone `&YA1` containers (`tools/resnametext`), taking each node's name as the bytes at record `+0x10` truncated at the first `0x00`/`0xCD` (`RES-NODE-007`'s own convention) — **0 of 45 865** examined name bytes are `>=0x80` across all 4 592 corpus nodes; 55 distinct values, range `0x27..0x7A`. Reproduces `EXP-0001`'s `nameBad=0` invariant as an explicit histogram. The corpus cannot discriminate any candidate code page that agrees on ASCII — same shape of limitation `REG-TEXT-037` recorded for `.reg` | High (for this install, this sweep) | ● active | [EXP-0034](../experiments/EXP-0034-res-name-codec/) |
| RES-LOOKUP-023 | **(rom.exe)** In-tree child-name lookup during `.res` path resolution reuses the exact shared function `.reg` uses (`FUN_004ce8e0`, `RES-HDR-017`/`REG-REC-032`) and, on its unsorted/linear branch, the exact CRT `_strnicmp` — freshly addressed as `FUN_00557030`, disassembled and confirmed byte-identical in shape to `REG-TEXT-036`'s description (ASCII `A`-`Z` fold only, `CMP AH,0x41`/`CMP AH,0x5a`/`ADD AH,0x20` on both operands, no locale active). Case-insensitive, 15-character bound. The sorted branch's `bsearch` (`FUN_00557140`, also freshly addressed — a generic bisection loop) is handed the same comparator address (`0x4ceaf0`) `.reg`'s sorted lookups already cite — the identical shared code, not an independent instance, though that specific comparator function was not itself re-disassembled this session (it sits in a byte range the existing Ghidra auto-analysis never marked as a function) | High (linear branch, freshly disassembled); Medium (sorted branch's comparator — address confirmed shared, instructions not independently re-read). **Amended by [EXP-0040]:** `FUN_00557140` now has a function and was read — it is the CRT's generic **`bsearch` driver**, not a comparator: the comparison function arrives as a pointer argument and is called indirectly at `00557181 CALL dword ptr [ESP+0x2c]`. So the Medium was right, for a better reason than this row gave — **reading this function could never have settled the sorted branch's semantics, which live in the callee**. The sorted branch's actual comparator is still unidentified. This row was the audit's **positive control**: it was flagged on shape and came back correct | ● active (amended) | [EXP-0034](../experiments/EXP-0034-res-name-codec/) |
| RES-IDENT-024 | **(rom.exe)** The **leading** path segment handed to a resolving object (`FUN_004ce800`) is matched against that object's **own** stored name (`this+0x10`) by a **direct, case-sensitive** byte compare (`CMP AL,CL`) — no fold, no code-page involvement — a distinct, narrower comparison from `RES-LOOKUP-023`'s in-tree fold. Corroborated by `FUN_004c9b10` (the sole caller of `FUN_004c9320`), whose retry loop tries the same path string against what its loop bound indicates are several resolving objects in turn, consistent with this check deciding which loaded archive a path belongs to before the remainder resolves through the case-insensitive in-tree lookup. **The role clause is now resolved, and the inference was right — [EXP-0052]:** the "several resolving objects" are the manager singleton's archive `CObArray`, `FUN_004c9b10` iterates it ascending and takes the first non-zero result, and this comparison is exactly what makes at most one archive answer — the leading segment must equal the archive's identity name or `FUN_004ce800` returns 0 (`RES-IDENT-034`, `RES-ORDER-033`). One correction to the *mechanism* the row assumed: the case-sensitivity is workable only because every lookup path is lower-cased in full beforehand (`RES-CASE-036`), which this row did not know | High (the comparison itself — `CMP AL,CL` against `this+0x10`, case-sensitive, read at instruction level) / the *role*: **Medium when published** (an inference from a retry loop's bound, and the row said so) → **High via `RES-IDENT-034` [EXP-0052]**, which read the loop and the check rather than inferring from the bound | ● active | [EXP-0034](../experiments/EXP-0034-res-name-codec/), **[EXP-0052](../experiments/EXP-0052-res-resolution/)** |
| RES-OPEN-026 | **(rom.exe)** The tail open's **complete acceptance predicate** (`FUN_004c90a0`, fresh listing): the stream open succeeds (`FUN_00573b09`, throw on fail), `u32@0 == 0x31415926` (`CMP @0x004c91f8` — the **only value check** in the routine), and `malloc(@0x14×32)` succeeds (`@0x004c929f`). Seek/read reject only on OS errors (`FUN_00573e3f` = raw `SetFilePointer`, past-EOF legal; `FUN_00573dba` = raw `ReadFile`); a **short registry read is silent** — the byte count is discarded (`@0x004c92cf`) and the `malloc`'d table is never zeroed (`FUN_005543b0`, CRT malloc-retry shape). **No instruction computes `EOF − @0x10`, tests 32-byte alignment, or validates `@0x14`** — `RES-HDR-003/004`'s mod-32 and `count = regLen/32` identities are corpus facts about packers, not engine checks. The seek is gated on `@0x0C` bit 31 clear (`TEST @0x004c92a8`; clear in all 23 shipped archives, both installs) | High (every clause an instruction reading with its anchor quoted; the falsification hunt for a geometry check came back empty three independent ways — see RES-OPEN-027) | ● active | [EXP-0051](../experiments/EXP-0051-res-ru/) |
| RES-PATH-025 | **(rom.exe)** Path-component splitting (`FUN_004ce800`/`FUN_004ce9e0`) treats `\` (`0x5c`) and `/` (`0x2f`) as interchangeable terminators, tested symmetrically at every position; neither function rewrites one into the other, so no separator-normalization pass exists (or is needed). A whole-image reachability sweep, extended beyond `EXP-0032`'s code-page needles specifically to check for a **lower-casing** routine, finds `CharLowerA`/`CharLowerBuffA`/`_strlwr`/`_strupr` absent from the binary entirely (no import symbol at all) — no separate case-normalization pass exists anywhere in `rom.exe`. **The lower-casing half is WRONG and is corrected by `RES-CASE-036` [EXP-0052]; see retracted.md.** A whole-path fold does exist — `FUN_004c9580`, applied to every name and every lookup path entering the archive manager, gated on `[0x005c1d00]` which is `1` in the image and written by no instruction — and it is *required*, because `RES-IDENT-024`'s archive-identity compare is case-sensitive and 95 shipped literals capitalise that segment. The four named routines really are absent from the import table; the fold is a hand-written loop over the CRT `tolower` (`FUN_005562d0`), which a search for those four names could never reach. **The separator half stands unchanged**: both separators are native, tested symmetrically, and neither function rewrites one into the other — so a consumer's `\`→`/` normalization remains unbacked, while its lower-casing turns out to match what the engine does | High (separator half — read at every test position) / **the lower-casing half carried High and was wrong**: the absence was established over the *import table*, which cannot see an inlined fold — the bound was real but did not cover the claim made from it (retracted.md) | ✖ partially retracted | [EXP-0034](../experiments/EXP-0034-res-name-codec/), **[EXP-0052](../experiments/EXP-0052-res-resolution/)** |
| RES-OPEN-027 | **(rom.exe)** The registry is located by `@0x10` **alone** (seek target) and sized by `@0x14×32` **alone** (alloc + the single bulk read); bytes in `[@0x10 + @0x14×32, EOF)` are **unreachable**: the walk (`FUN_004ce8e0`, re-read) bounds itself by `node.off`/`node.size` only — `[this+0x20]` (`nodeCount`) is never read there, so even `dir.off+dir.size ≤ nodeCount` is corpus-only — and payload resolve (`FUN_004c9320`, re-read) returns `{stream, node.off, node.size}` with no comparison against `@0x10` or EOF. The module **cannot observe EOF at all**: `GetFileSize` has exactly one call site in the image (`@0x00579e0a` in `FUN_00579db0`), reachable only via stream vmethods `+0x18/+0x1c/+0x20` (`FUN_00579c7d/cff/d81`, vtable-dispatch only), and 0 of the image's 628 `CALL [reg+0x18/0x1c/0x20]` dispatch sites lie in `0x4c9000..0x4cf000`. Instrument: `EnumRefs` `sym:`/`callto:`/`re:` on the repaired function table — sees orphan code (1 orphan hit, `@0x0044c902`, outside the module); blind spot: bytes never disassembled | High (enumeration with instrument and blind spot stated per AGENTS.md; the `sym:` import route is the immune instrument and agrees) | ● active | [EXP-0051](../experiments/EXP-0051-res-ru/) |
| RES-GEOM-028 | **(RU corpus)** RU `MAIN.RES` (sha256 `be919916…aa95c40`, `gameversions\ru`) is a **fully valid 504-node tail archive plus 23 stale trailing bytes**: header `(regOff 4906389, count 504)`, engine-style parse 0 violations (462 files + 42 dirs, 3 roots, reach 504/504, payloads tile `[24, regOff)` exactly), region `16151 = 504×32 + 23`. The residue `[4922517, EOF)` aligns as a node-record **byte-suffix duplicating record 499** (`onmap19.256`, `size 0x707`, `type 0`; full 16-byte name field ending exactly at EOF) on a +23-shifted grid — an in-place overwrite layer of an earlier registry write (EXP-0050's font-tail shape). Per RES-OPEN-026/027 **no engine read ever touches it**. Unique in both corpora: residue 0 on EN 12/12 and RU 10/11. Leading/short rival framings scored dead (502/504 bad types, reach 3/504; `16128 ≤ 16151`) | Medium (corpus measurement, exact; the overwrite-layer *interpretation* is Medium — the record-suffix fit discriminates against noise, but the external packer was not read; no tail writer exists in `rom.exe`) | ● active | [EXP-0051](../experiments/EXP-0051-res-ru/) |
| RES-HDR-029 | **(RU corpus — amends RES-HDR-012)** The RU corpus **refutes `@0x04 ∈ {0, nNodes−roots}` as a format law**: RU `SFX.RES` stores `186` with its 48 top-level nodes at indices 186..233 — 186 owned nodes before them, 88 after (verified by unowned-set computation, independent of `@0x04`). `@0x04` is exactly RES-HDR-017's "array index of the first top-level node", now confirmed on a **mid-table** value the EN corpus never exhibited; the `{0, n−roots}` dichotomy was EN-packer serialization order. RU: `MAIN.RES` 501 (=504−3, roots-last), `WORLD.RES` 3 (=4−1, roots-last vs EN roots-first), 8 others 0 | Medium (corpus) — and it **raises** the reach of RES-HDR-017's High: the dereference now has a discriminating witness | ● active | [EXP-0051](../experiments/EXP-0051-res-ru/) |
| RES-HDR-030 | **(RU corpus — amends RES-HDR-013)** `@0x0C = 1` on **all 11** RU archives, including `GRAPHICS.RES` and `MAIN.RES` (EN ships `17` on graphics+main) — the "17 on graphics/main" association was EN packing, not format. The flag is **honest** in both: RU `MAIN.RES` root children are genuinely unsorted (`text, graphics, id`); EN's are sorted (`graphics, id, text`) — corroborates RES-HDR-018 (a reader honouring bit 4 linear-scans the RU file correctly; a reader assuming graphics/main are always sorted would bsearch an unsorted array). Domain over all 23 archives remains `{1, 17}` | Medium (corpus) | ● active | [EXP-0051](../experiments/EXP-0051-res-ru/) |
| RES-SET-032 | **(rom.exe)** The **archive set the engine opens**, complete. The tail open `FUN_004c90a0` has **exactly one call site in the image** — inside `AddArchive` `FUN_004c95e0` — so the set is exactly the `AddArchive` (`FUN_004c9910`) call sites, of which there are **11 in 2 functions**. Ten are literal filenames pushed unconditionally by the startup routine `FUN_004709e0`, in this order: `graphics.res` `@00470e47`, `main.res` `@00470e54`, `patch.res` `@00470e61`, `music.res` `@00470e75`, `video4.res` `@00470f1e`, `video8.res` `@00470f2b`, `sfx.res` `@0047108f`, `movies.res` `@0047109c`, `scenario.res` `@004710a9`, `speech.res` `@004710bd`. The eleventh, `World.res` `@004db32a` (`FUN_004db20d`), is added **on demand at world load and only after a direct open of `Data.bin` has failed**, so the list grows after startup and that archive lands last. Each is opened as **`dir[0] + "\" + tolower(name)`** with `.res` appended only when the given name has no `.` (`@004c90ff`; all ten already do). **No directory scan and no config file contributes an archive**: the image's 8 `FindFirstFileA` sites include none in `0x4c9000..0x4cf000`, and `update.lst` (the sole config input) only masks (RES-MASK-035). A failed open throws and is swallowed by the startup's catch funclets `FUN_00470e0d`/`FUN_00470f35`/`FUN_004710c7`, which set the "no CD" flag `0x005eb5a4` and resume — so a missing archive is simply absent from the list. The `-4x`/`-8x` command-line test at `00470e84..00470f14` selects only the string copied to `0x005f0050`; **both** video archives register on either branch (the paths converge at `00470f14`) | High (enumeration with instrument and blind spot stated per AGENTS.md: `EnumRefs callto:`/`refto:` on the repaired function table, which sees orphan code — 0 orphan hits on the open/registrar, and the 2 orphan hits on the singleton are named in RES-DIR-037; blind only to bytes never disassembled. The bound rests on the tail open's **single** call site, which is what makes this a set rather than a sample) | ● active | [EXP-0052](../experiments/EXP-0052-res-resolution/) |
| RES-ORDER-033 | **(rom.exe)** **The resolution order.** `FUN_004c9b10` (sole caller `FUN_004c9f10`, the manager's virtual Open at vtable slot `0x0059bb40`, 32 call sites in 28 functions; `FUN_004c9320`'s sole caller) searches **two tiers of the singleton at `0x005f2140`**, both **ascending index, first hit wins**: (1) the archive `CObArray` at `+0x00` (`m_pData @+0x04`, `m_nSize @+0x08`) — `MOV ECX,[EAX+ESI*0x4]` `@004c9b5f`, `CALL FUN_004c9320` `@004c9b62`, `JNZ @004c9b69` leaves the loop on the **first** non-zero result and `JNZ @004c9b78` returns it, so **no later archive is ever consulted**; (2) on exhaustion, the directory `CStringArray` at `+0x14` (`m_pData @+0x18`, `m_nSize @+0x1c`) — `MOV EDI,[EAX+EBX*0x4]` `@004c9bd7`, builds `dir[j] + "\" + path` and opens it as a **plain file on disk**, `JNZ @004c9cdd` returning on the first success. Both tiers yield the same 12-byte record `{stream, offset, size}`: an archive entry gives `{archive stream, node.off, node.size}` (`@004c9353..004c9365`), a loose file `{file object, 0, GetLength()}` (`@004c9d86..004c9d92`). **What fixes the order is registration order, because the container appends**: `FUN_004c95e0` reads `m_nSize` into the index (`@004c9714`, `@004c9723`), grows by one, and writes `array[oldCount] = newArchive` `@004c98b5`. **There is no priority field and no sort** — the resolver dereferences only `m_pData` and `m_nSize`, and the only archive-object fields read anywhere in the chain are `+0x10` (identity), `+0x34` (stream) and the node words | High (each clause an instruction reading with its anchor quoted, from a raw listing rather than the decompiler; the live rivals — prepend, last-match-wins, a priority field, a sorted key, a second resolver — were each killed separately, and the "one archive at a time" framing dies on the loop itself) | ● active | [EXP-0052](../experiments/EXP-0052-res-resolution/) |
| RES-IDENT-034 | **(rom.exe — resolves `RES-IDENT-024`'s Medium *role* clause)** **The archive's own identity name is a mandatory leading path segment, and that is what makes the archive list a dispatch rather than a priority.** `FUN_004ce800` compares the lookup path's leading bytes against the resolving object's stored name (`this+0x10`) one byte at a time, **case-sensitively** (`CMP AL,CL @004ce81f`), and **returns 0 on any mismatch** (`JNZ @004ce821` → `XOR EAX,EAX`); it descends the tree only once the identity is consumed, or the path reaches a separator, or the identity is empty. The check is **skipped entirely when the path begins with `\` or `/`** (`@004ce80a`, `@004ce811`) — only then is each archive searched from its own root and only then can RES-ORDER-033's order decide an outcome. The identity is derived by the tail open `FUN_004c90a0` from the path it opened: basename (walk back to `\` or `/`, `@004c918a..@004c919c`), truncated at the **first** `.` (`@004c9187`), clamped to **15** characters with bit 28 (`0x10000000`) OR'd into the root node's type word when it was longer (`@004c91bf`). So `graphics.res` answers to `graphics\…` and to nothing else, and the shipped archives are **disjoint namespaces by construction**. Corroborated by the shipped call sites: every path-shaped literal in `.data` begins with one of these identities (335 `graphics\`, 95 `SFX\`+`sfx\`, 44 `main\`, 21 `music\`, 10 `movies\`, 7 `World\`, 7 `scenario\`+`Scenario\`, 4 `video4\`, 4 `speech\`, 1 `patch\`), including `main\graphics\chrgen\leftup.bmp` where `graphics` is a *subdirectory* of `main` | High (the comparison, its case-sensitivity, its skip conditions and the identity's derivation are each read at instruction level with anchors; this supplies exactly the dispatch rule `RES-IDENT-024` said "a reader that needs [it] must have read", and the literal census is independent corroboration rather than the basis) | ● active | [EXP-0052](../experiments/EXP-0052-res-resolution/) |
| RES-MASK-035 | **(rom.exe)** **`update.lst` is the engine's only config-driven input to the archive layer, and the only override mechanism — and it masks rather than adds.** `FUN_004c9a20` (sole call site `@004710e3`, argument `0x005be340 = "update.lst"`) opens the file with `fopen`, reads lines of at most `0xff` bytes, skips any line whose first character is not alphanumeric, trims `\n`/`\r`, and passes each to `FUN_004c9ad0`. That routine resolves the line against the archive array **ascending, first hit winning** (`@004c9ae6`, `@004c9af0`) and sets **bit 29** on the found node's in-memory type word: `OR dword ptr [EAX+0xc],0x20000000 @004c9b00`. `FUN_004c9320` then returns the scalar **`1`** for that node instead of a record (`TEST [ESI+0xc],0x20000000 @004c9339` → `MOV EAX,1 @004c9342`), and RES-ORDER-033's phase 1 treats `1` as "stop searching archives" — `CMP EAX,0x1 @004c9b75` falls through to the directory tier. **The net effect is that a masked entry is served from a loose file on disk instead of from its archive.** Neither shipped install contains an `update.lst`, so the mechanism ships unused. (`Data_updated.lst`, referenced by `FUN_004dc65d`, is *not* routed here — `FUN_004c9a20` has one call site) | High (every clause an instruction reading with its anchor; the bit is written in one place and read in one place, and both are quoted — the read/write pair that is the strongest available form for a flag) | ● active | [EXP-0052](../experiments/EXP-0052-res-resolution/) |
| RES-CASE-036 | **(rom.exe — corrects a clause of `RES-PATH-025`; see retracted.md)** **A whole-path lower-casing pass does exist and is required.** Every path entering the archive manager — the name in `AddArchive` (`@004c9920`, again at `@004c9615`), the directory in `AddDirectory` (`@004c9950`, `@004c999c`), and the lookup path in both the public open `FUN_004c9f10` (`@004c9f42`) and the resolver `FUN_004c9b10` (`@004c9b41`) — passes `FUN_004c9580`, which, when `[0x005c1d00] != 0`, copies the string byte by byte through the CRT `tolower` `FUN_005562d0` (`CMP 0x41`/`CMP 0x5a`/`ADD 0x20` on its no-locale path). **`[0x005c1d00]` is `01 00 00 00` in the file image and has exactly one reference in the whole image — the read itself** (`EnumRefs refto:5c1d00`), so nothing can turn the fold off and it is unconditionally on. It is **load-bearing, not cosmetic**: 95 of the shipped lookup literals capitalise the identity segment (`SFX\ChrGen\Skill\MAstral.wav`, `Scenario\…`, `World\…`) while `RES-IDENT-034`'s identity compare is **case-sensitive**, so without the fold each of those 95 would fail to find its archive. `RES-PATH-025`'s evidence was sound but its scope was not: `CharLowerA`/`CharLowerBuffA`/`_strlwr`/`_strupr` really are absent from the import table — the fold is a hand-written loop over the CRT `tolower`, which no search for those four names could reach. The separator half of `RES-PATH-025` is untouched and stands: no `\`↔`/` rewriting exists anywhere | High (the gate's value read from the image, its reference count enumerated with instrument stated, the transform read at instruction level, and the 95-literal census showing the fold is required rather than merely present) | ● active | [EXP-0052](../experiments/EXP-0052-res-resolution/) |
| RES-DIR-037 | **(rom.exe)** **The directory tier, and why `dir[0]` decides which archives exist.** The manager singleton `0x005f2140` is constructed before `main` by `FUN_004c93b0` — reached from the static-init thunk pair `ORPHAN[004c9370..004c937a]` (`CALL 004c9380` = `MOV ECX,0x5f2140; JMP 004c93b0`, then `JMP 004c9390` = `PUSH 0x4c93a0; CALL atexit`), with the registered destructor thunk `ORPHAN[004c93a0..004c93aa]` — and it initialises three arrays (`+0x00` archives, vtable `0x59bb00`; `+0x14` directories, `0x59bae8`; `+0x28` open loose files, `0x59bad0`) then calls `GetCurrentDirectoryA` and `AddDirectory(cwd)` (`@004c9425`, `@004c9432`). So **`dir[0]` is the process working directory captured before `main`**. Startup adds, in order: `INSTALLDIR` from `HKLM\SOFTWARE\1C\Allods` (`@00470c50`, conditional on `RegQueryValueExA`, and `SetCurrentDirectoryA`'d first `@00470c43`); `GetTempPathA` with its trailing separator stripped (`@00470cef`, unconditional); and the `CD` registry value + `Allods` (`@00470dfc`, conditional). Two further `AddDirectory` sites exist outside startup (`FUN_004dc65d @004dc6d7`, `FUN_0053a200 @0053a30e`). **`AddArchive` uses `dir[0]` alone** — `XOR EAX,EAX` then an inlined `GetAt` bounds assert `@004c9622` and `MOV EDI,[ECX+EAX*0x4]` `@004c963b`, with no loop — which is why an archive not sitting beside the CWD never opens at all, whatever other directories are registered. (At `@00470d83..@00470db0` the CD volume label is compared against `"ALLODS"` and the result is followed by an unconditional `JMP`, i.e. not branched on) | High for the code (each site and the `dir[0]` indexing read at instruction level; both orphan hits `EnumRefs refto:5f2140` returned are named here rather than dropped) / **Medium** for which directories a given launch actually holds — the CWD and the two registry values are properties of the environment, not of the image, and were not observed at runtime | ● active | [EXP-0052](../experiments/EXP-0052-res-resolution/) |
| RES-COLL-038 | **(EN+RU corpora)** **The collision negative, with its parameters.** Replaying the RES-SET-032 registration walk over each install root (`tools/resorder`, engine-style parse per RES-ACCEPT-031) gives **8 live archives in both releases**, at indices `0 graphics · 1 main · 2 patch · 3 sfx · 4 movies · 5 scenario · 6 speech · 7 world`. Over those: **0 identity collisions**, **0 cross-archive path collisions** (EN 3980 distinct internal paths, RU 3976), and **0 within-archive case-insensitive duplicate paths** — so no entry name in either release is reachable through two archives, and none is ambiguous inside one. **Three of the ten registered names are never live**: `music.res`, `video4.res`, `video8.res` ship under `Allods\` only, and RES-DIR-037's `dir[0]`-only rule means their opens throw and are swallowed. `patch.res` is **not** an override archive — it holds exactly one entry, `patch.txt` (EN 1418 B, RU 1480 B), addressed as `patch\patch.txt`, and is registered *after* `graphics` and `main` in any case. RU containers are upper-cased on disk (`MAIN.RES`) while the engine opens the lower-cased name; Win32 resolves that case-insensitively. Corpus geometry reproduces EXP-0051 exactly (RU `MAIN.RES` residue 23, RU `SFX.RES` `@0x04` = 186, `@0x0C` = 17 on EN graphics/main and 1 on all RU) | **Medium — and capped there deliberately.** This is a census over 23 containers, and a census can show that no collision *occurs*, never that one is *impossible*; what forbids collisions is `RES-IDENT-034`, which is a separate row and High. The "3 of 10 never live" clause is a property of the shipped layout **plus a normal launch**, not of the image — a launch whose CWD were `Allods\` would open those three and none of the others | ● active | [EXP-0052](../experiments/EXP-0052-res-resolution/) |
| RES-ACCEPT-031 | **The acceptance contract for a consumer** — accept exactly what the engine accepts. **Assert (engine-checked, High):** `u32@0 == "&YA1"`; locate the registry at `@0x10`, take **exactly `@0x14`** 32-byte records, ignore `[@0x10 + @0x14×32, EOF)`; do **not** require `(EOF−@0x10) % 32 == 0`; do **not** derive the count from region length (that is what rejected RU `MAIN.RES`). **Corpus (EN+RU, 23 archives, Medium cap):** `@0x10 + @0x14×32 ≤ EOF` (residue 0 on 22/23, 23 on RU `MAIN.RES`); `24 ≤ @0x10 ≤ EOF`; types ∈ {0,1}; dir ranges in-table; payloads tile `[24, @0x10)` exactly; `@0x0C ∈ {1,17}` with bit 4 honest. A reader may assert the corpus clauses as hardening, knowing it is then **stricter than the engine** (which tolerates even a silently-truncated registry read and unchecked dir bounds). **Unknown:** engine runtime behaviour outside the corpus envelope (garbage node words, `@0x0C` bit 31 set, short reads) — no defensive path exists; what it *checks* is pinned, what it would *do* is not | High (assert clauses — instruction-level per RES-OPEN-026/027) / Medium (corpus clauses) / Unknown (out-of-envelope behaviour) | ● active | [EXP-0051](../experiments/EXP-0051-res-ru/) |

> **Relocated placeholder, not a res.md finding.** `claims/registry.md` carried the
> paragraph and table below as generic contributor scaffolding — "copy this row into
> a format section when its first claim lands" — for any not-yet-populated area, not
> a statement about the RES/LM container. The knowledge repository's `k1` edition
> dropped it from the public index as internal review narrative under that page's own
> Publication-boundary rule, and it is moved here unedited, byte for byte, because the
> id it names, `RES-HDR-001`, belongs to this ledger's own `RES-HDR-*` series (the
> series otherwise runs `002`–`030`) and was never promoted into a published row here.
> Nothing about the id's confidence, status or evidence is touched or resolved by this
> move; the id remains exactly as unpromoted as it was.

Copy this table header into a format section when its first claim lands:

| ID | Claim | Confidence | Status | Evidence |
|----|-------|-----------|--------|----------|
| RES-HDR-001 | _statement_ | High | ● active | [EXP-0001](../experiments/EXP-0001-res-header/) |
