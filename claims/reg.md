# Claim registry — REG — class registries

Level 2 ledger. Index: [registry.md](registry.md) · spec: [`formats/reg/format.md`](../formats/reg/format.md). Legend: `✔ promoted` (also in the spec) · `● active` · `✖ retracted`. IDs are permanent.

> **Confidence re-graded by the standing sweep (2026-07-25).** This ledger came through the
> sweep in the best shape of any — EXP-0032/0033 had already re-derived it from the loader and
> withdrawn what the framing correction invalidated. One residue was found: `REG-ROSTER-019`'s
> name→sprite-path pairing was **not** among the figures regenerated. No new bytes were read.
>
> **Regenerated by EXP-0043 (2026-07-28), and it was worse than “stale”:** the published arrows
> are wrong on **34/34** classes and 14 of them index outside the `Files` table altogether
> (`REG-ROSTER-052`). The same experiment gave the 38 registries that had **no** per-key value
> table one — `REG-CUT-053`, `REG-SFX-057`, `REG-NPC-058`, `REG-SCN-059`, `REG-AI-060` — and
> measured two format-shape facts a reader must apply: the sorted flag sits on the **root
> alone** (`REG-KEY-054`) and a **`kind` belongs to the record, not the key name**
> (`REG-KIND-056`). `data/map.reg` is the one registry still without a table *here* — it has
> one, in the ALM ledger, which is how it escaped EXP-0033's frame in the first place.
> *(Corrected 2026-08-01: this line read "its `Cost`/`Pass` figures remain withdrawn under
> `ALM-TERR-015`", which stopped being true at EXP-0041 — the re-walk on the `0x18` framing
> **restored** them, byte for byte, and published the whole file as `ALM-TERR-043`. A
> withdrawal that outlives its restoration reads exactly like a live one.)*
>
> Useful fact this sweep pinned down, because it bounds how far the EXP-0032 shift reaches: a
> record's **name** sits at file `0x28 + 32i` under *both* the old and the corrected framing
> (old `[A][kind][name:20][C]` from `0x20`, corrected `[name:16] @ 0x18+32i+0x10`). So **key
> catalogues and name lists are safe everywhere in the repo; only value attributions moved** —
> the old `C @ 0x3C + 32i` is the *next* record's `value @ 0x1C + 32(i+1)`.

| ID | Claim | Confidence | Status | Evidence |
|----|-------|-----------|--------|----------|
| REG-LOC-016 | *(scope widened by EXP-0032 — see REG-LOC-038: these five are 5 of 44)* The unit/object/structure/projectile/material **class definitions** live in five `.reg` file nodes inside `graphics.res` (`units/units.reg`, `objects/objects.reg`, `structures/structures.reg`, `projectiles/projectiles.reg`, `units/material.reg`); the RT-03 key names (`AttackPhases`, `MovePhases`, `DyingPhases`, `Flip`, `Projectile`, …) are **literal ASCII** there — the labels are the game's own | High (a direct byte observation, not a fit: the names are literally present in the record array, at a file offset the EXP-0032 correction did not move) / **Medium** (the *scope* — "five" was wrong by a factor of nine, `REG-LOC-038`; it was the set then examined, which is the same error shape as the count in this row's own successor) | ● active (amended) | [EXP-0006](../experiments/EXP-0006-registry-keys/) |
| REG-FMT-017 | A `.reg` node is a nested `&YA1` holding a flat array of 32-byte records + a trailing pool. **AMENDED by EXP-0032 — the record framing published here was 4 bytes early.** The correct record is `[value:u32 @+0x04][size:u32 @+0x08][kind:u32 @+0x0C][name:char16 @+0x10]` (see REG-REC-032); the old reading `[A][kind][name:20][C]` paired each key's name with the **next** key's value. Kind semantics as amended: 1=subkey (`value`=child block **start**, `size`=count), 2=int32 (`value`), 0=string / 6=int32[] (`value`=pool offset, `size`=byte length), **4=double (REG-DBL-035)**, 10=double[] | High, as amended (every field and every kind carried by `REG-REC-032`/`REG-KIND-034`, i.e. by an explicit type test *and* an explicit writer in `rom.exe` — the corpus closure that preceded them held under the wrong framing too, which is exactly why this row needed amending) | ● active (amended) | [EXP-0006](../experiments/EXP-0006-registry-keys/), [EXP-0011](../experiments/EXP-0011-reg-values/), **[EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/)** |
| REG-UNITS-018 | *(**AMENDED by EXP-0032**: `UnitCount` reads **34** on the corrected framing, matching the 34 `Unit*` sections exactly — the published 33 was the off-by-one; `[Global] FileCount` reads **33**, matching the 33 `Files` children, where the old framing read 0. `AttackPhases`=7 / `MovePhases`=8 on `Unit0` now match their `Anim*Time`/`Anim*Frame` array lengths exactly, where the old framing read 128 and 2 against those same 7- and 8-element arrays.)* `units.reg` = 34 classes with keys `ID File Index Palette Sound DescText InfoPicture Parent InMapEditor Move/MoveBegin/Attack/Dying/Bone/IdlePhases Flip Width Height CenterX/Y SelectionX1..Y2 Z TileSize Dying AttackDelay Projectile ShootDelay ShootOffset {Attack,Move,Idle}AnimTime/Frame`; ranged-only keys appear only on the ~9–11 ranged classes; kind-6 arrays are per-phase anim tracks (e.g. `AttackAnimFrame=[0..6]`). **[EXP-0039] turns this catalogue into a field map**: every key's offset in the loaded class record, its default, and the three timelines the loader *builds* out of the kind-6 tracks are in `REG-UNITS-049`; what each `*Phases` scalar counts is `SPR256-UNIT-024`. **⚠ The clause that a kind-6 track's length "agrees with the matching `*Phases` scalar" is WITHDRAWN by [EXP-0037].** Re-derived on this same base: `AttackPhases` agrees on **24/24** classes carrying both, but `MovePhases` on only **15/18** (`Goblin` 8 vs 10, `Ghost` 3 vs 4, `Goblin slinger` 8 vs 10) and `IdlePhases` on **4/5** (`Ghost` 3 vs 4); `MoveBeginPhases`/`DyingPhases`/`BonePhases` have **no `*AnimTime`/`*AnimFrame` key at all**, so there is no length for them to agree with. The other two registries are weaker still: `objects.reg` `Phases` vs `len(AnimationTime)` **7/16**, `structures.reg` `Phases` vs `len(AnimTime)` **12/14**. The loader corroborates that equality was never a law — `FUN_0046af50` iterates the **array's own** element count (`*local_294`), never `Phases`. EXP-0032's own two examples (`Unit0` `AttackPhases=7`/`MovePhases=8` matching 7- and 8-element arrays) happen to be two of the agreeing cases | High, as amended (the key catalogue is a name list, and names did not move under the EXP-0032 correction; the counts 34/33 were re-read on the corrected base and each matches an independent structural count) / **Medium** ("ranged-only keys appear only on the ~9–11 ranged classes" — an approximate figure with no exact count attached; unparameterised as written) | ● active (amended) | [EXP-0006](../experiments/EXP-0006-registry-keys/), **[EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/)** |
| REG-ROSTER-019 | The 34-unit roster read from the pool: `Human Swordsman`, `Goblin`, `Dragon`, `Ogre (Lord)`, `Turtle`, `Human Archer/CrossBowMan`, `Catapult 1/2`, `Hero of the Lance/Sword`, … — **re-derived verbatim on the corrected framing** (EXP-0032 `evidence/units-roster.txt`, 34 `DescText` + a 33-entry `Files` table). **⚠ The `DescText` name → sprite `File` path *pairing* asserted by this row is RETRACTED and REPLACED by `REG-ROSTER-052` (EXP-0043)**, which regenerated it on `Files[class.File]`: the published arrows are wrong on **34/34** classes, not merely stale, and **14 of them index outside the 33-entry `Files` table** — the pairing was falsifiable from the shipped bytes alone at any time since EXP-0006. The *names* stand; the *arrows* are superseded | High (the 34 names, re-derived) / **✖ the name→path pairing, replaced by REG-ROSTER-052** | ✖ partially retracted | [EXP-0006](../experiments/EXP-0006-registry-keys/), [EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/), **[EXP-0043](../experiments/EXP-0043-reg-restorations/)** |
| REG-OBJ-020 | *(**AMENDED by EXP-0033 — see REG-OBJ-039: the object count was never a framing casualty, it was `FileCount` transposed for `ObjectCount`.**)* `objects.reg` keys incl. `Index` (frame), `Phases`, `DeadObject`, `FireObject`, `IconID`, `AnimationTime/Frame`, `Width/Height`, `CenterX/Y`, `Parent` — the RT-02 object frame + destroyed/burning refs; key catalog unaffected by the EXP-0032 framing shift (record names sit at the same file offset under both framings) | High (the immunity argument in the claim was checked by this sweep and is correct: `0x28 + 32i` under both splits — which is what makes a *name* list safe where a *value* list is not) | ● active | [EXP-0006](../experiments/EXP-0006-registry-keys/), **[EXP-0033](../experiments/EXP-0033-reg-value-tables/)** |
| REG-OBJ-039 | **Re-derived on the EXP-0032 base, amending `REG-OBJ-020`'s count.** `objects.reg` is **82** objects, not 56: `[Global] ObjectCount = 82`, matching 82 distinct top-level `Object*` subkeys *and* (independent cross-check) 82 distinct `ID` values `0..81`. **[EXP-0037] adds the detail that matters to a consumer: for this registry `ID == section index` on 82/82**, so the two possible keys coincide here and nowhere else (`REG-KEY-044`). The published 56 is `[Global] FileCount` — the size of the sprite-file `Files[]` leaf table, not the object count; this was a transposed-counter slip present since EXP-0006/0011, not itself a framing casualty (EXP-0011's own raw observations already recorded "objects → 82 + Global" without the claim reflecting it). Newly measured ranges (43/82 present unless noted): `Width` 32..128 [4], `Height` 32..128 [5], `DeadObject` −1..67 [22] (35/82), `FireObject` −2..−1 [2] (14/82) | High (the count carries **two independent** structural cross-checks — 82 `Object*` subkeys and 82 distinct `ID`s — on a framing read from the loader; the ranges are measurements on that framing, each with its presence denominator attached) | ● active | [EXP-0033](../experiments/EXP-0033-reg-value-tables/) |
| REG-STR-021 | *(**AMENDED by EXP-0033 — see REG-STR-040: the count held; `FullHeight`/`TileWidth` did not.**)* `structures.reg` = 66 structures (count holds); keys incl. `AnimMask`, `Phases`, `Flat`, `Indestructible`, `Usable`, `VariableSize`, `LightPulse`, `LightRadius`, `ShadowY`, `SelectionX1..Y2` — the RT-05 structure fields. **Withdrawn:** `FullHeight (0..20)` and the bundled `TileWidth/TileHeight (1..6)` — see REG-STR-040 for the corrected ranges | High (keys, count); ranges withdrawn | ● active | [EXP-0006](../experiments/EXP-0006-registry-keys/), **[EXP-0033](../experiments/EXP-0033-reg-value-tables/)** |
| REG-STR-040 | **Re-derived on the EXP-0032 base, amending `REG-STR-021`'s ranges.** `structures.reg` = 66 structures — `[Global] Count = 66`, matching 66 `Structure*` blocks and the `ID` domain `1..66` (66 distinct, and `ID == section index + 1` on 66/66). **⚠ The parenthetical "unlike `units`/`objects`' 0-based `ID`" is CORRECTED by [EXP-0037]: only `objects.reg` is 0-based.** `units.reg`'s `ID` domain is `1..80` with 34 distinct — sparse and 1-based, equal to neither the section index (0/34) nor index+1 (0/34) — exactly as this ledger's own `REG-PROJ-041` row already stated — count **holds**. `FullHeight` = **1..6** [6 distinct], all 66 present — **not** `0..20`; the published `0..20` is `SelectionX1`'s own corrected range exactly (`0..20` [13]), i.e. the old `FullHeight` column was reading the *next* key's true value, per the EXP-0032 mechanism. `TileHeight` = **1..6** [6] (holds — the old bundled figure, now attached to the right key); `TileWidth` = **1..11** [6] (changed — wider than the bundled `1..6`, which was in fact `TileHeight`'s range) | High (the count cross-checked twice, and the `FullHeight` correction is *self-demonstrating*: the withdrawn `0..20` turns out to be the neighbour key's own corrected range exactly, which is the EXP-0032 mechanism reproducing itself) | ● active (partially retracted) | [EXP-0033](../experiments/EXP-0033-reg-value-tables/) |
| REG-PROJ-022 | *(**AMENDED by EXP-0033 — see REG-PROJ-041: the count held; `SFX`'s range is withdrawn.**)* `projectiles.reg` = 31 projectiles (count holds); keys incl. `Phases`, `RotationPhases`, `Homing`, `Flip`, `Palette`, `Width/Height`. **Withdrawn:** `SFX (12..62)` — see REG-PROJ-041 | High (keys, count); `SFX` range withdrawn | ● active | [EXP-0006](../experiments/EXP-0006-registry-keys/), **[EXP-0033](../experiments/EXP-0033-reg-value-tables/)** |
| REG-PROJ-041 | **Re-derived on the EXP-0032 base, amending `REG-PROJ-022`.** `projectiles.reg` = 31 projectiles — `[Global] Count = 31`, matching 31 `Projectile*` blocks and the `ID` domain `1..62` (31 distinct, non-contiguous — the same shared-namespace pattern seen in `units.reg`'s `ID 1..80`/34 and `structures.reg`'s `ID 1..66`/66). **`SFX`'s published `12..62` is withdrawn and not re-attributable**: on the corrected base `SFX` is present on 23/31 projectiles and reads exactly `1` in every one — a presence flag, not a variable id on this base; no single re-measured neighbour key reproduces `12..62` closely enough to name (tried: `Width` 12..128 — close in shape, not identity — so this is reported as superseded, not re-explained). `Width`/`Height` newly measured at `12..128` (23/31 present). `File` is confirmed a kind-0 **string** (sprite path, e.g. `archer\arrow`), 31/31 — unlike `units.reg`/`objects.reg`, projectiles store the path directly rather than a `Files[]` index | High (count, `SFX` withdrawal); Medium (the non-re-attribution — see EXP-0033 Confidence) | ● active | [EXP-0033](../experiments/EXP-0033-reg-value-tables/) |
| EDITOR-023 | The install's Map Editor ships its own catalogs: 22 trigger **condition** opcodes (`Description Checks.ini`), 44 trigger **action** opcodes (`Description Instants.ini`), and a 1088-slot placeable-template id→name table (`Templates.ini`); type system `Target_Unit/Group/Player/Item/Structure/Building`, `X`, `Y`, `int`, `Enum`, `Const` | High (an enumeration of shipped plain-text `.ini` files — the counts are what the files list, with no model interposed; corpus = this install's Map Editor) | ● active | [EXP-0006](../experiments/EXP-0006-registry-keys/) |
| REG-VAL-024 | Header + pool location. **AMENDED by EXP-0032:** the header is **`0x18` B**, not `0x20` — `rom.exe`'s loader performs exactly six 4-byte reads (`magic`, root `value`, root `size`, root `kind`=17, record count `R`, one more word) before bulk-reading `R×32` bytes of records. The pool is **length-prefixed by a `u32`**, not overlapped by the last record's `C`: `poolStart = 0x18 + R*32 + 4`. That is the same *number* the old rule gave (`0x20 + R*32 − 4`), which is why the error was invisible. `@0x1C` is **not** a "leaf-table first index" header field — it is record 0's `value`, and record 0 happens to be the `Files` subkey in `units.reg`. Structural closure `0x18 + R*32 + 4 + poolLen == payloadLen` holds **44/44** over the whole install | High, as amended | ● active | [EXP-0011](../experiments/EXP-0011-reg-values/), **[EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/)** |
| REG-VAL-025 | **AMENDED by EXP-0032 — the running cursor was a symptom of the 4-byte shift, not the mechanism.** Each kind-0/6/10 record stores its pool byte offset *directly* in its own `value` field (`+0x04`), and its byte length in `size` (`+0x08`); `rom.exe` reads `poolBase + node->value` with no cursor. The cursor reproduced the right addresses only because it walked the same items in the same order. Pool items still tile the pool exactly, now verified as a direct-offset tiling with no gap, overlap or tail on all 44 registries | High, as amended | ● active | [EXP-0011](../experiments/EXP-0011-reg-values/), **[EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/)** |
| REG-VAL-026 | **AMENDED by EXP-0032 — same fact, wrong record.** `record[i-1].C` under the old framing *is* `record[i].value` under the correct one; the 259/163/396/31/16 = 100% match stands and is now explained rather than observed. The consequence drawn from it is **withdrawn**: there is no "snapshot slot", and a kind-2 key before a pool item does **not** lose its scalar. Human Archer reads `Projectile=1`, `ShootDelay=21`, `AttackDelay=19` under the correct framing; the `~` flag should be removed from tools | High, as amended | ● active | [EXP-0011](../experiments/EXP-0011-reg-values/), **[EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/)** |
| REG-VAL-027 | A kind-2 scalar's value is a signed int32 — **AMENDED by EXP-0032: it is the record's own `value` field at `+0x04`, not the `C` at `+0x1C`.** Under the old framing every scalar was attributed to the key one position too early, so the *values* were real but the *key each belonged to* was wrong. `FireObject = −1` and the `Width`/`Height`/`Center*`/`Selection*` sanity check must be re-measured on the corrected base before being relied on | Medium (mechanism High; per-key attributions not re-measured) | ● active | [EXP-0011](../experiments/EXP-0011-reg-values/), **[EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/)** |
| REG-VAL-028 | Subkey tree. **AMENDED by EXP-0032:** a subkey's `value` (`+0x04`) is the child block **start** index and `size` (`+0x08`) is the child count — `rom.exe` computes `&records[parent->value]` and iterates `parent->size` entries. The old "`C` = block **end**, children `[C−A,C)`, tile by sorted ends" rule was the shifted reading of the same data and needed a tiling carve-out; the direct rule needs none. Under it the root block plus every subkey block cover the record array **exactly once**, 0 unreachable and 0 doubly-reached records, on all 44 registries. `A`-on-inheriting-classes no longer arises | High, as amended | ● active | [EXP-0011](../experiments/EXP-0011-reg-values/), **[EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/)** |
| REG-VAL-029 | Resolved `units.reg`: 34 classes; `DescText` reproduces the EXP-0006 roster (re-derived on the corrected base, verbatim); 36 root children = `Files` + `Global` + 34 classes; 33-entry `Files` table. **AMENDED by EXP-0032:** the sprite is indexed by the class's own **`File` scalar**, not by `ID` — Human Archer has `File=8`→`humans\archer\archer` and `ID=14`; the old "indexed by ID" reading was the 4-byte shift showing through (the shifted `ID` slot held the true `File` value). `Flip=1` on 10 classes still holds. `Projectile`/`ShootDelay`/`ShootOffset` are present on the ranged classes and now carry readable values | High (roster, counts, `File` index) / Medium (per-key ranged-class census not re-run) | ● active | [EXP-0011](../experiments/EXP-0011-reg-values/), **[EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/)** |
| REG-VAL-030 | **RETRACTED-IN-PART by EXP-0032; the withdrawn SPR256 comparison is RESOLVED by EXP-0033 (REG-VAL-043).** The figures were measured on the 4-byte-shifted framing, so each named key reported the *next* key's value. Re-measured: `units.reg` `Width`/`Height` = **128×128** on 17 classes (Dragon **160×160**), `CenterX` = 64 (Dragon 80), `CenterY` per-class 58…84. The published "`128×64`, Dragon `160×80`" is withdrawn for good — no downstream table should quote it. The SPR256 comparison built on those withdrawn numbers (`0/18 exact`, `only 4/18 fit`) was left an open question by EXP-0032; **EXP-0033 re-measured it with both the corrected canvas and the corrected `Files[class.File]` sprite index and it resolves cleanly: 18/18 within-canvas, 1/18 exact** — see `REG-VAL-043`. The placeholder-phase-count observation remains unverified on the new base (not this experiment's target) | High for `Width`/`Height`/`CenterX`/`CenterY` and the SPR256 comparison (via REG-VAL-043); placeholder-phase-count still unverified | ✖ partially retracted | [EXP-0011](../experiments/EXP-0011-reg-values/), **[EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/)**, **[EXP-0033](../experiments/EXP-0033-reg-value-tables/)** |
| REG-FMT-031 | **File framing, transcribed from `rom.exe`'s loader `FUN_004cae80` (not fitted):** `+0x00` magic `0x31415926`, `+0x04` root `value`, `+0x08` root `size`, `+0x0C` root `kind` (=17), `+0x10` record count `R`, `+0x14` one further word, `+0x18` the `R×32` record array read verbatim, then a `u32` pool byte length, then the pool read verbatim. Header = **`0x18` B**. Exact structural closure on **44/44** registries in the install, **0** invariant violations; the previously published `0x20` framing produces **82** violations over the same 44 | High (transcribed from `FUN_004cae80`'s read sequence — and, unusually, the corpus here *does* discriminate: 0 violations against 82 for the alternative, over the same files) | ● active | [EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/), **[EXP-0040](../experiments/EXP-0040-enumeration-audit/)** |
| REG-REC-032 | **Record layout — settles which word is a node's value.** In absolute file terms record `i` is `[0x18+32i, 0x18+32(i+1))` and **its value word is `0x1C + 32i`** — eight bytes *before* the kind word, **not** the `0x3C + 32i` word after the name, which is record `i+1`'s value; the same word serves an integer key and a string key's pool offset. Stride **32 B** (`SHL 0x5` in the loader's allocation, in the lookup and in the node-insert), `value` `u32 @+0x04`, `size` `u32 @+0x08`, `kind` `u32 @+0x0C`, `name` `char[16] @+0x10` NUL-padded. The lookup `FUN_004ce8e0` compares names over **15** characters (`_strnicmp(key, node+0x10, 0xf)`), so 15 significant characters, not 20. Attested four independent ways: loader `FUN_004cae80` (six 4-byte header reads, then `Read(records, R<<5)`); lookup `FUN_004ce8e0` (`records + node[+4]*32`, iterate `node[+8]`, compare `node[+0x10]`); accessors `FUN_004ccc20` / `FUN_004cc670` (`*(u32*)(node+4)`; `poolBase + *(int*)(node+4)` with length `*(u32*)(node+8)`); and the **node-insert writer** `FUN_004ce6d0`, which places a new child at `records[parent[+4] + parent[+8]]`, increments `parent[+8]`, writes the name at `node+0x10` **clamped to 15 characters** — so the field is 16 B and cannot be 20 — and stores `0` into `node+0x00`. `+0x00` is therefore a real field, not name padding: zero in **4621/4621** records, read by no accessor found — purpose Unknown. **This is the same node record our own RES ledger already published**: `RES-NODE-016` gives `[+0 reserved][+4 off][+8 size][+0xc type/flags]`, `RES-HDR-017` quotes the very instruction `FUN_004ce8e0 @0x004ce8fb MOV ESI,[node+4]; SHL 5; ADD [this+0x28]`, and `RES-NODE-019` records that `[node+0]` is never dereferenced. A `.reg` record is that node plus a 16-byte name — one class, one lookup. The two ledgers had been contradicting each other since EXP-0017; the fix was already in the repo, unpropagated | High (four independent `rom.exe` attestations — loader, lookup, two accessors, and the **writer**, each named to a function, with the 15-character clamp fixing the field size from the write side; the sweep found no fifth reading that fits) | ● active | [EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/) |
| REG-KIND-033 | The kind word is a **bitfield, not an enum**: value type = `kind & 0x0E` (tested as `AND EDX,0xe` in every accessor and as `(kind>>1)&7` in the string converter), **bit 0** = the node is a subkey, **bit 4** = the children are sorted so the lookup `bsearch`es (`TEST AL,0x10` at `004ce909`) instead of linear-scanning. The root node's `kind = 17 = 0x11` is therefore *subkey \| sorted*, not a magic constant — which is why every `.reg` in the install carries `17` at file `+0x0C`. The node-insert `FUN_004ce6d0` clears bit 4 with `kind & 0xffffffef` when it appends a child, and sets **bit 28 (`0x10000000`)** when the name it was handed exceeded 15 characters and had to be truncated — no shipped record carries bit 28 | High (each bit pinned to the instruction that tests or writes it; "bitfield not enum" is decidable only from code, and the corpus — where every root reads the same `17` — is exactly what would have kept it looking like a magic constant) | ● active | [EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/) |
| REG-KIND-034 | **Full kind enumeration + storage**, each attested by an explicit type test *and* an explicit writer in `rom.exe`: `0` string (`value`=pool byte offset, `size`=NUL-inclusive length), `1` subkey (`value`=child block start, `size`=child count), `2` int32 (`value`, signed), `4` double (see REG-DBL-035), `6` int32[] (`value`=pool offset, `size`=byte length, `size/4` elements), `10` double[] (`size/8` elements). The string converter has one further case (`(kind>>1)&7 == 4`, i.e. kind 8/9: skip 4 bytes of the pool item, copy `size−4`, terminate with two NULs) for which **no writer exists anywhere in the binary and no record exists in the corpus** — its semantics are **Unknown** and no guess is made. Corpus counts (all 44 registries): kind 0 ×873, 1 ×512, 2 ×2792, 4 ×122, 6 ×322; kinds 8 and 10 ×0 | High for 0/1/2/4/6/10 and the flags; **Unknown** for kind 8/9 | ● active | [EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/) |
| REG-DBL-035 | **Kind 4 is a little-endian IEEE-754 `double` stored in the record's own `value`+`size` fields (8 bytes at `+0x04`), with no pool access.** Reader: `FUN_004ccda0` tests `(kind & 0xe) == 4` then `FLD qword ptr [node+0x4]`. Writer: the text importer `FUN_004cb340` does `*(double*)(node+4) = atof(text); node->kind = 4`. Cross-check: the string converter passes `*(u32*)(node+4)` and `*(u32*)(node+8)` to `sprintf` as one double. The game's own error string is `"not an double associated with specified key"`. **122 instances exist in the install** — `startfade`/`endfade` under `Fading<n>` in **31 of the 33** video registries of `Allods/VIDEO4.RES`+`VIDEO8.RES` — every one exactly `0x0000000000000000` (0.0, ×61) or `0x3FF0000000000000` (1.0, ×61), forming a fade-in then a fade-out whose count matches each file's own `nFadings`. Every other candidate 8-byte window yields denormal garbage (`8.49e-314`, `9.02e-314`, `5.30e-315`) | High (reader and **writer** both named to an instruction, plus a corpus in which the alternative windows are excluded by producing denormals — a rare case of the bytes themselves ruling out the competing placements) | ● active | [EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/) |
| REG-TEXT-036 | **`rom.exe` performs NO byte-to-character conversion on `.reg` text.** The loader bulk-reads the record array and the pool verbatim (`Read(ptr, n)`, no conversion call in `FUN_004cae80`); the string accessor `FUN_004cc670` does `strncpy(dst, poolBase + node->value, min(node->size, cap))`. No code page is named anywhere on the path. Reachability sweep of the whole image: `MultiByteToWideChar`/`WideCharToMultiByte`/`LCMapStringA`/`GetACP`/`GetOEMCP` are called only from the statically-linked MFC/CRT range `0x0056xxxx–0x0057xxxx`, never from the registry class `0x004ca000–0x004cf000`; the `CharToOemA`/`OemToCharA` thunks have **zero callers**; `setlocale`/`_setmbcp`/`IsDBCSLeadByte` appear nowhere. The only byte transform on the path is the name lookup's `_strnicmp`, whose fast path folds **`A`–`Z` only** (`CMP AH,0x41`/`CMP AH,0x5a`/`ADD AH,0x20`) and leaves bytes `>= 0x80` untouched. **Consequence: a code page for `.reg` is a display convention of ours, not a transcription of the original.** CP866 and CP1251 are both defensible renderings and neither is the game's | High (this is an **absence**, and it is established the only way an absence can be: a whole-image reachability sweep of every conversion entry point, plus the one surviving byte transform read and bounded to `A`–`Z`). **Re-checked by [EXP-0040]** on a repaired function table (the vtable blind spot closed, 607 functions created): **0** references to any code-page entry point fall inside `0x004ca000–0x004cf000`, and **0** of the 107 references image-wide lie in orphan code — **confirmed**. One correction of precision, not of conclusion: the caller range is `0x0055xxxx–0x0057xxxx`, not `0x0056xxxx–0x0057xxxx` — four references sit below `0x0056xxxx`, the lowest at `0055971d` | ● active | [EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/) |
| REG-TEXT-037 | Corpus fact, with its parameters: sweeping **every** regular file under `ROM_DIR`, opening every file beginning `&YA1`, classifying nested payloads by root `kind == 17`, taking each record's name as the 16 bytes at `+0x10` truncated at the first NUL and each string value as its kind-0 pool item — **0 bytes `>= 0x80`** occur in 4 621 name fields or 873 string values. Observed byte range `0x20…0x7A` in both (59 / 65 distinct values). So the corpus cannot discriminate any two code pages that agree on ASCII, and no shipped `.reg` datum exercises one | High (for this install, this sweep) | ● active | [EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/) |
| REG-LOC-038 | The install holds **44** `.reg` registries in **12** `&YA1` containers, not the 5 in `graphics.res` that REG-LOC-016 located: + `scenario.res` (`globalmap`, `npc`, `scenario`), `sfx.res` (`sfx`), `world.res` (`data/ai`, `data/map`), and **33 video-cutscene registries** in `Allods/VIDEO4.RES` (18) and `Allods/VIDEO8.RES` (15) with `Common{startx,starty,nFadings,nPanaramings}` / `Fading<n>{startframe,endframe,startfade,endfade}` / `Panaraming<n>{startframe,endframe,stepx,stepy}` sections, consumed by `FUN_004aee90`. Every nested `&YA1` payload in the install is a registry — 0 were rejected by the classifier. **This is where kind 4 lives — 31 of the 33 carry it (`LOGOS/1c.reg` and `LOGOS/buka.reg` do not); the earlier "kind 4 is absent" statement was true of `graphics.res` and false of the install** | High (an exhaustive enumeration of the install — every regular file opened, every nested `&YA1` classified, 0 rejected — so the count is a listing, not a sample; and it is the standing example of why a scope figure needs its sweep stated: `REG-LOC-016`'s "five" was five *examined*) | ● active | [EXP-0032](../experiments/EXP-0032-reg-text-and-kinds/) |
| REG-MAT-042 | `material.reg` = 16 `Material0..Material15` blocks, each carrying exactly one `Path` string, no `[Global]` counter — re-verified unchanged on the EXP-0032 base (the pre-EXP-0032 observation of this registry's shape happened not to depend on which value word was read, since it never resolved a scalar) | High (structure only — 16 blocks and one `Path` string each, re-verified on the corrected base, with the immunity argument stated rather than assumed) | ● active | [EXP-0033](../experiments/EXP-0033-reg-value-tables/) |
| REG-VAL-043 | **Resolves the open question `REG-VAL-030` left (the SPR256 canvas comparison), re-measured on the corrected base with the corrected sprite index.** `units.reg`'s per-class canvas (`Width`/`Height` = 128×128, Dragon 160×160, both already re-measured by EXP-0032) **contains** the SPR256 sprite's own max frame bounds — sprite resolved via `Files[class.File]` (`REG-VAL-029`'s corrected index, not `Files[class.ID]`) — on **18 of 18** classes that carry `Width`/`Height`, exact on **1/18** (`Ogre (Lord)`: canvas 128×128 = sprite max frame 128×128). The pre-EXP-0032 finding ("0/18 exact, only 4/18 fit within it") was built from the wrong canvas numbers (measured one key early) **and** the wrong sprite (indexed by `ID` instead of `File`) simultaneously, and does not survive either correction alone | High (both inputs re-measured on the corrected base, and the row states the discriminating detail — the old result fails under *either* correction alone, so the agreement is not a coincidence of two errors cancelling) | ● active | [EXP-0033](../experiments/EXP-0033-reg-value-tables/) |
| REG-KEY-044 | **Amended 2026-07-27 (re-audit): two of the three arrays are keyed by `ID`, not by section index, and `Parent` is an `ID`.** Each loader — `FUN_0046ba80` (`units.reg` → `DAT_005eb674`), `FUN_0046af50` (`objects.reg` → `DAT_005ef8c4`), `FUN_0046e8a0` (`structures.reg` → `DAT_005eb62c`) — formats the section name `"<Prefix>%d"` from a **0-based counter** running to `[Global]<Count> − 1`, looks it up by name, allocates the class (**`0x12c`** — corrected from `0x11c` by [EXP-0039], `0046bbec PUSH 0x12c`, which is exactly `DescText`'s `0x10c + 0x20` — / 0x6c / 0xa4 bytes) and stores the `ID` key at class `+0x04` (structures: `+0x0c`) **and the counter itself** at class `+0x08` (units/objects). **The subscript it then stores the class at is per-registry, and for two of the three it is not the counter:** `units` — `0046c6cb MOV ESI,[EBP+0x04]` (the class's own `ID`), grow `m_nSize` to `ID+1`, `0046c826 MOV [ECX+ESI*0x4],EBP` ⇒ `DAT_005eb674[ID] = class`, MFC `CObArray::SetAtGrow`; `structures` — `0046ed05 MOV ESI,[EBP+0x0c]` (the `ID`), `0046ee5f` ⇒ `DAT_005eb62c[ID] = class`, the same; `objects` — `0046b427 MOV EDX,[m_nSize]` / `0046b42d MOV ESI,EDX`, `0046b57e` ⇒ `DAT_005ef8c4[m_nSize] = class`, `CObArray::Add`, i.e. append order, **which for this registry equals both the section index and the `ID`**. In all three the loop counter is a separate spill slot (`[ESP+0x1c]` / `[ESP+0x14]` / `[ESP+0x20]`) and is never the subscript. So `m_nSize` is `maxID+1`, not the section count: `units.reg` **81 slots for 34 classes — 47 NULL, index 0 among them**; `structures.reg` **67 for 66** (index 0 NULL); `objects.reg` **82 for 82**, no holes. A consumer iterating the units array must null-check. `Parent` is therefore an **`ID`**: `0046bc5f/65` and `0046b122/28` load `array[Parent]` as a plain scaled-index dword — no call, no scan — and the loader's next instruction reads `[parent+0x08]` to format the parent's *section name*, an indirection that is only necessary because `Parent` is not itself a section number. The structures loader never reads the key at all: `"Parent"` (`0x5bd924`) is pushed at exactly two addresses image-wide, `0046bc42` and `0046b109` — the code-side counterpart of `Parent` occurring on 0/66 structures. **Inheritance is eager and per key, at load, not lazy at an accessor** — and its *guard* differs between scalar and array keys, so an explicit `= ""` does **not** clear an inherited array (`REG-KEY-045`). The parent's already-resolved field is passed as the *default* argument of `FUN_004ccc20(section, key, default)`, so a key present in the child's own section wins and an absent one falls back (19 scalar sites `0046bc8c`…`0046bfd2`, `File` at `+0x0c` among them); the array-valued keys (`Move`/`Idle`/`AttackAnimTime`/`AnimFrame`, `ShootOffset`) instead **re-read the parent's own `.reg` section** by that formatted name when the child's array comes back empty. Every consumer located subscripts directly: `FUN_0040d027` (`DAT_005ef8c4[arg]` → `Files[class+0x0c]`), `FUN_00407b1a` (`DAT_005eb674[unit+0x20]`, `DAT_005ef8c4[byte]`), `FUN_0041ab8e` / `FUN_0041a2d5` (`DAT_005eb62c[obj+0x20]`), `FUN_0041e28d`, `FUN_0041f3d4` — so those subscripts are `ID`s for units/structures. **No search-by-`ID` over any of the three arrays exists anywhere in the image — and for units and structures none is needed, because the array *is* the by-`ID` index.** That absence is what EXP-0037 established and it still holds; what it inferred from it does not. Measured `ID` domains, EXP-0032 base, unchanged: `units.reg` **1..80, 34 distinct — `ID == index` on 0/34 and `index+1` on 0/34** (sparse, 1-based); `objects.reg` **0..81, `ID == index` on 82/82**; `structures.reg` **1..66, `ID == index + 1` on 66/66**. Consumer consequence, **reversed** from EXP-0037's: a `.alm` placement that stores an `ID` (`ALM-CLS-036` type-4 `kind`, `ALM-CLS-038` type-6 `+0x08`) is used **directly as the array subscript, with no translation**; the type-3 grid's `code − 1` (`ALM-CLS-035`) is an `objects.reg` `ID`, numerically unchanged there | High for the three append sites, the `Parent` subscript and the eager-inheritance mechanism (instruction level, `evidence/disasm-regloaders.txt`), and the units reading is discriminated **twice more, by our own corpus**: of the 16/34 units classes carrying `Parent` (values `{3,12,21,26}`) the `ID` reading resolves 16/16 to a class already appended and supplying exactly the geometry the child omits, while the section-index reading has 4/16 parents **not yet appended** when the child loads — sections 1, 2, 3, 19, where the subscript exceeds the array's `m_nSize` — and makes `Unit3` its own parent (`evidence/reg-units-parent-resolution.csv`) / High (the three `ID` domains — a measurement over the whole of each registry) / Medium that `unit+0x20` and `obj+0x20` hold an `ID`: it follows from the array keying, but the instruction that *writes* `+0x20` is still unlocated (EXP-0037's own open item). **`objects.reg` cannot witness this question at all** — its `ID` equals its section index on 82/82 *and* its loader is the one that genuinely appends in order, so both readings are indistinguishable there; that is the registry the retracted clause was read in, and then generalised from | ● active (amended) | [EXP-0037](../experiments/EXP-0037-alm-class-binding/) (amended 2026-07-27) |
| REG-KEY-045 | **`Parent` inheritance is guarded differently for scalar keys than for array keys, and an explicit `= ""` does NOT clear an inherited array.** Both mechanisms are eager, per key, at load (`REG-KEY-044`), but the test differs. **Scalars** — `File` and the 19 `Index`…`AttackDelay` sites — go through `FUN_004ccc20(section, key, default)`, which substitutes the `default` (= the parent's already-resolved field) **only when the record is not found**: `004ccc40 TEST EBX,EBX / JZ 004ccca7` → `MOV EAX,[ESP+0x14]`, versus `004ccc9f MOV EAX,[EBX+0x04]` when found. So a scalar written explicitly as `0` returns `0` and **overrides** the parent; a scalar present with a non-int kind *throws* (`004ccc4f`) rather than defaulting. **Arrays** — `Move`/`Idle`/`AttackAnimTime` and `*AnimFrame`, `ShootOffset` — go through `FUN_004cd240(section, key, dest)` and the loader then tests **`dest.m_nSize == 0`**, i.e. the array's *length after the read*, and nothing about presence: `0046c607 MOV EAX,[ESP+0x28]` where `ESP+0x20` is the destination `CArray` and `+0x08` is `m_nSize` (offsets fixed by `FUN_00570490` = `CArray::SetSize`, which at `005704b5/b8/bb` frees `m_pData` and zeroes `m_nMaxSize`/`m_nSize` when `nNewSize == 0`); nonzero ⇒ skip, zero ⇒ `0046c625` re-reads the **same key from the parent's section** into the same destination. And a **present, empty** record reaches length 0 by a *success* path: `004cd26a` computes `kind = (record+0x0c >> 1) & 7`, and `kind == 0 && size < 2` takes `004cd2b6 SetSize(0,-1); return 1` (a kind-6 record of size 0 lands on the same length via `004cd315 SetSize(size>>2)`). Pinned on the shipped corpus, not inferred across branches: `units.reg`'s `AttackAnimTime`/`AttackAnimFrame` occur on 33 records each, **32 of kind 6 and exactly one of kind 0**, and that one pair is `Unit33` "Unarmed Fighter with Shield" (`ID` 2, `Parent = 3`) at **kind 0, size 1** — the NUL of an empty string (records 758/759, our own `tools/regdump -mode records`). So `Unit33` **inherits `Unit0`'s `AttackAnimTime = [2 2 1 1 1 2 2]`** and its `= ""` changes nothing. **Consumer consequence: there is no representation of "clear this array" in the format.** A typed loader must treat an empty array key as *absent*, and must not treat an empty scalar the same way — the two are not symmetric. A non-empty string where an array is expected is a hard error (`004cd2c7`), not a clear | High (the guard, both accessors' absence/presence behaviour, and the `SetSize` field offsets are read off the instructions — `evidence/disasm-reginherit.txt` — and the one corpus case that could discriminate is pinned to a measured `kind`/`size` rather than argued over both branches. The reading was **not** settled from the corpus, which is consistent with either: `Unit33`'s output is not observable in our evidence) / **Unknown** (the absent-key residual: `004cd35e` returns without touching the destination, and the loader reuses only **two** `CArray` objects — `[ESP+0x20]` for the three `*AnimTime`, `[ESP+0x3c]` for the three `*AnimFrame` — draining them between pairs with `RemoveAt(0,1)`. So for an *absent* key the guard reads the previous pair's residue, normally 0 but not zeroed by the accessor. Whether any shipped class reaches a stale nonzero length is not established) | ● active | [EXP-0037](../experiments/EXP-0037-alm-class-binding/) (added 2026-07-27) |
| REG-OBJ-046 | **The `objects.reg` class record, field by field, from the loader's own read sequence — and the animation timeline it *builds* rather than stores.** `FUN_0046af50` allocates `0x6c` bytes per class and fills: `+0x04` `ID`, `+0x08` the loader's own 0-based counter (the `%d` of `Object%d`), `+0x0c` `File`, `+0x10` `Index`, `+0x14` `Phases`, `+0x18` `Width`, `+0x1c` `Height`, `+0x20` `CenterX`, `+0x24` `CenterY`, `+0x28..+0x38` an MFC `CArray<int>` (`vptr`, `m_pData @+0x2c`, `m_nSize @+0x30`, `m_nMaxSize`, `m_nGrowBy`), `+0x3c` a copy of `m_nSize` (`0046b421/0046b424`), `+0x40` `FireObject`, `+0x44` `DeadObject`, `+0x48` `InMapEditor` (default **0**, not −1), `+0x4c` `DescText` read as `0x1f` bytes. Every scalar's default is the parent class's already-resolved field when `Parent != -1` (`REG-KEY-045`), **except `File`, whose default is the literal `-1` unconditionally — `0046b144 PUSH -0x1` — so `File` does not inherit.** The array at `+0x2c` is not a registry key: the loader **run-length expands** the two kind-6 tracks into it, appending `AnimationFrame[i]` exactly `AnimationTime[i]` times (`0046b3c6..0046b41f`, `CArray::SetAtGrow(m_nSize, B[0])` at `0046b3f1`, unsigned `JBE`/`JC` so a non-positive time contributes nothing, both heads dropped per round, loop ends when either track empties), and stores the resulting length at `+0x3c`. That length is what the renderer takes the animation phase modulo (`TERR-SPR-042`). Corpus: 82 classes, timeline lengths **0 ×52, 24 ×2, 28 ×21, 105 ×7**; the array fallback of `REG-KEY-045` is load-bearing — without it **39** of the 82 classes lose their timeline and 7 lose `FireObject = -2`, since only 16 sections spell `AnimationTime`/`AnimationFrame` and 39 inherit them | High (each key→offset pair is a `PUSH`/`CALL`/`MOV` triple in a listing read end to end, and the expansion loop is reproduced instruction by instruction; `File`'s non-inheritance is one named immediate) / Medium (the timeline-length distribution — a measurement over one registry) | ● active | [EXP-0038](../experiments/EXP-0038-object-frame/) |
| REG-OBJ-047 | **`DeadObject` and `FireObject` are not the same kind of field: one is a class subscript, the other is a two-valued flag that is never dereferenced.** **`DeadObject` (`class+0x44`)** is used as an index into the very array the type-3 code indexes: the map renderer does `classes[class+0x44]` and takes *its* `File` (`0040a684..0040a693`, twice), and `FUN_0041f3d4` tests it to decide whether a cell holds a still-standing destructible object (`0041f766`). Value space over the 82 shipped classes after inheritance: **−1 on 54**, and on the other 28 a valid section index — 21 distinct targets, **0 out of range**, and **every one of the 21 has a `DescText` ending "(dead)"**. **`FireObject` (`class+0x40`)** has exactly one reader in the whole image — `FUN_0041e28d`, the ambient-sound loop — and there it is only ever compared: `0041e939 CMP [EAX+0x40],0x0 ; JL` then `0041ea7b CMP [EDX+0x40],-0x2 ; JNZ`. It is never a subscript, never an operand of arithmetic, never stored. The two comparison outcomes feed two separate counters whose **ratio** picks the sound slot: `0041ee13 CMP EDX,[EBP-0x6c]` against `rand()/(0x7fff/total)` chooses slots `0x3c..0x3e` (60–62) when the `>= 0` count wins and `0x46` (70) otherwise, played non-looping through `FUN_00453b08(vol, pan, 0, 0xdc, 0)`. Value space: **`{-2 ×21, -1 ×61}` — never `>= 0`**, so slot 70 is the only reachable one and the `>= 0` branch is dead on shipped data. The 21 classes carrying −2 are **exactly** the 21 that are some other class's `DeadObject`, both directions. Neither key occurs in any of the other 43 registries in the install (whole-install sweep, `evidence/reg-key-domains.csv`) | High (the asymmetry is decidable from the instructions alone — one field is an array subscript, the other appears only in `CMP`; both reader sets are whole-image enumerations, which is what an "is never dereferenced" claim needs) / High (both value spaces — an exhaustive read of the only registry that carries the keys) / **Medium** (that −2 *means* "burnt-out remains": the biconditional with `DeadObject` targets is exact and the consumer is a fire ambience, but no instruction or string names it) / **Unknown** (what `FireObject >= 0` would mean — no shipped class uses it) | ● active | [EXP-0038](../experiments/EXP-0038-object-frame/) |
| REG-UNITS-049 | **The `units.reg` class record, field by field, from the loader's own read sequence — and the three animation timelines it *builds* rather than stores.** `FUN_0046ba80` allocates **`0x12c`** bytes per class (`0046bbec PUSH 0x12c`; `0x10c + 0x20` for `DescText` is exactly that) and fills: `+0x04` `ID`, `+0x08` the loader's own 0-based counter (the `%d` of `Unit%d`), `+0x0c` `File`, `+0x10` `Index`, `+0x14` `MovePhases`, `+0x18` `MoveBeginPhases`, `+0x1c` `AttackPhases`, `+0x20` `DyingPhases`, `+0x24` `BonePhases`, `+0x28` `IdlePhases`, `+0x2c` `Width`, `+0x30` `Height`, `+0x34` `CenterX`, `+0x38` `CenterY`, `+0x3c` an MFC `CArray<int>` (the **Move** timeline; `m_pData +0x40`, `m_nSize +0x44`) with its length copied to `+0x50`, `+0x54` the **Attack** timeline with its length at `+0x68`, `+0x6c` the **Idle** timeline with its length at `+0x80`, `+0x84..+0x90` `SelectionX1/Y1/X2/Y2`, `+0x94` `Dying`, `+0x98` `Palette` followed by `Palette` palette objects from `+0x9c` and their `0x400`-byte buffers from `+0xac`, `+0xbc` a `CArray` for `Sound`, `+0xd0` `TileSize`, `+0xd4` `Projectile`, `+0xd8` `InfoPicture` read as `0x10` bytes, `+0xe8` a `CArray` for `ShootOffset`, `+0xfc` `ShootDelay`, `+0x100` `AttackDelay`, `+0x104` `Z`, `+0x108` `Flip`, `+0x10c` `DescText` read as `0x20` bytes. Each scalar's default is the parent's already-resolved field at the *same* offset when `Parent != -1` (`REG-KEY-045`); the fallback when there is no parent is the literal **`-1`** except `IdlePhases`, `Dying`, `Palette`, `Projectile`, `ShootDelay`, `AttackDelay`, `Z` and `Flip`, which default to **`0`**, and `TileSize`, which defaults to **`1`** (`0046c25f MOV EAX,0x1`). **Unlike `objects.reg` (`REG-OBJ-046`), `File` here *does* inherit** — `0046bc8f` loads the parent's `+0x0c` as the default. The three timelines are the same run-length expansion the objects loader performs, run three times (`0046c4af`, `0046c59a`, `0046c688`): append `<track>AnimFrame[i]` exactly `<track>AnimTime[i]` times via `CArray::SetAtGrow(m_nSize, …)`, dropping both heads each round, ending when either track empties. The resulting length is what the move arm takes the animation phase modulo (`TERR-SPR-047`); the idle and attack arms take **no** modulus. Corpus, 34 classes on the EXP-0032 framing: expanded lengths 4…24 for Move, 8…28 for Attack, 0…14 for Idle (`evidence/unit-timelines.csv`) | High (each key→offset pair is a `PUSH`/`PUSH`/`CALL`/`MOV` quadruple in a listing read end to end, key names resolved from the operand addresses — `evidence/rom-unit-frame-excerpt.md` §5; the expansion loop is reproduced instruction by instruction; `File`'s inheritance is one named load) / Medium (the length distributions — one registry) | ● active | [EXP-0039](../experiments/EXP-0039-unit-frame/) |
| REG-UNITS-050 | **The death chain: `Dying` is a `units.reg` `ID` naming the class whose sheet carries the corpse, and `unit+0x15a` is the stage.** The dying and corpse arms of `FUN_0045bf00` do not merely pick a different block — they **replace the class**: `0045c217`/`0045c01a`/`0045c08f` load `class+0x94` (`Dying`), subscript `DAT_005eb674` with it (so it is an `ID`, `REG-KEY-044`), park it in the slot the sprite lookup later uses, and read that class's `File`, `Flip`, `MoveBeginPhases`, `MovePhases`, `AttackPhases`, `DyingPhases` and `BonePhases` in place of the unit's own. **So `DyingPhases`/`BonePhases` describe frames in the sheet of whoever names this class as their `Dying`, not necessarily the class's own** — for 20 of 34 classes that is itself, and the human classes point at a sibling: all six swordsman/axeman variants and both pikemen die as `Human Swordsman with shield` (`ID 4`), both clubmen and both archers as `Unarmed Fighter` (`ID 1`), and both mages as `Human Mage` (`ID 24`). Every one of the 34 classes has `Dying != 0`, every target resolves to a loaded class, and every target has `DyingPhases > 0` (`evidence/unit-corpse-chain.csv`). **`unit+0x15a` is the corpse stage**: `0` alive (the standing frame), `1` freezes on the **last** dying frame — `(dir+1)·DyingPhases` past the dying base, i.e. index `DyingPhases−1` of the direction's slot (`0045c060 INC EAX ; 0045c061 IMUL EAX,[ECX+0x20]`) — and `>= 2` draws bone frame `stage − 2` (`0045c101 AND EDX,0xff ; 0045c107 LEA EDI,[EDI + EDX + 7]`, the `+7`/`+0xe` being `S−2`); when the target class's `BonePhases` is `0` the whole corpse branch falls back to the standing frame (`0045c09e`). At stage `3` two further per-unit passes switch themselves off (`FUN_00462f80`/`FUN_00462f90`, `CMP byte ptr [ECX+0x15a],0x3 ; JNC ret`), and CUnit's `vt+0x34` stops at stage `> 1` (`0045ce11`). 5 of the 34 classes carry no `BonePhases` — exactly the 5 that carry `IdlePhases` (the bat, the bee, the dragon, the ghost and the Death Star) — which is why the bone and idle blocks can share a base (`SPR256-UNIT-024`). **What advances the stage is answered by EXP-0071: nothing on the client does.** The whole-image scan for `[reg + 0x15a]` finds five writes — three zeroings plus the constructor's, and one at `0041184f` that copies it wholesale from `[0x005cd702]` under bit 3 of a mask, in the same routine and the same shape as HP (`+0xfc`), mana (`+0x13c`), facing (`+0x6c`) and position (`+0x08`) — and that mask is the **field-present mask of the server's state-sync message** (opcodes `0x6c`/`0x6e`/`0x6f`/`0x70`, arm `0x00410e0e`). The value is the simulation actor's `actor+0x13c`, the decay stage `HERO-DEATH-026` reads: 1 at death, 2/3/4 as health passes -10/-20/-40, 5 below -600. **No instruction increments it because the stage is not the client's to advance** (`ANIM-DEATH-007`) | High (the class substitution, both corpse formulas, the `BonePhases == 0` fallback and the three stage thresholds are named instructions — `evidence/rom-unit-frame-excerpt.md` §3, §6, §7; the writer set is a whole-image enumeration) / High (the `Dying` domain and the bone/idle exclusivity — an exhaustive read of the only registry carrying the keys) / High (what advances the stage — EXP-0071 traced the one non-zeroing write to the state sync's mask bit 3 and the value to `actor+0x13c`; the enumeration's shape still cannot see a write through a computed pointer, the same blind spot `TERR-TILE-044` names) | ● active (amended) | [EXP-0039](../experiments/EXP-0039-unit-frame/), [EXP-0071](../experiments/EXP-0071-animation-driver/) |
| REG-UNITS-051 | **`Flip` is not a mirror flag on the art — it halves the sheet.** `class+0x108` is read at five sites in `FUN_0045bf00` and at none of them is it passed to a blitter; every one is a branch selecting between two hard-coded layout constants. With `Flip == 0` the sheet stores **16** standing frames and **8** directions per animated block, and nothing is ever mirrored. With `Flip != 0` it stores **9** and **5**, and the missing half is produced at runtime by reflecting the stored half: `0045c175 CMP EDI,0x8 ; JLE` then `0045c17e MOV EDX,0x10 ; SUB EDX,EDI` for the 16-way standing index, and `0045bf71 CMP ECX,0x4 ; JLE` then `0045bf76 MOV EDX,0x8 ; SUB EDX,ECX` for the 8-way ones — each also setting a flag slot to `1` (`0045bf7b`, `0045c151`, `0045c183`, `0045c2cd`, and the same pair inside the corpse arms) which is handed to the sprite blit as its **sixth argument** (`0045c7a5`, `0045c41c`), the argument the object path passes as a literal `0` (`TERR-SPR-038`, `TERR-SPR-048`). The facing itself is `unit+0x6c`, and the two direction indices derived from it are **`dir16 = (facing − 8) & 0xf`** and **`dir8 = ((facing − 8) >> 1) & 7`** (`0045bf41`, `0045bf66`, `0045c168`) — so the standing block is 16-way and every other block 8-way, on both layouts. Corpus: `Flip ∈ {0 ×24, 1 ×10}` over the 34 classes; the constant pairs are what make the sheet-tiling test of `SPR256-UNIT-024` come out exact on 33 of them | High (all five reads, both reflection idioms and both direction derivations are named instructions, and "never passed to a blitter" is an enumeration of the field's readers inside the only routine that reads it) / High (that the flag reaches the blit as argument six — it is the same slot, pushed last, at two call sites) | ● active | [EXP-0039](../experiments/EXP-0039-unit-frame/) |
| REG-ROSTER-052 | **Restores the pairing `REG-ROSTER-019` owed, and refutes the one it published.** A `units.reg` class's sprite is `units/` + `Files[class.File]` + `.256` (`REG-VAL-029`'s corrected index, read off the loader at `0046bc8f` into `class+0x0c`, `REG-UNITS-049`). Emitted for all 34 classes on the EXP-0032 base (`evidence/units-roster-restored.csv`): **34/34 carry their own `File` and `DescText` keys** (nothing inherits here), **34/34 resolve to a `.256` node that exists in `graphics.res`**, and **`File` equals the section's own `%d` on 33/34** — the exception is `Unit33` "Unarmed Fighter with Shield", `File = 21`, which shares `Unit21` "Unarmed Fighter"'s sheet, the only shared entry in the table. Measured against the withdrawn `Files[class.ID]` pairing on the same walk: the two **differ on 34/34** and `ID` is inside the 33-entry table on only **20/34**, so 14 of the published arrows indexed past the end of `Files[]` and the other 20 named another class's sheet (`Human Archer` `ID` 14 → `monsters\legg\sprites`, `Human Mage` `ID` 24 → `monsters\ghost\sprites`). **The old pairing was refutable from the shipped bytes alone at any time since EXP-0006** — no framing correction was needed to see 14 out-of-range subscripts | High (the resolution rule is code-read and already published — `REG-VAL-029`, `REG-UNITS-049`; what this row adds is its mechanical application plus three discriminators against the alternative: 14/34 of the old arrows out of range, 34/34 of the new ones opening a node that exists, and a render that draws frame 0 of **both** candidate sheets for every class, where a wrong arrow is a picture that does not match its own `DescText`) | ● active | [EXP-0043](../experiments/EXP-0043-reg-restorations/) |
| REG-CUT-053 | **The 33 video-cutscene registries are one schema with two optional section families, not 33 formats.** Grouping all 44 registries by a schema signature that strips digits from every section and key name yields 14 distinct shapes, and the 33 cutscene files occupy 3 of them, each a subset of the next: `Common{startx,starty,nFadings}` + `Fading<n>{startframe,endframe,startfade,endfade}` ×27; the same plus `Common.nPanaramings` + `Panaraming<n>{startframe,endframe,stepx,stepy}` ×4 (`M10/01`, `M50/01` in both containers); and `Common` alone with `nFadings = 0` ×2 (`LOGOS/1c.reg`, `LOGOS/buka.reg`) — a degenerate instance of the first, not a different format. Every declared count agrees with its own section count: **`nFadings` 33/33**, **`nPanaramings` 4/4**. `startx == starty == 0` on 33/33. `startframe <= endframe` on 65/65 sections. The kind-4 census reproduces `REG-DBL-035` exactly — `startfade`/`endfade` take only `0.0` (×61) and `1.0` (×61), `Fading1` always fading **in** and the last `Fading` always **out**. Structurally each registry sits beside a `.smk` node of its own basename in its own container directory, **33/33**, and the **15 node paths present in both `VIDEO4.RES` and `VIDEO8.RES` have byte-identical resolved content, 15/15**, while their `.smk` siblings differ in size — so the two containers are two encodings of one set of cutscenes sharing one timing sidecar | High (a listing: every cutscene registry in the install, every section, with the two declared counters cross-checked against counts derived by walking section names — a different path from the counters) / Medium (that these values *are* playback timing for the sibling video: the 1:1 file pairing, the fade-in/fade-out shape and the pan step are strong, but `FUN_004aee90` — the consumer `REG-LOC-038` names — was not read this round) | ● active | [EXP-0043](../experiments/EXP-0043-reg-restorations/) |
| REG-KEY-054 | **The `kind` sorted bit is set on exactly one node per registry — the root — so every lookup below the root is a linear scan.** `REG-KIND-033` pinned bit 4 to the instruction that tests it (`TEST AL,0x10` at `004ce909`, `bsearch` when set, linear scan when clear); this row is the census of which nodes set it. Over all 44 registries: the root's `kind` is `17 = subkey\|sorted` on **44/44**, and of the **512 subkey records** in the corpus **0** set bit 4. So the `bsearch` path is reachable only for a top-level section name, and every per-key lookup inside a section is a scan. The corresponding obligation on the data is met: the root's child list is sorted under the lookup's own comparator (case-insensitive ASCII over 15 characters) on **44/44**, while only 81 of the 512 non-root child lists happen to be sorted. **Consumer consequence:** sections are stored in *lexicographic*, not numeric, order (`MapObject1, MapObject10, …, MapObject2`), so the order in which a loader's `"<Prefix>%d"` counter visits sections (`REG-KEY-044`) is **not** the record order; and a writer must emit root children sorted or the game will `bsearch` a list that is not | High (an exhaustive census over all 4 621 records — the denominator is the population, and the instrument is a **corpus census**, the immune one; the consuming instruction is `REG-KIND-033`'s, not re-read here) | ● active | [EXP-0043](../experiments/EXP-0043-reg-restorations/) |
| REG-NAME-055 | **The 15-character name clamp, seen from the data side — and repaired from the game's own strings.** `REG-REC-032` fixed the name field at 16 bytes with 15 significant characters from the writer's clamp (`FUN_004ce6d0`) and the lookup's `_strnicmp(key, node+0x10, 0xf)`. Census over all 4 621 records: name lengths run 1..15, **0 records are 16 or longer**, and **69 are exactly 15** — the length at which a longer authored name would have been cut. Falsification the clamp invites: **0 sibling pairs anywhere in the install are indistinguishable under the lookup's 15-character case-insensitive compare**, so the clamp never bites on shipped data. Of the 6 fifteen-character keys in the registries this experiment tabulates, `rom.exe` carries a longer identifier sharing all 15 characters for **3**, which recovers the author's own name: `MinimalGuardRan` → **`MinimalGuardRange`**, `AddPictureDocum` → **`AddPictureDocument`**, `ScenarioMission` → **`ScenarioMissionCount`**. The other three (`AddTextDocument`, `EnableMercenary`, `IntelligentCons`) have no longer literal, so whether they were truncated at all is undecidable from the image | High (the length histogram and the collision hunt — exhaustive over the corpus) / **Medium** (the un-truncation: a printable-ASCII-run census over our own `rom.exe`, so it is immune to the function-table blind spot but shows only that the image *contains* the longer string; no call site was read pushing it to `FUN_004ccc20`) | ● active | [EXP-0043](../experiments/EXP-0043-reg-restorations/) |
| REG-KIND-056 | **A record's `kind` is a property of the record, not of the key name — the same key can be a scalar in one section and an array in the next.** Census over all 44 registries: **9 key names carry more than one kind.** Four are the empty-string-vs-array pair `REG-KEY-045` already explains, where a present-but-empty kind-0 record reaches array length 0 by a success path: `units.reg` `AttackAnimTime`/`AttackAnimFrame` (kind 0 ×1, kind 6 ×32) and `structures.reg` `AnimTime`/`AnimFrame` (kind 0 ×52, kind 6 ×14). The remaining five are a different phenomenon and occur only in `scenario.reg`, where a key is spelled as a **bare int32** in some `[Mission<n>]` sections and an **int32 array** in others: `Mercenaries` (kind 2 ×3 / kind 6 ×12), `InnNPC` (5/8), `InnMission` (5/8), `EnableMercenary` (4/1), `AddTextDocument` (1/1). **Consumer consequence: a typed reader for these keys must accept a bare scalar where an array is expected and treat it as a one-element list** — reading only kind 6 loses 18 of the 31 `InnNPC`/`InnMission` records | High (a census over every leaf record in the install, denominator = population) / **Unknown** (how `rom.exe`'s array accessor `FUN_004cd240` treats a kind-2 record: `REG-KEY-045` read its kind-0 and kind-6 arms, and no disassembly was run this round — one `DisasmFn` on `FUN_004cd240` settles it. The one-element-list reading is what the shipped data forces, not what an instruction was seen to do) | ● active | [EXP-0043](../experiments/EXP-0043-reg-restorations/) |
| REG-SFX-057 | **`sfx.res::sfx.reg` is the sound-slot table the rest of this ledger was already indexing blind.** Two sections: `[Global] SfxCount = 564` and `[Sfx]` holding **115** kind-0 strings named `Sfx<n>`. **`SfxCount` is the highest slot id, not the entry count** — the ids are sparse (1..8, 11..16, 50, 60..62, 70, 80, 81, 90, 100, 110, …, 564) and 564 is exactly the largest present, the same shape as `Templates.ini`'s own `Number = 1088` against 165 labelled slots. Each value is a path relative to `sfx.res` with no extension (`click00`, `units\sword`, `ambient\river`, `magic\firewall`). It closes two open references elsewhere in the repo: **every nonzero element of a `units.reg` class's `Sound[]` array is an existing slot — 153/153**, with 17 further elements reading `0`; and **`REG-OBJ-047`'s ambient-sound slots resolve** — the `>= 0` arm's `0x3c..0x3e` are `ambient\bird1/2/3` and the `-2` arm's `0x46` is `ambient\crow` | High (the table is an exhaustive read of the registry; both resolution censuses carry their denominators, and the `Sound[]` one is over every class in `units.reg`) / **Medium** (that slot 70 being a crow corroborates `REG-OBJ-047`'s Medium reading of `FireObject = -2` as burnt-out remains: the slot numbers are code-read there and the sample name is now known, but nothing in the image names the intent) | ● active | [EXP-0043](../experiments/EXP-0043-reg-restorations/) |
| REG-NPC-058 | **`scenario.res::npc.reg`, tabulated.** 110 top-level sections: **105 `npc<n>`** (ids 1..132, sparse), four character-archetype blocks `MaleFighter`/`MaleMage`/`FemaleFighter`/`FemaleMage` each carrying exactly `Face Body Reaction Mind Spirit Skill` as int32, and `Multiplayer` carrying four kind-6 face-id lists `FacesMM`(6) `FacesMF`(15) `FacesFM`(4) `FacesFF`(8). Per-npc keys with presence out of 105 and measured domain: `Flags` **105/105** (kind 0), `Face` 86 (1..30), **`PortraitX1` 48 (7..69), `PortraitY1` 48 (8..72), `PortraitX2` 10 (48..54), `PortraitY2` 10 (15..32)** (corrected by [EXP-0141]: this row read *“`PortraitX1/X2`+`PortraitY1/Y2` 58”*, which is the sum of two of the four and not any key's own count — the split is the corpus side of `X2`/`Y2` having no reader, `REG-NPC-089`), `Picture` 33 (64..80), `DataBinID` 23, `PriceA` 13 (28..80), `PriceB` 13 (0..15). **`Flags` is a comma-separated token list with `!` negation over exactly 11 tokens** — `Human, Mage, Female, Face, Picture, Platoon, Hero, Me, Start, MySex, MyClass` — 15 distinct combinations, and **all 11 occur as whole ASCII strings in `rom.exe`**. **`DataBinID` is an id in `Templates.ini`'s space** (`EDITOR-023`'s 1088-slot placeable-template table, the install's own text): all **23/23** lie inside the declared `1..Number=1088`, and of those, the **13 at or above the table's lowest labelled slot (103) name a labelled slot on 13/13** — `509 → M10_Witch`, `510 → M10_Merchant`, `511 → M30_Healer`, `523 → M40_Hima`, `520 → M130_Veglud`, `521 → M151_Lord`, `522 → M71_PoorGuy`, `1011 → A_PeasantGuard3`, `1012 → U_PeasantLeader3`, `1028 → F_BrigandLeader3`, `1036 → M_EvilFemale2`, `1051 → F_Knight3`, `1056 → F_Knight4`. Full table: `evidence/values-scenario_npc.csv`. **Amended by [EXP-0057] on `PriceA`/`PriceB`:** the loader `FUN_0048c510` stores them at `npc+0x18` / `npc+0x1c` of the `0x30`-byte record in the array `DAT_005f075c`, and **no reader of either field was located** — `EnumRefs refto:5f075c` enumerates every route to a record (20 hits, 7 owners, 0 in orphan/undisassembled code) and a `DisasmFn` pass over all six non-loader owners finds no `[reg+0x18]`/`[reg+0x1c]` operand at all. They are **not** the shop's: nothing in the shop machinery touches the npc array (`SHOP-NPC-012`). **Amended by [EXP-0062]: they are the two terms of the *mercenary hire price*** — `cost = (PriceA + n·PriceB) × unitPrice(mission)`, read by `FUN_005056f1` (`005057d7`…`00505801`) and by the object serializer `FUN_004e7de3`, both through `FUN_004c86a0(0x5f0758, id)` — the array *object*, not its data pointer, which is why a `refto:5f075c` sweep could not see them (`MERC-PRICE-004`). The 13 npcs that carry them are exactly the 13 mercenary types with a nonzero `[General] MercenaryCount`, 15/15 | High (the tables, the presence counts and the `Flags` vocabulary — exhaustive over the registry) / **Medium** (`DataBinID` is a Templates.ini id: 13/13 above slot 103 is corpus agreement, not a discriminator, and the remaining **10 values — `26`×4 and `42`..`46` — are all below the table's lowest labelled slot and name nothing shipped**) / **Unknown** (no gloss is put on `Body`/`Mind`/`Reaction`/`Spirit`/`Skill` beyond their measured values). **The `Face`/`Picture`/`Portrait*` Unknown is discharged by [EXP-0141]** (`REG-NPC-088`, `REG-NPC-089`): none of them indexes a picture resource — `Picture` and `Face` are stores into a synthesised actor's typeID and face, and `Portrait*` is a Win32 `RECT` | ● active (amended; `PriceA`/`PriceB` answered by MERC-PRICE-004, the `Portrait*` presence count and the `Face`/`Picture`/`Portrait*` Unknown by EXP-0141) | [EXP-0043](../experiments/EXP-0043-reg-restorations/), **[EXP-0062](../experiments/EXP-0062-mercenaries/)**, **[EXP-0141](../experiments/EXP-0141-npc-fields-and-tag/)** |
| REG-SCN-059 | **`scenario.res::scenario.reg` + `scenario.res::globalmap.reg` are the campaign spine, tabulated — and every id in them resolves.** `scenario.reg`: `[General] TotalMissions = 15`, `ScenarioMissionCount = 15` (name un-truncated from `rom.exe`, `REG-NAME-055`) and `MercenaryCount` a **15**-element kind-6 array; **24 `[Mission<n>]` sections** — the 15 main missions numbered by tens (`Mission10`..`Mission150`) plus 9 sub-missions. Per-mission keys and domains: `Mercenaries` 15, `InnNPC` 13, `InnMission` 13, `ShopMinPrice` 13 (0..5000), `ShopMaxPrice` 13 (1000..10 000 000), `Payment` 9 (700..700 000), `EnableMercenary` 5, `ShopMission` 4, `TCMission` 3, `AddTextDocument` 2, `AddHero`/`AddPictureDocument`/`AutoGetMission`/`LastMission` 1 each. `globalmap.reg`: `[General] ObjectCount = 35` matching **35 `[MapObject<n>]` sections** (1-based, 1..35), each carrying `MapPoint` = 2 ints and `MapRect` = 4 ints and 5 of them a `Picture` string (`onmap13`, `onmap14`, `onmap18`, `onmap19`, …); plus `[MissionObjects]`, **28** `Mission<n>` keys mapping a mission number to a map-object number. **Resolution censuses, each over the whole registry:** every `MissionObjects` value names an existing `MapObject<n>` — **28/28**; every npc-shaped reference in `scenario.reg` names an existing `npc.reg` `[npc<n>]` section — `InnNPC` **22/22**, `AddHero` **1/1**, `Mercenaries` **107/107**, `EnableMercenary` **13/13**, i.e. **143/143**; and every `scenario.reg` mission has a `MissionObjects` entry — **24/24**, with 4 entries there (`81`, `101`, `141`, `151`) naming no `scenario.reg` section. **Amended by [EXP-0059]: the readers this row left unlocated are found, and they are all one routine.** `FUN_004879f0` is the mission-record loader, and a `FindStrXref` sweep makes it the **only** referrer of every per-mission key literal — `ShopMinPrice` → `record+0x10`, `ShopMaxPrice` → `record+0x14` (`SHOP-MISSION-018/019`), `ShopMission`/`InnMission`/`TCMission` → three `u16` arrays merged into one sub-mission list (`REG-SCN-062`), `Payment` → `+0x0c`, `AutoGetMission` → `+0x110`, `LastMission` → `+0x11c`, `Mercenaries` → `+0x98`, `EnableMercenary` → `+0x30`, `InnNPC` → `+0xc0`, `AddHero` → `+0x1c`, and `AddTextDocument`/`AddPictureDocum` element-wise into `FUN_00488cf0`/`FUN_00488fb0`. `ScenarioMissionCount` **is** read, by that same routine, as the campaign's mission-number bound: `00487a1f` reads it (default −1) and `00487a48…00487a5e` rejects any request above `10 × count`. `MercenaryCount`'s reader is `FUN_00487640`, the record's reset (`004877ab`/`004877b5`), which copies its 15 `u16` elements to `record+0x60` beside a parallel 15-`dword` array at `+0x88` it zeroes — **located, not interpreted** | High (the tables and all five resolution censuses — exhaustive, each with its denominator, and the npc census is the kind that fails visibly if the id space were wrong: 143 subscripts into a 105-member sparse set) / High (**what `MercenaryCount` counts is answered by [EXP-0062], `REG-SCN-067`**: the 15 elements are **not** one per main mission — they are one per mercenary **type** 1..15, a pool count, which is why the per-mission reading failed 12/15. `record+0x60` is the working copy's `m_pData` and the parallel `+0x88` array is the per-type hire flag; all three consumers are now read at instruction level) / **Unknown** (whether `TotalMissions` is read at all: **no printable-ASCII run in `rom.exe` or `Map Editor.exe` equals it**, while its neighbour `ScenarioMissionCount` is present, holds the same 15, and is now shown to be read as the mission-number bound) | ● active (amended; the `MercenaryCount` Unknown answered by REG-SCN-067) | [EXP-0043](../experiments/EXP-0043-reg-restorations/), **[EXP-0062](../experiments/EXP-0062-mercenaries/)** |
| REG-AI-060 | **`world.res::data/ai.reg` is the install's smallest registry and half of it is unreferenced.** 4 records, **empty pool**, two sections of one int32 each: `[Scanning] MinimalGuardRan = 8` and `[Tasker] IntelligentCons = 15` — both names at exactly 15 characters, i.e. clamped (`REG-NAME-055`). `rom.exe` carries `MinimalGuardRange` and `Scanning` as whole strings, which recovers the first key's authored name. **Neither `Tasker` nor any printable-ASCII run beginning `IntelligentCons` occurs anywhere in `rom.exe` or in `Map Editor.exe`**, so no site in either executable can look that key up by a literal name | High (the content — four records read in full) / **Medium** (the absence: the instrument is a printable-ASCII-run census over our own executables, which is immune to the function-table blind spot because no disassembly is involved, but blind to a name assembled at run time or stored non-contiguously. A cross-reference sweep on `"Scanning"`/`"MinimalGuardRange"` and their loader would settle whether the section is read at all) | ● active | [EXP-0043](../experiments/EXP-0043-reg-restorations/) |
| REG-UNITS-061 | **`Z` is the key that picks a unit's C++ class — and all it buys is the draw layer.** `FUN_004104e8` is the only site in `rom.exe` that allocates a `0x1b0` unit object, and it does so twice: `0041151f` loads the `units.reg` class array `0x005eb674`, `0041152a` subscripts it, `0041152d CMP dword ptr [ECX+0x104],0x0` tests **`Z`** (`class+0x104`, `REG-UNITS-049`), and the branch chooses `CUnit` (`Z == 0`, `CALL 0045ae30`) or **`CAirUnit`** (`Z != 0`, `CALL 00461560`). The two classes are MFC `CRuntimeClass`-registered at `0x599208`/`0x599220`, both `0x1b0` bytes, `CAirUnit` derived from `CUnit`. Their vtables differ in **four** of 33 slots, three of which are `GetRuntimeClass`, the destructor and the class-name stub; the single behavioural override is `+0x38`, where `CUnit` (`FUN_0045b270`) registers the sprite in draw layer **2** when the corpse stage `[this+0x15a] < 2` and `[this+0x18c] & 0x80` is clear, else layer **4**, and `CAirUnit` (`FUN_004615d0`) registers layer **3** unconditionally and carries no corpse arm — both then blit through `[0x00632f88]` at the same computed destination. So `Z` is a **z-order/altitude flag on the drawable**, not a movement property: it never reaches the simulation actor's `+0x4a` (`TERR-MOVE-055`), and the simulation module never reads the class array at all. Corpus, 34 classes on the EXP-0032 framing with `Parent` resolved: `Z != 0` on exactly **2** — `Sonic Bat` (ID 70) and `Dragon` (ID 71), both `Z = 96`; every other class either carries `Z = 0` or no `Z` key at all (default 0). Their 715 placements over the 38 shipped maps account for 45 of the 263 that EXP-0041 found stranded under the ground mask | High (the test, both allocation arms and the one behavioural override are named instructions, and the class identities come from the `CRuntimeClass` records rather than from inference) / Medium (reading `Z` as "flying": layer 3 plus the two class names is what carries it, and the value 96 is otherwise unexplained. **Two independent witnesses added by [EXP-0078]**, neither from the drawable side: the `Z != 0` pair is *exactly* the `Data.bin` `movementType == 3` pair (`MOVE-DOM-028`), and the AI's target chooser adds one cell to a candidate's distance when its `vt+0x20` is 3 and the decider's is not — `0052e18f…0052e19e`, the image's own arithmetic for a flier (`MOVE-DOM-024`(e)). The value 96 is still unexplained) | ● active | [EXP-0044](../experiments/EXP-0044-mover-domain/) |
| REG-SCN-062 | **`ShopMission` is a list of mission numbers, not a shop parameter — and it shares one consumer with `InnMission` and `TCMission`.** `FUN_004879f0` reads all three into `u16` arrays at `record+0xd4` / `+0xe8` / `+0xfc` (`00487c4a` / `00487c5a` / `00487c6e`) and then runs **three consecutive, instruction-for-instruction identical loops** over them. Each element is tested `value % 10 != 0` (`XOR EAX,EAX ; MOV AX,word ptr […] ; CDQ ; IDIV 10 ; TEST EDX,EDX ; JZ`) — a multiple of ten is **dropped**; anything else has its own `[Mission<n>]` section opened for `AddHero`, `Payment` and `EnableMercenary`, is given its `GlobalMap.reg` `[MissionObjects]` object via `FUN_00487940`, and is appended as a **0x4c-byte sub-mission record** to one shared CArray at `record+0x48`. **No instruction records which of the three arrays an entry came from**, and the sub-record's `+0x10`/`+0x14` — where a shop window would sit — are never written by any of the three loops. Those records are the campaign's offered side missions: `FUN_004883f0(n)` routes `n % 10 == 0` to *load mission n* (`FUN_00488460`) and everything else to `FUN_00488420`, a linear search of the array by the record's own `+0x04`; `FUN_00488760` returns the next entry whose `+0x18` is nonzero and is called from `FUN_00464140`, which holds a mission record at its own `+0x548`. Each entry carries a counter at `+0x48` that every mission load increments (`00487d03…00487d0b`) and is deleted at 2 (`00487d11`). **Corpus, identical in all three roots** (`EXP-0059/evidence/submissions.csv`): `ShopMission` ships **4 elements over 24 sections** — `Mission30 → 31`, `Mission90 → 91`, `Mission110 → 110`, `Mission130 → 130` — so two of the four are multiples of ten; the same test drops 14 of `InnMission`'s 22 elements and 0 of `TCMission`'s 3. **⚠ “and are therefore inert” is RETRACTED by [EXP-0060]** (retracted.md): the `% 10` test governs only whether a value becomes a sub-mission *record*. The value the building hands to `FUN_0048a360` is unfiltered, and its `FUN_004883f0` step routes `n % 10 == 0` to `FUN_00488460(n)`, which accepts `n` whenever `n >= record+0x04`, after which `FUN_004887c0` latches the **main** record's `+0x18`. A multiple of ten is how a building hands you the *main* mission — the inn does it in 11 of the 13 town chapters and the shop in the other two (`REG-SCN-064`). Four admitted values (`81`, `101`, `141`, `151`) name no `[Mission<n>]` section at all, so their sub-record takes every default — the same four `REG-SCN-059` already found dangling in `[MissionObjects]` | High (the loader is read at instruction level and the three loops are the same instructions on three arrays; the corpus census is exhaustive over the registry and reproduces in all three roots) / **Medium** (that the array *is* the town's offer list rather than some other queue: `FUN_00488420`/`FUN_00488760`/`FUN_004883f0` are read and their callers enumerated, but the screen that renders the list was not read) / **Unknown** (whether anything distinguishes an inn-, shop- or TC-supplied entry downstream. An `EnumRefs disp:` sweep over the record's class range `00487000..0048c000` — 75 functions, **0 orphan bytes**, 1093 undisassembled — finds only the ctor, the copy/destroy pair, the reset, this loader and the two streaming routines touching `record+0xe8`, so nothing outside the class reads the ShopMission array; the instrument is blind to an access through a pointer laundered out of the class. **[EXP-0060] answers it: the sweep was complete and the question was asked in the wrong place.** Provenance is not in the sub-mission record, it is in *which array the screen reads* — `InnMission` is read only by the inn view, `ShopMission` only by the shop view, `TCMission` only by the school view, `REG-SCN-064`) | ● active (amended; the “inert” clause retracted) | **[EXP-0059](../experiments/EXP-0059-shop-mission/)**, **[EXP-0060](../experiments/EXP-0060-town-screen/)** |
| REG-SCN-063 | **`AutoGetMission` and `LastMission` are the campaign's routing keys, and the first is what keeps the player out of the town.** `FUN_004879f0` reads `[Mission<n>] AutoGetMission` into `record+0x110` with default **-1** (`00487b1e` the literal, `00487b30` the store) and `LastMission` into `record+0x11c` with default **0** (`00487b3c` / `00487b53`). `record+0x110` has exactly one reader, `FUN_004884d0`, itself called from exactly one site (`00473f35`, the mission-end arm of `FUN_00473110`). Its two arms are the campaign's fork: `!= -1` calls `FUN_00488460(that mission)`, which loads that mission's record, then `FUN_004887c0(record+0x04)` which latches the announce flag, and the party travels to the new mission's own map object; `== -1` returns 0 and the caller sends the party to global-map object 0, the city. **Corpus, identical in all three roots** (`EXP-0060/evidence/missions.csv`): `AutoGetMission` ships **once in the whole campaign**, `[Mission10] = 20`, and `LastMission` ships once, `[Mission150] = 1`. So missions 10 and 20 are the only two whose end does not pass through the town, which is what makes `SHOP-TOWN-023` true | High (both readers, both defaults and the fork's two arms read at instruction level; `callto:4884d0` returns 1 hit, 0 in orphan code) / **Unknown** (what reads `record+0x11c`: the `LastMission` store is read at instruction level, its consumer was not searched) | ● active | **[EXP-0060](../experiments/EXP-0060-town-screen/)** |
| REG-SCN-064 | **`InnMission`, `ShopMission` and `TCMission` are the offer lists of three town buildings — tavern, shop and school — and each key has exactly one reader.** The record is embedded at `campaignScreen+0x548`, so the four arrays sit at `+0x608` (`InnNPC`), `+0x61c` (`InnMission`), `+0x630` (`ShopMission`), `+0x644` (`TCMission`) of the campaign screen. An image-wide `EnumRefs disp:` over all four objects and their data/size fields, plus `imm:630` for the two `ADD ECX,0x630` forms, returns: `ShopMission` to `FUN_004a8bc3` only, the **shop** view's `vt+0x80`; `TCMission` to `FUN_004b7f20` only, the **school** view's `vt+0x80`; `InnMission` to `FUN_00480fd0` only, and `InnNPC` to `FUN_00480560` + `FUN_00480fd0`, both **inn**-view methods. 0 hits in orphan or undisassembled code. Shop and school each take element **0** on entry, hand it to `FUN_0048a360` and `RemoveAt(0,1)` the array (`004a90b5`…`004a9122`, `004b834c`…`004b83a0`); the inn is per-NPC — `FUN_00480fd0` reads `InnMission[i]` and `InnNPC[i]` at the **same subscript** (`00481007`…`0048102b`), speaks `inn\NPC\npc%02dm%d`, and queues the mission, while `FUN_0048a300` later removes the accepted value from **both** arrays at the same index (`0048a335` `InnMission`, `0048a343` `InnNPC`). So `InnNPC[i]` pairs positionally with `InnMission[i]` — `REG-SCN-059`'s unpaired `InnNPC` is closed. `InnMission[i] == 0` is the sentinel for an NPC with nothing to give: the zero arm speaks a line keyed by the live main mission and queues nothing (`00481038`, `0048107a`). **`TC` is the school** — the view that reads `TCMission` is the one `music\schoolw.wav` / `music\schoolm.wav` and `SFX\Town\School\Point.wav` belong to. **Corpus, all three roots identical** (`EXP-0060/evidence/offers.csv`, `timeline.txt`): the three keys name **26 distinct missions over the 13 town chapters, each by exactly one building and none by two** — tavern **19**, shop **4**, school **3** — covering every main mission 30…150 and every side mission 31…151 with no gap. The tavern's 19 is its **22** `InnMission` elements minus the **3** that are `0`, the no-mission sentinel this row names above (`[Mission50]`, `[Mission110]`, `[Mission130]`, each at index 0); 19 + 4 + 3 = 26, which is the same clause's own distinct-mission total, and `len(InnMission) == len(InnNPC)` in 22/22 sections. *(Published 2026-07-30 as “tavern 21” — a figure that is neither the element count 22 nor the offer count 19, i.e. an arithmetic slip inside a clause that already stated the correct total. Repaired in place the same day: no mechanism moves and no `retracted.md` row is owed, since nothing a consumer could implement was ever different.)* | High (each key's single reader established by an image-wide displacement enumeration on the repaired table, and each consuming routine read at instruction level; the pairing and the zero sentinel are named instructions) / **Medium** (that no *fourth* building exists: the three views were reached from the campaign screen's slot table and the town's own five-way hit test, not from an exhaustive sweep of every view class) / **Unknown** (what happens when one mission number appears in two of the three keys — the corpus never does it. From the code it would be appended **twice**, since both loops append unconditionally and no dedup exists, and resolved once by the acceptor; that is a reading of the loops, not an observation) | ● active | **[EXP-0060](../experiments/EXP-0060-town-screen/)** |
| REG-SCN-065 | **The sub-mission array's whole lifecycle, and what separates a mission that is *listed* from one that is *offered*.** Appended at every main-mission load by the three `% 10 != 0` loops of `FUN_004879f0` (`REG-SCN-062`), as 0x4c-byte records with vptr `0x59a460`, mission at `+0x04`, map object `+0x08`, `Payment` `+0x0c`, announce latch `+0x18`, `AddHero[]` `+0x1c`, `EnableMercenary[]` `+0x30`, owning record `+0x44`, age `+0x48`. **Aged and dropped:** the pass at `00487cfe`…`00487d16`, which runs *before* the appends, increments every surviving element's `+0x48` and deletes it once the counter reaches **2** — so an offer lives through the load that created it and one further main-mission load, and no longer. **Removed on completion:** `FUN_00488970` deletes the finished sub-mission by `memmove` and decrements `record+0x50`. **Offered:** the latch `+0x18`, whose only writer is `FUN_00486fd0` (`00486fef`, 0 to 1, guarded by its own current value), reached only through `FUN_004887c0` — `callto:486fd0` = **1 hit**, `callto:4887c0` = **2 hits** (`FUN_0048a360`, `FUN_004884d0`). **Consumed:** `FUN_00488740` resets the cursor `record+0x44` and yields the main mission first if the record's own `+0x18` is set, then `FUN_00488760` walks the array yielding only latched entries; `-1` means nothing is on offer. **Bounded:** `FUN_004879f0` rejects any mission number above `10 × [General] ScenarioMissionCount` (`00487a1f`…`00487a5e`). `FUN_00488850`, which also reads `+0x18`, has **0 callers and 0 `.rdata` slots** — dead code | High (every step is a named instruction, and the two latch enumerations ran on the repaired table with `callto` covering `.rdata` slots so virtual dispatch is included; blind to a call through a register Ghidra resolved to no target) | ● active | **[EXP-0060](../experiments/EXP-0060-town-screen/)** |
| REG-GMAP-066 | **`Scenario\GlobalMap.reg` is keyed `[MissionObjects] Mission<n>`, not `[Mission<n>] MissionObjects` — and the city is object 1, which no mission ever occupies.** `FUN_00487940` formats `"Mission%d"`, then pushes `"MissionObjects"` **last** (`004879a1`) so that is the *section*, with default **1** (`0048799e`), and returns the value **minus one** (`004879b1 DEC ESI`) — a 0-based index into the global-map view's object array at `view+0xc4`. The record caches it at `record+0x08`; a sub-mission record caches its own at `elem+0x08`. `[General] ObjectCount = 35` matches 35 `[MapObject<k>]` sections, 1-based, each with `MapPoint` (2 ints) and `MapRect` (4 ints) and 5 of them a `Picture` string, which `FUN_0048a390` compares against the literal `nothing` to decide whether the object gets a global-map icon. **Corpus, all three roots identical** (`EXP-0060/evidence/globalmap.csv`): 28 `Mission<n>` keys, values 2–35, **0 of them 1**; `MapObject1` carries `MapPoint 215 234` / `MapRect 184 189 110 48`, the largest rect in the file, and is the position `FUN_00464140` tests the party against (`0046426d`…`0046428c`) before building the offered-mission list, and the position `FUN_00473110` travels to at `00473fba` when a mission ends with no `AutoGetMission`. **Numbering, which this row had left implicit:** `MapObject<k>` is the **1-based section name** and `k − 1` the **0-based index** `FUN_00487940` returns; every object named below is given as a section. `MapObject20`…`MapObject35` (indices 19…34) are the file's only `1×1` rects — sixteen of them, each named by exactly one `[MissionObjects]` key — and they are **not** all side missions: `MapObject20`/`21`/`22` carry the **main** missions **20**, **40** and **80**, and the thirteen side missions occupy `MapObject23`…`MapObject35` contiguously, one each. The other twelve main missions sit on the large, sometimes-pictured objects 2…19, of which only `MapObject2` carries two (`Mission140` and `Mission150`). Four of the thirteen side entries — `Mission81`, `Mission101`, `Mission141`, `Mission151` → `MapObject27`/`29`/`32`/`33` — are `REG-SCN-059`'s dangling keys: present in `[MissionObjects]`, with no `[Mission<n>]` section in `Scenario.reg`. **⚠ The sentence this replaces — “Objects 20–35 are all `1×1` rects and every one of them is the target of a *side* mission” — was published at High, and its second half is RETRACTED** (retracted.md): it fails on `MapObject20`/`21`/`22` under **either** numbering convention | High (the section/key order and the default are the push order in the listing, and the object-0 identity has two independent consumers read at instruction level; the object partition is a complete census of the file's own 35 `MapObject` sections against its own 28 `[MissionObjects]` keys, `evidence/globalmap.csv`, reproducing in all three roots) / **Medium** (that `MapObject1` *is the city* as a matter of art rather than of code: no instruction names it; what the code says is that it is object index 0, the only object no mission is placed on, and the one the game returns to) | ● active (amended; the “20–35 are all side missions” clause retracted) | **[EXP-0060](../experiments/EXP-0060-town-screen/)** |
| REG-SCN-067 | **The three mercenary keys are one type space, and `Payment` is the mission's cash reward.** `[General] MercenaryCount` is **not** per mission — it is a **15-element pool indexed by mercenary *type*, 1..15**, which is also the `[npc<t>]` section number; `FUN_00487640` reads it into a `CWordArray` at `record+0x70` and copies it element-for-element into a working array at `record+0x5c` (`004877be`…`004877df`), zeroing a parallel 15-`int` hire-flag array at `record+0x88` in the same loop. Every consumer subscripts all three by `byte [unit+0x15b] − 1` (`0047f456`, `00466a74`, `0048a12e`). This is why `REG-SCN-059`'s per-main-mission reading failed 12/15: the array was right and the index space was wrong. `[Mission<n>] Mercenaries` (`record+0x98`) is that mission's **shelf** of type ids and `[Mission<n>] EnableMercenary` (`record+0x30`) is a **permanent unlock list**, drained into `record+0xac` by vtable slot 0 of both record classes and applied on mission *completion* (`MERC-SHELF-002`). `[Mission<n>] Payment` (`record+0x0c`, `REG-SCN-059`'s destination) is returned by vtable **slot 1** of both classes — `FUN_0048a870` and `FUN_00487140`, each a bare `MOV EAX,[ECX+0xc] ; RET` — and `FUN_0048a200` picks the finished mission's record and hands the value to `FUN_0041dacf` at `00473f01`, on the mission-end arm, immediately before the `EnableMercenary` step. **Corpus, all three roots identical** (`EXP-0062/evidence/types.csv`, `offers.csv`, `checks.txt`): `MercenaryCount` = `[1 1 1 1 1 4 4 3 3 3 0 3 4 3 0]`; all **107** `Mercenaries` elements and all **13** `EnableMercenary` elements lie in **1..15**; and `MercenaryCount[t] > 0` **iff** `[npc<t>]` carries both `PriceA` and `PriceB` — **15/15**, the two zero slots (types 11 and 15) being exactly the two `npc` sections without them, offered by no mission and unlocked by none | High (the index space is three named subscript computations plus the reset's copy loop, and the `MercenaryCount`↔`PriceA`/`PriceB` coincidence is a discriminator, not corpus agreement: a per-mission array has no reason to agree slot-for-slot with a different registry's presence set) / **Medium** (`Payment` as the *reward*: the getter, the picker and the call site are named instructions, but `FUN_0041dacf` was read only as far as its call — nothing was checked about sign or currency) | ● active | **[EXP-0062](../experiments/EXP-0062-mercenaries/)** |
| REG-STR-080 | **(rom.exe)** The in-memory `structures.reg` class record is **`0xa4` bytes** (`PUSH 0xa4` at `0046e974`) and `FUN_0046e8a0` fills it in this order: `+0x04`/`+0x08` the two `CSprite256` sheets (lazy, `REG-STR-081`), `+0x0c ID`, `+0x10 TileWidth`, `+0x14 TileHeight`, `+0x18 FullHeight`, `+0x1c Phases`, `+0x20..+0x2c SelectionX1/Y1/X2/Y2`, `+0x30 ShadowY`, `+0x34` the `AnimMask` string, `+0x38` its live-cell count, `+0x3c` the expanded animation timeline's length, `+0x40` the `CArray` holding it, `+0x54 Picture[0x10]`, `+0x64 Indestructible`, `+0x68 DescText[0x20]`, `+0x88 VariableSize`, `+0x8c Usable`, `+0x90 Flat`, `+0x94 LightRadius`, `+0x98 LightPulse`, `+0x9c` the `CString` `"graphics\structures\" + File`, `+0xa0` the sheets-loaded flag. Defaults are `-1` for `ID..SelectionY2` and `0` for `ShadowY` and the five flags. Two structural facts a key list cannot show: **there is no `Parent` key in this loader at all**, so unlike `objects.reg`/`units.reg` (`REG-KEY-045`) a structure class **inherits nothing**; and the array is `SetAtGrow(ID)`-keyed (`0046ee5f`), which `REG-KEY-044` already states. `AnimTime`, `AnimFrame` and `AnimMask` are read **only when `Phases > 1`** (`0046ebb1 CMP EAX,1 / JLE`); the timeline is the same run-length expansion `REG-OBJ-046` publishes for objects (`AnimFrame[i]` appended `AnimTime[i]` times) | High (every field is the `MOV [EBP+X],EAX` following its own key `PUSH`, listed in `evidence/listings.md` §1; the absent `Parent` is an absence over a routine read end to end, not a sweep) | ● active | [EXP-0092](../experiments/EXP-0092-structure-art/) |
| REG-STR-081 | **(rom.exe)** `structures.reg`'s `File` is a **path string**, not an index into a `Files[]` table as in `units.reg`/`objects.reg` (`REG-VAL-029`, `REG-ROSTER-052`) — `FUN_004cc670` reads it into a `0x100` local at `0046e969` and passes it to the class constructor, which stores `"graphics\structures\" + File` at `+0x9c`. The sheets are opened **lazily, on first draw**: every draw arm tests `class+0xa0` and calls `FUN_0046e720`, which constructs `<path>.256` into `+0x04` and `<path>b.256` into `+0x08` and sets the flag. Corpus: all 66 classes resolve, under the whole-path lower-casing `RES-CASE-036` established (12 of the 66 spell `File` with a capital that no archive node carries) | High (two `PUSH`es of the suffix literals in one routine, the flag's single writer in the same routine, and 66/66 resolving in `graphics.res`) | ● active | [EXP-0092](../experiments/EXP-0092-structure-art/) |
| REG-STR-082 | **(rom.exe)** `AnimMask` is a **picture of the sheet's frame grid, not of the footprint**: the loader allocates exactly `FullHeight * TileWidth + 1` bytes for it (`0046ec77`…`0046ec86`) and counts the bytes that are **not `'-'`** into `class+0x38` (`0046ecc9`/`0046eccf`). The draw reads `AnimMask[k·TileWidth + c]` for image cell `(k,c)`: `'-'` means *this cell never animates, draw its base frame*; anything else means *this cell has one frame per animation phase*, at the rank it holds among the live cells (`SPR256-STR-041`). Corpus: 14 of the 66 classes spell a non-empty `AnimMask` and its length is `TileWidth × FullHeight` on **14/14** — the rival `TileWidth × TileHeight` differs on 6 of those 14 and fails all 6. The alphabet over all 14 masks is exactly two bytes, `'+'` (78 occurrences) and `'-'` (56). Ten further classes spell `Phases > 1` and then spell neither `AnimMask` nor `AnimTime`/`AnimFrame`, so `Phases > 1` alone does **not** mean animated: with an empty timeline the phase frame is 0 and the null mask pointer is never dereferenced | High (the buffer size is one `IMUL`/`INC` pair, the live count one `CMP 0x2d`, and the corpus discriminates the only live rival 14/14 vs 8/14) | ● active | [EXP-0092](../experiments/EXP-0092-structure-art/) |
| REG-PICT-083 | **The two registry fields that name a picture, joined to the shipped corpus in both directions — and the join fails on both roots, differently.** `units.reg InfoPicture` (`class+0xd8`, `REG-UNITS-049`) and `structures.reg Picture` (`class+0x54`, `0046eabb`/`0046eabe`, `0x10` bytes each) are the only two, and the engine's leaf is `<name>.bmp` or `<name><tier>.bmp` with the digit dropped at tier 1 (`UNIT-PICT-036`). Joining every section of both registries against `graphics\infowindow` over tiers 1..4: **EN — 34 unit classes of which 33 carry `InfoPicture`; 66 structure classes, all 66 carrying `Picture`; 56 nodes named by `units.reg` and 27 by `structures.reg`; `horse.bmp` a hardcoded literal (`HERO-DOLL-078`); `root.bmp` and `teleport.bmp` named by nothing — 56+27+1+2 = 86, the whole tree.** Thirteen unit classes name a value with no node at any tier (`SwordsMan SwordsManSh 2HSwordsMan AxeMan AxeManSh 2HAxeMan ClubMan ClubManSh PikeMan PikemanSh Archer XBowMan`, twelve distinct names over thirteen sections), and they are exactly the classes `UNIT-PICT-035` shows can never reach the formatter. **RU — the unit half is identical cell for cell; the structure half is not: thirteen of the 66 sections name `magic`, `ruins` or `sphinx`, and `GRAPHICS.RES` ships no such node.** So the RU root ships thirteen dangling picture references, and both roots ship nodes no class names. The discriminating cross-check: over the sixteen classes whose id is `>= 0x1a` — the ones the portrait arm can reach — **`units.reg Palette` equals the number of shipped picture files for that class's name, 16 of 16**, at both of its values (`4` on thirteen classes, `1` on `Catapult1`, `Catap2` and `daemon`), which is `PAL-LIMIT-009`'s four-tier model predicting a file count in a tree it says nothing about | High for the counts and the two set comparisons (exact archive and registry measurements on both roots, joined through the engine's own formatter rather than by pattern) / **Medium** for `Palette == files`, which is sixteen agreements over two values — corpus agreement, and it is carried as corroboration rather than as the reason the tier is the tier | ● active | [EXP-0138](../experiments/EXP-0138-actor-pictures/) |
| REG-PROJ-086 | **(rom.exe) `projectiles.reg`, field by field from its loader — and the array is keyed by `ID`, so `ID` is an address and not a label.** `FUN_0046e0f0` reads `[Global] Count` (31), then for each `i` in `0..Count-1` builds section `Projectile%d`, takes `File` as a string and `A16` as the constructor's second argument, allocates `0x3c` bytes through `FUN_0046dd20` (which stores `A16` at `+0x34` and zeroes `+0x38`, the not-yet-loaded flag), and fills `+0x14 = ID` (default -1), `+0x10 = Phases` (-1), `+0x18 = RotationPhases` (**0x10**), `+0x1c = Width` (**0x40**), `+0x20 = Height` (**0x40**), `+0x28 = Palette` (0), `+0x2c = Homing` (0), `+0x30 = Flip` (0), `+0x24 = SFX` (0) — every default is its own `PUSH` and is the live value wherever the key is absent, which on the shipped file is 8 rows for `Width`, `Height`, `RotationPhases` and `SFX`, 7 for `A16`, 3 for `Palette`, 19 for `Homing` and 29 for `Flip`. It then grows the array to `ID + 1` when needed (`FUN_004707d0(ID + 1, -1)`) and stores **`DAT_005eb644[ID] = record`** — so the shipped `ID` domain 1..62 over 31 rows makes a 63-slot array with **32 permanently null slots**, and every consumer indexes by `ID`: `FUN_00461fc0`, `FUN_004617b0`, `FUN_0045d680`, `FUN_004104e8` and `FUN_0040cd70`, with `FUN_004707d0` the grow and `FUN_0046f1e0` the release (**`FUN_0046e4e0`, which this row named as the grow, is the teardown** -- it calls `vt+4` on every record, frees the array and releases both shade tables and both smoke sprites; corrected by [EXP-0140], which also settles what `Palette` selects, `REG-PROJ-087`) (`EnumRefs refto:5eb644` — 19 hits / 9 owners / 0 orphan). Outside the loop and outside the registry the same routine loads `projectiles.pal` into `DAT_005efa24`, `projectile_.pal` into `DAT_005efa28`, and `graphics\projectiles\smoke%d\sprites.16a` for 0 and 1 into `DAT_005eb688`. Corpus, both roots, parsed identically: `Count = 31` matches 31 blocks and 31 distinct `ID`s; two blocks name the same `File` (`catap2\sprites`, `ID` 6 and 7); the tree holds 32 `projectiles/*` directories and the two the registry never names are `smoke0` and `smoke1`. The **other** consumer of the same id space is `units.reg`: 9 of the 34 classes carry a `Projectile` key, values 1 2 3 4 5 6 7 10 12 — and 10 and 12 are `MAGIC-PIC-026`'s `fire_arrow` and `fire_ball` pictures, so two unit classes shoot a spell's art. **G2:** `Count` and `ID` are both **engine** limits and both already elastic — the array grows to `ID + 1` with no clamp — but the id a *cast* can reach is fixed at `2*spellId + 8` or `+9` (`MAGIC-PIC-026`), so slots above 65 are reachable only through a `units.reg` `Projectile`; adding a row changes `projectiles.reg`'s bytes and nothing else | High (each field is its own key string and `PUSH`ed default in a decompiled loader whose every store target is a distinct displacement, and the by-`ID` indexing is confirmed independently by two consumers reading `DAT_005eb644` with the object's `+0x20`) / Medium (the per-key present-or-absent counts, which are a corpus census) | ● active (amended) | [EXP-0139](../experiments/EXP-0139-spell-pictures/) |
| REG-PROJ-087 | **(rom.exe + corpus) `Palette` in `projectiles.reg` is a BOOLEAN -- "this sheet carries its own colour table" -- not an index into anything, and it agrees with the sheet's own trailer bit 31 on 31 of 31 rows.** `REG-PROJ-086` records the field at `+0x28` with default 0 and three shipped rows defaulted, and records `projectiles.pal`/`projectile_.pal` being loaded outside the loop; what the field *selects* was open. `FUN_0046dd90`, the lazy loader, answers it: `0046dfe3 MOV EAX,[EBX + 0x28]` / `TEST EAX,EAX` / `JZ 0046e018` -- **when `Palette` is zero the routine builds no lookup at all**; when it is non-zero it calls `FUN_00428ad0(this, 0x10, mode, tint)` on the sprite it has just built, with `(4, 0)` on the `.16a` arm and `(2, 1)` on the `.256` arm, and **again** on the `b` sibling at `+0x08`. The draw reads the same field and picks the blit slot from it (`ANIM-CAST-027`). The corpus half is a two-sided check rather than a census: `tools/castart -mode arms` walks each sheet reading the trailer's bit 31 instead of assuming an offset, and **`Palette != 0` <-> embedded palette present holds 31/31** on both preserved roots and the live install. The three rows that default are exactly the three sheets shipped with bit 31 clear -- `archer\arrow`, `xbowman\arrow`, `orc\arrow` -- whose frames therefore begin at offset **0**, not 1024. Two consequences a decoder needs: the walker in `tools/spellart` starts every `.16a` at 1024 unconditionally and would mis-frame any sheet in that state; and because the `b` sibling is drawn only on the `Palette != 0` arm, `archer\arrowb.256`, `xbowman\arrowb.256` and `orc\arrowb.256` are **loaded and never drawn**. The companion field `A16` at `+0x34` selects more than the extension: `FUN_0046dd90` builds a `.256` sprite (vtable `0x00597418`) for 0 and a `.16a` sprite (`0x005974f8`) for non-0, and those two vtables differ in exactly **two** slots -- the deleting destructor and `+0x18` -- so the class chooses the blit and, through the branch above, the shade mode too. **G2:** `Palette` is a registry value and free, but it is not independent -- setting it to 1 on a sheet with no embedded table builds a lookup from 1 024 bytes of frame data, and clearing it on a sheet that has one silently redirects that sheet to the shared `projectiles.pal`. Adding art that needs its own colours means shipping a palette-bearing sheet, which changes that file's bytes | High (the gate and both build calls read as raw instructions with their `PUSH`ed arguments; the 31/31 agreement is not corpus-only because each side of it is read from a different place -- one from the registry the loader parses, one from the four bytes the constructor tests) | ● active | [EXP-0140](../experiments/EXP-0140-cast-art-drawn/) |
| REG-NPC-088 | **(rom.exe + corpus) `Face` and `Picture` index no picture resource: they are the `typeID` and the `face` of a dialogue actor the engine synthesises for an npc, and each store is gated on the matching token of that section's own `Flags` list.** `FUN_00421f46(npcId)` allocates `0x1b0` bytes, constructs at `0045ae30`, folds the section's `Flags` tokens into `object+0x18c` (`Hero`→bit 0, `Mage`→bit 1, `Female`→bit 2, `Human`→bit 4, and `MySex`/`MyClass`/`!MySex`/`!MyClass` copying bits 1 and 2 from the player's own drawable at `[this+0x3f54]+0x18c`), then takes the record `((void**)DAT_005f075c)[npcId]` and performs exactly two field copies: **`0042227b MOV EAX,[EDX + 0x10]` / `0042227e MOV [ECX + 0x20],EAX`** under the `Flags` token `Picture` (`0x5b8a5c`, tested by `FUN_0048c3d0`), and **`0042235b MOV EAX,[EDX + 0xc]` / `00422367 MOV [ECX + 0x24],EDX`** when the token `Start` is *absent*. `object+0x20` is a drawable's typeID (`UNIT-APPEAR-030`, `ALM-CLS-054`) and `object+0x24` its face byte (`PAL-FACE-005`) — so the portrait that follows is `UNIT-PICT-036`'s existing formatter with no new mechanism: `FUN_00421b46` reads the same two fields back (`00421ce3`, `00421cea`), subscripts the `units.reg` class-by-`ID` array `[0x005eb674 + 4*typeID]` (`REG-KEY-044`), takes `class+0xd8` = `InfoPicture` (`REG-UNITS-049`) and formats `graphics\infowindow\%s%d`, dropping the digit at `face == 1`. With `Start` present the face comes instead from one of the four archetype sections, chosen by a jump table at `0x0042237d` on `object+0x18c & 6` — `0`→`MaleFighter`, `2`→`MaleMage`, `4`→`FemaleFighter`, `6`→`FemaleMage` — each read as `Face` from `scenario\npc.reg` reopened at `004222b1`; the loader caches the same four at `0x5f0770`/`74`/`78`/`7c`. **The `Flags` list is the record's schema:** over 105 sections on both roots the token and the key are biconditional — `Picture` token ⟺ `Picture` key (33/33 and 72/72), `Face` token ⟺ `Face` key (86/86 and 19/19) — and the domain splits along `FUN_00421b46`'s own branch `00421ccc AND ECX,0x11` (`Hero` or `Human`): where `Picture` is present `Face` is 1..4, a tier, and the section carries `!Human`; where absent, `Face` is 1..30 and the section carries `Human` or `Hero` and takes the composed-doll arm (`UNIT-PICT-035`) instead. Corpus join, both roots identically: all **33/33** sections carrying `Picture` resolve as a `units.reg` `ID`, that class carries `InfoPicture`, and `graphics.res` ships the exact leaf the formatter builds — and the sections fall in (class, tier) order, `npc100..103` Goblin 1..4, `npc104..107` Orc 1..4, through `npc132` daemon 1 (`evidence/picture-join-*.csv`). **G2:** both fields are registry values and free to change, but neither is independent — `Picture` must name a live `units.reg` `ID` whose class carries an `InfoPicture`, and a `Face` above the tier count for that name resolves to a node that does not ship. Adding a portrait means adding a `graphics.res` node, which changes that file's bytes; re-pointing an existing npc changes only `npc.reg` | High (both stores are named instructions with their displacements, the `Flags` gates are the `PUSH`ed key strings of the same routine, and the destination fields are identified by two prior rows that read them from unrelated paths) / **Medium** for the 33/33 corpus join, which is corroboration and not the reason for the grade | ● active | [EXP-0141](../experiments/EXP-0141-npc-fields-and-tag/) |
| REG-NPC-089 | **(rom.exe + corpus) `PortraitX1/Y1/X2/Y2` is a Win32 `RECT` — and only its `left` and `top` are ever read.** The loader `FUN_0048c510` reads the four keys in the order `PortraitX1`, `PortraitY1`, `PortraitX2`, `PortraitY2`, each with default **-1**, into four consecutive stack dwords, and the record constructor `FUN_0048c2e0` copies them whole with **`0048c343 LEA EDX,[ESI + 0x20]` / `0048c346 PUSH ECX` / `0048c347 PUSH EDX` / `0048c34b CALL dword ptr [0x00632f8c]`** — the import `CopyRect` from `USER32.dll`, whose parameters are `LPRECT` and `const RECT*`. So the mapping is fixed by a type and not by the key names: `record+0x20` = `left` = `PortraitX1`, `+0x24` = `top` = `PortraitY1`, `+0x28` = `right` = `PortraitX2`, `+0x2c` = `bottom` = `PortraitY2`. **The rectangle's only reader is `FUN_00421b46`**, which loads all four (`00421da7`, `00421dac`, `00421db2`, `00421db8` into `[EBP-0x30]`, `-0x2c`, `-0x28`, `-0x24`) and then uses `left` four times and `top` five, while `right` and `bottom` are **never read again** — a complete enumeration of four adjacent locals inside one function body. What `left` and `top` do: `00421dce CMP [EBP-0x2c],-0x1` tests `top` against the loader's own key-absent default and picks between two six-argument draw calls, `(8, 7, 0x24, 0x8c, 0x6c, 0xe8)` when the key is absent and `(8, 7, X1, 0x90-Y1, X1+0x48, 0xf0-Y1)` when it is present, made on the global `[0x5ef6bc]` through vtable slot `+0x34` and on the caller's own canvas through `+0x38`. Both slots (`FUN_004299b0`, `FUN_004299f0`) append `this->buf+8`, `buf[0]` and `buf[1]` and call `FUN_0044d2a0` / `FUN_0044d4a0`, which clip **argument 1** against `[0x5e4408]` and push the shortfall into **argument 3** — so args 1–2 are a destination point and 3–6 a **source rectangle**. The canvas is `FUN_00429520(0xa0, 0xf0)`, `w*h*2 + 8` bytes with `w` at `[0]` and `h` at `[4]`: **160 x 240, 16-bit**. The window is therefore a fixed **72 x 96** whose blit-space `top` is `0x90 - Y1`, against a fixed **72 x 92** default at `(36,140)`. **Those are canvas rows, not picture rows** — `REG-NPC-091` shows the canvas is the 160 x 240 `infowindow` BMP kept in its own bottom-up order, so `PortraitY1` is an ordinary offset from the **top** of the picture and the window's visual top is `Y1 - 1`. Corpus, both roots identically: `PortraitX1` and `PortraitY1` are present on **48** sections, `PortraitX2` and `PortraitY2` on **10** — `npc31..35`, `npc41..44`, `npc54` — and the derived source rectangle lies inside the 160 x 240 canvas on **48/48**, out of a 32-bit domain (`evidence/portrait-geometry-*.csv`). **G2:** `X1`/`Y1` are free within `0 <= X1 <= 88` and `0 <= Y1 <= 144`; the 72 x 96 window size, the 160 x 240 canvas and the `(8,7)` destination are all engine constants and moving any of them is a code change. `X2`/`Y2` are inert — editing them changes nothing this image does | High (the `RECT` identification is an import's parameter type, the field order is the loader's own read order into consecutive slots, the dead pair is an exhaustive read of four locals in the single reader, and the source/destination split is the clip arithmetic of the leaf) / **Medium** that `FUN_00421b46` is the *only* reader: instrument `EnumRefs refto:5f075c` (20 hits / 7 owners / 0 orphan) plus `imm:` and `refto:5f0758` on the array object (10 / 7 / 1 orphan, disassembled and read); **blind spot** — a wholesale `memcpy` of a record carries no displacement, and a route computing the array address rather than naming it is invisible to both modes / **Unknown** whether `X2`/`Y2` were ever live in a shipped build: absence of a reader in this image is established, intent is not | ● active | [EXP-0141](../experiments/EXP-0141-npc-fields-and-tag/) |
| REG-NPC-090 | **(rom.exe + corpus) `npc21`..`npc24` are a four-slot block the image knows by literal number, and `DataBinID` gains its first located reader.** Three independent sites: `FUN_004c5886` gates one of its two npc calls on **`004c5b1d CMP dword ptr [EBP + -0x24],0x15` / `JL`** and **`004c5b27 CMP dword ptr [EBP + -0x24],0x18` / `JG`**; `FUN_0041fe3f` carries the identical pair at `0041fe98`/`0041fea2` on its own argument; and `FUN_0048c980` reaches the same four as **`0048c9d8 MOV EDI,dword ptr [ECX + EAX*0x4 + 0x54]`** with `ECX = [0x5f075c]`, i.e. `array[id + 21]`. The corpus says the same thing from the other side: the four sections carrying the `Start` token — the only ones whose face is taken from an archetype rather than from their own `Face` key (`REG-NPC-088`) — are exactly `npc21` `Hero,Me,Start`, `npc22` `Hero,Mage,!MySex,Start`, `npc23` `Hero,!Mage,!MySex,Start`, `npc24` `Hero,!MyClass,MySex,Start`, on both roots, and all four carry `DataBinID` 26 and no `Face`, `Picture` or `Portrait*` at all. `FUN_0048c980` also reads `record+0xc` (`0048cb87`) under the `Flags` token `Face`, and compares the four cached archetype values as a table — **`0048cbb9 MOV EAX,dword ptr [EDX*0x4 + 0x5f0770]`**, the only read of that table. **`DataBinID` (`record+0x14`)**: its one located reader is `FUN_0041fe3f` at **`0042004f MOV EAX,dword ptr [EDX + 0x14]`** / **`00420052 MOV dword ptr [ECX + 0xa],EAX`**, writing it into a message body whose byte tag at `+0x9` is set to `0x49` two instructions later — consistent with `REG-NPC-058`'s reading of the field as a `Templates.ini` placeable id, and the first instruction in the image that consumes it. **G2:** the `21..24` range is an engine constant in three places; an npc outside it cannot take the archetype-face path or whatever the gated call at `004c5b3e` provides, and moving the block is a code change, not a data change | High for the three gate sites, the `+0x54` subscript and the `DataBinID` store (named instructions with their immediates and displacements) and for the corpus census of `Start` (exhaustive over 105 sections on both roots) / **Medium** that `FUN_0041fe3f` is `DataBinID`'s only reader — same instrument and same blind spot as `REG-NPC-089` / **Unknown** what the message tagged `0x49` is: the tag byte and the field's landing offset are read, the message's own grammar is not | ● active | [EXP-0141](../experiments/EXP-0141-npc-fields-and-tag/) |
| REG-NPC-091 | **(corpus + render) The canvas the `Portrait*` window is cut from is the `infowindow` bitmap itself, one row per row — and the window frames the creature's head only if the canvas keeps the BMP's own bottom-up row order.** Every one of the **86** `graphics.res::infowindow/*` nodes is a `BM` file declaring **160 x 240, 24 bpp, compression 0** with a positive height, i.e. a bottom-up DIB — the exact shape of the canvas `FUN_00429520(0xa0, 0xf0)` allocates (`REG-NPC-089`), on both roots. So the blit's source rectangle `(X1, 0x90-Y1)..(X1+0x48, 0xf0-Y1)` names a region of that bitmap, and exactly two readings survive the instructions: **BOTTOM-UP** — the canvas holds the file's rows unflipped, canvas row `r` is visual row `239-r`, and the window's visual top is `Y1-1`; or **TOP-DOWN** — the canvas is flipped at load and the visual top is `0x90-Y1` straight off. The two coincide only where `Y1 = 72` and are up to 122 rows apart elsewhere. `tools/npcportrait` renders both for all **33** sections that carry `Picture` and `PortraitX1`/`PortraitY1`, and under **BOTTOM-UP** the 72 x 96 window frames the head and shoulders on **33/33**, while under TOP-DOWN it lands on legs, wings or empty background — for `npc132` (`daemon`, `Y1 = 11`) the TOP-DOWN window runs to visual row 229 and holds almost no figure at all. Both PNGs are byte-identical between the two preserved roots. **So `PortraitY1` is an ordinary top-down offset from the top of the picture**, and the engine's `0x90 - Y1` / `0xf0 - Y1` is precisely the bottom-up conversion of a 96-tall window (`240 - (Y1 + 96)` and `240 - Y1`) — which also fixes the **default** rectangle `(0x24, 0x8c, 0x6c, 0xe8)` as visual rows **8..100**, the top of the frame, not the bottom. **G2:** an author retargeting a portrait sets `PortraitX1`/`PortraitY1` in picture coordinates with the top-left corner as origin, bounded by `0 <= X1 <= 88` and `0 <= Y1 <= 144`; nothing else in the path is a data value | High for the bitmap census (an exhaustive header read of all 86 nodes on both roots) and for the two readings being the only survivors (the blit's rect is fixed by `REG-NPC-089` and a DIB has exactly two row orders) / **Medium** for BOTTOM-UP being the live one: 33/33 is a corpus agreement judged by the eye, and the instruction that would settle it — whether the loader that fills the canvas from the BMP walks the rows forwards or backwards — was not read. **The falsifier is cheap and named:** read that loader, or observe one dialogue portrait of `npc132` on a running original — under BOTTOM-UP it shows a horned head, under TOP-DOWN a nearly empty pane | ● active | [EXP-0141](../experiments/EXP-0141-npc-fields-and-tag/) |
| REG-DESC-096 | **`units.reg`'s `DescText` is identical on both roots, is pure ASCII, and its shipped maximum is exactly the field it is read into — so it is a limit as well as dead data.** Measured over both installs (`evidence/desctext-roots.txt`): **34 of 34** classes carry the key, the value is byte-identical `en` to `ru` on **34 of 34**, and **no value carries a byte >= 0x80** on either root. The longest value plus its NUL is **31 bytes**, against the `0x20` the loader reads into at `class+0x10c` (`REG-UNITS-049`, `0x10c + 0x20 = 0x12c` = the allocation) — one byte of headroom on the longest shipped class. Two models fit those bytes and the corpus alone cannot separate them: *display text the Russian translator never touched*, or *editor metadata nothing draws*. Two independent things separate them and the second survives: nothing in the image reads the field, and the name a class is actually **shown** by is a line of `main\text\unitname.txt` whose text *disagrees* with `DescText` on several classes — `Death Star` against `Daemon`, `Ghost` against `Spirit` (`UNIT-NAME-039`, `UNIT-NAMETAB-041`). **The G2 limit, stated:** a `DescText` may be at most 31 characters; the class record is a fixed `0x12c` allocation with the string inline at the end, so a longer value writes past the object rather than being truncated by a `CString`, and lifting it changes the allocation size and every offset after it — a **code** change, not a file change | **High** for the corpus (both roots, all 34 classes, whole values) and for the 31-vs-`0x20` arithmetic (the loader's own `PUSH 0x12c` and its `0x20`-byte read) / **Medium** for "a longer value overruns rather than truncates": the loader's string-read helper was not read this round, only its byte count, so a clamp inside that helper would change the failure mode without changing the limit | ● active | [EXP-0143](../experiments/EXP-0143-actor-names/) |
| REG-SCN-097 | **`AddTextDocument` and `AddPictureDocument` are one append onto one campaign-lifetime collection, and the two readers differ in three bytes.** Corpus, both roots identical, over the whole registry: `[Mission10] AddTextDocument` kind 6 = `{1,2,3}`, `[Mission50] AddTextDocument` kind 2 = `{4}`, `[Mission60] AddPictureDocum` kind 2 = `{1}` — **4 text elements over 2 sections and 1 picture element over 1 section, of 24 `[Mission<n>]` sections**; `[General]` is exactly `{MercenaryCount, ScenarioMissionCount, TotalMissions}`. `FUN_004879f0` reads each key as a `u16` array and calls one routine per element. `FUN_00488cf0` and `FUN_00488fb0` are the **same** growable-array append over a **60-byte** element on the same four fields of the same object — `+0x148` data, `+0x14c` size, `+0x150` capacity, `+0x154` grow-by, growth `min(max(size/8,4),1024)` at `00488ea1`…`00488ec0` — and neither clears the array. They differ only in the reject polarity (`00488d2d` `JNZ` on a non-zero `+0x04`, `00488fed` `JZ` on a zero one), the kind literal constructed with (`00488d44` `PUSH 0x1`, `00489004` `PUSH EDI`=0) and the exception frame. Element `+0x00` is therefore the registry value and `+0x04` the kind — **1 text, 0 picture** — with the append deduplicated on the pair. The owner is the campaign record built by `FUN_00487190` (six such arrays; this one's vptr at `+0x144`), and `FUN_00488460`, the loader's only caller, runs it only for a mission strictly higher than the current one and **returns 0 for a lower one**, so the collection only grows | High for the corpus (exhaustive, both roots, every value resolving to a shipped resource 5/5) / High for the append and the kind assignment (both routines read whole at instruction level; the kind is discriminated by each routine's reject polarity agreeing with its own literal, so a swapped reading contradicts itself) / **Medium** for "only grows": the monotonic guard is one compare read in a decompiled caller and the campaign's own mission sequence was not driven | ● active | [EXP-0151](../experiments/EXP-0151-mission-documents/) |
| REG-SCN-098 | **`[Mission30] AddHero = 22` is consumed on the first town activation after mission 30 becomes current.** Town activation calls `FUN_0048a6e0`, which walks `AddHero[]`, calls `FUN_0041fe3f` per element and clears the array. The special `npc21..24` branch reads `npc22` tokens `Mage,!MySex`: an existing female character selects Humans row 28 `PC_Fergard`, and an existing male selects row 29 `PC_Reniesta`. The stored `DataBinID = 26` is replaced by `26 + selector`; the name comes from localized `text/npcnames.txt` at `20 + selector`. Mission 20 makes mission 30 current before town activation, so the companion exists before mission 30 starts and cannot be added twice from the cleared array. | High (the caller, array walk, clear, selector arithmetic, two table lookups and shipped registry values are direct evidence; completion-time and direct-DataBinID alternatives are excluded by the instruction path) | ● active | [EXP-0153](../experiments/EXP-0153-addhero-consumer/) |
