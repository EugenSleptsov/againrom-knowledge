# Claim registry — SHOP (the shop: stock, price, trade)

Level 2 ledger. Index: [registry.md](registry.md) · spec: [`formats/shop/format.md`](../formats/shop/format.md). Legend: `✔ promoted` (also in the spec) · `● active` · `✖ retracted`. IDs are permanent.

Not a file format — the **simulation area** that turns a placed structure into a place you can
trade at. It sits on `ALM-CLS-037` (which type-4 records take the structure arm), on
`DAT-OBJ-002`/`DAT-SCHEMA-004` (the `Data.bin` item collections the stock is drawn from) and
on `REG-SCN-059` (the campaign's per-mission keys). Opened by
[EXP-0057](../experiments/EXP-0057-shop/), which read the class, the generator and both commit
paths at instruction level; the listings are `EXP-0057/evidence/rom-shop-excerpt.md` and the
placement census is `EXP-0057/evidence/shop-placements.csv`. Continued by
[EXP-0058](../experiments/EXP-0058-shop-lifecycle/), which read the **lifecycle** — when the stock is
built, cleared and restocked, whether it is saved, where the seed comes from, and which shop a
session can reach — and corrected two of EXP-0057's readings in the process. Closed on the campaign
side by [EXP-0059](../experiments/EXP-0059-shop-mission/), which traced the **town shop's only
per-mission input** from `scenario.reg` to the generator's `max` and measured what that ceiling
admits. The **trade half** — the shop screen, the table, the two commit buttons, the move command and
what a duplicate is — is [EXP-0061](../experiments/EXP-0061-shop-trade/); its listings are
`EXP-0061/evidence/rom-shop-trade-excerpt.md` and its measured screen geometry is
`EXP-0061/evidence/shop-screen-grid.json`.

> **Owner testimony appears in this ledger only as an `Origin:` note, never in a Confidence cell.**
> The owner's recollection of playing his own install is a third evidence class: it may raise a
> question, aim a search and bound a claim, and it may never enter or raise a grade. It is recorded
> verbatim in `EXP-0058/evidence/owner-testimony.md`, and the rule is normative in
> `METHODOLOGY.md` → *A third evidence class*. Where the image contradicts it, the image wins.

| ID | Claim | Confidence | Status | Evidence |
|----|-------|-----------|--------|----------|
| SHOP-ANIMATION-081 | **Shop-panel progression is inside paint, gated by unsigned elapsed time >=100 ms, not one advance per paint or elapsed-time catch-up.** `004ab37e`, merchant vtable `0059ad10+2c`, first requires `view+148 != 0`. Its eligible block calls `004a976f` for sound, replaces global stamp `005f1b14` with a new `timeGetTime` result, advances enabled racks, then at most one merchant mode. The first stamp initialization is `now-100`. Idle eligibility separately compares `now-005f1b10` with a newly computed `5000+1000*(rand()%5)` on each eligible pass, even while a merchant mode prevents arming; completion refreshes that second stamp. Both stamps are static globals, not per-rack counters. This establishes local paint order and candidate thresholds, not guaranteed visible 10 fps or a sampled-once uniform wait. | High for this function's branch/order/arithmetic; outer dispatch, wall-clock presentation and original-runtime witness Unknown. | ✔ promoted | [EXP-0301](../experiments/EXP-0301-shop-interior-animation/) |
| SHOP-ANIMATION-082 | **Each selected rack loads eleven files but its normal loop draws indices3..8, files4..9.** Selector `004abcc3(i)` loads folder `4-i` through `004aac94`, enables bit `1<<i`, and resets its index at `panel+244+4*i` to0. A different previous rack is assigned index9. `004abe0f` increments once, maps9 to3, and at10 clears that rack bit and frees its files before drawing. Thus file10 is only a possible inter-step closing draw; file11 is not drawn by this normal progression. Same-shelf selection returns without work. Entry `004a8bc3` selects0, folder04. Input `004abf0c` selects another hit rack and additionally arms Yes. Four folders, each eleven BMPs, exist on both roots with equal per-file hashes; no claim that every loaded file visibly animates follows from that inventory. | High for local selector/advance/draw relation; Medium for resource census; visible first/closing frames depend on paint timing and are Unknown. | ✔ promoted | [EXP-0301](../experiments/EXP-0301-shop-interior-animation/) |
| SHOP-ANIMATION-083 | **The merchant has a static base and three priority-ordered draw modes, not one loop over all files.** Base `Pose2-3/1.bmp` is `panel+128`. Idle bit10 uses the replaceable bitmap at+12c; `004ab005` increments shared index+254 modulo30 and loads file `index+1`. From index0, uninterrupted idle draws files2..28; step28 loads29 then clears idle and resets the index before drawing. Yes bit20 and No bit40 use twelve-slot arrays at+1e0/+210: slot0 is the base pose, slots1..11 load Yes/No files2..12. Paint increments before drawing and at index12 clears that mode, resets0 and releases its array. The archive contains29 Pose,13 Yes and12 No files per root; Yes/1, Yes/13 and No/1 are not loaded by these reaction loaders. A slot0 draw can be skipped when the first paint is already eligible. | High for normal index0 code paths and priority; Medium for two-root resource counts; interrupted/nonzero-index sequences and actual presentation are not generalized from the normal trace. | ✔ promoted | [EXP-0301](../experiments/EXP-0301-shop-interior-animation/) |
| SHOP-ANIMATION-084 | **Reaction requests add mode bits without restarting the shared merchant index.** Sell-with-items and affordable-buy helpers load Yes and OR20; unaffordable buy loads No and OR40; rack-click selection OR20. These trigger bodies neither clear other merchant bits nor reset+254. Paint both advances and draws with priority idle10, Yes20, No40, so overlapping bits can defer a reaction. Constructors `004a9ccf`/`004aa37a` zero+254; ordinary mode completion also zeros it. Exit `004a89a6` disables view+148, clears flags and calls static/animation cleanup; entry reloads resources and resets rack selection, but neither inspected entry/exit nor cleanup resets+254 or the static clock stamps. This is not proof that a reused live panel retains a particular index: inherited transitions, object reuse and interruption outcomes were not witnessed. | High for the named trigger/cleanup bodies and priority; Medium for lifecycle inference; runtime re-entry, malformed retained-index behavior and visible interruption remain Unknown. | ✔ promoted | [EXP-0301](../experiments/EXP-0301-shop-interior-animation/) |
| SHOP-ANIMATION-085 | **Shop sound requests are conditional and idle cues inspect the old index before the eligible paint advances it.** `004a976f` requests view slot+b4 at idle index1, +94 at10/14/18/22, and+b8 at24. With no merchant mode it can request+b0 after a separately redrawn `30000+004ae540(30000)` threshold, `004ae650(view+70+20b0)==0`, nonnull sample and guard result0. Shelf switch requests+a0; sell+a8; affordable buy+a4; refusal+90; entry requests+ac and conditionally+b0. The common helper `004ae5e0` calls `00453b08` only for a nonnull slot whose `00453a3d` guard returns0, passing priority128, not sound id128 (`VIDEO-SFX-013`). Request sites and slot offsets are proved; sample names, the unexpanded table-query meaning, guard/backend interpretation beyond the accepted terminal claim, overlap/audibility, and the unexpanded entry helper at+bc remain Unknown. | High for bounded request conditions, offsets and pre-advance order; no audible event or complete shop sound census claimed. | ✔ promoted | [EXP-0301](../experiments/EXP-0301-shop-interior-animation/) |
| SHOP-CONSUME-073 | **Shelf spell subtypes and price/power identities are explicit and distinct.** `509d3f` constructs `Book_Fire/Water/Air/Earth/Astral` through `50806a`, appends fresh Effect kind 42 with spell id at Effect+40, and sets Item+1c from Spells parameter 21. Its second loop constructs `Scroll_*`, appends kind 41, takes parameter20 as base price, admits that base within inclusive min/max, sets `n=min(100,10*floor(max/base))`, draws power1..n through `50402c`, writes it to **Effect+42**, and sets stored Item price to `base*(floor(power/10)+1)`. The scaled result is not re-tested against max. Both candidate loops run ids1..27; Book additionally excludes11,17,27 and has no school constructor for boundary ids1,6,12,18, leaving ids2..5,7..10,13..16,19..26. Slow28 is outside both loops. This is not a stack-quantity law. At release `4feadb` reads signed Effect+42 without the book clamp and passes its low byte to `4fe25d`; ordinary generated powers are1..100. | High; complete constructor, admission, assignment and release distinguish Item count from Effect power and label both subtypes. | ✔ promoted | [EXP-0275](../experiments/EXP-0275-consumable-use/) |
| SHOP-CONSUME-074 | **Actual shelf quantities come from a later draw and six literal-Potion additions.** `5073e2` calls `509d3f`, then `50b055(rand1..8,1,shop+68)`. Collector kind 3 first copies every admitted Book once; later random selection draws from the full candidate collection. Base Item copy preserves price and ordered Effects; non-stackable enchanted Books/Scrolls receive count 1, and `50aae3` does not merge such copies. Afterwards `5073e2` constructs six explicit names — Health Regeneration, Medium Healing, Big Healing, Mana Regeneration, Medium Mana, Big Mana — assigns each Item+42 `50+rand1..50` (51..100) and adds/merges through `5062e8`. These additions have no price-window test. They are separate from the1..100 Scroll **power** draw and the unavailable name-filtered Potion pool. Book's first-effect value and packed Item identity are `ITEM-VALUE-115`; RNG seed timing and a particular unobserved generation's displayed outcome remain Unknown. | High for connected generator/draw/copy/merge and their distinct stores; no runtime generation claimed. | ✔ promoted | [EXP-0275](../experiments/EXP-0275-consumable-use/) |
| SHOP-CLS-001 | **The shop is one MFC class and the image names it `Shop`.** `.data` carries a 28-record `CRuntimeClass` table, 24 bytes each — `{name, objectSize, schema, CreateObject, base, next}` — in which `Shop` is `0x74` bytes, created by `FUN_00505acf`, derived from `Building` (`0x6c`, `FUN_00504093`), itself derived from `Token` (`0x3c`). `Shop::GetRuntimeClass` is `00505b39 MOV EAX,0x5c3328` — that record. So `ALM-CLS-037`'s unnamed "`0x74`-byte `CStructure`" and "`0x6c`-byte `CObject`" are `Shop` and `Building`; the walker's own `PUSH 0x74` / `PUSH 0x6c` are the same two sizes. The table also names `Item 0x50` with `Armor 0x68` / `Shield 0x68` / `Weapon 0x84` derived from it, `Sack 0x44`, `Player 0x70`, and the three shop helpers `CMultiShopShelf 0x1c`, `CMultiShopInstance 0xa0`, `CMultiShopTemplate 0x98`. Full table: `evidence/runtime-classes.csv` | High (a self-describing table the image's own `GetRuntimeClass` returns, with the object size independently confirmed by the allocator immediate at each construction site; there is no second reading of a `CRuntimeClass` record) | ● active | [EXP-0057](../experiments/EXP-0057-shop/) |
| SHOP-OBJ-002 | **The shop's state: a `Shop` owning one `CMultiShopTemplate`, which owns four shelves; one `CMultiShopInstance` per customer.** `Shop::Shop` allocates a `0x98` object into `shop+0x6c` and sets `shop+0x70 = 10000000`. `CMultiShopTemplate` embeds **four `CMultiShopShelf` (`0x1c`) at `+0x0c`, stride `0x1c`** (`FUN_00554c50(this+0x0c, 0x1c, 4, ctor, dtor)`) and stamps their `+0x04` codes **`1, 2, 4, 3`**; `+0x04` is the stocked-item count, `+0x08` a regenerate flag, **`+0x90` the value cap**. A shelf is `{vptr, code, CArray}` with its array at `+0x08`. `CMultiShopInstance` (`0xa0`) embeds the same four shelves at `+0x04` with the same codes, holds the customer at `+0x74` and the customer's tray list at `+0x78`; `Shop::Open` refuses past **250** instances (`CMP …,0xfa`). **[EXP-0061] identifies `+0x78`**: it is the *table*, the container the shop screen's five-place strip mirrors (`SHOP-TRAY-024`), an embedded list with **no capacity of its own** — the five is enforced in the view, not here. **⚠ `+0x04` is NOT the stocked-item count — that label is RETRACTED by [EXP-0058] (retracted.md).** It is the number of customers who currently have the shop open: `FUN_00507162` increments it at `005071e3` when it creates a `CMultiShopInstance` and back-links the template at `instance+0x9c`, and the instance destructor `FUN_005067c2` decrements it at `0050683e` through that back-pointer. Over the whole 132-function family listing there are five `dword` stores to `+0x4` and the other three are the two template constructors' zeroing and the shelf constructor's own `-1` code. The instance's own array of open instances is `template+0x7c` (count at `+0x84`), which is what the 250-instance test reads — `SHOP-LIFE-013` | High (the two counter instructions and the back-pointer they reach through; the four-shelf shape is the array ctor's own `(0x1c, 4)` and the codes are four immediate stores; the "only writers" clause is a grep over a `range:` census of `00505acf..0050c000` — function bytes 25865, **orphan bytes 0**, 40 undisassembled, so its blind spot is those 40 bytes and any writer outside the range) | ✖ partially retracted | [EXP-0057](../experiments/EXP-0057-shop/), **[EXP-0058](../experiments/EXP-0058-shop-lifecycle/)** |
| SHOP-ENTRY-003 | **Both entry points build the same object: one class, one state block, two providers.** `Shop` has exactly three construction sites on a repaired function table (`EnumRefs callto:`, 0 hits in orphan/undisassembled code): the ALM type-4 walker `FUN_004e2462` (map placements), a **placement-new into the static object at `0x0060a120`** (`FUN_004d4d42` → `Shop::Shop()`; destructor `FUN_004d4d63`), and the MFC `CreateObject` thunk `FUN_00505acf`, which has **0 direct callers** and is therefore reachable only through the `CRuntimeClass` record, i.e. `CArchive`. The shop-by-id resolver `FUN_004d4db1` has two arms: it returns `&DAT_0060a120` when `server+0x14c != 0`, otherwise it looks the id up and **requires `IsKindOf(&Shop)`**. Every transaction routine below is a `Shop` method, so both providers share them. **Amended by [EXP-0058]: the Medium clause is settled — `server+0x14c` is the single-player condition** (`SHOP-ENTRY-016`), so the static object is the campaign shop and the map arm is the multiplayer one, and on a campaign map the resolver never reaches a placed shop at all | High (the construction enumeration and the resolver's `IsKindOf` guard — a second, lookalike shop class would have to be constructed somewhere, and no fourth site exists) / **High, was Medium** (that the static `0x0060a120` shop is the **campaign base** shop: no longer resting on the owner's description of two entry points but on the flag's two writers, which compute `single-player` from the map's own participant count — `SHOP-ENTRY-016`) | ● active (amended) | [EXP-0057](../experiments/EXP-0057-shop/), **[EXP-0058](../experiments/EXP-0058-shop-lifecycle/)** |
| SHOP-CAP-004 | **The one per-shop parameter is a value cap, and a map shop takes it from its own ALM record: `(i16)rec+0x06 × 1000`.** In `FUN_004e2462`'s `Shop` arm, `004e256a MOVSX EAX,word ptr [EDX + 0x6]` / `004e256e IMUL EAX,EAX,0x3e8` / `004e2578 CALL 0x00505e10` — and that `IMUL …,0x3e8` is the **only** one in the whole walker. `FUN_00505e10` stores it at `shop+0x70` and forwards it through `FUN_00524d80` to `template+0x90`, which the generator passes as the `max` of its value window (`SHOP-POOL-006`). `FUN_00505e10` has two callers (the walker; the command dispatcher `FUN_004d5dd8` at `004d7192`, which feeds it a command record's `+0x0e` and then generates — **that second caller is traced end to end by [EXP-0059], `SHOP-MISSION-018`: `+0x0e` is `scenario.reg`'s per-mission `ShopMaxPrice`, taken raw with no `× 1000`**); `FUN_00524d80` has two (that setter and a template-ctor helper). **Census**, `tools/shopcensus` over both preserved roots: EN 3141 type-4 records → **50 shop placements, 26 × id 34 + 24 × id 35** (reproducing `ALM-CLS-037` exactly), RU 1681 → 24. The EN `+0x0c` words take **23 distinct values / 30 distinct (id, value) pairs** — caps from **4 000 to 10 000 000** — and of the 14 (map, id) groups with more than one placement, **12 hold differing words**. 49 of the 50 sit on the ten loose root maps, 1 on `scn:120.alm` | High (the forwarding chain is four named instructions end to end with an enumerated writer set; and the census kills the rival "the stock is selected by class id" outright — that rival admits two values and the corpus shows thirty, differing even between two same-id shops on one map) | ● active | [EXP-0057](../experiments/EXP-0057-shop/) |
| SHOP-GEN-005 | **The stock is generated by exactly one routine, which always clears before it fills.** `FUN_005073e2` returns immediately unless `template+0x04 < 1`. **⚠ "i.e. it stocks only an empty shop" is RETRACTED by [EXP-0058] (retracted.md): `+0x04` counts open customers, not items** (`SHOP-OBJ-002`), so the gate means *not while somebody is inside*, and the routine's own first act after it is `FUN_005073a9` — four calls to `FUN_00506670`, each of which **deletes every element of a shelf and then `CObArray::RemoveAll`s it**. So generation is a clear-and-refill, never an append, and nothing about it is lazy: the caller decides when. Its three callers are `Shop::Open` (`FUN_005079a7`), the restock gate `FUN_00507e56` (`SHOP-LIFE-014`) and `Shop::Generate` (`FUN_00505e54`), which the city-screen command dispatcher calls **unconditionally** at `004d719c` right after `Shop::SetCap(cmd+0x0e)`. It fills the four shelves with **100** (code 2, weapons), **100** (code 1, armour + shields), **20** (code 4, magic items) and **`rand(1..8)`** (code 3, consumables) drawn items, then appends **six literal potions by name** — `"Potion Health Regeneration"`, `"Potion Medium Healing"`, `"Potion Big Healing"`, `"Potion Mana Regeneration"`, `"Potion Medium Mana"`, `"Potion Big Mana"` — each at quantity `rand(1..50) + 50`. The drawing routine `FUN_0050b055` has **4 call sites, all four inside `FUN_005073e2`**; the only other caller of the pool builder, `FUN_005094fd`, has **0 callers**. **The `51..100` potion range is image-only**: the owner was asked whether potions ever appear outside it and answered that he cannot say, only that they come "in batches" — which is consistent with `rand(1..50)+50` and with most other rules, so it discriminates nothing and is not corroboration (`EXP-0058/evidence/owner-testimony.md`) | High (the four counts and the six names are immediates and string operands in one function; the clear is `FUN_005073a9`'s own four-iteration loop and `FUN_00506670`'s delete-then-`RemoveAll`; the "exactly one generator" clause is an `EnumRefs callto:` enumeration on the repaired table, 0 orphan hits, and its blind spot — a call through a vtable slot — is covered because `callto:` also scans `.rdata` dwords) / **Medium** (that the gate's purpose is to protect a browsing customer rather than to avoid duplicate work: the instructions give the condition, not the intent) | ✖ partially retracted | [EXP-0057](../experiments/EXP-0057-shop/), **[EXP-0058](../experiments/EXP-0058-shop-lifecycle/)** |
| SHOP-POOL-006 | **What a candidate is, and what the value window bounds.** `FUN_00509ac8(kind, min, max, tier)` stores `min`/`max` at `collector+0x34`/`+0x38` and dispatches over five kinds onto the `Data.bin` collections at `0x609b18 + {0x28, 0x3c, 0x50, 0x64}` = **Shields, Armors, Weapons, Magic Items** (`DAT-OBJ-002`): kind 1 = Shields (code 7) + Armors (code 1), kind 2 = Weapons (code 2), kinds 3/4/5 = Magic Items filtered by the names `"Potion"`, `"Scroll"`, `"Book"`. `FUN_005098e2` reads `tier == 5` as *all five tiers* `0..4`, which is what every generator call passes. `FUN_005095ee` then walks the collection's entries (`shape`, from index 1) and, per entry, the 16 bits of a **`u16` mask at `entry+0x1c + 2·tier`**, and admits `(tier, materialBit, shape)` iff `collector+0x34 <= v && v <= collector+0x38` (`005096cb`/`005096da`) where `v = __ftol(param[2])` of that entry. **⚠ That last clause is incomplete and is corrected by [EXP-0059] (`SHOP-POOL-021`, retracted.md): `v = ftol(param[2] × Materials[m].+0x30 × Shapes[t].+0x30)` — two `FMUL`s at `005096b4`/`005096ba` sit between the `FILD` and the `__ftol`, so the window bounds the item's own *price*, not its base slot. The kind 3/4/5 arms named below are also on a path no live caller reaches.** The item constructors store the triple at `item+0x45` (tier), `item+0x46` (material), `item+0x0c` (shape). **So `ShopMinPrice`/`ShopMaxPrice`-shaped bounds bound an *item's value*, not the purse, the takings or the stock size** — and the five masks × 16 bits explain `DAT-OBJ-002`'s otherwise-uninterpreted "+10 raw bytes" on an armor/shield/weapon entry against `Data.bin`'s 16 Materials | High (the two `CMP`s against `+0x34`/`+0x38`, the collection base addresses as literal `PUSH`es, the mask displacement `entry+0x1c + 2·tier` and the three constructor stores — all read from the listing; the rival "the bounds gate the purse" has no instruction, since neither commit path touches them) / **Medium** (that the entry the mask hangs on is a *shape* and the bit a *material*: the count 16 matches `Data.bin`'s Materials collection and 5 matches the tiers, but the ctor's use of the triple was not opened) | ✖ partially retracted | [EXP-0057](../experiments/EXP-0057-shop/), **[EXP-0059](../experiments/EXP-0059-shop-mission/)** |
| SHOP-MAGIC-007 | *(amended by EXP-0226: the gates are inclusive draws, the outer bound includes both endpoints, and route completeness is scoped to direct calls.)* **On the measured direct-call shop-stock route, enchantment is a second stage over a drawn base item and mode 6 is the route that demands it.** `FUN_0050b055` clones a mask-admitted Armor, Shield, or Weapon triple; all four direct calls to it lie in `FUN_005073e2`, and its mode-6 arm calls `FUN_0050ba63`. A required-effect failure frees the item and retries while `attempt <= 10*n`, so the request for 20 permits 201 rejected attempts and may still return short. One effect is required. Independent second and third opportunity draws are `rand(0..100)<50` and `<25`, exactly `50/101` and `25/101`; the third gate is drawn even when the second is false unless an earlier null/cast return has ended the routine. Each call receives `2*ceiling-currentStoredPrice`, not an immutable base budget. The ordered Effect population, payload, price, retry, and early-return rules are `SHOP-EFFPOOL-061` through `SHOP-EFFCAP-069` | High for the positive live route, gate bounds, branch destinations, and outer comparison / Medium for excluding another generator route beyond direct `CALL` references; EXP-0226 did not commit a raw-pointer/indirect/orphan census | ● active (amended) | [EXP-0057](../experiments/EXP-0057-shop/), **[EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/)** |
| SHOP-RNG-008 | *(amended by EXP-0226: seed sources are complete; the exact first-shelf timing is not.)* **Shop draws use the CRT stream, and the shop generator does not reseed it.** `FUN_00554a60` is the MSVC LCG over `tls+0x14`; `FUN_00504003(n)` draws inclusive `0..n` and `FUN_0050402c(n)` inclusive `1..n`. The sole state writer `FUN_00554a50` has three callers: audio initialization and `FUN_00503fd6` pass `timeGetTime`, while the server-family constructor passes `time(0)`. `FUN_00503fd6` has two callers; the `World\\Mission`/optional `game0000.sav` loader `FUN_004d00e9` is reached by campaign mission load and cache miss, so that route reseeds. An ordinary homecoming which does not cross that loader continues the existing stream. No seed is derived from mission id, map name, map bytes, or saved stock. Exact clock values and the intervening draw count at the first regenerated shelf remain Unknown | High for the RNG and sole-writer/caller population / Medium for the named loader reachability / Unknown for runtime timing and the first-shelf seed state | ● active (amended) | [EXP-0057](../experiments/EXP-0057-shop/), [EXP-0058](../experiments/EXP-0058-shop-lifecycle/), **[EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/)** |
| SHOP-BUY-009 | **Buying: `gold −= quantity × unitPrice`, and an unaffordable item aborts the whole commit.** `FUN_005069d0` walks the customer's tray `instance+0x78`; for each element still owned by the shop (`elem+0x14 == 0`) it computes `(u16)elem+0x42 × elem+0x1c` and compares it with the customer's money at `Player+0x38` — reached as `instance+0x74` → `+0x14` → `+0x38`. `00506a25 CMP dword ptr [ECX + 0x38],EDX / 00506a28 JGE / 00506a2a JMP 0x00506a9d` **leaves the loop**, so nothing after the unaffordable element is bought. On success it debits, unlinks the element, sets `elem+0x14` to the `Player` and inserts it into `customer+0x7c`. **⚠ Narrowed by [EXP-0058]: this is a property of the commit routine, and its reachability is UNWITNESSED.** *Origin:* the owner, asked what happens to the rest of a basket when one line is unaffordable, answered that nothing happens — you simply cannot buy it — which reads as *there is no multi-item basket in the UI to reach the abort with*. That does not falsify the branch, which is in the listing; it removes the witness that two elements can be in one tray at once. `FUN_005069d0` is written as a loop and the abort is real **if** it is ever entered with more than one element. Settling it needs the shop view's trade half, which no experiment has read — **[EXP-0060] read that view's `vt+0x80` (`FUN_004a8bc3`, its mission-offer half) and the town view around it, but not the tray**. **⚠ [EXP-0061] settles it, and in the direction the loop did not suggest.** The table holds **five** elements (`SHOP-TRAY-024`), so this really is a loop over several lines — but the client will not send the buy command unless the *whole* basket is affordable (`SHOP-TRAY-026`), and since the server debits monotonically as it walks, an accepted basket makes every prefix affordable. **The `JMP` past the iterator is a guard against a client that lies, not a behaviour a player can produce** | High (every term of the arithmetic and the abort-vs-skip branch target are named instructions: the `JMP` exits past the iterator) / **High, was Unknown** (that more than one element can be on one tray: five 80×80 places with four consumers of the count, `SHOP-TRAY-024`) / **Unknown** (whether the client's purse mirror can ever disagree with `Player+0x38`, which is the one thing that would make the abort reachable — `SHOP-TRAY-026`) | ● active (amended) | [EXP-0057](../experiments/EXP-0057-shop/), **[EXP-0058](../experiments/EXP-0058-shop-lifecycle/)**, **[EXP-0061](../experiments/EXP-0061-shop-trade/)** |
| SHOP-SELL-010 | **Selling exists, and the shop pays exactly half: `gold += ftol(0.5 × (quantity × unitPrice) + 0.5)`.** `FUN_00506893` walks the same tray for elements the customer owns (`elem+0x14 != 0`) that carry a price (`elem+0x1c != 0`), computes `(u16)elem+0x42 × elem+0x1c` as an integer, then `FILD` / `FMUL [0x0059bc48]` / `FADD [0x0059bc48]` / `__ftol` — and the double at `0x0059bc48` is **`0.5`**, used as both the multiplier and the rounding addend, so the result is half the buy price rounded half-up. It credits `Player+0x38`, clears `elem+0x14`, and hands the element to `FUN_00506aea`, which puts it back on a shelf by class code: `item+0x44 ∈ {3,4,5}` → shelf 3, a false `vt+0x50` → shelf 2, `== 2` → shelf 1, `== 1` → shelf 0, anything else → shelf 2 with the log line `"Item of strange type is returned to shop - placed to Magic Items"`. **[EXP-0058] adds the consequence a consumer will otherwise reproduce wrongly: the rounding is applied to the whole stack, not per unit.** `qty × price` is one 32-bit `IMUL` before the `FILD`, so the payout is `ceil(n·p/2)`; selling the same `n` odd-priced items one at a time pays `n·(p+1)/2`, which is `⌊n/2⌋` more. Splitting a stack therefore changes the total by up to one coin per pair, on odd prices only. *Origin:* the owner remembers an odd-price discrepancy of 0-1 on buy-back; [EXP-0058] enumerated every rounding site and found **no second payout site** (`SHOP-ROUND-017`), so this consequence is what the published formula already implies — stated because the testimony made it worth stating, not graded on it | High (the FP pair exists on the sell path and nowhere on the buy path, which is what discriminates "two prices" from "one"; the constant is read out of `.rdata`; `__ftol` at `0x0055458c` is read and really does round toward zero, so `ftol(0.5n + 0.5)` is exactly `ceil(n/2)` with no representation error) / **Unknown, narrowed** (whether the UI ever offers a partial-stack sell: `FUN_00506552`'s caller is now read — it is the move command's shelf arm, `FUN_00506c87`, and the quantity is a command field, so the *protocol* splits; but the tray's drop handler passes the dragged element's whole quantity and the pick-up's split argument was not traced, `SHOP-TRAY-027`). **⚠ The consequence no longer needs the split to be observable**: the shop screen's own sell total halves **per unit** and then multiplies, so on an odd price it promises `⌊qty/2⌋` more than the shop pays (`SHOP-TRAY-027`) | ● active (amended) | [EXP-0057](../experiments/EXP-0057-shop/), **[EXP-0058](../experiments/EXP-0058-shop-lifecycle/)**, **[EXP-0061](../experiments/EXP-0061-shop-trade/)** |
| SHOP-PRICE-011 | **The price charged is a property of the item, not of the customer.** Both commit routines read exactly **one** customer-dependent quantity, `Player+0x38` (the money), and take the unit price from `item+0x1c`; the generator that fixes `item+0x1c` sees only `(min, max, tier)` and never a customer, a hero or a player. So a hero-dependent price has no instruction to live on in either path, and two customers at one shop are charged identically. **The Unknown is answered by [EXP-0058]: `item+0x1c` is written by three per-class routines** — `FUN_0050c740` (Armor), `FUN_0050d0ce` (Shield), `FUN_0050dc4e` (Weapon) — with byte-identical arithmetic, `item+0x1c = ftol(base × material.+0x30 × tier.+0x30 + 0.5)`, where `base` is parameter slot 2 of the item's own `Data.bin` entry and the two factors are `+0x30` doubles of the Materials entry `0x609b18[item+0x46]` and the tier entry `0x609b2c[item+0x45]`; the enchantment sum is then added through `FUN_00502a10` and the result clamped to `9 999 999` (`SHOP-ROUND-017`). Neither factor is a customer, so the absence this row asserts is now bounded by the constructor as well as by the two commits | High (an absence bounded by two fully-read routines and a fully-read generator, not by an image-wide sweep — the claim is scoped to the buy path, the sell path and stock creation, which is where a price could be applied) / High (the price formula: eight instructions per routine, three parallel routines, the two collection bases as literal `MOV ECX` operands) | ● active (amended) | [EXP-0057](../experiments/EXP-0057-shop/), **[EXP-0058](../experiments/EXP-0058-shop-lifecycle/)** |
| SHOP-LIFE-013 | **A shop's stock is torn down and rebuilt from nothing every time the generator runs, and the only thing that can stop it is a customer standing in the shop.** `CMultiShopTemplate+0x04` is the count of open `CMultiShopInstance`s (`SHOP-OBJ-002`), incremented at `005071e3` by `FUN_00507162` and decremented at `0050683e` by the instance destructor `FUN_005067c2`. `FUN_005073e2`'s gate `00507416 CMP dword ptr [EAX + 0x4],0x0 ; JG` therefore reads *return if anyone is inside*; its next act is `FUN_005073a9`, a four-iteration loop over `FUN_00506670`, which for each shelf walks the element array, calls each element's `vt+0x04` (the scalar-deleting destructor) with argument 1, nulls the slot, and then calls `CObArray::RemoveAll` (`FUN_00525350`). **There is no clearing writer anywhere else**: over the `range:00505acf:0050c000` census (135 entries, function bytes 25865, **orphan bytes 0**, 40 undisassembled) the only element-destroying shelf walks are `FUN_00506670` and the instance destructor's `FUN_00506610`, which merely decrements each element's `+0x4c` before releasing the instance's own copy of the array | High (the two counter instructions, the gate, and `FUN_005073a9`/`FUN_00506670` read end to end) / **Medium** (that `elem+0x4c` is a per-item count of the instances currently holding it: `FUN_00506610` decrements it and `FUN_00507a1c` removes elements whose byte is 0, which is consistent with a reference count and is not named by an instruction) / **Unknown** (whether a shop can be reached by a path that never creates an instance, which would make the gate unreachable — the town-screen UI is unread) | ● active | **[EXP-0058](../experiments/EXP-0058-shop-lifecycle/)** |
| SHOP-LIFE-014 | **The restock timer is real, it is multiplayer-only, its period is 180, and the rebuild is deferred until the shop is empty of customers.** `Shop` vtable slot `+0x14` is `FUN_00505e3e`, which forwards to `FUN_00507db0` on `shop+0x6c`. That routine returns at once unless `[0x005cd758]+0x0c != 0` — the session's *more than one participant* flag (`SHOP-ENTRY-016`), so **nothing in it runs in single player** — and otherwise divides `server+0x00` by `0xb4` (`00507dd2 MOV ECX,0xb4 ; IDIV`) and, on a zero remainder, does `template+0x08 += 1` (`00507de9`) and calls `FUN_00507e56`. `FUN_00507e56` regenerates only when `+0x08 > 0` **and** `+0x04 == 0`, then zeroes `+0x08` — so a request raised while a customer is inside is held, not lost, and is discharged the moment the last one leaves, which is why `FUN_00507a1c` (the customer-leaves path, which destroys the instance and prunes shelf elements whose `+0x4c` has reached 0) calls `FUN_00507e56` as its last act. `FUN_00507e56` has exactly two callers, `FUN_00507db0` and `FUN_00507a1c`, 0 in orphan code | High (every instruction named: the multiplayer gate, the `IDIV` by `0xb4`, the counter increment, the two-part restock condition and the reset; the caller sets are `EnumRefs callto:` on the repaired table) / ~~**Unknown** (**what advances `server+0x00`**…)~~ — **closed by [EXP-0076]** (`SESS-TICK-004`, `SESS-CLOCK-005`): `server+0x00` is the **full-tick** counter, advanced by one instruction sequence (`004d2606`…`004d260e` in `FUN_004d2551`) once per **16** sub-ticks, and a sub-tick is paced at `campaign+0x3f0 = 1000/R` ms with `R` from a nine-arm ladder defaulting to **16**. So the period here is **180 full ticks ≈ 178.6 s at the shipped default game speed**, and it scales with the speed index from 360 s (index 0) to 89 s (index 8) — `EXP-0076/evidence/tick-rate.csv`. Two bounds a consumer must carry: the pacing is a *deadline* loop with catch-up, so the figure is a ceiling; and this timer is multiplayer-only, where the sub-tick may instead be driven by the wire (`SESS-IDLE-007`) | ● active (amended) | **[EXP-0058](../experiments/EXP-0058-shop-lifecycle/)**, **[EXP-0076](../experiments/EXP-0076-session-lifecycle/)** |
| SHOP-SAVE-015 | **No part of a shop's stock is written to a save.** `Shop::Serialize` is `FUN_00505fcc` — MFC vtable slot `+0x08` of `0x0059c828`, and `EnumRefs callto:505fcc` returns exactly **one** hit, that slot itself, so the archive is its only caller. Its whole body is `FUN_005047c6` (`Building::Serialize`), then a single `int` through the archive at `shop+0x70` — `FUN_005188a0(this+0x70)` on load, `FUN_005187e0(*(this+0x70))` on store, selected by `FUN_00449630` — i.e. the value cap and nothing else. It never touches `shop+0x6c`, the `CMultiShopTemplate`. And **`CMultiShopTemplate` does not override `Serialize` at all**: slot `+0x08` of its vtable `0x0059c8b0` is `FUN_00401950`, a bare `RET`, the same stub that fills the `AssertValid`/`Dump` slots throughout this family. So neither the template, nor the four shelves, nor one stocked item enters the object graph `SAV-STREAM-013` walks; a loaded game starts with an empty shop and the next generate fills it | High (the two vtable slots dumped with their targets, the three-call body of the one real serializer, and the empty stub read verbatim; the MFC slot identification is carried by slot `+0x00` returning the class's own `CRuntimeClass` on both vtables, which fixes the layout) | ● active | **[EXP-0058](../experiments/EXP-0058-shop-lifecycle/)** |
| SHOP-ENTRY-016 | **`server+0x14c` is the single-player condition, and it is why a shop placed on a campaign map can never be opened.** The flag has exactly two writers and both compute the identical expression, `004e0d42…004e0d57` in `FUN_004e0b0c` and `004e1ea9…004e1ebe` in `FUN_004e1924`: `MOV EDX,[0x005cd758] ; CMP dword ptr [EDX+0xc],0x0 ; SETZ AL ; MOV [0x005cd758]+0x14c,EAX`. `server+0x0c` is itself set 40 instructions earlier in the same routine from the loaded map — `004e1bc1 CMP dword ptr [EAX+0xd4],0x1 ; SETG CL ; MOV [0x005cd758]+0x0c,ECX` — and `map+0xd4` is **`ALM-META-058`'s type-0 `+0x70`**, which reads `1` on 56/56 embedded campaign maps against 4/8/12/16 on 15 of 16 loose ones; the very next store, `server+0x120 = (server+0x0c == 0)`, is that claim's own second consumer, so the identification borrows nothing new. Hence `server+0x0c` = *more than one participant* and `server+0x14c` = *single player*. The consumer is `SHOP-ENTRY-003`'s resolver: `004d4dbd CMP dword ptr [EAX+0x14c],0x0 ; JZ` → `004d4dc6 MOV EAX,0x60a120` returns the static town shop **for every id, without reading the id**. The type-4 walker still builds the placement — `004e1c76 CALL 0x004e2462` runs in the same routine — so on `scn:120.alm`, the one campaign map that ships a shop, the object exists and no lookup can reach it. *Origin:* the owner's independent proposal that this placement is an ordinary shop that is simply inert; the mechanism, not the proposal, is what is graded, and the frozen census had already ruled out a degenerate record | High (the two writers are the same three instructions and there is no third; the reader is the resolver's first branch; `map+0xd4` is `ALM-META-058`'s field on that claim's own evidence) / **High, was Medium** (that unreachability is *complete*. [EXP-0061] made the sweep exhaustive on a fourth instrument — **every reference to the `Shop` `CRuntimeClass` record `0x5c3328`**, which any `IsKindOf(&Shop)` must make: 5 hits, 5 owners, 0 in orphan/undisassembled code, of which `FUN_00505b39`/`FUN_00505b53`/`FUN_00505b67` are `Shop`'s own and exactly **two** identify an arbitrary object — the resolver, and a site nobody had seen, `FUN_004f37be` at `004f3ee9`, an actor's use-target handler. That one **opens nothing**: it posts notification `0x83` and returns. Alongside it: `callto:505cbd` = 1 construction site, `callto:505acf` = 0, `refto:60a120` = 5 hits over 4 owners, `callto:4d4db1` = 9 over 2. Every routine that reads or mutates a shop's state is non-virtual and its only caller is a `FUN_004d5dd8` arm that has already resolved. **Blind spot, stated:** a route holding the object through a `Building`/`Token` pointer that never asks what it is would not appear in the `refto:` sweep; such a route cannot call the trade methods, but it can call an inherited virtual — and one does, `vt+0x14`, the restock tick, which is itself multiplayer-gated, `SHOP-LIFE-014`). *(amended by [EXP-0076]: **`FUN_004e0b0c` is not a map-load path.** Its only string operand is `"Mission description loaded from "`, it reads through the CRT stdio helpers, and it contains **no `map+0xd4` access at all** — it is the `World\Mission\<n>.ini` reader, called by `FUN_004d00e9` *before* the map is opened (`SESS-LOAD-009`). Its `server+0x14c` store therefore recomputes the flag from whatever `server+0x0c` already holds, i.e. it carries the **previous** session's value forward; on the fresh-load path `FUN_004e1924` overwrites it at the end, and on the **save-restore** path `FUN_004e1924` never runs, so `FUN_004e0b0c`'s store is the only one. The two writers and the identical expression stand; the label "both map-load paths" does not.)* | ● active (amended, contested) | **[EXP-0058](../experiments/EXP-0058-shop-lifecycle/)**, **[EXP-0061](../experiments/EXP-0061-shop-trade/)**, **[EXP-0076](../experiments/EXP-0076-session-lifecycle/)** |
| SHOP-ROUND-017 | **Every site that rounds a money quantity, and the one place a coin moves.** `__ftol` is `0x0055458c` and rounds **toward zero** (`FSTCW ; OR AH,0xc ; FLDCW ; FISTP`). Four sites, no more: **(a)(b)(c)** the three per-class price constructors `FUN_0050c740` / `FUN_0050d0ce` / `FUN_0050dc4e` (Armor / Shield / Weapon), each `item+0x1c = ftol(base × material.+0x30 × tier.+0x30 + 0.5)` — a fractional product rounded **half up**; **(d)** the enchantment addend `FUN_00502a10(n) = ftol((pow(n/70.0, 1.5) + 1.0) × n × 50.0)`, the only one of the four with **no `+0.5`**, so it **truncates** (constants at `0x0059bce8` = 70.0, `0x0059bc80` = 1.0, `0x0059bcf0` = 50.0, exponent the immediate `0x3ff8000000000000`); and **(e)** the sell payout `SHOP-SELL-010`. The buy path `FUN_005069d0` executes **no floating point at all**. So a price is an integer from the moment the item exists, and the only rounding a transaction performs is the sell half — which is why the odd-price effect a player sees needs no second site: `ceil` runs on the whole stack, so splitting one changes the total (`SHOP-SELL-010`) | High (each site read at instruction level with its constants dumped from `.rdata`) / **Medium** (that these are *all* of them). The completeness clause carries three instruments, each with its blind spot named: `EnumRefs refto:59bc48` on the `0.5` constant — 21 hits, 13 owners, **0 in orphan code**, blind to a rounding that materialises `0.5` some other way; an image-wide `EnumRefs re:` over `MOV dword ptr [reg + 0x1c],` — 716 hits, 402 owners, **2 in orphan code**, both named and neither a price (`0049a0d7` in `ORPHAN[0049a0c0..0049a0f0]` stores the `.rdata` pointer `0x59a910`; `00580813` in `ORPHAN[005807f8..0058085c]` is inside the CRT region above `0x570000`); and a `range:004a79c1:004aab16` census of the shop UI — 29 functions, **0 orphan bytes**, 32 undisassembled — in which nothing reads any object's `+0x1c` and nothing references `0x0059bc48`, which bounds but does not exclude a UI-side price display | ● active | **[EXP-0058](../experiments/EXP-0058-shop-lifecycle/)** |
| SHOP-NPC-012 | **`npc.reg`'s `PriceA`/`PriceB` are written and no reader was located — and they are not on the shop path.** `FUN_0048c510` reads both keys and stores them at `npc+0x18` / `npc+0x1c` of the `0x30`-byte npc record, into the array `DAT_005f075c` (count `DAT_005f0760`). `EnumRefs refto:5f075c` on the repaired table enumerates every route to a record — **20 hits, 7 distinct owners, 0 in orphan/undisassembled code** — and `DisasmFn` over all six non-loader owners (plus the one caller of the accessor `FUN_0048c980`) finds **no `[reg+0x18]` or `[reg+0x1c]` memory operand at all**; the only `0x18` hit is `00421e9a CALL dword ptr [EDX + 0x18]`, a vtable slot. Nothing in the shop machinery touches the npc array. This settles the ledger question negatively on the shop side. **⚠ The “no reader was located” clause is SUPERSEDED by [EXP-0062]** (retracted.md): the readers exist and they are the mercenary path this row named as the unevidenced rival — `PriceA` and `PriceB` are the two terms of the hire price, `cost = (PriceA + n·PriceB) × unitPrice(mission)`, read at `005057de`/`005057f9` by the `Tavern` spawn routine `FUN_005056f1` and at `004e8539`/`004e8564` by the object serializer `FUN_004e7de3` (`MERC-PRICE-004`, `REG-SCN-067`). Both reach a record through `FUN_004c86a0(0x5f0758, id)`, the array **object**, whose body reads `[ECX + 0x4]` — the absolute `0x5f075c` never appears, which is precisely the laundering this row's Unknown named as its instrument's blind spot. The “not the shop's” half stands unaltered | Medium (that the two fields are not the shop's: an absence over an enumerated owner set, whose instrument — `refto:` on the array global — is immune to the orphan blind spot for *this* global but cannot see a record pointer laundered through a routine that never touches `0x5f075c`) / **Unknown** (what they are for. The obvious rival is the inn/mercenary path; no reader supports it either, and `REG-NPC-058`'s measured domains — `PriceA` 28..80, `PriceB` 0..15, 13 npcs each — **answered by `MERC-PRICE-004`**, and the 13 npcs that carry them are exactly the 13 mercenary types with a nonzero `[General] MercenaryCount`) | ● active (amended; the “no reader was located” clause superseded) | [EXP-0057](../experiments/EXP-0057-shop/), **[EXP-0062](../experiments/EXP-0062-mercenaries/)** |
| SHOP-MISSION-018 | **The campaign town shop's ceiling is `scenario.reg`'s per-mission `ShopMaxPrice`, and the chain has no unread hop.** `FUN_004879f0` (the mission-record loader) opens `Scenario\Scenario.reg`, rejects a mission number above `10 × [General] ScenarioMissionCount` (`00487a48…00487a5e`), formats `"Mission%d"` and reads the key with the scalar getter `FUN_004ccc20(section, key, default = 0)`: `00487cb0 PUSH 0x5bf0d0` / `00487cbd CALL` / `00487cc2 MOV dword ptr [EBP + 0x14],EAX` — `record+0x14`. The record is **embedded at `+0x548`** of the campaign screen object, which `FUN_00473110` establishes by calling the record's own reset and load-mission methods on `LEA ESI,[EBP + 0x548]` (`00473aa9…00473acb`). Both readers of `record+0x14` load it as `[obj+0x55c]` and call `FUN_0041da75(min, max)`, which fills the staging command at `0x609c38`: `0041da88 MOV byte ptr [EAX + 0x9],0x3f` (opcode), `0041da9b MOV dword ptr [EAX + 0xe],ECX` (**the ceiling**). The dispatcher `FUN_004d5dd8` switches on `cmd+0x09`; its `0x3f` arm runs **only when `server+0x0c == 0`** (single player, `SHOP-ENTRY-016`) and does exactly two things — `004d7189 MOV EDX,[ECX + 0xe]` → `004d7192 Shop::SetCap` on the static shop `0x60a120`, then `004d719c Shop::Generate`. `SetCap` (`FUN_00505e10`) stores it **verbatim** at `shop+0x70` (`00505e1d`) and forwards it to `template+0x90` (`00524d8d`), the generator's `max`. **So the campaign ceiling is `ShopMaxPrice` in coins, with no multiplier — unlike the ALM path's `(i16)rec+0x06 × 1000` (`SHOP-CAP-004`).** Corpus, all three roots identical: 13 of the 15 main missions carry the key, values **1000, 3000, 7000, 10 000, 30 000, 70 000, 100 000, 300 000, 700 000, 1 000 000, 3 000 000, 7 000 000, 10 000 000** — strictly increasing; **`Mission10` and `Mission20` carry no `ShopMaxPrice`**, so the loader's own default makes their ceiling **0**; **0 of the 9 sub-mission sections carry either shop key** (`evidence/missions.csv`) | High (every hop is a named instruction, and the two enumerations that could hide a rival came back clean on the repaired table: `EnumRefs disp:558 disp:55c` finds two readers and **no writer** of the pair image-wide, 0 in orphan code, and `imm:3f` finds `0041da88` as the only writer of that opcode — blind only to an opcode assembled in a register, which a whole-image `re:` census of the store form shows no command builder does) | ● active | **[EXP-0059](../experiments/EXP-0059-shop-mission/)** |
| SHOP-MISSION-019 | **`ShopMinPrice` is read, transmitted and never used: the value window's floor is the literal 0 everywhere.** The loader stores it at `record+0x10` (`00487cba`) and the command builder packs it into `cmd+0x0a` (`0041da92`), but the dispatcher's `0x3f` arm reads `cmd+0x0e` and nothing else. Inside the generator `FUN_005073e2`, all four pool constructions push the same triple — `PUSH 0x5` (tier), `template+0x90` (max), **`PUSH 0x0`** (min) — at `00507444/53`, `00507473/82`, `005074a2/b1`, `005074d1/e0`; the four wrappers pass their three arguments through unchanged to `FUN_00509ac8(kind, min, max, tier)`, which stores `min` at `collector+0x34` (`00509af3`) and `max` at `+0x38` (`00509afc`). **Consequence for a consumer: the campaign raises a ceiling, it never moves a band — a level-1 item stays drawable in the last mission.** The literal floor is not inert, though: it is what excludes `Data.bin`'s **negative-price sentinels**, the deleted `rem` weapon row and the **25 `"Quest …"` magic items**, all at `-1` | High (the four `PUSH 0x0` immediates plus two enumerations: `callto:509ac8` → 5 sites, 0 in orphan code — the four wrappers and the caller-less `FUN_005094fd`; and a whole-image `re:` census over `MOV dword ptr [reg + 0x34],`, 303 hits over 142 owners, **0 in orphan code**, of which exactly three are in the shop module — the collector's ctor `005094cc` and reset `005095b1`, both storing `0`, and `00509af3`. Blind spot: a write that is not a `MOV dword` — a `STOSD`, a struct copy — or one in bytes never disassembled) | ● active | **[EXP-0059](../experiments/EXP-0059-shop-mission/)** |
| SHOP-MISSION-020 | *(amended by EXP-0226: the equipment corpus is now walked independently on EN and RU.)* **The ceiling is the only per-mission generator input, and it enters the base-value window, enchantment interaction budget, and Scroll power law.** Every other generator input is an immediate or literal address. `max` is the collector upper bound; the Magic Items stage receives `2*max-currentStoredPrice`; the Scroll-effect builder uses `n=min(100,10*floor(max/price))`, `power=rand(1..n)`, then multiplies unit price by `power/10+1`. The complete mask walk produces 367 Armor/Shield/Weapon triples on each preserved root — 193 Armor, 36 Shield, 138 Weapon — and the two listings are row-identical. All 367 are admitted from ceiling 1,000,000. The older ceiling-0 four-item result remains a data-window fact, not a reachable forward-campaign town state (`SHOP-TOWN-023`) | High for the complete generator input/use census / Medium for the 367-row two-root arithmetic re-execution | ● active (amended) | [EXP-0059](../experiments/EXP-0059-shop-mission/), [EXP-0060](../experiments/EXP-0060-town-screen/), **[EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/)**  · [EXP-0275](../experiments/EXP-0275-consumable-use/) |
| SHOP-POOL-021 | **The equipment admission value is material/shape-adjusted, while shelf 3 has connected spell constructors.** `FUN_005095ee` computes `ftol(base × Materials[m].+0x30 × Shapes[t].+0x30)` before the price-window comparisons; `0x609b18` is Materials and `0x609b2c` Shapes, and element `+0x30` is double #2 of the serialized 0x48-byte block. `FUN_00509ac8`'s name-filter kinds 3/4/5 have no live generator callers. The separate `FUN_00509d3f` explicitly constructs `Book_*` and `Scroll_*`, then appends Effect kind 42 and 41 respectively; the spell id is at **Effect+0x40**, not Item+0x40. Spells parameters 21 and 20 price the two branches. `SHOP-CONSUME-073/074` close subtype, power and actual stock quantities; the former assertion that nothing in the image labels the subtypes is refuted. | High for adjusted admission and connected constructor/Effect identities. | ✔ promoted (amended) | [EXP-0059](../experiments/EXP-0059-shop-mission/), [EXP-0275](../experiments/EXP-0275-consumable-use/) |
| SHOP-TOWN-022 | **The ceiling is applied on the way INTO the town, never at a mission's start — and the value sent is the *next* mission's.** Command `0x3f` has exactly two senders (`callto:41da75` = 2, `imm:3f` puts the only opcode store at `0041da88`, `SHOP-MISSION-018`), and [EXP-0060] read both whole. **(a)** `FUN_00473110`'s message-`0x41d` arm, state `+0x6bc == 2`, the end of a mission: `FUN_004768c0` has already switched to the global-map view; `FUN_004884d0` decides whether the party goes home (`REG-SCN-063`); on the home arm `FUN_00488970` runs **first** at `00473fb5` — and when the finished mission was a main one that call advances `record+0x04` by 10 and **loads the next mission's record** through `FUN_004884b0` → `FUN_00488460` → `FUN_004879f0` — the party is then walked to global-map object 0, and only at `00473ff1`/`00473ff7` are `campaignScreen+0x55c`/`+0x558` read and packed into the command. So **finishing main mission *N* caps the town shop at `[Mission(N+10)] ShopMaxPrice`**, not at the finished mission's; finishing a *side* mission leaves the record where it is and re-sends the same ceiling. **(b)** `FUN_00478af0`, the load path (one caller, message `0x419`): it reads `CurrentState`/`InBattle` out of the save via `FUN_00477560`, and a battle save resumes the battle at `00478bba` **without** sending anything, while a non-battle save sends `0x3f` at `00478bff` and then posts `0x42e`, enter town. There is no third sender and no sender at mission start. Because the `0x3f` arm does `Shop::SetCap` **and** `Shop::Generate` (`SHOP-LIFE-013`), **the assortment is re-rolled on every homecoming**, including after a side mission that changed no ceiling | High (both senders read whole at instruction level; the ordering claim rests on the call order at `00473fb5` before `00473ff1`, and on `FUN_00488970` → `FUN_004884b0` → `FUN_00488460` being the only path by which `record+0x04` advances — `callto:488460` = 6 sites, 0 in orphan code, of which the reset, the acceptor and the `AutoGetMission` fork are the other three) | ● active | **[EXP-0060](../experiments/EXP-0060-town-screen/)** |
| SHOP-TOWN-023 | **A ceiling of 0 is not reachable on the campaign's forward path, so `SHOP-MISSION-020`'s ceiling-0 census is a statement about the data and not about play.** Three facts, each read: **(1)** a new campaign never sees the town — message `0x425` resets the record (`FUN_00487640`, ending in `FUN_00488460(10)` at `00487883`), runs hero creation, and the completion arm of `FUN_004757b0` posts **`0x42f`**, start battle, not `0x42e`; **(2)** `[Mission10] AutoGetMission = 20` is the campaign's only instance of that key (`REG-SCN-063`), and the arm it selects in `FUN_004884d0` returns 1 — the caller then travels to mission 20's own map object and **sends no `0x3f` at all**; **(3)** the town is the state `campaignScreen+0x3dc == 0`, reached only by message `0x42e`, whose six posters are the load path, the three building exits, `FUN_004757b0`'s dialog-close arm and `FUN_004a7133` — none of them on the mission-10/20 stretch. So the first `Shop::SetCap` the campaign ever executes carries `[Mission30] ShopMaxPrice = 1000`, and since single-player `Shop::Generate` has no caller but that same arm, the static town shop has **no stock at all** until that first homecoming — not four items. Forward-path walk, all three roots identical: `EXP-0060/evidence/timeline.txt`, 13 town visits, ceilings 1000 … 10 000 000 | High (each of the three legs is a named instruction or a completed enumeration on the repaired table) / **Unknown** (one route is not excluded: `FUN_00478c40`, the save routine, is gated only on `campaignScreen+0x6bc == 2` — set at new-campaign start by `FUN_00477450` — and records `InBattle` from `+0x3dc & 1`, so a **non-battle save taken while the record is on `Mission10`/`Mission20`** would on load be handed cap 0 and then posted into the town. Whether the game offers a save at that point was not established; the view that opens the save dialog was not read). *Origin note:* the owner reports from play that the first two missions have no city and prices are first seen after the second (`EXP-0060/evidence/owner-testimony.txt`). That recollection **aimed** this search and agrees with it; it carries no part of the grade, which stands if it is withdrawn | ● active | **[EXP-0060](../experiments/EXP-0060-town-screen/)** |
| SHOP-TRAY-024 | **The table is ONE strip of five 80×80 places, and both sides of the deal share it.** The shop view (vtable `0x0059ac88`) builds five children in `FUN_004a8634`, and each child class has **exactly one construction site image-wide** — that routine (`EnumRefs callto:` on the repaired table, which also scans `.rdata` dwords, so a construction through a vtable slot would appear; 0 orphan hits) — so each rect is the literal in its own `PUSH` block. Three children are item grids. Each computes, in its own constructor, `cols = (right−left)/80` (`IMUL 0x66666667 ; SAR EDX,5`, i.e. ÷80) **minus one** on the two strip classes and not on the third, and `rows = (bottom−top)/80`; `vt+0xac` allocates `cols·rows` **16-byte CRects** and tiles them at `0x50` px from `left+32`; and the hit test `FUN_004a2eb0` — `vt+0x8c`, **not overridden by any of the three** — walks exactly those `cols·rows` rectangles, returning `*(this+0x90) + i` on a hit and `−1` on a miss. The three grids and their container codes (`vt+0xa8`): `+0x70` **5×1 = 5, code 4 — the tray**, the widget that loads `ShopTable.bmp`; `+0x6c` 5×1 = 5, code 2 — the party backpack; `+0x68` 2×3 = 6, code `parent+0x132 + 5` — the shop's four shelves. **The tray does not scroll**: an image-wide `re:` census over `MOV dword ptr [reg + 0x90],` finds writers only in the backpack class's ctors and in the shop view's binding of the *other* two grids, so the tray's index range is exactly `0..4`; and the tray class's own display-list add `FUN_004a6160` refuses to append once the list holds `cols·rows` — the only such test in the widget family. The two sides are drawn, not seated: `myitem.256` + `costm1..7` for the customer's, `shopitem.256` + `costs1..7` for the shop's, **7 plaques per side in both roots**, which is the digit count of `SHOP-ROUND-017`'s `9 999 999` clamp. *Origin:* the owner recalls laying out "5(?) items" on the merchant's table and "the same number" from his side (`EXP-0061/evidence/owner-testimony.md`). The five is what the code computes; **the per-side reading is what the measurement bounds** — there are five places in all, shared. The testimony carries no part of the grade | High (four independent consumers of the same `cols`/`rows` pair — the `malloc` size, the tile loop, the hit test's bound and the add's refusal — over rects that a completed construction-site enumeration shows are the only rects these classes ever get; the ÷80 is the compiler's own magic-number idiom, re-executed in `tools/shoptrade`) / **Medium** (that `+0x68` is the shelf grid and `+0x6c` the backpack: carried by `vt+0xa8`'s constants 2 and `+0x132+5` against the server's own code namespace, and by which list the shop view binds to each — not by a rendered screen) | ● active | **[EXP-0061](../experiments/EXP-0061-shop-trade/)** |
| SHOP-TRAY-025 | **A trade move is command `0x22`, it carries a quantity, and the destination stamps ownership.** `FUN_0041c98f(srcCode, srcIdx, dstCode, dstPos, qty)` writes opcode `0x22` at `cmd+0x09` and the five fields at `+0x0c`, `+0x0e`, `+0x0d`, `+0x10`, `+0x12`. The dispatcher `FUN_004d5dd8`'s arm is a **generic** item move shared with the inventory screen: source `2` = the party backpack (`FUN_0050ebf9(player+0x7c, idx, qty)`), `1` = an equipment slot, `4` = the shop tray; destination `1`/`2`/`4` symmetrically, and for `4 <= cmd+0x0c <= 8` **and** `4 <= cmd+0x0d <= 8` it resolves a shop and calls `FUN_00506c87`, which reads `code−4 == 0` as the tray and otherwise indexes shelf `code−5`. `FUN_00506552` (shelf) clamps `qty` to the stack, does `stack -= (qty−1)`, detaches one unit through the item's `vt+0x40` and gives the detached object `qty`. **Amended by `ITEM-WHOLE-128`: `FUN_0050ebf9` (list) splits only when count exceeds requested quantity. At `0050ec4f/0050ec52`, count <= quantity branches to whole removal `0050ec86`, bypassing split `vt+0x40` at `0050ec73` and returning the same Item pointer; count one/request one is a concrete whole-item case.** The smaller-quantity branch retains the published split arithmetic. This correction does not remeasure the shelf helper or city-sale lifecycle. **The two sides of the tray are `elem+0x14`**: the dispatcher's destination-4 arm stores `item+0x14 = Player` (`004d69c4..004d69cd`) and nothing stamps it on the shelf→tray path, which is exactly the `== 0` that `SHOP-BUY-009` walks and the `!= 0` that `SHOP-SELL-010` walks. Clearing the table (`FUN_00506d8f`, opcode `0x35`) sends each element home by the same test and then `RemoveAll`s the tray. The tray container itself has **no capacity test anywhere** — `FUN_0050e902` merges into an equal stackable element or inserts, `FUN_0050ebf9` walks to an index, and neither reads a maximum | High (the opcode and all five field stores are immediates in one builder whose call sites are enumerated; the two range checks, the `code−4`/`code−5` arithmetic and the ownership store are named instructions; the split arithmetic is read in both containers, and the list helper's whole-removal branch bypasses the split virtual) | ● active (amended) | **[EXP-0061](../experiments/EXP-0061-shop-trade/)**, amended by [EXP-0285](../experiments/EXP-0285-whole-item-transfer/) |
| SHOP-TRAY-026 | **Two commit buttons, one guard — and the guard is why `SHOP-BUY-009`'s abort can never fire on the shipped client.** `FUN_004a95ec` walks the tray widget's display list once and produces three numbers: `+0x150` the purse, `+0x154` the **buy** total (negative, full price × quantity for every element whose container code is not 2) and `+0x158` the **sell** total (half price, for every element whose code is 2), plus `+0x15c` their sum. `FUN_004a93d4` (buy) sends opcode `0x33` only if `+0x154 != 0`, and only if `+0x150 + +0x154 >= 0`; otherwise it plays the merchant's refusal animation and posts nothing. `FUN_004a936a` (sell) sends `0x34` only if `+0x158 != 0`. `FUN_004a9497` sends `0x35`. Each of the three builders `FUN_0041cb6a`/`FUN_0041cbc4`/`FUN_0041cc1e` has **exactly one call site image-wide** and it is one of these three shop-view methods. **So `SHOP-BUY-009`'s Unknown closes in both halves at once**: a tray can hold up to five elements, so `FUN_005069d0` really is a loop — but because the client refuses a basket whose *total* exceeds the purse, and the server debits monotonically as it walks, an accepted basket makes every prefix affordable and the `JMP` past the iterator is unreachable. It is a guard against a client that lies, not a behaviour a player can produce | High (the three totals, the two gates and the three one-site senders are named instructions and completed `callto:` enumerations) / **Unknown** (whether the client's purse mirror `campaign+0x9b4 → +0x0c` can disagree with the server's `Player+0x38` — the accessor `FUN_00484060` behind both prices was not read, and multiplayer desync was not established; if it can, the abort becomes reachable) | ● active | **[EXP-0061](../experiments/EXP-0061-shop-trade/)** |
| SHOP-TRAY-027 | **The screen halves per unit; the shop pays per stack — so `SHOP-SELL-010`'s per-stack `ceil` is observable without splitting anything.** The client's sell total accumulates `ceil(price/2) × qty` per element (`004a9676` price, `004a967b..81` `(v+1)/2`, `004a9686 IMUL EAX,[EDX+0x10]`), while the payout `FUN_00506893` computes `ftol(0.5·(qty × price) + 0.5)` — one `IMUL` **before** the halving. On an odd unit price the displayed total therefore exceeds the coins received by exactly `⌊qty/2⌋`, which is the same delta `SHOP-SELL-010` derives for selling a stack one unit at a time. The buy arm of the same routine uses the same price accessor with no halving and agrees with the server, which is what makes `SHOP-TRAY-026`'s guard sound. *Origin:* the owner remembers an odd-price discrepancy of 0–1 on buy-back (`EXP-0058/evidence/owner-testimony.md`); this is a mechanism for it and is graded on the instructions alone | High (the two arms differ in exactly where the halving sits, four instructions each, and `__ftol` is already read as truncating — `SHOP-ROUND-017`) / **Medium** (the coin count: it assumes `FUN_004ae4a0` → `FUN_00484060(1)` returns the unit price `item+0x1c`, which is inferred from the buy arm's agreement with the server rather than read) / **Unknown** (whether a **partial-stack** move is reachable at all: the protocol and both containers carry and honour a quantity, but the tray's drop handler `FUN_004a65d0` passes the dragged element's whole `+0x10`, and the pick-up `FUN_00482710`'s `vt+0x84(element, arg2)` — `FUN_00482580` / `FUN_004a4c60` — was not traced to its caller) | ● active | **[EXP-0061](../experiments/EXP-0061-shop-trade/)** |
| SHOP-DUP-028 | *(amended by EXP-0225: enchantment separates equal-code non-stackable items, not Potions.)* **Randomly drawn stock is never deduplicated; the six literal-Potion additions and returned items do use the merging insert.** `FUN_00508671(a,b)` first compares item code, then both stackable → equal, exactly one stackable → unequal, neither stackable → equal only when their ordered effect lists agree. `FUN_00508635` defines stackable as `item+0x44 == 3 || effects.empty`, and `FUN_005081f1` assigns literal 3 to a `MagicItems` row whose name begins `Potion`. Thus two `SHOP-MAGIC-007` enchantments separate equal-code non-stackable equipment, while equal-code Potions compare equal even if their effects differ. The merging insert `FUN_005062e8` has 8 calls in 2 owners: six literal Potions inside `FUN_005073e2` and two returns in `FUN_00506aea`. Random draws from `FUN_0050b055` use plain `CObArray::Add`; the other merge routine `FUN_00506434` has 0 callers including `.rdata` slots. Randomly drawn equals remain separate, while each literal-Potion addition and each returned equal item uses merge. The screen-row merge remains Medium because its two add paths compare client words not pinned here to item fields | High for item equality, Potion classification and the complete merge/append caller populations / Medium for the screen presentation clause | ● active (amended) | [EXP-0061](../experiments/EXP-0061-shop-trade/), [EXP-0225](../experiments/EXP-0225-item-effect-grammar/) |
| SHOP-DOC-029 | **Shop generation cannot provide `Quest Documents` at campaign start.** A new campaign enters mission 10 without visiting town; mission 10 routes directly to mission 20, and the first stock generation occurs after mission 20. Live stock generation uses weapons, armor, shields, spells, and six literal potions. The MagicItems name-filter routine has no live caller. Its independent price exclusion also applies: `Quest Documents` has price -1 and every live pool floor is 0 | High for timing, live source families, and price exclusion (reproduced instruction paths and both-root data) | ✔ promoted | [EXP-0154](../experiments/EXP-0154-campaign-start/) |
| SHOP-SCREEN-030 | **The shop screen is five children of one view, and every rect is a literal in one builder.** `FUN_004a8634` (shop view vtable `0x0059ac88`, slot `+0x78`) allocates and constructs five widgets, each with `ctor(controlID, left, top, right, bottom, parent)`; every argument is an immediate `PUSH`. The five, with the field each is stored in: `+0x70` **the table**, id 1003, `(0, 303, 480, 390)`, `004a867d`/`004a8696`; `+0x6c` **the party backpack**, id 1001, `(0, 390, 480, 480)`, `004a86db`/`004a86f4`; `+0x68` **the shelf grid**, id 1002, `(0, 0, 164, 303)`, `004a8739`/`004a874f`; `+0x74` **the merchant panel**, id 1005, `(164, 0, 480, 303)`, `004a8794`/`004a87ad`; `+0x78` **the button panel**, id 1006, `(464, 0, 640, 238)`, `004a87f2`/`004a880b`. The object sizes are the `PUSH` before each allocation: `0x2108`, `0x20ec`, `0x2150`, `0x25c`, `0x100`. The rects are in the view's own coordinate space and each draw adds `view+0x08`/`view+0x0c` back in. **This corrects `EXP-0061/evidence/shop-screen-grid.json`, whose `roleFromItsOwnAssets` labels for `+0x68` and `+0x6c` are interchanged** — `SHOP-TRAY-024`'s prose has them the right way round and stands | High (six immediates per child in one routine whose five call sites are the only construction sites of these classes, `SHOP-TRAY-024`; and each region's own art matches its rect in pixels, `SHOP-SCREEN-032`) | ● active | [EXP-0164](../experiments/EXP-0164-shop-screen/) |
| SHOP-SCREEN-031 | **Each grid tiles 80x80 cells from its own first edge rect, and the three formulas differ in their offsets.** `vt+0xac` allocates `cols*rows` 16-byte `CRect`s and fills two edge rects first — `+0x20cc` and `+0x20dc` — then reads the tiling origin out of `+0x20cc`'s own `right` (`+0x20d4`) and `top` (`+0x20d0`) fields, which is why a `disp:` sweep finds those two displacements read and never written. **Table** (`FUN_004a5cd0`): `+0x20cc = (left, top, left+32, bottom)`, `+0x20dc = (left+432, top, right, bottom)`, cell `= (32+80c, 303+80r, 112+80c, 383+80r)` — five cells at x 32..432, y 303..383. **Backpack** (`FUN_004a5190`): the same two edge rects, cell top offset by 5 — `(32+80c, 395, 112+80c, 475)`. **Shelf grid** (`FUN_004a4090`): `+0x20cc = (46, 0, 118, 32)`, `+0x20dc = (46, 271, 118, 303)`, cell `= (1+80c, 31+80r, 81+80c, 111+80r)` with `index = row*cols + col` — six cells in two columns of three, x 1..161, y 31..271. `cols` is `(right-left)/80` **minus one** on the table and the backpack and not on the shelf grid (`SHOP-TRAY-024`), giving 5, 5 and 2 | High (every offset is an immediate in a `LEA` or an `ADD` in the routine that stores it, and the array these fill is the same one the shared hit test `FUN_004a2eb0` walks) | ● active | [EXP-0164](../experiments/EXP-0164-shop-screen/) |
| SHOP-SCREEN-032 | **Which shipped resource paints each region, and the one place the code asks for more pixels than the file has.** Each widget's `vt+0xb4` loads its own art by literal path: the table `graphics\interface\ShopTable.bmp` into `+0x20ec` (`004a5c7b`); the shelf grid `ShopArrow1/3/2/4.bmp` into `+0x20ec`/`+0x20f0`/`+0x20f4`/`+0x20f8` and `ShopInv.bmp` into `+0x20fc` (`004a3edc` … `004a3fd7`); the button panel `ShopButton1..4.bmp` into `+0x64` … `+0x70` and `ShopMenu.bmp` into `+0x74` (`004ac86a` … `004ac9aa`); the merchant panel `ShopFrame.256` into `+0x120`, `shopanim\ShopMain.bmp` into `+0x124` and `movies\shopanim\Pose2-3\1.bmp` into `+0x128` (`004aaaaf`/`004aab16`/`004aab5f`); the view itself `myitem.256`, `shopitem.256`, `costs1..7.bmp`, `costm1..7.bmp` and `backinvg/b/s.bmp` into `+0xd4`, `+0xd8`, the `CArray` at `+0xf0`, the `CArray` at `+0xdc` and `+0xc8`/`+0xcc`/`+0xd0` (`FUN_004a79c1`). **Measured** (`tools/shopscreen`, `evidence/shop-screen-art-en.csv`): `ShopInv.bmp` 164x303 = the shelf grid's rect; `ShopFrame.256` 316x303 = the merchant panel's rect, drawn at its origin (`004ab6e1`); `ShopMenu.bmp` 176x238 = the button panel's rect; `ShopArrow1..4.bmp` 72x32 = both arrow rects; `ShopMain.bmp` 288x288 drawn at `(169, 8)` (`004ab735`); `backinvg/b/s.bmp`, `myitem.256` and `shopitem.256` all 80x80 = one cell. **The exception is the table**: `FUN_004a5e70` computes the blit's source width and height from the widget rect (`004a5e9b`/`004a5ea7`), asking for **480x87**, and `ShopTable.bmp` measures **472x87** — an 8 px overrun, on both roots at the same payload hash | High (each path is a single-site `PUSH` whose store offset is the next instruction, and the pixel sizes are read from the shipped DIB headers) / **Medium** (that the overrun is visible rather than absorbed by the blitter's own clipping: the blit routine `vt+0x38` was not read) | ● active | [EXP-0164](../experiments/EXP-0164-shop-screen/) |
| SHOP-SCREEN-033 | **The shelf grid scrolls, by whole rows, from two arrow rects, and it is the only one of the three that scrolls.** `FUN_004a4590` (`vt+0x50`) and `FUN_004a46c0` (`vt+0x54`) test the pointer against `widget+0x20cc` = `(46, 0, 118, 32)` and `widget+0x20dc` = `(46, 271, 118, 303)` with USER32 `PtInRect` (`[0x00632f6c]`), in view-relative coordinates. A hit on the upper rect calls `FUN_004820d0` **twice** (`004a45d1`, `004a45d8`), a hit on the lower one `FUN_00482010` twice (`004a4646`, `004a464d`); the grid is two columns wide and indexed `row*cols + col`, so two steps is one row. Either hit then plays the click sound through `FUN_00453b08`, and only if none is already playing. A miss on both falls through to the base item click `FUN_004a3770` (`004a47ed`). Neither of the other two grids has an arrow rect: the table's scroll base is never written at all (`SHOP-TRAY-024`), and the backpack's `+0x90` points at the shown party member's own `+0xdc`, whose writers were not enumerated here | High (the two rects, the two doubled calls and the fall-through are named instructions; that the arrows are 72x32 is confirmed by `ShopArrow1..4.bmp` measuring 72x32) / **Medium** (that the doubling means one row rather than two independent single steps: the step routine's body was not read) | ● active | [EXP-0164](../experiments/EXP-0164-shop-screen/) |
| SHOP-SCREEN-034 | *(refuted in part by EXP-0173: the four rects below are the shelf DRAW rects, listed in the reverse of the index order; the hit loop reads a different array, and the folder is `4 - index` - see `SHOP-SHELF-047` and `retracted.md`. The loop bound, the rect stride, `FUN_004abcc3`, `view+0x132` and the container code are untouched.)* **The four shelves are clicked in the room picture, and each hit rect is exactly the size of its own animation.** `FUN_004a9ccf` builds ten rects with `FUN_00447cc0(left, top, right, bottom)` at panel-relative offsets and copies each into `panel+0x60 + 0x10*i`. The first four are `(201, 20, 313, 108)`, `(313, 20, 445, 108)`, `(197, 108, 277, 220)` and `(353, 108, 433, 220)` — 112x88, 132x88, 80x112, 80x112. The four shipped animation folders `graphics\interface\shopanim\01..04` measure **112x88, 132x88, 80x112, 80x112**, 11 frames each, in the same order. `FUN_004abf0c` (`vt+0x54`) walks `i = 0..3` over those rects (`004ac0f9`) and on a hit calls `FUN_004abcc3(i)`, then the merchant's Yes animation `FUN_004aae0f` and the grid binding `FUN_004a4550`. `FUN_004abcc3` returns 0 if that shelf is already open; otherwise it sets the previous shelf's animation frame to 9 (`004abd3c`), marks bit `i` in `panel+0x240`, loads `shopanim\%.2d\%d.bmp` for `i` (`FUN_004aac94`), sets frame 0, stores `view+0x132 = i` (`004abde8`) and **zeroes the shelf grid's scroll base** (`004abdfe`). `view+0x132` is initialised to **`0x64`** at `004a8e7e`, which is outside `0..3` and is the no-shelf-open value; the shelf grid's container code is `view+0x132 + 5` (`SHOP-TRAY-024`), so with no shelf open it reports 105, outside the server's `4..8` range | High (the four rects are immediates, the loop bound and the rect stride are named instructions, and each rect's size is independently matched by its own shipped animation's pixel size) | ● active (partly refuted) | [EXP-0164](../experiments/EXP-0164-shop-screen/) |
| SHOP-SCREEN-035 | **The button panel is four rects, four numbers and four commands.** `FUN_004ac1ff` stores eight rects at `panel+0x78` in steps of 16: `(494, 15, 614, 67)`, `(483, 67, 623, 113)`, `(483, 114, 623, 160)`, `(494, 160, 614, 212)`, then `(515, 15, 590, 35)`, `(494, 35, 635, 67)`, `(494, 160, 614, 192)`, `(515, 192, 590, 212)`. `FUN_004adaa4` returns the index of the first of the first four that contains the point, accepting `+0xb8`/`+0xc8` as well for index 0 and `+0xd8`/`+0xe8` for index 3. `FUN_004ad8df` dispatches through the jump table at `0x004ada6f`, whose four entries read `0x4ad9cf, 0x4ad9dc, 0x4ad9e9, 0x4ad9f6` in the image: **0** clear the table (`FUN_004a9497`, opcode `0x35`), **1** buy (`FUN_004a93d4`, `0x33`), **2** sell (`FUN_004a936a`, `0x34`), **3** clear the table (`FUN_004a9497`) and then `FUN_004a954e`, which sends the leave through the builder `FUN_0041dfa6` on `view+0x80` when `view+0x84` is set (`004a9572`), clears the table again (`004a957a`) and posts message `0x445` through `vt+0x48` (`004a9583`); the caller then posts `0x42e` when `app+0x3dc == 2` (`004ada1c`). The opcode `FUN_0041dfa6` writes was not read; EXP-0061's `evidence/shop-opcodes.json` names `0x36` as the server's `Shop::Leave` arm. `+0xf8` holds the pressed index and `+0xfc` the hovered one, both `-1` for none; `FUN_004ada7f` resets both. The paint `FUN_004acb50` draws one number per rect: `view+0x150` the purse on rect 0, `view+0x154` the buy total on rect 1, `view+0x158` the sell total on rect 2, `view+0x15c` their sum on rect 3 — the four quantities `FUN_004a95ec` computes (`SHOP-TRAY-026`). `ShopButton1..4.bmp` measure 120x52, 140x46, 140x46, 120x52, matching the four rects in load order. **Amended by [EXP-0204]: the panel also draws a caption beside every number.** This row is silent on captions, not a claim of their absence — `SHOP-050` traces the same paint routine's caption draws: rects 0 and 3 draw the caption and the number as two separate calls, rects 1 and 2 concatenate them into one string | High (the eight rects, the loop, the jump table's own bytes and the four number reads are all read; the three commit senders are `SHOP-TRAY-026`'s completed enumerations) / **Medium** (the pairing of button 1 with `ShopButton1.bmp`: rects 0 and 3 are the same size and rects 1 and 2 are the same size, so the pairing rests on the store order of the two constructors and not on the sizes) | ● active (amended) | [EXP-0164](../experiments/EXP-0164-shop-screen/), **[EXP-0204](../experiments/EXP-0204-interface-census-and-panel-text/)** |
| SHOP-SCREEN-036 | *(amended by EXP-0168: the Medium clause is closed — `FUN_00460440` was read, and it is the class usability test of `SHOP-USABLE-040`.)* **One routine paints every cell of all three grids, and it chooses between five backgrounds before it draws the icon.** `FUN_004a2fe0` (`vt+0x2c`, not overridden by any of the three) walks `i = 0 .. cols*rows-1`, takes the element at `list[i + *(widget+0x90)]`, and draws at `(view.left + cell.left, view.top + cell.top)`. The background: **(a)** an element whose `+0x6` is `0xffff` is the money element — its `+0x10` is re-read from `campaign+0x9b4 -> +0x0c` (`004a312a`) and it draws the two shared graphics `[0x005ef954]` and `[0x005ef998]`; **(b)** quantity `+0x10 == 0` draws `view+0xc8`, `backinvg.bmp`; **(c)** an element whose container code `+0x18` is in `5..8` — a shop shelf — draws `view+0xd0`, `backinvs.bmp`, when the purse `view+0x150` is at least the unit price **and** `FUN_00460440(view+0x10c[view+0x130], element)` is true; **(d)** otherwise, when that test is true, `[0x005ef954]`; **(e)** otherwise `view+0xc8`. `[0x005ef954]` is `graphics\interface\backinv.bmp`, loaded by the shared interface loader `FUN_00469f80` at `0046a680` and stored at `0046a699`. The item's own icon is then blitted from `widget+0x60[i]` (`004a3229`), and `element+0x8 & 0x20` adds an overlay through `FUN_00482a70`. The quantity is drawn only when `+0x10 > 1`, as `"%d"` at `(cell.left + 10, cell.bottom - 15)` (`004a32c9`, `004a32e1`), through the text renderer `FUN_00456b50` with the width table `0x5e9b88` | High (the five arms, their conditions and their sources are named instructions in one routine read end to end; `backinv.bmp` is identified by the standard load-then-store idiom in `FUN_00469f80`) / **Medium** (that `FUN_00460440` is a usability test for the shown party member: it takes `view+0x10c[view+0x130]` and the element, and `view+0x130` is the index the backpack binding uses, but the routine itself was not read — now read, `SHOP-USABLE-040`) / **Unknown** (what `[0x005ef998]` is: `FUN_00469f80` names 40 files and this store was not tied to one) | ● active (amended) | [EXP-0164](../experiments/EXP-0164-shop-screen/) |
| SHOP-SCREEN-037 | **The price plaque is chosen by the price's digit count and by which side of the deal the element is on, and the figure printed on it is the figure the player transacts at.** In `FUN_004a2fe0`, for every element with `+0x6 != 0xffff` and `+0x10 != 0`: `price = FUN_00484060(element, 1)`; `FLDLG2 ; FILD price ; FYL2X ; __ftol` (`004a335f` … `004a3367`) gives `d = floor(log10(price))`; then `element+0x18 == 2` — the party backpack, the sell side — selects `costm(min(d, count-1) + 1).bmp` from the `CArray` at `view+0xdc`, and anything else selects `costs(…).bmp` from the `CArray` at `view+0xf0` (`004a338b` … `004a33bb`). The plaque is right-aligned to `view.left + cell.right` by subtracting its own width (`004a33cb`/`004a33d1`) and drawn at `y = view.top + cell.top + 1`. The number printed is `(price + 1) / 2` when `+0x18 == 2` and `price` otherwise (`004a3422` … `004a3439`). **Corpus:** `costs1..7.bmp` and `costm1..7.bmp` are 14 files, all **60x10**, byte-identical on both roots. **Two consequences.** The seven plaques a side are one per digit count 1..7, which is the digit count of `SHOP-ROUND-017`'s `9 999 999` clamp, so `SHOP-TRAY-024`'s "seven plaques per side for the seven digits" is the right count for the right reason. And `SHOP-TRAY-027`'s per-unit halving is visible **per cell** as well as in the total: an odd-priced stack on the player's side of the table shows `ceil(price/2)` a unit while the shop pays `ceil(qty x price / 2)` for the stack. This also raises that row's Medium clause — `FUN_00484060(element, 1)` returns a value whose base-10 digit count indexes a seven-entry array sized by the price clamp, which no non-price quantity would fit | High (the selection arithmetic, both array bases, the right-alignment and the two price arms are named instructions; the 14 measured plaque sizes are read from the shipped DIB headers) | ● active | [EXP-0164](../experiments/EXP-0164-shop-screen/) |
| SHOP-SCREEN-038 | **The shop screen's art is identical on the EN and RU installs; only two text files differ.** `tools/shopscreen` resolves all 34 literal paths the shop routines name, plus the whole `graphics\interface\shopanim` and `movies\shopanim` subtrees, against each root's own archives — the first path component names the archive, so `graphics\interface\X.bmp` is node `interface/X.bmp` inside `graphics.res` — and records archive, node size, pixel geometry and a payload SHA-256 for each. **134 resources on each root. 132 have identical size, identical pixel geometry and identical payload hash.** The two that differ are `main\text\tips\shop1.txt` (EN 208 bytes, RU 150) and `main\text\tips\shop2.txt` (EN 222, RU 183), loaded by `FUN_004a8bc3` (`004a8c8d`) and `FUN_004a914e` (`004a91b9`). The merchant's own dialogue, `shop\npc31m%d` (`004a90db`), resolves to `text/shop/npc31m{31,91,110,130}.txt`, which also differ. **So no shop-screen graphic is localised, and every rect in `SHOP-SCREEN-030` … `035` holds on both roots** | High (a payload hash over a complete, path-driven population on both roots; the instrument's blind spot is a resource the screen loads by a path this experiment did not enumerate, which is bounded by the `StrDump` sweep of every `shop`-, `font`-, `infowindow`- and `tips`-containing literal in `rom.exe`) | ● active | [EXP-0164](../experiments/EXP-0164-shop-screen/) |
| SHOP-SCREEN-039 | **The stock grids print exactly two text runs a cell — the quantity and the price — and no item name or characteristics panel was found on this screen.** What was searched: every `CALL 0x00456b50`, the text renderer both cell texts use, inside `004a2000..004ad200`, over a `DisasmRange` dump of those 45568 bytes whose `range:` census reads function bytes 44367, orphan bytes 23, undisassembled 1180 — **2 hits, both in `FUN_004a2fe0`**, the quantity at `004a32e1` and the price at `004a3499`; every reference to `graphics\infowindow\%s.bmp` (`0x5beae8`) and `graphics\infowindow\%s%d` (`0x5b89e0`) image-wide on the repaired table — 4 hits over 3 owners, `FUN_0047d4f0`, `FUN_00491930` and `FUN_00421b46`, **none inside the shop UI range**; and `FUN_004a2fe0` itself, read end to end. No item-name draw and no info-window construction were found in the three grids. **Bounds:** the merchant panel draws text through a different call form — `FUN_004ab37e` loads the width table `0x5e9b88` into a local at `004abb84` and `004abc68` — so this claim covers the item grids and not that panel; and a renderer other than `FUN_00456b50` reached from the grids would not appear in the count | Medium (an absence over one address range on one call form, with both blind spots named; the positive half — that the two texts are the quantity and the price — is High and read at instruction level) | ● active | [EXP-0164](../experiments/EXP-0164-shop-screen/) |
| SHOP-USABLE-040 | **`FUN_00460440` is a class usability test, it is the same routine the equipment doll refuses a drop with, and on the shop screen it changes nothing but the cell background.** This closes `SHOP-SCREEN-036`'s Medium clause, which graded the routine's purpose without reading it. The routine is 13 instructions at `00460440`..`00460473` and returns `((element+0x08 & 2) && !(member+0x18c & 2)) \|\| ((element+0x08 & 4) && (member+0x18c & 2))`; `member+0x18c` bit 1 is the mage flag and `element+0x08` bits 1 and 2 are the item's `sutableFor` column (`ITEM-WEAR-055`). The shop passes `view+0x10c[view+0x130]`, the shown party member, exactly as that row read it. What the shop does with the answer is arm selection only: `004a31e7 JZ` picks `view+0xc8` (`backinvg.bmp`) instead of `[0x005ef954]` (`backinv.bmp`), which is the same background arm b gives a cell of quantity 0, and the item's icon is blitted either way at `004a3229`. **Nothing else on the shop screen calls the predicate**: `EnumRefs callto:460440` is 2 hits over 2 owners with 0 in orphan code, and the other owner is `FUN_00492410`. In particular the purchase path does not consult it, so an item the shown member cannot use is drawn grey and can still be bought | High — the routine is read whole, the background arms are two named instructions in a routine already read whole by `SHOP-SCREEN-036`, and the call site set is a whole-image enumeration on a routine both of whose callers reach it by direct `CALL` / **Medium** for the purchase being unaffected: it rests on the predicate having no other caller, not on the buy handler having been read end to end | ● active | **[EXP-0168](../experiments/EXP-0168-wear-rule/)** |
| SHOP-FIGURE-041 | **The shop screen's sixth region is not built by the shop: it is the mission screen's character panel, borrowed for the visit.** `FUN_004a8634` constructs exactly five children and then zeroes twenty fields; it contains no sixth `ctor(id,l,t,r,b,parent)` call. Activation `FUN_004a8bc3` instead takes `campaign+0xe0` into `view+0x7c` (`004a8c6a`), removes it from `campaign+0xd4` (`004a8da8`), reads its rect (`FUN_00448bf0`, `this+0x08`), offsets it by `640 − width` — the literal `0x280` at `004a8dc5` — through `OffsetRect` (`004a8deb`; the wrapper `FUN_004ade70` calls `[0x00632f7c]`, which the image's own import table names `OffsetRect`, and `callto:4ade70` is 2 hits / 2 owners / 0 orphan — these two sites), re-sets it (`004a8dfa`) and adds it as a child of the shop view (`004a8e09`). Deactivation `FUN_004a89a6` is the exact inverse: `width − 640` (`004a8a96`), `OffsetRect` (`004a8aba`), `RemoveChild` (`004a8ad8`), `campaign+0xd4 . AddChild` (`004a8aed`), `view+0x7c = 0`. `campaign+0xe0` is built once, at `00472217` in the main-frame builder, with the same six-immediate convention: `ctor(id 7, 0, 238, 160, 480)`; `EnumRefs callto:490a40` = 1 hit / 1 owner / 0 orphan. `(0,238,160,480)` offset by 480 is **`(480,238,640,480)`** — exactly the region no decoded child covers, and the six regions then tile 640x480 with one overlap, the button panel over the merchant panel's right 16 columns. Its constructor `FUN_00490a40` writes vtable `0x0059a768` and allocates **two 160x240 surfaces** into `+0x74` and `+0x78` (`00490ac1`, `00490af7`). The main-frame builder stacks four widgets in that 160-wide column — ids 5, 6, 7, 8 at `(0,0,160,158)`, `(0,158,160,238)`, `(0,238,160,480)`, `(0,480,160,screenH)` — which is the 160-pixel right strip `SESS-VIEW-028` measures from the map view's side; **only id 7 is named by any instruction in the shop routines** | High (the transfer, the offset arithmetic and its inverse are named instructions in two routines read whole; the panel's rect is six immediates at a construction site a whole-image `callto:` shows is the only one; the offset arithmetic is re-derived from raw PE bytes by `tools/shopfigure -mode rects`, which shares no code with the disassembler) | ● active | **[EXP-0173](../experiments/EXP-0173-shop-figure/)** |
| SHOP-FIGURE-042 | **What that panel draws is the shown character composed in its own equipment, through the same compositor the world view and the dialogue panel use.** `FUN_00491930` (`vt+0x2c` of `0x0059a768`) branches on `member+0x18c & 0x11` (`00491dc8`). The set side calls virtual slot `+0x80` of the member's own drawable at `00491ed6` with three surface arguments — a stack stencil, `panel+0x74` and `panel+0x78` — which is the slot, the argument count and the 160x240 canvas geometry `DLG-FIGURE-020` identifies as `FUN_0045ed10`; `DLG-FIGURE-021` shows that compositor is `RET 0xc` with no layer selector, so no caller can exclude a layer and the head is drawn. The composed canvas is blitted whole at `(panel.left, panel.top + 2)`, 160x240, at `00491d9c` and `00492038` — at the shipped resolution `(480,240)`-`(640,480)`. Composition is **cached**: `panel+0x7c` holds a string built as `"%s%s"` of a temporary directory and `"allods<runtimeId>.$$$"` off `member+0x04` (`00491df1`), and the routine recomposes when that differs from the stored key or when `member+0x18c` bit 3 is set (`00491e86`) — the bit the shop sets at activation (`004a8f73`) and on every member change. The zero side of the `& 0x11` test is a different model in the same routine: it formats a name off `[0x005eb674][member+0x20]` (`00491ee6`) and draws a flat picture | High for the mechanism (the branch, the three surface arguments, the virtual slot and the blit are named instructions in one routine read at instruction level, and the slot is independently identified by `DLG-FIGURE-020` from a different caller) / **Medium** for what a player sees, which needs a running original: the compositor itself was not re-read here, and which members take the flat arm was not established | ● active | **[EXP-0173](../experiments/EXP-0173-shop-figure/)** |
| SHOP-PICKER-043 | **The party picker belongs to the borrowed panel, not to the shop, and one step rebinds the figure and the item strip together.** `EnumRefs imm:414` and `imm:415` over the whole image return one message-immediate site each, both in `FUN_00491330`, the panel's `vt+0x58` (`00491620`, `00491661`). That routine maps the panel's own rect through `FUN_004bccb3` and tests three 32x32 rects with the `[0x00632f6c]` point-in-rect import: panel-relative `(1,205,33,237)` posts **`0x414`** and `(119,205,151,237)` posts **`0x415`**, both gated on `campaignScreen+0x3dc & 0x226` and **amended by [EXP-0230], both selecting registry slot 1 (`click00`) through `[0x005f1b84][1]` and passing that sample in `ECX` to `FUN_00453b08` first; the pushed `0xdc` is its priority/category byte, not the sound or slot**; a third, `(126,206,158,238)`, posts `0x416` elsewhere under `& 1`. **Amended by [EXP-0210]: `FUN_00491330` tests six rects, not three.** This row's "tests three 32x32 rects" is accurate for the three 32x32 rectangles it names and is not a claim about the routine's whole population; `TOWN-346` enumerates all six, the other three being 28x40, 28x36 and 32x36. At 640x480 the two picker rects are `(481,443,513,475)` and `(599,443,631,475)`. The shop view's table (`FUN_004a7193`, ids `0x402..0x46d`, 108 entries, 101 on the default arm, 7 handled) routes `0x415` to `FUN_004a8154` (next: `view+0x130 += 1`, reset to 0 at the roster's upper bound, `004a81b9`) and `0x414` to `FUN_004a82c5` (previous: `−= 1`, or the last index when already 0, `004a8329`); `callto:` on each is 1 hit / 1 owner / 0 orphan. The roster is the `CArray` at `view+0x108`, copied at activation from `[0x005ea0c0]` (`004a8e13`..`004a8e2c`), and `view+0x130` is a `u16` index into it. Both routines then do the same eight things: post `0x405` to `view+0x80` before the index moves; call the new member's `vt+0x14(1)`; `FUN_004190f0(view+0x80)`; bind the backpack grid's list through `vt+0x90(member+0xc8)`; store `[view+0x6c]+0x90 = member+0xdc`; `FUN_004a2cd0` then `FUN_004a2fa0` on that grid; `member+0x18c \|= 8`; `view+0x14c \|= 9`. So **the grid at `(0,390,480,480)` is the shown member's own container, not a party-wide one**, and it changes in the same call that makes the figure recompose — which closes `SHOP-SCREEN-033`'s open clause about that grid's `+0x90` writers and narrows `SHOP-TRAY-024`'s "party backpack" label | High (two whole-image message-immediate censuses with one hit each, two completed `callto:` enumerations, and both switch routines read end to end at instruction level; the message table is re-derived from raw PE bytes by `tools/shopfigure -mode msg` on both roots) / **Medium** for the two rects being drawn arrows rather than invisible hot spots: no art was tied to either rect. **`TOWN-354` answers this clause**: the rectangle-to-art binding for the panel's six rects, including these two | ● active (amended) | [EXP-0173](../experiments/EXP-0173-shop-figure/), **[EXP-0210](../experiments/EXP-0210-figure-panel-controls/)**, **[EXP-0230](../experiments/EXP-0230-non-music-sfx-events/)** |
| SHOP-VIEW-044 | **The shop screen is a fixed 640x480 panel centred on the display, and the borrowed panel's placement is pinned to 640 rather than to the display.** The shop view is `campaign+0xf0`; `EnumRefs callto:` over its four constructors finds exactly one construction site image-wide, `0047235f`, with `ctor(id 1000, [0x005ea210], [0x005ea214], [0x005ea218], [0x005ea21c], parent 0)`. `refto:` on those four globals gives **one writer each, all four in `FUN_00471790`**: `[0x005ea210] = (screenW − 640)/2` (`00471972`), `[0x005ea214] = (screenH − 480)/2` (`0047198c`), `[0x005ea218] = screenW − (screenW − 640)/2` (`0047197f`), `[0x005ea21c] = screenH − (screenH − 480)/2` (`004719bc`). So `view+0x08`/`view+0x0c` are `(0,0)` on the shipped 640x480 default — every rect in `SHOP-SCREEN-030`..`035` is then also a screen coordinate — `(80,60)` at 800x600 and `(192,144)` at 1024x768. The character panel's re-parenting uses the literal `0x280` and not that origin, so it lands at view-relative `640 − width` whatever the display is. Two fills also run: activation clears the whole screen with `FUN_0044f990([0x005ea200], [0x005ea204], [0x005ea208], [0x005ea20c], 0)` at `004a8ff4`, where the first two globals have one writer each in `FUN_0044bff0` and both write 0 (`0044c026`, `0044c007`); and the view's `vt+0x30` `FUN_004a8059` fills `(rect.left, rect.top + 388, rect.right + 464, rect.bottom + 392)` when `view+0x148` is set, with a colour packed from three zero channel values through the display's own shift and width globals, so 0 in any pixel format — `FUN_0044f990` clamps `x` to `[0x005e4408, 0x005e4410]` and skips rows outside `[0x005e440c, 0x005e4414)`, so the oversized right and bottom are clipped to the screen | High for the origin (four globals with one writer each, one construction site by a completed `callto:`, and the arithmetic is four named instructions) / **Medium** for the second fill's effect: the three constants 388, 464 and 392 have no read explanation, and the region they cover at 640x480 is already covered by two children | ● active | **[EXP-0173](../experiments/EXP-0173-shop-figure/)** |
| SHOP-TIP-045 | **The two tip texts are one widget of a different class, drawn inside the merchant panel, and the second replaces the first at most once.** When `[0x005eb52c] != 0` (`004a8c6d`), activation loads `main\text\tips\shop1.txt` (`004a8c8d`), allocates `0x74` bytes and constructs `FUN_004c7422(id 1011, left 0, top 162, right 312, bottom 298, text)` — `RET 0x18`, vtable `0x0059ba18` — stores it at `view+0x88` (`004a8cf1`) and hands it to **the merchant panel** (`view+0x74`) as a child, not to the view (`004a8d07`). Merchant-panel-relative `(0,162,312,298)` is view-relative `(164,162,476,298)`, over the room picture's lower half. `FUN_004a914e`, reached from the `0x402` arm at `004a72c1`, replaces the text with `main\text\tips\shop2.txt` (`004a91be`, installed by `FUN_004ae440` at `004a91d3`) behind the latch `view+0x8c` (`004a9190`, set at `004a919c`). `refto:5eb52c` is 16 hits over 16 owners with two writers, `0043d415` (`= 1`) and `004c7a8f`, plus four address-taking sites in `0x0043d4xx`..`0x0043e9xx`; with it clear the shop draws no tip at all. This closes `SHOP-SCREEN-038`'s open half about where the two files are drawn | High for the widget, its rect, its parent and the latch — six immediates at one construction site plus three named stores, re-derived from raw PE bytes by `tools/shopfigure -mode rects` / **Unknown** for what sets `[0x005eb52c]` from the user's side: the four address-taking sites were not read | ● active | **[EXP-0173](../experiments/EXP-0173-shop-figure/)** |
| SHOP-MERCHANT-046 | **The merchant is placed by two immediates in the paint, which is why no rect matches him.** `FUN_004ab37e`, the merchant panel's paint, draws `merchantPanel+0x128` — `movies\shopanim\Pose2-3\1.bmp`, the file `SHOP-SCREEN-032` names — at `(panel.left + 113, panel.top + 112)` (`004ab992`, `004ab9a3`, `004ab9c2`). With the panel at `(164,0)` that is **`(277,112)`**, and the file measures **76x176** on both roots, so the merchant occupies `(277,112)`-`(353,288)`. An animated arm draws `merchantPanel+0x210[merchantPanel+0x254]` at the same anchor when `merchantPanel+0x240 & 0x40` is set (`004ab922`, `004ab933`, `004ab96c`), so the Yes and No poses replace the static one in place. The same routine's two other draws pin the coordinate convention: `+0x120` (`ShopFrame.256`) at the panel origin and `+0x124` (`ShopMain.bmp`) at `(left + 5, top + 8)` = `(169,8)`, which is the figure `SHOP-SCREEN-032` already publishes. The nearest rect, `merchantPanel+0xe0` at `(274,110,354,295)`, contains the sprite and is not read by the shelf hit loop | High (three named instructions for the anchor, two for the convention that reproduces an already-published number, and the pixel size is read from the shipped DIB header on both roots) | ● active | **[EXP-0173](../experiments/EXP-0173-shop-figure/)** |
| SHOP-SHELF-047 | **The merchant panel has TWO four-rect arrays and they are not the same: one is where a shelf is drawn, the other is where it is clicked.** `FUN_004a9ccf` builds twelve `CRect`s at `merchantPanel+0x60` in steps of `0x10` — two vector-constructor runs of four at `+0x60` and `+0xa0` (`004a9d2e`, `004a9d48`) and four singles at `+0xe0`..`+0x110` — each filled by `FUN_00447cc0(left, top, right, bottom)` at offsets from the panel's own top-left `(164, 0)`. The shelf **hit** loop reads `panel + 0x60 + 0x10 i` (`004ac0f9`); the shelf **draw** loop reads `panel + 0xa0 + 0x10 i` for `x` and `+0xa4 + 0x10 i` for `y` (`004ab7cb`, `004ab7b4`); and the art loader `FUN_004aac94` names the folder as **`4 − i`** (`004aacdd`, `004aace2`). Index by index: `i=0` hit `(354,110,459,295)` draw `(353,108,433,220)` 80x112 folder `04` 80x112; `i=1` hit `(169,110,274,295)` draw `(197,108,277,220)` 80x112 folder `03` 80x112; `i=2` hit `(314,5,454,105)` draw `(313,20,445,108)` 132x88 folder `02` 132x88; `i=3` hit `(172,5,314,105)` draw `(201,20,313,108)` 112x88 folder `01` 112x88. **This corrects `SHOP-SCREEN-034`**, whose four rects are the draw rects, are listed in the reverse of the index order, and are named as the hit rects. The size agreement that carried that row's grade survives and is exact — but only once the `4 − i` map is applied, which the row does not have. Everything else in it is unaffected | High (the two loop bases are named instructions with the same `SHL reg,4` stride, the twelve rect fills are read in store order at instruction level, the folder arithmetic is two named instructions, and the four folder sizes are measured from shipped DIB headers) | ● active | **[EXP-0173](../experiments/EXP-0173-shop-figure/)** |
| SHOP-MONEY-048 | **The money cell's second graphic is a coin sprite exactly one cell in size.** `[0x005ef998]` is written once, in the shared interface loader `FUN_00469f80` at `0046a9cd`, by the same load-then-store idiom that produces `[0x005ef954]`: `PUSH 0x5bd254` = `graphics\interface\money\money.16a` (`0046a9b1`), ctor `FUN_0042b920`, then `FUN_00428ad0(0x10, 4, 0)`. Measured on both roots by `tools/shopfigure -mode art`, `money.16a` is **5 596 bytes, one frame, 80x80** — the same size as `backinv.bmp` and as every item icon — and it is a `.16a`, so under `SPR16A-ALPHA-025` it carries a per-pixel level and composites over what is already in the cell. `refto:5ef998` is 4 hits over 4 owners with 0 in orphan code: the one write and three reads, `FUN_004a2fe0` at `004a3141` (`SHOP-SCREEN-036`'s money arm), `FUN_0046aba0` at `0046ae8c` and `FUN_00482e70` at `00483254`. So arm (a) of `SHOP-SCREEN-036` draws `backinv.bmp` and then a coin over it. **Scope:** the money *cell* is one 80x80 cell of an item grid selected by `element+0x6 == 0xffff`; the shop screen's four money **numbers** are a different region — the button panel's four rects, painted from `view+0x150`..`+0x15c` by `FUN_004acb50` (`SHOP-SCREEN-035`) — and `[0x005ef998]` reaches neither them nor that panel | High (one writer and three readers over a whole-image reference enumeration with a never-blank attribution, the path is the operand of the store's own load, and the geometry is read from the shipped container header on both roots at equal payload size) | ● active | **[EXP-0173](../experiments/EXP-0173-shop-figure/)** |
| SHOP-LIMIT-049 | **(G2) What this decode limits, and what lifting each limit costs.** Engine-class, all literals in `rom.exe`, none of which changes a shipped file's bytes: the shop screen is a fixed 640x480 panel centred at `((screenW−640)/2, (screenH−480)/2)`; the borrowed character panel is placed at view-relative `640 − width` from the literal `0x280`; its two composition canvases are 160x240; the figure is blitted at panel `+0`, `+2` at 160x240; the two picker rects are 32x32 at panel `(1,205)` and `(119,205)`; the roster index `view+0x130` is a `u16`; the tip widget's rect is `(0,162,312,298)` inside the merchant panel and its text is replaced once per activation behind `view+0x8c`; the merchant's anchor is `(+113, +112)` from the merchant panel. Data-class, where lifting the limit does change archive bytes: the money cell's coin is one 80x80 frame in `graphics\interface\money\money.16a`, and the shelf count is four with one shipped animation folder each, addressed `4 − i`, so a fifth shelf needs a fifth folder. **The seam that matters for a reimplementation is the borrowed panel**: the shop screen and the mission screen show the same widget object at two positions, so a build that gives the shop its own copy diverges from the original the moment the panel carries state — and it does, in `panel+0x7c`, the composition cache key | High for the enumeration of engine literals, each cited above at its own instruction / **Medium** for the seam being load-bearing: the panel's in-mission surface was not decoded, so what else it carries across the boundary is not known | ● active | **[EXP-0173](../experiments/EXP-0173-shop-figure/)** |
| SHOP-050 | **The button panel draws a caption beside every number, not the number alone, and on two of the four rects the caption is joined to the number as one string rather than drawn as two.** `FUN_004acb50` calls `FUN_00423220` with `this=0x5eb3d0` (raw disassembly, `MOV ECX,0x5eb3d0` immediately before `CALL 0x00423220`, all 8 sites) and ids `0x48`, `0x46`, `0x47`, `0x49` for rects 0-3 respectively (source order, `evidence/004acb50_FUN_004acb50.c`), each id pushed as a literal (`PUSH 0x48` before the first call site, `evidence/disasm-4acb50-full.txt`) and used twice — once in the routine's unconditional pass and once inside a `switch` on the pressed-and-hovered rect index (`this+0xfc`), gated the same as `TOWN-260`'s highlight-bitmap blit in this same routine. `tools/strtabprobe`, extended to ids `0x46`-`0x49`, resolves the four against `text/main.txt` on both roots: `0x46`="Buy"/"Купить", `0x47`="Sell"/"Продать", `0x48`="Undo"/"Отмена", `0x49`="Exit"/"Выход" (CP866 decode). The rect-to-id pairing matches `SHOP-SCREEN-035`'s own jump table exactly: rect 0 = Undo/clear-table (opcode 0), rect 1 = Buy (opcode 1), rect 2 = Sell (opcode 2), rect 3 = Exit (opcode 3). Composition, read from the routine's own local-variable structure: on rect 0 and rect 3, the caption is set as the current string (`FUN_00572b52` or `FUN_00572a2a`+`FUN_00572b03`) and drawn through `DAT_005e9bc0+0x14` alone, then the number (formatted by `FUN_0056f3d4`+`FUN_00468f60` from `this+0x150`/`+0x15c`) is drawn through a second, separate call to the same primitive. On rect 1 and rect 2, the number is formatted first, then concatenated with the caption through `FUN_0056ee34` (pad to width `0x20`) and `FUN_00572bfb`, and the single combined string is drawn through one call to the primitive. The `switch` block mirrors this exactly, case by case, for whichever rect is currently pressed-and-hovered — the same two-separate-draws-versus-one-combined-draw split recurs identically in an independently coded block. This corrects `SHOP-SCREEN-035`'s "draws one number per rect": every rect draws a caption, and two of the four rects draw the caption and the number as one joined string rather than as two separate ones | High for the ids, the `this`-pointer (raw-disassembly-verified), the string-table resolution on both roots and the rect/opcode correspondence (each read at instruction level or corroborated by an independently published jump table); High for the caption/number composition (read directly from the decompiled routine's own local-variable reuse, corroborated by the same asymmetry recurring identically in the unconditional pass and the switch block); the exact register-level wiring was not independently re-verified in raw disassembly for the composition claim specifically, unlike the `this`-pointer check | ● active | [EXP-0204](../experiments/EXP-0204-interface-census-and-panel-text/) |
| SHOP-051 | **The button panel's text renders through the font4 primitive `DAT_005e9bc0+0x14`, not through `CALL 0x00456b50` — closing `SHOP-SCREEN-039`'s own named blind spot ("a renderer other than `FUN_00456b50`") for this one routine.** All 12 text-draw calls in `FUN_004acb50` are `(**(*DAT_005e9bc0+0x14))(...)`; none is `CALL 0x00456b50`. `SHOP-SCREEN-039`'s own census scoped `CALL 0x00456b50` over `004a2000..004ad200` and named exactly this gap; `FUN_004acb50` (address `0x004acb50`) falls inside that address range, so its 12 draws were structurally invisible to that census's own instrument, and this experiment finds them by reading the routine directly rather than by sweeping `CALL 0x00456b50`. This closes the blind spot for `FUN_004acb50` alone; whether any other routine in `004a2000..004ad200` also renders through `DAT_005e9bc0+0x14`, or through a third call form, was not swept here (`evidence/004acb50_FUN_004acb50.c`) | High for the call form this routine uses (read directly, all 12 sites); the blind spot is closed for this routine only, not swept image-wide | ● active | [EXP-0204](../experiments/EXP-0204-interface-census-and-panel-text/) |
| SHOP-052 | **`FUN_00468f60`, called immediately after every `FUN_0056f3d4` number-format in the button panel, is a thousands-separator formatter, not a draw call.** Decompiled in full: it reads the string's own length prefix at `param_1-8` and, while more than 3 (4 for a leading sign) digits remain, inserts a comma (`0x2c`) every three digits from the right, rebuilding the string in place through the shared string-buffer helpers (`FUN_0056efa6`, `FUN_0056ee96`, `FUN_00572bfb`/`FUN_00572b03`/`FUN_005729bc`). It issues no reference to `DAT_005e9bc0` and no call matching the draw-primitive form (`evidence/00468f60_FUN_00468f60.c`). This corrects an initial reading in this experiment that took the call for a draw; it is recorded because the same misreading is available to any future reader who sees the `FUN_0056f3d4`+`FUN_00468f60` pair immediately before a `DAT_005e9bc0+0x14` call and assumes the pair is one unit rather than a format step feeding a separate draw | High — the routine is read whole, and the two candidate interpretations (formatter vs. draw call) produce visibly different instruction sequences | ● active | [EXP-0204](../experiments/EXP-0204-interface-census-and-panel-text/) |
| SHOP-EFFPOOL-061 | **The Magic Items shelf's effect candidate population is the positive differences in 24 cumulative columns of the 50-row `Magic` collection.** Columns are fighter slots 1..12 then mage slots 1..12. Across both roots the complete listing has 331 positive class/slot rows; the shipped shelf pool reaches 273 rows and 36 distinct kinds. The live key is `(fighter flag, equipment slot)`: Weapon=1, Shield=2, Armor=`definition param[4]`. `UsableBy` is carried in the row but not read by this selector | High for the population rule and dimensions / Medium for the two-root generated counts | ● active | [EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/) |
| SHOP-EFFWEIGHT-062 | **An effect is selected by a cumulative weighted draw, not by constructing an eligible list.** For class `C` (fighter 1, mage 0) and slot `s`, the endpoint parameter is `s+3+(1-C)*12`. The routine draws inclusive `1..finalEndpoint`; kind `k` wins exactly when `endpoint[k-1] < draw <= endpoint[k]`, so its weight is the endpoint difference. Fighter-weapon weights are toHit 100, castSpell 30, damage Fire/Water/Air/Earth 10 each, damageAstral 5, damageBonus 100, endpoint 275 | High (address arithmetic, RNG wrapper, interval comparisons, and the complete consumer chain discriminate filter and unweighted rivals) | ● active | [EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/) |
| SHOP-EFFPAY-063 | **Ordinary payload admission is a capacity-and-budget bound applied after kind selection.** With kind point cost `c`, declared range `a..z`, interaction budget `B`, and remaining MagCap `M`: `capMax=trunc(M/c)`, `budgetMax=trunc((70/c)*log_1.5(B/(50*M)-1))`, and `payloadMax=min(capMax,budgetMax,z)`. `B<=0`, `M<=0`, or `payloadMax<a` returns null without a rival-kind redraw. Otherwise one inclusive `rand(1..payloadMax)` is stored as `max(draw,a)`, and the point debit is `payload*c` | High (the feasibility branches, x87 formula, row consumers, and post-feasibility draw order are read end to end) | ● active | [EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/) |
| SHOP-EFFRANGE-064 | **Kinds 44..48 use a two-byte range payload and consume three payload draws.** The common ordinary `rand(1..payloadMax)` runs first and is discarded. `payloadMax` is then capped at 255; byte base is `rand(1..max)` and byte spread independently `rand(1..floor(max/2))`. Their capacity/price point debit is `(base+spread)*c`, not either field alone | High (three call sites and the byte stores are read in one constructor; the discarded-draw model predicts a different CRT state than a two-draw model) | ● active | [EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/) |
| SHOP-EFFCAST-065 | **`castSpell` has two executable-resident spell populations and a separate power law.** Fighter selection is Stone Curse or Drain Life; mage selection is Fire Arrow, Lightning, Prismatic Spray, Stone Curse, or Drain Life. Let `S` be spell param 20: `r=log_2(B/(10*S))`; `r<=0` rejects, otherwise `powerMax=min(100,trunc(30*(1.2^r-1)))` and power is inclusive `rand(1..powerMax)`. Weighted fighter-weapon cast substitutes `currentPrice*10` for `B`. All ten shipped mage-only weapon triples bypass the mage slot-1 endpoint and use a forced-cast loop with `B=min(2*ceiling-currentPrice,currentPrice*100)` | High for routes, arrays, and formula / Medium for the two-root ten-triple census | ● active | [EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/) |
| SHOP-EFFPRICE-066 | **Effects change price through one aggregate non-cast addition plus a separate addition per retained cast.** If `N` is summed non-cast point debit, the non-cast addition is `trunc((pow(N/70,1.5)+1)*N*50)`. A cast with spell scalar `S` and power `p` adds `trunc(10*S*pow(log_1.2(p/30+1),2))`. After every append the stored price remains its result only below 9,999,999; otherwise it becomes 9,999,999. The next call recalculates `2*ceiling-currentStoredPrice`, so the clamp changes later admission | High (constructors, ordered-list price consumer, comparison polarity, and later budget read discriminate additive-only and immutable-budget rivals) | ● active | [EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/) |
| SHOP-EFFORDER-067 | **After the required effect, the second and third opportunity gates are independent inclusive draws.** The live order is required kind, optional cast-spell choice, payload draw(s), append/price; then `rand(0..100)<50` and its construction; then `rand(0..100)<25` and its construction. Unless an earlier return ends the routine, the third gate is drawn even when the second is false. The exact gate probabilities are `50/101` and `25/101`, yielding opportunity-only counts 3,876/10,201 one, 5,075/10,201 two, 1,250/10,201 three before construction failures and early casts | High (both branches, their common successor, and inclusive wrapper are read; this refutes exact halves and a conditional third gate) | ● active | [EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/) |
| SHOP-EFFRETRY-068 | **Failure handling depends on the stage and has two off-by-one bounds.** A required null rejects the whole item. An optional null retains earlier Effects and returns success, skipping the next gate. An optional cast is selected and randomized, then freed, and returns success; a first cast is retained and returns success. Cumulative selection retries only an uncovered interval, absent in shipped endpoints; payload infeasibility does not redraw a kind. Selector success on attempt 100 is discarded, so only attempts 1..99 survive. The outer item loop tests `attempt<=10*n`, allowing 201 rejected attempts for `n=20` | High (all exits and comparisons are read in the live caller chain, and complete endpoint coverage falsifies active shipped interval retry) | ● active | [EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/) |
| SHOP-EFFCAP-069 | **MagCap is both an admission input and a deferred debit.** Base MagCap is `trunc(shape.MagCap*material.MagCap)`. Each later selector sees base MagCap minus the non-cast point sum already attached. Only the ordinary final fallthrough stores `item.MagCap -= totalNonCastPoints`; the successful early returns for optional null/cast and first cast retain their ordered Effects but skip that store. Cast contributes no non-cast points | High (the running-sum consumer and sole live debit store are read together with every predecessor return) | ● active | [EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/) |
| SHOP-EFFBASE-070 | **The complete shipped Magic Items equipment pool is 367 admitted triples on each preserved root, row-identical: 193 Armor, 36 Shield, 138 Weapon.** A triple exists for each set bit in an entry's five tier masks over 16 materials. Window value is `trunc(base*shape.price*material.price)`, stored base price is `trunc(exact+0.5)`, and MagCap is `trunc(shape.MagCap*material.MagCap)`. Tier and material select the base triple and its numbers; neither is an effect-selector dimension. The fighter bit is `sutableFor&1`; its absence chooses mage, and the ten mage-only Weapons take forced cast | Medium (complete arithmetic re-execution on two independently parsed corpora; agreement alone does not raise the census to High) | ● active | [EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/) |
| SHOP-EFFALT-071 | **The measured direct-call population has one positive Magic Items effect route, and the cumulative table is live rather than unused.** `FUN_0050ba63` has two direct callers: `FUN_0050b055`, whose four direct callers all lie in `FUN_005073e2`, and `FUN_0050ac75`, which has no direct caller. The older alternative `FUN_0050b6e3` also has no direct caller. All three direct calls to the ordinary selector and the sole direct call to the forced-cast selector lie inside `FUN_0050ba63`. On the positive route the cumulative table feeds selection, payload construction, ordered-list append, price recomputation, and capacity debit. This row does not claim absence of an indirect or raw-pointer route | High for the positive live route and table consumers / Medium for generator-family exclusivity, because the negative population is direct-`CALL` only and no exact raw-pointer/indirect/orphan census is committed | ● active | [EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/) |
| SHOP-EFFSEED-072 | **The shop generator has no seed call; mission/save map load can reseed the shared CRT stream.** The only `srand` writer has three clock callers. Its `timeGetTime` helper is called by a session constructor and by the `World\\Mission`/optional `game0000.sav` loader reached from campaign mission load and mission-cache miss; server-family construction uses `time(0)`. Ordinary homecoming that does not cross the loader continues the stream. Exact seeder timing, clock value, and intervening draws at the first shelf are Unknown. Runtime prediction: two separately timed loads that both traverse this loader and regenerate the same nonbattle save's shelf produce different first shelf triples | Medium for caller-boundary reachability / Unknown for timing and the prediction until a witnessed trace | ● active | [EXP-0226](../experiments/EXP-0226-magic-shop-effect-generator/) |
