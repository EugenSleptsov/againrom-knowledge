# Claim registry — AI (target acquisition and the diplomacy matrix)

Level 2 ledger. Index: [registry.md](registry.md) · spec: [`formats/ai/format.md`](../formats/ai/format.md).
Legend: `✔ promoted` (also in the spec) · `● active` · `✖ retracted`. IDs are permanent.

Not a file format — the **simulation area** that answers "whom does this unit fight, without being
told". It sits between `claims/move.md` (how it gets there), `claims/hero.md` and `claims/unit.md`
(what happens when it arrives) and `claims/alm.md` (what a scenario authored). Opened by
[EXP-0074](../experiments/EXP-0074-target-acquisition/).

Vocabulary, extended by [EXP-0082](../experiments/EXP-0082-ai-behaviour/): a **group** is the
`0x48`-byte `CObList` subclass at `player+0x24`, `grp+0x1c` its authored group id, `grp+0x44` its
owning `Player`, and `grpAI = grp+0x3c` its `0x50`-byte AI record — order `grpAI+0x20`, centre
`grpAI+0x28`, working radius `grpAI+0x2d`, base radius `grpAI+0x38`, AI-enable `grpAI+0x45`,
patrol path `grpAI+0x4c`. The group is the unit of AI: `actor+0x50`, the per-actor state, is
evaluated only while `grpAI+0x20 == 0`.

Vocabulary, fixed once here. The **actor** is the simulation object of vtable family
`0x59c3c0`/`0x59c448`/`0x59c4d0` (`TERR-MOVE-054`); `actor+0x14` is its owning `Player` and
`Player+0x28` is 0 exactly for a human participant (`UNIT-OWNER-009`); `actor+0x154` is the mover,
`actor+0x158` the **order block**, `actor+0x12c` reach, `actor+0xa5` sight. The **session** is the
`0xc320`-byte object at `[0x005f21c4]` — the AI's owner, holding the world at `+0xa50`, two scratch
lists at `+0xba4`/`+0xbc4` and the diplomacy matrix at `+0xa9c4`. The **world** is the `0xa4558`-byte
object `claims/terrain.md` describes.

**EXP-0243 reconciliation.** `AI-GROUPSEE-068`'s five-phase builder account survives and now has
its outside caller's exact use: Prismatic Spray consumes list A then list B in source order, with
the primary target outside both. `AI-DIPLO-084`'s formerly Medium “spell-cast family” identity is
now direct: the complete caller is the id-14 Prismatic Spray selector, which flips hostility before
building secondaries. Its secondaries pass the builder's directional first-member diplomacy test;
the primary does not.

| ID | Claim | Confidence | Status | Evidence |
|----|-------|-----------|--------|----------|
| AI-SPELLIDENT-286 | Valid book cell `i` in 0..23 is a signed controller index. Map targeting passes `i+1`; book producers write that ordinal byte to command+10 for opcodes1e/1f. Dispatcher `004d5f8c` reads the byte at `005c2328+4*ordinal`, rewrites command+10, then calls `00500a4a(actor+140,id)` and parks the result at actor+44. The statistics reader uses the same storage via `005c232c+4*i`. Cell5 therefore carries command6 but resolves spell23. The 24 IDs are `1 2 3 4 5 23 24 16 15 14 13 12 6 7 8 9 10 25 26 22 21 20 19 18`; the preceding dword is0. This closes AI-CMD-032's unread table contents without changing MAGIC-ICON-024's mapping. | High for the bounded representation chain: byte stores, in-place table rewrite, table alias and actor lookup are independently decoded. Malformed indices, stale actor+44 and eventual effect success remain Unknown. | ✔ promoted | [EXP-0294](../experiments/EXP-0294-quick-spell-identity/) |
| AI-SPELLPOP-287 | Selection recomputation `004190f0` accepts each selected node's object only when object+7c is nonnull, increments view+140, and ORs object+18 into availability view+148 before session/class/type predicates. It is a union, not intersection or primary-only availability. The first accepted object becomes view+138. Book-command producers `0041c0dc/0041c2d6` separately include only selected objects with +7c nonnull and bit `1<<(ordinal-1)` in +18; item context bypasses the bit. Append helper `004efbb6` caps the list at253. | High for the complete local population loops and guards. Upstream selection restrictions, snapshot+18 refresh, arbitrary mixed-population reachability and delivery interleaving remain Unknown; no global health/mana absence claim. | ✔ promoted | [EXP-0294](../experiments/EXP-0294-quick-spell-identity/) |
| AI-SPELLCAP-288 | For session+3dc equal1 or carrying bit2, every accepted selected object is tested: runtime class-name `CUnit`, object+20 in17h/18h, nonzero object+18 OR200h into view+144. The predicate uses the current loop object, not only the primary; one later eligible object can enable Cast. Other session states assign summary8. The primary owner alone supplies bit4. `00419e7b` returns0 for bit4, elseEFh plus10h for200h. The panel separately blanks on zero accepted count or summary24h. Action5/9 needs the Cast mode bit; item action0Ah bypasses the mode refusal. This partially corrects AI-PANEL-061's primary-only200h clause. | High for the reached per-object accumulator and distinct gates. Runtime class taxonomy beyond the literal tests and reachability of every synthetic mixed state remain Unknown. | ✔ promoted | [EXP-0294](../experiments/EXP-0294-quick-spell-identity/) |
| AI-SPELLGUARD-289 | Mouse book selection writes current+60 only after a nonnegative grid hit and its bit in view+148. The plain shortcut path writes any nonnegative binding first; unavailable does not imply impossible as current. AI-QUICKINVOKE-279's later availability/arming guard stands. In the reached map Cast branch, item+74 takes precedence over current+60 and a negative result emits no cast. For book selection `004b0e20` reads005bcef8 by cell:14 of24 entries are nonzero. A unit-like hit plus nonzero flag selects actor production; other hits select cell production; without a hit, a nonzero flag emits no order. | High for the bounded selection and target-production branches, not all hover/target validity or runtime feedback. Malformed stored indices, global current-state writers and every input focus state remain Unknown. | ✔ promoted | [EXP-0294](../experiments/EXP-0294-quick-spell-identity/) |
| AI-SPELLITEM-290 | Active controller item+74 switches actor/cell producers to opcodes25h/26h and writes the word at controller+78 into command+10. This is an inventory item position, consumed by0050ebf9, not a book ordinal. The1e/1f table-rewrite prologue is skipped. The item arm requires a retrieved item and first effect kind29h, constructs Spell from effect byte+40, then calls the shared actor/cell setters. The target predicate also uses another table,005bcfb8, indexed by item+74. | High for the reached operand-width/opcode fork and item constructor chain. Item UI snapshot synchronization, all invalid-item states and the broader refund/use lifecycle are not expanded. | ✔ promoted | [EXP-0294](../experiments/EXP-0294-quick-spell-identity/) |
| AI-QUICKASSIGN-278 | F5–F8 reach map branches `0040f51c/0040f56f/0040f5c3/0040f617`, sending message `0x417`, slots 0–3 and the Ctrl latch to the spellbook controller at `campaign+0xec`. Its handler `004b0e40` assigns on nonzero Ctrl: current signed spell index `+0x60` when not `-1`, otherwise a nonnegative mouse-grid hit from `004b0bf0`. It stores to `+0x64+4*slot` and empties every other equal slot to `-1`; this moves a duplicate binding, not a swap. The hover assignment arm has no spell-availability test and does not set `+0x60`. Ctrl still passes through the key handler's later arming checks. | High for the exact physical-key table and reached local assignment branches on the identical EN/RU executables. Original GUI reachability under every focus/modal state was not witnessed. | ● active | [EXP-0273](../experiments/EXP-0273-quick-slots/) |
| AI-QUICKINVOKE-279 | Plain quick-key message `0x417` copies a nonnegative stored index into controller `+0x60`; a negative value leaves it unchanged. With the spellbook closed, the map handler tests the slot through `004b0ce0`: exactly `-1` is empty, otherwise the stored-index bit must exist in selected-population mask `view+0x148`. An available slot calls `0041b439(9)`, which maps to mode 5 and requires the Cast-capability bit before storing `view+0x99c`. Case 9 neither opens the book nor directly emits a cast order. With the book open it only selects. Empty `-1` leaves the local mode/current spell unchanged; an unavailable populated slot still changes current spell but requests no mode. Later map targeting adds one to the selected index for the cast producer. | High for the reached local dataflow and guards. Generic child forwarding, selected-item override, malformed indices and actual runtime casting/feedback are not globally closed. | ● active | [EXP-0273](../experiments/EXP-0273-quick-slots/) |
| AI-QUICKOWNER-280 | The reached quick-slot owner is the spellbook controller constructed by `004b02a0` and retained at `campaign+0xec`, not an actor/group-relative array. Construction sets its four dwords `+0x64/+0x68/+0x6c/+0x70` to `-1`, separately from current spell `+0x60` and item override `+0x74`. Message `0x411` and controller right-down/up clear only `+0x60`; `004b0d90` clears the separate item override. Selected-population recomputation `004190f0` rebuilds availability mask `view+0x148` without directly writing bindings; book open/close attaches/detaches the same controller. | High for owner construction and these direct operations. Medium for selection carry across all callbacks; no-load mission entry, new-campaign reset and network/session interleaving remain Unknown. This is not an exclusive-writer census. | ● active | [EXP-0273](../experiments/EXP-0273-quick-slots/) |
| AI-QUICKSAVE-281 | Original writer `00478c40` emits controller offsets `+0x64..+0x70` in order as `SpellBook/Shortcuts`, kind 6 and 16 payload bytes: F5, F6, F7, F8 signed zero-based spell indices, with `-1` unbound. It separately emits current spell `+0x60` as `SpellBook/Pressed` and book visibility as `IsOpen`. Restore paths `00477650` and `00477c00` read the array and copy four dwords to the same controller, without per-index validation; `Pressed` is separately restored. This identifies the four meanings left open by `SAV-ORIGFAULT-335`, not full malformed-save compatibility. All 27 preserved-root saves (EN 23, RU 4) have four `-1` slots and `Pressed=-1`; they contain no populated round-trip witness. | High for the bounded original save/load programme and exact corpus measurements. Populated-slot original reload behaviour and later invalid-value handling are unwitnessed; broader session persistence remains Unknown. | ● active | [EXP-0273](../experiments/EXP-0273-quick-slots/) |
| AI-RETREAT-270 | **Explicit Retreat admission has separate panel, producer and dispatcher gates.** Panel left-down `00490500` requires no selected item at `session+0x3cc`, active panel, an enabled hit cell and a changed overlay cell. `004190f0` disables the panel when the counted inventory-bearing selection is empty or summary has `0x24` (foreign first owner or structure); `00419e7b` supplies Retreat bit `0x80` without a spell-capability requirement. `0041b439(8)` calls `0041c63f` immediately, then clears the armed mode. The producer appends selected entries with nonnull `actor+0x7c`; its append helper caps the list at 253. The dispatcher requires the AI manager, nonempty list and a resolvable first actor. An absent first actor aborts the entire command; absent later actors are skipped. Resolution uses the requested player's actor list and rejects action `actor+0x54 == 0x10`, not all nonpositive HP. The Retreat arm has no own health-threshold, hostile-presence, movement or casting admission test. | High for these concrete code paths and gates. Physical keyboard focus, network delivery and selection changes between producer and dispatch were not runtime-witnessed. | ● active | [EXP-0272](../experiments/EXP-0272-immediate-retreat/) |
| AI-RETREAT-271 | **Explicit Retreat installs a persistent actor state, not a single destination or the automatic threshold policy.** `004d607c` calls `00534e20` on the newly grouped command members. Its reset passes call `005306e0`, align mover desired facing to current facing, conditionally clear a noncurrent reserved cell only at a cell centre, copy reach to `ord+0x14`, and zero `ord+0x60`, `mover+0x7c`, `actor+0x54`, `ord+0x50`, and `ord+0x38`. It writes `actor+0x50 = 0x16` at `00534f2d`, pending order `ord+8 = 0`, and group order `grpAI+0x20 = 0`. Neither `00534e20` nor `005306e0` clears order-progress `ord+9`, attack target `actor+0x5c`, or the queued cast target/spell slots. The dispatcher separately calls the conditional spell-cancellation helper `004f4c97`; universal spell-object destruction is not established. | High for writes and nonwrites of the two complete setter/reset bodies. No claim that every referenced object survives the separate cancellation/lifecycle helpers. | ● active | [EXP-0272](../experiments/EXP-0272-immediate-retreat/) |
| AI-RETREAT-272 | **Retreat does not unconditionally interrupt an in-progress step, attack or cast.** After the setter leaves `ord+9` unchanged, `005310e0` consumes nonzero progress before the pending-order switch: progress 1 restores action 3; progress 2 restores action `0xd` or `0xe` from `ord+0x5c`; both increment `ord+0x15` and clear progress only when it exceeds 2 and `actor+0x136` is nonzero. Progress 3 calls `005495f0`, keeps movement action 1 and clears progress on the cell-centre predicate. Progress 4 retains the status hold until effect bit `0x100000` clears. Thus a new Retreat state can coexist with completion of an older action. | High for static state-machine precedence, including both original tables and branch targets. Exact visible final-hit/cast timing, emitted effects and animation-phase outcomes are Unknown without an original runtime witness. | ● active | [EXP-0272](../experiments/EXP-0272-immediate-retreat/) |
| AI-RETREAT-273 | **The explicit Retreat state recomputes an away order through `0052f090`; reaching one flee cell is not its own state terminator.** Group order 0 calls `0052ce50`, whose state `0x16` calls that helper at `0052d24d`. It collects the occupancy square about the actor with radius `mover+8`, applies the directional diplomacy/invisibility filter, and gates on a nonzero byte count of positive-HP entries. It then averages fine coordinates over the whole surviving list, divides by the low byte of the list count, calls `0054cc40` with distance 3, and writes ordinary pending move 1 plus its cell. A zero positive-HP count calls ordinary acquisition `0052e110`, which can install a pursuit or idle/autoheal order. Neither this helper nor the direct actor-state arm clears state `0x16` or has a timer/arrival test. Route failure reaches the ordinary `005327d0` fallback. | High for the bounded local control flow and arithmetic. Medium for sustained in-game persistence: whole-session event interleaving, all transitive lifecycle effects, dense-list byte wrap and runtime corpse occupancy are not closed. | ● active | [EXP-0272](../experiments/EXP-0272-immediate-retreat/) |
| AI-RETREAT-274 | **Automatic withdrawal shares execution helpers with explicit Retreat but not its admission or state installation; the two automatic helpers are not identical.** The group-tail tests `0052f200` then `0052f260` compare signed current HP with `ord+0x44` and `ord+0x40`, using radius `mover+8` and literal 2. They call `0052f090` and `0052f3e0` respectively, without installing state `0x16`. In `0052f090`, `0052f0ef` counts positive HP only for the branch at `0052f110`; the averaging loop starts from the entire filtered list and its divisor is `listCount & 0xff`. `0052f3e0` has only a nonempty-list gate and no positive-HP scan. This retracts `AI-WITHDRAW-028`'s living-only centroid and shared positive-HP gate, not its distance-3 geometry or absence of a passability test in the picker. | High for the two whole helper bodies, the tail and the discriminating branch/loop dataflow. Whether a mixed or all-dead hostile list reaches either helper during ordinary play remains Unknown. | ● active | [EXP-0272](../experiments/EXP-0272-immediate-retreat/) |
| AI-RETREAT-275 | **Later admitted commands can replace explicit Retreat, and zero HP is not an extra Retreat-refusal check.** Move `005340a0` finishes with group order 4, so group dispatch no longer uses the per-actor state-`0x16` arm. Target attack `005348c0` installs actor state 3 or fallback `0xc`; cast-at-actor `00533d00` installs `0xd` or `0xc`; cast-at-cell `00533ed0` installs `0xe` or `0xc`. These are concrete replacement paths, not a claim about every opcode. The resolver rejects completed-death action `0x10`; an earlier death phase with HP at or below zero can still resolve. The unit tick `004f37be` checks HP before order execution, zeros state/action in its nonpositive branch and does not call `005310e0` there. Retreat therefore supplies no resurrection path in the traced tick. | High for the named replacement and death paths. Unknown whether ordinary selection/queue timing can deliver Retreat in every intermediate death phase, or what a missing target/new command does on untraced opcodes. | ● active | [EXP-0272](../experiments/EXP-0272-immediate-retreat/) |
| AI-SPRAY-266 | **Prismatic Spray ranks the shared group candidate lists in their existing order, rather than performing a radius search.** `FUN_0053ddd0` calls `FUN_005365e0(session,caster+0x70)`, then traverses scratch list A head to tail followed by list B head to tail. Its stack pointer array holds 100 entries, while scores go to `[session+0xd74+4*i]`, whose capacity is Unknown; neither copy loop checks a bound. It scores A by `((edgeDistance<<8)+turnCost)&0xffff` and B by `((((edgeDistance<<8)+turnCost)&0xffff)<<8)`, then repeatedly selects the strict minimum. Strict `<` preserves source order on equal stored scores. The primary target is not admitted through either list: the selector appends it first and skips its duplicate among winners. It is therefore possible for a primary that fails the secondary filters to remain the first victim. | High. The direct caller, complete selector and complete builder already carried by `AI-GROUPSEE-068` distinguish this from an unordered set and a spatial-radius collector. The score-scratch capacity is explicitly outside the claim. | ● active | [EXP-0243](../experiments/EXP-0243-prismatic-victims/) |
| AI-SPRAY-267 | **Only Prismatic Spray's secondaries pass the group visibility, first-member diplomacy/invisibility and corpse fallback gates; its primary bypasses them.** `FUN_005365e0` clears sight once, stamps every group member, walks the global actor list head to tail, applies `FUN_0052ab60(session,firstMember,A,0)`, and moves health-below-one entries A→B. When A is empty, the builder moves all B back into A and those corpses use the A score. Otherwise B remains a distinct source list and uses `((((edgeDistance<<8)+turnCost)&0xffff)<<8)`. Selection first requires that score below 65530; append later requires the saved threshold below 65000. The bytes therefore do not establish a categorical living-excludes-corpse rule: a sufficiently low B score can pass, and this round did not establish whether the required edge-distance geometry is reachable. `FUN_0053ddd0` separately writes the primary target's alarm and calls the directional hostility flip before invoking the builder. Thus the old statement that the Prismatic collector consults no diplomacy mistook absence of a direct matrix load for absence of the builder call. | High for the gate order, primary bypass, B formula and sentinel tests: the selector and builder are read whole. Unknown whether a B corpse can attain an appendable score beside living A in a valid runtime layout. The candidate is visible to any group member but diplomacy is decided by the first member, as `AI-GROUPSEE-068` already establishes. | ● active | [EXP-0243](../experiments/EXP-0243-prismatic-victims/) |
| AI-INPUT-121 | **The map's physical left contract is inactive-down-to-start, active-up-to-act.** `WM_LBUTTONDOWN` clips to the map and stores a marquee origin only while global `[0x005cd82c]` is zero; a repeated down while it is nonzero is a consumed no-op, and double-click aliases this same gate. `WM_LBUTTONUP` closes the marquee, updates the hit and reaches selection or the cursor command only while that global is nonzero. It does **not** call `ClipCursor(NULL)`: `FUN_00476420` passes the session rectangle at `session+0x6c0`. An up without an active marquee skips clipping, hit update, selection and cursor action, but the later selected-item placement arm still runs when `session+0x3cc` is nonzero, emitting `0x23` for the `item+6 == 0xffff` branch or `0x22/0x32` through the alternate transfer builder. The threshold is `screenW*10/640` = 10/12/16 pixels. A rectangle strictly beyond it goes to selection; it is not discarded. At equality the result depends on whether the caller entered selection for another reason, because the outer test is `>` while the selection routine's click test is `<` | High (the physical vtable slots, both `[0x005cd82c]` gates, post-gate selected-item branches/opcodes, the `ADD ECX,0x6c0; PUSH ECX; CALL ClipCursor` bytes and both threshold comparisons are read end to end and raw-asserted) | ● active | [EXP-0190](../experiments/EXP-0190-combat-controls/) |
| AI-SELECT-122 | **Mission selection has four exact forms, with a gate on both Shift forms.** Plain click replaces with the topmost intersecting non-structure regardless of owner, or a structure whose class `+0x64` is zero. Shift-click and Shift-rectangle toggle an owned qualifier only when the **old** selection summary has bit `0x04` clear; an owned candidate is a no-op when that bit is set. Therefore, after a plain click selects a foreign actor, Shift-click or Shift-rectangle over an owned actor does not add or toggle it. Ground and foreign candidates also preserve selection. Plain rectangle selects every owned non-structure whose drawable rectangle overlaps by strictly more than half, but preserves the old selection if none qualifies. Alt after a one-object click expands to its stored group without centring; E selects every owned exact-name `CUnit`; plain/Shift digit selects or augments a group; Ctrl+digit assigns; Alt+digit selects and centres with message `0x406`, and Ctrl wins over Alt | High for the four selection forms, old-summary/ownership gates and modifier precedence (selection and key routines read whole, and the `view+0x144 & 4` gate is asserted from raw bytes); Medium for exact-name `CUnit` as a behavioural category because it is a string compare rather than a runtime-class test | ● active | [EXP-0190](../experiments/EXP-0190-combat-controls/) |
| AI-PANEL-123 | **The command-panel/key contract is:** Attack mode 1 → target `0x19`, ground fallback move `0x16`; Move mode 2 → `0x16`; Guard immediate `0x17`; Defend mode 4 → target `0x1b`, ground no-op; Cast mode 5 → spell `0x1e/0x1f` or item `0x25/0x26`; Swarm mode 6 → `0x1a`; Stand Ground immediate `0x18`; Retreat immediate `0x14`. An armed cursor is one-shot and clears after a consuming click. Repeated keydown reissues Guard, Stand Ground and Retreat; repeated A/M/D/S only reasserts a mode; C is suppressed after its UI opens. The eight-cell panel is inactive for no selection, foreign ownership or the structure flag; cast alone needs the spell-capable flag. **It is not the only item-order producer:** character-panel `WM_LBUTTONUP` and nested inventory-grid `WM_LBUTTONDBLCLK` can pass an admitted item through vslot `+0x7c`, store its slot/effect context, and call `FUN_0041b439(0x0a)`, which remaps to Cast mode 5; the next map down/up emits item `0x25/0x26`. Inventory-grid `WM_LBUTTONUP` is a separate transfer path: live `grid+0x84` and selected `session+0x3cc` call grid vslot `+0xa4`; its destination code is 2, and `00482931` emits `0x22/0x32` even when hit vslot `+0x88` returns `-1`, using merge/insert-before-gold/append fallback. Its sibling handlers scroll on left-down, build item/money drag state on left-button move, and consume every right-button edge as a no-op; none emits another order. The grid double-click's gold-sentinel arm opens Drop Gold; action button-up or Enter emits `0x23` with the resolver-returned amount and current cell, while cancel button-up or Esc emits none | High (panel/key tables, the complete inventory-grid physical mouse vtable, three physical inventory-item producer paths, modal controls and raw item/gold gates, every direct caller of all fifteen producer targets with a physical or explicit non-mission classification, order builders and selection-summary predicates are read and asserted) | ● active | [EXP-0190](../experiments/EXP-0190-combat-controls/) |
| AI-MINIMAP-124 | **The 160x158 minimap acts on left DOWN, not up:** default moves the camera `0x406`; move emits `0x16`; attack emits `0x19` on any non-zero cell object id and otherwise `0x1a`; defend emits `0x1b` only with an id; cast discards its hit-test result and emits nothing; patrol emits `0x1d`. Left-drag repeats the current small-cursor action per delivered move. Right down and right-drag move the camera; there is no capture. When a mouse-move arrives with both `MK_LBUTTON|MK_RBUTTON`, the handler tests the left bit first, executes the left action and returns, so no right-camera pan occurs. Left up only clears a special cursor; both double-clicks and right up are no-ops. The object's identity is established from construction, rectangle and overview draw, not the `s*` filenames | High (constructor/store, draw, physical vtable and handlers read; the left-test/early-return bytes and all producer opcodes re-measured on both roots) | ● active | [EXP-0190](../experiments/EXP-0190-combat-controls/) |
| AI-KEY-125 | **The complete decoded default mission key surface is committed in `keyboard.tsv`.** It includes Esc menus; F1 help; F2 save; F3 load or Diplomacy by phase; F5..F8 quick spells; Pause; arrows; A/M/G/D/C/S/T/R/E; Space, B/Q, I/backtick; digits and numpad digits with Shift/Ctrl/Alt; numpad speed; Ctrl+F/H/L/N/O/U/W settings; Enter text entry; focused-character-panel Tab message `0x412`; and Alt+S screenshot. F4 and F9 are explicit no-ops; no wheel route exists. Windows repeat is not filtered. Open text entry consumes map shortcuts; focused popup children take precedence; focus loss clears all three modifier latches. Ctrl+numpad plus sets `campaign+0x40c=1`, selecting the unpaced/max-speed owner loop with one sub-tick per idle callback and no deadline; minus clears it, resets phase/epoch and restores paced mode (`SESS-CLOCK-005`, `SESS-IDLE-007`). F12, Backspace's clear, character-panel `0x412` and Alt+B..Y except S's outbound `0x46` record have exact mechanisms but Unknown gameplay meaning | High for the event/key/action mapping and repeat/focus predicates (frame message map plus frame/map/character-panel handlers and existing clock provenance read); Unknown only for the four mechanisms explicitly named | ● active | [EXP-0190](../experiments/EXP-0190-combat-controls/) |
| AI-CURSOR-126 | **ROM1 contains a Patrol cursor and order but no shipped default gesture that leaves Patrol armed.** Mode 8 maps to the patrol cursor; map and minimap consumers emit `0x1d`. The executable direct-call census for `FUN_0041b439` contains 14 sites, including item route `0049257a`; both its character-panel left-up and inventory-grid double-click entrances pass `0x0a`, which remaps to Cast mode 5, not Patrol. The corrected whole-image census begins with all 77 direct calls to the common enqueue routine in 64 producer functions, enumerates every direct caller of every producer, and uses physical traces to select the fifteen order/item targets reached by default input. It includes inventory-grid left-up reaching transfer call `00482931` through vslot `+0xa4` and the second `FUN_0041ce3e` caller in Drop Gold, but no new arm routine. All 42 direct call sites under the selected targets have a physical predecessor, inherit one through the shared command dispatcher, or are explicitly non-mission. The panel/custom/key routes leave R as immediate Retreat `0x14` with mode reset. The capability mask's `0xef` admits internal mode 8 but does not make it reachable. **G2:** adding a binding is an authored executable/UI change; it changes no shipped data file, while changing the cursor sheet changes `graphics.res` bytes | High for default unreachability within the global enqueue/direct-caller plus physical vtable/pointer/construction census; a dynamically manufactured engine custom message is outside the shipped default contract | ● active | [EXP-0190](../experiments/EXP-0190-combat-controls/) |
| AI-INPUT-127 | **The map's physical right contract is capture-to-pan, up-to-cancel.** Right down captures and stores the origin. With `MK_RBUTTON`, each non-zero signed-truncated `(current-last)/8` cell delta sends camera message `0x406` and marks a drag. Right up releases capture; a marked drag performs no cancel, while a click sends `0x405`, cancelling an armed mode or deselecting all when no mode is armed. Right double-click is a no-op. No mission handler exists for capture loss or cancel mode, so the image does not establish the external-loss outcome | High for all in-image event edges and branches; Unknown for Windows-driven external capture loss, explicitly outside the handled set | ● active | [EXP-0190](../experiments/EXP-0190-combat-controls/) |
| AI-FILTER-001 | **`FUN_0052ab60` is not an acquisition routine — it is a diplomacy predicate over a candidate list, and the relation it keeps is a *parameter*.** `RET 0xc`, so the signature is `__thiscall(this = session, me, list, wantBit)`: for each element it reads `MOVSX EAX,byte ptr [EDI + EAX*0x2 + 0xa9c4]` (`0052abbe`) at `session + 0xa9c4 + 50·myPlayerIdx + hisPlayerIdx`, `AND EAX,0x1`, and **unlinks the element when the bit equals `wantBit`** (`0052abc9`/`0052abcb`), by an inlined `CObList::RemoveAt` at `0052ac49`. An element the diplomacy test would keep is unlinked anyway if it is invisible (`cand+0x144 & 0x8000`, `0052abd1`/`0052abd7`) unless some member of the decider's group list `me+0x70` is within that member's `order+0x71` (`0052ac0e`). **Callers: 15 hits, 15 distinct owners, 0 in orphan/undisassembled code** (`EnumRefs callto:52ab60`, repaired function table). Two pass `wantBit = 1` — `FUN_0052eb60` (`0052ec36`) and `FUN_0052ed30` (`0052edc5`) — and both then minimise `cand+0x94 / cand+0x96`, the health fraction, and cast: they are the heal routines, which is what fixes the polarity. **Three producers fill the list**, not one: `FUN_00543ab0(cell, r)` builds the occupants of the `(2r+1)²` block into `world+0x5400c` (12 of the 15 sites; `callto:543ab0` = 14 hits / 13 owners / 0 orphan), `FUN_005327d0` fills `session+0xba4` from the world actor list by **reach**, and `FUN_0052dea0`/`FUN_005365e0` fill it from the same list by **sight** | High (the signature is `RET 0xc` with the three pushes read at every site; the index arithmetic and the unlink test are single instructions; the polarity is discriminated by the two `wantBit = 1` callers doing the opposite thing, not by assumption). **Medium** that the caller list is complete — `callto:` sees direct calls and `.rdata` code pointers, so an indirect call through a computed pointer would be missed | ● active | [EXP-0074](../experiments/EXP-0074-target-acquisition/) |
| AI-ACQUIRE-002 | **The standing acquisition rule is `FUN_0052e110`: everything the unit can see, filtered to enemies, nearest first and cheapest turn to break a tie — then discarded unless it is within reach.** It is the only caller of `FUN_0052dea0` (`callto:52dea0` = 1 hit) and has **13 callers over 12 owners** itself, against `FUN_005327d0`'s **one**, so it — not `HERO-TARGET-024`'s route — is what an unordered unit runs. Selection: `bestDist` is seeded `reach + 1` (`0052e123 MOV BL,byte ptr [EDI + 0x12c]`, `0052e129 INC BL`) and `bestTurn` `0xff`; per candidate `d = FUN_0054a960(me, cand)` (`0052e181`), **`INC BL` when the candidate's `vt+0x20` is 3 and the decider's is not** (`0052e18f`…`0052e19e`, i.e. a flier costs one more), `turn = FUN_0054f090(FUN_0054a680(me, cand), mover[0])` (`0052e1c5`); `d < bestDist` takes it outright, `d == bestDist` takes it only if `d <= reach` **and** `turn <= bestTurn` (`0052e1e2`…`0052e1f0`); afterwards `bestDist > reach` clears the pick (`0052e21b`…`0052e225`). On success `order+8 = 6`, `order+0xc = target`, `order+0x14 = reach`. **The population has a second half nobody had described**: hostile candidates at `HP < 1` are moved to `session+0xbc4` (`0052e000`, `0052e02f`, `0052e03b`), and when no living candidate survives **the dead are moved back and one of them becomes the target** (`0052e0c1`; the same arm exists in `FUN_005327d0` at `0053292f`). With no target at all: a human owner's unit tries `FUN_0052eb60` (heal), an AI owner's goes to `order+8 = 0xb` (guard) | High (every branch above is a cited instruction in one function read end to end, and the reach cap is decided by the seed `reach+1` plus the post-loop test, which no rival ordering satisfies). **Medium** that no *fourth* route exists — the enumeration behind that is `callto:52ab60`, whose blind spot `AI-FILTER-001` states | ● active | [EXP-0074](../experiments/EXP-0074-target-acquisition/) |
| AI-LIST-003 | **`world+0xa4554` is the global on-map actor list, walked linearly — there is no spatial index anywhere on the acquisition path.** The world is `0xa4558` bytes (`004e1c17 PUSH 0xa4558`), so the field is its last dword. `disp:a4554` returns **19 hits over 14 owners, 0 orphan**, with exactly **two writers** — `00547ed6` in the world initialiser `FUN_00547eb0` and `00541d95` in `FUN_00541c50`, which has no caller (`callto:541c50` = 0 hits) — and both store a constructor argument, so the field is assigned once and never mutated. The single construction site passes the global `CObList` at `[0x00609558]`: `004e1c31 MOV EDX,dword ptr [0x00609558]` / `004e1c3f CALL 0x005417f0`. That global has **one writer** (`004d0015`, in `FUN_004cfdb0`, from the list constructor `FUN_0050fb5e`) and one clear-to-zero (`004cecb9`) over **40 references across 28 owners** (`refto:609558`). Membership is the actor's own bit: `FUN_004f47e6` removes it and sets `actor+0x4c \| 8`; `FUN_004f4865` and `FUN_004f4905` place it at a free cell, add it and clear that bit, printing `"Unit can't enter map - no free p…"` / `"Unit can't return to map - no fr…"` on failure. All three candidate producers walk it head to tail testing every element | High (the size constant fixes the field's position, the two writers are the complete `disp:` population, and the construction site names the object; the identity is then confirmed from the other side by the add/remove pair and their strings). **Medium** on writer completeness: a `disp:` sweep cannot see a wholesale `REP MOVSD` of the world, and `imm:a4554` returns 0 | ● active | [EXP-0074](../experiments/EXP-0074-target-acquisition/) |
| AI-DIPLO-004 | **The relation a unit consults is a 50×50 byte matrix at `session+0xa9c4`, and it is empty until something fills it.** The session constructor `FUN_0052b2b0` zeroes `0x271` dwords there — 625 dwords = **2500 bytes = 50×50** — so with no further writing nobody is hostile to anybody; it also sets the four header bytes of the sub-object at `session+0xa9bc` and reads `World\Data\ai.reg` `[Scanning] MinimalGuardRange` (code default **10**, `0052b52c PUSH 0xa`) into `session+0xa9b8` — *amended by [EXP-0082]: the **shipped** `ai.reg` sets it to **8**, so the default is never the effective floor; the file has exactly 4 records and `[Tasker] IntelligentCons…` = 15 has no literal in the image at all* — whose only reader is `FUN_00534ac0` (`00534c7e`/`00534c8d`/`00534c99`), which raises a group's guard radius `grp+0x2d`/`grp+0x38` to it (`disp:a9b8` = 4 hits / 2 owners). The row and column index is `Player+0x04`, a signed word (`0052aba7`, `0052abb4`), stride **50** (`LEA ×5` twice then `×2`). **Bit 0 = hostile; bit 1 blocks the flip**: `FUN_0053d9b0` (`this = session+0xa9bc`, so the matrix is its `+8`) does `TEST AL,0x3` before `OR AL,1` on both `[A][V]` and `[V][A]` (`0053d9f3`, `0053da25`), notifying `FUN_004ea0ac` for whichever side is a human participant. The engine reads the cell as three bits: `004e1e17 AND EAX,0x7`. Whole-image sweep `disp:a9c4` = **32 hits / 14 owners / 0 orphan**, `imm:a9c4` = **0** | High (the size, the base, the stride and the two bits are single instructions in two functions read end to end, and `FUN_0053d9b0`'s `this` is fixed by `HERO-AGGRO-028`'s call site). **Medium** that the writer list is complete: the matrix is also reachable as `sub-object + 8`, which carries displacement `8` and is invisible to `disp:a9c4` — `FUN_0053d9b0` is exactly such a site and was found through `HERO-AGGRO-028`, not through the sweep | ● active (amended) | [EXP-0074](../experiments/EXP-0074-target-acquisition/) |
| AI-DIPLO-005 | **A scenario authors the matrix, one row per player, and the `.alm` loader is the consumer `ALM-GRP-041` could not find.** `FUN_004e1924` (the map loader — its own strings are `ALM-REQ-055`'s `"Tiles block not found"` / `"Altitudes block not found"`) iterates the player collection and, for each player `P` with index `idx = P+0x04` (= type-5 slot + 1), reads `k = 0…15` from `ALM-GRP-041`'s sixteen `u16` through `FUN_00448bd0(map+0x28, idx−1)` then `FUN_004adcf0(player+0x34, k)` and stores the **low byte** at `matrix[idx][k+1]` (`004e1d68 MOV byte ptr [EDX + ECX*0x1 + 0x1],AL`, `EDX = session+0xa9c4 + 50·idx`); then it forces `matrix[idx][idx] = 2` (`004e1d8b`). So the file's row is 0-based over players while the matrix is 1-based, and **column 0 is never written**. On a mission join `FUN_004d303e` clones a reference player's row *and* column for `i = 2…15` (`004d3247`/`004d327d`), sets `matrix[ref][new] = 2`, and forces **0 in both directions between every pair of human participants** (`004d3304`, `004d3326`); `FUN_004d8963` is the same with `2` in both directions. **Corpus, 38 maps / 191 player records / 3056 row cells** (`tools/diplomacy`, replaying the store above): the values are exactly `{0, 1, 2}` — 2362 / 471 / 223 — so nothing truncates in the byte store (0 cells exceed `0xff`) and no bit above 1 is used; **no map ships more than 16 players**, so the 16-column row always covers the whole roster. The file spells its own diagonal as 2 in 186 rows, as **1 in 2 rows and 0 in 3**, so the forced diagonal is load-bearing rather than decorative. Over the 866 ordered off-diagonal pairs: **469 hostile, 37 carrying bit 1, and 102 disagreeing with their mirror on bit 0 across 19 of the 38 maps** — the relation the engine indexes is `[me][him]` and the shipped content uses that asymmetry (`Islands.alm`: `Guards → Monsters` = 1 while `Monsters → Guards` = 0) | High (the store, its index arithmetic and the forced diagonal are cited instructions, and the corpus replay of that exact store closes over every shipped map). **Medium** that `{0,1,2}` is the whole value space and that no bit above 1 exists — that is a corpus statistic, and the engine's own `AND …,0x7` admits a third bit no shipped map uses | ● active | [EXP-0074](../experiments/EXP-0074-target-acquisition/) |
| AI-SIGHT-006 | **`actor+0xa5` is the sight radius and the guard leash — it is never an acquisition radius.** `disp:a5` returns **10 hits over 10 owners, 0 orphan**; two are writes (`004f3374`, the actor constructor's default **5**, and `00532e8c`), one is a `LEA` on a different object in the presentation module (`0042ea78 LEA ECX,[EBP + 0xa5]`, `FUN_0042e250`), and seven are reads. *Amended by [EXP-0120]: those two are not the writer set. The sweep is reproducible hit for hit and it was asked a question `disp:` on a byte cannot answer — **there are six writers**, and `+0xa4` is a `u16` sight in 1/256 cell of which `+0xa5` is the high byte (`AI-SIGHT-092`). The `complete writer set is two` clause is withdrawn.* The one that matters is `00546d81` inside `FUN_00546d20`, reached from `FUN_0052dea0` through `FUN_005472a0`: it zeroes the `0x10000`-byte map at `world+0x82ef0`, marks the actor's own cell, and expands **ring by ring inside a 41×41 window** (`(cell>>8 & 0xff) − 0x14`, `(cell & 0xff) − 0x14`, loop bound `0x14`), each cell tested by `FUN_00546c70` — *which [EXP-0112] reads whole and which is **not** an altitude comparison but an accumulator march carrying a budget from cell to cell, the altitude being one of its four terms (`AI-LOS-081`)* — against the height plane `world+0x9451c`, stopping when an entire ring is blocked; the radius enters as `(1 << (k−1)) + (scanRange << k)` at `fog+0x25450`. `FUN_0052dea0` then walks `world+0xa4554` and keeps every actor whose cell is marked (`0052df8a`/`0052df93`) — plus, for an AI-owned unit only, the remembered attacker's cell `order+0x58`, which is marked, counted in `order+0x5a` and forgotten after **20** ticks (`0052df19`). The second consumer is `FUN_0052e3c0` (AI state 0xb): `CMP AL,byte ptr [ESI + 0xa5]` (`0052e413`) against the distance from the guard post `order+0x00`, and beyond it the unit is ordered to walk back. **Neither uses it to accept a target**: `AI-ACQUIRE-002`'s cap is `actor+0x12c` | High (the sight stamp and the leash are cited instructions in two functions read end to end, and the 10-hit sweep is stated with its instrument). ~~**Unknown**: the remaining five readers `FUN_0052d3c0`, `FUN_0052d500`, `FUN_00533210`, `FUN_005333a0`, `FUN_0054e220` were located, not read~~ — *narrowed by [EXP-0082]: `FUN_00533210` takes the **max** of `actor+0xa5` over a group's members and adds it to the member's distance from the group centroid, which is where the group notice radius `grpAI+0x2c` comes from (`AI-RADIUS-014`); `FUN_0052d3c0`/`FUN_0052d500` are the follow pair (states 8 and 0x11) and use `actor+0xa5` as the **stop distance** of the follow order they emit, but only when `order+0x70` is 0 (`0052d445`/`0052d44b`, `0052d57f`/`0052d585`); a nonzero `order+0x70` is used instead. `FUN_005333a0` and `FUN_0054e220` are still located, not read* — *and amended by [EXP-0088], which read `FUN_00546d20`/`FUN_00546c70` for the object rather than for the radius and corrects two mechanism sentences: **`fog+0x25450` is not a field.** It is `losAcc[20·64 + 20]` — the **centre cell of the 41×41 accumulator grid** at `fog+0x24000` (`0x25450 − 0x24000 = 0x1450 = 1300 dwords = 20·64+20`), so the seed `(1 << (k−1)) + (scanRange << k)` is written into the accumulator, not into a scalar, and `k` is `[Scanning] ScanShift` from `World\Data\map.reg`, shipping as **7** in both roots. **And what `FUN_00546d20` zeroes is that accumulator, not the map:** `00546d57 LEA EDI,[ESI + 0x24000]` with `MOV ECX,0x1000` is `0x4000` bytes. The `0x10000`-byte byte map at `fog+0x2a008` (= `world+0x82ef0`) is cleared by a **separate** routine `FUN_005474f0`, which has **two** callers — `FUN_0052dea0` `0052debb`, which clears then stamps one actor, and `FUN_005365e0` `00536627`, which clears then stamps **every member of a group**, so the same array serves a per-actor and a group-shared reading depending on the caller. The object's base is `world+0x58ee8` (`0053661f`). `TERR-FOG-084`* | ● active (amended, contested; the writer-set clause withdrawn by EXP-0120) | [EXP-0074](../experiments/EXP-0074-target-acquisition/), **[EXP-0082](../experiments/EXP-0082-ai-behaviour/)**, **[EXP-0088](../experiments/EXP-0088-fog-of-war/)**, **[EXP-0120](../experiments/EXP-0120-sight-writers/)** |
| AI-GUARD-007 | **What closes the distance is a pursuit order, not a wider scan — and two clauses stop a unit engaging at all.** `FUN_0052e940` is the engage routine: given a target, or else the first survivor of the occupancy block around its own cell, it sets `order+8 = 5`, `order+0xc = target`, `order+0x14 = actor+0x12c` — a target with a **stop distance**, which is the only construct in this area that survives being farther away than reach. Its 13 call sites over 9 owners are the AI's engage arms, `FUN_0052e3c0`'s post scan among them. Both `FUN_0052e940` and `FUN_005327d0` refuse outright when `(actor+0x4c & 4) && actor+0x12c < 2 && Player+0x28 == 0` — a human participant's short-reach unit with that flag does **not** auto-engage; `FUN_005327d0` writes the target into `order+0xc` first and then sets `order+8 = 0` (`005329f3`, `005329fc`, `00532a20`), so the pointer is left behind but the order is idle. And `FUN_00532e60` (state 0x17) **overwrites both radii on the actor**: `00532e8c MOV byte ptr [EDI + 0xa5],AL` and `00532e92 MOV byte ptr [EDI + 0x12c],AL` with `AL = 5`, gated on `word ptr [EDI + 0xe] == 0x18`, immediately before calling `FUN_0052e110` — which is why EXP-0072 saw sight and reach written together, and which means **`actor+0x12c` is not immutable class data** | High (the order fields, the two suppression clauses and the paired radius write are cited instructions). ~~**Medium** on the *meaning* of `actor+0x4c` bit 2 and of `actor+0x0e == 0x18`~~ — *both settled by [EXP-0112]: bit 2 is `HERO-CLASS-013`'s **mage** flag, so this row's suppression clause is about a human participant's short-reach spellcaster and `FUN_0052ed30` is the caster AI; `actor+0x0e == 0x18` is `Humans` typeID 24, 57 named rows on both roots, tested only inside an arm `AI-STATE-043` shows has no writer (`AI-GATE-079`)*. ~~and on how `order+8` states 1/5/6/0xb are consumed~~ — *closed by [EXP-0099] (`AI-ORDER-039`, `AI-PURSUE-040`) and, for the group orders that re-issue them, by [EXP-0112] (`AI-REISSUE-077`)* | ● active | [EXP-0074](../experiments/EXP-0074-target-acquisition/) |
| AI-TICK-008 | **The AI is one slot of the full tick, it is driven per group, and the per-actor state machine is a single arm inside it.** `SESS-TICK-006` located `FUN_005336a0` at `server+0x04 % 16 == 6` and left it unread; it is the whole AI. `FUN_004d2551` reaches it at `004d2595` after the signed modulo at `004d2577`…`004d2586` (`CMP EAX,0x6`), so the AI runs **once per full tick** — 992 ms at the shipped speed index (`SESS-CLOCK-005`) — and never per sub-tick. `EnumRefs callto:5336a0`: **2 hits / 2 owners / 0 orphan** (`FUN_004d2551`, `FUN_004d214b`). `FUN_005336a0(playerList)` walks players, then each player's group list at `player+0x24`, and calls the group order machine `FUN_00533ae0` once per group (`00533849`), gated on `session+0xb388 != 0` **or** that group's own `grpAI+0x45` (`0053383f`), whose constructor default is 1. Nothing in it iterates actors. The per-actor state machine `FUN_0052ce50` has **2 callers** (`EnumRefs callto:52ce50`: 2 / 2 / 0) — `FUN_0052cd10` and `FUN_00533ae0` at `00533b43`, the latter being jump-table **arm 0** of the group order — so **an actor's own AI state is evaluated only while its group's order byte is 0**. The routine also profiles itself and, when `[0x005cd758]+0x118`'s `[+0]` is set, queues a per-group average into the UI (`005339da`…`00533a28`) | High (the slot, the two walks, the per-group gate and both caller enumerations are named instructions and printed counts on the repaired function table). **Medium** that the caller sets are complete — `callto:` sees direct calls and `.rdata` code pointers, so an indirect call through a computed pointer would be missed | ● active (contested) | [EXP-0082](../experiments/EXP-0082-ai-behaviour/) |
| AI-GROUP-009 | **The unit of AI is a runtime *group*: a `0x48`-byte `CObList` subclass owning a `0x50`-byte AI record, formed at spawn by equality of the `.alm` type-6 group id.** Constructor `FUN_0050f81b`: `grp+0x40 = 0`, `grp+0x44 = 0`, then `new(0x50)` then `FUN_0052cc40` then `grp+0x3c` (`0050f86d`…`0050f8a8`). That record's constructor zeroes `0x14` dwords, sets `grpAI+0x20 = 0` (the order) and **`grpAI+0x45 = 1`** (AI enabled), and allocates a `0x1c`-byte `CList` with growBy 10 at `grpAI+0x4c` — the patrol path (`0052cc4a`…`0052cc89`). Membership: the type-6 spawner `FUN_004e26bb` adds each actor to the world list, sets `actor+0x14 = Player`, then scans `player+0x24` for a group whose **`grp+0x1c` equals the loader struct's `+0x40`** (`004e2f55`/`004e2f58`); on a miss it allocates `0x48` (`004e2f89`), appends to `player+0x24` (`004e2fec`) and stores that value (`004e3006`). The struct's `+0x40` is the file's `+0x42` at instruction level — case 6 reads 4 bytes into `struct+0x40` at `00512f1a` and keeps its running maximum at `mapObj+0x90` (`00512f3d`/`00512f40`) — so this is `ALM-UNIT-048`'s **group id** with a consumer, not a label from the trigger grammar. `FUN_0050f950` (AddMember, 15 sites / 11 owners) also writes `grp+0x44 = actor+0x14`, the owning `Player`. **Corpus, 38 maps / 8094 placed units** (`tools/aibehav`, replaying that rule per owner-and-groupId pair): **2264 groups**, mean **3.58** members, **686 singletons (30.3 %)**, largest **133** (`Horror.alm`), most groups in one map **362** (`Forester.alm`), and only **5** units carry group id 0 | High (the allocation sizes, the ctor defaults, the match instruction and the loader's own store are cited instructions; the file-to-struct binding is a read boundary, not an inference). **Medium** that the corpus figures describe what the engine builds — they replay the match rule but do not observe the engine doing it | ● active | [EXP-0082](../experiments/EXP-0082-ai-behaviour/) |
| AI-ORDER-010 | **`grpAI+0x20` is the group order byte and it has eight live values; `0` is what hands control to the members' own states.** `FUN_00533ae0` forces `0xff` when the group is empty (`00533af4`), then `CMP ECX,0x11` / `JG` (above `0x11`, only `0xff` acts) / `JZ` / `CMP ECX,0x5` / `JA` (6..0x10: nothing) / `JMP [ECX*4 + 0x533cb8]` (`00533b00`…`00533b18`). The 6-entry table, **read out of the PE through its own section table** (`tools/aibehav`), is `00533b1f 00533b7a 00533b87 00533b99 00533c13 00533c22` — **0** per-member `FUN_0052ce50`; **1** `FUN_00536ef0` guard; **2** `FUN_005370f0(grp, grpAI+0x0a)`, `MOVE-ORDER-023`'s group move; **3** `FUN_005365e0` + `FUN_00536d50` + per-member engage/heal — *labelled "attack" here and **amended to `Stand Ground` by [EXP-0112]**: the arm has no radius clip and no walk, and its scorer vetoes every candidate past reach (`AI-STAND-076`, `AI-REACH-072`)*; **4** `FUN_00537510`; **5** `FUN_005371e0`; plus **0x11** `FUN_005372b0` (`00533c31`) and **0xff** per-member `FUN_0052e110` (`00533c43`). After every arm a tail runs for every member: `FUN_0052f200` then `FUN_0052f090`, else `FUN_0052f260` then `FUN_0052f3e0` (`00533c7b`…`00533cae`). Writers, from `EnumRefs` on both store forms filtered to the AI module: `0` — the ctor plus twelve command handlers over 8 owners; `1` — `FUN_00534ac0` only (`00534c30`); `3` — `FUN_00534cb0` (`00534e0b`), `FUN_00535920` (`00535ae3`), `FUN_0053c030` (`0053c147`); `2` — `FUN_0053c030` and one orphan run `ORPHAN[005382ed..005384b2]` at `005383ab`; `4` — `FUN_005340a0`; `5` — `FUN_00534390`; `0x11` — `FUN_00534790`, `FUN_0053c030`. **A group under order 1 or 3 never evaluates `actor+0x50` at all** | High (the bounds, the table and the arm targets are the routine's own instructions plus a PE read, and the table's extent is fixed by the `CMP ECX,0x5`). **Medium** on the writer enumeration's completeness — it is over two store forms (a byte store of an immediate at `+0x20`, and the `BL` register form), so a wider store spanning `+0x20` and its neighbour, or a copy of the whole record, is invisible | ● active | [EXP-0082](../experiments/EXP-0082-ai-behaviour/) |
| AI-STATE-011 | **`actor+0x50` is the per-actor AI state, its switch has 27 arms of which 12 are distinct, and its value at construction is `0xb` — guard.** `FUN_0052ce50`: `CMP EAX,0x1a` / `JA 0052d267` / `JMP [EAX*4 + 0x52d27c]` (`0052cfc6`…`0052cfcf`). The 27-entry table, read out of the PE: `0` clear `actor+0x54` and return; `1` re-issue the stored move (`order+8 = 1`); `2` walk to `order+0xa`, then `order+8 = 7` on arrival; `3` `FUN_0052e940(actor, order+0xc)`; `4` occupancy block at `order+0xa`, diplomacy filter, engage, else walk there; **`0xa` `FUN_0052d940` — patrol**; **`0xb` `FUN_0052e3c0` — guard**; **`0xc` `FUN_0052e110` — acquire with no leash**; `8` and `0x11` `FUN_0052d3c0`/`FUN_0052d500(actor, order+0x10)`, the follow pair, whose own 5-arm inner table sits at `0x0052d4e4`; `0xd` `FUN_0052eea0`; `0xe` and `0xf` set `order+8` to 9 and 0xf; `0x16` `FUN_0052f090`; `0x17` `FUN_00532e60` (`AI-GUARD-007`'s radius overwrite); `0x18` and `0x19` write **hard-coded** cells (`0x4f5b`/`0x2f52`; `world+0xa453c`); `0x1a` stores `0x1a` into `actor+0x54`; `5,6,7,9,0x10,0x12..0x15` fall to the default `0052d267`, which clears `actor+0x54` and runs `FUN_0052e110`. **The default value is written by the actor's own initialiser** `FUN_004f3317`: `004f3321 MOV dword ptr [EAX + 0x50],0xb`, four instructions from `AI-SIGHT-006`'s sight default `004f3374` and `UNIT-COMBAT-006`'s reach default `004f3427`. **The `.alm` spawner writes no `+0x50`**: `EnumRefs re:` over the immediate-store form returns **81 hits / 55 owners / 0 orphan** and `FUN_004e26bb` is not among them | High (the bound, the table and the constructor default are cited instructions and a PE read; the arm labels are each that arm's own call or store). **Medium** that the state's writer set is complete — the sweep is over one store form and cannot see a state assigned through a register or copied with the actor. *[EXP-0090]: **C-6 is resolved and this row's arm-2 reading is confirmed exactly** — `actor+0x50 = 2` has one consumer, this arm, and one player-side writer, `FUN_005308a0` (order opcode `0x21`), which also supplies the `ord+0x0a` the arm walks to; `ITEM-CMD-007`'s "deferred order" and this row's "AI state" are one path (`ITEM-PICK-016`). The row's own **Medium** on writer-set completeness is **vindicated rather than lifted**: the register-form writers it could not see are real, and one of them, `005314e4`, is the pick-up's bridge (`AI-PROGRESS-034`)* | ● active (amended) | [EXP-0082](../experiments/EXP-0082-ai-behaviour/) |
| AI-GUARD-012 | **Guard is the whole of “motion without an order”, home is the cell the actor stood on when guard first ran, and ~~there is no wander anywhere in the module~~.** — *first clause amended by [EXP-0083]: on the eight maps whose script commands a patrol it is not, and the routine this row cites as an RNG destination site, `FUN_00530550`, is itself a patrol setter (`00530573 MOV dword ptr [EDI + 0x50],0xa`) rather than a stand-alone destination picker — dead, since `callto:532c00` on its only caller returns 0. The broad “no wander” clause remains withdrawn: [EXP-0087] identifies a Group-AI destination generator outside the **per-actor state machine** examined by [EXP-0082]. `FUN_005372b0` picks one of eight compass directions and steps 20 cells, re-rolling until the result is inside the map rectangle (`AI-ROAM-025`). Its local Group-cell/counter program and Swarm2 evaluation call do not by themselves prove a producer for actor destinations or a composed wandering-motion result (`AI-SWARM2GATE-107`, `AI-MOVE-023`). **`Roam` ships 0 nodes over 38 measured maps**; this is an authored-node census, not proof that no shipped actor wanders through any path. Neither the former positive “it is the wander” wording nor a restored whole-image no-wander conclusion follows. See [`retracted.md`](retracted.md).* `FUN_0052e3c0` (state `0xb`): if `order+0x00` is zero it is set to the actor's **current** cell (`0052e3d0`…`0052e3e4`) — so home is emergent, never authored; the distance from it (`FUN_00545cd0`, `0052e40e`) is compared against `actor+0xa5` (`0052e413`) and beyond it the actor gets `order+8 = 1`, `order+0xa = order+0x00` — **walk back** (`0052e424`/`0052e42e`); inside it, the occupancy block around the **post** is filtered by `FUN_0052ab60` with `wantBit 0` and any survivor goes to `FUN_0052e940` (`0052e44c`…`0052e46f`); with nothing to fight, an actor away from its post walks back (`0052e4a7`) and one at its post runs `FUN_0052e110` (`0052e4bf`). The group arm `FUN_00536ef0` (order 1) repeats this for a whole group around `grpAI+0x28` and ends each member the same way (`00537049` engage, `0053707d` `order+8 = 0xb`, `005370a5` walk home). **The following per-actor RNG-site notes are not a no-wander proof**: the earlier enumeration cited 19 `rand()` sites in `0052a000..0053f000` (`EnumRefs callto:554a60`, 87 / 44 / 0 image-wide); its two described per-actor *destination* sites write constants — `FUN_00530550` builds x in {70,100} from `rand()*2/0x8000` and y in 60..70 from `rand()*11/0x8000` (`0053067a`…`005306ba`), and jump-table arm `0x18` writes the literals `0x4f5b` and `0x2f52` (`0052d151`, `0052d185`). Neither is a neighbourhood of the actor | High (the post's initialisation, the leash test and both walk-back stores are cited instructions in two routines read end to end). The former **Medium** whole-image “no wander” conclusion is withdrawn. The local Group-cell generator does not establish composed actor wandering motion; that conclusion remains **Unknown** | ● active (amended) | [EXP-0082](../experiments/EXP-0082-ai-behaviour/) |
| AI-PATROL-013 | ~~**There is a patrol state, it is a ring of authored waypoints, and no shipped file reaches it.**~~ — *the second clause is **refuted** by [EXP-0083]: the `.ini` census below is exact and stands, but the mission description file is not the only writer of `actor+0x50 = 0xa`. The `.alm`'s **own type-7 script** reaches the state — 14 nodes on 8 campaign maps (`AI-PATROL-017`) — and the ring it builds is two nodes, not an authored path (`AI-PATROL-018`). Read the original clause as: no shipped file reaches **`FUN_004e147b`**. The rest of the row is untouched.* **There is a patrol state, it is a ring of waypoints, and the mission description file that authors one does not ship.** `FUN_0052d940` (state `0xa`) clears the move order, **runs guard first** (`0052d980 CALL 0x0052e3c0` — a patrolling actor still fights), and when guard leaves the order idle or guarding it advances the path: `order+0x02` is the current waypoint; on arrival it finds that node in the list at `order+0x90` and takes the **next**, falling back to the head, so the path is a **ring** (`0052d9bb`…`0052d9e4`); then `order+8 = 1`, `order+0xa = waypoint`. `FUN_0052d940` has **one caller** — the jump table. The path is loaded by `FUN_004e147b(group, sectionName)`, whose own strings are `Patrol path in section [`, `] not found`, `Error in patrol point [`, `] line `, `Patrol path loaded.`: it empties `grpAI+0x4c`, finds the named section of the **mission description file**, and for each line requires a `';'` at index 1 or above (`004e1573`/`004e1580`), `atoi`s the two halves, requires **`7 < x < 0x88` and `7 < y < 0x88`** (`004e162a`…`004e1646`), appends `x + (y << 8)`, and if the list is non-empty calls `FUN_005353e0`, which puts every member into state `0xa`. `SESS-LOAD-009` gives that file's path: `World\Mission\<n>.ini`. **Corpus: 0 nodes ending `.ini` or under `world\mission` in any of the 8 live archives, and 0 files under any `Mission` directory in the GOG root (120 files), `gameversions/en` (120) or `gameversions/ru` (42).** So the state, its loader, its four operator strings and its ring-walk are reachable code with no shipped input | High (the ring advance, the semicolon grammar and the four bounds are cited instructions in two routines read end to end; the corpus half is a walk of both preserved roots and every archive's node list, which is immune to a code-enumeration blind spot). **Medium** that a patrol can *only* be authored this way — `FUN_005353e0` has one caller and `grpAI+0x4c` was not swept for other writers — *[EXP-0083] swept the **state** instead (`re:` on the store form, 4 hits / 4 owners / 0 orphan) and found three further setters, one of them live: `AI-PATROL-019`* | ● partially retracted | [EXP-0082](../experiments/EXP-0082-ai-behaviour/), **[EXP-0083](../experiments/EXP-0083-patrol/)** |
| AI-RADIUS-014 | **The aggression radius exists, it is separate from reach and from sight, and it belongs to the *group* — derived from geometry, floored by `ai.reg`, and jittered by ±1 every time it is used.** `FUN_00533210(grp, memberCount)` sums each member's fine coordinates (`FUN_005449e0`/`FUN_005449f0`), divides by the count, and stores the centroid at `grpAI+0x28` (cell) and `grpAI+0x24` (fine) (`0053329c`…`005332d1`); then over the members it takes `grpAI+0x2a` = max Chebyshev distance from the centroid (`FUN_0054d100`), `grpAI+0x2b` = max member sight `actor+0xa5`, and **`grpAI+0x2c` = max of (distance + that member's sight)** (`00533316`/`00533328`/`0053334e`, stored `00533370`/`0053337a`/`00533387`). `FUN_00534ac0` installs it: `grpAI+0x2d = grpAI+0x38 = grpAI+0x2c`, then either raises both to the caller's override or, when that is 0, to **`[Scanning] MinimalGuardRange`** at `session+0xa9b8` (`00534c49`…`00534c9f`). The group guard arm re-rolls the working radius ~~on every run~~ (*corrected by [EXP-0083]: on the tick a latch flips — the roll is gated on `grpAI+0x30`, which the same arm sets (`00536fbc MOV ECX,dword ptr [EAX + 0x30]` / `00536fc1 CMP ECX,EBP` / `00536fc3 JNZ 0x00536ff1`), and the taken branch only increments the counter `grpAI+0x34`; the `+4` at `00536fd9 ADD DL,0x4` is on the has-members edge alone, the empty-group edge rolling the same expression without it at `00537016`…`0053701c`. `AI-GUARD-021` names both fields*): `grpAI+0x2d = grpAI+0x38 + 4 + r` with `r = rand()*3/0x8000 - 1` in {-1,0,1} (`00536fc5`…`00536fdc`; the divisor is `session+0x00`, a constant `0x8000` set at `0052b2ea`). It clips the **candidate list** around `grpAI+0x28` (`00536f66`/`00536f6b`, drop at `00536f8b`); it is not a hit range — `AI-ACQUIRE-002`'s `actor+0x12c` still gates the blow. **Measured: the shipped `World\Data\ai.reg` sets `MinimalGuardRange = 8`, not the code default `10`** (`0052b52c PUSH 0xa`), so no group's radius is ever floored at 10. The RNG is the CRT's LCG (state times 214013 plus 2531011, shifted right 16 and masked `0x7fff`), seeded once per session from `time` (`0052b2d9`/`0052b2e2`) | High (every arithmetic step is a cited instruction in two routines read end to end, the divisor is a constant with a named writer, and the shipped value is a record in a 4-record file). ~~**Medium** that `grpAI+0x2c` is never authored — the claim rests on `FUN_00533210` being the only producer, which was not swept for~~ — *swept by [EXP-0112]: there is a **second** producer, `FUN_005333a0`, and it is the one that runs — every guard tick of every group, as `FUN_00536ef0`'s first act. Neither is authored; what the sweep also shows is that `grpAI+0x2c`'s **only readers** are the two inside `FUN_00534ac0`, so the value this row computes is consumed at load and never again, and the radius the arm rolls is frozen at the last guard issue (`AI-RADFREEZE-075`)* — and **Medium** that the roll is once per latch flip, since `grpAI+0x30` was not swept for writers outside `FUN_00536ef0` | ● active (amended, contested) | [EXP-0082](../experiments/EXP-0082-ai-behaviour/), **[EXP-0083](../experiments/EXP-0083-patrol/)** |
| AI-AUTHOR-015 | **A behaviour is not authored per unit anywhere.** ~~Two setters exist, and what picks between them is the map's roster order — or, for content that does not ship, two keys in the mission description file.~~ — *amended by [EXP-0083]: the per-unit half stands, because a command names a **group**; the rest does not. What the two setters decide is the **load-time** stance, and a shipped campaign map then commands its groups from its **own type-7 script** — 135 group-command nodes over 20 maps, nine of the eleven authored sub-commands, patrol among them (`AI-GROUPCMD-020`, `AI-PATROL-017`).* The load-time half, unchanged: `FUN_00534ac0` ends **guard**: every member `actor+0x50 = 0xb` (`00534bba`) and `grpAI+0x20 = 1` (`00534c30`). `FUN_00534cb0` and `FUN_00535920` end **aggressive** — *the name is **amended to `Stand Ground` by [EXP-0112]**, which reads the arm that order selects: no clip, no walk, and a reach-vetoing scorer (`AI-STAND-076`); the key that picks this setter is itself spelled `StandGround` two sentences below* —: every member `actor+0x50 = 0xc` (`00534dba`, `00535a84`) and `grpAI+0x20 = 3` (`00534e0b`, `00535ae3`). Both setters are **also** reachable as player-issued group commands out of the dispatcher `FUN_004d5dd8` — `004d60c6` → guard, `004d60e3` → aggressive (`EnumRefs callto:534ac0` = 6 / 6 / 0, `callto:534cb0` = **2 / 2 / 0**, `callto:535920` = 6 / 5 / 0) — so the load-path choice below is the *initial* setting, not a permanent one. On the map-load path `FUN_004e1924` calls `FUN_004e3591(map, 1)` (`004e1e44 PUSH 0x1`); that routine walks `[0x00609544]`, then `player+0x24`, and — gated on its argument and on **`server+0x0c == 0`, i.e. single player** (`004e37c5`…`004e37d4`) — reads `grp+0x44` (the owning `Player`) and compares `player+0x04` against **1**: the **first type-5 record**, since `ALM-GRP-041`'s loader stores `slot+1` there. That player's groups get `FUN_00535920`; every other player's get `FUN_00534ac0(grp, 0)` (`004e37df`…`004e380b`). **Corpus: type-5 slot 0 is named `Self` on 38/38 shipped maps** (32 `Self`, 6 `self`). The second surface is `FUN_004f164c`, which builds a group from a section of the mission description file and then reads `Patrol` — a section name, non-empty then `FUN_004e147b` (`004f19f7`…`004f1a7c`) — else `StandGround` compared against `Yes` (`004f1a86`…`004f1b32`), equal then `FUN_00534cb0`, otherwise `FUN_00534ac0`. `Yes` is the **comparand, not a default**: `FUN_004e0dc5` is `RET 0xc` against four pushes and returns the global empty `CString` for an absent key (`004e0df1 PUSH 0x62e820`), so an absent `StandGround` means guard *[EXP-0125]: the open item on `FUN_004e3591`'s gate is **closed, and it wraps the whole walk**: `004e37c9` and `004e37d4` both carry a `rel8` to `004e3810`, the group-list advance, with the two setter calls the only instructions between. So a multiplayer load leaves every group at the constructor's 0 — and that is benign, because order 0 hands each member to its own `actor+0x50` machine, which writes the post instead (`AI-GATE-100`). This row's setters are also shown to anchor a post before they set the order (`AI-POST-095`, `AI-STANCE-098`).* | High (both setters, the roster test, the single-player gate and the two key reads are cited instructions; the `RET 0xc` fixes the comparand reading, and the 38/38 slot-0 name is a corpus census that could have failed). **Medium** on `server+0x0c == 0` meaning single player — that is `SESS-MAP-010`'s reading of `map+0xd4 > 1`, quoted here rather than re-derived | ● active (amended) | [EXP-0082](../experiments/EXP-0082-ai-behaviour/), **[EXP-0083](../experiments/EXP-0083-patrol/)** |
| AI-DIFF-016 | **The difficulty level does not reach behaviour — not as stats read by the AI, not as counts, not as aggression, not as cadence.** `UNIT-GATE-014` fixes the control (`precreate+0x1d0`, three buttons) and `UNIT-GATE-012` its state (`[0x005cd758]+0x84`, value set 1/2/3, default 2), and `UNIT-GATE-013` gives its one consumer: the `.alm` spawner scaling the actor's health, toHit and defence. **`EnumRefs refto:5cd758` returns 172 hits over 93 owners, 0 orphan; 21 lie in `0052a000..0053f000` and not one of them reads `+0x84`** — they take `+0x118` (the message/profiler sub-object: `005339da`, `00539ae4`, `00539c42`, `0053a118`, `0053ceb5`, `0053cf6a`), `+0x14` (the world sub-objects: `0053e403`, `0053e49f`, `0053e4da`, `0053ca40`, `0053be90`, `00539f02`, `00539c9f`), `+0xa4 + 4*i` (`0053e446`), and seven `LEA [EDX + 0x88]` on a neighbouring `CMap` in `FUN_0053b590`. Nothing else carries the level in: the only registry the AI module reads is `World\Data\ai.reg`, whose whole content is **4 records** — subkeys `Scanning` and `Tasker`, and the scalars `MinimalGuardRange = 8` and `IntelligentCons` = 15 — with no per-level dimension; the group count is content, the cadence is `SESS-CLOCK-005`'s speed index, and both radii are computed from geometry (`AI-RADIUS-014`). **`Tasker` and `Intelligent` occur zero times as literals in `rom.exe`** (against one each for `Scanning` and `MinimalGuardRange`), so that second scalar is dead config and cannot be the missing lever either | **Medium** — this is a negative resting on a reference sweep, and `refto:` cannot see the singleton reached through a pointer another routine has already loaded, nor a whole-object copy. What it establishes at High is the narrower statement it is written as: **no instruction in the AI module names `[0x005cd758]` and reads `+0x84`**. The `ai.reg` half is immune — a 4-record census and a literal count, not a code enumeration | ● active | [EXP-0082](../experiments/EXP-0082-ai-behaviour/) |
| AI-PATROL-017 | **A shipped map authors patrol through its own `.alm` script, and fourteen nodes on eight campaign maps do.** The action vocabulary's opcode 6 is a second dispatch on the node's first parameter (`TRIG-GROUP-005`); the shipped editor catalogue `Description Instants.ini` names literal **14** `Group Command : Patrol`, with `Par1 = X`, `Par2 = Y` and `Par9` a `Target_Group`. The runtime honours it: `FUN_0053c030` reads `Par0` at `rec+0x08`, `0053c040 DEC ECX` / `0053c041 CMP ECX,0x10` / `0053c044 JA 0x0053c371` bounds the switch at `Par0 ≤ 17`, and the 17-entry table at `0x0053c37c` — **read out of the PE through its own section table** (`tools/aipatrol`) — has entry 13 = `0053c2c4`, an arm that calls `FUN_005356b0(group)` (`0053c2d8`) and then `FUN_005301f0(member, X, Y)` for every member (`0053c2fd`). **Corpus, 38 maps: 135 opcode-6 nodes over 20 campaign maps and 0 over the ten loose maps**, of which **14 are `Par0 = 14`, on 8 maps** — `scn:10` ×2, `scn:50`, `scn:60`, `scn:80` ×2, `scn:90`, `scn:100` ×3, `scn:131`, `scn:141` ×3 — naming groups whose **34** type-6 members are owned by `Villagers`, `Enemies`, `Beists`, `Monsters`, `Hima` and `Friends`, and by **no** type-5 slot 0, the `Self` of `AI-AUTHOR-015`. Every one hangs off a trigger whose condition pair is an opcode-`0x10002` constant node compared **with itself** under code 0 (`TRIG-COND-003`, `TRIG-CMP-006`), so it fires on the first evaluation pass; seven of the eight maps set `+0xb4 = 1` (fire once) and `scn:131`'s `"WhenDragonStartPatrol"` sets 0, re-issuing every full tick (`TRIG-FIRE-007`). `scn:10` is the mission a new campaign enters from hero creation (`SHOP-TOWN-023`), and its trigger `"Just Start"` commands two. The `ru` root carries the identical census | High (the bound and the arm are the routine's own instructions, the table was read out of the PE rather than off a listing, and the census is a walk of every shipped map's own records — a map with no patrol node would have shown as one; the catalogue is a shipped file this repo did not author). **Medium** for the per-node member counts, which replay the group id against the type-6 records rather than observe the resolver, and which one node (`scn:100` group 25) makes ambiguous by naming a group id two owners share | ● active | **[EXP-0083](../experiments/EXP-0083-patrol/)** |
| AI-PATROL-018 | **What the command builds is a two-node ring between the creature's own cell and the commanded one — and it opens `AI-ORDER-010`'s gate itself.** `FUN_005356b0(group)` stops every member (`FUN_005306e0`'s work inline: `order+0x14 = actor+0x12c`, `order+0x60 = 0`, `mover+0x7c = 0`, `actor+0x54 = 0`, `order+0x50 = 0`, `order+0x38 = 0`) and then, once, **`00535790 MOV byte ptr [EAX + 0x20],BL` with `EAX = grp+0x3c` and `BL = 0`** (`005356b8 XOR EBX,EBX`) — `grpAI+0x20 = 0`, the arm that hands each member to its own `actor+0x50` state machine. `FUN_005301f0(actor, X, Y)` then writes **`00530283 MOV dword ptr [EDI + 0x50],0xa`**, sets `order+0x00` to the actor's current cell (`00530295`), **empties** the waypoint list at `order+0x90` (`005302b4`…`005302c0`) and rebuilds it with exactly two nodes: the actor's **own current cell** (`00530345`) and `(Y << 8) \| X` (`00530373`…`00530384`, the engine's own cell packing, `SAV-OBJ-014`); `order+0x02` is then the tail (`005303ac`), `order+0x04 = 0`, `order+0x08 = 0`, and the visibility footprint is re-stamped (`005303e1`). So a shipped patrol is **not** an authored path: it is one commanded point and wherever the creature happened to be standing. `FUN_0052d940` walks it (`AI-PATROL-013`), and `order+0x04` is a **re-anchor latch** — set on every advance (`0052d997`), consumed at the next entry to move the guard post `order+0x00` to the actor's current cell (`0052d95d`…`0052d976`) — which is why `AI-GUARD-012`'s leash never pulls a patroller home | High (every store is a cited instruction in two routines read end to end, and the group-order clear is the one write that decides whether any of it executes) | ● active | **[EXP-0083](../experiments/EXP-0083-patrol/)** |
| AI-PATROL-019 | **Four routines put an actor into the patrol state; two are live, one has no input and two are dead.** `EnumRefs "re:MOV dword ptr \[E.. \+ 0x50\],0xa"` returns **4 hits / 4 owners / 0 orphan**: `00530283 FUN_005301f0`, `0053047c FUN_005303f0`, `00530573 FUN_00530550`, `00535505 FUN_005353e0`. `FUN_005301f0` serves **two** callers (`callto:5301f0` = 2 / 2 / 0) — the script arm `FUN_0053c030` and `FUN_00535300`, whose own single caller is the **player command dispatcher** `FUN_004d5dd8` at `004d60a7`, one slot below the guard and aggressive commands `AI-AUTHOR-015` names at `004d60c6`/`004d60e3`; `FUN_00535300` clears the group order the same way (`00535370`) before the per-member loop, **so a player can order a patrol too**. `FUN_005353e0` is the mission-`.ini` route (`callto:5353e0` = 1, from `FUN_004e147b`), which ships no input. `FUN_005303f0` has **0 callers**; `FUN_00530550` has one (`FUN_00532c00`) whose own count is **0** — and it is the routine `AI-GUARD-012` cites as an RNG destination site, so that site is a patrol setter rather than a wander: it picks `x ∈ {70,100}` and `y ∈ 60..70` into `order+0x02` while the ring it builds holds the actor's cell and a literal `0` (`00530663`), which `FUN_0052d940`'s search cannot find | High for the four writers and each caller count (each is a printed enumeration on the repaired table, with 0 orphan hits, and each callee was read). **Medium** that the writer set is complete — the instrument is a regex over one store form and cannot see a state assigned through a register or copied with the actor, the same blind spot `AI-STATE-011` states | ● active | **[EXP-0083](../experiments/EXP-0083-patrol/)** |
| AI-GROUPCMD-020 | **The third authoring surface is the map's own script, it is the one shipped maps use, and it writes the group order directly.** Nine of the eleven `Par0` literals the catalogue declares appear in shipped content, 135 nodes over 20 campaign maps and none over the ten loose ones: `1 Guard` ×1, `2 Swarm` ×6, `3 Stand Ground` ×24, `4 Move` ×29, `5 Swarm 2` ×40, `10 Attack` ×5, `11 Defend` ×6, `14 Patrol` ×14, `15 Follow` ×10. What each does to `grpAI+0x20`, from the arms and from `EnumRefs "re:MOV byte ptr \[E.. \+ 0x20\],0x"`: `2` → **2** (`0053c0c7`, plus `grpAI+0x0a = (Y<<8)\|X`), `3` → **3** (`0053c147`), `17 Roam` → **0x11** (`0053c32a`, with `grpAI+0x0a` = the first member's cell), `4` and `5` through `FUN_005340a0`/`FUN_00534390` → **4** (`00534365`) and **5** (`00534655`); `1` delegates to `AI-AUTHOR-015`'s guard setter `FUN_00534ac0`; and **`10`, `11`, `14` and `15` leave the group order at 0**, each by writing it — `0053c1d3` and `0053c286` in the arms themselves, `00535790` in `FUN_005356b0` for `14` (and for `17`, before it raises `0x11`), `005351a0` in `FUN_00535140` for `15` — every one a `MOV byte ptr [E.. + 0x20],BL` against a register zeroed at the routine's head, which is why the immediate-form store sweep does not see them. So on a shipped campaign map a group's order is **not** the load-time 1 or 3 for the whole mission; it is whatever the last command left. `Par0 = 18` (`Dwell`) has no case and no shipped node, and `17` has a case and no shipped node | High for the census and for the order byte each arm writes (the sub-dispatch is one routine read whole against a PE-read table, the store sweep prints its own 75 hits / 32 owners / 1 orphan, and the catalogue is shipped). **Unknown** what group orders 2, 4, 5 and `0x11` *do* past their entry points `FUN_005370f0`/`FUN_00537510`/`FUN_005371e0`/`FUN_005372b0` — the names `Swarm`, `Move`, `Swarm 2`, `Roam` are the catalogue's, not a reading of those arms | ● active (amended) | **[EXP-0083](../experiments/EXP-0083-patrol/)** |
| AI-GUARD-021 | **`grpAI+0x30` is a has-members latch and `grpAI+0x34` a tick counter with no reader — the guard arm's unread pair, and neither is a wander.** In `FUN_00536ef0`: `00536fa4` loads the group's member count, `00536fbc MOV ECX,dword ptr [EAX + 0x30]` the latch. Non-empty and latch 0 → roll `grpAI+0x2d = grpAI+0x38 + 4 + r` (`00536fc5`…`00536fdc`), `grpAI+0x34 = 0` (`00536fe2`), `grpAI+0x30 = 1` (`00536fe8`). Non-empty and latch set → **only** `grpAI+0x34++` (`00536ff1`…`00536ff5`) and the latch re-written. Empty and latch set → the same roll **without the `+4`** (`00537016`…`0053701c`) and the counter zeroed; empty and latch clear → counter++; then latch = 0 (`0053702d`). Nothing in the routine reads `+0x34`, and nothing in it picks a destination — so `AI-GUARD-012`'s Medium clause ("a wander built from a counter rather than `rand()` would not appear") is discharged for this arm, and `AI-RADIUS-014`'s "every run" is corrected to *the tick the latch flips* *[EXP-0125]: the `+r` term is now exact rather than assumed. The divisor `[EBX]` is `AImgr+0x00` = `0x8000` (`AI-RANGE-102`), so `3 × rand() / 0x8000 − 1` is `{−1, 0, +1}` in thirds, and the empty-members edge is the identical sequence without the `+4` (`AI-JITTER-103`).* | High for the two branches and every store (one routine read end to end, and the member-count test at `00536fa4`/`00536fbf` is what separates the two edges). **Medium** that the roll is once per flip in play — `grpAI+0x30` was not swept for writers outside this routine, and the AI record is reachable as `grp+0x3c` so a displacement sweep on `0x30` would not be the instrument | ● active (amended) | **[EXP-0083](../experiments/EXP-0083-patrol/)** |

| AI-SWARM-022 | **Group order 2 (`Swarm`) is *walk to the commanded cell and fight what you see*.** `FUN_005370f0(grp, cell)` — the cell is the dispatcher's `grpAI+0x0a` (`00533b87`). It calls `FUN_005365e0(grp)` (build the candidate list) then `FUN_00536c10(grp)` (score, winner into each member's `ord+0x20`), then per member: `0053712c` target non-null → `FUN_0052e940(member, target)`; else `00537146 CMP AX,BP` current cell vs the commanded one and `0053714e FUN_00545c30` idle — not both → `0053718c MOV byte ptr [ECX + 0x8],0x1` and `00537196 MOV word ptr [EDX + 0xa],BP`, an ordinary move order to that cell; both → `0053715d` on `[member+0x14]+0x28` (`UNIT-OWNER-009`): non-zero (scenario owner) → `00537167 MOV byte ptr [EAX + 0x8],0xb` and, if `actor+0x4c` bit 2, `FUN_0052ed30`; zero (human participant) → `FUN_0052eb60`, the heal AI. **No formation, no spread, no per-member offset** | High — one routine read end to end; every store cited; the entry the arm is reached by is the PE-read table `0x00533cb8[2]` = `00533b87` | ● active | **[EXP-0087](../experiments/EXP-0087-group-arms/)** |
| AI-MOVE-023 | **Group order 4 (`Move`) walks each member to its *own* `ord+0x0a` and re-acquires on arrival.** `FUN_00537510(grp, ignored)` — it takes a second argument and **never reads it**; the destination reaches the members from the setter `FUN_005340a0`, not from here. `0053751b` recomputes the notice radius (`FUN_00533210`). Per member: arrived (`00537549 CMP AX,word ptr [ECX + 0xa]`) **and** idle **and** `ord+0x50 == 0` → `005375cf MOV byte ptr [EDX + 0x14],AL` with `AL = actor+0x12c` (reach becomes the stop distance), `FUN_005306e0` × 1, `FUN_0052fd40`, `ord+0x50 = 1`, route list `pth+0x90` cleared; else if the route list is non-empty and `pth+0x76` equals the current cell → three `FUN_005306e0` calls, `0053766c MOV dword ptr [ESI + 0x50],0xc` (actor state 0xc), `ord+0x00` = current cell, `ord+0x08 = 0`; then `005376a9`: `ord+0x50 != 0` → `FUN_0052e110` (acquisition), else `grpAI+0x44 != 0` → `ord+0x6c = 1` and the move is re-issued (`005376d1`) | High — one routine read end to end; the unread second argument is a bounded statement about *this* routine, not about the order | ● active | **[EXP-0087](../experiments/EXP-0087-group-arms/)** |
| AI-SWARM2-024 | **Group order 5 (`Swarm 2`) is `Swarm` minus the walk, and falls back to `Move`.** `FUN_005371e0(grp, cell)`: `005371eb FUN_005365e0(grp)`, then `005371f0 MOV EAX,dword ptr [EDI + 0xbb4]` / `005371f8 JZ 0x00537294` — on zero it **tail-calls `FUN_00537510` with both arguments forwarded** (`0053729c`). Otherwise `FUN_00536c10(grp)` and the same per-member body as `AI-SWARM-022` **without** the move-to-the-cell branch: engage `ord+0x20`, or `ord+0x08 = 0xb` + cast for a scenario-owned mage, or the heal AI for a human participant's unit | High for the body and the branch (one routine, cited stores). **Medium** that the fallback is or is not taken in play: what fills `AImanager+0xbb4` is not established — `disp:bb4` returns 15 hits / 13 owners, the ten in the AI module all reads and the five writes on a different object in the `0x0049xxxx` module, which is consistent with a member of the inlined `CObList` at `+0xba4` (head at `+0xba8`) rather than an assigned flag | ● active (amended) — the Medium clause is closed by `AI-CANDCOUNT-105`; the field is the candidate collection's element count, the gate is "did the build find anything", and the fallback is the ordinary path. Two sweep figures in that clause are corrected in `retracted.md` | **[EXP-0087](../experiments/EXP-0087-group-arms/)** |
| AI-ROAM-025 | **Group order `0x11` (`Roam`) updates a Group-AI cell and counter, then invokes Swarm 2 evaluation; the measured corpus contains no authored Roam nodes.** `FUN_005372b0(grp)`, reached by the out-of-table arm `00533b09 JZ 0x00533c31`. Per tick it takes the **maximum** over members of `FUN_0054d100(memberCell, grpAI+0x0a)`, the Chebyshev distance (`005372e8`, `005372f2`). `00537325 CMP EDI,0xa / JL` — **max < 10** → re-roll; else `0053732d CMP byte ptr [EAX + 0x15],0x32 / JBE` — the group counter `grpAI+0x15` **> 50** → re-roll; otherwise keep the destination. The roll: `00537337 CALL 0x00554a60` (`rand`), `SHL EAX,0x3`, `IDIV dword ptr [EBP]` → 0..7; `0053734c` / `00537354` index two signed byte tables at `world+0x58eb0` and `world+0x58eb8`, which the world init `FUN_00547eb0` fills in code as `dx = {0,1,1,1,0,-1,-1,-1}` / `dy = {-1,-1,0,1,1,1,0,-1}` (`00547f86`…`00548003`); `0053735c`…`00537376` compute `(X + 20·dx, Y + 20·dy)` from `grpAI+0x0a`/`+0x0b`; `0053737b`…`005373bf` reject and re-roll unless the result lies inside the rectangle `world+0x58ee0..+0x58ee3` (`005485a0 MOV byte ptr [EDI + 0x58ee0],0x8`). On acceptance `005373cd MOV word ptr [ECX + 0xa],DI` and `005373d4 MOV byte ptr [EDX + 0x15],0x0`; then `005373e3` calls `Swarm 2` evaluation `005371e0` and `005373f1` increments the counter after normal return. This is not a command-setter call. `AI-SWARM2GATE-107` forwards the cell only on its zero-candidate fallback to `AI-MOVE-023`, whose local body ignores that argument and uses current actor `ord+0x0a`. These bodies do not establish a producer copying the generated Group-AI cell into actor destinations. Reached movement can depend on member order and called helpers; neither guaranteed wandering motion nor absence of all other wander paths follows. The former genuine-wander/only-one headline is narrowed in `retracted.md`. **`Par0 = 17` ships 0 nodes over 38 maps** | High for the local Group-AI cell/counter program and evaluation call, not a composed movement result — one routine read end to end, the two byte tables read from their filler rather than from memory, the dispatch entry from the PE. High for the corpus 0 (a walk of every shipped map's type-7). **Medium** that the loop terminates for a group cornered against the rectangle: the re-roll has no attempt bound | ● active (amended) | **[EXP-0087](../experiments/EXP-0087-group-arms/)** |
| AI-WITHDRAW-026 | **Every group member is tested for withdrawal once per full tick, after the order dispatch, whatever the order.** The tail of `FUN_00533ae0` at `00533c66` is reached from the jump table's fall-through, from the `0xff` arm and from the end of every case. Per member: `FUN_0052f200` — `0052f20f MOVSX EAX,word ptr [ESI + 0x94]` (current health, signed) `CMP EAX,dword ptr [ECX + 0x44]`, proceed only when `health <= ord+0x44`, then `FUN_00543ab0(cell, pth+0x08)` and `FUN_0052ab60(actor, coll, 0)` keep the hostiles; non-empty → `FUN_0052f090`. Else `FUN_0052f260`, the same with `ord+0x40` and the **literal radius 2** (`0052f27e PUSH 0x2`) → `FUN_0052f3e0`. `FUN_005336a0` calls the dispatcher once per group per full tick for **every** player, gated only on `session+0xb388` or `grpAI+0x45` | High — both gates and the tail read end to end; the tail's reachability is a control-flow fact of one routine, and `00533c88 JZ 0x00533c91` (rel8 = 7) fixes the boundary | ● active | **[EXP-0087](../experiments/EXP-0087-group-arms/)** |
| AI-WITHDRAW-027 | **The two thresholds are the `Data.bin` Units columns the engine names itself — `Withdraw` and `Wimpy` — and only ranged monster classes carry a `Withdraw`.** `FUN_004f5604` streams nine consecutive slots whose titles read `typeID face tokenSize movementType dyingTime Withdraw Wimpy "See invisible" XPvalue` into `actor+0x0e`, `+0x4b`, `+0x49`, `+0x4a`, a local, **`ord+0x40`** (`004f58fb`), **`ord+0x44`** (`004f5910`), `ord+0x71`, `actor+0x1c`. Corpus, both roots identical: 56 parameterised Units rows, **12 with `Withdraw` > 0 and they are exactly `Goblin_Sling` ×4 (30), `Orc_Bow` ×4 (60), `Bat_Sonic` ×4 (15)** — every one a ranged `EquipItem`, no melee class anywhere; 24 with `Wimpy` > 0 (`Goblin_Pike` 10, `Goblin_Sling` 8, `Bee` 3, `Squirrel` 5, `Bat_Sonic` 4, `Dragon` 63). The threshold is **absolute health**, and the four tiers share the tier-1 value while `healthMax` scales ×1.6 per tier, so a tier-1 ranged monster satisfies `health <= Withdraw` **always** and tiers 2/3/4 below 62 % / 39 % / 24 %. The **Humans** table carries neither column, so a hero and a mercenary never withdraw. Shipped placements: EN 1527 of 8094 can withdraw, RU 817 of 3991 | High for the binding (four of the nine slots are independently published by `TERR-MOVE-054`, `UNIT-STREAM-001` and `HERO-XP-010`, so a one-slot misalignment breaks all four) and for the census (a walk of every shipped map on two roots; a withdrawing melee class would have shown as one) | ● active | **[EXP-0087](../experiments/EXP-0087-group-arms/)** |
| AI-WITHDRAW-028 | **The flee cell uses a distance-3 away projection without a passability test in the picker.** The former living-only centroid and identical positive-HP gate for `FUN_0052f090`/`FUN_0052f3e0` are retracted by `AI-RETREAT-274`: the first counts positive HP only to choose whether to proceed, then averages the entire filtered list; the second uses a nonempty-list gate without that scan. Both divide coordinate sums by the low byte of list count. The surviving geometry is `FUN_0054cc40(self, centroid, 3)`: fine-coordinate differences of zero become 1; the larger axis advances by signed `3*256`, the other is projected through `__ftol`; coordinates shift right 8 and clamp to `[8, dimension-9]`. The caller writes pending move 1 and its cell. No block-plane read occurs in the picker; route handling is separate (`MOVE-ALT-019`). | High for the retained geometry and corrected helper dataflow. Unknown whether mixed/all-dead filtered lists occur in ordinary runtime occupancy. | ● active (amended) | [EXP-0087](../experiments/EXP-0087-group-arms/), [EXP-0272](../experiments/EXP-0272-immediate-retreat/) |
| AI-CLASS-029 | **A spawn-time classifier exists that would overwrite both thresholds, and it never writes anything.** `FUN_00537ee0(actor)` reads `actor->vt+0x30()`; non-zero and a spellbook → `ord+0x4c = 2`, `ord+0x40 = healthMax`, `ord+0x44 = healthMax/4`; non-zero, reach > 1 and `vt+0x20() != 3` → `ord+0x4c = 1`, `ord+0x40 = healthMax/2`, `ord+0x44 = 0`; non-zero otherwise → all three zeroed; **zero → `00537eef JZ 0x00537f95`, which is the epilogue: it returns writing nothing.** `vt+0x30` is `FUN_005232d0` (`XOR EAX,EAX`) in the base actor vtable `0x59c3c0` and `FUN_00523530` (`MOV EAX,1`) in `Humanoid` `0x59c448` and `Human` `0x59c4d0`. Reachability: `callto:537ee0` → **1** (`004f6aeb`, the last act of `FUN_004f59de`); `callto:4f59de` → **1** (`004f3302` in `FUN_004f30a2`); `callto:4f30a2` → **5**, and every one of the five installs `0x59c3c0` **before** the call (`004f2c56`<`004f2c74`, `004f2d58`<`004f2d65`, `004f2e43`<`004f2e50`, `004f2f30`<`004f2f4e`, `004f303e`<`004f305c`), while both derived classes install their own vptr only after the base returns (`004f6d5e`<`004f6d6d`, `004f8e99`<`004f8ea8`). **So on every construction path the classifier sees the base slot and takes its first exit**, which is what lets the streamed `Withdraw`/`Wimpy` survive | High — three `callto:` enumerations on a repaired function table with 0 orphan hits, plus five address comparisons inside single routines. The instrument's blind spot is an indirect call through a pointer no `.rdata` slot holds; the whole chain here is `CALL rel32` | ● active | **[EXP-0087](../experiments/EXP-0087-group-arms/)** |
| AI-CLASS-030 | **A command overwrites both thresholds at runtime, and its per-class table is unreachable.** `FUN_00537fb0(a0,a1,a2,player)` and `FUN_00538100(…)` walk `world+0xa4554` and set `ord+0x40` / `ord+0x44` to `healthMax × pct / 100` (`0x51eb851f` × / `SAR ,5`), choosing `pct` by `ord+0x4c` — 0 → a0, 1 → a1, 2 → a2, `> 2` → the byte is forced to 0. `player == 0` applies to every actor whose `Player+0x28` is 0; otherwise to that player's actors. Their single caller each is one sub-arm of `FUN_004d5dd8` (`004d72ae`, `004d72d0`) that reads a mode from `cmd+0x0e` and passes `(v, 2v, 3v)` and `(0, v, v)` with **`v = 0 / 10 / 30`**. Because `AI-CLASS-029` leaves `ord+0x4c` at 0 on every path, the 2v and 3v columns are never selected: the command's effect is uniform — mode 0 disables the withdraw, mode 1 sets it to 10 % of maximum health, mode 2 to 30 %, and all three overwrite the authored `Wimpy` | High for both routines and the three modes (read end to end, one caller each). **Medium** for the *name* of the command: the outer opcode was not traced past the sub-dispatch at `004d7216` | ● active | **[EXP-0087](../experiments/EXP-0087-group-arms/)** |
| AI-ORDER-031 | **The complete immediate-form writer set of `grpAI+0x20`, with its instrument and both blind spots.** `re:MOV byte ptr \[E.. \+ 0x20\],0x` returns **75 hits / 32 owners / 1 in orphan code**; the group-order members are: `0xff` `00533af4` (`FUN_00533ae0`, empty group) · `0` `00534126`/`00534275`, `4` `00534365` (`FUN_005340a0`) · `0` `00534416`/`00534565`, `5` `00534655` (`FUN_00534390`) · `0x11` `00534885` (`FUN_00534790`, **`callto:534790` → 0**) · `1` `00534c30` (`FUN_00534ac0`) · `3` `00534e0b` (`FUN_00534cb0`) · `0` `00535451` (`FUN_005353e0`) · `3` `00535ae3` (`FUN_00535920`) · **`2` `005383ab`, inside `ORPHAN[005382ed..005384b2, vtPtrs=0]`** · `2` `0053c0c7`, `3` `0053c147`, `0x11` `0053c32a` (`FUN_0053c030`). Blind spot 1: the sweep is on **immediates** and cannot see `00535790 MOV byte ptr [EAX + 0x20],BL` (`AI-PATROL-018`). Blind spot 2: the orphan hit has **no containing function**, `callto:5382ed` → 0, `refto:5382ed` → 1 DATA reference at `005386ec`, and `range:538240:5384c0` reports 453 orphan bytes with 177 undisassembled before them — so its entry point and reachability are **not established** | **Medium** as a completeness claim, for the two named reasons. High for each listed site individually | ● active | **[EXP-0087](../experiments/EXP-0087-group-arms/)** |


| AI-CMD-032 | **The player's order vocabulary is nineteen slots of which four are empty, and it is a different table from the session's.** `FUN_004d5dd8` selects on `cmd+0x04`: `>= 1` is the order space (`004d600c`: opcode `cmd+0x09`, `SUB 0x14`, `CMP 0x12`, `JA 004d6623`, **19 direct dwords at `0x4d86ae`**), `== 0` the session space (`SESS-CMD-016`). The two overlap on the opcode byte and are otherwise unrelated — `0x22` is empty here and the item move there. Every arm opens by testing the AI manager global `[0x005f21c4]` and does nothing when it is null. **The table's *shape* was already published** — `SESS-CMD-008` gives both bounds and `ITEM-CMD-007` the 19 dwords at `0x4d86ae`; what neither has is a single one of its arms, and that is what follows. Read out of the PE by `tools/cmdtable`, with what each arm dispatches to: `0x14 → FUN_00534e20`; **`0x15` EMPTY**; `0x16 → FUN_005340a0(grp, col, row)`; `0x17 → FUN_00534ac0(grp, 0)` guard; `0x18 → FUN_00534cb0` aggressive; `0x19 → FUN_005348c0(grp, target)`; `0x1a → FUN_00534aa0 → FUN_00534390(grp, col, row)`; `0x1b → FUN_00534f80(grp, target, 0)`; `0x1c` the engine's own `"defend location comes"` then `FUN_005340a0`; **`0x1d → FUN_00535300` patrol** (`AI-PATROL-019`); `0x1e → FUN_00533d00(grp, target, spell)`; `0x1f → FUN_00533ed0(grp, col, row, spell)`; **`0x20` EMPTY**; **`0x21` the sack pick-up** (`ITEM-PICK-016`); **`0x22` EMPTY**; **`0x23` EMPTY**; `0x24 → FUN_0052f8b0(actor, building)`, refusing with `"No building #"`; `0x25` and `0x26` **share** `004d6466`, which takes item `cmd+0x10` out of `actor+0x7c`, requires an effect of kind `0x29` on it, parks the spell in `actor+0x44` and then routes to `FUN_00533d00` (at a unit) or `FUN_00533ed0` (at a cell) on the opcode. The spell of `0x1e`/`0x1f` is remapped first through a **stride-4 byte table at `0x5c2328`** indexed by `cmd+0x10` (`004d5f8c`), whose contents are not decoded here. **Nine of the fourteen live arms have the dispatcher as their only caller in the image** — `callto:` on each, 0 in orphan code: `535300` 1/1, `534e20` 1/1, `5348c0` 1/1, `534aa0` 1/1, `534f80` 1/1, `533d00` 2/1, `533ed0` 2/1, `5308a0` 1/1, `52f8b0` 1/1 — while three targets are shared with the script module's `FUN_0053c030` (`5340a0` 3/2, `534390` 2/2, `534ac0` 6/6), and the script **never enters the dispatcher**, whose whole caller chain is `FUN_004d88bf` ← `FUN_004d88f1`/`FUN_004d891a`. **That is a statement about the ROUTINES and it does not make the behaviours player-only** — a player-only routine can reach a shared behaviour one level down, and patrol is exactly that: `FUN_00535300` has one caller, but it tail-calls `FUN_005301f0`, whose `callto:` is **2 / 2 / 0** — this arm *and* `FUN_0053c030`, which is `AI-PATROL-017`'s script route. The **pick-up** does survive the same test as a *behaviour*: the complete immediate-form writer set of `actor+0x50 = 2` is **3 hits / 3 owners / 0 orphan** — `0053093b` in `FUN_005308a0`, `004d6797` in the dispatcher's own session-space `0x22` arm, and `00509cd1` in `FUN_00509cbf`, which is in the shop module and is a different class's `+0x50` — so **both writers that reach an actor are inside `FUN_004d5dd8`'s two spaces**. **Customisation: the four empty slots are absent implementation and free** — nothing in a shipped `.alm`, `.res` or save carries an order opcode — **while the opcode's width (one byte at `cmd+0x09`) and the member list (`u8` count at `cmd+0x12`, `u16` ids from `cmd+0x13`) are the multiplayer wire and are a coupling**, with a 255-member ceiling on one order | High (the table is read from the PE through the section table and printed beside its raw bytes; every arm's call, argument list and guard is a named instruction; six opcodes published from other areas land on their known arms) / **Medium** for the *labels* of the arms not read past their entry — `0x14`, `0x19`, `0x1a`, `0x1b`, `0x1e`, `0x1f`, `0x24` are named by the routine they call and by their final state store, not by a reading of the body | ● active | [EXP-0090](../experiments/EXP-0090-commands/) |
| AI-CMD-033 | **Every player order builds a brand-new group at group order 0 — so `grpAI+0x20 == 0` is not a precondition the player must arrange, it is what the dispatcher establishes — and the orders then divide into two families by what they leave behind.** The order space's prologue resolves the commanded actors (`FUN_004d4ade` per id), calls `FUN_0050fa33(Player+0x24)` — which scans the player's group list until the first group failing `FUN_00449800`, deletes that one and exits the scan, then `PUSH 0x48` / `operator new` / `FUN_0050f81b` — adds each actor with `FUN_0050f950`, and appends the group with `FUN_0051a050` (`004d5e74`…`004d5f08`). The group's AI block is constructed by `FUN_0052cc40`, whose `0052cc5a MOV byte ptr [ESI + 0x20],BL` writes **0** with `EBX` zeroed at `0052cc4e`. From each order routine's own final store: **guard leaves 1** (`00534c30`), **aggressive 3** (`00534e0b`), **move 4** (`00534365`, orders `0x16`/`0x1c`), **`0x1a` 5** (`00534655`) — those four act through `FUN_00533ae0`'s group-order arm — while **ten live arms leave 0** and act by writing a per-actor `actor+0x50` state that only `FUN_00533ae0` arm 0 evaluates: `0x14 → 0x16`, `0x19 → 3` then `0xc`, `0x1b → 0xc`/`8`, `0x1d → 0xa` patrol (through `FUN_005301f0`, `00530283`), `0x1e → 0xd`/`0xc`, `0x1f → 0xe`/`0xc`, `0x21 → 2`, `0x24 → 0xf`. `FUN_005308a0` and `FUN_0052f8b0` contain **no** `+0x20` store at all, so their group stays at the constructed 0. **This is `AI-ORDER-010`'s gate seen from the player's end**: `AI-AUTHOR-015`'s "every group on a shipped single-player map is set to order 1 or 3 at load" is about **authored** groups and says nothing about the group a command creates | High (the allocation chain, the constructor's zero and each routine's final `+0x20` and `actor+0x50` stores are all named instructions, and `EBX`/`BL` is zero in every routine quoted, each opening with its own `XOR EBX,EBX`) / **Medium** that the family split is complete: it rests on each routine's *last* `+0x20` store in program order, which cannot see a store made by a callee this experiment did not read | ● active (amended; cleanup scope, see retracted.md and SAV-GRPCMD-578) | [EXP-0090](../experiments/EXP-0090-commands/) |
| AI-PROGRESS-034 | **`FUN_005310e0` is the per-actor order-progress routine, it has two switches, and it is the bridge from the order object to the actor's act state.** The actor tick calls it four instructions before it dispatches on `actor+0x54` (`004f398c CALL 0x005310e0`, then `004f3994 MOV EAX,dword ptr [EDX + 0x54]`), and no experiment had read the call. It first **preserves** `actor+0x54` when it is 2 or `0xf` and clears it otherwise (`00531136`…`00531147`, with `EBX = 2` from `00531139`). It then dispatches on the **progress** byte `ord+0x09` — `DEC`, `CMP 0xfe`, `JA 0053124a`, 255 index bytes at `0x531828` into **6** dwords at `0x531810`, of which only `1..4` are live (`005311a4`, `005311ad`, `005311ef`, `00531228`) *(FIVE are live: [EXP-0179] reads the index table out of the PE and `0xff` maps to its own arm `0053124e`, written at `00531789` and cleared only by `FUN_00532e60`. The same routine also writes progress 4 at `0053116e`, eleven instructions before the dispatch this row describes, from `actor+0x144 & 0x100000` — `MAGIC-ACTGATE-079`; see [`retracted.md`](retracted.md))* — and, when that byte is 0 (`0053117c JZ 0053125a`), on the **pending order** byte `ord+0x08`: `DEC`, `CMP 0x0e`, **15 direct dwords at `0x531928`**, with arms `3`, `0xd` and `0xe` EMPTY. **Arm 7 is the pick-up bridge** (`005314df`): `actor+0x54 = 2` on the first pass and, on the second — when the tick has already run its own arm 2 — the completion `actor+0x50 = 0xc`, `ord+0x08 = 0`, `ord+0x50 = 1`. Both bytes are **inside the order object's serialized `0x94`-byte raw block** (`FUN_005390c0`: `ar.Write(this, 0x94)` at `005390f6`, `ar.Read(this, 0x94)` at `00539158`), so a consumer that inserts a field before them cannot load a save the original wrote — **and because a wholesale block move carries no displacement, no store-form sweep over `+0x08`/`+0x09` can see the save path at all**. Adding a *value* inside `1..0x0f` is free; adding a *field* is not | High for both switches, the preserve test and the arm-7 bridge (the two range tests, both table bases and the arm-7 body are named instructions; the tables are read from the PE) / **Medium** that arms `1..6` and `8..0xf` are what their calls suggest — they were classified by their calls and their `actor+0x54` stores, not read line by line | ● active (amended) | [EXP-0090](../experiments/EXP-0090-commands/), [EXP-0179](../experiments/EXP-0179-effect-action/) |
| AI-DEAD-035 | **Two routines sweep every actor in the world with no group test, and neither is reachable — positively, not for want of looking.** `FUN_0052cd10` and `FUN_00531070` are the same loop twice: both walk `[[AImanager+0xa50]+0xa4554]+4`, the world's whole actor list, and call `FUN_0052ce50` (the per-actor AI state machine) / `FUN_005310e0` (the order-progress routine) on every member, with **no `grpAI+0x20` test anywhere in either**. Were either live, every actor on the map would evaluate its own state regardless of its group's order — which is precisely the shape that would make the per-actor states unconditional and dissolve `AI-ORDER-010`'s gate from the other side. For both: `callto:` **0 hits**, `refto:` **0 hits**, `imm:<address>` **0 hits**. The `imm:` leg is what upgrades this past `callto:`'s known blind spot — a call through a pointer another routine loaded still needs the address materialised by some instruction, and it is not. The live callers are one each: `FUN_00533ae0` arm 0 for the state machine (`callto:52ce50` = 2 / 2 / 0, the other hit being the dead sweep) and the actor tick `FUN_004f37be` for the progress routine (`callto:5310e0` = 2 / 2 / 0, likewise) | High (three independent enumerations over the repaired table, each returning zero, and the alternative they rule out is named) | ● active | [EXP-0090](../experiments/EXP-0090-commands/) |
| AI-DEAD-036 | **`FUN_005355c0` — the only routine in the image that clears the group rate byte — is unreachable, and the fourth instrument is what settles it.** `EnumRefs callto:005355c0` **0 hits**, `refto:005355c0` **0 hits**, `imm:005355c0` **0 hits** — the same three `AI-DEAD-035` uses. Those three are not enough here, because **every dispatch table in this area lives in `.text`**, outside `callto:`'s `.rdata` window `0x580000..0x5b0000`, and a table slot is neither a reference nor an operand: `FUN_00537510`, which certainly runs, is invisible to all three for exactly that reason. So the fourth leg is a scan of the whole image for the little-endian dword `c0 55 53 00`, read through the PE's own section table (`tools/grouprate -mode scan`), and it returns **nothing** — while its three **controls**, the known jump-table slots `00533b1f`, `00539d2d` and `0053c155`, are each found at their table (`00533cb8`, `0053a190`, `0053c388`). Nor is it fallen into: the preceding function ends `c2 04 00` (`RET 0x4`) at `005355b8` and five `0x90` pad bytes separate it from `005355c0`'s `PUSH EBX` (`evidence/cited-bytes.txt`), and `EnumRefs range:00535140:005356c0` reports 5 functions, **0 orphan bytes** and 32 undisassembled — the padding. Two live neighbours were run through the Ghidra instruments as controls and both came back non-zero (`callto:005356b0` 2/1/0, `callto:00535300` 1/1/0), so the zeros are the routine's and not the tool's. Consequence: `MOVE-GROUP-030`'s "cleared to 0 when the group's pass ends" is retracted, and nothing clears `grpAI+0x44` (`MOVE-GROUP-037`) | High (four independent enumerations, one of them immune to Ghidra's function table, each returning zero, with positive controls on both the instrument and the neighbourhood) | ● active | [EXP-0094](../experiments/EXP-0094-group-rate/) |
| AI-FORM-037 | **Every `Player` carries a formation mode, it is one byte with three behaviours, and both of the things that can write it are authored surfaces.** The `Player` constructor `FUN_004faa81` allocates a `0x20`-byte settings block (`004fac7d PUSH 0x20`), constructs it with `FUN_0052ccb0` — which zeroes it and writes **one** nonzero default, `0052ccc0 MOV byte ptr [EDX + 0x1f],0x2` — and stores it at `Player+0x30` (`004facb8`); its `CreateObject` thunk `FUN_004fa9cd` allocates `0x70`, the `Player` runtime-class size, so the object is identified rather than assumed. The byte's whole surface is four instructions: `EnumRefs "re:.*\+ 0x1f\].*"` gives **8 hits / 8 owners / 0 orphan** image-wide, four of them `[ESP+0x1f]`, and the rest are that default, the single setter `FUN_00537e90` (`00537e9b`), and the two reads in the group-move setters. Behaviour, from `MOVE-GATE-035`: **`0`** never in formation, **`2`** in formation iff the spread test passes, **any other nonzero** in formation unconditionally — so 254 of 256 values alias to one behaviour. Two callers, both authored surfaces and both read out of the PE rather than off a listing: **player command opcode `0x46`, sub-code 2** (the second command dispatch's index byte table at `0x004d876e` sends `0x46` to slot 21 = `004d71a6`; the player comes from `cmd+0x05` through `FUN_004d4c6e`, the value from `cmd+0x0e` **remapped** `0→0, 1→2, 2→1`, default 2), and **trigger instant id 7**, which the shipped `Description Instants.ini` `[Group6]` names **`Set formation`** (instant table `0x0053a178` slot 6 = `00539d2d`) and which writes `rec+0x08` **raw**, with no remapping. It is in a save: `Player::Serialize` hands the block to `FUN_005392d0`, a raw `CArchive::Write`/`::Read` of `0x20` bytes (`005392e3`/`005392f3`). Corpus, both roots: **one** `Set formation` node ships in the entire 38-map corpus — `scn:110.alm`, node id 29. ~~`Par0 = 2`, every other `Par` zero — i.e. the constructor's own default~~ *— amended by [EXP-0170]: the node's slot 0 is the **`Player` reference**, type 3, value 2; its slot 1 is the `Formation` int, type 1, value **0**. Under `TRIG-PARAM-030`, published after this row, a reference-typed slot takes no `p` index, so the arm's `p0` is slot 1 and the byte written to `+0x1f` is **0** — `MOVE-GATE-035`'s *never in formation*, not the default. The node's own author label, identical on both roots, is `"D - brigands dont use formations"`. The value recorded here was the player index.* | High (the allocation, the ctor default, the setter, the two dispatch tables and the serializer are cited instructions and PE reads, and the field's surface is a complete image-wide enumeration; two published opcodes on the same table, `SHOP-MISSION-018`'s `0x3f` and `ITEM-CMD-007`'s `0x22`, land on their claimed arms) / **Medium** (that a shipped map's one node *means* what the catalogue's `Par0_NAME=Player` says: the runtime takes the value from `Par0` and the object from the resolved-target slot `rec+0x38`, and the compiled record's slot-to-parameter mapping past `rec+0x2c` was not derived) — *that Medium was correct and [EXP-0170] resolves it against the row's own corpus reading* | ● active (amended) | [EXP-0094](../experiments/EXP-0094-group-rate/) |
| AI-SPREAD-038 | **The formation spread threshold is a compile-time `2`, it lives on the AI manager and not on the world, and no shipped byte carries it.** `EnumRefs disp:a824` → **3 hits / 3 owners / 0 orphan**: two `CMP`s (the two group-move setters) and exactly one write, `0052b449 MOV byte ptr [ESI + 0xa824],BL` inside `FUN_0052b2b0`. `BL` is `2` from `0052b3d8 MOV BL,0x2` and is not written again before the store — the same register still holds 2 at `0052b45e`, twenty-one instructions later. `FUN_0052b2b0` is the constructor of the object whose `+0xa9c4` it `REP STOSD`s with `0x271` dwords, i.e. `AI-DIPLO-004`'s 50×50 diplomacy matrix, which is what fixes the object as the **AI manager**; `AImanager+0xa50` is the *world* (`AI-DEAD-035`), so `MOVE-GROUP-030`'s `world+0xa824` names the wrong object and a consumer looking for the field there would not find it. What it bounds is a **Chebyshev distance in whole cells from a member to the group centroid**, compared `JBE`, so a group of two units four cells apart passes and five cells apart fails. Corpus, at the **shipped placement positions** (`tools/grouprate -mode groups`, replaying `AI-GROUP-009`'s grouping over 38 EN maps): **1255 of 2264** groups satisfy it, and of the **1578** with more than one member, **569** (36.1 %); 686 groups are singletons. The RU root's `rom.exe`-derived evidence is byte-identical | High (a complete displacement enumeration with one writer, the value fixed by a register that no intervening instruction touches, and the owning object fixed by a second field of the same constructor that another claim already identifies) / **Medium** (the corpus figures: they measure where the *author* put the units, not where they stand when a script command fires) | ● active | [EXP-0094](../experiments/EXP-0094-group-rate/) |
| AI-ORDER-039 | **The per-actor *order* machine is `ord+0x08`, its fifteen-slot switch has twelve live arms, and all twelve are now read rather than classified by their calls.** `FUN_005310e0` reaches it only while `ord+0x09` is 0 (`00531177`…`0053117c`) — the tick the actor stands on a cell centre, `FUN_00545c30` testing `pos+0x4 == pos+0x5 == 0x80` — and dispatches `DEC ECX` / `CMP ECX,0x0e` / 15 direct dwords at `0x531928` (`0053125f`…`00531269`). Read out of the PE by `tools/aistates`: **15 entries, 3 on the default `0053165f`, 12 distinct live arms**. `1` walk to `ord+0x0a` (`FUN_00548f70`, stop 0); `2` attack `ord+0x0c` with **no distance test at all** — `ord+0x09 = 1`, `actor+0x54 = 3`, `actor+0x5c = target`; `3` empty; `4` close on the actor `ord+0x18` with stop distance `ord+0x14` (`FUN_005492a0`); **`5` and `6` the pursuit pair** (`AI-PURSUE-040`); `7` the sack pick-up bridge (`AI-PROGRESS-034`); `8` cast `ord+0x30` at the actor `ord+0x28`, `actor+0x54 = 0xd`; `9` cast at the cell `ord+0x3c`; `0xa` turn in place until `mover+0x01 == mover+0x00`, then `ord+0x08 = 0`; **`0xb` the idle turn** `FUN_00531a20` — `mover+0x01 = mover+0x00 + 0x21`, one eighth of a circle, ~~the quotient added to it being 0 for any clock value because the divisor is the session's own first dword~~ — *amended by [EXP-0109]: there is no clock value. The dividend is `190 * FUN_00554a60()` and `FUN_00554a60` is `rand()` (`AI-RAND-058`), so the dividend reaches `6 225 730`, above every vtable pointer seen in this module (`0x0059cbd0..0x0059cdf4`), and an identically-zero quotient is **not** established. The same routine's own entry gate was also unstated: the arm re-picks a facing only when `ord+0x54` is non-zero — the flag a blow sets (`AI-RETAL-056`) — or when `rand() < 0xcd`, about one evaluation in 160. See [`retracted.md`](retracted.md);* `0xc` walk to `ord+0x0a` then run the `actor+0x54 = 2` action there; `0xd`/`0xe` empty; `0xf` reach the cell `ord+0x0a` within `ord+0x14`, then `actor+0x54 = 0xf` and `FUN_0052fd40`. The **progress** byte `ord+0x09` is a second switch, 5 live arms over 255 index bytes at `0x531828`: 1 and 2 share a tail that increments the attack counter `ord+0x15` and, once it exceeds 2 with `actor+0x136` set, returns progress to 0; 3 is the step-along-the-path arm; 4 and the default park `actor+0x54 = 0x1a` *[EXP-0125]: the arm-`0xb` divisor is pinned — `AImgr+0x00` = `0x8000` — so the quotient is uniform on **0…189** and the stored facing is `mv+0x00 + 0x21 + U(0…189)` truncated to a byte. The row's remaining `0x21` = one-eighth wording describes only that term (`AI-TURN-104`, `AI-RANGE-102`).* | High (both range tests, both table bases and every arm body are cited instructions; the tables are read from the PE on both roots and 7 branch anchors re-decode to the cited addresses, 0 disagree). **Medium** on the `0xb` divisor being the session's vtable pointer — the field was read as an operand, not traced to its writer, and [EXP-0109] confirmed the gap rather than closing it: `FUN_0052b2b0` writes no vtable at `this` | ● active (amended) | [EXP-0099](../experiments/EXP-0099-actor-states/) |
| AI-PURSUE-040 | **A pursuit is an order object, not a state: `ord+0x08` in {5,6}, `ord+0x0c` the target, `ord+0x14` the stop distance — and the in-position test is *facing plus edge-to-edge distance*, with reach re-read live.** `FUN_0052e940` writes 5 (`0052ea60`/`0052ea6a`/`0052ea79`, `ord+0x14 = actor+0x12c`); `FUN_0052e110` writes 6 (`0052e31f`). Arm 5 (`005313ee`) loads `actor+0x12c` **itself**, not `ord+0x14`, and calls `FUN_00548ea0(self, target, reach)`, which is two tests: `FUN_0054a680` returns the 8-way direction self→target and it must equal `mover+0x00`, the current facing (`00548ec1`); then `FUN_0054a960` must return `<= reach` (`00548ece`). `FUN_0054a960` is **edge to edge**: each axis is `(2*cell + tokenSize + 0x1ff) * 128 + subcell`, the two token sizes are subtracted and the result clamped at 0, the axes maxed, then `SAR 8` and `INC` — so two touching actors measure **1**. In position → `ord+0x09 = 1`, `actor+0x54 = 3`, `actor+0x5c = target`. Out of position → `FUN_00532100`, which reads `ord+0x14` (`00532111`) and calls `FUN_005492a0`: inside the stop distance it **turns to face and stands** (`005492eb` `CMP AL,stop` / `JA`, then `FUN_0054a210`), outside it it paths. Arm 6 is arm 5 plus `AI-GUARD-007`'s suppression clause (`00531450`, and on it `ord+0x08 = 0`) and a turn-only fallback instead of the path. **Nothing in either arm measures elapsed time, health, or how far the pursuer has come** | High (every instruction cited is a PE-verified boundary; `005313ee`/`00531407` is anchored by its own `JZ 0x30` and `005492eb`/`005492ef` by `JA 0x1c`) | ● active | [EXP-0099](../experiments/EXP-0099-actor-states/) |
| AI-BREAK-041 | **A pursuit can be broken off, the input is the distance from the creature's *post* to the *target*, and for the default state that distance is a hard-coded 5 cells Chebyshev — not sight, not reach, not the group radius.** Nothing inside the pursuit arms ends them (`AI-PURSUE-040`); what ends one is that the `actor+0x50` arm rewrites `ord+0x08` from scratch on its own tick. For state `0xb` — the constructor default (`004f3321`) — `FUN_0052e3c0` does, in order: post ← my cell if `ord+0x00` is 0; **if `Chebyshev(me, post) >= actor+0xa5` → `ord+0x08 = 1`, `ord+0x0a = post`** (`0052e413`…`0052e42e`); build the occupancy block of Chebyshev radius **`mover+0x08`** around **`ord+0x00`** (`0052e43e`/`0052e441`/`FUN_00543ab0`), keep hostiles, engage the first survivor (`0052e485` → `FUN_0052e940`); block empty and not home → the same walk-home (`0052e4a7`); block empty and home → `FUN_0052e110`. The engage overwrites the sight test whenever it fires and the empty-block branch writes exactly what it writes, so **the `actor+0xa5` test is behaviourally dead** but for one corner — engage refuses *and* its acquire fallback writes nothing, which needs a human owner with nothing acquirable. **`mover+0x08` is 5**, written once in the image by the mover constructor `FUN_00545c00` (`00545c1a`), and read by nine instructions over eight owners, every one an occupancy-block radius: `re:MOV byte ptr \[E.. \+ 0x8\],` returns **118 hits / 69 owners / 0 orphan** and the constructor's is the only one whose base is a mover — every other hit in the AI module follows a `MOV reg,[actor+0x158]` and is an `ord+0x08` write. The metric is `FUN_00545cd0`, the max of the two absolute byte differences. The consequence a consumer must not invert: **how far the creature has chased is not measured** — it re-engages at any pursuit length while the target is within 5 of the post, and breaks off at any pursuit length once the target passes 5 | High (both routines read end to end, every cited address PE-verified with a branch anchor, and two rival models — a group radius and a pursuer leash — ruled out by the same listings rather than by absence of search). **Medium** that no *other* mechanism ends a pursuit: the enumeration behind that is a store-form sweep of `ord+0x08` and of the state arms, which cannot see a wholesale block write | ● active | [EXP-0099](../experiments/EXP-0099-actor-states/) |
| AI-POST-042 | **`ord+0x00` is the post, it is emergent rather than authored, and for a guard it never moves.** `FUN_0052e3c0` sets it to the actor's current packed cell the first time it runs on a zero (`0052e3d0` `CMP word ptr [EAX],0x0`, anchored by its own `JNZ` to `0052e3e7`). Its complete set of later writers is four: the three teardowns at the top of `FUN_0052ce50` — state 3 with `ord+0x0c` dead, states 8/`0x11` with `ord+0x10` dead, state 1 arrived — each writing the actor's *current* cell and then `actor+0x50 = 0xc`, `ord+0x08 = 0` (`0052ceb7`, `0052cf2e`, `0052cfaa`); `FUN_005310e0`'s epilogue for state 1 (`005316c0`); and the patrol arm's re-anchor (`0052d96d`). **A state-`0xb` actor is in none of those**, so its post is the cell it stood on when guard first ran — its spawn cell for a scenario creature — and `AI-BREAK-041`'s 5-cell block is fixed there for the whole mission. Patrol is guard with a moving post: `FUN_0052d940` clears `ord+0x08`, re-anchors, **calls `FUN_0052e3c0`** (`0052d980`), and advances its ring only if guard left the order at 0 or `0xb` (`0052d98b`…`0052d995`) — which is why `AI-PATROL-018`'s leash never pulls a patroller back *[EXP-0125]: **three clauses retracted.** (a) The writer set is not four — `ord+0x00` is at displacement **0** of `[actor+0x158]`, so a `disp:158` sweep sees the pointer load and never the store; the sweep that can see them finds at least **26 over 23 routines** (`AI-POST-096`). (b) It is not fixed for the mission: six reachable sites re-anchor it (`AI-POST-097`). (c) For a group under **order 1** the initialiser `FUN_0052e3c0` never runs at all, because `AI-ORDER-010` gives that order's members no `actor+0x50` evaluation — the post is the spawn cell because the **setter** wrote it (`AI-POST-095`, `AI-GATE-100`). The field's identity, its emergence, and the value for an uncommanded creature all stand. See [`retracted.md`](retracted.md).* | High (all five writers are cited instructions, the initialiser is branch-anchored, and the negative is over the complete `disp:158` population — 598 hits / 118 owners / 4 in orphan) | ● active (amended) | [EXP-0099](../experiments/EXP-0099-actor-states/) |
| AI-STATE-043 | **The complete written value set of `actor+0x50`, and the five live arms nothing writes.** Two store-form sweeps: `re:MOV dword ptr \[E.. \+ 0x50\],0x` **81 hits / 55 owners / 0 orphan** and `re:MOV dword ptr \[E.. \+ 0x50\],E..` **151 / 78 / 3 in orphan**. The displacement is shared with `ord+0x50`, so a hit counts only when the destination register is the actor rather than one just loaded from `+0x158`; that leaves the value set **{0,1,2,3,4,8,0xa,0xb,0xc,0xd,0xe,0xf,0x11,0x16}**. The register forms resolve: `0052cea9`/`0052cf20`/`0052cf9c` = `0xc` (`EBP` at `0052ce9e`), `00534dba` = `0xc` (`00534d31`), `00535a84` = `0xc` (`00535a69`), `0052f5d0` = `1` (`0052f5ca`), and **`00534f2d` = `0x16`** (`EBP` at `00534ea1`, in `FUN_00534e20`, player order `0x14`) — which closes this ledger's open item 9 on a register-form writer for the withdraw state. Against `AI-STATE-011`'s 27-arm table: `5,6,7,9,0x10,0x12..0x15` are unwritten *and* on the default, consistent; but **`0x17`, `0x18`, `0x19`, `0x1a` have live arms and no writer**, and `4` — also a live arm — is written at exactly one site, `0052d17e`, which is *inside arm `0x18`*. Five live arms therefore form a cluster reachable only from itself. `FUN_0052e3c0` has a fourth caller, `FUN_00532c00`, and `callto:532c00` is **0 hits**, so it adds no path either | **Medium**, and the instrument's blind spot is the reason: a store-form sweep cannot see a state restored by `CArchive` from a save, written through a scaled-index displacement, or produced by arithmetic in place. The *positive* half — that the listed values are written at the listed addresses — is High | ● active | [EXP-0099](../experiments/EXP-0099-actor-states/) |
| AI-THREAT-044 | **The engage routine consults a timed three-slot threat cache before it selects anything, which is why an abandoned target can be re-taken on the next tick.** `FUN_0052e940` walks `ESI = 0x78` to `0x84` step 4 over `ord+0x78`/`+0x7c`/`+0x80` (`0052e989`…`0052e9b1`); a non-null slot whose companion dword at `+0xc` — `ord+0x84`/`+0x88`/`+0x8c` — is **greater than `FUN_00554a60()`**, ~~the tick clock~~, short-circuits the whole selection into `FUN_0052e6d0(self, target, cached)` — *amended by [EXP-0109]: `FUN_00554a60` is the CRT `rand()`, read whole as a linear congruential generator returning `0..0x7fff` (`AI-RAND-058`), so the comparison this row cites correctly is against a **random draw**, not a clock, and the words "timed", "unexpired" and "expiry" below describe nothing in the image. The cache is a probabilistic short-circuit whose odds are the slot's stored value over 0x8000; whatever fills a slot fills it with a threshold, not a deadline. The structural half of the row is untouched. See [`retracted.md`](retracted.md).* Nothing anywhere records that a target was *abandoned*: the teardowns end a pursuit by changing the state, not by suppressing re-selection, so the same actor may re-enter a pursuit on the very next group tick — and while a cache slot is unexpired it will | High for the structure and the read (the loop bounds, the call and the comparison are cited instructions). **Medium** for what the cache means: the write side was not swept, so which event fills a slot, and with what threshold, is unread — *and [EXP-0109] narrows that negative without closing it: whole-image `EnumRefs disp:78` (438 hits / 210 owners) and `disp:84` (578 / 273) contain **no** access to `ord+0x78` or `ord+0x84` anywhere in the AI module, the only non-stack hits at those displacements below `0x00550000` belonging to an inventory class and to the order deserializer `FUN_00510518`; a `disp:` sweep still cannot see a wholesale copy of the order block* | ● active (amended) | [EXP-0099](../experiments/EXP-0099-actor-states/) |
| AI-ROUTE-045 | **The second thing that ends a pursuit is the route search failing, and `mover+0x98` is how it says so — closing `claims/move.md`'s "read but not traced to an effect".** `FUN_005492a0` sets `mover+0x98 = 1` when the search leaves the path list empty (`005494a4`, a ten-byte `MOV dword ptr [EAX + 0x98],0x1`). `FUN_005310e0`'s epilogue is its **only** consumer: `0053166d` `JZ` — a rel32 re-decoded from the bytes to `005317a1` — skips everything when it is 0, else it is consumed and the actor's *state* selects the response: `1` → the teardown to `0xc` with the post reset (`005316ae`); `0xa` → advance the patrol ring; `0x17` → `ord+0x09 = 0xff`; **everything else → `ord+0x08 = 0` and `FUN_005327d0`** (`00531798`/`0053179c`; `callto:5327d0` is **1 hit / 1 owner / 0 orphan**). `FUN_005327d0` then walks the whole `world+0xa4554` list keeping only candidates with `dist <= actor+0x12c` (`0053283d`/`0053283f`), diplomacy-filters, scores on **turn cost** rather than distance, and writes `ord+0x08 = 6` — or `0xb` for a non-human owner with nothing in reach (`00532aad`), or `0` for a human one. So an unreachable target does not merely stall a chase: it cancels the order and drops the actor into a reach-bounded idle | High (the flag's writer, its single reader and every branch of the epilogue are cited instructions, the epilogue's own `JZ` is a re-decoded rel32, and the consumer enumeration is a `callto:` with 0 orphan) | ● active | [EXP-0099](../experiments/EXP-0099-actor-states/) |
| AI-CENSUS-046 | **The load-time census: over every shipped map of both roots the group order byte takes exactly two values, and `0` is not one of them — so no creature *starts* a mission under `AI-BREAK-041`'s post rule.** `tools/grouporder` enumerates maps by `M7R\0` signature rather than by filename — **EN 38 (28 in `scenario.res`, 10 loose), RU 34 (28, 6)** — builds the runtime group set as the distinct `(owner, +0x42)` pairs the spawner would form (`AI-GROUP-009`), and assigns each the order `AI-AUTHOR-015`'s load walk gives it: **3** for the groups of type-5 slot 0, **1** for every other slot. Result, weighted by placed creatures: **EN 8085 at order 1, 9 at order 3, 0 at order 0** over 8094 placements in 2264 groups; **RU 3982 / 9 / 0** over 3991 in 1372. The nine order-3 placements are on four campaign maps (`41` 3, `71` 4, `150` 1, `151` 1); slot 0 owns nothing on the other 24 campaign maps and on every loose map. Five published figures reproduce to the digit from this independently written probe — `ALM-OWN-039`'s **8094** type-6 records, `AI-GROUP-009`'s **2264** groups / mean **3.58** / **30.3 %** singletons, `AI-AUTHOR-015`'s slot-0 name on **38/38** EN maps (32 `Self`, 6 `self`), `AI-GROUPCMD-020`'s node census below, and `REG-SCN-059`'s four `MissionObjects` entries with no `scenario.reg` section (`81 101 141 151`) | **Medium** — a census, and `METHODOLOGY.md` caps corpus agreement there; the counting half is exact and reproducible, but reading a count as "the post rule is not met at load" runs through `AI-AUTHOR-015`, which is itself **Medium** on the `server+0x0c == 0` single-player gate. **This experiment did not read whether that gate wraps the whole walk or only its aggressive branch** — if it wraps the walk, a multiplayer load leaves every group at the constructor's 0 (`AI-CMD-033`) and the answer inverts to 100 % post rule | ● active | [EXP-0102](../experiments/EXP-0102-group-order-census/) |
| AI-CENSUS-047 | **What the script changes, bounded twice — a player meets the group-CENTRE rule, and the post rule is a curiosity.** The type-7 action opcode 6 (`AI-GROUPCMD-020`) is the only surface that moves the byte on shipped content, and its reach is small: **135 nodes, identical on both roots, over 20 campaign maps and 0 of the loose ones**, naming **91 of 2264 groups** and **361 of 8094 placements** (4.5 %). Per sub-command, against each root's own PE table at `0x0053c37c`: `1 Guard` 1, `2 Swarm` 6, `3 Stand Ground` 24, `4 Move` 29, `5 Swarm 2` 40, `10 Attack` 5, `11 Defend` 6, `14 Patrol` 14, `15 Follow` 10; `17 Roam` and `18 Dwell` ship **0**, reproducing `AI-ROAM-025`. **45 of the 135 are named by no trigger's action slot at all** and so can never be dispatched — a counting fact, not a simulation one. Two bounds follow, pessimistic (every node fires) and carried-only: order **0**, the post rule, governs **68 down to 34 of 8094 placements (0.8 % down to 0.4 %)**; order **1**, the centre rule, **7739 up to 7819 (95.6 % up to 96.6 %)**; the rest are 106/92 at order 3, 88/86 at 5, 37/37 at 2, 10/14 at 4, and 46/12 in groups commanded to more than one order. Restricted to creatures hostile to slot 0 in either direction of the map's own diplomacy rows (`AI-DIPLO-004`/`AI-DIPLO-005`) — the only ones whose break-off a player can observe — the centre rule takes **7293 of 7571, 96.3 %**, the post rule **52, 0.7 %**. The rival that the script makes the load-time value irrelevant fails by two orders of magnitude | **Medium** — corpus, and it inherits `AI-GROUPCMD-020`'s reading of which order each arm leaves plus `AI-ORDER-010`'s writer enumeration, itself **Medium** on completeness. The keying rival was tested: 4 nodes name a group id that more than one owner slot carries, and the **28 placements** in groups reached only through such a node bound what that ambiguity can move | ● active | [EXP-0102](../experiments/EXP-0102-group-order-census/) |
| AI-CENSUS-048 | **The campaign in mission order — what the first missions actually put in front of the player.** Ordering by the map's own number, with `scenario.reg`'s `[Mission<n>]` sections separating 15 main missions from 9 sub-missions (`REG-SCN-059`): **mission 10, the first, has 35 placements of which 21 are hostile, all 35 at order 1 at load, 6 opcode-6 nodes touching 3 placements, and 2 placements ever reaching order 0 — 32 of 35 under the centre rule.** **Eight of the 28 campaign maps carry no group command whatsoever** (`20 30 31 41 51 61 101 121`), so on those the load-time order stands unchanged for the entire mission. No campaign map has a majority of its creatures away from order 1: the largest order-0 share on any map is `100.alm`'s 20 of 113, and the largest non-1 share of any kind is `151.alm`'s 92 of 237 at order 3. Per-mission table in `evidence/mission.txt`, per-map row in `evidence/map-census.csv` | **Medium** — the same census restricted to the campaign; the mission ordering is the map numbering and `scenario.reg`'s own section numbering, not a read of the mission-advance code | ● active | [EXP-0102](../experiments/EXP-0102-group-order-census/) |
| AI-ROOT-049 | **The two roots differ in four places and in none that moves this census.** All **28 campaign maps agree placement for placement, group for group and opcode-6 node for node**, and 30 of the 34 maps both roots ship have byte-identical type-5, type-6 and type-7 payloads. The four exceptions: (a) the RU root ships **6 loose maps against EN's 10** — `Beast`, `Cross`, `Kids2` and `Tomb` are EN-only; (b) RU's **`Horror.alm` is a four-record terrain-only container** — 262876 B, header record count **4**, types `0 1 2 3` only, while its own type-0 metadata still declares 415 structures and 1815 units and EN's file carries all ten records at 409386 B, so on the RU root that map has no roster, no placement, no script and no trigger; (c) `scn:100.alm` has the same 36 action nodes but **42 checks / 19 triggers on EN against 40 / 17 on RU**, a 1960-byte type-7 difference; (d) `scn:140.alm` differs in exactly one byte outside a text field — **trigger 5's fire-once flag at `+0xb4`, 1 on EN and 0 on RU**, so that trigger re-arms every pass on RU (`TRIG-FIRE-007`). The remaining differences are authored **text**: type-5 records differing only inside their 32-byte name (`lumoir` 2, `scn:100` 4, 0 outside the name in both) and one `lumoir` node differing only in a label field | **Medium** — a census over two installs; the payload comparison is sha256 plus a field-masked byte diff, which shows *that* the bytes differ and where, not what the engine does with a truncated container. **Nothing here was run**: whether RU's `Horror.alm` loads at all is untested | ● active | [EXP-0102](../experiments/EXP-0102-group-order-census/) |
| AI-CLICK-050 | **A map click is turned into an order by the CURSOR it was made under, not by what it hit — and the attack cursor is the only one that produces two different orders.** `FUN_00419ec1` is the map-click handler: ~~it discards the click as a drag when either axis moved more than `[0x005ea208] * 10 / 0x280`~~ *[EXP-0190] reads the callee and corrects this: a rectangle strictly beyond that threshold is sent to `FUN_0041a2d5` for multi-selection; it is not discarded*, returns to selection `FUN_0041a2d5` when `view+0x140` is 0, and otherwise compares the current cursor `[0x005cd794]` against each registered cursor's own handle at `cur+0x04`, one arm per cursor. `tools/attackorder` re-derives the whole map out of the PE by byte pattern on both roots (`evidence/attack-order-tables.txt`): **attack** → `FUN_0041b7b1` opcode **`0x19`** when `view+0x990` is non-zero **and** `FUN_0057272f(view+0x98c, 0x00599208)` holds, else `FUN_0041b620` opcode **`0x16`**, a plain move to the cell; **swarm** → `0x1a`; **move** → `0x16`; **patrol** → `0x1d`; **defend** → `0x1b`, and **nothing at all** when no actor is under the cursor; **select** → `FUN_0041a2d5`, no order; **pickup** → `0x21`; **town** → `0x24`; **cast** → `0x25`/`0x1e` at a unit and `0x1f`/`0x26` at a cell. `FUN_0057272f` is `CObject::IsKindOf` — `MOV EAX,[ECX]` then `CALL [EAX]`, MFC's vslot 0 `GetRuntimeClass`, then `FUN_005727e6` — and `0x00599208` is a `CRuntimeClass`. **So the click-time test is a runtime-CLASS test, and no diplomacy is consulted at click time at all** (`AI-CURSOR-052` has where it is consulted). *[EXP-0190] also fixes the physical edge: `WM_LBUTTONDOWN` begins the marquee and `WM_LBUTTONUP` reaches this routine.* | High for the cursor arms and physical edge. The drag-discard clause is retracted by EXP-0190. *[EXP-0110] read the second attack-builder caller (`AI-MINIMAP-062`) and swept the whole family (`AI-SURFACE-063`).* | ● partially retracted (EXP-0190) | [EXP-0109](../experiments/EXP-0109-attack-order/), [EXP-0190](../experiments/EXP-0190-combat-controls/) |
| AI-CLICK-051 | **What an attack order carries, from its producer: one static command record, a target actor id, and every selected unit by id.** `FUN_0041b7b1` writes into the **static** buffer at `0x0060a5b8` — there is no allocation — `MOV byte ptr [EDX + 0x9],0x19` (`0041b7d4`), the issuing player's index `word [player+0x04]` at `+0x05` (`0041b7e8`), `0` at `+0x07`, **the target actor id as a `u16` at `+0x0e`** (`0041b7fc`, the value being `view+0x990`), and `0` at `+0x12`; it then walks ~~the selection array at `view+0x9b8` (`+0x04` data, `+0x08` count)~~ — *[EXP-0110]: a **`CMapWordToPtr`**, `+0x04` the bucket array and `+0x08` the bucket count, element count at `+0x0c` = `view+0x9c4`; the walk and its conclusion are unchanged (`AI-SELECT-065`)* — and calls `FUN_004efbb6(cmd, word[unit+0x04])` once per member, **for members only, and for at most 253 of them** (`AI-FANOUT-064`), which is the count-and-ids append `AI-CMD-032` describes from the consumer end. It enqueues with `FUN_004e74fe(0x005f22d0, cmd)` and the record then travels `FUN_004d88f1`/`FUN_004d891a` → `FUN_004d88bf` → the dispatcher `FUN_004d5dd8` (`callto:4d5dd8` = **1 hit / 1 owner / 0 orphan**). The move builder `FUN_0041b620` is the same shape with a **cell** (`view+0x994`, `view+0x998`) where the target id sits. **Customisation:** the record is a fixed global, so one order is in flight at a time by construction, and the target is addressed by a `u16` **id**, not a pointer — the same wire the multiplayer path uses | High (each store is a named instruction in one routine read end to end, and the enqueue's whole caller chain is a printed `callto:` count on the repaired function table). **Medium** that `0x0060a5b8` is never written by anything else between fill and dispatch — no sweep of that address was run | ● active (amended) | [EXP-0109](../experiments/EXP-0109-attack-order/) |
| AI-CURSOR-052 | **The hostility test happens at HOVER, one step before the click, and it reads a view-side row rather than the session's diplomacy matrix.** `FUN_0041ab8e` (the hit test) returns a capability mask and stores the hit in `view+0x98c` / `view+0x990`; `UNIT-HOVER-020` gives the mask's bits. `FUN_0040d1f1` turns mask into cursor: with the gate `[0x005eb558]` set, `mask & 3` — the `CUnit`/`CAirUnit` bits — chooses **attack** (`0x005ef9c0`) and its absence chooses **swarm** (`0x005ef9bc`) (`0040d741`…`0040d75b`); with `[0x005eb560]` set the cursor is **move** ~~regardless~~ (`0040d72b`) *-- “regardless” is refuted by `AI-CURSOR-227` ([EXP-0218]): six later stores on that same path replace the value, and the second cascade's first arm never reads the latch*; and ~~the hostility bit `0x4` appears only inside `mask & 0x24`~~ *-- refuted by `AI-CURSOR-227` ([EXP-0218]): `0040d7ba` and `0040d9c4` each test the bit alone, and the second of them selects **attack** with no modifier key held --* the `mask & 0x24` test's own effect is to **suppress the select cursor** (`0040d760`…`0040d773`). The cursor identities are not inferred: `FUN_0046d3f0` registers 28 cursors by art path and `tools/attackorder` pairs each `PUSH <path>` with its `MOV [global],EAX` out of the PE — the attack cursor's art is `graphics\cursors\attack\sprites.16a` and its global is `0x005ef9c0` on both roots | ~~**Medium**, and the weakest input is named: the three gate globals are read, not explained, so every clause standing on them is capped here~~ — *the cap is **lifted** by [EXP-0110] (`AI-KEYMOD-059`), which reads all three: they are **modifier-key-held latches**, set on WM_KEYDOWN of vkey `0x11` / `0x10` and on WM_SYSKEYDOWN of vkey `0x12`, cleared on the matching WM_KEYUP and wholesale on WM_KILLFOCUS. The four-routine family this cell names is right and incomplete — two further writers, `FUN_00472b50` and `FUN_004730e0`, both clear `[0x005eb560]`.* **High** throughout, therefore: the cursor-to-art binding (a PE read on both roots), each `AND` and `MOV` cited above, and now the gates themselves. In consumer terms `[0x005eb558]` held turns the hover into the attack cursor and `[0x005eb560]` held forces the move cursor *inside the cascade at `0040d72b`, which `AI-CURSOR-226` ([EXP-0218]) establishes is the cast-aiming path and not the one an ordinary hover runs*; naming those keys Ctrl and Alt is the Windows virtual-key contract and never raises this grade | ● partially retracted (EXP-0218), amended | [EXP-0109](../experiments/EXP-0109-attack-order/), [EXP-0218](../experiments/EXP-0218-hover-cursor-cascades/) |
| AI-PANEL-053 | **Ordering an attack takes two clicks, and the first one only ARMS a mode** ~~— which the panel refuses unless the current selection supports it~~ — *[EXP-0110] read `FUN_00419e7b` and it never touches the selection: it returns `0xef` (every mode but cast) for any selection the local participant owns, `0` for one he does not, and adds cast on one flag. See `AI-PANEL-060`, `AI-PANEL-061`.* `FUN_0041b439` is the command-panel handler: `mode = button` (buttons 9 and 0xa fold to 5, `0041b487`/`0041b494`), and `view+0x99c = mode` is reached **only** when `FUN_00419e7b(view)`'s mask — *a read of `view+0x144`, not of the selection* — carries `1 << (mode-1)` or the button is 0xa (`0041b4cf`…`0041b4f1`); otherwise the routine returns having armed nothing. The armed mode is one-shot: `FUN_00419ec1` clears `view+0x99c` and notifies `0x40b` after every consuming click (`0041a27d`…`0041a289`). Both tables are read out of the PE by `tools/attackorder`, identically on both roots. **Armed-mode table, 8 dwords at `0x0040dcbd`, index `view+0x99c - 1`:** mode **1 attack**, 2 move, 3 and 7 on the join (no cursor), 4 defend, 5 cast, 6 swarm, 8 patrol. **Panel table, 6 dwords at `0x0041b608`, index `button - 3`:** buttons 3, 7 and 8 **issue an order immediately** — `FUN_0041ba8f` `0x17` Guard, `FUN_0041bbe6` `0x18` ~~aggressive~~ *Stand Ground*, `FUN_0041c63f` `0x14` *Retreat* — while 4, 5 and 6 only arm; buttons 1 and 2 are below that table's base and therefore only ever arm. So **Guard, Stand Ground and Retreat are one-click commands and attack is a two-click targeted order**. *[EXP-0190] supplies the labels from `main.txt[0..7]`, proves R takes the immediate Retreat arm and finds no default route that leaves Patrol armed.* | High for the tables and action routes. The “aggressive” label is retracted by EXP-0190; opcode `0x18` was always the same correctly read arm. | ● partially retracted (EXP-0110, EXP-0190) | [EXP-0109](../experiments/EXP-0109-attack-order/), [EXP-0110](../experiments/EXP-0110-order-gates/), [EXP-0190](../experiments/EXP-0190-combat-controls/) |
| AI-CMD-054 | **Order opcodes `0x19` and `0x1b` read as bodies rather than named by the routine they call, which lifts two of `AI-CMD-032`'s Medium labels.** `0x19` is **attack**: `FUN_005348c0(group, target)` clears every member (`FUN_005306e0`), zeroes `ord+0x50` and `ord+0x38`, sets **`grpAI+0x20 = 0`** (`00534920`), and then per member either `actor+0x50 = 3` with `ord+0x0c = target`, `ord+0x14 = actor+0x12c` and `ord+0x08 = 0` (`00534986`…`005349a5`) — `AI-STATE-011`'s arm 3, the engage routine `FUN_0052e940` — or, when `FUN_00535d30(member, target)` returns `0xffffff`, it turns the member to face and sets `actor+0x50 = 0xc` with `ord+0x00 = own cell` (`00534a3b`…`00534a59`), arm `0xc`, `FUN_0052e110`, acquire with no leash. `0x1b` is **defend a unit**: `FUN_00534f80` gives the *target* `actor+0x50 = 0xc` (`005350aa`) and every other member `actor+0x50 = 8` with `ord+0x10 = target` (`005350d4`/`005350dd`), the follow pair. So a player attack order and an unordered AI unit converge on the **same two per-actor states**, and the difference is only which one the member starts in | High (both routines read end to end; every state, order field and group-order store above is a cited instruction, and the two values reproduce `AI-CMD-033`'s independently derived `0x19` → 3 then `0xc`, and `0x1b` → `0xc`/8). **Medium** for what `FUN_00535d30` computes — it is read as a guard, not traced | ● active | [EXP-0109](../experiments/EXP-0109-attack-order/) |
| AI-STRIKE-055 | **Everything that makes a unit attempt a blow arrives through two per-actor states, and the shipped campaign reaches all of the surfaces that write them.** `actor+0x50 = 3` (engage a named target, `FUN_0052e940`) and `actor+0x50 = 0xc` (acquire whatever is visible, `FUN_0052e110`) are the only two `AI-STATE-011` arms that end in a target, and every cause reaches one of them: a **player order** `0x19`/`0x1b` (`AI-CMD-054`), the **load-time aggressive stance** `FUN_00534cb0`/`FUN_00535920`, which writes `0xc` to every member and `grpAI+0x20 = 3` (`AI-AUTHOR-015`), the **guard state** `0xb`, whose occupancy-block survivor goes to `FUN_0052e940` (`AI-GUARD-012`), the **group guard arm** `FUN_00536ef0` under group order 1, and the **script** `Par0 = 10 Attack` (`AI-GROUPCMD-020`). Being struck is **not** on this list (`AI-RETAL-056`). What shipped content reaches, from the published censuses rather than re-derived here: every group starts at order **1** or **3** and none at 0 (`AI-CENSUS-046`, EN 8085 / 9 / 0 over 8094 placements), the script's `10 Attack` ships **5** nodes and `11 Defend` **6**, over 20 campaign maps and 0 loose ones, and 8 of the 28 campaign maps carry no group command at all (`AI-CENSUS-047`, `AI-CENSUS-048`) | **Medium** — the *routing* of each named cause is High and each is a cited instruction in the row that owns it, but the claim that the list is **complete** is an enumeration over `actor+0x50`'s writers, and `AI-STATE-043` grades that set Medium for exactly the reason quoted there: a store-form sweep cannot see a state restored from a save or produced by arithmetic in place. The corpus half inherits `AI-CENSUS-046`/`AI-CENSUS-047`'s own Medium | ● active | [EXP-0109](../experiments/EXP-0109-attack-order/) |
| AI-RETAL-056 | **Being struck issues no order and produces no target: it sets one flag whose single consumer makes the victim TURN.** `FUN_00531ac0(session, attacker, victim)` is nine instructions (`evidence/rom-attackorder-excerpt2.md` section 7): `ord+0x54 = 1` (`00531ad3`), `ord+0x58 = FUN_00544a00(attacker+0x10)`, the attacker's cell (`00531aea`), `ord+0x5a = 0` (`00531afa`), then `FUN_0053d9b0(session+0xa9bc, attacker, victim)`, the diplomacy flip. `EnumRefs re:` over the whole `+0x54` access form finds exactly **one** read of `ord+0x54` — `00531a41` in `FUN_00531a20`, `AI-ORDER-039`'s order arm `0xb`, the idle turn — where a non-zero flag **skips the `rand()` gate at `00531a4d`** so the actor re-picks a facing on this evaluation instead of on about one evaluation in 160, and the flag is then cleared (`00531a93`). It does not touch `actor+0x5c`, `ord+0x08` or `actor+0x50`. **So the whole of retaliation is: turn now, become mutually hostile, and let ordinary acquisition do the rest** — and for an AI-owned victim only, `ord+0x58`/`ord+0x5a` additionally force the attacker's cell into the sight stamp for 20 ticks (`AI-SIGHT-006`). This closes one direction of `HERO-AGGRO-028`'s Medium on "the victim's own AI arms were not enumerated" | High for the hook and the arm (both read whole, every field a cited instruction). **Medium** for "exactly one reader": `re:` prints one preceding instruction, so a load of `actor+0x158` two or more instructions before its use is invisible to the filter, and a wholesale copy of the order block carries no displacement at all | ● active | [EXP-0109](../experiments/EXP-0109-attack-order/) |
| AI-ARBITER-057 | **One arbiter, not two: the AI tick applies the same machine to every player's groups, and ownership enters only as a local clause inside shared routines.** `FUN_005336a0` walks the player list it is handed, then each player's group list at `player+0x24`, and calls `FUN_00533ae0` per group gated **only** on `session+0xb388` or that group's own `grpAI+0x45` (`00533832`…`00533849`). **It contains no owner test at all**: all sixteen of its `+ 0x28` accesses are `[ESP + 0x28]`, `[EDI + 0x28]` or `[EBX + 0x28]` on the tick's own timer sub-objects, and none is preceded by a `+0x14` load (listing in `evidence/rom-attackorder-excerpt2.md` section 11). Where ownership does act it is `UNIT-OWNER-009`'s `actor+0x14` then `owner+0x28` idiom, which `EnumRefs re:` finds at **22 sites over 21 distinct owners in `0x0052a000..0x0054f680`, 0 in orphan code** — among them `FUN_0052e110` (`0052e340`), `FUN_0052e940` (`0052e96c`, `0052ea40`), `FUN_005310e0` (`00531460`), `FUN_005327d0` (`00532a13`, `00532a9f`), `FUN_00533ae0` (`00533bdf`), `FUN_005365e0`, `FUN_00536ef0`, `FUN_005370f0`, `FUN_005371e0` and `FUN_0053d9b0` (`0053da02`, `0053da30`). **So the customisation seam for AI behaviour is one machine with 22 owner-conditioned clauses, not two behaviour trees** | High for the tick (read whole; the absence is an exhaustive read of one routine, not a search) and for each listed site individually. **Medium** for the clause list's completeness — `re:` prints one preceding instruction, so an owner fetched more than one instruction before its use, or held in a register across a call, is not counted | ● active | [EXP-0109](../experiments/EXP-0109-attack-order/) |
| AI-RAND-058 | **`FUN_00554a60` is the CRT `rand()`, and two published rows read it as a clock.** Fourteen instructions, read whole: `FUN_0055bcb0()` returns a block whose `+0x14` holds the seed; `seed = seed * 214013 + 2531011` is built as `LEA EDX,[ECX + ECX*0x2]` / `LEA EDX,[ECX + EDX*0x4]` / `SHL EDX,0x4` / `ADD EDX,ECX` / `SHL EDX,0x8` / `SUB EDX,ECX` / `LEA ECX,[ECX + EDX*0x4 + 0x269ec3]` (`00554a68`…`00554a78`); the return is `SHR EAX,0x10` then `AND EAX,0x7fff` — MSVC's linear congruential generator with `RAND_MAX` `0x7fff`. `AI-GUARD-012` calls it `rand()` and is right; **`AI-THREAT-044` calls it "the tick clock" and is not**, so that row's threat cache is gated by a **random draw in `0..0x7fff`**, not by an expiry, and its words "timed", "unexpired" and "expiry" do not describe the comparison it otherwise cites correctly. **`AI-ORDER-039`'s arm-`0xb` clause fails for the same reason**: there is no clock value, and the quotient it calls "0 for any clock value" is `190 * rand() / [session]`, whose numerator reaches `6 225 730` while every vtable pointer this experiment saw in the AI module lies in `0x0059cbd0..0x0059cdf4`, all **below** it — so an identically-zero quotient is not established *[EXP-0125]: the Medium is **closed**. `FUN_0052b2b0` writes no vtable at `this` because it writes a number: `0052b2ea MOV dword ptr [ESI],0x8000` on the AI manager, the global at `[0x005f21c4]`. `0x8000` is `RAND_MAX + 1`, so `n × rand() / AImgr[0]` is `floor(n·rand()/32768)` and the arm-`0xb` quotient is **uniform on 0…189**, not merely unestablished as zero (`AI-RANGE-102`, `AI-TURN-104`).* | High for the identification (a complete LCG read at instruction level, with both constants recovered from the shift-and-add chain rather than from an immediate, and the `0x7fff` mask fixing `RAND_MAX`). **Medium** for the arm-`0xb` consequence, because the session's own vtable address was not pinned — `FUN_0052b2b0` writes no vtable at `this`, so the bound above is over the module's vtables and not over the divisor itself | ● active (amended) | [EXP-0109](../experiments/EXP-0109-attack-order/) |
| AI-KEYMOD-059 | **The three globals that gate the cursors are modifier-key latches: `[0x005eb558]` vkey `0x11`, `[0x005eb55c]` vkey `0x10`, `[0x005eb560]` vkey `0x12` — the last of them the system key, established from inside the image.** `tools/ordergates` reads the WM_KEYDOWN handler's own jump table out of the PE (`0xb9` index bytes at `0x00472ee8`, arms at `0x00472e94`) and only **two** of its thirteen arms touch a global: vkey `0x11` reaches `00472d39 MOV dword ptr [0x005eb558],0x1` and vkey `0x10` reaches `00472d48 MOV dword ptr [0x005eb55c],0x1`. `FUN_00473070` clears them by the same vkeys and `[0x005eb560]` by vkey `0x12` — its `83 e8 10` `SUB EAX,0x10` / `JZ` / `DEC` / `JZ` / `DEC` chain is decoded from the bytes, not assumed — and `FUN_00475780` clears all three together (`00475790`/`00475795`/`0047579a`). The **message map** is read from the PE as `AFX_MSGMAP_ENTRY` records at `pfn-0x14`: `00472b80` from `0x100` WM_KEYDOWN, `00473070` from `0x101` WM_KEYUP, `00472fb0` from `0x104` WM_SYSKEYDOWN, `00475780` from `0x008` WM_KILLFOCUS. **Set on key-down, cleared on key-up of the same key, cleared wholesale on focus loss** — a key-held latch by construction, with no second model fitting it. That `0x12` is the **system** key needs no outside fact: `FUN_00472fb0`, the WM_SYSKEYDOWN handler, sets `[0x005eb560]` on two independent routes — `nChar == 0x12` (`00472fc1`) and the three hotkey arms guarded by `nFlags & 0x2000` (`00472fdd`, `00473006`) — which agree only if `0x12` is the key that bit reports. **Consumer reading:** `[0x005eb558]` is the modifier that turns the hover into an attack cursor, `[0x005eb560]` the one that forces the move cursor (`AI-CURSOR-052`), and `[0x005eb55c]` the one that makes a marquee *add* to the selection instead of replacing it (`0041a379` `SETZ AL`, `FUN_0041a2d5`). Two more writers `EXP-0109` did not name, both clearing `[0x005eb560]`: `00472b5f` (`FUN_00472b50`) and `004730e7` (`FUN_004730e0`) | High for the **mechanism** and the vkeys — the jump table and the message map are PE reads on both roots with byte-identical bodies, every store and clear is a cited instruction, and `refto:` gives the three globals 19 / 17 / 11 references over 5 / 10 / 8 owners, 0 orphan, so no writer is unaccounted for. **Naming vkey `0x10`/`0x11`/`0x12` Shift/Ctrl/Alt, and `0x100`/`0x101`/`0x104`/`0x008` WM_KEYDOWN/KEYUP/SYSKEYDOWN/KILLFOCUS, is the Windows platform contract, not a reading of ours** — it is labelled, it never raises a grade, and every clause above survives its withdrawal because the mechanism does not depend on it. Owner testimony is the falsifier for the naming and the prediction is what this experiment ships in its place | ● active | [EXP-0110](../experiments/EXP-0110-order-gates/) |
| AI-PANEL-060 | **The routine `AI-PANEL-053` calls the selection capability mask never looks at the selection: it is twenty instructions over one view-side flags word, and the only mode any selection can be refused is CAST.** `FUN_00419e7b(view)` read whole (`evidence/rom-ordergates-excerpt.md` section 1): `00419e87` loads `view+0x144`, `00419e8d AND ECX,0x4` — non-zero **returns 0** (`00419e94 XOR EAX,EAX`); otherwise `00419e98 MOV dword ptr [EBP + -0x4],0xef`, then `00419ea2` reloads `view+0x144`, `00419ea8 AND EAX,0x200` and on that bit `00419eb4 OR ECX,0x10`. It touches `view+0x9b8`, `view+0x138` and `view+0x140` **not at all**. Against `AI-PANEL-053`'s mode numbering (1 attack, 2 move, 3 join, 4 defend, 5 cast, 6 swarm, 7 join, 8 patrol) and its arming test `1 << (mode-1)`, the constant `0xef` is **every mode except 5** — so **attack, move, defend, swarm and patrol are armable for any selection whose bit `0x4` is clear, whatever classes it holds**, and cast alone is conditional. **Customisation:** a consumer that offers the attack mode over any owned selection matches the original exactly; one that filters the panel by unit class shows a control the original does not withhold | High (the routine is read end to end at instruction level, and `tools/ordergates` re-reads its immediates out of the PE on both roots by **linear decode** from the entry point — it must reach `RET` or print where it stopped, and its output matches the listing instruction for instruction). The alternative ruled out is named and was this experiment's own H2: a per-class walk over the selection, which predicts reads of `view+0x9b8` that the routine does not contain | ● active | [EXP-0110](../experiments/EXP-0110-order-gates/) |
| AI-PANEL-061 | **`view+0x144` is a selection-derived flags word rebuilt on every selection change, and only two of its six bits reach the panel — one of them ownership.** Its sole writer is `FUN_004190f0`, the selection-maintenance routine and one of `FUN_00419e7b`'s two callers (`disp:144` = 125 hits / 63 owners; every store in the view is inside `FUN_004190f0`). Reset at `00419166`, then: **`0x1`** `0041948b OR AL,0x1` after `strcmp` against `"CUnit"`; **`0x200`** `004194c2 OR AH,0x2` when the current accepted loop object's `+0x20` is `0x17` or `0x18` (primary-only wording corrected by `AI-SPELLCAP-288`) (`00419499`/`004194a2`) **and** its `+0x18` is non-zero (`004194af`); **`0x8`** `00419cbb OR AL,0x8` when `[obj+0x18c] & 1`; **`0x2`** `00419ce6 OR EDX,0x2` after `strcmp` against `0x5b8944` = `"CAirUnit"`; **`0x20`** `00419d12 OR ECX,0x20` after `strcmp` against `0x5b8950` = `"CStructure"`; and an arm that forces the whole word to `0x8` (`00419d23`). **Bit `0x4` is ownership**: `00419d35 CMP dword ptr [view + 0x140],0x0` (the selection count), then `00419d41` loads the primary object `view+0x138`, `00419d4a MOV EDX,[EAX + 0x14]` its `CPlayer`, `00419d4d CMP EDX,dword ptr [ECX + 0x9b4]`, and **only when they differ** `00419d5e OR ECX,0x4`. So the panel is disabled outright for a selection the local participant does not own, and that is the same comparison `UNIT-VPLAYER-022` settles. The routine then notifies the panel `0x409` with `FUN_00419e7b`'s mask, or `0x40a` with **zero** when `view+0x144 & 0x24` (`00419d7f`) | High for each bit's set site and its guard — every `OR` above is a cited instruction with its `strcmp` literal dumped from the image (`StrDump`) or its compare quoted, in one routine read whole. **Medium** that these are all the writers: `disp:144` is a displacement sweep and cannot see a store through a computed base, though `MOV [reg + 0x144],x` is the only form present. **Not established**: what bits `0x1`, `0x2`, `0x8` and `0x20` are *for* — only where each is set. Only `0x4` and `0x200` reach `FUN_00419e7b` | ● partially retracted (primary-only Cast clause) | [EXP-0110](../experiments/EXP-0110-order-gates/) |
| AI-MINIMAP-062 | **There is a second click surface, it is the SMALL-cursor one, and its gates are not the map click's.** `FUN_0048fdb0` is a `RET 0xc` handler reading a `CPoint` at `[ESP+0x34]`/`[ESP+0x38]` and never reading its first argument, reached through exactly one `.rdata` vtable slot (`callto:48fdb0` = 1 hit / 0 owners / 0 orphan, slot `0059a6cc`). Like `FUN_00419ec1` it dispatches on the current cursor `[0x005cd794]` against each registered cursor's own handle — and it covers **exactly the six cursors whose shipped art paths begin `s`**, while `FUN_00419ec1` covers exactly the eleven that do not, a clean and complete partition of `AI-CLICK-050`'s registry. The arm map, re-read out of the PE by `tools/ordergates` on both roots: `0048fed2` **sdefault** notifies `0x406` with the computed cell and issues **no order**; `0048ff19` **smove** calls `FUN_0041b620` opcode `0x16`; `0048ff34` **sattack** calls `FUN_0040e59a`, and on a non-zero id `FUN_0041b7b1` `0x19`, else `FUN_0041b916` **`0x1a` swarm**; `0048ff68` **sdefend** calls `FUN_0040e59a`, and on a non-zero id `FUN_0041bd3d` `0x1b`, else nothing; `0048ff8f` **scast** calls `FUN_0040e59a` and **discards the result**; `0048ffa7` **spatrol** calls `FUN_0041bf6c` `0x1d`. It ends by clearing `view+0x99c` and notifying `0x40b` (`0048ffc9`…`0048ffe0`), the same one-shot armed-mode clear the map click performs. **Five gates differ.** No drag threshold and no `view+0x140` return-to-select. **No `IsKindOf("CUnit")` on the attack arm** — the map click requires `view+0x990 != 0` *and* the class test (`AI-CLICK-050`); here a non-zero id is enough, so a `CStructure` under the pointer is an acceptable target. A different hit test: `FUN_0040e59a(x,y)` walks the object map at `view+0x9b8` and returns the **key** of the first entry whose `+0x78` is 0 and whose `+0x34`/`+0x38` equal the two arguments (`0040e671`…`0040e691`), against the hover's screen-rect walk. A different attack fallback: `0x1a` swarm, where the map click falls back to `0x16` move. And cast is unreachable from it | High for the arm map and the five differences (the cursor-to-builder pairing and each builder's opcode byte are read out of the PE through its own section table on both roots with identical bodies; the handler is read end to end; the `s`/non-`s` partition is a property of the shipped art paths `AI-CLICK-050` already publishes). **The window's identity is inferred from those art names, not established** — what the image fixes is a second window whose click dispatches the small cursor set, and `s` for the scaled overview is the shipped naming, not a reading | ● active | [EXP-0110](../experiments/EXP-0110-order-gates/) |
| AI-SURFACE-063 | **The complete producer census: exactly three input surfaces reach the order-builder family, plus one producer with no cursor arm at all.** `callto:` over all thirteen builders on the repaired table (`evidence/rom-ordergates-enum.md`), 0 orphan throughout: `FUN_00419ec1` the map click (`41b620` twice, `41b7b1`, `41b916`, `41bd3d`, `41bf6c`, `41c0dc` twice, `41c2d6`, `41c4be`, `41c796`); `FUN_0041b439` the command panel (`41ba8f`, `41bbe6`, `41c63f` — the three buttons `AI-PANEL-053` says issue at once); `FUN_0048fdb0` the small-cursor surface (`41b620`, `41b7b1`, `41b916`, `41bd3d`, `41bf6c`). Nothing else calls any of them. The thirteenth, **`FUN_0041c98f`, writes opcode `0x22`** (`0041c9a2 MOV byte ptr [EAX + 0x9],0x22`) and enqueues through the same `FUN_004e74fe` (`0041ca11`), and has **7 hits over 6 owners** — `FUN_0040f0a0`, `FUN_00492410` (twice), `FUN_004a8125`, `FUN_00482820`, `FUN_004a4e40`, `FUN_004a65d0` — **none of them a cursor arm**. That is very likely not an *order* at all: `AI-CMD-032` has the order space's `0x22` **empty** and the session space's `0x22` as the item move, `ITEM-CMD-007` names `0x22` as the one command that moves everything, and those six owners sit in the shop, inventory and town modules. **Inferred, not established** — no instruction setting `cmd+0x04` on this path was read, and `cmd+0x04` is what chooses the space. So `AI-CLICK-050`'s open Medium is bounded rather than closed: the attack builder has two callers and both are now read, the three cursor/panel surfaces are the whole of the order side, and the one remaining producer is probably the inventory's | **Medium**, and the instrument is named: `callto:` decompiles only hits with a containing function and prints `.rdata` slots separately, so it shares the blind spots `AI-FILTER-001` and `AI-ORDER-031` state — which is exactly how `EXP-0109`, sweeping one builder, reported two surfaces where there are three. High for each individual pairing quoted, every one a cited `CALL` site with its target | ● partially retracted | [EXP-0110](../experiments/EXP-0110-order-gates/) |
| AI-FANOUT-064 | **One order carries at most 253 unit ids and the 254th is dropped in silence.** `FUN_004efbb6(cmd, id)` is fourteen instructions read whole: `004efbc2 MOV CL,byte ptr [EAX + 0x12]` — the member count is a **single byte** at `cmd+0x12` — `004efbc5 CMP ECX,0xfd` / `004efbcb JGE 0x004efbf0`, **which returns having done nothing**; otherwise `004efbdc MOV word ptr [ECX + EAX*0x2 + 0x13],DX` and `004efbed` increments the byte. So the ids are `u16` at `cmd + 0x13 + 2*i`, the record reaches `0x13 + 2*253 = 0x20d` bytes at most, and past 253 there is **no error, no truncation flag and no wrap** — the append is a no-op. **Customisation:** this is the one place where a consumer with an unbounded selection and the engine diverge without a symptom. The selection container itself is a growing `CMapWordToPtr` (`AI-SELECT-065`) and imposes no such bound, so a selection above 253 is representable and only the *order* truncates; and because the builder iterates in hash-bucket order, **which** 253 survive is decided by the hash, not by selection order | High (the routine is read end to end; the guard, the stride, the base displacement and the increment are four cited instructions in one fourteen-instruction body, and `0xfd` is an immediate in the image, not a derived figure). The alternative ruled out — that the ceiling is a buffer size checked elsewhere — is excluded by the guard being inside the append itself and by the count's width being one byte | ● active | [EXP-0110](../experiments/EXP-0110-order-gates/) |
| AI-SELECT-065 | **`view+0x9b8` is a `CMapWordToPtr` from unit id to object, not an array, and the order carries every entry the per-object flag `+0x7c` marks.** The layout is fixed by three independent uses rather than asserted: `FUN_0041b7b1`, `FUN_0040e59a` and `FUN_0041a2d5` all reach it as `ADD reg,0x9b8` and then read `+0x04` as a bucket **array of pointers** (`0041b84f MOV ECX,[EDX + EAX*0x4]`), `+0x08` as the **bucket count** they compare an index against, and the assoc's `+0x00` `pNext` / `+0x04` `nHashValue` / `+0x08` `key` (a `WORD`) / `+0x0c` `value` — MFC's `GetNextAssoc` walk, bucket chain then forward scan. `m_nCount` is therefore `0x9b8 + 0xc` = **`view+0x9c4`**, and that is exactly the field all three feed to the inlined `GetStartPosition` idiom `NEG reg` / `SBB reg,reg` (`0041b7c3`, `0040e5ac`, `0041a501`), which yields `-1` for a non-empty map and `0` for an empty one. `FUN_0041a2d5` uses the same object as a **`Lookup`** of an on-screen id (`0041a479`), which an array of selected units could not serve. The order builder walks the whole map and calls `FUN_004efbb6` with `word[obj+0x04]` **only when `[obj+0x7c]` is non-zero** (`0041b8c5`…`0041b8dc`). **So `AI-CLICK-051`'s conclusion stands and its container does not**: every selected unit travels by id, but `+0x04` is not the data pointer and `+0x08` is not the count | High for the container's identity and layout (three routines reach it independently, each read at instruction level, and the `m_nCount` displacement is confirmed by arithmetic — `0x9b8 + 0xc` — against the field the `GetStartPosition` idiom actually loads, which is a check the layout could have failed). **Medium** for the writer set: `disp:9b4` gives `view+0x9b4` two stores over 123 hits / 78 owners / 0 orphan, but `disp:9b8` returns only **3** hits and none in the input path, because `ADD reg,0x9b8` is an immediate — the readers are visible only to `imm:9b8` (51 hits). **Inferred, not established**: that `obj+0x7c` means *selected*. It is the flag the builder filters on; `disp:7c` is 790 hits over 433 owners, too common a displacement to close by sweep | ● active | [EXP-0110](../experiments/EXP-0110-order-gates/) |
| AI-FACE-066 | **An attacker must already be facing its victim, and the swing neither tests facing nor produces it — the mirror of `AI-RETAL-056`, where the victim's turn IS the consequence.** `FUN_004fb853` (the swing start) and `FUN_004fba0e` (the strike) are both read end to end and their complete guard sets are: both actors non-null with a non-null `+0x14` `Player`; the attacker's health `> 0`; and, in the strike only, `reach >= FUN_004fb702(attacker, target)` (`004fba5f`..`004fba7c`, `HERO-REACH-025`). **Neither contains a facing test, a facing write, or a call to any turn routine.** The test lives one level up, in `FUN_00548ea0`: `FUN_0054a680` returns the direction from self to target and it must equal `mover+0x00`, the current facing (`00548ebb MOV ECX,[ESI + 0x154]` / `00548ec1 CMP byte ptr [ECX],AL`), and only if it does is the edge-to-edge distance compared against the reach argument (`00548ece`). Every route into the attack act-state passes through it. `EnumRefs "re:MOV dword ptr \[E.. \+ 0x54\],0x3"` is **7 hits / 4 owners / 0 orphan**; three are `[ESP + 0x54]` float stores in `FUN_0049b8c0` and the four real ones are `005311a4`, `005312e1`, `00531b2e` and `00532a57`. `00531b2e` sits inside `FUN_00531b10`, whose `callto:` is **1 hit** and whose only caller `FUN_005327d0` runs `FUN_00548ea0` five instructions earlier (`00532a46`). `005312e1` and the pursuit arms at `005313ee` and `00531450` each sit on the taken branch of their own `FUN_00548ea0` (`00531306`, `00531400`, `00531482`). And `005311a4` is **not a fourth route but the latch**: it is the arm for `ord+0x9 == 1` — byte table `0x00531828` into jump table `0x00531810`, decoded out of the image in `evidence/tables.md` — and `ord+0x9 = 1` is written only by those same passing branches (`0053140f`/`00531414`, `00531491`, `00531b1a`). The test is re-run every tick, so a victim that circles its attacker drops it back to `FUN_00532100`, which inside the stop distance **turns to face and stands** (`AI-PURSUE-040`). **So facing is a precondition, maintained continuously by the approach, and never a consequence of the blow** | **High** for the two routines' guard sets and for `FUN_00548ea0`'s two tests — all read end to end, every clause a named instruction, and the latch arm settled by a static table rather than by inference / **Medium** for *every route*: the store-form sweep is `AI-STATE-043`'s instrument and inherits its blind spot, a state restored from a save or produced by arithmetic in place being invisible to it, and `AI-PROGRESS-034`'s second switch was read only at the one arm this row needs | ● active | [EXP-0111](../experiments/EXP-0111-blow-display/) |
| AI-FACE-067 | **The complete turn-to-face producer set is six routines, and not one of them is on the swing path.** `FUN_0054a210(actor, dir)` is the only routine in the image that aims an actor: it stores the wanted facing at `mover+0x01` (`0054a221`), takes the shortest arc against `mover+0x00`, and — when `mover+0xa0` is 0 and that arc is at most `0x21` in either direction (`0054a267 CMP SI,0x21`, `0054a27c CMP EBP,0x21`) — **snaps** `mover+0x00 := mover+0x01` and raises `mover+0xa4`; otherwise it hands off to `FUN_00548e20`, which is what actually issues a turn. `EnumRefs callto:54a210` is **7 hits / 6 owners / 0 orphan**: `FUN_00531a20` at `00531aab` (the idle-turn arm whose `rand()` gate `AI-RETAL-056`'s struck flag skips), `FUN_005310e0` at `0053156a`, `FUN_00532bb0` at `00532bca`, `FUN_005492a0` at `005492fe` (the approach's stand-and-face), `FUN_00548f70` twice, and `FUN_00549990`. **None of the six is `FUN_004fb853`, `FUN_004fba0e` or `FUN_004f37be`**, and none is reached from them. Two consequences a consumer must carry: the snap threshold means an actor already inside its reach usually corrects its facing within one tick and without any visible turn animation, since a turn under 45 degrees never becomes action code 5; and the turn belongs to the mover or to the order machine, never to the strike, so a consumer that turns the attacker as part of the swing has invented a coupling the original does not have | **High** for the routine — read end to end, both immediates and the snap transcribed — and for the caller set, a `callto:` enumeration on the repaired function table reporting 0 orphan, which is the shape that can carry an *only* / **Medium** for *none is reached from them*: that is a reading of three call chains rather than an enumeration, and a `callto:` sweep cannot see an indirect call | ● active | [EXP-0111](../experiments/EXP-0111-blow-display/) |
| AI-GROUPSEE-068 | **A group sees as one animal: the candidate list is built from a sight map every member stamps into, swept over the whole world actor list, filtered by the *first* member's diplomacy row — and the dead are parked rather than dropped.** `FUN_005365e0(AImanager, group)`, `RET 0x4`, read end to end (`evidence/rom-groupscan-excerpt.md` §1). Five phases. (a) `FUN_00518320` empties scratch list A; `FUN_005474f0(world+0x58ee8)` clears the `0x10000`-byte visibility map **once** at `00536627`, before the member loop, and then each member's sight is stamped with `FUN_005472a0(fog, member, FUN_00544a00(member+0x10))` — so a candidate visible to any member is visible to every member. (b) For a member whose owner is not a human participant (`[member+0x14]+0x28 != 0`, `UNIT-OWNER-009`) and whose `ord+0x58` is non-zero, the remembered attacker's cell is **incremented** in the same byte map (`0053665b`, `0053666b`), `ord+0x5a` is incremented, and at `ord+0x5a > 0x14` — **20** — `ord+0x58` is cleared (`00536681`/`00536686`): the group-side twin of `AI-SIGHT-006`'s per-actor memory. (c) `world+0xa4554`, the whole on-map actor list (`AI-LIST-003`), is walked head to tail and every actor whose own cell byte is non-zero is appended to A (`005366ec`…`00536702`) — no neighbourhood, no spatial index. (d) `FUN_0052ab60(AImgr, firstMember, listA, 0)` — `AI-FILTER-001`'s diplomacy predicate with the group's **first member** as the decider, which also fixes whose `me+0x70` the See-invisible exception consults. (e) Every candidate with `word[cand+0x94] < 1` is unlinked into list B at `+0xbc4` (`00536778`…`005367bd`); then **if A is empty and B is not, the whole of B is moved back into A** (`005367c5`…`00536846`), so a group with only corpses in view still has a candidate list. The routine overwrites its own argument slot at `0053673f` with `AImgr+0xba4`, which is why `[ESP+0x24]` names the group before that instruction and the scratch list after it — read the other way it inverts phase (e). Callers: `EnumRefs callto:5365e0` = **5 hits / 5 owners / 0 orphan** — group orders 1, 2, 5, the dispatcher's inline order-3 arm, and `FUN_0053ddd0` from outside the module | High (one routine read end to end, every phase a cited instruction; the caller count is a printed enumeration on the repaired table). **Medium** that the caller set is complete — `callto:` sees direct calls and `.rdata` code pointers, and every dispatch table in this area lives in `.text` (`AI-DEAD-036`) | ● active | [EXP-0112](../experiments/EXP-0112-ai-execution/) |
| AI-SCORE-069 | **`ord+0x20` is the group-assigned target; it is rewritten for every member on every evaluation and cleared when nothing scores, so nothing at the group layer is sticky.** `FUN_00536c10` and `FUN_00536d50` are **the same routine twice** — instruction for instruction identical apart from `00536c9f CALL 0x00535d30` against `00536ddf CALL 0x00535e90` (`evidence/rom-groupscan-excerpt.md` §2). Both open `TEST CL,CL` on `byte [group+0xc]` and `TEST DL,DL` on `byte [AImgr+0xbb4]` — the **low byte only** of each count — and on either zero walk the members writing `ord+0x20 = 0` (`00536d28`, `00536e68`). Otherwise, per member, over every candidate: `cost = f(AImgr, member, cand)`, taken when `cost < best` **strictly** (`00536ca4 CMP EAX,EBP` / `JGE`), so the first candidate wins a tie and list order decides; `best` is seeded `0xffffff` (`00536c70`) and, if it survives, `ord+0x20 = 0` (`00536cdc`), else `ord+0x20 = winner` (`00536cd7`). **The complete surface of `ord+0x20`**, from a full disassembly of `0052a000..0053f000` read for every dword access at displacement `0x20` (stack slots and vtable `CALL`s removed, each survivor classified by its own base register): **6 writers**, all in these two routines, and **4 readers** — `0053703e` group order 1, `0053712c` order 2, `00537227` order 5, and `00533bca` inline in the dispatcher for order 3, which belongs to no named routine and is the hit a routine-by-routine reading loses. The discriminator against `grpAI+0x20` is width: the group order byte is a **byte**, this is a **dword** | High for the structure and for each cited site (both routines read whole; the seed and the `JGE` fix the tie rule). **Medium** as a completeness claim: the instrument is module-scoped and blind to the order block reached at another displacement, to a wholesale copy of it, and to anything outside `0052a000..0053f000` | ● active | [EXP-0112](../experiments/EXP-0112-ai-execution/) |
| AI-PREF-070 | **The group's target choice runs through a 4×4 preference matrix of compile-time constants at `AImanager+0xb94`, and two of its cells are an absolute veto.** All sixteen bytes are written once each, in the AI manager constructor `FUN_0052b2b0`, at consecutive addresses `0052b45e`…`0052b4bb` (`evidence/rom-scorer-excerpt.md` §5). Their values come from three registers none of which is rewritten between its load and the block: `BL = 2` (`0052b3d8`, the same register `AI-SPREAD-038` reads at `0052b449`), `DL = 1` (`0052b404`), `AL = 0` (`0052b3fc XOR EAX,EAX`); the remaining four stores carry the immediate `4`. Indexed `M[member domain][candidate domain]` — the stride-4 axis is the member's, fixed by `00535dd2 LEA ECX,[EDI + EAX*0x4 + 0xb94]` with `EAX` the member's `vt+0x20()` and `EDI` the candidate's, which is what rules out a flat 16-entry table. Rows, member domain first: `0: 2 1 2 4` · `1: 4 2 1 0` · `2: 2 1 4 0` · `3: 2 1 2 4`. The domain is `actor+0x4a` through `vt+0x20` (`MOVE-DOM-024`), 3 being the image's own word for flier. **A `0` cell returns the selection loop's own seed `0xffffff`** (`00535e83`), and the loop compares `JGE`, so a vetoed candidate can never be taken. **The instrument lesson is the row's own**: `EnumRefs disp:b94` returns 19 hits over 12 owners and exactly **one** store in the AI module, which alone reads as "one byte, set to 2"; the other fifteen bytes are at fifteen other displacements. `docs/INSTRUMENT.md` rule 4 says to reduce a common displacement by the field's **width** — the same trap on the *array* side is that a sixteen-byte table has sixteen displacements and a sweep for one of them is not a sweep for the table | High (sixteen cited stores in one routine, three register provenances each traced to a load with no intervening write, and the index law discriminated by a stride the rival model does not have). **Medium** that no other instruction writes the block: the sixteen `disp:` sweeps cannot see a `REP MOVSD` of the containing object | ● active | [EXP-0112](../experiments/EXP-0112-ai-execution/) |
| AI-COST-071 | **The cost is `(distance term << 8) + turn cost`, so distance decides and facing breaks the tie — and a ranged candidate is scored as though it could not move.** `FUN_00535d30(AImgr, member, cand)`, `RET 0x8`, read whole (`evidence/rom-scorer-excerpt.md` §3), lower is better. `d = FUN_0054d100(world, memberCell, candCell)`, the Chebyshev distance in cells, as a byte. `k = cand->vt+0x20()`; **when `k == 1` and `cand+0x12c > 1`, `k := 0`** (`00535d77`…`00535d89`) — an ordinary ground creature holding a reach above 1 is indexed as the immobile column, which is what makes `M[1][0] = 4` a *prefer the archer* rule for a ground melee decider. A member with reach `> 1` takes `pref = AImgr[0xb94 + k]` — **row 0 only, the member's own domain never consulted** — and `d <= reach ? d := 1 : d += 1 - reach` (`00535d9c`…`00535db9`); a member with reach `<= 1` takes `pref = AImgr[0xb94 + k + 4*(member->vt+0x20())]` and leaves `d` alone. Then `cost = (d << 8) + FUN_0054f090(world, mover[0], FUN_0054a680(world, member, cand))` (`00535e09`…`00535e11`) — the same direction/turn pair `AI-ACQUIRE-002` names, folded into one scalar instead of a two-key comparison. Three modifiers follow: `pref == 1` → `cost += cost/2`, `pref == 4` → `cost -= cost/4`, both gated on `cand->vt+0x1c() < 2` (token size 1, `MOVE-DOM-024`'s footprint getter) **and** `word[cand+0x88] >= 0xf` (Mind, `FUN_004f5998`'s operand); and `cand+0x144 & 0x100000` → `cost += 0x7f` clamped at 1 (`00535e61`…`00535e75`), `+0x144` being the active-spell bitmask `FUN_004f4c25` indexes, so **spell id 20 makes its bearer the least attractive target on the map**. The actor constructor `FUN_004f3317` defaults `word[actor+0x88]` to **20** (`004f3343 MOV word ptr [EAX + 0x88],0x14`) and the `Data.bin` Units collection carries no Mind column, so the Mind half of the gate is satisfied by default everywhere and only token size discriminates | High (one routine read end to end, every arithmetic step and both table indexings cited; the three field identifications are each another ledger's published operand). **Unknown** which spell carries id 20 — the bit was read, `claims/magic.md`'s id space was not cross-walked | ● active | [EXP-0112](../experiments/EXP-0112-ai-execution/) |
| AI-REACH-072 | **The second scorer refuses anything past reach outright, which is why group order 3's members never chase.** `FUN_00535e90(AImgr, member, cand)` (`evidence/rom-scorer-excerpt.md` §4) is `FUN_00535d30` with three differences. The candidate's domain is forced to 0 whenever `cand+0x12c > 1` — for **any** domain, not only domain 1 (`00535ec2`…`00535ece`). **`00535f64 CMP ESI,0x1` / `JLE` — a distance term above 1 returns `0xffffff` immediately** (`00535f69`), before the turn cost is even folded in, so this scorer can only ever name a target the member could strike where it stands: adjacent for a melee member, within reach for a ranged one. And the preference multipliers are ungated and coarser — `pref == 1` → `cost <<= 1`, `pref == 4` → `cost >>= 1`. Its single consumer is `FUN_00536d50` (`callto:535e90` = **1 hit / 1 owner / 0 orphan**), whose single consumer is the dispatcher's inline order-3 arm (`callto:536d50` = **1 / 1 / 0**, `00533ba4`). `FUN_00535d30` has three (`callto:535d30` = **3 / 3 / 0**): `FUN_00536c10`, the player attack order `FUN_005348c0`, and the script dispatcher `FUN_0053c030`. `MOVE-DOM-024` lists both routines as located and not read; they are read here | High — both routines read end to end and all four caller counts are printed enumerations with 0 orphan hits | ● active | [EXP-0112](../experiments/EXP-0112-ai-execution/) |
| AI-FLIER-073 | **A melee ground creature never auto-selects a flying target, a ranged one always can, and shipped content places fliers on thirty of the thirty-eight maps.** `M[1][3] = M[2][3] = 0` (`AI-PREF-070`) is the veto, and three constructor facts decide who it binds. The actor constructor `FUN_004f30a2` defaults `actor+0x4a` to **1** and `actor+0x49` to **1** (`004f30e7`, `004f30e0`; `re:` over the byte-store form returns 2 hits / 2 owners / 0 orphan for each). The `Data.bin` **Humans** collection ships **no** `MovementType` cell at all — all **210** named rows on both roots carry the absent-cell sentinel, so the streamer skips the store (`DAT-SCHEMA-004`) and every human actor, the player's hero included, is domain 1. The **Units** collection splits **40 / 8 / 8** over domains 1 / 2 / 3, identical on both roots. And the ranged branch of both scorers indexes **row 0**, whose four entries are `2 1 2 4` and contain no zero — so reach, not class, is what decides whether a creature will chase something in the air. Corpus, resolving every type-6 record through `ALM-CLS-052`'s rule (`tools/aiexec`): **EN 701 of 8094 placements are domain 3, over 30 of 38 maps; RU 261 of 3991, over 25 of 34** — the RU shortfall being the four EN-only loose maps and the terrain-only `Horror.alm` (`AI-ROOT-049`), not a rule difference. Domain histograms EN `1: 4878, 2: 1008, 3: 701` plus 1507 human placements with no cell; RU `1: 2597, 2: 494, 3: 261` plus 639 | High for the code half (the matrix cell, the veto's mechanism and both constructor defaults are cited instructions, and the ranged branch's row-0 indexing is a single `MOV` with no member term). **Medium** for the corpus half — `METHODOLOGY.md` caps a census there, and it also inherits `ALM-CLS-052`'s resolution rule rather than observing the resolver. **Not established**: whether a *reach* above 1 is ever streamed onto a domain-1 creature in a way that would put it on the ranged branch — `actor+0x12c`'s filler was not traced this round | ● active | [EXP-0112](../experiments/EXP-0112-ai-execution/) |
| AI-GRPGUARD-074 | **Group order 1, the arm 95.6 % of shipped hostile placements run, read end to end — and its walk-home target is the member's own post, not the group's centre.** `FUN_00536ef0(AImgr, group)` (`evidence/rom-grouparm-excerpt.md` §6), in order: `FUN_005333a0` the geometry pass (`00536f02`); `FUN_005365e0` the candidate list (`00536f0a`); then `BL = grpAI+0x2d`, `BP = grpAI+0x28` and every candidate whose `FUN_0054d100(world, candCell, centroid)` exceeds `BL` is unlinked (`00536f66`…`00536f8b`) — the clip `AI-RADIUS-014` names; then `FUN_00536c10` (`00536f9f`); then `AI-GUARD-021`'s latch and radius re-roll. The per-member tail (`00537038`…`005370de`) has exactly three outcomes: **`ord+0x20 != 0` → `FUN_0052e940(AImgr, member, target)`** (`00537049`), the engage; else the member's current cell is compared against **`ord+0x00`, its own post** (`0053705e CMP AX,word ptr [ECX]`) and, differing — or `FUN_00545c30` reporting it not idle — **`ord+0x08 = 1`, `ord+0x0a = ord+0x00`** (`005370a5`/`005370af`), walk to the post; else, at the post and idle, a human participant's unit goes to `FUN_0052eb60` (heal) and an AI owner's to **`ord+0x08 = 0xb`** (`0053707d`), the *idle turn* order arm of `AI-ORDER-039` — not the guard state `actor+0x50 = 0xb` — followed by `FUN_0052ed30` when `member+0x4c & 4`. This confirms `AI-GUARD-012`'s three cited addresses exactly and corrects its gloss in one place: the arm does not send a member home to `grpAI+0x28`; the centroid is the **candidate clip's** origin and the post is the walk's destination, and for a group under order 1 from load nothing has ever written `ord+0x00` (`AI-POST-042`'s writer set is complete and contains no group-order path) *[EXP-0125]: the parenthesis is **refuted**. The load-time guard setter `FUN_00534ac0` writes `ord+0x00` itself, `0x76` bytes before the `grpAI+0x20 = 1` that puts the group under order 1 — `00534bed` from the member's current cell, `00534bda` from `mv+0x06` for one in motion. The arm's three outcomes and every address above stand; what fails is that the post is unwritten. See [`retracted.md`](retracted.md) and `AI-POST-095`.* | High (one routine read end to end, every branch and store a cited instruction, and the two objects told apart by their base registers — `[EDI + 0x3c]` the group AI record against `[ESI + 0x158]` the member's order block) | ● active (amended) | [EXP-0112](../experiments/EXP-0112-ai-execution/) |
| AI-RADFREEZE-075 | **A group's notice radius is frozen at the geometry it had when guard was last issued, while the circle it draws follows the group every tick.** `FUN_005333a0(AImgr, group)` (`evidence/rom-grouparm-excerpt.md` §8) is `FUN_00533210` with the member count taken from `[group+0xc]`: it sums each member's fine coordinates, divides, and stores the fine centroid `grpAI+0x24` (`0053345a`) and the cell centroid `grpAI+0x28` (`00533464`), then over the members the maximum Chebyshev spread `grpAI+0x2a`, the maximum member sight `grpAI+0x2b` and the maximum of the two summed `grpAI+0x2c` (`005334fd`/`00533507`/`00533515`). It is `FUN_00536ef0`'s first act and its only caller (`callto:5333a0` = **1 hit / 1 owner / 0 orphan**), so it runs on every guard tick of every group. **And `grpAI+0x2c` is then never read again.** Whole-image `disp:2c` restricted to byte accesses and stripped of stack slots returns **19 hits**, of which four touch this record: two writers, `00533387` (`FUN_00533210`) and `00533515` (`FUN_005333a0`), and two readers, `00534c46` and `00534c4f`, **both inside the guard setter `FUN_00534ac0`**, which runs at map load, on the player's guard command and on the script's `Par0 = 1` (`callto:534ac0` = 6 / 6 / 0). What the arm actually rolls from is `grpAI+0x38`, whose three writers are all in that same setter (`00534c52`, `00534c6b`, `00534c9f`) and whose four readers are the two live roll sites and two dead ones (`AI-DEADROLL-078`). So the radius is a load-time constant plus `AI-GUARD-021`'s ±1 jitter, and a group that spreads out, loses members or crosses the map keeps it. This **amends `AI-RADIUS-014`**, whose Medium rested on `FUN_00533210` being the only producer: there is a second, and it is the one that runs per tick *[EXP-0125]: the ±1 this row quotes is confirmed to the instruction and its distribution fixed — `{−1, 0, +1}`, each about a third (`AI-JITTER-103`, `AI-RANGE-102`).* | High (both writer sets and both reader sets are complete enumerations with their instruments named, and the per-tick producer is fixed by a `callto:` with 0 orphan). **Medium** that the enumeration is complete for this record: a byte-displacement sweep cannot tell one object from another, and the attribution rests on each site's own base register | ● active (amended) | [EXP-0112](../experiments/EXP-0112-ai-execution/) |
| AI-STAND-076 | **Group order 3 is Stand Ground, not attack — its scorer refuses anything past reach and its members never take a step.** The arm is inline in the dispatcher at `00533b99`…`00533c11` (`evidence/rom-grouparm-excerpt.md` §7): `FUN_005365e0` (`00533b9c`), `FUN_00536d50` (`00533ba4`), then per member `ord+0x20 != 0` → `FUN_0052e940` (`00533bd5`); else an AI owner gets `ord+0x08 = 0xb` (`00533be6`) plus `FUN_0052ed30` on `member+0x4c & 4`, and a human participant's unit `FUN_0052eb60`. **There is no radius clip and no walk of any kind**, and `AI-REACH-072`'s scorer vetoes every candidate past reach — so the arm's whole behaviour is *hit what is already next to you, otherwise turn on the spot*. Three independent names agree and the ledger's label does not: the mission-description key that selects this setter is spelled **`StandGround`** (`AI-AUTHOR-015`, `FUN_004f164c`), the shipped editor catalogue names `Par0 = 3` **`Stand Ground`** (`AI-GROUPCMD-020`, 24 nodes over the corpus), and the arm stands its ground. `AI-ORDER-010`'s "attack" and `AI-AUTHOR-015`'s "aggressive" are **amended** to Stand Ground. The consequence a consumer must not invert: the load walk gives order 3 to the groups of type-5 slot 0 — the player's own `Self` — and order 1 to everyone else's (`AI-AUTHOR-015`, `AI-CENSUS-046`: EN 8085 / 9 / 0), so *the player's units stand and the scenario's units watch a circle*, which is the opposite of what the two labels suggest | High for the behaviour (one arm read end to end against a PE-read dispatch table, and its scorer read whole). **Medium** for the name — three shipped or authored spellings agree, but a name is not a measurement, and the setters also write `actor+0x50 = 0xc` (`AI-AUTHOR-015`) which only group order 0 would ever evaluate | ● active | [EXP-0112](../experiments/EXP-0112-ai-execution/) |
| AI-REISSUE-077 | **What re-issues a pursuit is the group arm itself, unconditionally, every AI tick — and a group with nothing but corpses in sight keeps attacking a corpse.** Neither group arm carries any test that a target is still worth having: `AI-SCORE-069`'s scorer rewrites `ord+0x20` from scratch, and the arm calls `FUN_0052e940` on whatever it finds there, which writes `ord+0x08 = 5`, `ord+0x0c = target` and `ord+0x14 = actor+0x12c` afresh (`AI-GUARD-007`). So the answer to *what ends a pursuit* differs by group order: under order **0** it is `AI-BREAK-041`'s post rule, and under orders 1/2/3/5 there is no break-off at all — only a re-score whose inputs may no longer contain the old target. Three consequences follow from routines already read rather than from new ones. **Death does not release a target**: `AI-GROUPSEE-068` phase (e) moves corpses aside, but restores every one of them when no living hostile is in sight, so the scorer is handed a corpse, `ord+0x20` takes it, and the engage is re-issued at it. **Leaving sight does release one**, because the candidate list is rebuilt from a freshly cleared stamp each tick — except for an AI-owned member's remembered attacker cell, which is forced into the stamp for 20 group ticks (`AI-GROUPSEE-068` phase (b)). **And unreachability is handled one layer down**, by `AI-ROUTE-045`'s `mover+0x98`, which the order layer consumes on the actor tick without the group layer ever hearing of it | High for the mechanism (it is a control-flow reading of two arms and one builder, all read end to end this round). **Medium** for "no other mechanism ends a group pursuit": the negative is over the two arms' own instructions plus `AI-SCORE-069`'s module-scoped enumeration of `ord+0x20`, and neither can see a wholesale copy of the order block | ● active | [EXP-0112](../experiments/EXP-0112-ai-execution/) |
| AI-DEADROLL-078 | **The guard radius roll exists three times in the image and two of the copies are dead.** `EnumRefs "re:MOV byte ptr \[E.. \+ 0x2d\],"` returns **7 hits / 4 owners / 0 orphan** — the complete writer set of the group's working radius. Three are the guard setter `FUN_00534ac0` (`00534c49`, `00534c65`, `00534c93`), two are `FUN_00536ef0`'s two latch edges (`00536fdc`, `0053701c`, `AI-GUARD-021`), and the remaining two are `FUN_00536e90` (`00536eaf`) and `FUN_00536ec0` (`00536edc`) — **nine-instruction routines, byte-identical to each other**, each computing `grpAI+0x2d = grpAI+0x38 + 4 + (rand()*3/AImgr[0] - 1)`, i.e. `FUN_00536ef0`'s has-members edge extracted and duplicated. Both are unreachable on three independent instruments: `callto:` **0**, `refto:` **0**, `imm:` **0** for each — the triple `AI-DEAD-035` uses. This discharges the completeness half of `AI-GUARD-021`'s Medium for `grpAI+0x2d`: its writer set is now enumerated and every member classified | **Medium**, and the reason is `AI-DEAD-036`'s: those three instruments are all blind to a `.text` dispatch-table slot, and this area's tables are all in `.text`. The fourth leg — a raw scan of the image for the little-endian dword through the PE section table — was **not** run, so "unreachable" is stated at the confidence three legs support and not the four that would make it High. High for the writer set itself and for the two routines' contents | ● active | [EXP-0112](../experiments/EXP-0112-ai-execution/) |
| AI-GATE-079 | **`AI-GUARD-007`'s class gate is the mage bit, while `actor+0x0e == 0x18` remains inside an actor-state arm with no located writer.** `actor+0x4c & 4` is the fighter/mage axis and set is mage; it is derived from positive mana and can be set by spellbook construction, not copied verbatim from Data.bin. Thus the short-reach suppression is a human participant's mage not auto-engaging. Separately, 57 Humans rows on both roots author table `typeID=0x18`. A non-zero player-character constructor mode overwrites that value, but zero-mode map Humans retain it, so the overwrite is **not** a second reason the compare cannot match. The test's reachability depends only on the still-unlocated producer of `actor+0x50=0x17` (`AI-STATE-043`) | High for the class bit and 57-row census; Medium for the `0x17` arm's unreachability, unchanged from `AI-STATE-043` | ● active (corrected) | [EXP-0112](../experiments/EXP-0112-ai-execution/), [EXP-0133](../experiments/EXP-0133-person-health/), [EXP-0192](../experiments/EXP-0192-mission20-party-boundary/) |
| AI-CLOCK-080 | **Engagement runs on two clocks, and the decision one is the slow one.** The order layer steps a pursuit from the **actor tick**: `FUN_004f37be` calls `FUN_005310e0` four instructions before it dispatches on `actor+0x54` (`AI-PROGRESS-034`), and that is where `AI-PURSUE-040`'s arms 5 and 6 re-test position and re-path. The decision layer runs from the **AI slot**: `FUN_005336a0` walks players and groups and calls `FUN_00533ae0` once per group (`AI-TICK-008`). So a group re-scores its targets once per full tick while the pursuit it ordered advances on every actor tick — every field the group layer writes is a decision the order layer then executes many times, which is why nothing in the group arms needs to be sticky (`AI-REISSUE-077`). One clause of `AI-TICK-008` is narrowed rather than confirmed: `callto:5336a0` is 2 hits, and only the first, `004d2595` in `FUN_004d2551`, sits behind the signed `% 16 == 6` test (`004d257c AND EAX,0xf` / `004d2583 CMP EAX,0x6`). The second, `004d245c` in `FUN_004d214b`, has **no modulo in front of it** — it is bracketed by two `CALL dword ptr [0x00632fa4]` whose difference is stored, i.e. the pass is being timed. `FUN_004d2551` has 4 callers over 4 owners, all in the pacer module `0x00475280`…`0x00477c00`; `FUN_004d214b` has **1** (`FUN_004d0c2e`), so it is not dead | High for the two clocks and for both dispatch paths (all cited instructions, and the four caller counts are printed enumerations with 0 orphan). **Unknown** whether `FUN_004d214b` runs during ordinary play: its one caller was located, not read, and if it does, the AI decision rate is not `AI-TICK-008`'s once per full tick | ● active | [EXP-0112](../experiments/EXP-0112-ai-execution/) |
| AI-LOS-081 | **The line-of-sight test is an accumulator march, not an altitude comparison — it carries a budget from cell to cell and stops when the budget runs out.** `FUN_00546c70`, `RET 0x10`, four stack args, read whole (`evidence/rom-grouparm-excerpt.md` §9). For the cell named by its first two arguments it reads a **signed byte pair** out of one word of the grid at `fog+0x22000` — the low byte at `00546c8f`, the high at `00546c9d` — and uses them as a step back to a predecessor cell; loads that predecessor's running value out of the dword accumulator at `fog+0x24000`, stride 64 (`00546cc1`, the grid `TERR-FOG-084` identifies); subtracts a signed word from a third grid at `fog+0x28000` (`00546cb4`); subtracts the terrain height `world+0x9451c[cell]` reached through the world pointer cached at `fog+0x3a008` (`00546cc8`…`00546cd9`); adds the observer's altitude, the fourth argument (`00546ce7`); **stores the result into this cell's own accumulator slot** (`00546cef`); and then branches on its sign — `<= 0` returns **1**, blocked, which is what stops `FUN_00546d20`'s ring; `> 0` sets `fog[0x2a008 + cell] = 1` (`00546cf8`) and returns **0**. This **amends `AI-SIGHT-006`**, which describes the cell as "tested against the actor's own altitude from the height plane": the altitude is one of four terms and the test is on an accumulated remainder, so a consumer implementing a per-cell altitude compare reproduces neither the shape of the visible region nor where it stops. Two smaller corrections ride along: the visible-cell byte is **set to 1**, while `AI-GROUPSEE-068`'s attacker-memory path **increments** the same byte, so the array is not a boolean; and `00546ceb TEST EAX,EAX` is inert, its flags overwritten by the next instruction | High (one routine read end to end; every grid base is an arithmetic identity from its own scale factor, and the two exits are distinguished by their own `MOV EAX` immediates). **Unknown** what fills the `fog+0x22000` step grid and the `fog+0x28000` cost grid: both are read here and neither was traced to a writer | ● active | [EXP-0112](../experiments/EXP-0112-ai-execution/) |
| AI-DIPLO-082 | **The complete writer set of the matrix after construction is SIX routines, and two of them no `disp:a9c4` sweep can name.** `AI-DIPLO-004`'s enumeration is reproduced unchanged — `EnumRefs disp:a9c4` = **32 hits / 14 distinct owners / 0 orphan**, `imm:a9c4` = **0** — and then extended in the two directions it could not reach. **(a)** That row named its own blind spot, the `sub-object + 8` alias, and did not close it; the address of `session+0xa9bc` can only be taken by a `disp:a9bc` or an `imm:a9bc` instruction, so both sweeps are run — **3 hits / 3 owners** and **5 hits / 4 owners**, 0 orphan, **8 hits over 6 owners** in all — and every one is read. **(b)** A `disp:` sweep names the instruction that *forms* an address, not the one that *uses* it: twenty of the 32 hits are `LEA`s, and following each to its use found `FUN_004d5dd8` spilling the pointer at `004d83d3` and storing through it at `004d8416 MOV byte ptr [EDX],CL` — displacement **0**, invisible to any sweep for `0xa9c4`. The six are: the `.alm` loader `FUN_004e1924` and the two mission joins `FUN_004d303e` / `FUN_004d8963` (`AI-DIPLO-005`); the script action `FUN_00539be0` (`TRIG-DIPLO-019`); session command `0x45` inside `FUN_004d5dd8` (`SESS-DIPLO-024`); the join/leave pair `FUN_0053d8a0` / `FUN_0053d9a0` (`AI-DIPLO-083`); and the event flip `FUN_0053d9b0` (`HERO-AGGRO-028`, `AI-DIPLO-084`) — **four by sweep, six by reading**. The nine **readers** and the bit each tests are listed in `evidence/rom-writers.md` §8; the widest is the loader's own `004e1e17 AND EAX,0x7`, which is why the value space cannot be closed at `{0,1,2}` from the corpus | High for each named writer (every store is a transcribed instruction) and for both enumerations, whose instrument and 0-orphan counts are stated. **Medium** for *six is all of them*: a wholesale `memcpy` / `REP MOVSD` of the session carries no displacement at all and no sweep here can see one — the session `Serialize` (`AI-DIPLO-085`) is exactly such a copy and was found by reading the serialiser, not by a sweep | ● active | [EXP-0114](../experiments/EXP-0114-diplomacy-change/) |
| AI-DIPLO-083 | **Row 0 and column 0 are the join machinery, and the four header bytes `AI-DIPLO-004` observed are a 2x2 template that decides every relation a joining player gets.** `FUN_0053d8a0(player)`, on `this = session+0xa9bc`, stores the player's owner kind `player+0x28` (`UNIT-OWNER-009`: 0 = a human participant owns it, 1 or 2 = the scenario authored the owner) into **`matrix[idx][0]`** at `0053d8b5`, overwrites it with 0 when the kind is exactly 2 (`0053d8bc CMP EAX,0x2`, `0053d8cb`), marks the slot live with **`matrix[0][idx] = 1`** at `0053d8de`, then walks `EBP = 1..49` skipping every slot whose `matrix[0][EBP]` is zero and, for each live one, selects one of four bytes at `this+0..3` by whether `matrix[idx][0]` and `matrix[EBP][0]` are both non-zero (`template[2]`), differ (`template[0]` one way and `template[1]` the other) or are both zero (`template[3]`), writing `matrix[idx][EBP]` at `0053d913`/`925`/`940`/`952` and `matrix[EBP][idx]` at `0053d960`; finally `MOV byte ptr [EAX + ECX*0x1 + 0x8],0x2` at `0053d991`, whose index is `51*idx` from `SHL EAX,0x4` / `ADD EAX,EDX` / `LEA ECX,[ECX + EAX*0x2]`, i.e. **the forced diagonal again**. The constructor sets the template `{1, 1, 0, 0}` (`0052b408`…`0052b41a`, `DL = 1` from `0052b404` and `AL = 0` from `0052b3fc`, neither rewritten in between), so a joining player is **hostile in both directions to exactly those live players whose column-0 flag differs from his, and neutral to the rest**. The inverse is `FUN_0053d9a0`, four instructions, `MOV byte ptr [EDX + ECX*0x1 + 0x8],0x0` at `0053d9a8` — `matrix[0][idx] = 0`, the slot goes dead. **This amends `AI-DIPLO-005`**, whose *"column 0 is never written"* is true of the `.alm` loader and false of the matrix, and whose forced diagonal turns out to be written on **three** independent paths rather than one: the loader (`004e1d8b`), the mission joins (`004d32a7`, `004d8b66`, `004d8b88`) and here | High (both routines read whole; the template's four bytes read out of the constructor with their register provenance; the diagonal's index recovered from the `LEA` arithmetic rather than assumed) | ● active | [EXP-0114](../experiments/EXP-0114-diplomacy-change/) |
| AI-DIPLO-084 | **The event flip has three callers and a fourth entry point, and one of them re-scans immediately — which is what `FUN_0053ddd0` turns out to be.** `HERO-AGGRO-028` names `FUN_004fbc92` as the hook into `FUN_00531ac0`; `EnumRefs callto:531ac0` returns **3 hits / 3 owners / 0 orphan** — `FUN_004fbc92` (`004fc1fd`, the routine whose own string is `"Unknown magic damage type"`), `FUN_004fc5ff` (`004fc70f`) and `FUN_004fd37a` (`004fd4ff`) — all three guarding on both actors having an owner (`+0x14 != 0`). The fourth entry point does not go through `FUN_00531ac0` at all: **`FUN_0053ddd0`** calls `FUN_0053d9b0` directly at `0053de48`, after the identical alarm preamble (`0053de1d` `[victim+0x158]+0x54 = 1`, `0053de34` `+0x58 = the caster's cell`, `0053de44` `+0x5a = 0`, matching `00531ad3`…`00531afa` instruction for instruction), and then — alone among the six writers — calls the group candidate builder **in the same instruction stream**, `0053de53 CALL 0x005365e0`. Its chain is `FUN_004fe6d3` → `FUN_004fe92e` → `FUN_0053ddd0`, one caller at each step (`callto:` 1 hit / 1 owner / 0 orphan), and `FUN_004fe6d3` subtracts a cost from `[target+0x9a]` while `FUN_004fe92e` also calls `FUN_004feadb`, the routine that tests `matrix[caster][target] & 1` — the spell-cast family. So **casting at somebody makes the two players mutually hostile on the same rule as striking him**, and this closes what `DISCOVERY.md` recorded as *"the fifth caller of the candidate builder, lives outside the AI module, and is unread"*. The flip itself is unchanged and re-read here: `0053d9f3 TEST AL,0x3` before `0053d9fb OR AL,0x1`, **separately gated for each direction** (`0053da25` / `0053da29`), so a pair can flip one way and not the other | High for the caller enumeration and for both preambles (a printed enumeration with 0 orphan, and the two alarm sequences are instruction-for-instruction the same). **Medium** for *"the spell-cast family"* — that identification rests on the cost subtraction and on `FUN_004feadb`'s diplomacy test, not on reading `FUN_004fe6d3` end to end | ● active | [EXP-0114](../experiments/EXP-0114-diplomacy-change/) |
| AI-DIPLO-085 | **A relation changed during a mission survives a save, because the matrix is serialised verbatim — and the byte count is exact.** The session `Serialize` `FUN_00539310` (`SAV-SESS-031`) carries the sub-object at `session+0xa9bc` in both directions with the same length immediate: store `0053937b PUSH 0x9cc` / `00539383 ADD ECX,0xa9bc` / `0053938d CALL 0x0057ba16`, load `00539458 PUSH 0x9cc` / `00539460 ADD EAX,0xa9bc` / `00539469 CALL 0x0057b908`. **`0x9cc` = 2508 = 8 + 2500** — the four template bytes of `AI-DIPLO-083` plus their padding, and the 50x50 matrix of `AI-DIPLO-004` — so the whole object goes into the **world** half of the save and comes back byte for byte. Nothing on the load path re-derives it from the `.alm` type-5 row: the loader `FUN_004e1924` runs on a **map** load, not on a save load. A consumer that rebuilds diplomacy from the map when restoring a save therefore reverts every script change, every join and every flip that had already happened | High (the length is the routine's own immediate, appearing identically on the store and the load arm, and 2508 admits exactly one split into a documented 8-byte header and a documented 50x50 byte matrix — a rival in which only the template were saved would have pushed 8) | ● active | [EXP-0114](../experiments/EXP-0114-diplomacy-change/) |
| AI-DIPLO-086 | **A diplomacy change is not propagated — it is only observed, so it never interrupts anything already under way.** The script arm ends at `00539da2 JMP 0x0053a15b`, its own epilogue: no notify, no re-scan, no order invalidation (`TRIG-DIPLO-019`). Session command `0x45` notifies the affected players and does not re-scan (`SESS-DIPLO-024`). The join and leave routines do neither. **Only `FUN_0053ddd0` re-scans** (`AI-DIPLO-084`). Everything else downstream happens because a consumer re-reads the cell later, and the published group layer fixes when that is: the candidate list is rebuilt from a freshly cleared sight stamp and re-filtered through `AI-FILTER-001` on every evaluation (`AI-GROUPSEE-068`), `ord+0x20` is rewritten for every member every time (`AI-SCORE-069`), and that runs once per full tick (`AI-CLOCK-080`) — so for the **95.6 %** of shipped hostile placements sitting under a group order (`AI-CENSUS-047`) a change takes effect at the next AI tick. But the order already issued keeps advancing on the **actor** tick, and under group orders 1/2/3/5 there is no break-off at all (`AI-REISSUE-077`). The consequence a consumer needs: **making a faction friendly mid-mission does not stop a unit that is already attacking — it stops that unit being re-selected**, and the attack ends when the next re-score no longer produces the target, not when the byte changes | High that the five non-cast writers are bare (each arm read to its own epilogue, and the absence of a call is a property of transcribed instructions). **Medium** for the reach: it is a derivation over `AI-GROUPSEE-068` / `AI-SCORE-069` / `AI-REISSUE-077` / `AI-CLOCK-080` rather than a reading of a re-filter that exists, and no running session was observed — the write-up states the prediction and what would refute it | ● active | [EXP-0114](../experiments/EXP-0114-diplomacy-change/) |
| AI-LOS-087 | **Both of `AI-LOS-081`'s untraced grids have ONE writer, it runs at world construction, and it depends on the map for nothing.** `FUN_00546790` (`__thiscall`, no args) writes the step grid based at `fog+0x22a28`/`+0x22a29` and the cost grid at `fog+0x28a28`, walking `i = 0..20` outer (pointer stride `0x80`) x `j = 0..20` inner (stride 2) and storing **eight bytes per iteration** — the low and high byte of one `u16` in each of four quadrant mirrors. `callto:546790` returns **1 hit**, `005475ff` in `FUN_00547510`, the sight object's init; `callto:547510` returns **4**, each preceded by `LEA ECX,[ESI + 0x58ee8]` in one of the four world constructors (`FUN_00540fe0` `005411c6`, `FUN_00541280` `00541466`, `FUN_00541510` `005416f5`, `FUN_005417f0` `005419d6`), all of which run it **before** the map load `FUN_00548550`. So: not per tick, not per stamp, not on a terrain change — **once, before terrain exists**. The init's own `REP STOSD` (`00547604`, `ECX=0x8000` from `+0`) clears `+0x00000..+0x20000` and stops below all three tables. The only input is `k = [Scanning] ScanShift` of `World\Data\map.reg` (`005475b6 PUSH 0x7` is the compiled default; the key is present and **7** in both roots), which reaches the cost table as `FIMUL dword ptr [ESI + 0x2a000]` = `1 << k`. **The builder ends with four literal stores** — `0054699e`/`005469a5` writing `0xff`,`0x00` at `fog+0x22aa8`/`+0x22aa9` and `005469ac`/`005469b3` writing `0x01`,`0x00` at `fog+0x229a8`/`+0x229a9` — which are the cells `(+1,0)` and `(-1,0)`. They are not redundant: on the `j == 0` axis the `+j` and `-j` quadrant addresses coincide, the last store wins, and `(i,j) = (1,0)` is the one axis cell falling in the diagonal zone, so both cells leave the loop pointing diagonally. Re-executing the builder with the listing's store order and **without** those four stores leaves **exactly 2** cells whose predecessor is not closer to the centre, and they are exactly those two | High (the writer, its single caller, that caller's four call sites and the ordering against the map load are cited instructions; the four trailing stores are explained by a consequence of the store order that was derived before it was measured, and no other reading gives them a function) | ● active | [EXP-0117](../experiments/EXP-0117-sight-region/) |
| AI-LOS-088 | **What the two grids hold.** **Step grid**, `fog+0x22000`, one `u16` per window cell, row stride `0x80`, addressed by `FUN_00546c70` as `fog + 0x22000 + (a<<7) + 2b` — its **low byte is a signed column delta and its high byte a signed row delta**, values in `{-1,0,+1}`, naming the cell one Bresenham step **toward the centre**. The builder picks the pair by the ray's slope: `j < i>>1` (`0054681c`) gives a pure column step, `j > 2i` (`00546863`) a pure row step, and the band between them the diagonal. Every one of the 1 680 non-centre cells therefore has a predecessor **strictly closer** to the centre in Chebyshev distance, so every chain terminates at the seeded cell — measured, not assumed. **Cost grid**, `fog+0x28000`, one **`i16`** per cell, same geometry, value `ftol((1<<k) * sqrt(i*i + j*j) / max(i,j))` (`005468f2` `FILD` / `FSQRT` / `FIMUL [ESI+0x2a000]` / `FIDIV [ESP+0x30]` / `CALL 0x0055458c`, which is `__ftol` with `OR AH,0xc` — truncation toward zero). That is the ray's Euclidean length divided by its Bresenham step count: **the mean length of one step along that ray**, in units of `1/(1<<k)` cell. For the shipped `k = 7` the table runs **128..181** — exactly `1<<k` on both axes and `181` on the diagonals — so the accumulator seeded with `(1<<(k-1)) + (scanRange<<k)` is a **budget in 1/128 cell, equal to `scanRange` cells plus a half-cell rounding term**. The centre cell's cost is computed by a division by `max(0,0)` and is never read. **Neither grid is a copy or projection of any plane already decoded** — not `TERR-PASS-073`, not `TERR-COST-052`, not a fog plane; a consumer has nothing to reuse and must build both | High (both addressing forms, the FPU sequence with its four operands and the zone branches are cited instructions; the closed form's own bounds `128..181` are reproduced by re-execution, and the chain property is a measured invariant over the whole window) | ● active | [EXP-0117](../experiments/EXP-0117-sight-region/) |
| AI-LOS-089 | **The region the predicate admits is a DISC of radius `scanRange`, not a square and not the 41x41 window.** On flat ground with `k = 7` the march reaches exactly `scanRange` cells along the axes (each axis cell costs `1<<k`, so the seed `128*sr + 64` survives `sr` steps and no more) and `floor((128*sr+64)/181)` along the diagonals — measured visible-cell counts **9 / 21 / 45 / 69 / 105 / 145** for `scanRange` 1..6, and 1 253 at 19, where the ring bound `r <= 19` (`00546f8d CMP EAX,0x14 ; JL`) begins to bind. The window is 41x41 but only rings 1..19 are ever walked, so its outermost ring is unreachable in principle. Terrain moves this a long way in both directions, because the per-cell term is `h(observer) - h(cell)` and descending ground **returns** budget: over the shipped corpus at `scanRange = 6`, `k = 7`, stride 3, **81 048 EN and 61 364 RU observers on 38 / 34 maps**, the visible count runs **29 minimum, 151.5 mean, 1 248 maximum** — a **43x spread from where the unit is standing**, with the mean *above* the 145-cell flat baseline. At `scanRange = 12` the same sweep gives 84 / 517.9 / 1 521, the maximum being the whole reachable window. Unrolled, the recurrence says why the spread is that large and that asymmetric: `acc(n) = seed - sum(cost) - sum(h(cell)) + n*h(observer)`, so **the observer's altitude is added at every step of a ray while a cell's own height is charged once**, when the ray passes through it. Reach therefore scales with where the unit stands and barely with what is in the way — a single tall cell costs its height once and casts no shadow behind itself. **The consequence for a consumer, in one sentence: sight range is a budget, not a radius, and the term that dominates it is the observer's own altitude** | High (the ring bound, the seed and the cost landmarks are cited instructions and closed forms) / **Medium** for every corpus figure: they are this round's transcription of four routines re-executed on shipped altitude grids, with no units, no session and no running original, and they carry their parameters — `k = 7`, stride 3, the stated `scanRange`, the 72-map corpus | ● active | [EXP-0117](../experiments/EXP-0117-sight-region/) |
| AI-LOS-090 | **A cell that blocks is NOT pruned — the march walks past it — and the reason nothing is ever re-lit behind a blocker is a corpus fact about altitude, not a property of the code.** `FUN_00546c70` stores the failing value into the cell's own accumulator slot at `00546cef`, **before** the `JLE` at `00546cf2`, and returns 1; `FUN_00546d20` consumes that return only at `00546e48`/`00546e92`/`00546ee6`/`00546f3c`, to clear the per-ring all-blocked flag `[ESP+0x30]`, and the ring runs to its end regardless. A later cell whose predecessor is the blocked one reads the stored negative value and continues from it. Whether the accumulator can rise is arithmetic: `acc[cur] = acc[pred] - cost - h(cell) + h(observer)`, the smallest cost in the table is `1 << k = 128`, and the height plane is read **signed** (`00546cd9 MOVSX`), so a rise needs `h(observer) - h(cell) >= 128`. Corpus: the `.alm` type2 altitude bytes run **0..127 across all 72 shipped maps, with zero bytes >= 0x80**, so the largest rise available anywhere is **127 — one short of 128**. The accumulator is therefore strictly decreasing along every ray on shipped data, the visible set is star-shaped, and a re-execution over 81 048 + 61 364 observers finds **0 cells** visible whose predecessor was evaluated and blocked, at every `scanRange` from 4 to 19. **This is a customisation limit and it is one unit wide:** a map authoring a single altitude byte >= 0x80 makes that term exceed the axis cost and the region stops being star-shaped, so an implementation that prunes at the first blocker is indistinguishable from the original on shipped maps and diverges on an authored one | High (the store-before-branch, the flag's only consumers and the `MOVSX` are cited instructions, and the 128-vs-127 margin is arithmetic on them) / **Medium** that no re-lighting occurs, which rests on a corpus census of 72 maps and on nothing in the image enforcing the byte range | ● active | [EXP-0117](../experiments/EXP-0117-sight-region/) |
| AI-LOS-091 | **The complete reader and writer sets of both grids — and the sweep that reads as 'nobody touches these' is the one an agent would run first.** Step grid: written only by `FUN_00546790` (`disp:22a28` 3 hits, `disp:22a29` 3 hits, plus the four trailing stores at `0x22aa8`/`0x22aa9`/`0x229a8`/`0x229a9`, all one owner), read only by `FUN_00546c70` (`disp:22001` **1 hit** image-wide for the high byte, `disp:440` for the low). Cost grid: written only by `FUN_00546790` (`disp:28a28`, 1 hit), read only by `FUN_00546c70` (`disp:500`). **`disp:22000` and `disp:28000` each return 0 hits, and so do `imm:22000` and `imm:28000`** — the base is folded into a `SHL`, so it appears as `LEA ESI,[EDX + 0x440]` (`00546c7b`) and `LEA EBP,[EDX + 0x500]` (`00546c89`), and `imm:440`/`imm:500` cannot see those either, because `EnumRefs imm:` **excludes displacements by construction** and a `LEA`'s operand is a displacement. Only `disp:440`/`disp:500` see them. Every power-of-two folding of both bases was swept — `11000 8800 4400 2200 1100 880 220` and `14000 a000 5000 2800 1400 a00 280`, in both modes — and all are 0 on this object. Blind spots, stated: a wholesale `REP MOVSD` of the containing structure carries no displacement, bounded here by `range:546000:548600` reporting **0 orphan bytes** over 24 functions with the class's only bulk moves (`00547614`, `00546d62`, `00547500`) all read; and `callto:` cannot see a pointer table, closed by a raw whole-image scan (`tools/sightregion -mode ptrscan`, which walks the PE section table and is committed with the figure) finding **0 occurrences** of `547510`, `546790`, `5466c0`, `546c70`, `546d20`, `5474f0` or `5472a0` as a dword anywhere in the 1 977 344-byte image. The visibility byte map `fog+0x2a008` has 9 accesses over 5 owners, **all in the simulation** — no renderer reads it, so the AI's sight and the drawn fog share no storage | High (each sweep is quoted with its mode, its hit count and its owners; both instruments' blind spots are named, and each is closed by a second instrument of a different kind) | ● active | [EXP-0117](../experiments/EXP-0117-sight-region/) |
| AI-SIGHT-092 | **`actor+0xa4` is a `u16` sight radius in 1/256 cell whose high byte is `actor+0xa5`, and it has SIX writers — a `disp:a5` sweep sees two of them.** The pairing is the constructor's own two stores: `004f3374 MOV byte ptr [EDX + 0xa5],0x5` and `004f337e MOV byte ptr [EAX + 0xa4],0x0` are the little-endian halves of `0x0500` = 5.0 cells, and `004f8114 MOV word ptr [ECX + 0xa4],AX` writes the pair as one word. The complete set **over the two forms an offset sweep can enumerate**, each with the sweep that finds it: (1) `004f3374`, the constructor default, `disp:a5`; (2) `00532e8c MOV byte ptr [EDI + 0xa5],AL` with `AL = 5`, in `FUN_00532e60` behind `00532e78 CMP word ptr [EDI + 0xe],0x18`, which leaves the low byte alone, `disp:a5`; (3) `004f8114`, the hero recompute `FUN_004f7dfc` (`HERO-SIGHT-007`'s `ftol(((mind + reaction)/25 + 4) × 256)`), a **word** store spanning the byte, `disp:a4` only; (4) `004f570c ADD EAX,0xa5` → `004f5715 CALL FUN_00523410`, the `Data.bin` **Units** streamer's `scanRange` slot (`TERR-FOG-081`), `imm:a5` only; (5) `004f981f ADD ECX,0xa5` → `004f9829 CALL FUN_00523410`, the **Humans** streamer's slot 8 (`DAT-HUMANS-008`), `imm:a5` only; (6) `00510ba0 ADD ECX,0xa4` → `00510baa CALL FUN_00524020`, the actor's stream **load** arm, `imm:a4` only. Both helpers store through a register — `00523441 MOV byte ptr [ECX],AL`, `00524060 MOV word ptr [EAX],CX` — so neither carries a displacement. `disp:a3` and `disp:a2` are 4 hits each, all byte-wide on their own field, so no wider store spans `+0xa5` from below | High that each of the six is a writer (every one is a cited instruction re-read from raw bytes on both roots, and each helper's own store was read) / **Medium** that six is the whole set: the enumeration is complete over displacement form and immediate-computed-address form and over **nothing else** — an address reached in two steps, an address carried in a field, and a wholesale `memcpy` of the actor are invisible to every sweep run here. Refuting `two` needs one counterexample and has four; asserting `six` needs an instrument this round does not supply | ● active | [EXP-0120](../experiments/EXP-0120-sight-writers/) |
| AI-SIGHT-093 | **The drawn fog and the AI's vision are ONE algorithm written twice, and at a whole-cell sight they agree exactly — so `TERR-FOG-088`'s Medium is discharged.** Nineteen clauses of `FUN_00403718`/`FUN_00403c8c`/`FUN_00403b77` against `FUN_00546790`/`FUN_00546d20`/`FUN_00546c70` (`evidence/rom-two-implementations.md`): both slope tests (`j < i>>1` at `0040376f`/`0054681e`, `j > 2i` at `00403872`/`00546869`), all three zone deltas, the four-mirror store order, the cost form `ftol(S·√(i²+j²)/max(i,j))` with `S = 1<<k` (`00403a7a`…`00403ab0` / `005468f2`…`00546902`), the `i,j = 0..20` bounds, the recurrence `pred − cost − h(cell) + h(obs)` stored **before** the test (`00403c5e`/`00403c77`, `00546cef`/`00546cf2`), *visible iff > 0*, the ring bound `r = 1..19` (`00403d53`/`00546f8d`), four edges of `2r+1`, and — the clause `TERR-FOG-080` missed — the **same four trailing literal stores**, `00403b4b`…`00403b6c` against `0054699e`…`005469b3`, repairing the same two cells `(+1,0)` and `(-1,0)` from the same wrong values. And the seeds are algebraically equal for a whole-cell sight: `(sr·256 >> (8−k)) + (1<<(k−1))` = `(sr<<k) + (1<<(k−1))` at `k = 7`. Executed by `tools/sightwriters`: the four tables differ in **0 of 1681** window cells, patched or unpatched; the two marches over the shipped altitude grids at `scanRange = 6`, stride 4, 72 maps and both roots give **43 640 observers more than 27 cells from any edge and 0 disagreements** (27 = window radius 20 + the wider inset). **A consumer needs one implementation, parameterised** | High (every clause is a cited instruction on both sides, and the agreement is executed rather than described; the two live rivals — different tables, different seeds — are each refuted by their own measurement) | ● active | [EXP-0120](../experiments/EXP-0120-sight-writers/) |
| AI-SIGHT-094 | **Where the two DO diverge: the server truncates a hero's sight to whole cells and the drawn fog does not, so the fog can show a hero cells his own simulation does not grant him.** The client seeds from the whole `u16` — `00403cdb MOVSX EDX,word ptr [ECX + 0x102]`, the drawable field `TERR-FOG-081` traces to `actor+0xa4` — then `00403cf0 SAR EDX,CL` with `CL = 8 − k`, keeping 1/128-cell precision. The server seeds from the byte: `00546d81 MOV CL,byte ptr [EDI + 0xa5]`, `00546d93 SHL EBX,CL`, discarding all eight low bits. For a monster the streamers write whole cells and the two coincide. For a **hero** they do not: `HERO-SIGHT-007`'s derive is `ftol(((mind + reaction)/25 + 4) × 256)`, a multiple of 256 only by accident. On flat ground at `k = 7`, `actor+0xa4 = 1535` (5.996 cells) reveals **145** cells to the fog and admits **105** to the stamp — a **40-cell** gap; 1408 gives 117 against 105, 1152 gives 77 against 69. The second divergence is `TERR-FOG-118`'s in-bounds inset. Nothing else differs | High for the arithmetic (four cited instructions, two per side, and the region figures are this round's transcription of both) / **Medium** for the reach: whether a player-controlled hero is ever the subject of a server sight stamp is not established here — `FUN_00546d20`'s two callers are taken from EXP-0117's call graph and were not re-read — and no runtime session witnessed the gap | ● active | [EXP-0120](../experiments/EXP-0120-sight-writers/) |
| AI-POST-095 | **A load-time guard's post is written by the guard SETTER, not by the per-actor initialiser — which that member never reaches.** `FUN_00534ac0`, the routine `AI-AUTHOR-015` reads for `grpAI+0x20 = 1`, writes every member's `ord+0x00` first: `00534bba MOV dword ptr [ESI + 0x50],0xb`, then `00534bc1 CALL 0x00545c30` (`MOV DL,[ECX+0x4]` / `MOV AL,0x80` / `CMP DL,AL` / `CMP [ECX+0x5],AL` — on a cell centre?), then either `00534bed MOV word ptr [ECX],AX` from `FUN_00544a00` (`MOV AX,word ptr [ECX + 0x2]` / `RET`, the current packed cell) or `00534bda` from `mv+0x06`; then `00534bf6 MOV byte ptr [EDX + 0x8],BL`, no order. That is **33 bytes after** the state store and **0x76 bytes before** `00534c30 MOV byte ptr [EAX + 0x20],0x1`, and the setter's member walk `00534b3a`…`00534c24` is instruction-for-instruction the arm's own `005370b3`…`005370de`, so the population written is the population read. `AI-GRPGUARD-074`'s "for a group under order 1 from load nothing has ever written `ord+0x00`" is **refuted**, and `AI-POST-042`'s mechanism is **corrected**: `FUN_0052e3c0` is reached only through `FUN_0052ce50` arm `0xb`, `FUN_0052d940`, `FUN_00532e60` and `FUN_00532c00` (`callto:52e3c0` = 6 / 4 / 0), all under the per-actor machine, which `AI-ORDER-010` establishes a group under order 1 never evaluates (`callto:52ce50` = 2 / 2 / 0, the live one being `00533b43`, inside arm **0**'s body at `00533b1f`). The **value** both rows give — the spawn cell — survives; the route to it does not, and three behaviours turn on the difference (`AI-STANCE-098`, `AI-POST-097`, `AI-GATE-100`) | High (three routines read end to end, all 69 cited encodings re-read out of the PE section table by `tools/guardpost -mode cites` and 0 disagree, and every branch and call target fixed by its own rel8/rel32 rather than by a second decode) | ● active | [EXP-0125](../experiments/EXP-0125-guard-post/) |
| AI-POST-096 | **`ord+0x00`'s writer set is at least 26 stores over 23 routines, not four.** The field sits at displacement **0** of a block reached through `[actor+0x158]`, so a store into it carries no displacement of its own and `EnumRefs disp:` — the instrument behind `AI-POST-042`'s enumeration — cannot see it at all. The sweep that can is `re:MOV word ptr \[E..\],`: **154 hits / 118 owners / 5 in orphan code** image-wide, reduced to the hits whose *preceding* instruction loads the base from `[<reg> + 0x158]`, which leaves **26 stores / 23 routines** (`evidence/enum-post-writers.md`). `AI-POST-042` names five of them; the eighteen it does not include `FUN_00534ac0`, `FUN_00534cb0`, `FUN_00535920` and `FUN_0053c7f0` — that is, every stance setter. So the clause "Its complete set of later writers is four" is **retracted** | High for the refutation — one counterexample is enough against a completeness clause and `00534bed` is one, cited and re-read. **Medium** for the figure 26/23, which is a **lower bound**: the filter keeps only stores whose base was loaded in the immediately preceding instruction, and a wholesale copy of the block carries neither a displacement nor a load | ● active | [EXP-0125](../experiments/EXP-0125-guard-post/) |
| AI-POST-097 | **A guard's post is not fixed for the mission — six reachable sites re-anchor it, and `AI-BREAK-041`'s 5-cell block moves with it.** `EnumRefs callto:534ac0` = **6 hits / 6 owners / 0 orphan**: the map-load stance walk `004e380b`, the mission-description builder `004f1b32`, the **player's** group-command dispatcher `004d60c6`, the **script's** `0053c05b`, plus `004e2261` and `00505001`. Every one runs the per-member body of `AI-POST-095`, so every one rewrites `ord+0x00` to wherever the unit is standing at that moment. `FUN_0053c7f0` (`callto:` = 1 / 1 / 0, from `0053a087`) is the same body for a single actor, with the same two-way branch at `0053c8a8`…`0053c8e1`. `AI-POST-042`'s "for a guard it never moves" and "fixed there for the whole mission" are therefore **retracted**: they hold for a creature nobody commands, which is most of a shipped map, and fail the moment Guard is issued again | High (the caller enumeration is a `callto:` with 0 orphan, and the body it reaches is one routine read end to end). **Medium** that the enumeration is complete — `callto:` sees direct calls and `.rdata` code pointers, so a dispatch through a computed pointer would be missed | ● active | [EXP-0125](../experiments/EXP-0125-guard-post/) |
| AI-STANCE-098 | **All four stance setters anchor a post, and only the guard forms have a mid-step branch.** `FUN_00534cb0` (`00534dc8`) and `FUN_00535920` (`00535a92`) — the two Stand Ground setters `AI-STAND-076` names — write `ord+0x00` from `FUN_00544a00` **unconditionally**, with no idle test at all, two instructions after their own `actor+0x50 = 0xc`. The guard forms `FUN_00534ac0` and `FUN_0053c7f0` branch on `FUN_00545c30` and, for a unit **not** on a cell centre, take `mv+0x06` instead (`00534bd6`, `0053c8bd`) — the cell it is stepping **into**, not the one it is leaving. `FUN_00549990` is what puts a cell there: `005499b9 MOV DX,word ptr [EAX + 0x8]` off the live path node `[actor+0x17c]`, `005499bd MOV word ptr [ECX + 0x6],DX`, with `mv+0x80` taking the same value once the cell is reserved. So guarding a walking unit anchors it one cell ahead of itself | High for the four bodies and the branch (each read end to end, every store a cited and re-read instruction). **Medium** for `mv+0x06` being the step target: `FUN_00549990` was read whole, but that field's writer set was not enumerated | ● active | [EXP-0125](../experiments/EXP-0125-guard-post/) |
| AI-LOAD-099 | **The map load spawns the placements and then sets the stances, seventeen bytes apart in one basic block — and the order block starts zeroed.** `FUN_004e1924`: `004e1e3f CALL 0x004e26bb` (the `.alm` spawner, `callto:4e26bb` = **1 / 1 / 0**), `004e1e44 PUSH 0x1`, `004e1e50 CALL 0x004e3591` (the stance walk). No branch separates them. So "the actor's current cell" at the setter is the placement's own cell, which is what makes `AI-POST-095`'s post the spawn cell rather than a stale value. The block it writes into is `new`'d at `004f315a PUSH 0x94` and constructed by `FUN_0052cbb0`, whose first act is `0052cbb5 MOV ECX,0x25` / `XOR EAX,EAX` / `0052cbc0 REP STOSD` — `0x25` dwords is exactly `0x94` bytes, so `ord+0x00` is 0 until something writes it and `FUN_0052e3c0`'s `CMP word ptr [EAX],0x0` tests a real zero. The sibling allocation two lines up is `004f3110 PUSH 0xb4` / `004f3130 CALL 0x00545c00` for `[actor+0x154]`, likewise zeroed whole before `+0x5 = 0x41`, `+0x8 = 5`, `+0x9 = 0xff`, `+0xa = 0x10` | High (both allocations, both constructors and the two calls are cited instructions re-read out of the PE, and the `CALL rel32` targets are computed from the bytes rather than taken off a listing) | ● active | [EXP-0125](../experiments/EXP-0125-guard-post/) |
| AI-GATE-100 | **The stance walk's session gate wraps BOTH setter branches — closing `AI-AUTHOR-015`'s open item — and the inversion is benign for a reason that row did not have.** In `FUN_004e3591`: `004e37c5 CMP dword ptr [EBP + 0xc],0x0` / `004e37c9 JZ` and `004e37d0 CMP dword ptr [EAX + 0xc],0x0` / `004e37d4 JNZ`, both `rel8` to **`004e3810`**, which is the group-list advance — and `004e37f5 CALL 0x00535920` and `004e380b CALL 0x00534ac0` are the only instructions between them and it. So when either test fails **no group is set at all** and every group keeps `FUN_0052cc40`'s `grpAI+0x20 = 0`. That is `AI-ORDER-010` arm 0, which runs each member's own `actor+0x50` machine, whose constructor default is `0xb` (`AI-STATE-011`, `004f3321`), whose body is `FUN_0052e3c0` — which writes the post at the first tick. **The post is written on both configurations, by different routines**, and `AI-POST-042`'s mechanism is the live one only here | High (both gate tests and both `rel8` targets are re-read out of the image, and the two calls' position between the gate and its target is a property of the byte layout, not of a decode). **Medium** on `session+0x0c` meaning multiplayer — that is `SESS-MAP-010`'s reading, quoted rather than re-derived | ● active | [EXP-0125](../experiments/EXP-0125-guard-post/) |
| AI-CENTRE-101 | **A placement is centred, and that is what decides which branch the guard setter takes.** The position record `[actor+0x10]` holds the packed cell at `+0x02` and two sub-cell offsets at `+0x04`/`+0x05`; `FUN_00545c30` returns 1 exactly when both are `0x80`. It has **four** placement entry points and every one writes `0x80` into both: the reset `FUN_00544530` (`00544532 MOV DL,0x80`, cell 0), `FUN_00544550` (`0054456c MOV CL,0x80`), `FUN_005445d0` (`005445ea MOV AL,0x80`) and `FUN_00544810` (`0054489d MOV DL,0x80`), the last of which additionally **clamps both coordinates into `[8, dim−9]`** — `0054482f MOV byte ptr [ESP + 0x18],0x8`, `00544850 SUB DL,0x9`, `00544860 MOV BL,0x8` — reading the map's dimensions from `[world+0x50000]` and `[world+0x50004]`. The only other writer of those offsets, `FUN_005495f0` (`0054984b`/`0054984f`), re-centres on **arrival** and is reached from three movement routines (`callto:5495f0` = 3 / 3 / 0). A unit that has been placed and has not moved is therefore centred, and the setter takes `00534bdf` | High (all four entry points read end to end, every `0x80` store a cited and re-read instruction, and the clamp's bounds are the routine's own immediates). **Medium** that the four are the complete placement set — the sweep behind that is `re:MOV word ptr \[E.. \+ 0x2\],` over the image, which cannot see a placement made by copying the record | ● active | [EXP-0125](../experiments/EXP-0125-guard-post/) |
| AI-RANGE-102 | **`AImgr+0x00` is `0x8000`, and `n × rand() / AImgr[0]` is the AI module's uniform-range idiom.** The AI manager is the global at `[0x005f21c4]`, `new`'d at `004e1c80`, constructed by `FUN_0052c400` (`004e1ca6`) and stored at `004e1ccd`; `FUN_0052c400` forwards the same `this` to `FUN_0052b2b0` (`0052c41e`), which seeds the CRT generator — `PUSH 0x0` / `CALL 0x00557560` / `CALL 0x00554a50` — and then writes `0052b2ea MOV dword ptr [ESI],0x8000`. `EnumRefs "re:MOV dword ptr \[E..\],0x8000"` returns **1 hit / 1 owner / 0 orphan**, that instruction. `0x8000` is `RAND_MAX + 1` (`AI-RAND-058` fixes `RAND_MAX` at `0x7fff`), so every `CDQ` / `IDIV dword ptr [<AImgr>]` after a `CALL 0x00554a60` yields `floor(n · rand() / 32768)`, uniform on `0…n−1`. `EnumRefs "re:IDIV dword ptr \[E"` = 86 hits / 50 owners, of which **19 divide by a bare pointer and 15 of the 19 are inside `0x52a000..0x53f000`**. This **closes `AI-RAND-058`'s Medium**: `FUN_0052b2b0` writes no vtable at `this` because it writes a number there | High for the constructor's store, the allocation chain and the counts (each a cited instruction re-read out of the PE, and the global's writer set is a printed enumeration). **Medium** that the field still holds `0x8000` at any given roll — the sweep is one store form and cannot see a register-form write, and the other 13 bare-pointer `IDIV` sites were counted rather than read | ● active | [EXP-0125](../experiments/EXP-0125-guard-post/) |
| AI-JITTER-103 | **The guard radius jitter is exactly `{−1, 0, +1}`, in thirds — `AI-GUARD-021`'s ±1 confirmed rather than assumed — and the empty-members edge omits the `+4`.** `FUN_00536ef0`'s has-members edge: `00536fc5 CALL 0x00554a60` / `00536fca LEA EAX,[EAX + EAX*0x2]` (3 × rand) / `00536fd0 CDQ` / `00536fd1 IDIV dword ptr [EBX]` with `EBX` the AI manager (reloaded at `00536f98`, and passed as `ECX` to `FUN_0052e940` at `00537047`) / `00536fd3 MOV DL,byte ptr [ECX + 0x38]` / `00536fd6 DEC EAX` / `00536fd7 ADD DL,AL` / `00536fd9 ADD DL,0x4` / `00536fdc MOV byte ptr [ECX + 0x2d],DL`. With `AImgr[0] = 0x8000` (`AI-RANGE-102`) the quotient is `floor(3·rand()/32768)` ∈ `{0,1,2}` — 0 for `rand() < 10923`, 1 for `10923…21845`, 2 for `21846…32767` — so `grpAI+0x2d = grpAI+0x38 + 4 + {−1,0,+1}`, each about a third of the time. The **empty**-members edge at `00537008`…`0053701c` is the identical sequence **without** `ADD DL,0x4`, giving `grpAI+0x38 − 1 … +1`. Both dead copies `AI-DEADROLL-078` found, `FUN_00536e90` and `FUN_00536ec0`, carry the has-members form | High (one routine read end to end, every instruction of both edges re-read out of the PE, and the divisor's object fixed by the register's own reload and its later use as a `this`). **Medium** for the thirds, which inherit `AI-RANGE-102`'s Medium on the divisor still holding `0x8000` at the roll | ● active | [EXP-0125](../experiments/EXP-0125-guard-post/) |
| AI-TURN-104 | **The idle turn's quotient runs 0…189, not 0 — so the arm re-picks a near-uniform facing over a byte, not one eighth of a circle.** `FUN_00531a20`'s dividend is built from `rand()` as `00531a75 LEA EAX,[ECX + ECX*0x2]` / `00531a78 SHL EAX,0x5` / `00531a7b SUB EAX,ECX` / `00531a7d SHL EAX,0x1` — `((3x · 32) − x) · 2` = **190 × rand()** — and `00531a80 IDIV dword ptr [EDI]` divides by the routine's own `this`, which is the AI manager (`00531aa4 MOV ECX,dword ptr [EDI + 0xa50]` is the same world field `FUN_0052e3c0` reads at `0052e407`). With `AImgr[0] = 0x8000` the quotient is `floor(190·rand()/32768)`, **uniform on 0…189**. The stored facing is then `00531a69 MOV BL,byte ptr [ECX]` / `00531a6b ADD EBX,0x21` / `00531a88 ADD EBX,EAX` / `00531a8a MOV byte ptr [EDX + 0x1],BL` — the current facing plus `0x21` plus that quotient, truncated to a byte. `AI-RAND-058` left "an identically-zero quotient is not established"; it is now positively **refuted**, and `AI-ORDER-039`'s surviving "one eighth of a circle" wording describes only the `0x21` term. The arm's entry gate is unchanged: `00531a3b`…`00531a52`, `ord+0x54` non-zero or `rand() < 0xcd` | High (the routine read end to end, the multiplier recovered from the shift-and-add chain rather than from an immediate, and the divisor's object fixed by `[EDI+0xa50]` matching a known AI-manager field). **Medium** for the range, inheriting `AI-RANGE-102`'s Medium on the divisor | ● active | [EXP-0125](../experiments/EXP-0125-guard-post/) |
| AI-CANDCOUNT-105 | **`AImanager+0xbb4` is the element count of the candidate collection at `+0xba8`** — so what group order 5 branches on is *"did the build that just ran find anything"*, not an assigned flag. The collection's own `RemoveAll` `FUN_00518320` fixes the layout in its body: it walks a chain from `[this+0x4]` through `[node+0x0]` destroying each `[node+0x8]`, then `00518355 MOV dword ptr [EDX + 0xc],0x0`, `0051835f [EAX+0x10] = 0`, `00518369 [ECX+0x8] = 0`, `00518373 [EDX+0x4] = 0`, and frees the plex chain at `[this+0x14]` — count first. It is called with `LEA ECX,[AImgr + 0xba8]` (`005365e8`) and `LEA EBP,[AImgr + 0xbc8]` (`00536748`), so `+0xc` on the first collection *is* `+0xbb4`. The cross-read a coincidence would have to survive sits inside one routine: `FUN_005365e0` reads the **second** collection's count as `005367cf MOV EAX,dword ptr [EBX + 0xbd4]` and again as `005367e2 MOV EAX,dword ptr [EBP + 0xc]` with `EBP = EBX + 0xbc8` set at `00536748` and not reassigned between — the same field through two bases, fourteen instructions apart, both used as *"is this collection empty"*. The owner is the AI manager, the global at `[0x005f21c4]` (`AI-RANGE-102`): its constructor `FUN_0052c400` keeps `this` in ESI (`0052c418`) and calls `FUN_0052b2b0` with ECX untouched (`0052c41e`), and that callee holds the same `this` from `0052b2cf` to its epilogue while constructing both outer objects (`0052b3da LEA ECX,[ESI + 0xba4]` / `0052b3e3 CALL 0x0050fb5e`, and `0052b3e8` for `+0xbc4`). Every address and branch target above is re-read out of the PE by `tools/candcount` — 41 anchors, 9 recomputed branch targets, 0 failures | High. `RemoveAll`'s own body defines `+0xc`; the same field is read through two different bases inside one routine; the owner is a cited constructor chain rather than an inference from displacement; and every citation is checked against the file's bytes rather than against a second decode (`docs/INSTRUMENT.md` rule 6). The reading this refutes — an assigned mode flag — is excluded by the write population, not merely unsupported (`AI-BB4SWEEP-109`) | ● active | **[EXP-0126](../experiments/EXP-0126-swarm2-gate/)** |
| AI-CANDLIST-106 | **The AI manager carries two candidate collections, at `+0xba8` and `+0xbc8`, each four bytes inside an outer object at `+0xba4`/`+0xbc4` — and the pair is shared scratch, used by per-*actor* acquisition as well as by the group layer.** `FUN_0050fbee` is the outer object's add and is the whole proof of the `+4`: `0050fbfc ADD ECX,0x4` then `0050fbff CALL 0x0051a200` (AddTail). The compiler emits the null-preserving form of the same adjustment where the outer pointer could be null — `00536759 MOV EAX,ESI` / `NEG EAX` / `SBB EAX,EAX` / `00536763 AND EAX,EDX` with `ESI = AImgr+0xba4` and `EDX = AImgr+0xba8` — and its simple form at `00536c75 TEST EAX,EAX` / `00536c79 LEA EAX,[ESI + 0xba8]`. Stride `0x20` = 4 + the collection's `0x1c`, and the first outer object begins exactly where the preceding member ends: `AI-PREF-070`'s 4×4 byte matrix occupies `+0xb94`…`+0xba3`, sixteen bytes, written one at a time in this same constructor at `0052b45e`…`0052b4bb`. That adjacency also settles the sibling class for good — on it a *collection* spans `+0xb8c`…`+0xba7` (`004983bd`…`004983e5`), which the matrix's sixteen individual byte stores exclude here. `EnumRefs disp:` at `ba4 ba8 bb4 bc4 bc8 bd4` returns 23 / 15 / 15 / 20 / 4 / 4 hits, **0 orphan** in all six; **ten** AI-module routines reach the pair through their own `this`, each prologue cited — `FUN_0052dea0` (which restores `this` into EBX at `0052dfb8`, before its first hit), `FUN_0052e110`, `FUN_005327d0`, `FUN_005365e0`, `FUN_00536c10`, `FUN_00536d50`, `FUN_00536ef0`, `FUN_005371e0`, `FUN_0053ddd0`, `FUN_0053e2d0` — plus six that touch only the outer objects: `FUN_0052b250`, `FUN_0052b2b0`, `FUN_0052b580`, `FUN_0052b8e0`, `FUN_0052bb10`, `FUN_0052c710`. `FUN_0052e110` is the per-actor acquisition rule of `AI-ACQUIRE-002`, so the pair is **not** per-group state and could not carry a per-group mode even if something wrote one | High for the layout — the `+4` adjustment is a cited instruction in two forms and the stride is arithmetic on the sweeps' own hits. **Medium** for the owner enumeration: six `disp:` sweeps over the whole image, blind by construction to a wholesale copy of the containing structure and to any access that reaches a collection through a pointer saved earlier rather than through `AImgr + <disp>` | ● active | **[EXP-0126](../experiments/EXP-0126-swarm2-gate/)** |
| AI-SWARM2GATE-107 | **Group order 5 runs order 4's arm whenever the group sees nothing, and that fallback is the ordinary path rather than an exotic one.** `FUN_005371e0` is `__thiscall(AImgr, group, cell)`, `RET 0x8`: `005371e8 MOV EDI,ECX` keeps the manager, `005371ea PUSH EBP` passes the group, `005371eb CALL 0x005365e0` builds the candidate list, and the very next instruction reads the count that build produced — `005371f0 MOV EAX,dword ptr [EDI + 0xbb4]`, `005371f6 TEST EAX,EAX`, `005371f8 JZ` with `rel32 = +0x96`, which pins the target at `00537294` however the bytes are decoded. There `00537294 MOV EDX,dword ptr [ESP + 0x18]` and `0053729c CALL 0x00537510` forward **both** arguments to order 4's arm (`AI-MOVE-023`), which never reads the second one. The value cannot be stale: `FUN_005365e0` empties the collection at `005365f7`, **before** its own empty-group early exit at `00536613`, so a group with no members reaches the gate at count 0 and falls through. And the count is whatever that build produced, which by `AI-GROUPSEE-068` includes corpses — collection B is moved back into A when A ends empty — so a group whose only visible candidate is dead still runs order 5's own body. **The two zero-target cases are therefore different, and the difference is a walk:** *no candidate at all* takes the gate to order 4's arm and the members walk; *candidates the scorer vetoes* — `FUN_00536c10` writing `ord+0x20 = 0` for every member (`AI-SCORE-069`, `AI-PREF-070`) — leaves the arm in its own body, where `00537227 MOV ECX,dword ptr [EAX + 0x20]` / `0053722a TEST ECX,ECX` / `0053722c JZ 0x00537239` sends each member to `00537243 MOV byte ptr [EAX + 0x8],0xb` plus the optional cast, or to the heal AI at `0053725b` for a human participant's unit. **Neither of those branches walks**, so a vetoed order-5 group stands still while an empty-sighted one moves | High for the mechanism: one routine read end to end, the branch target fixed by its own `rel32`, the argument forwarding cited, and the pre-clear ordering read out of `FUN_005365e0`'s own instruction order. **Not established, and not establishable from the image:** how often the branch is taken in a shipped mission, which is a fact about play. `experiments/EXP-0126-swarm2-gate/EXP-0126.md` states three falsifiable predictions and what refutes each | ● active | **[EXP-0126](../experiments/EXP-0126-swarm2-gate/)** |
| AI-CMDSET45-108 | **The order-4 and order-5 command setters are one routine emitted twice, and they differ in exactly one byte in the whole image.** `FUN_005340a0` (`Move`) and `FUN_00534390` (`Swarm 2`) are adjacent `0x2f0`-byte blocks, `0x2f0` apart. `tools/candcount` compares them in the file and folds away only a difference that is the tail of an `E8`/`E9` `rel32` whose two encodings differ by exactly the block distance — the same absolute target named from two places, with the opcode byte required to match. **29 bytes differ; 14 such branches account for 28 of them; one byte survives**, `00534368` = `0x04` against `00534658` = `0x05`, the immediate of `MOV byte ptr [EDX + 0x20],imm` — the group order itself (`AI-GROUPCMD-020`'s `00534365` and `00534655`). The fourteen shared targets are `005306e0`, `00519780`, `00521b00`, `00533210`, `00544a00`, `0054d100`, `00533210`, `005306e0`, `00519780`, `00521b00`, `00544a10`, `00544a20`, `0052f4f0`, `0052f4f0`. **Consequence:** every per-member state an order-4 group holds when `FUN_00537510` runs, an order-5 group holds identically, so the fallback is coherent rather than degenerate — `Par0 = 5` is `Par0 = 4` plus the gate. What the shared routine *does* is already published: `MOVE-FORM-036` reads the formation arm and `MOVE-GATE-035` the two gates, both citing the two routines as parallel address pairs (`005342b9`/`005345a9`, `005341a0`/`00534490`, …). This row does not re-derive them; it closes the question those pairs leave open, namely whether the two routines agree **everywhere else** as well | High. The comparison is over the file's bytes rather than over two disassembly listings, and the fold rule is mechanical and stated: a listing diff of these blocks shows fourteen "differences" that are not differences, and dismissing them by eye is the same fold applied silently. **This is a sharpening, not a discovery** — [EXP-0094] had already read both routines end to end and found them to behave alike; what was not established is that they are the same bytes | ● active | **[EXP-0126](../experiments/EXP-0126-swarm2-gate/)** |
| AI-BB4SWEEP-109 | **No `disp:bb4` sweep can see a writer of `AImanager+0xbb4` — the complete write population is three instructions and every one carries displacement `0xc`.** They are `005191e9 MOV dword ptr [ECX + 0xc],EAX` after `005191e0` and `005191e3 ADD EAX,0x1` (node allocation, `FUN_00519130`); `00519b15 MOV dword ptr [EDX + 0xc],ECX` after `00519b0c` and `00519b0f SUB ECX,0x1` (node free, `FUN_00519ae0`, which then runs the *count reached zero, so RemoveAll* test at `00519b1b` and `00519b24`); and `00518355 MOV dword ptr [EDX + 0xc],0x0` in `RemoveAll` itself. All three are the shared list implementation, reached with `this` = `AImgr + 0xba8`; none is reachable by any sweep keyed on `0xbb4`, at any completeness. **The `0x0049xxxx` hits are a different class**, and `AI-SWARM2-024`'s reading of them is corrected twice over: they are **four writes and one read** (`0049b062`), not five writes; and `FUN_004982e0` constructs six collections inline over `+0x844`…`+0xbc4` on a `0x1c` stride — `004983eb MOV dword ptr [ESI + 0xba8],0x59a868` is a `CObject`-derived vtable (`imm:59a868` = 3 hits / 3 owners / 0 orphan) and `00498401 MOV dword ptr [ESI + 0xbb4],EDI` is that collection's own count — so the two layouts coincide at exactly this displacement and diverge at the next, the sibling's run continuing at `+0xbc4` where the manager's second collection is at `+0xbc8`. **The instrument shape**, a fourth kind for `docs/INSTRUMENT.md`'s family: a field inside an embedded object is addressed by two disjoint displacements — `outer + BIG` by everything outside it, `inner + small` by everything that writes it — and the tell is a sweep whose hits are all reads in a class big enough to embed things. The cure is to sweep the *neighbouring* displacements and read the layout off the pattern: `bd4 − bc8 = 0xc` on the second collection is what fixed `bb4 − ba8 = 0xc` on the first | High as a negative claim about the instrument, because it does not rest on the sweep: it rests on reading the three routines that hold the arithmetic and on the `+4` / `+0xc` layout `AI-CANDCOUNT-105` establishes. **Unknown** whether some other instruction writes the field through a pointer neither path produces — a saved `AImgr+0xba8` used outside the list implementation would be invisible to `disp:bb4` and `disp:ba8` alike | ● active | **[EXP-0126](../experiments/EXP-0126-swarm2-gate/)** |
| AI-CANDBYTE-110 | **The order-5 gate tests the candidate count as a dword; the scorer tests only its low byte — so a count that is a non-zero multiple of `0x100` passes the gate and scores nothing.** `005371f0 MOV EAX,dword ptr [EDI + 0xbb4]` / `005371f6 TEST EAX,EAX` against `00536c24 MOV EDX,dword ptr [ESI + 0xbb4]` / `00536c32 TEST DL,DL` / `00536c34 JZ` `rel32 +0xce` to `00536d08`, the arm that walks the members writing `ord+0x20 = 0` (`AI-SCORE-069`), and the same pair in the twin `FUN_00536d50`. The group's own member count is read the same way, `TEST CL,CL` on `byte [group+0xc]`, which is that collection's `+0xc` in turn. The effect is bounded and disclosed rather than observed: at exactly `0x100` visible candidates order 5 would enter its own body, clear every member's target, and send each member down the `ord+0x08 = 0xb` or heal branch instead of engaging | Medium. Both instructions are cited and the asymmetry is not in doubt; what is not measured is whether `0x100` simultaneously visible candidates is reachable on any shipped map, a census this experiment did not run. Recorded because it is a **limit**, and lifting an actor cap is exactly the kind of change that would make it reachable | ● active | **[EXP-0126](../experiments/EXP-0126-swarm2-gate/)** |
| AI-DEFEND-111 | **`actor+0x50 = 8` is *defend a unit*, and the whole of its engagement is fought on the protected unit's behalf, not on the defender's own.** `FUN_0052ce50` arm 8 loads `ord+0x10` (`0052d0f9`) and calls `FUN_0052d3c0(self, that actor)` (`0052d0fe`), `RET 0x8`, `ECX` = the AI manager. The arm reads `CL = ord+0x70` (`0052d3d5`) and `AL = FUN_00545cd0` — `TRIG-DIST-014`'s Chebyshev — over the two actors' `+0x10` position objects (`0052d406`), saving both. **`dist > range`** (`0052d413 CMP AL,CL` / `0052d415 JBE 0052d456`): `ord+0x08 = 4` (`0052d41f`), `ord+0x18 =` the protected unit (`0052d429`), `ord+0x14 =` the range or, when the range is 0, `actor+0xa5` (`0052d434`/`0052d44b`, selected by `0052d41d TEST CL,CL`) — that is `AI-ORDER-039`'s arm 4, close on the actor `ord+0x18` with stop distance `ord+0x14` — and **return**, so a defender outside its range never engages. **`dist <= range`**: `FUN_0052d600` when `actor+0x140` is set, else `FUN_0052e4d0` (`0052d456`…`0052d46b`), both `(self, protected unit)`; then the inner table (`AI-FOLLOWTAB-113`); then the crowding check (`AI-FOLLOWGAP-114`). **`FUN_0052e4d0` is the cover**: it builds the occupants of the `(2r+1)²` block around **the protected unit's** cell with `r =` that unit's `mover+0x08` (`0052e4dd`…`0052e500`, `FUN_00543ab0`, `AI-FILTER-001`), filters them with `FUN_0052ab60(protectedUnit, list, 0)` (`0052e510`) — so the survivors are what is hostile **to the protected unit**, which need not be hostile to the defender — and engages via `FUN_0052e940` the candidate nearest *self* by `FUN_0054a960`, preferring those whose virtual `[vt+0x20]` returns 3 (`0052e55a`/`0052e55d`) and falling back to any; an empty block is `FUN_0052e110(self)` (`0052e648`) | High for the arm and for cover's polarity (both routines read end to end from a raw listing, and all 100 cited encodings re-read out of the PE with 18 branch/call anchors landing exactly — `evidence/cites.txt`, `anchors.txt`; the polarity is discriminated by the *arguments*, `ESI` = arg2 at `0052e4d6` being the protected unit at every one of the three sites that use it). **Medium** that the block is `11×11` — `mover+0x08 = 5` rests on `00545c1a` plus an immediate-form sweep (57/31/0, its only mover-based hit), and a register-form store is invisible to that. **Unknown** what the virtual `[vt+0x20]` returns 3 for | ● active | [EXP-0127](../experiments/EXP-0127-follow-pair/) |
| AI-FOLLOW-112 | **`actor+0x50 = 0x11` is *follow*, it is the defend arm minus the fighting, and the two are not one behaviour parameterised.** `FUN_0052ce50` arm `0x11` (`0052d110`/`0052d117`) calls `FUN_0052d500(self, ord+0x10)`. Its out-of-range half is `AI-DEFEND-111`'s, store for store — `ord+0x08 = 4`, `ord+0x18 =` the unit, `ord+0x14 =` range or `actor+0xa5` (`0052d55a`, `0052d564`, `0052d56f`, `0052d585`). Its **within-range half is three instructions**: `0052d58f CMP AL,0x2` / `0052d591 JNC 0052d5e4` → `FUN_0052e110(self)` (`0052d5e7`), the ordinary acquire-with-no-leash any unordered unit runs; below 2 → the step-away of `AI-FOLLOWGAP-114`. There is **no** `CALL 0x0052d600`, **no** `CALL 0x0052e4d0` and **no** indirect `JMP` anywhere in the routine, so a follower never scans on the followed unit's behalf and never heals it — it fights only what it would have fought standing alone. `EnumRefs refto:52d4e4` = **1 hit / 1 owner / 0 orphan**, `0052d483`, inside `FUN_0052d3c0`, and `imm:52d4e4` = 0 as `docs/INSTRUMENT.md` rule 3 predicts for a SIB displacement: the inner table belongs to the defend arm alone | High (the routine is 41 instructions read whole; the discriminating byte `0052d58f 3c02` re-reads out of the PE, and `0052d591 7351` is a `rel8` fixing its target at `0052d5e4` independently of any decode) | ● active | [EXP-0127](../experiments/EXP-0127-follow-pair/) |
| AI-FOLLOWTAB-113 | **The five-arm table at `0x0052d4e4` is a two-way test wearing a jump table, and its index is a field re-read after the call rather than a return value.** `0052d470 MOV EDX,[ESI+0x158]` / `0052d478 MOV AL,byte ptr [EDX+0x8]` / `0052d47b ADD EAX,-0x5` / `0052d47e CMP EAX,0x4` / `0052d481 JA 0052d48a` / `0052d483 JMP dword ptr [EAX*0x4 + 0x52d4e4]`. Read out of the PE through its own section table (`tools/followarm -mode table`) the five entries are **`0052d4db 0052d4db 0052d48a 0052d4db 0052d4db`** — four of five are the routine's own epilogue and the fifth is the same block the bound's `JA` falls through to. So the domain is `ord+0x08` in 5…9, which by `AI-ORDER-039` is exactly *what the engagement helper just started*: 5 and 6 the pursuit pair, **7** the sack pick-up bridge, 8 and 9 the two casts. The behaviour is one sentence — **a pursuit or a cast means the defender is busy and does nothing further this tick; anything else falls to the crowding check** — and a consumer needs no table to implement it. This is why the arm has an inner switch nobody had read: it is a compiler's emission for a `switch` four of whose cases `break` | High (the table is a PE read, not a listing; its extent is fixed by `0052d47e 83f804` and its base by the SIB displacement `e4d45200` at `0052d486`, both re-read; the arm map is `AI-ORDER-039`'s, quoted) | ● active | [EXP-0127](../experiments/EXP-0127-follow-pair/) |
| AI-FOLLOWGAP-114 | **Both arms enforce a minimum separation of a hard-coded 2, and the step-away target is a straight line away from the escorted unit, clamped to the playable rectangle.** `0052d48a CMP byte ptr [ESP+0x18],0x2` / `JNC` (defend) and `0052d58f CMP AL,0x2` / `JNC` (follow) test the saved Chebyshev distance. Below 2, the escorted unit's cell is packed as an 8.8 fixed-point pair — `EDI = ((Y & 0xff) << 16) + (X & 0xff)` then `0052d4bc SHL EDI,0x8`, so the low word is `X*256` and the high word `Y*256` — and `FUN_0054cc40(self, thatPair, ord+0x70)` is called (`0052d4c2`, `0052d5c4`). That helper reads self's own cell **and sub-cell** (`[pos+0]`,`[pos+1]`,`[pos+4]`,`[pos+5]`, `0054cc52`…`0054cc60`) into the same 8.8 form, takes `dx = selfX − otherX` and `dy = selfY − otherY` (`0054cc8e`/`0054cc90`), **forces a zero delta to 1** (`0054cca6`, `0054ccb3`) so the answer is never the escorted unit's own cell, moves `ord+0x70` cells along the dominant axis *away*, rounds the minor axis onto the line through `FUN_0055458c`, clamps both to `[8, dim−9]` from `[map+0x50000]`/`[map+0x50004]` (`0054cd74`…`0054cda8`) and returns `(Y<<8)\|X`. The caller then writes `ord+0x08 = 1` and `ord+0x0a =` that cell (`0052d4cd`/`0052d4d7`, `0052d5cf`/`0052d5d9`). So an escort orbits between **2** and `ord+0x70` cells of its subject. The clamp is `TERR-SIGHT-116`'s playable rectangle **recomputed inline** rather than read from `world+0x58ee0`, and in 32-bit rather than as bytes, so this site cannot exhibit the edge disagreement that row records. `callto:54cc40` = **4 hits / 4 owners / 0 orphan**: these two, plus `FUN_0052f090` and `FUN_0052f3e0`, the withdraw pair | High (the helper is 60 instructions read whole; the packing is checked at both ends, producer and consumer, and `[map+0x50000]`/`+0x50004` are `TERR-SIGHT-116`'s W and H). **Medium** on the rounding helper `FUN_0055458c` being a plain float→int — it is read as a use, not traced | ● active | [EXP-0127](../experiments/EXP-0127-follow-pair/) |
| AI-FOLLOWRANGE-115 | **`ord+0x70` is the escort range, it is a closed field, and it can never be 0 at the arm — but only the displacement forms are closed, and a save load is not one.** Complete byte-store population, `re:MOV byte ptr \[E.. \+ 0x70\],` = **8 hits / 5 owners / 0 orphan**, of which two are stack slots (`00477615`, `0049b91c`) and **six** are the three assignment sites of `AI-FOLLOWSET-116`, each a pair: the argument (`0052ffcc`, `005350f1`, `005352b1`) or the immediate **3** when the argument is 0 (`0052ffde`, `005350fc`, `005352bc`). Complete byte-read population, `re:MOV .L,byte ptr \[E.. \+ 0x70\]` = **7 hits / 4 owners / 0 orphan**, of which exactly **two** have an order block as their base — `0052d3d5` and `0052d514`, each on the instruction after `MOV EAX,[ESI+0x158]` — the two arms themselves. `docs/INSTRUMENT.md` rule 8 (sweep by the width of the widest store that can *touch* the byte) is discharged rather than assumed: **`disp:6f`, `disp:6e` and `disp:6d` return 0 hits each, whole image**, so no word or dword store reaches `+0x70` from below, and every dword hit at `disp:70` inside `0052a000..0054a000` was read and is a load on another base or a stack slot. Corpus, identical on **both roots**: Defend `1×2, 3×3, 4×1`; Follow `3×4, 5×5, 6×1` — **never 0**, so the 0→3 coercion is authored-unreachable, established positively | High for the two reader instructions and for the six writers (three enumerations plus three zero-returning neighbour sweeps, all on the repaired table, with the width rule discharged in both directions). **Medium** that the field's write population is complete: `AI-PROGRESS-034` establishes that the order block's first `0x94` bytes are serialized with `ar.Read`/`ar.Write`, and a wholesale block move carries no displacement at all, so the **load** path writes this byte invisibly to every sweep above | ● active | [EXP-0127](../experiments/EXP-0127-follow-pair/) |
| AI-FOLLOWSET-116 | **Three sites put an actor into an escort state, they are one shape, and two of them are literally one piece of source emitted twice.** Each walks a group and splits on `member == the named unit`: the named unit itself gets `actor+0x50 = 0xc` — `AI-STATE-011`'s acquire-with-no-leash — with `ord+0x00 =` its own cell and `ord+0x08 = 0`; **every other member** gets the escort state with `ord+0x10 =` the named unit, `ord+0x08 = 0`, and `ord+0x70` per `AI-FOLLOWRANGE-115`. The three: `FUN_0052fdf0` out of line (`RET 0xc`; split `0052fe7c CMP ESI,EAX` / `0052fe7e JNZ 0052ffa5`; `0052ff81` → `0xc`, `0052ffab` → **8**, `0052ffb2` → `ord+0x10`), and inlined in `FUN_00534f80` (`005350aa` → `0xc`, `005350d4` → **8**, `005350dd`) and `FUN_00535140` (`0053526a` → `0xc`, `00535294` → **`0x11`**, `0053529d`; split `005351ce`/`005351d3`). `re:MOV dword ptr \[E.. \+ 0x50\],0x8` = **2 hits / 2 owners / 0 orphan** and `…,0x11` = **1 / 1 / 0**, so those are the complete immediate-form populations. The twin: `[00535080..00535140)` against `[00535240..00535300)`, 192 bytes at a delta of `0x1c0`, differ in **exactly 3 bytes** — `+0x32` and `+0x33`, the two changing halves of the same `CALL 0x00544a00` rel32 (both resolving to `00544a00`), and `+0x57`, the state immediate `08` against `11`. The same shape `AI-CMDSET45-108` found for the order-4 and order-5 setters | High for the shape and the twin (the 192-byte comparison is a PE read printed hit by hit, and the two rel32s are shown resolving to one target). **Medium** that these are the only three: the immediate-form sweep cannot see a state assigned through a register, which is exactly the gap `AI-STATE-011`'s own Medium records and `AI-PROGRESS-034` confirmed is real | ● active | [EXP-0127](../experiments/EXP-0127-follow-pair/) |
| AI-FOLLOWAUTH-117 | **The editor's own two names for these states are *Defend* and *Follow*, both authoring surfaces reach them, and the resolved script node is addressed by parameter TYPE rather than by index.** Action opcode 6's sub-dispatch is on `Par0 − 1`, bound `0053c041 CMP ECX,0x10`: **case 10 (`Par0 = 11`, catalogue "Group Command : Defend") → `0053c23b`**, which writes `grpAI+0x20 = 0` (`0053c286`) and calls `FUN_0052fdf0` per member (`0053c2a7`) → state **8**; **case 14 (`Par0 = 15`, "Group Command : Follow") → `0053c35e`**, which calls `FUN_00535140` (`0053c36c`), whose own `005351a0` writes `grpAI+0x20 = 0` → state **`0x11`**. The player-order road is `AI-CMD-054`'s opcode `0x1b`, `FUN_00534f80`, reached once from `FUN_004d5dd8` at `004d6437` → state **8**. `FUN_0052fdf0` has one further caller, `FUN_004d403c` at `004d499a`, unread here. Both group orders staying **0** is `AI-CMD-033`'s family split seen from the arm side, and it is what lets `FUN_00533ae0` arm 0 reach the per-actor machine at all — the world-wide sweep `FUN_0052cd10` that also calls it is dead code (`AI-DEAD-035`). **Corpus, identical on both roots**, 38 maps with a parsable type-7: **6** Defend nodes and **10** Follow nodes, `AI-GROUPCMD-020`'s census reproduced. The two cases read the *same* three runtime offsets — `node+0x0c` the int, `node+0x30` the unit, `node+0x34` the group (`0053c23b`…`0053c241`, `0053c35e`…`0053c364`) — while the catalogue puts them in different `Par` slots (Defend: unit 1, range 2, group 9; Follow: group 1, unit 2, range 3), and Patrol's two ints land at `+0x0c` and `+0x10`; so the resolved record has typed slots, not indexed ones. Nine of the ten Follow nodes name unit `10001`; the named group holds one member in half of the sixteen nodes and eight at most | High for the two cases, the census and the group-order stores (the arm table is `tools/aipatrol`'s PE read, the census runs on both roots and agrees exactly, and every cited encoding re-reads). **Medium** for the typed-slot reading — it rests on three commands agreeing, which is corpus agreement over the *image*, and no routine that fills the resolved record was read. `Description Instants.ini` exists only on the root that also ships the editor (`TRIG-CAT-026`), so the parameter **names** are one root's; the parameter **types** are in every node's own 796 bytes on both | ● active | [EXP-0127](../experiments/EXP-0127-follow-pair/) |
| AI-FOLLOWHEAL-118 | **A defender carrying a spell book heals the unit it protects before it fights for it, and the spell is a hard-coded id 6.** `0052d456 MOV EAX,[ESI+0x140]` / `0052d462 JZ 0052d46b` picks `FUN_0052d600(self, protected)` over `FUN_0052e4d0`. `FUN_0052d600` selects id **6** (`0052d658 MOV EDI,0x6`) on two disjoint conditions: `self+0xa0 == 0` **and** `protected+0x94 < protected+0x96` (`0052d60a`…`0052d62c`), or `self+0xa0 < self+0x9c + 3` **and** `protected+0x94 < protected+0x96 >> 1` (`0052d630`…`0052d656`) — that is, at any damage when one term is clear and only below half health otherwise. It then looks the record up with `FUN_00500a4a(self+0x140, 6)` (`0052d668`) and casts only if `record+0x0c <= self+0x9a` (`0052d671` / `0052d678 JG`), setting `ord+0x60 = 1` and calling `FUN_0052eea0` (`0052d686`/`0052d69d`) — `AI-STATE-011`'s arm `0xd`. Nothing cast falls straight through to `FUN_0052e4d0` (`0052d6ae`). `callto:52d600` = **1 hit / 1 owner / 0 orphan**, `0052d464`: this wrapper exists for the defend arm and for nothing else. The follow arm never reaches it (`AI-FOLLOW-112`) | High for the control flow and the id (the routine is 47 instructions read whole and every cited encoding re-reads; the two conditions are separated by `0052d618 JNZ 0052d630`, a `rel8`). **Unknown** what spell 6 *is* — the id is taken from the instruction, not resolved against a spell table, and `+0x9a`/`+0x9c`/`+0xa0` are read as cost-side operands rather than identified | ● active | [EXP-0127](../experiments/EXP-0127-follow-pair/) |
| AI-FOLLOWDEATH-119 | **Nothing guards `ord+0x10`: the per-actor machine's only "my subject has died" test is on a different state and a different field.** `FUN_0052ce50`'s prologue is `0052ce5a MOV EAX,[ESI+0x50]` / `0052ce5f CMP EAX,0x3` / `0052ce62 JNZ 0052ced2` — it runs **only** for `actor+0x50 == 3` — and what it then tests is `ord+0x0c` (`0052ce6a`), null-checked at `0052ce6d` and probed for the dead marker at `0052ce71 CMP dword ptr [EAX+0x54],0x10`, on which it halts the actor into `0xc` (`0052cea9`). Arms 8 and `0x11` hold their subject in **`ord+0x10`**, load it at `0052d0f9` and `0052d110`, and pass it to a routine that dereferences it at `0052d3dc` / `0052d51b` with **no null test and no `+0x54` test** anywhere between. This experiment found nothing that clears `ord+0x10` or resets either state when the named unit dies; that absence is a **lower bound**, because `+0x10` is one of the commonest displacements in the image (`re:MOV dword ptr \[E.. \+ 0x10\],E` = 1466 hits / 773 owners / 8 orphan) and the EXP-0125 base-load filter returns **zero** here — the compiler interleaved the load and the store at all three known writers, so the filter that worked on `ord+0x00` cannot be applied to this field at all. **Falsifiable prediction, and only a running original can settle it:** a group placed under script `Follow` or `Defend` whose named unit is then killed keeps the same behaviour aimed at the dead unit, re-issuing order 4 toward it while its object survives. Refuted by that group reverting to any other behaviour without a further script node firing, or by the followers dispersing rather than converging on the death site; a failure at the moment the object is released would establish a *stale* pointer instead, which is a different defect and is distinguishable by whether anything happens at all first | High for the guard's shape and for the absence of a test on the two arms' path (four cited instructions, each re-read, and the gate `0052ce62` is a `rel8`). **Medium** for "nothing clears `ord+0x10`" — that is the lower bound above, and the instrument's failure is stated rather than worked around. **Unknown** what the game does, which is what the prediction is for | ● active | [EXP-0127](../experiments/EXP-0127-follow-pair/) |

| AI-SCRIPTATTACK-120 | **Trigger group subcommand 10 orders one named member to acquire and every other member to attack it.** The dispatcher clears `grpAI+0x20`, walks the referenced group, and calls `FUN_0052f6d0(member, namedUnit)`. That helper first performs the common route/reservation cleanup. For `member == namedUnit` it sets `actor+0x50 = 0xc`, `ord+0x00 = own cell`, and `ord+0x08 = 0`: acquire-with-no-leash. For every other member it sets `actor+0x50 = 3`, `ord+0x0c = namedUnit`, `ord+0x14 = actor+0x12c`, and `ord+0x08 = 0`: engage the named target at the member's current reach. It does not resolve a hostile candidate or copy the target's owner relation. Both campaigns author five nodes and reference three. *[EXP-0170]: the dispatcher was **not** read end to end and the headline is false for one class of member. Between the group-order store and the per-member call the arm walks the group a second time and calls `FUN_00535d30(member, namedUnit)`, comparing the result with `0xffffff` (`0053c1ef`, `0053c1f4`) — `AI-COST-071`'s target cost, whose only path to that value is a **0** cell of `AI-PREF-070`'s preference matrix. On the sentinel the arm skips `FUN_0052f6d0` entirely and calls `FUN_0052fd40` (`0053c21e`): the vetoed member acquires **in place** instead of attacking the named unit. `AI-CMD-054` had already read the identical gate on the player's order `0x19`; it was not carried across to this arm. The row's other statements reproduce exactly, and the veto is authored-unreachable in the shipped campaign (`TRIG-GRPARM-047`).* | High (dispatcher and helper read end to end; the self/non-self branch gives two distinct, already classified order shapes and excludes a group-wide acquire) — *amended by [EXP-0170]: the per-member gate was missed, so the High covered a routine that had not in fact been read whole* | ● active (amended) | [EXP-0155](../experiments/EXP-0155-trigger-closure/) |
| AI-MINIMAP-156 | **`FUN_0048f470`, the minimap's own paint routine (`TOWN-091`, `AI-MINIMAP-062`), is read here to its own terminator, `RET` at `0048fb7f`, and it does call the `vt+0x18`/`vt+0x20`/`vt+0x24` external-object blit primitives `TOWN-091` states it does not use.** `TOWN-091` reads roughly the first 210 of the routine's 623 disassembled lines and reports that the routine performs "a masked 2x2-block pixel copy/blend loop… not a discrete-icon blit through the `vt+0x18`/`vt+0x34` primitives every other widget in this experiment's paint routine calls." Within those same first ~210 lines the routine in fact makes three calls on the resource object at global `0x005ef8dc`: `0048f5e0 CALL [EAX+0x18]`, positioned from `this+0x64`/`this+0x68`; `0048f637 CALL [EDX+0x18]`, on a second object reached through `this+0x60` or `this+0x64` chosen by a null test on `campaign+0xdc` (`0048f5e6`-`0048f5f1`); and, past line 210, `0048f664 CALL [EDX+0x20]` and `0048f6df CALL [EDX+0x24]` on the same `0x005ef8dc` object. The masked pixel loop `TOWN-091` describes runs afterward, reading a raw buffer at `[0x005e43bc]` with stride `[0x005e43a4]`, and is additional to the blit calls, not a replacement for them. Past line 210: at `0048f93f`-`0048f9dc` the routine walks a count-and-pointer pair on the widget itself, `[this+0x9c0]` entries at `[this+0x9bc]`, and for the first entry whose own first dword is non-null it calls `entry->vt+0x34` with a destination point built from `this+0x60`/`this+0x64`/`this+0x68` and the caller-supplied scale — the same external-object `vt+0x34` sub-rect blit convention, on an array this experiment did not identify beyond "count and pointer fields on the minimap widget." At `0048f9e6` the routine forks on a local flag into one of two structurally symmetric closing arms, `0048f9f0`-`0048fab5` and `0048fab6`-`0048fb7f`; each computes a rectangle from `this+0x60`/`0x64`/`0x68` and calls `FUN_0044fef0` once, then `FUN_0044c410`, then returns. Neither arm performs any further blit. | High for every cited instruction and control-flow edge (the routine reaches its own `RET`, 623 lines, and every branch target is walked). The identity of the `this+0x9bc` array (candidate: per-unit map icons) is Unknown; the loop's mechanism and blit convention are established, its semantic content is not | ● active | [EXP-0211](../experiments/EXP-0211-column-controls/) |
| AI-MINIMAP-157 | **Widget 5 of the right-column container (`TOWN-086`, `TOWN-091`) and the minimap object of `AI-MINIMAP-062`/`AI-MINIMAP-124`/`SESS-VIEW-031` are the same object.** `TOWN-091` cites `FUN_0048f470` as widget 5's own paint routine, matching the class's `vt+0x2c` slot by the project's own construction/paint convention; `AI-MINIMAP-062` cites `FUN_0048fdb0` at "slot `0059a6cc`" as the minimap's left-button-down handler. A fresh 30-slot dump of the vtable at `0x0059a678` (`tools/figurecontrols -mode dwords -n 30`) reads `[0x2c] -> 0048f470` and `[0x54] -> 0048fdb0` (absolute `0x0059a6cc`) in the same table, and the same dump's `[0x4c] -> 00490100` and `[0x60] -> 0048fff0` reproduce the mousemove and right-down handlers this experiment separately read to their own terminators. `TOWN-091` names widget 5's functional identity an open question; this closes it. | High (one vtable, both cited routines present at the cited offsets, re-dumped fresh; a shared vtable base is the strongest identity proof available to static reading, short of tracing the single constructor call in `FUN_00472070` argument by argument, which this experiment also did not need to do because the two paint/handler citations already pin the same table) | ● active | [EXP-0211](../experiments/EXP-0211-column-controls/) |
| AI-PANEL-158 | **The command panel's own mouse-hit-test routine, `FUN_00490500`, is a distinct routine from `FUN_0041b439`, the order-arming routine `AI-PANEL-053` calls "the command-panel handler."** A fresh 24-slot dump of the command panel's vtable at `0x0059a6f0` reads `[0x54] -> 00490500` (matching the physical `WM_LBUTTONDOWN` slot convention) and `[0x2c] -> 00490710`, the panel's own paint routine. A search for raw-dword references to `0x0041b439` finds **zero** — it is never installed as any vtable slot — against **14** direct `CALL rel32` sites in `.text`. `tools/figurecontrols`'s section-scoped raw-dword scan covers 1,904,808 of the image's 1,977,344 bytes (the 5 declared PE sections, capped at each section's own `VirtualSize`); a second, whole-file scan (`-mode rawfull`) covers all 1,977,344 bytes directly, including the PE header, inter-section alignment padding, and the appended CodeView/debug directory outside every section (72,536 bytes together), and also finds zero occurrences, on both roots. `FUN_00490500`, read to both its `RET 0xc` returns, hit-tests all 8 cells through the global function pointer at `0x00632f6c`, gates cell 4 on `FUN_0041ddea` (`AI-PANEL-053`'s existing shared predicate), and on a hit posts message `0x40c` with the cell index (or `-1` to clear a stale selection first) to `[this+0x5c]->vt+0x48`. It does not call `FUN_0041b439` directly, and no call site among the 14 was traced back to a `0x40c` handler in this experiment; the path connecting the panel's own click-to-cell translation to `AI-PANEL-053`'s order-arming routine remains unestablished. | High for the two routines' distinctness and for `FUN_00490500`'s own read (both terminators reached, all cited addresses re-measured). Unknown for the `0x40c`-to-`FUN_0041b439` call path | ● active | [EXP-0211](../experiments/EXP-0211-column-controls/) |
| AI-CURSOR-172 | **Setting the current cursor is one path through one manager object, and that object owns a `timeGetTime`-paced frame counter with a modulo wrap that never lets a reader see a frame at or above the registered count.** The manager is the global at `0x005cd790`; `0x005cd794` (`[current-cursor]`, `AI-CLICK-050`/`AI-MINIMAP-062`) is its own `+0x04`. Every path that changes it goes through `FUN_0046d3d0(cursorObj)`, which unpacks `cursorObj+0x4/0x8/0xc/0x10/0x14` and thiscalls `FUN_0042a300(mgr, ...)`. `FUN_0042a300` calls `FUN_0042a410(mgr)` (decrements a manager-owned refcount at `mgr+0x20`), releases the manager's own two prior sub-objects at `mgr+0x8`/`mgr+0xc` through each one's `vt+0x4`, copies the five incoming fields to `mgr+0x4`/`mgr+0x18`/`mgr+0x1c`/`mgr+0x28`/`mgr+0x2c` (the fourth and fifth being the incoming object's own `+0x10` frame-count and `+0x14` period arguments — the same two `SPR16A-CURSOR-046` already publishes per cursor), unconditionally zeroes `mgr+0x24`/`mgr+0x30`/`mgr+0x34`, allocates two fresh sub-objects into `mgr+0x8`/`mgr+0xc`, and calls `FUN_0042a430(mgr)`. `FUN_0042a430` increments the refcount and, only when it was `0` on entry and `mgr+0x4` is non-zero, first calls `FUN_0042a450(mgr)`: this calls WINMM.dll `timeGetTime` (IAT `0x00632fa4`, ~~the sole caller of that import in `.text`~~ *one of 85 direct call sites of that import in `.text`, `AI-CURSOR-224`*), computes `elapsed = now - mgr+0x30`, and when `elapsed > mgr+0x2c` (the period) increments `mgr+0x24` (the frame index) and stores `now` to `mgr+0x30`; if the incremented index is not less than `mgr+0x28` (the frame count) it is reset to `0` in the same call, before any external reader can observe it. `FUN_0042a450` has exactly one caller in the whole `.text` section (`0042a441`, inside `FUN_0042a430`; `go run ./tools/cursorlife -callers 42a450`). Because `FUN_0042a300` resets `mgr+0x24`/`mgr+0x30` to `0` on every call before `FUN_0042a430` runs, a single "set cursor" call can only be observed to leave the frame index at `0` or `1` (`0` again if the frame count is `1`); reaching a higher index needs `FUN_0042a430` to run again later **without** an intervening `FUN_0042a300` reset. `FUN_0042a430` has **12** callers in `.text` (`go run ./tools/cursorlife -callers 42a430`); only one (`0042a3f0`) is inside `FUN_0042a300`. The other 11 (`0042a678`, `0042a91b`, `0042b129`, `0042b151`, `0042b4f9`, `0042b8ea`, `004381fd`, `00471dbe`, `0047a294`, `0047a4a5`, `0047a5b8`) were not traced in this experiment. `FUN_0042a410`'s own decrement-to-zero arm (`0042a417`-`0042a420`) tail-jumps to `0x0042a510`, a distinct, untraced routine, rather than to `FUN_0042a450` | High for the mechanism as traced — the `timeGetTime` source, the elapsed/period compare, the increment-then-wrap and the field mapping are single instructions read end to end and cross-checked against a Ghidra disassembly of `FUN_0046d3d0`/`FUN_0042a300` (`evidence/disasm-excerpts.md`). This excludes the live alternative that cursor animation is driven by a Windows timer message: `SESS-TIMER-022` already establishes, from a full PE import-directory walk, that `SetTimer`/`KillTimer`/`timeSetEvent`/`timeKillEvent` are absent from the whole image and that `timeGetTime` is the only clock imported; this experiment adds that ~~`timeGetTime`'s one call site is inside this exact chain~~ *one of `timeGetTime`'s 85 call sites is inside this exact chain, `0042a455`; the uniqueness is withdrawn by `AI-CURSOR-224` and the exclusion of a timer-driven animation rests on `SESS-TIMER-022` alone*. **Medium** for whether the mechanism is ever observed to animate past frame index 1 in play: that depends on whether any of the 11 untraced callers of `FUN_0042a430` runs on a cadence that does not immediately follow a `FUN_0042a300` reset, which this experiment did not check. This answers `SPR16A-CURSOR-046`'s open question — "no code evidence establishes whether the cursor animator visits frames beyond its registered count" — for the negative half only: code exists that would advance a frame, and it never stores a value at or above the registered count | ● active (amended) | [EXP-0213](../experiments/EXP-0213-cursor-lifecycle/), [EXP-0217](../experiments/EXP-0217-cursor-composition-order/) |
| AI-CURSOR-173 | **Exactly 5 call sites in the whole `.text` section reach Win32 `SetCursor`, `LoadCursorA` has none, and none of the 5 touches the cursor-manager object or any of its 29 constructed cursors.** Instrument: `callSitesToIAT` (`tools/cursorlife`) scans `.text` for direct `FF15 <iat>` and `E8`-to-thunk forms against IAT slots `0x00632f44` (`SetCursor`) and `0x00632e0c` (`LoadCursorA`); this is a complete population sweep of one call form pair against two fixed addresses, reproducible with `go run ./tools/cursorlife`. Result: `SetCursor` 5 direct, 0 via thunk; `LoadCursorA` 0 direct, 0 via thunk. The 5: `00470cad`, inside `FUN_004709e0`, a large `SUB ESP,0x630` routine that opens a registry key (`RegOpenKeyExA` on `HKEY_LOCAL_MACHINE`, `00470a18`) before calling `SetCursor(NULL)` — a settings/display-mode probe with 0 direct `E8` callers in `.text`, so its own caller was not established. `00472b42`, in code this experiment's Ghidra pass could not attribute to a function boundary (orphan), not further investigated. `0058036c`/`00580394`, both inside one routine, `FUN_00580347` (`RET 0x4`, one stack argument): it adds the argument to a nesting counter at `this+0xa0` and, entering (`this+0xa0>0`), pushes the fixed global `[0x0063069c]` and calls `SetCursor`, saving the returned previous handle to `this+0xa4` on the `0->1` transition; leaving (`this+0xa0<=0`), it restores the saved handle from `this+0xa4` and calls `SetCursor` again. This is the push/pop shape of a busy-cursor nesting counter; its own callers were not traced. `005818be`, inside `FUN_005818aa` (`RET 0xc`, three stack arguments): it calls `0x00577cfb` for an object, and when that object's `+0x50` is non-zero pushes the fixed global `[0x006306a4]` and calls `SetCursor`, returning `1`; otherwise it forwards to an unread routine, `0x0057638c`, and returns its result. Neither `0x0063069c` nor `0x006306a4` lies inside the 28-slot cursor registry (`0x005ef9b4`-`0x005efa20`) or equals the manager (`0x005cd790`) or `[current-cursor]` (`0x005cd794`) | High for the enumeration and the non-overlap with the manager's own state (a complete sweep of two fixed IAT slots against a fixed set of known cursor-object addresses; the alternative excluded is that some other, un-enumerated call form reaches `SetCursor` — the sweep covers both the direct and the thunked call forms, which is the complete set this repository's instruments recognise). **Medium** for identifying `FUN_00580347`/`FUN_005818aa` as MFC's busy-cursor/`OnSetCursor` idiom: this is inferred from the stack-cleanup size (`RET 0x4`, `RET 0xc`) and the push/pop or gate-then-fallthrough shape, not from a resolved symbol, vtable slot, or message-map entry | ● active | [EXP-0213](../experiments/EXP-0213-cursor-lifecycle/) |
| AI-CURSOR-174 | **No named Win32 API delivers the manager's own cursor set to the screen: `SetCursor` never receives one of its objects, and `SetClassLongA`/`SetClassLong` is not imported at all.** `AI-CURSOR-173` shows the 5 `SetCursor` call sites reach two fixed busy-cursor globals and a `NULL`, never the manager (`0x005cd790`), `[current-cursor]` (`0x005cd794`), or a registry slot. A walk of the complete PE import directory (`go run ./tools/panelmodal -mode imports`) finds no `SetClassLong`/`SetClassLongA`/`SetClassLongPtrA` entry from any DLL, so no code path in this image can install a cursor into the window class either. Between them these are the two ways a Win32 program ordinarily hands a custom `HCURSOR` to the desktop. **This is a falsifiable prediction, not yet checked against the running original**: if the manager's own cursor set never reaches the OS cursor by either mechanism, the system mouse cursor should stay whatever the desktop default is (or invisible, given the `SetCursor(NULL)` at `00470cad`) while hovering the mission map, and the visible in-game cursor art must instead be the game's own sprite, drawn at the mouse position by its normal render loop. A refuting observation: a screen capture or a second monitor showing the true Windows cursor icon change to match the game's own art while the game window has focus | High for the absence half (`SetCursor` call-site enumeration in `AI-CURSOR-173`; the import-table walk for `SetClassLong*` is exhaustive over the whole import directory, the same instrument `SESS-TIMER-022` uses for its own absence claim). This excludes the live alternative that some other `SetCursor`/`SetClassLong` call site was missed by construction, not by search depth — the import walk cannot miss a name for want of a containing function, and the `SetCursor` sweep covers both direct and thunked call forms. **Unknown** for the positive claim (a self-drawn sprite cursor): this experiment did not locate the render-loop blit that would confirm it, and only the running original can settle it | ● active | [EXP-0213](../experiments/EXP-0213-cursor-lifecycle/) |
| AI-CURSOR-175 | **Classifying all 69 callers of the set-cursor adapter by the nearest preceding load of one of the 28 registry slots gives a concrete surface for five of `AI-PANEL-053`/`AI-CURSOR-126`'s unnamed cursors.** Instrument: `slotLoadSites`/`classifySetCursorCalls` (`tools/cursorlife`) finds every `MOV r32,[slot]` (forms `A1`/`8B05`) against the 28 registry addresses across `.text`, then pairs each of the adapter's 69 callers (`callSites` against `0x0046d3d0`) with the nearest such load within 64 bytes back. Result, reproducible with `go run ./tools/cursorlife`: `default` 36 sites (scattered widely — the largest single group); `wait` 12 sites, all in `0x00476564`-`0x00479f71`, each interleaved with a `default` site immediately after it (a set-wait/set-default pattern); `select` 9 sites; `cantput` 4 sites, all in `0x004a4885`-`0x004a5882`; `dice` 1 site (`0042fcce`); `backpack` 1 site (`0040dcb4`); `sdefault` 1 site (`0048fd74`), 60 bytes before the minimap dispatcher's own established address range `0x0048fdb0`-`0x0048ffee` (`AI-MINIMAP-062`/`AI-MINIMAP-124`); 5 sites (`004a35fc`, `004a4868`, `004a5838`, `004a6062`, `004ae585`) have no static slot-load within the window, clustered among the `cantput` sites, and are candidates for a dynamically-built cursor object such as the held-item drag cursor (`AI-CURSOR-176`) rather than a registered slot. This gives `sdefault` (small-default) exactly the "surface of its own rather than a per-hover test" shape the brief's own hypothesis named, and gives `wait`, `cantput`, `dice` and `backpack` concrete call-site populations for the first time | Medium. The instrument is a proximity heuristic, not a verified data-flow trace: it does not confirm that the matched slot-load's own register is what reaches the `CALL 0x0046d3d0`, so a load that happens to sit within 64 bytes of an unrelated call site would misclassify, and a genuine slot-load more than 64 bytes back, or reached through a form the scan does not recognise (e.g. a computed or indirect load), would misreport as "dynamic." The counts and addresses are exact and reproducible; the surface each cluster belongs to (which UI/game state actually triggers it) was not read from the containing functions and remains an inference from address locality and slot name alone | ● active | [EXP-0213](../experiments/EXP-0213-cursor-lifecycle/) |
| AI-CURSOR-176 | **A 29th cursor is registered at runtime, through the same constructor as the 28 static ones, and its storage field is one of the four the held-item state occupies on the session object.** *(This headline was amended 2026-08-22 with the body below: as published it said the storage field matches `TOWN-348`'s "cursor-held item" by name, which the correction makes false -- the cursor is at `+0x3d8` and `TOWN-348`'s held item is at `+0x3cc`.)* The registry walk (`SPR16A-CURSOR-046`'s `FUN_0046d3f0`, `0x0046d3f0`-`0x0046dac0`) uses one shared constructor, `FUN_0046d280`, 28 times. A whole-`.text` search for other callers of that same constructor (`callSites` against each distinct callee found in the registry) finds exactly one more call site, `00476473`, inside `FUN_00476430`, outside the registry routine's own address range. Unlike the 28 static registrations, its art path is a computed value rather than a `68 <addr>` literal string push, and its hotspot/period arguments differ from the registry's own values. `FUN_00476430` stores the constructed cursor object into `[this+0x3d8]`, and `[this+0x3cc]` receives the routine's incoming argument 1. **Amended 2026-08-22 by `AI-CURSOR-192` ([EXP-0214](../experiments/EXP-0214-cursor-surfaces/)): as published this sentence named `[this+0x3cc]` as the cursor object's storage field, which is wrong.** The convergence with `TOWN-348` survives the correction and is unchanged in substance -- `[this+0x3cc]` still holds the cursor-held item, it is simply written from an argument rather than from the constructor. `TOWN-350` names `sess+0x3d8` in the same clear set as `sess+0x3cc`. `TOWN-348` independently names `[sess+0x3cc]` "the cursor-held item," reached from a completely different routine (`FUN_00491330`, the character figure widget's item-drop hit test, `EXP-0210`) that tests it before any of the widget's own rectangles. The two decode paths — this experiment's cursor-registration trace and `TOWN-348`'s figure-widget rect trace — converge on the identical field offset and name without either citing the other's evidence | High. The live alternative excluded is that `[this+0x3cc]`/`[sess+0x3cc]` is a coincidental shared offset on two unrelated objects (the false-positive risk `docs/INSTRUMENT.md` names for a common displacement): it is excluded here because the offset was reached twice, by two structurally unrelated routines read in two separate experiments, and both readings independently name the same role (an item being carried by the cursor) rather than being asserted from the offset alone | ● active (amended) | [EXP-0213](../experiments/EXP-0213-cursor-lifecycle/) |
| AI-CURSOR-177 | **The map view answers a private dispatcher of 132 message ids, `0x401`-`0x484`, and message `0x405` — already known as the right-up cancel (`MENU-COMBAT-019`) — arms `FUN_00418a29`, which the corpus had not named.** The dispatcher (prologue `0x0040ec30`) subtracts `0x401` from the message id, range-checks it against `0x83`, indexes a byte table at `0x0040eeb8`, and jumps through a dword table at `0x0040ee94`; decoded whole with `go run ./tools/cursorlife -msgtable 40eeb8:40ee94:132:401`, reproducible against both roots (`evidence/msgtable.txt`). Of the 132 slots, 124 (ids `0x403`-`0x404`, `0x407`-`0x483` except `0x40c`, `0x40e`, `0x40f`) share one default arm (`0x0040ee8b`); `0x401 -> 0x0040ecb9`, which opens `MOV ECX,[EBP-0x34]` then `CALL 0x0040dcdd` (`FUN_0040dcdd`'s own entry, confirmed by its own `PUSH EBP; MOV EBP,ESP` + SEH prologue at that address), `0x402 -> 0x0040ecde`, `0x405 -> 0x0040eceb` (`FUN_00418a29` by literal jump-table value), `0x406 -> 0x0040ecff`, `0x40c -> 0x0040ed1b`, `0x40e -> 0x0040ed5d`, `0x40f -> 0x0040edd9`, `0x484 -> 0x0040ee52` are the remaining distinct arms. `FUN_00418a29` unconditionally clears `view+0x99c` (the armed-mode field, `AI-PANEL-053`) when it is non-zero and, only when the cleared value was specifically `5` (Cast), first calls two further routines (`FUN_0041ddea`, `FUN_0041dfa6`) before the clear — extending `MENU-COMBAT-019`'s "right up cancels through map message `0x405`" with the clear's own target field and the Cast-specific extra step | High for the dispatcher's own structure and the `0x405 -> FUN_00418a29` identity: both are a direct read of the index and jump tables, reproducible byte for byte on both roots, and this excludes the live alternative that `MENU-COMBAT-019`'s "map message `0x405`" names a different arm — the jump table has one entry for `0x405` and it is this one. **Unknown** for the semantic trigger of message `0x401`, the dispatcher's own first slot and `FUN_0040dcdd`'s arm: no code path posting it was traced in this experiment | ● active | [EXP-0213](../experiments/EXP-0213-cursor-lifecycle/) |
| AI-CURSOR-178 | **Beyond the two sites the corpus already names — `AI-PANEL-053`'s consuming-click clear and `AI-MINIMAP-062`'s minimap clear — `view+0x99c` (the armed-mode field) is also cleared unconditionally by the selection routine and by three "immediate order" panel buttons that never arm it, and conditionally by message `0x401`'s arm.** A `disp:0x99c` sweep across `.text` (`dispAccesses`, `tools/cursorlife`) finds every `MOV`/`CMP` form touching displacement `0x99c` off any base register. Beyond the two already-published sites: `0041a95e`, inside the selection routine `FUN_0041a2d5`, unconditional. `0041b551`/`0041b58b`/`0041b5c2`, inside the command-panel handler `FUN_0041b439` — the tails of the Guard/Stand Ground/Retreat "issue order immediately" arms `AI-PANEL-053` already names, each additionally clearing `view+0x99c` and notifying message `0x40b` right after its order call, even though none of these three buttons ever arms the field in the first place. `0040dfd3`, inside `FUN_0040dcdd` (`AI-CURSOR-177`'s message-`0x401` arm), conditional on a found-flag set by walking three spatial collections at `view+0x9b8`/`view+0x9c4`/`view+0x9d4`/`view+0x9e0` (the latter three offsets not cited elsewhere in this ledger), followed by the same notify-`0x40b` step. A second, address-distinct population of `0x99c` hits exists at `0x00492f7e`-`0x0049a2ff`, inside the town/shop code range this ledger's `AI-MINIMAP-156` and neighbours already work in; this experiment did **not** determine whether that population reads the same `view` object or a different struct that coincidentally shares the offset, and excludes it from the count above | Medium. The `disp:` sweep is a complete scan of one instruction-form family (`docs/INSTRUMENT.md`'s enumeration checklist), so the mission-view sites listed are a real, reproducible population — but the sweep by construction cannot distinguish two different base objects sharing the same field offset, which is exactly the open question for the second population it also found. Because that question is unresolved, this row does not claim the enumeration is complete over "every clear of the armed mode," only over the mission-view sites this experiment could attribute | ● active | [EXP-0213](../experiments/EXP-0213-cursor-lifecycle/) |
| AI-CURSOR-188 | **All 28 cursor registry slots have a consumer reference, and none is reached by either of the two addressing forms a `disp32` sweep cannot see.** A whole-image little-endian dword scan for values in `[0x005ef9b4, 0x005efa24)` returns **212 references**, all 212 in `.text` and **0** in `.data`, `.rdata` or any other section. **0** are SIB-indexed and **0** are unclassified; every one is a `disp32`-only load or store. 28 are in the registry constructor `FUN_0046d3f0` and 28 in the destructor `FUN_0046dac0`, one per slot, leaving 156 consumer references, with at least one for each of the 28 slots. Slot `n` is the dword at `0x005ef9b4 + 4n`, and joining those addresses against `EXP-0190`'s `cursor-construction.tsv` in construction order gives the slot-to-name map published as `SPR16A-CURSOR-067`. This scan is the instrument `docs/INSTRUMENT.md` rule 7 requires for a reachability question: it reads bytes, not instructions, so a pointer stored in a data table would have been found. **The residual blind spot, which this scan does not close:** a slot address computed at runtime rather than encoded in an instruction leaves no dword to find, so this is an absence of the two encoded forms, not an absence of every possible reach | High. The live alternative excluded is the one this experiment's own author predicted, that `AI-CURSOR-175`'s 21 unattributed slots are reached through a SIB-indexed table read (`8B 04 8D <disp32>`) or through a pointer stored in data, neither of which a displacement sweep can see. A byte-level scan of every section cannot miss either form, and it returned 0 of both. `AI-CURSOR-189` names the actual cause | ● active | [EXP-0214](../experiments/EXP-0214-cursor-surfaces/) |
| AI-CURSOR-189 | **`AI-CURSOR-175`'s 21 unattributed slots are an artefact of a store-to-local idiom, not of an unread code path.** Both routines that select among many slots compute the chosen cursor into a stack local, branch to a common tail, and call the set-cursor adapter **once** from that tail. `FUN_0040d1f1` holds 49 of the 212 registry references (`AI-CURSOR-188`) and contains exactly one adapter call, at `0x0040dcb4`; the nearest preceding slot load is `0x0040dc8c` (`MOV EAX,[0x5efa20]`, slot 27 `backpack`), 40 bytes back, so a 64-byte proximity window attributes that single call to `backpack` and nothing at all to the other 48 slot references in the same routine. `FUN_0048fb80` behaves identically: its adapter call at `0x0048fd74` has `0x0048fd4b` (`MOV ECX,[0x5ef9f8]`, slot 17 `sdefault`) as nearest preceding load, which is why `sdefault` is the one small cursor `AI-CURSOR-175` did attribute. `AI-CURSOR-175`'s 7 attributions are correct and are exactly the routines that load a slot immediately before calling; its stated blind spot is confirmed and its cause is named here | High. Both routines were decoded at instruction level from a verified prologue, in fragments covering the blocks named here rather than end to end, and the byte distance from each adapter call back to the nearest slot load is arithmetic on addresses in the committed listings. The live alternative excluded is that the unattributed slots are set somewhere `AI-CURSOR-175` did not look: `AI-CURSOR-188` enumerates every reference in the image, and all 49 of `FUN_0040d1f1`'s lie inside that one routine | ● active | [EXP-0214](../experiments/EXP-0214-cursor-surfaces/) |
| AI-CURSOR-190 | **The eight edge arrows are selected by a screen-position test inside the mission-map hover routine, and the arrow numbering is a clockwise compass starting at north.** All eight arrow slots (9..16, `0x005ef9d8`..`0x005ef9f4`) are referenced exactly once each **inside `FUN_0040d1f1`**, in the block `0x0040d318`..`0x0040d43e`. **They are referenced elsewhere as well**: the whole-image scan gives 23 arrow consumer references over five routines -- `0040d1f1` (8), `0048fb80` (4), `00490860` (3), `0049223c` (4), `004928a0` (4) -- so this row describes one routine's block and not the whole population. **`0049223c` is corrected to `0x00492180` by `AI-CURSOR-205` ([EXP-0216](../experiments/EXP-0216-mission-cursor-selection/)): it names an address inside that routine's body, not a routine of its own, and the count of 4 is unaffected.** The block tests only four globals: `X = [0x5cd7a0]`, `Y = [0x5cd7a4]`, `W = [0x5ea208]`, `H = [0x5ea20c]`. Read whole: `X == 0` selects `arrow7` when `Y == 0` (`0040d342`), `arrow5` when `Y >= H-2` (`0040d369`), else `arrow6` (`0040d373`); `X >= W-2` selects `arrow1` when `Y == 0` (`0040d3b2`), `arrow3` when `Y >= H-2` (`0040d3d9`), else `arrow2` (`0040d3e4`); otherwise `arrow0` when `Y == 0` (`0040d406`), `arrow4` when `Y >= H-2` (`0040d430`), else the routine falls through to its normal hover path at `0x0040d43e`. Each of the eight stores into `[EBP-0x28]` and jumps to `0x0040dabb` in the common tail. The direction map is arrow0 north, arrow1 north-east, arrow2 east, arrow3 south-east, arrow4 south, arrow5 south-west, arrow6 west, arrow7 north-west. The right and bottom bands are 2 pixels deep (`SUB EDX,0x2` before each `CMP`); the top and left tests are equality with 0 | High. Every branch in the block is a named instruction read from a verified instruction boundary, and the eight loads are the complete population **within this routine**. The direction map is corroborated twice from outside this block: `0048fb80` and `00490860` run the same `[0x5cd7a4]` against 0 and `[0x5ea20c]-2` test and pick `arrow1`/`arrow3`/`arrow2` for the top, bottom and middle of a right-hand column; and `EXP-0190`'s `cursor-construction.tsv` gives the eight registered hotspots as a compass rose -- `arrow0` (15,5) top-centre, `arrow1` (22,8) top-right, `arrow2` (25,15) right, `arrow3` (23,22) bottom-right, `arrow4` (16,25) bottom, `arrow5` (8,23) bottom-left, `arrow6` (6,16) left, `arrow7` (8,9) top-left. That is a measurement in the shipped art, taken before this experiment and independent of any code reading, so the compass is not an interpretation laid over the branch tree. **This refutes `EXP-0213`'s H3**, the live alternative that the edge arrows have a surface of their own rather than a per-hover test: they are selected on the same surface, in the same routine, and through the same store-to-local path as every other selection `FUN_0040d1f1` makes | ● active | [EXP-0214](../experiments/EXP-0214-cursor-surfaces/) |
| AI-CURSOR-191 | **The six small cursors are selected in `FUN_0048fb80`, by the mission view's own armed-mode field, through an eight-entry jump table.** `sdefault` (slot 17) and the five `.256` cursors (slots 18..22) have twelve consumer references, split 6/6 between two adjacent routines. `FUN_0048fb80` runs from `0x0048fb80` to its `RET` at `0x0048fd80`, followed by the jump table and twelve `0x90` padding bytes; **a different routine begins at `0x0048fdb0`** (`SUB ESP,0x1c; PUSH EBX; PUSH EBP; PUSH ESI; MOV ESI,ECX; PUSH EDI; MOV EDI,[ESI+0x5c]`), and that one is the minimap dispatcher `AI-MINIMAP-062` and `AI-CURSOR-175` already name. Its six references (`0048fed2`, `0048ff19`, `0048ff34`, `0048ff68`, `0048ff8f`, `0048ffa7`) **compare** each slot's `+0x4` against `[0x5cd794]` to dispatch an order; they select nothing. The selection is `FUN_0048fb80`'s alone. That routine loads the mission view at `0x0048fc5c` (`MOV EBP,[ESI+0x5c]`, `ESI` being `this` from `MOV ESI,ECX` at `0x0048fb87`) and then uses `EBP` as an object base, not as a frame pointer, so `[EBP+0x99c]` is the same armed-mode field `AI-CURSOR-178` names on the mission view. The selection: `[EBP+0x140] == 0` gives `sdefault` outright (`0048fcfd`); otherwise mode 0 gives `smove` and modes 1..8 index the jump table at `0x0048fd84`, whose eight dwords are `0048fd34, 0048fd42, 0048fd42, 0048fd3c, 0048fd2c, 0048fd34, 0048fd42, 0048fd24`, mapping mode 1..8 to `sattack, smove, smove, sdefend, scast, sattack, smove, spatrol`; any mode above 8 gives `smove` (`CMP EAX,0x7 ; JA`). `TEST [EBP+0x144],0x24` then overrides the result with `sdefault` when either bit is set (`0048fd42`). Four further branches reach `sdefault` from the block `0x0048fcdd`..`0x0048fcfb`, which this experiment did not decode. Finally `[sess+0x3cc] != 0` overrides everything with `[sess+0x3d8]`, the held-item cursor (`0048fd5f`, `AI-CURSOR-192`). **Decoded by `AI-CURSOR-208` ([EXP-0216](../experiments/EXP-0216-mission-cursor-selection/)): the four branches are a zoom-scaled rectangle test against a widget read from `view+0x80`/`+0x84`/`+0x88`, the widget itself not identified. `AI-CURSOR-207` ([EXP-0216]) further finds that `FUN_0048fb80` selects among four edge-arrow cursors in a block before this one, at `0x0048fbac`-`0x0048fc72`.** | High for the selection tree and the jump table, which are named instructions and eight dwords read from the image. High for the routine boundary at `0x0048fdb0`, which is fixed by a prologue, a `RET` and twelve padding bytes in the committed hex dump, and which `AI-CURSOR-175` and `AI-MINIMAP-062` both already published -- **the first draft of this row called `FUN_0048fb80` the minimap dispatcher, which those two rows contradict**. The live alternative excluded for the selection scope is that some other routine also selects a small cursor: the twelve references are the complete population (`AI-CURSOR-188`) and the six outside `FUN_0048fb80` were read and are comparisons. **This refutes the `sdefault` half of `EXP-0213`'s H3**: `sdefault` is not selected by a surface of its own but by an armed-mode test. **Unknown** for the meaning of `view+0x140` and of the two bits `0x24` in `view+0x144`, which are read here but not traced to a writer. **Resolved by `AI-CURSOR-202` and `AI-CURSOR-203` ([EXP-0216](../experiments/EXP-0216-mission-cursor-selection/)): `view+0x140` is the selection-loop's own object count, and the `0x24` mask is the same one `FUN_004190f0` tests on itself to blank the command panel.** | ● active | [EXP-0214](../experiments/EXP-0214-cursor-surfaces/) |
| AI-CURSOR-192 | **The runtime-constructed held-item cursor is stored in `[sess+0x3d8]`, not `[sess+0x3cc]`. This corrects `AI-CURSOR-176`.** `FUN_00476430` is a thiscall with four arguments, pinned by its own epilogue constant `RET 0x10` at `0x004764af` and by the 20 bytes it pushes after the SEH prologue, so at `0x0047647c` argument `n` sits at `[ESP+0x14+4n]`. It passes argument 3 as the art path to the shared cursor constructor `FUN_0046d280` with hotspot `(0x28, 0x28)` and period `0x3b9aca00` (`0x00476463`..`0x00476473`), then writes the constructor's return value to `[ESI+0x3d8]` (`0x00476484`), argument 1 to `[ESI+0x3cc]` (`0x0047648e`), argument 2 to `[ESI+0x3d0]` (`0x00476498`) and argument 4 to `[ESI+0x3d4]` (`0x0047649e`). `TOWN-350` reads the same four fields from the opposite direction: `FUN_004922f0` writes the item pointer to `sess+0x3cc`, the slot id to `sess+0x3d0` and the constant 1 to `sess+0x3d4`, and `FUN_004764c0` zeroes `sess+0x3cc` **and `sess+0x3d8`** and sets the other two to -1. A `disp:3d8` sweep gives **14 containing-routine labels** for the field: `0040d1f1`, `004306eb`, `00471790`, `00476430`, `004764c0`, `0048fb80`, `00490860`, `00492180`, `0049223c`, `004928a0`, `004a3520`, `004a4550`, `004a5610`, `004a5ee0`. **`00492180` and `0049223c` are the same routine, not two** (`AI-CURSOR-205`, [EXP-0216](../experiments/EXP-0216-mission-cursor-selection/)): the sweep's own containing-function lookup split one routine's `+0x3d8` references across a real prologue and a false one, so those 14 labels are **13 distinct routines**. Three were read: `FUN_004a3520` sets it (`004a35e6 MOV ECX,[ESI+0x3d8]`, `004a35fc CALL 0046d3d0`), and `FUN_0048fb80` (`0048fd5f`) and `FUN_00490860` (`00490938`) apply it through the same idiom -- test `[+0x3cc]`, load `[+0x3d8]`, idempotence guard, adapter. `FUN_004764c0` clears it (`TOWN-350`). **`FUN_004a3630` does not touch `+0x3d8`**: it restores slot 0 `default` (`004a3633 MOV ECX,[0x5ef9b4]`, `004a3649 CALL 0046d3d0`), which is the paired restore rather than a consumer of the field. ~~Seven~~ *eight* of the thirteen have now been read: `FUN_00476430` and `FUN_004a3520` here, `FUN_004764c0` in `TOWN-350`, `FUN_0048fb80` and `FUN_00490860` here, and `FUN_00492180` and `FUN_004928a0` in [EXP-0216](../experiments/EXP-0216-mission-cursor-selection/) (`AI-CURSOR-205`, `AI-CURSOR-206`), which find the same test-`[+0x3cc]`/load-`[+0x3d8]` idiom in both. ~~The six not read are `0040d1f1`, `004306eb`, `00471790`, `004a4550`, `004a5610` and `004a5ee0`~~ *-- `AI-CURSOR-230` ([EXP-0218](../experiments/EXP-0218-hover-cursor-cascades/)) reads `FUN_0040d1f1`'s reference: `0x0040db7c MOV EDX,[ECX+0x3d8]` behind `CMP [EAX+0x3cc],0x0` at `0x0040db5e`, the same test-`[+0x3cc]`/load-`[+0x3d8]` idiom this row names in `FUN_0048fb80` and `FUN_00490860`. **The five not read are `004306eb`, `00471790`, `004a4550`, `004a5610` and `004a5ee0`.*** | High. The live alternative excluded is `AI-CURSOR-176`'s own reading, that the constructed cursor goes to `[this+0x3cc]`. Three independent facts exclude it: the store at `0x00476484` names `+0x3d8` and takes the constructor's return register, while the store at `0x0047648e` names `+0x3cc` and takes a value loaded from the argument area; the argument displacements are fixed by the routine's own `RET 0x10`, so they do not depend on the constructor's calling convention; and `TOWN-350`, decoded in a different experiment from a different routine, lists `sess+0x3d8` in the clear set beside `sess+0x3cc` and gives `sess+0x3cc` a value `FUN_00476430` receives as an argument. `AI-CURSOR-176`'s headline, that a 29th cursor is built at runtime through the same constructor, is unaffected | ● active | [EXP-0214](../experiments/EXP-0214-cursor-surfaces/) |
| AI-CURSOR-193 | **No path that sets no cursor clears the one already displayed.** Nothing outside the set-cursor adapter writes `[0x5cd794]`: a whole-image dword scan of that address returns 23 references and **every one is a read**. So an exit that calls no adapter leaves the pointer unchanged, whatever routine it is in. Six such exits were read. In `FUN_0040d1f1`: the entry gate `CMP [ECX+0x3dc],0x1 ; JZ ; JMP 0040dcb9` (`0x0040d243`) returns when the surface state word is not exactly 1; `CALL 0041ab8e ; JMP 0040dcb9` (`0x0040d2e2`) returns after a failed hit test; `CMP [EBP-0x28],0x0 ; JZ 0040dcb9` (`0x0040dc94`) returns when the chosen-cursor local, initialised to 0 at `0x0040d273`, was never written; and `CMP EAX,[EBP-0xe4] ; JZ 0040dcb9` (`0x0040dca9`) returns when the chosen cursor's `+0x4` already equals `[0x5cd794]` as saved at entry (`0x0040d251`). `FUN_0048fb80` carries the last two at `0x0048fd65` and `0x0048fd70`, and has **a seventh** at `0x0048fbdf` (`JZ 0048fd79` when `CALL [0x632f6c]` returns 0), inside a block this experiment did not decode. **The no-cursor state is a no-change state.** The idempotence guard also means the adapter is never called for a cursor already displayed | High for the general statement, and it rests on the read-only scan rather than on the exit enumeration: `[0x5cd794]` has 23 references image-wide and no writer outside the adapter, which `AI-CURSOR-172` independently establishes as the writer. The live alternative excluded is that some routine clears the global directly to hide the pointer -- a byte-level scan of every section cannot miss such a store, and there is none. **The exit enumeration itself is not complete**: the two routines were decoded in fragments, the seventh exit was found by the adversarial pass in a block this experiment skipped, and `00419ec1`, `00490860`, `0049223c` and `004928a0` also select among slots and were not read. **Corrected by `AI-CURSOR-204`..`209` ([EXP-0216](../experiments/EXP-0216-mission-cursor-selection/)): `00490860`, `0049223c` (which is `0x00492180`, not a separate routine) and `004928a0` are now read and each carries the same three no-op exit shapes named here; `00419ec1` does not select among slots — `AI-CLICK-050` establishes it compares the current cursor against each registered cursor's own `+0x4` and dispatches an order, and `AI-CURSOR-204` reads the one slot reference `AI-CURSOR-196` names inside it (`0x0041a136`) as exactly that comparison — and the seventh exit's own block (`FUN_0048fb80`, `0x0048fbac`-`0x0048fc72`) is also now read.** `AI-CURSOR-194` gives the one `SetCursor(NULL)` site, which is outside the manager | ● active | [EXP-0214](../experiments/EXP-0214-cursor-surfaces/) |
| AI-CURSOR-194 | **The `SetCursor(NULL)` routine's address is installed as vtable slot 22 of `0x00599d10`, and once the routine is entered the call cannot be skipped.** `EXP-0213` reported `FUN_004709e0` as having 0 direct callers and called that a weak negative. Direct `E8 rel32` callers in `.text` are indeed **0**. A whole-image dword scan finds exactly **one** stored pointer to `0x004709e0`, at `0x00599d68` in `.rdata`. A second scan finds exactly **one** reference to `0x00599d10`, the start of the table that address lies in, at `0x0047096c`, inside `0047096a  c7 06 10 9d 59 00  MOV [ESI],0x599d10` -- a store to offset 0 of the object `ESI` points at, which is a vtable install. `(0x00599d68 - 0x00599d10) / 4 = 22`, so `FUN_004709e0` is slot 22 of that vtable. The installing routine begins at `0x00470960` and is called from `0x00470983` inside the routine at `0x00470980`. Within `FUN_004709e0` (252 instructions, `0x004709e0`..`0x00470cd8`, decoded with 0 stops from its `PUSH EBP; MOV EBP,ESP; PUSH -1; PUSH 0x58a7c2` prologue) the `00470cad  ff 15 44 2f 63 00  CALL [0x632f44]` with `PUSH 0x0` at `0x00470ca0` is unconditional: no `RET` occurs anywhere before it, and the highest branch target in the whole routine is `0x00470c85` | Medium. The two byte-level scans are complete for both pointer values, which excludes the alternative that nothing anywhere in the image can reach the routine. What is **not** established is that anything calls vtable slot 22 of `0x00599d10`: no call through that slot was searched for or found, so reachability here is by construction, a constructor installing the table, rather than by a traced call. `AI-CURSOR-173` and `AI-CURSOR-174` stand -- this call passes no manager cursor and is not part of the manager's cursor set | ● active | [EXP-0214](../experiments/EXP-0214-cursor-surfaces/) |
| AI-CURSOR-195 | **The second `0x99c` population `AI-CURSOR-178` flagged belongs to a different object, and the field there is an array index rather than a mode enum.** `[base+0x99c]` is accessed in 15 routines. Two further sweeps separate them. Six routines touch both `+0x99c` and `+0x910` -- `00492e90`, `00493200`, `00493280`, `00493560`, `00495a70`, `0049a290`, all in the `0x49xxxx` range. Two routines touch both `+0x99c` and `+0x98c` -- `0040d1f1` and `00419ec1`, both in the `0x40xxxx` range and both named by `AI-CURSOR-178` as mission-view routines. **No routine touches both `+0x910` and `+0x98c`.** At the `0x49xxxx` sites the field is consumed as `MOV EAX,[ESI+0x99c]` ; `MOV EDX,[ESI+0x910]` ; `MOV ECX,EAX` ; `SHL ECX,5` ; `SUB ECX,EAX` ; `MOV EAX,[EDX+ECX*4+0x74]` (`0x00492f7e`), which computes `((v*32) - v)*4 = v*124` -- an index into an array whose base is `+0x910`, with a 124-byte element stride. The sibling loop at `0x0049a366` confirms the stride with `ADD EBX,0x7c`. `AI-CURSOR-178` reads the mission view's `+0x99c` as a small enum cleared to 0. The two readings cannot describe the same field | High. The live alternative excluded is that the two populations are the same field and differ only in which call sites each sweep happened to sample. Two facts exclude it: the routine sets are disjoint under `+0x910` versus `+0x98c`, so the split is a property of the routines rather than of the sampling; and the instruction shape at the `0x49xxxx` sites uses the value as a multiplied array subscript, which a 0-or-small-enum mode field cannot be. **Unknown** for what object the `0x49xxxx` population belongs to: its routines are named, its class is not. `+0x9a4` was tried as a discriminator first and discarded, being touched from both ranges | ● active | [EXP-0214](../experiments/EXP-0214-cursor-surfaces/) |
| AI-CURSOR-196 | **The `town` cursor (slot 24, `0x005efa14`) has exactly three consumer references, all in mission-view routines.** `AI-CURSOR-175` attributed no call site to it. The whole-image scan of `AI-CURSOR-188` finds three references outside the registry constructor and destructor: `0x0040d833` and `0x0040da41`, both inside `FUN_0040d1f1`, the mission-map hover routine; and `0x0041a136`, inside `FUN_00419ec1`, which `AI-CURSOR-178` names as a mission-view routine. There is **no** reference to slot 24 in any routine of the town-screen transition family (`TOWN-372`), and none in any data section. The cursor named `town` is selected while the mission map is on screen, not while the town screen is | Medium. The reference population is complete and its containing routines are named, which excludes the alternative that the slot is set from a town-screen routine the proximity instrument missed. What is **not** established is the condition under which either site is chosen: both lie inside the region `0x0040d445`..`0x0040dc31` of `FUN_0040d1f1` that this experiment did not decode, so what the pointer is over when `town` is selected is unknown. **`AI-CURSOR-209` ([EXP-0216](../experiments/EXP-0216-mission-cursor-selection/)) reads all three references: the two inside `FUN_0040d1f1` are each gated by the same selection-count/CUnit-flag/object-flag/hit-test-bit condition, with the hit-test routine itself and the two flag bits' game meaning still unread, and `0x0041a136` is a comparison arm of `FUN_00419ec1`'s cursor-to-order dispatch (`AI-CLICK-050`), not a selection.** | ● active | [EXP-0214](../experiments/EXP-0214-cursor-surfaces/) |
| AI-CURSOR-202 | **`view+0x140` is the mission view's own count of currently selected objects, incremented once per object the selection-rebuild loop accepts.** `FUN_004190f0` (`AI-PANEL-061`'s sole writer of `view+0x144`) resets `view+0x140` to 0 at `0x00419142`, in the same block that resets `view+0x144` (`0x00419166`) and `view+0x148` (`0x00419173`). The per-object loop that walks the current selection reads `[ECX+0x140]` at `0x004193eb`, adds 1 at `0x004193f1` (`ADD EDX,0x1`), and stores the result back at `0x004193f7`, once per pass through a loop bounded by `CMP EDX,[ECX+0x8]` against a per-source array size (`0x00419354`, `0x0041939d`) and gated by `CMP [EBP-0x68],0x0 ; JZ 0x00419d2d` on a per-object test (`[obj+0x7c]` non-zero). The same iteration sets `view+0x138` to the first accepted object (`0x00419421`), matching `AI-PANEL-061`'s "primary object `view+0x138`". `FUN_004190f0` reads `view+0x140` twice more, both as integer comparisons: `CMP [ECX+0x140],0x0 ; JLE` at `0x00419d35` (guarding ownership bit `0x4`) and `CMP [EAX+0x140],0x0 ; JZ` at `0x00419d6d` (guarding the panel-blank notify, `AI-CURSOR-203`). `AI-CLICK-050` independently reads `view+0x140` the same way, as a plain selected/not test, at its own routine's entry. This resolves `AI-CURSOR-191`'s Unknown for `view+0x140` | High. The live alternative excluded is that `0x140` is a boolean latch rather than a count: the store at `0x004193f7` is an increment of the field's own prior value inside a loop that can run more than once per call, and every consumer treats it with integer comparisons (`==0`, `<=0`, `==1`) rather than a bit test | ● active | [EXP-0216](../experiments/EXP-0216-mission-cursor-selection/) |
| AI-CURSOR-203 | **The `view+0x144` mask `0x24` that `FUN_0048fb80` tests to force `sdefault` is the same mask, on the same field, that `FUN_004190f0` tests on itself to blank the command panel. This resolves `AI-CURSOR-191`'s other Unknown.** `AI-PANEL-061` names the writer of `view+0x144` and the set sites of bit `0x4` (ownership mismatch, `0x00419d5e`) and bit `0x20` (a selected `CStructure`, `0x00419d12`), but does not connect the field to `FUN_0048fb80`'s use of it. `FUN_004190f0` reads its own field at the end of the same routine: `CMP [ECX+0x140],0x0 ; JLE 0x00419d6a` gates bit `0x4`, and separately `CMP [EAX+0x140],0x0 ; JZ 0x00419d86` followed by `AND EDX,0x24 ; TEST EDX,EDX ; JZ 0x00419da8` at `0x00419d79`/`0x00419d7f` — when nothing is selected, or when something is selected and either bit `0x4` or bit `0x20` is set, the routine notifies the panel with message `0x40a` and two zero arguments (`0x00419d86`); only when something is selected and neither bit is set does it fall through to notify `0x409` with `AI-PANEL-060`'s capability mask. `FUN_0048fb80` tests the identical mask on the identical field at `0x0048fd42` (`TEST [EBP+0x144],0x24 ; JZ`) to override its own selection with `sdefault` instead of the armed-mode pick | High for the correlation: both routines read the same struct field with the same immediate mask, one write site (`view+0x144`'s sole writer per `AI-PANEL-061`) and two same-mask readers producing an analogous neutral outcome. Not established beyond `AI-PANEL-061`'s own statement: the game-level meaning of bits `0x1`, `0x2`, `0x8` and `0x20`. `AI-PANEL-061` gives `0x20`'s set site, a `strcmp` against `"CStructure"`, and lists `0x20` in the same not-established set as `0x1`, `0x2` and `0x8`; this row adds one consumer for `0x20` and for `0x4` — the `0x24` mask read here — and none for `0x1`, `0x2` or `0x8` | ● active | [EXP-0216](../experiments/EXP-0216-mission-cursor-selection/) |
| AI-CURSOR-204 | **`FUN_00490860` selects one of three edge-arrow cursors on the mission view's right screen edge, falls back to `default` on a second hit test, and applies a held-item override — one of the "unread selector routines" `AI-CURSOR-193` names.** Bounded `0x00490860`-`0x0049095b` (`RET`), one direct caller (`0x0049046c`, `-callers`/`-fnof` verified), NOP-padded on both sides. Two calls to `0x00573196` and a vtable dispatch through `[+0x7c]` fetch the mission view into a local; a hit test through the shared function pointer `[0x632f6c]` (`AI-CURSOR-193`) on the current pointer position gates everything after — a miss (`JZ 0x00490954`) is a no-op exit. On a hit, a right-edge test (`X >= [0x5ea208]-2` and `[view+0x3dc]==1`) picks one of three registry slots by `Y`: `0x005ef9f0` (`arrow1`, north-east) when `Y==0`, `0x005ef9f4` (`arrow3`, south-east) when `Y >= [0x5ea20c]-2`, else `0x005ef9e4` (`arrow2`, east) — matching `AI-CURSOR-190`'s count of 3 arrow references for this routine, with the names taken from `SPR16A-CURSOR-067`'s construction order. Outside the right edge, a second call through the same hit-test pointer, on failure leaves the chosen-cursor local unset and on success picks `0x005ef9b4` (`default`, slot 0). At the join, `[view+0x3cc]!=0` overrides the pick with `[view+0x3d8]` (`AI-CURSOR-192`). Three no-op exits reach the epilogue with no adapter call: the hit-test miss, the local still unset, and an idempotence match against the displayed cursor's own `+0x4` (`0x00490949`) — the same three shapes `AI-CURSOR-193` establishes for `FUN_0048fb80`. `AI-CURSOR-193`'s unread-routine list also names `00419ec1`; that routine is `AI-CLICK-050`'s order dispatcher, which compares the current cursor `[0x005cd794]` against each registered cursor's own `+0x4` handle, one arm per cursor, and dispatches an order rather than selecting a cursor. The one slot reference `AI-CURSOR-196` names inside it, `0x0041a136`, is read here and is that arm: `MOV EAX,[0x5efa14]` at `0x0041a135`, then `MOV ECX,[EAX+0x4] ; MOV [EBP-0x40],ECX ; MOV EDX,[EBP-0x1c] ; CMP EDX,[EBP-0x40] ; JNZ 0x0041a160` through `0x0041a146`, with no store into a chosen-cursor local. `0x0041a135` is the target of `JNZ 0x0041a135` at `0x0041a112`, so that boundary is fixed independently of the linear decode. `00419ec1` is not part of this population | High. RET-bounded, single verified caller, every branch a named instruction from a verified boundary | ● active | [EXP-0216](../experiments/EXP-0216-mission-cursor-selection/) |
| AI-CURSOR-205 | **The routine `AI-CURSOR-190`/`192`/`193` cite as `0049223c` does not exist as a separate function. It is a false function boundary from `tools/cursorsurf`'s unchecked whole-`.text` scan for `0xE8` bytes; every reference attributed to it belongs to the routine at `0x00492180`.** `-callers 49223c` reports one caller, `0x0044a8b4`. Disassembly of `0x0044a840`-`0x0044a8c5` shows `0x0044a8b4` is not an instruction boundary: it is the disp8 byte of `MOV ECX,[EBP-0x18]` (`8b 4d e8`, `0x0044a8b2`-`0x0044a8b4`), followed by `CMP dword ptr [ECX+0x4],0x0`. Reading the same five bytes as `E8 <rel32>` (`e8 83 79 04 00`) computes target `0x0044a8b9 + 0x00047983 = 0x0049223c`, reproducing the phantom exactly. `-xrefs 49223c:49223c` finds 0 stored dword references to that address in any section, excluding a vtable or jump-table entry. `0x00492180` is a genuine bounded routine: prologue `83 ec 18 53 55 56 57 8b f1`, preceded by nine `0x90` padding bytes (`0x00492177`-`0x0049217f`), themselves preceded by a `RET` at `0x00492169` with three instructions between the two, one direct caller (`0x00491165`, inside `FUN_00490d88`), and a single `RET` at `0x004922db`, after which `FUN_004922f0` begins cleanly (the routine `AI-CURSOR-192`'s `TOWN-350` cross-reference already names). The disputed byte falls inside `MOV EAX,[0x005cd7a0]` (`0x00492239`-`0x0049223d`), mid-instruction, with continuous flow from `0x00492180` through it and no `RET` in between. `FUN_00492180`'s own structure: after the shared hit test, a state-bit test (`TEST CL,0x2` on `[view+0x3dc]`) branches to a held-item-legacy path unique to this routine among its three siblings — with `[view+0x3cc]!=0` (`TEST EAX,EAX ; JZ 0x004922be` at `0x004921ff`), it picks `[view+0x3d8]` when either `[[view+0xf0]+0x144]` is non-zero (`TEST ESI,ESI ; JNZ 0x00492228` at `0x00492213`) or `[[view+0x3cc]+0x18]==1` (`CMP [EAX+0x18],0x1 ; JZ 0x00492228` at `0x00492217`), and registry slot 23, `cantput` (`0x005efa10`, `SPR16A-CURSOR-067`), only when NEITHER holds: both branches jump to `0x00492228`, and the `cantput` load at `0x0049221d` is their fall-through. With `[view+0x3cc]==0` the branch loads no cursor slot at all and jumps straight to the common tail at `0x004922be` (`0x00492201`); so do its other two outcomes, by `JMP` from `0x00492223` and `0x0049222e`, so the whole legacy branch bypasses the held-item override at `0x004922aa`. `evidence/disasm-listings.txt` prints this sequence at `0x00492213`-`0x00492228`; an earlier draft of this sentence stated the polarity the other way round, and that draft error is not a further `tools/cursorsurf` artifact — the instrument printed the bytes and both branch targets correctly. On the other branch, the same right-edge test as `FUN_00490860` (`arrow1`/`arrow3`/`arrow2`) plus, uniquely among the three, a bottom-edge test (`Y >= [0x5ea20c]-2`, state`==1`) picking `0x005ef9dc` (`arrow4`, south) — four arrow picks, matching `AI-CURSOR-190`'s count of 4 for "`0049223c`". A second hit-test call on failure of both edge tests picks `default` on success and loads no cursor slot on failure, then the same held-item override, the same three-exit idempotence pattern, and a call to `FUN_0046d3d0`. `AI-CURSOR-190`'s arrow census, `AI-CURSOR-192`'s `disp:3d8` sweep, and `AI-CURSOR-193`'s unread-routine list each name `0049223c`; the correct address in each is `0x00492180`, and no count, headline or conclusion those rows draw depends on the address being different from what it names — only the address token is wrong | High. The live alternative excluded is that `0x0049223c` is a genuine alternate entry point: excluded by the absence of any `RET` before it in a continuous decode from `0x00492180`, by zero stored dword references to it in any section, and by the disputed byte's own position mid-instruction inside a `MOV` whose other four bytes are a verified data address | ● active | [EXP-0216](../experiments/EXP-0216-mission-cursor-selection/) |
| AI-CURSOR-206 | **`FUN_004928a0` repeats `FUN_00492180`'s arrow-and-bottom-edge selection exactly, without the held-item-legacy branch, and is a second, separate routine from the one beginning immediately after its own `RET`.** Bounded `0x004928a0`-`0x004929c5` (`RET`), NOP-padded to `0x004929d0`, where a structurally different routine (5-register prologue `83 ec 20 53 55 56 8b f1 57`) begins. `-fnof`/`-callers 4928a0` report a caller at `0x00492d4c` as "in `004928a0`". That call site is in neither `FUN_004928a0` nor the routine at `0x004929d0`. `FUN_004929d0` ends at its own `RET` at `0x00492c67`, which `TOWN-093` gives independently of this experiment, decoding that routine whole as widget 8's `vt+0x2c` handler. The listing `-dis 492c60:492d60` in `evidence/disasm-listings.txt` prints that `RET`, the eleven `0x90` bytes that follow at `0x00492c75`-`0x00492c7f`, a third routine beginning at `0x00492c80` (`MOV EAX,[ESP+0xc] ; SUB ESP,0x10 ; PUSH EBX`), the call site `0x00492d4c` itself and that routine's `RET 0xc` at `0x00492d5a`, in one continuous decode with 0 stops; its start `0x00492c60` is the target of the `JNZ 0x00492c60` at `0x00492a06` that `TOWN-093` names, so the range begins on a verified instruction boundary. `0x00492d4c` is inside that third routine, two `RET`s past `FUN_004928a0`. The tool's containing-function report groups the call site under the nearest preceding heuristic hit rather than the true boundary — a display-grouping artefact of the same unchecked-boundary heuristic as the `0049223c` case, but one that fabricates no address. `go run ./tools/claim -k "00492d4c"` and `-k "00492c80"` each return this row and no other, so neither the call site nor its containing routine is named elsewhere in the corpus; `-k "4929d0"` returns this row and `TOWN-093`, where the `004929d0` routine start is already published (`evidence/corpus-searches.txt`). `FUN_004928a0`'s own selection tree is the same shape as `FUN_00492180`'s arrow/bottom-edge block: right edge picks `0x005ef9f0`/`f4`/`e4` (`arrow1`/`arrow3`/`arrow2`) by `Y`, and a bottom-edge test (state`==1`, no right-edge match) picks `0x005ef9dc` (`arrow4`) — the same four slots, matching `AI-CURSOR-190`'s count of 4 for this routine. It has no state-bit-`0x2` held-item-legacy branch: after the arrow tests, a second hit-test call on failure picks `default` on success or leaves the local unset, then the same held-item override and the same three-exit idempotence-guarded call to `FUN_0046d3d0` | High for the routine's own bounds and selection tree, read from a verified prologue to its `RET`. The live alternative for the boundary — that `0x00492d4c`'s containing routine really is `0x004928a0` — is excluded by two intervening `RET`+padding+new-prologue sequences, at `0x004929c5`-`0x004929d0` and at `0x00492c67`-`0x00492c80`, both printed in `evidence/disasm-listings.txt` and the second's terminator also published by `TOWN-093` | ● active | [EXP-0216](../experiments/EXP-0216-mission-cursor-selection/) |
| AI-CURSOR-207 | **`FUN_0048fb80` also selects among four edge-arrow cursors, in a block before the armed-mode tree `AI-CURSOR-191` already describes; this is where the routine's 4 arrow references (`AI-CURSOR-190`) and its seventh no-op exit (`AI-CURSOR-193`) live.** Read whole from `0x0048fbac` to `0x0048fc72`. The block's fall-through end is the mission-view load `AI-CURSOR-191` cites, `MOV EBP,[ESI+0x5c]` at `0x0048fc5c`. After loading the current cursor, a hit test through `[0x632f6c]` on the current pointer position; a miss (`JZ 0x0048fd79`) is `AI-CURSOR-193`'s seventh, previously-undecoded exit. On a hit: a right-edge test (`X >= [0x5ea208]-2`, `[view+0x3dc]==1`) picks `0x005ef9f0` (`arrow1`, NE) when `Y==0`, `0x005ef9f4` (`arrow3`, SE) when `Y >= [0x5ea20c]-2`, else `0x005ef9e4` (`arrow2`, E); otherwise a top-edge test (`Y==0`, state`==1`) picks `0x005ef9d8` (`arrow0`, N). Each of the four jumps directly to `0x0048fd51`, the convergence point `AI-CURSOR-191`'s `TEST [EBP+0x144],0x24` override guards, bypassing the mission-view load, `AI-CURSOR-208`'s bounds-check block, the `[EBP+0x140]==0` test, the armed-mode jump table, the `0x24` override itself and the `sdefault` load at `0x0048fd4b`. The held-item override at `0x0048fd55` is past `0x0048fd51` and does apply to an arrow pick. That is exactly 4 slot loads, matching `AI-CURSOR-190`'s count for this routine. Together with `FUN_00490860`, `FUN_00492180` and `FUN_004928a0` (`AI-CURSOR-204`..`206`), these four routines' edge-arrow tests cover north, north-east, east, south-east and south; none of the four tests west, south-west or north-west, which are read only inside `FUN_0040d1f1`'s own block (`AI-CURSOR-190`) | High. Every branch is a named instruction from a verified boundary, the exit matches `AI-CURSOR-193`'s own citation of address and condition, and the slot count matches `AI-CURSOR-190`'s independently-obtained whole-image census exactly | ● active | [EXP-0216](../experiments/EXP-0216-mission-cursor-selection/) |
| AI-CURSOR-208 | **The block `AI-CURSOR-191` names but does not decode, `0x0048fcdd`-`0x0048fcfb`, is a zoom-scaled rectangle test against a widget rectangle read from `view+0x80`/`+0x84`/`+0x88`; what the widget is was not established.** Read whole from `0x0048fc5c`, where the mission view is loaded. A value at `[this+0x68]` — `MOV ECX,[ESI+0x68]` at `0x0048fc72`, `ESI` being `this` from `MOV ESI,ECX` at `0x0048fb87`, not the mission view, which is `EBP` from `MOV EBP,[ESI+0x5c]` at `0x0048fc5c` (`AI-CURSOR-191`) — is used twice. Its sign selects between two derivations of a half-width and half-height from `[[view+0x80]+0x4]` and `[[view+0x80]+0x8]`, the `view+0x80` load being `MOV EDI,[EBP+0x80]` at `0x0048fc65`: non-negative takes `SHL` then `SAR 1` (`0x0048fc95`, `0x0048fca1`), negative takes `SAR 2` (`0x0048fcaf`, `0x0048fcba`). Its value, clamped to 0 when negative (`XOR ECX,ECX` at `0x0048fc7e`), is the shift count `CL` used in the first derivation and on both differences. Those combine with the current pointer position around two fixed constants: `0x48` against `X` (`MOV EDX,0x48` at `0x0048fc90` and `0x0048fcaa`, subtracted from `[0x5cd7a0]` at `0x0048fccf`) and `0x52` against `Y` (`MOV ESI,0x52` at `0x0048fcc3`, subtracted from `[0x5cd7a4]` at `0x0048fcd1`). Each difference is kept twice, raw and shifted right by the same count (`SAR EDX,CL`, `SAR ESI,CL` at `0x0048fcd7`/`0x0048fcd9`). Four branches reject the pick and force `sdefault` (slot 17, `0x005ef9f8`) at `0x0048fd4b`: the raw `X` difference negative (`0x0048fcdd`), the raw `Y` difference negative (`0x0048fce1`), the shifted `X` difference greater than `[view+0x84]-0x10` (`0x0048fcee`), the shifted `Y` difference greater than `[view+0x88]-0x10` (`0x0048fcfb`) — matching `AI-CURSOR-191`'s "four further branches reach `sdefault`" exactly. Only when the transformed pointer position falls inside this rectangle does the routine continue to the `[EBP+0x140]==0` test and the armed-mode jump table. A keyword search of every ledger for "party figure" and for "figure row" returns one row each, this row itself (`evidence/corpus-searches.txt`); no other row uses either phrase. A search for "view+0x80"/"view+0x84"/"view+0x88" (`go run ./tools/claim -k`, the literal `+` escaped so it is not read as a regex quantifier) returns this row, and for `+0x80` also `AI-CURSOR-191` carrying this experiment's own amendment, plus rows in `anim`, `dialogue`, `magic`, `shop` and `terrain` that predate this experiment and are not about the mission view: the offsets recur on unrelated structs, `TERR-EDGE-024`'s own text naming `CMapView+0x84` as a grid width, an object the mission view is not shown to be. ~~No row names a widget on the mission view at these offsets; the widget this rectangle describes was not identified from any other routine or from the shipped art in this experiment.~~ *-- corrected by `AI-CURSOR-232` ([EXP-0218](../experiments/EXP-0218-hover-cursor-cascades/)): the three offsets are not a widget's. `FUN_0041ab8e` reads `[view+0x80]+0xc` as the tile-word plane `TERR-FOG-082` names as `[CMapView+0x80]+0xc`, and `[view+0x84]` as that plane's row stride, off the same register that carries `view+0x98c` and `view+0x144`. The mission view is `CMapView`, which is the identity this row records as not shown; `view+0x88` is still unread on either side.* The corpus was not searched for `+0x68` on the containing object, so this absence is scoped to the five searches above and says nothing about that field | Medium. The branch structure and its four conditions are a direct instruction-level read. What remains open is the widget's game identity, which decides whether "outside this rectangle forces `sdefault`" is the fact worth publishing or a proxy for a more specific one, and the game-level meaning of `[this+0x68]`, which is read here only as a sign test and a shift count and is called a zoom value by analogy rather than from any established writer. No live alternative for the widget's identity was tested | ● active | [EXP-0216](../experiments/EXP-0216-mission-cursor-selection/) |
| AI-CURSOR-209 | **Both `town`-slot references `AI-CURSOR-196` names share one condition: exactly one `CUnit` selected, that unit's own `+0x18c` bit `0x1` set, and a hover hit-test result (`FUN_0041ab8e`, read whole by `AI-CURSOR-231`) with bit `0x800` set.** Both sites are cited by operand address, as `AI-CURSOR-196` cites them. At the second site (`0x0040da41`, the operand of `MOV EAX,[0x5efa14]` at `0x0040da40`, reached from a hit-test result stored at `[EBP-0x60]`): the routine tests `[view+0x140]==1` (`0x0040d8c8`) and `[view+0x144]&0x1` (`0x0040d8dd`) — both established fields, `AI-PANEL-061` — into `[EBP-0x104]`, which is copied to `[EBP-0x68]` and to `[EBP-0x64]` at `0x0040d8fa`-`0x0040d906`; then, only when `[EBP-0x64]` is set, it additionally tests `[[view+0x138]+0x18c]&0x1` (the selected object's own field) and `[EBP-0x60]&0x800` (the hit-test result) at `0x0040d97c`-`0x0040d9be`, and picks `town` at `0x0040da40` when `[EBP-0x64]` survives that refinement (`CMP [EBP-0x64],0x0 ; JZ 0x0040da4a` at `0x0040da3a`). At the first site (`0x0040d833`, the operand of `MOV EDX,[0x5efa14]` at `0x0040d831`, reached from a separate hit-test result stored at `[EBP-0x5c]`), the same shape recurs: the same `[view+0x140]==1` (`0x0040d632`) and `[view+0x144]&0x1` (`0x0040d641`) test into `[EBP-0xf8]`, copied to `[EBP-0x58]` and to `[EBP-0x54]` at `0x0040d664`-`0x0040d670`; the same two refinement tests verbatim (`0x0040d6e6`-`0x0040d728`) against the same `+0x18c` field and the same `0x800` bit; and the pick gated on `[EBP-0x54]` at `0x0040d82b`. The two committed listings run `0x0040d62c`-`0x0040d7f0` and `0x0040d7f0`-`0x0040da55` with no gap between them, and within that window `[EBP-0x54]` is written only at `0x0040d670` and `0x0040d728`. The two `view+0x144` bits are already named by `AI-PANEL-061`: bit `0x1` is set at `0x0041948b` after a `strcmp` against `"CUnit"`, which with `view+0x140==1` makes the single selected object a `CUnit`; and the refinement's own `[[view+0x138]+0x18c]&0x1` is the same field and bit that routine folds into `view+0x144` bit `0x8` at `0x00419cbb`, so the refinement re-tests a predicate the flags word already records. `AI-PANEL-061` does not establish what either predicate means in game terms. `AI-CURSOR-196`'s third reference (`0x0041a136`, inside `FUN_00419ec1`) is not a selection: it is the operand of `MOV EAX,[0x5efa14]` at `0x0041a135`, whose next instructions load the slot's own `+0x4`, compare it against `[EBP-0x1c]` and branch (`0x0041a13a`-`0x0041a146`) — one arm of the cursor-to-order dispatch `AI-CLICK-050` establishes for that routine | Medium. The whole condition — `view+0x140==1`, `view+0x144` bit `0x1`, `+0x18c` bit `0x1` and hit-test bit `0x800` — is read at instruction level and recurs at both sites independently, and the local carrying it is written at only two instructions inside the committed listings. Not established: ~~the game-level meaning of `+0x18c` bit `0x1`~~ *-- `PARTY-FLAG-003` ([EXP-0077](../experiments/EXP-0077-party-and-save/)) reads `CUnit+0x18c` bit `0x1` as the player-character flag, at Medium; `AI-CURSOR-242` ([EXP-0220](../experiments/EXP-0220-pickup-cursor-route/)) finds the same field and bit gating `FUN_0040d1f1`'s `pickup` arms* or of ~~hit-test bit `0x800` (`FUN_0041ab8e` itself was not read)~~ *-- that is true of this experiment and not of the corpus: `UNIT-HOVER-020` ([EXP-0109](../experiments/EXP-0109-attack-order/)) reads `FUN_0041ab8e` and gives bit `0x800` at High -- it is set for a `CStructure` whose `[0x005eb62c][obj+0x20]` record has a non-zero `+0x8c`. `AI-CURSOR-231` ([EXP-0218](../experiments/EXP-0218-hover-cursor-cascades/)) reads the routine whole and enumerates all seven of its bits*. "What is under the pointer" when `town` is chosen is answered in terms of these two fields' own bits: `+0x18c` bit `0x1`, read as the player-character flag at Medium (`PARTY-FLAG-003`), and hit-test bit `0x800`, read as a hit `CStructure` at High (`UNIT-HOVER-020`/`AI-CURSOR-231`) | ● active (amended) | [EXP-0216](../experiments/EXP-0216-mission-cursor-selection/), [EXP-0220](../experiments/EXP-0220-pickup-cursor-route/) |
| AI-CURSOR-218 | **The cursor's own blit is `0042ad45 CALL [EAX+0x18]` on the cursor object at `mgr+0x4`, inside `FUN_0042aa20`, and it writes into the back surface. This closes the Unknown half of `AI-CURSOR-174`.** `FUN_0042aa20` has one call site in the whole image, `0044ce64`, inside `FUN_0044cda0` (`SESS-COMPOSE-057`), reached with `0044ce5f MOV ECX,0x5cd790` and the presented rectangle pushed at `0044ce5e`. Read to its `RET 0x4` at `0042ad54`, the routine returns immediately unless `mgr+0x4` is non-zero (`0042aa29`, `0042aa2e JZ 0042ad4d`) and `mgr+0x20` is non-zero (`0042aa34`, `0042aa39 JZ 0042ad4d`); `AI-CURSOR-172` establishes `mgr+0x4` as the current cursor object and `mgr+0x20` as the manager's refcount. It then runs at most one of two earlier blocks and always ends at the cursor block beginning `0042acb3`. That block is: `0042acbe CALL [0x00632f70]`, USER32 `IntersectRect` by the image's own import directory, against the cursor's own rectangle, returning without drawing when it is empty (`0042acc6 JZ 0042ad4d`); `0042accc CALL 0044c3b0`, the back-surface lock; `0042ad1e CALL 0044c660`, which copies the pixels under the cursor into the buffer object at `mgr+0xc`; the blit at `0042ad45`, whose five arguments are pushed at `0042ad32`-`0042ad44` as `0`, `0`, `mgr+0x24`, `mgr+0x14 - mgr+0x1c`, `mgr+0x10 - mgr+0x18`; and `0042ad48 CALL 0044c410`, the unlock. `AI-CURSOR-172` establishes `mgr+0x24` as the animation frame index and `mgr+0x18` and `mgr+0x1c` as two fields copied from the cursor object, so the first two arguments are a position minus a per-cursor origin. `AI-MINIMAP-156` reads `vt+0x18` on the same kind of resource object as a sub-rectangle blit. `AI-CURSOR-174` records that its experiment "did not locate the render-loop blit" and grades that half Unknown; this is the blit (`evidence/disasm-listings.txt`, `evidence/composition-sites.txt` sections A and D) | High for the call, its five arguments, its enclosing lock and its single caller. Every one is a named instruction in a listing read from `0x0042aa20` to its own `RET`, and the caller count pairs a direct-call scan of `.text` with a relative-branch scan and a stored-dword scan over every section's raw extent. The alternative excluded is that the cursor is a widget in the composition tree: `FUN_0042aa20` has 0 stored pointers anywhere in the image, so it sits in no vtable, and its only entry is from inside the present. **Medium** for reading `mgr+0x18` and `mgr+0x1c` as a hotspot: the subtraction is read here, the meaning of the two fields is not | ● active | [EXP-0217](../experiments/EXP-0217-cursor-composition-order/) |
| AI-CURSOR-219 | **The cursor is the last write into the back surface before any pixel becomes visible, so it covers everything the widget tree paints.** `FUN_0044cda0` runs the overlay composite at `0044ce64` and then does nothing but copy: the back-surface lock at `0044ce80`, the primary-surface lock at `0044cea5`, `0044ced2 CALL 0044cb20`, and the two unlocks (`SESS-COMPOSE-057`). Inside `FUN_0042aa20` the three overlay blocks converge on the cursor block: the `mgr+0x60` block ends `0042ab27 JMP 0042acb3`, and the `mgr+0x9c` block falls through to the same address, so the cursor is composited after whichever of the two ran. Widget content reaches the screen through this routine: every widget presents through `FUN_004bd716`, which calls `FUN_0044cda0` at `004bd73e` (`TOWN-063`, and 96 vtables carry that slot, `SESS-COMPOSE-059`). That is an enumeration of the present slot, not of everything in the image that can copy to the display surface, which is why this row is scoped to regions presented through `FUN_0044cda0`. The remaining ten call sites of the generic copy `FUN_0044cb20` lie between `0044cfda` and `0044d276`, inside `FUN_0044cf30` and `FUN_0044d1a0`. Neither routine references `[0x005e4390]` and neither is among the nine `FUN_0044c460` call sites: both lock the third surface at `[0x005e4178]` through `FUN_0044c4f0` (`SESS-COMPOSE-056`, `SESS-COMPOSE-058`) | High for regions presented through `FUN_0044cda0`. The alternative excluded is that something writes to the display surface after the copy inside this routine: `FUN_0044cda0` past `0044ced2` does nothing but unlock at `0044ceda`, restore `[0x005e43bc]` at `0044cee3`, unlock again at `0044cee9` and call `FUN_0042ad60` at `0044cef4`, and that is one listing read to the `RET`. **This row says nothing about a region that never enters `FUN_0044cda0`**, and it does not assert that no widget can ever write to the display surface: `SESS-COMPOSE-058` grades that Medium and names what it does not exclude. `FUN_004ae840`, which receives the display surface object at `0047a650`, was not read | ● active | [EXP-0217](../experiments/EXP-0217-cursor-composition-order/) |
| AI-CURSOR-220 | **The answer does not differ per mission panel surface: the cursor is above the control panel, the character figure widget, the worn box, the pack bar and the minimap column alike, for the same structural reason.** Every widget's content reaches the screen through `FUN_004bd716`, which calls `FUN_0044cda0` (`TOWN-063`, `SESS-COMPOSE-059`), and `FUN_0044cda0` copies the back surface at `[0x005e438c]`. Whatever a widget painted was therefore in the back surface when the present ran, and the cursor is composited into the back surface immediately before that copy (`AI-CURSOR-219`). The argument is about the present's own order and does not need a claim that no widget can ever reach the display surface, which `SESS-COMPOSE-058` grades Medium. The worked instance is the minimap: `AI-MINIMAP-156` reads `FUN_0048f470`, the paint routine of `campaign+0xd8`'s own widget, from entry to its `RET` at `0048fb7f`, and finds it writing through `[0x005e43bc]` and closing with `FUN_0044c410`; the back-surface lock site inside that routine is `0048f4ee`. Ten of the 85 back-surface lock sites fall between `0x00480000` and `0x00493000`: `00480a07`, `004861ce`, `0048668e`, `0048e4ab`, `0048e719`, `0048f4ee`, `0049074f`, `004919a7`, `00491edc`, `00492a29`. None of the nine primary-surface lock sites is in that range. `MENU-COMBAT-017` names the right column's children as `campaign+0xd8`, `campaign+0xdc`, `campaign+0xe0` and `campaign+0xe4` (`evidence/composition-sites.txt` section A) | High for the uniformity, which rests on the present's own order rather than on reading each widget: whatever reaches the screen through `FUN_0044cda0` was in the back surface before the copy, and the cursor is the last write into the back surface (`AI-CURSOR-219`). The alternative excluded is that one of these surfaces is presented by a different routine that composites no cursor: every widget presents through `FUN_004bd716`, which has one call to `FUN_0044cda0` at `004bd73e` and no other present. **Medium** for the attribution of individual back-surface lock sites to individual widgets: only `0048f4ee` is attributed, and that through a published row which reads its containing routine end to end. The other nine were not attributed to a routine. **Medium** for the residual the excluded alternative does not cover, that one of these five writes to the display surface outside its present slot: that is excluded at instruction level only for the minimap, whose paint routine `AI-MINIMAP-156` reads end to end, and for the other four it rests on the nine primary-surface lock sites all falling outside `0x00480000`..`0x00493000` while ten back-surface lock sites fall inside it, which is a lock-site range observation and not a read of those routines | ● active | [EXP-0217](../experiments/EXP-0217-cursor-composition-order/) |
| AI-CURSOR-221 | **Both Esc panels are below the cursor, and the answer is the same for the mission panel and the town panel.** `MENU-ESC-010` names the two constructions and the shared `0047466c … CALL 0x00476810`. Re-read here: `0047345e CALL 0043c23b` is followed by `00473463 JMP 0047466c`, `00474663 CALL 0043c90b` by `00474668 JMP 0047466c`, and at `0047466c` both push the constructed object and reach `00474677 CALL 00476810`. The rectangles pushed at `0047344d`-`0047345b` and `00474652`-`00474660` are `(100,60)-(440,400)` and `(100,100)-(440,340)`, which agree with `MENU-ESC-010`. Inside `FUN_00476810`, after the screen darkening `DLG-DIM-013` reads, the order is `00476871 CALL 0044c410` (back-surface unlock), `00476876 CALL 0044cf00`, and `0047687d CALL [EDI+0x34]`, the panel's own draw-subtree slot. `FUN_0044cf00` builds the whole client rectangle through `CopyRect` and calls `FUN_0044cda0` at `0044cf18`, so the darkened screen becomes visible through the present, which composites the cursor last. The panel's own draw at `0047687d` runs after that present, so its content becomes visible through a later one. Every present in the widget path is `FUN_0044cda0`: 96 vtables carry `FUN_004bd716`, which has one call to it (`SESS-COMPOSE-059`, `TOWN-063`), and this experiment found no second present routine. Three vtables in the image replace the draw-subtree slot (`SESS-COMPOSE-059`); one of them, base `0x00598118`, replaces the slot at `0x0059814c` with `FUN_0043d1f8`, whose whole body is `0043d1ff CALL 0044c3b0` / `0043d207 CALL 004bd6e4` / `0043d20c CALL 0044c410` - the base draw bracketed by a back-surface lock and unlock (`SESS-COMPOSE-059`). `FUN_00476810` also calls `FUN_0042b140` at `004768a3`, gated on `[0x005cd82c]`; that routine reaches `FUN_0042b330` at `0042b14a` and `FUN_0042a430` at `0042b151`, both cursor-manager routines, and neither changes the composition order | High for the chain from either construction site to `FUN_0044cda0`, and for both rectangles: every step is a named instruction, and the two rectangles are immediates re-read here and already published by `MENU-ESC-010`. Both panels were measured, not one. The alternative excluded is that the panel's own content is composited into a frame after the cursor: `0047687d CALL [EDI+0x34]` is the draw-subtree slot and it runs after `00476876 CALL 0044cf00` has returned, so it cannot execute inside the present that call performed, and the present is the routine that composites the cursor. **Medium** for the step from that to "below the cursor on the screen", which needs the panel's content to reach the screen through `FUN_0044cda0` as well: `FUN_004bd716` has one call to it and this experiment found no second present routine, but the search was a stored-dword scan for `0x004bd716` and a direct-call scan for `FUN_0044cda0`, not an enumeration of everything that can copy to the display surface. **Unknown** whether vtable `0x00598118` is either Esc panel's own class: the constructors were not followed to a vtable store, and the row does not depend on it | ● active | [EXP-0217](../experiments/EXP-0217-cursor-composition-order/) |
| AI-CURSOR-222 | **The same cursor blit is issued a second time, directly onto the primary surface, by a path that runs outside any present and uses its own backing store.** `FUN_0042a450` — the routine `AI-CURSOR-172` reads for its `timeGetTime` frame advance and does not read past it — continues at `0042a483 CALL 0044c460`, the primary-surface lock, saves the pixels under the cursor into the buffer object at `mgr+0x8` (`0042a488 MOV EAX,[ESI+0x8]`, `0042a4d5 CALL 0044c660`), issues `0042a4fc CALL [EAX+0x18]` on `mgr+0x4` with the same five arguments as `AI-CURSOR-218`'s blit (`0`, `0`, `mgr+0x24`, `mgr+0x14 - mgr+0x1c`, `mgr+0x10 - mgr+0x18`), and unlocks at `0042a4ff`. Its counterpart is `FUN_0042a510`: `0042a516 CALL 0044c460`, a restore from the `mgr+0x8` buffer, `0042a559 CALL 0044c4b0`. `FUN_0042a510` has 0 direct call sites and 0 stored pointers; its only reference in the image is the tail jump `0042a420 JMP 0042a510` out of `FUN_0042a410`'s decrement-to-zero arm, which is the "distinct, untraced routine" `AI-CURSOR-172` names. So the manager's two sub-objects have one job each: `mgr+0x8` backs the primary surface and `mgr+0xc` backs the back surface, and which surface the one blit primitive writes to is decided by the enclosing lock (`SESS-COMPOSE-056`). `FUN_0042a430`, the only caller of `FUN_0042a450`, has 12 call sites, all listed by `AI-CURSOR-172`; one is `0042a91b` inside `FUN_0042a810`, which itself has 260 call sites | High for the second blit, its surface, its backing store and the two routines' reference sets: each is a named instruction in a listing read to its own `RET`, and the reference sets pair a direct-call scan with a relative-branch scan and a stored-dword scan. The relative-branch scan is what finds `FUN_0042a510` at all, and it agrees with `AI-CURSOR-172`, which reached the same tail jump independently. **Medium** for this being a live per-motion path in play: the cadence of the 12 call sites was not traced here, which is the same gap `AI-CURSOR-172` records for its own reading of the same 12 | ● active | [EXP-0217](../experiments/EXP-0217-cursor-composition-order/) |
| AI-CURSOR-223 | **Nothing moves the cursor's position in the composition order. What state changes is whether the cursor is composited at all, and over which rectangle.** Three kinds of gate were found, all inside the cursor manager. First, both overlay routines return without touching anything unless `mgr+0x4` and `mgr+0x20` are both non-zero: `0042aa29`/`0042aa34` in `FUN_0042aa20` and `0042ad69`/`0042ad74` in `FUN_0042ad60`, the same pair in the same order. Second, each overlay element is tested with `IntersectRect` against the rectangle being presented — `0042aab9` for the `mgr+0x60` element, `0042ab37` for the `mgr+0x9c` element, `0042acbe` for the cursor — and an empty result skips that element only. Third, the `mgr+0x60` block ends `0042ab27 JMP 0042acb3`, so when it runs the `mgr+0x9c` block does not. Within a present the surviving elements are always in the order `mgr+0x60` or `mgr+0x9c`, then the cursor; no branch reorders them and no phase word selects a different routine. `FUN_0042aa20` and `FUN_0042ad60` have one call site each in the whole image | High for the gates and for the fixed order, which are named instructions in two listings read to their own terminators, plus a call-site enumeration. The alternative excluded is a second compositor selected by state: `FUN_0042aa20` and `FUN_0042ad60` are the only routines the present calls for this purpose and each has exactly one call site, so there is no second one to select. **Unknown** what `mgr+0x60` and `mgr+0x9c` are. The first block does one save-under and one call to `FUN_0042b510`; the second does four save-unders over four strips of one rectangle, which is the shape of an outline. Neither was identified | ● active | [EXP-0217](../experiments/EXP-0217-cursor-composition-order/) |
| AI-CURSOR-224 | **Correction to `AI-CURSOR-172`: the WINMM import at IAT slot `0x00632fa4` has 85 direct call sites in `.text`, not one. The row's conclusion is unaffected.** `AI-CURSOR-172` describes `FUN_0042a450`'s clock read as "WINMM.dll `timeGetTime` (IAT `0x00632fa4`, the sole caller of that import in `.text`)" and its confidence paragraph repeats it as "`timeGetTime`'s one call site is inside this exact chain". A scan of the whole image for the stored dword `0x00632fa4` returns 108 hits, every one in `.text`: 85 of the form `FF 15 A4 2F 63 00`, a direct call through the slot; 22 of the form `MOV reg,[0x00632fa4]`, which load the resolved address into a register for a later call (11 into EDI, 5 into ESI, 4 into EBX, 2 into EBP); and one `JMP [0x00632fa4]` at `004ceb4c`, an import thunk with 0 direct call sites. `0042a455` is one of the 85. The row's conclusion does not rest on the clause: what excludes a Windows-timer-driven animation is `SESS-TIMER-022`'s import-directory walk, which finds `SetTimer`, `KillTimer`, `timeSetEvent` and `timeKillEvent` absent from the image, and that is unchanged. Only the uniqueness of the call site is withdrawn (`evidence/disasm-listings.txt`, final section) | High. The count is a complete stored-dword sweep over the raw extent of every section against one fixed address, and the two forms are read out of the printed bytes rather than inferred. The alternative excluded is that the other 107 sites reference a different address that happens to print the same: the scan matches the four bytes of the address itself and prints six bytes of context on each side, and `FF 15` before it is unambiguous | ● active (corrects `AI-CURSOR-172`) | [EXP-0217](../experiments/EXP-0217-cursor-composition-order/) |
| AI-CURSOR-225 | **Correction to `TOWN-209`: `FUN_0044c3b0` and `FUN_0044c410` are the back surface's Lock and Unlock, not a clip save and restore.** `TOWN-209` reads the tip popup's `vt+0x30` and describes the pair as "`FUN_0044c3b0()` (no arguments)" and "`FUN_0044c410()` (no arguments, clip-restore-shaped)". `DLG-DIM-013`, published earlier, already names the same two routines as the DirectDraw Lock and Unlock. The correction is scoped to this one pair: `TOWN-209` also names `FUN_0044cac0` and `FUN_0044ca60` as a clip-save/clip-set pair, and that reading is correct. `FUN_0044ca60` copies a `RECT` from its argument into the four consecutive globals `[0x005e4408]`, `[0x005e440c]`, `[0x005e4410]` and `[0x005e4414]` and returns; `FUN_0044cac0` passes `0x005e4408` to `CopyRect` at `0044caca CALL [0x00632f8c]`, copying the same four out. `DLG-DIM-013` cites `FUN_0044c3b0` reaching `vt+0x64` with `0044c3cd MOV dword ptr [0x005e4398],0x6c` and `FUN_0044c410` reaching `vt+0x80`. This experiment reads them the same way and adds the population: `FUN_0044c3b0` has 85 call sites and `FUN_0044c410` 88, and `SESS-COMPOSE-056` shows the pair publishing the locked surface's pixel pointer and pitch into `[0x005e43bc]` and `[0x005e43a8]`. The correction does not change what `TOWN-209` establishes about the popup: the tile ids, the loops and the pitch constants stand, and the bracket it describes is the lock that makes the tile blits legal rather than a clipping state | High. The alternative excluded is `TOWN-209`'s own reading, that `FUN_0044c3b0` and `FUN_0044c410` are the clip-save and clip-restore pair. That pair is `FUN_0044ca60` and `FUN_0044cac0`, which copy a `RECT` into `[0x005e4408]`..`[0x005e4414]` and back out through `CopyRect` at `0044caca` and touch no surface. `FUN_0044c3b0` instead stores the descriptor size at `0044c3cd MOV dword ptr [0x005e4398],0x6c` and calls the surface object's `vt+0x64`, after which `[0x005e43bc]` holds a pixel pointer and `[0x005e43a8]` a pitch, both read by the caller as named instructions. The two functions are therefore distinct routines with distinct globals, not two readings of one. That two published readings of the lock pair now agree is corroboration and is not what the grade rests on | ● active (corrects `TOWN-209`) | [EXP-0217](../experiments/EXP-0217-cursor-composition-order/) |
| AI-CURSOR-226 | **`FUN_0040d1f1` contains two mask-to-cursor cascades, not one, and an ordinary hover runs the second: over a hostile unit with no modifier key held it selects `attack`.** The cascade `AI-CURSOR-052` reads at `0x0040d72b` is reachable only through `0x0040d550`, which `0x0040d477 JZ` enters only when `view+0x99c` is zero, and which then passes four gates. Each gate is a conditional branch to `0x0040d84d` taken when its own condition FAILS, so the four listed here are the conditions a hover must SATISFY to stay on this path: `FUN_0041ddea(view)` non-zero (`0x0040d55b TEST` / `0x0040d55d 0f 84 JZ`), the id `[[sess+0xec]+0x74]` when non-negative else `[[sess+0xec]+0x60]` non-negative (`0x0040d59e CMP` / `0x0040d5a5 0f 8c JL`), `view+0x144 & 0x200` set (`0x0040d5b7 AND` / `0x0040d5be 0f 84 JZ`), and `view+0x144 & 0x4` clear (`0x0040d5d0 AND` / `0x0040d5d5 0f 85 JNZ`). An ordinary hover has `view+0x144 & 0x200` clear, so it fails gate 3 and leaves for `0x0040d84d` there at the latest. `TOWN-354` reads `FUN_0041ddea(v)` as `FUN_004bd28f(sess+0xf0,3) != 0` when `sess+0x3dc & 2` and `FUN_004bd28f(v,3) != 0` otherwise, and `AI-CURSOR-233`'s entry gate requires `sess+0x3dc == 1`, so on this routine's path gate 1 is the presence of the view's child with id 3. Every other hover runs the cascade at `0x0040d84d`, which recomputes the mask into `[EBP-0x60]`: `view+0x140 == 0` (`0x0040d861`) gives `select` on `mask & 0x23` and `default` otherwise; `view+0x144 & 0x24` set (`0x0040d898`, `AI-CURSOR-203`'s mask on `AI-CURSOR-202`'s field) gives the same pair; then `mask & 0x4` (`0x0040d9c4`) gives `move` with the Alt latch set, `select` on `mask & 0x20` (`0x0040d9df`), and otherwise **`attack`**, slot 3, `0x005ef9c0`, loaded at `0x0040d9e6` and stored at `0x0040d9eb`; then `mask & 0x23` (`0x0040da01`) gives `move` with Alt, `attack` with Ctrl (`0x0040da1e`), `pickup` (`0x0040da2f`), `town` (`0x0040da40`) or `select` (`0x0040da4a`); and empty ground (`0x0040da55`) gives `swarm` with Ctrl (`0x0040da5b`), `move` with Alt, `pickup`, else `move` (`0x0040da87`). Each address named for a cursor in this enumeration is that slot's own load; the store into the cursor local `[EBP-0x28]` is the next instruction, as with `0x0040d9e6`/`0x0040d9eb`. A hostile `CStructure` therefore gives `select`, not `attack`, because every hit `CStructure` carries bit `0x20` and `0x0040d9df` tests `0x20`. `UNIT-HOVER-020` states `0x20` and `0x800` as alternatives; they are not alternatives, and `AI-CURSOR-231` carries the correction. **The Shift latch `[0x005eb55c]` is read at exactly one address in the whole routine, `0x0040d76a`, inside the first cascade**, so Shift changes nothing on an ordinary hover. Slot names are `SPR16A-CURSOR-067`'s, latch identities `AI-KEYMOD-059`'s, mask bits `UNIT-HOVER-020`'s and `AI-CURSOR-231`'s | High. The live alternative excluded is the reading the corpus currently supports: that `AI-CURSOR-052`'s cascade is what an ordinary hover runs. `evidence/control-flow.txt`, a mechanical extraction of every explicit branch edge of the routine from the committed listing, gives `0x0040d550` exactly one predecessor and shows no branch from outside landing anywhere in `0x0040d550`-`0x0040d72b`; the routine's one indirect branch is the jump table at `0x0040d4ad`, whose eight targets were read out of the image and all lie in `0x0040d4b4`-`0x0040d4f2`, so the edge set for that region is complete rather than the explicit-branch subset. The `attack` result is stated with its guards: it holds when at least one object is selected, `view+0x144 & 0x24` is clear, no item is held, no marquee is active and the pointer is outside both child rectangles, which are the five replacements `AI-CURSOR-230` enumerates | ● active | [EXP-0218](../experiments/EXP-0218-hover-cursor-cascades/) |
| AI-CURSOR-227 | **Two clauses of `AI-CURSOR-052` are refuted. The hostility bit `0x4` is tested alone at two addresses, and at one of them it selects `attack` with no modifier key held; and the Alt latch does not force `move` "regardless".** `AI-CURSOR-052` states that "the hostility bit `0x4` appears only inside `mask & 0x24`, whose effect is to suppress the select cursor". Its reading of `0x0040d760`-`0x0040d773` is correct. The word "only" is not: `0x0040d7ba` (`83 e1 04`, `AND ECX,0x4`) tests the bit alone in the first cascade's tail, and `0x0040d9c4` (`83 e0 04`, `AND EAX,0x4`) tests it alone at the head of the second cascade, where a set bit with the Alt latch clear and `mask & 0x20` clear selects `attack` at `0x0040d9e6`/`0x0040d9eb` -- the opposite of suppression, and with no modifier key held. `AI-CURSOR-052` also states that "with `[0x005eb560]` set the cursor is **move** regardless". Its store at `0x0040d736` is correct and "regardless" is not: on that same path the local is replaced by `pickup` at `0x0040d793` when the composite `[EBP-0x58]` holds, by `town` at `0x0040d837`, by `default` at `0x0040db18` or `0x0040db58`, by `[sess+0x3d8]` at `0x0040db82` and by `backpack` at `0x0040dc91` -- six stores, the same six the `AI-CURSOR-052` entry in `claims/retracted.md` lists. The marquee's own `default` store at `0x0040dab8` is not among them: this path ends `0x0040d848 e9 6e 02 00 00 JMP 0040dabb`, which lands past it (`AI-CURSOR-230`); and the second cascade's first arm (`0x0040d861`-`0x0040d887`, taken when `view+0x140 == 0`) contains no read of `[0x005eb560]` at all, so with the Alt latch set and nothing selected a hover over a unit gives `select`. Both refuted clauses were published at High. `AI-CURSOR-052`'s other clauses -- the hover-time hostility test, the `mask & 3` split under Ctrl, and the cursor-to-art binding -- are unaffected | High. The live alternative excluded for the first clause is that the two `AND` sites are the low half of a wider `mask & 0x24` test written in two instructions: both encodings carry the immediate `0x04`, `evidence/disasm-listings.txt` prints their bytes, and neither is adjacent to a second `AND` on the same value. For the second clause the exclusion is a path rather than an encoding: the `JNZ` at `0x0040d868` is the only edge past the `view+0x140 == 0` arm, and that arm's twelve instructions, `0x0040d861` to `0x0040d887` inclusive, are in the same listing | ● active | [EXP-0218](../experiments/EXP-0218-hover-cursor-cascades/) |
| AI-CURSOR-228 | **The selection local `[EBP-0x28]` of `FUN_0040d1f1` has 48 writers, and on entry to the modifier cascade at `0x0040d72b` it holds `cast` or `move`, never the zero it was initialised to.** The complete writer set, each with the instruction that produced its value, is listed in `evidence/control-flow.txt`, derived mechanically from the committed listing: one immediate zero at `0x0040d273`, 46 stores of a value loaded from one of `SPR16A-CURSOR-067`'s 28 registry slots, and one store of `[sess+0x3d8]` at `0x0040db82`. Eight of the 48 lie in `0x0040d4d2`-`0x0040d62b`, the range absent from `EXP-0216`'s committed listing. The same listing carries **49** references to a cursor-registry slot inside the routine, and they account for the writer set exactly: 46 are a `MOV` whose destination register the very next instruction stores into `[EBP-0x28]`, and three are a `CMP` of the local's current value against a slot -- `0x0040d510` and `0x0040d799` against `cast` `0x005ef9d0`, `0x0040d785` against `move` `0x005ef9b8`. 46 slot loads plus the zero at `0x0040d273` plus the `[sess+0x3d8]` store at `0x0040db82` is the 48. `0x0040d5ee` (`MOV [EBP-0x28],EAX` after `MOV EAX,[0x005ef9d0]`) dominates `0x0040d72b`: that cascade is entered only from `0x0040d6e4 JZ` or the fall-through from `0x0040d728`, both inside `0x0040d62c`-`0x0040d728`, whose only entries are `0x0040d60f JNZ`, `0x0040d621 JZ` and the fall-through from `0x0040d629`, all past the unconditional store at `0x0040d5ee`. So the local holds `cast` there, or `move` where `0x0040d604` (`mask & 0x400`) or `0x0040d629` (`mask & 3` clear and `FUN_004b0e20([sess+0xec])` non-zero) replaced it. **`[EBP-0x8]`, written at `0x0040d315` as the bitwise OR of the Ctrl latch, the Alt latch and their own OR, has no reader in the routine** | High. Two alternatives are excluded. A writer reaching the slot through a pointer would need its address, and the routine contains no `LEA` of `[EBP-0x28]`, so the 48 stores are the population rather than a sample of one addressing form. A path on which the `0x0040d273` zero survives to `0x0040d72b` would need an entry into `0x0040d550`-`0x0040d72b` that skips `0x0040d5ee`; the extracted predecessor set contains no such edge, and the routine's only indirect branch targets eight addresses all below `0x0040d4f3` | ● active | [EXP-0218](../experiments/EXP-0218-hover-cursor-cascades/) |
| AI-CURSOR-229 | **`FUN_0040d1f1`'s first cascade has four gates, a `cast` default and three replacements, two of them driven by three parallel 24-entry boolean tables. Its own default and one of its gates read as a cast-target test, at Medium.** Past the four gates of `AI-CURSOR-226`, `0x0040d5ee` writes `cast` (slot 7, `0x005ef9d0`). `0x0040d604` replaces it with `move` when `mask & 0x400`. `0x0040d629` replaces it with `move` when `mask & 3` is clear and `FUN_004b0e20([sess+0xec])` is non-zero. After the modifier cascade, `0x0040d796`-`0x0040d828` replaces it with `move` when the local is still `cast`, `FUN_004b0e20([sess+0xec])` is non-zero, `mask & 0x4` is clear (`0x0040d7ba`), `[[sess+0xec]+0x74]` is negative (`0x0040d7cc` `CMP`/`SETGE`/`TEST`/`JNZ`) and `[0x005bcf58 + [[sess+0xec]+0x60]*4]` is non-zero (`0x0040d818`). `FUN_004b0e20` is eight instructions with two `RET`s: `[ECX+0x74]` non-negative returns `[0x005bcfb8 + [ECX+0x74]*4]`, otherwise `[0x005bcef8 + [ECX+0x60]*4]`. The three tables are 24 dwords each, adjacent at `0x005bcef8`, `0x005bcf58` and `0x005bcfb8`, and all 72 values are 0 or 1 (`evidence/imports-and-tables.txt`). The index recomputation at `0x0040d7e6`-`0x0040d812` selects between `+0x74` and `+0x60` a second time although `0x0040d7d5` has already established `+0x74` negative, so the index at `0x0040d818` is always `+0x60` | High for the gates, the branch structure, `FUN_004b0e20`'s body and the tables' shape and contents: all are instructions in the committed listing or dwords read out of the image, and the alternative that some gate is off the path is excluded by the extracted predecessor set. **Medium** for reading the branch as a cast-target test. That rests on two facts and not on any decoded target predicate: the branch's own default is the `cast` slot, and one of its gates is `view+0x144 & 0x200`, which `AI-PANEL-061` and `AI-PANEL-053`'s retraction entry name as the bit that enables the cast mode. **Unknown** for what `[sess+0xec]` is and what the three tables hold: `go run ./tools/claim -k` returns no row for `0x005bcef8`, `0x005bcf58`, `0x005bcfb8`, `campaign+0xec` or `sess+0xec` (`evidence/corpus-searches.txt`), `FUN_0041ddea` is read by `TOWN-354` at High -- it returns `FUN_004bd28f(sess+0xf0,3) != 0` when `sess+0x3dc & 2` and `FUN_004bd28f(v,3) != 0` otherwise, so gate 1 is the presence of a child with id 3, through the same accessor and the same index `AI-CURSOR-230` finds in the tail. `go run ./tools/claim -k "0041ddea"` returns seven rows, and this experiment ran that search (`evidence/corpus-searches.txt`) without reading its output | ● active | [EXP-0218](../experiments/EXP-0218-hover-cursor-cascades/) |
| AI-CURSOR-230 | **Five replacements run after the ordinary cascade of `FUN_0040d1f1` in a fixed order and four of the five after the first cascade, and the last two are this routine's own use of the held-item cursor that `AI-CURSOR-192` left unread.** In order: (1) `0x0040da90`-`0x0040dab8`, the marquee, which runs on the ordinary-cascade path only: the first cascade's tail ends `0x0040d848 e9 6e 02 00 00 JMP 0040dabb`, which lands past it, and every edge into `0x0040da90` comes from inside the ordinary cascade. Replacements (2) to (5) run on both paths. The marquee test is -- `[0x005cd82c]` non-zero (`AI-INPUT-121`'s flag) and `IsRectEmpty` false on the normalised copy of `[0x005cd7fc]` gives `default`, and the identical test is repeated on the armed-mode path at `0x0040d520`-`0x0040d548`; (2) `FUN_004bd28f(this, 2)` non-null and `PtInRect(child+0x8)` gives `default`, loaded at `0x0040db12` and stored at `0x0040db18`; (3) the same with index 3, `0x0040db52`/`0x0040db58`; (4) `[sess+0x3cc]` non-zero gives `[sess+0x3d8]` at `0x0040db82`, the held-item cursor of `AI-CURSOR-192`, reached through the same test-`+0x3cc`-then-load-`+0x3d8` idiom that row finds in `FUN_0048fb80` and `FUN_00490860`; (5) inside (4), `sess+0x3dc == 1`, `view+0x140 == 1`, `[[view+0x138]+0x18c] & 1`, `[[view+0x138]+0x14] == view+0x9b4`, the pointer's map column and row within 2 of the selected object's `+0x34` and `+0x38`, and `mask & 1` give `backpack` (slot 27, `0x005efa20`) at `0x0040dc91`. `FUN_005545c0`, called twice for the two differences, is five instructions (`0x005545c0`-`0x005545ca`) and is `abs`. The three imports behind the indirect calls are `PtInRect` `[0x00632f6c]`, `IsRectEmpty` `[0x00632f78]` and `CopyRect` `[0x00632f8c]`, resolved from the PE import directory rather than by name guess. This makes `FUN_0040d1f1` the eighth of `AI-CURSOR-192`'s thirteen `+0x3d8` routines to be read; the five not read are `004306eb`, `00471790`, `004a4550`, `004a5610` and `004a5ee0` | High for the order and the five replacements. The live alternative excluded is that they are arms of one chain rather than a sequence: each is a separate `CMP`/`JZ` whose taken and not-taken edges both converge on the next test, which the extracted predecessor set shows directly, so a later replacement overwrites an earlier one rather than being skipped by it. What is **not** established: what `FUN_004bd28f`'s children 2 and 3 are on the mission view. `TOWN-347` reads the same accessor with the same two indices on the town screen, and whether those are the same widgets was not tested | ● active | [EXP-0218](../experiments/EXP-0218-hover-cursor-cascades/) |
| AI-CURSOR-231 | **The hover hit test's capability mask has exactly seven bits. Bit `0x400` is set when the four corners of the hovered cell do not OR to `0xc000` and bit `0x40` when they do and the cell carries a drawable on the `CMapView+0x98` plane. `TERR-FOG-082`'s state mapping reads the first as no corner currently visible, at Medium.** This closes the open item `UNIT-HOVER-020` names. `FUN_0041ab8e`'s mask local `[EBP-0x50]` is written at exactly eight addresses in the whole routine: `0x0041abdd` zeroes it, then `0x0041af0e` bit `0x1`, `0x0041af2d` bit `0x2`, `0x0041af6b` bit `0x800`, `0x0041af84` bit `0x20`, **and those two are not alternatives**: `0x0041af63 74 0b JZ 0041af70` sends a zero `+0x8c` past the `0x800` store, and `0x0041af6e eb 0e JMP 0041af7e` lands the non-zero case ON the `0x20` store rather than past it, so a hit `CStructure` always carries `0x20` and carries `0x800` in addition when `+0x8c` is non-zero; the third arm, `+0x8c` zero with `[EBP-0x80]+0x64` non-zero, jumps to `0x0041ad6a` and sets neither bit. `UNIT-HOVER-020` reads these as `0x800` when `+0x8c` is non-zero and `0x20` **otherwise**, at High, and that clause is refuted. Continuing, `0x0041afb2` bit `0x4`, `0x0041b260` bit `0x40`, `0x0041b278` bit `0x400`. The last two: `0x0041b129`-`0x0041b217` builds the flat tile index `(X>>5 + view+0x5c) + (FUN_0041a9f9(X,Y) + view+0x60) * [view+0x84]` into the tile-word plane `[[view+0x80]+0xc]` and ORs the `0xc000` field of the four words at index, index+1, index+1+stride and index+stride -- the same four-corner OR, the same mask and the same `CMP …,0xc000` that `TERR-FOG-086` reads in `FUN_004597f0`. `0x0041b224 CMP EDX,0xc000` with `JNZ` skips the `0x40` arm and `0x0041b26b CMP EAX,0xc000` with `JZ` skips the `0x400` arm, so the two bits are the same comparison read both ways and cannot both be set. The `0x40` arm additionally requires `[[view+0x98] + ((r+3)*([view+0x64]+6) + c+3)*4]` non-zero at `0x0041b254`, where `c = X>>5` and `r = FUN_0041a9f9(X,Y)` are the RAW view-relative pair and not the map column and row of the sentence before it: `view+0x5c` and `view+0x60` are added into `[EBP-0x24]` and `[EBP-0x2c]` only at `0x0041b287` and `0x0041b296`, after this index is built at `0x0041b22c`-`0x0041b244`, while the tile index adds them into a separate temporary `[EBP-0x38]` at `0x0041b16b` and `0x0041b177`. That is the same index form `TERR-SPR-137` gives for its five drawable planes (`CMapView+0x8c/+0x90/+0x94/+0x98/+0x9c`, all indexed `(row+3)*(visCols+6) + (col+3)`); `TERR-SPR-138` reads `view+0x64` as the visible-column count, which is what makes the two index forms the same expression. The routine also writes `view+0x994` = `X>>5 + view+0x5c` and `view+0x998` = `FUN_0041a9f9(X,Y) + view+0x60` at `0x0041b41d` and `0x0041b42c`, the hovered cell's map column and row, beside the `view+0x98c` and `view+0x990` that `UNIT-HOVER-020` names. `-fnof` gives the routine six direct `E8` callers and all six are inside `FUN_0040d1f1`, at `0x0040d2e2`, `0x0040d4f8`, `0x0040d5e1`, `0x0040d840`, `0x0040d853` and `0x0040db71`, listed with every other `CALL` of that routine in `evidence/control-flow.txt`. The returned mask is unused at two of the six: `0x0040d2e2` stores nothing and `0x0040d2e7 JMP` leaves for the routine's exit, and `0x0040d845` stores it into `[EBP-0x5c]`, which has no reader at or after that address. That scan finds direct relative calls only, so it says nothing about a call reached through a vtable slot | High for the seven-bit enumeration: the routine was decoded whole from its prologue to its own `RET`, the local is a stack slot in a fixed `EBP` frame, and there is no `LEA` of it anywhere in the routine, which excludes a writer reaching it through a pointer. High for the `0x20`/`0x800` flow that refutes `UNIT-HOVER-020`: the live alternative excluded is that `0x0041af6e` jumps PAST the `0x20` block, and the displacement decides it -- `eb 0e` taken from `0x0041af70` is `0x0041af7e`, the first instruction of that block, and `evidence/disasm-listings.txt` prints both addresses with their bytes. High for the two comparisons and for the mutual exclusion, which follows from both arms testing one accumulator against one constant with opposite branch senses. **Medium** for the game reading of bit `0x400` as "under fog": it converts the OR test into "at least one corner currently visible" using `TERR-FOG-082`'s state mapping, and inherits that row's own Medium blind spot on the unreachability of the `01` state -- were `01` reachable, a corner holding `0x4000` and another holding `0x8000` would OR to `0xc000` with no corner visible. **Unknown** which class of drawable occupies `CMapView+0x98`: `TERR-SPR-137` names the five planes and their index form and does not say what each holds | ● active | [EXP-0218](../experiments/EXP-0218-hover-cursor-cascades/) |
| AI-CURSOR-232 | **The mission view of the `AI-CURSOR` and `AI-PANEL` rows and the `CMapView` of the `TERR` rows are one object. `AI-CURSOR-208`'s unidentified widget rectangle is read from the map's own grid fields.** `FUN_0041ab8e` receives the mission view as `this` and stores into `view+0x98c`, `view+0x990`, `view+0x994` and `view+0x998` (`UNIT-HOVER-020` for the first two, `AI-CURSOR-231` for the other two). `view+0x144` (`AI-PANEL-061`) is read by `FUN_0040d1f1` off the same object and not by this routine: the token `0x144` does not appear anywhere in `FUN_0041ab8e`'s committed listing. Off the same register it reads `[this+0x80]+0xc` as a word plane masked with `0xc000`, which is `TERR-FOG-082`'s `[CMapView+0x80]+0xc` tile plane; `[this+0x84]` as that plane's row stride (`0x0041b180 IMUL ECX,[EAX+0x84]`); and `[this+0x98]` as one of `TERR-SPR-137`'s five drawable planes at that row's index form `(row+3)*(visCols+6) + (col+3)` with `visCols = [this+0x64]`, which is `TERR-SPR-138`'s inner loop bound. `AI-CURSOR-208` reads `[[view+0x80]+0x4]`, `[[view+0x80]+0x8]`, `[view+0x84]-0x10` and `[view+0x88]-0x10` in `FUN_0048fb80`, where `view` is `[this+0x5c]` (`AI-CURSOR-191`), and records that no row names a widget at those offsets and that `TERR-EDGE-024`'s `CMapView+0x84` belongs to "an object the mission view is not shown to be". It is that object: `view+0x80` is the map, `[view+0x80]+0x4` and `+0x8` its dimensions, and `view+0x84` the tile-grid row stride | High for the object identity. The live alternative excluded is that the mission view and `CMapView` are different structures whose fields happen to coincide: one routine reads or writes `+0x98c`, `+0x990`, `+0x80`, `+0x84`, `+0x64`, `+0x5c` and `+0x60` off one register in one frame, and the first two of those are mission-view fields by `UNIT-HOVER-020` (EXP-0109) while the rest are `CMapView` fields by `TERR-FOG-082` (EXP-0088), `TERR-SPR-137` and `TERR-SPR-138`. The exclusion is not a coincidence argument: the two vocabularies are reached off the SAME register, `[EBP-0x110]`, within one stack frame, so one pointer serves both. **Medium** for what `AI-CURSOR-208`'s rectangle then measures. `view+0x88` is not read in either listing here, so it is the map's row count by position in the pair rather than by a read, and the screen role of the rectangle -- what widget draws the map at origin `(0x48, 0x52)` scaled by `[this+0x68]` -- was not established | ● active | [EXP-0218](../experiments/EXP-0218-hover-cursor-cascades/) |
| AI-CURSOR-233 | **`FUN_0040d1f1` has one `RET` and exactly four exits that call no cursor adapter, so `AI-CURSOR-193`'s exit enumeration is now complete for this routine; and its entry gate is an equality test against a field `TOWN-352` reads as a bitmask.** The routine spans `0x0040d1f1`-`0x0040dcbc` with a single `RET`. The extracted predecessor set gives `0x0040dcb9`, the epilogue, exactly four explicit predecessors -- `0x0040d24c JMP` (the entry gate), `0x0040d2e7 JMP` (the failed `PtInRect` at `0x0040d2d2`), `0x0040dc98 JZ` (the selection local still zero) and `0x0040dcaf JZ` (the local's `+0x4` already equal to `[0x005cd794]` as sampled at `0x0040d251`) -- plus the fall-through from `0x0040dcb4 CALL 0046d3d0`, which is the path that does call the adapter. Those are exactly the four `AI-CURSOR-193` names for this routine, and the enumeration is complete because the routine has one exit instruction and its predecessor set was extracted mechanically. **The routine sets the cursor itself**: `0x0040dcb4` is the set-cursor adapter (`AI-CURSOR-172`), and no caller is needed to turn the routine's result into a drawn cursor. `-fnof` gives four direct callers -- `0x0040cc51`, which it attributes to `0040b726`, an address inside `FUN_00407b1a`, whose span `TERR-SPR-137` gives as `0x00407b1a`-`0x0040cd6f`; that attribution is the false-boundary artefact `AI-CURSOR-205` describes, and the call site belongs to the map-view frame routine; and `0x0040ed9f`, `0x0040ee1b` and `0x0040f10e`, attributed by the same `-fnof` query to `FUN_0040eaee`'s nearest called start, which also contains every message-handler address `AI-CURSOR-177` lists for the map view's private dispatcher. **Amended by `EXP-0219` (`AI-CURSOR-235`): `0x0040ed9f` and `0x0040ee1b` are not inside `FUN_0040eaee`.** `FUN_0040eaee` is a separate routine, `0x0040eaee`-`0x0040ec2f`, with its own `PUSH EBP` and a `RET` at `0x0040ec2f`; the map view's private dispatcher begins at the next instruction, `0x0040ec30`, with its own `PUSH EBP` and its own `RET` at `0x0040ee91`. `0x0040ed9f` and `0x0040ee1b` are inside that second routine, past `FUN_0040eaee`'s `RET`. `FUN_0040eaee` has exactly one direct `rel32` caller (`0x0040dd9a`); the dispatcher has none and is reached only through an `.rdata` vtable slot (`0x005970d0`), so `-fnof`'s nearest-directly-called-start heuristic has no entry for the dispatcher and reports `FUN_0040eaee` instead, across the clean `RET` boundary between them. `0x0040f10e` is also not inside `FUN_0040eaee`: it is past the same `RET`, inside a third routine, `0x0040f0a0`-`0x0040f2db`, that `EXP-0219` (`AI-CURSOR-235`) locates but does not read. None of the four call sites was read here, so what decides *when* the routine runs is not established. That scan finds direct relative calls only. The entry gate is `0x0040d243 CMP [sess+0x3dc],0x1` with `JZ`; `sess` is the main-frame object (`SESS-OBJ-001`) and `TOWN-352` establishes `campaign+0x3dc` as a bitmask whose bit 1 is the shop, set without clearing bit 0 when a shop is opened from a mission. An equality test against 1 therefore makes the whole routine inert whenever any bit above bit 0 is set | High for the exit set. The live alternative excluded is a fifth no-adapter exit reached by a path the explicit-edge extraction cannot see: the routine has one `RET`, so every exit passes `0x0040dcb9`, and the only fall-through into it is from the adapter call. **Medium** for the consequence drawn about the shop, which rests entirely on `TOWN-352`'s bitmask reading and on that row's own statement that bit 0 is not cleared; no state of `sess+0x3dc` was observed here | ● active (amended) | [EXP-0218](../experiments/EXP-0218-hover-cursor-cascades/), [EXP-0219](../experiments/EXP-0219-mapview-panel-messages/) |
| AI-CURSOR-234 | **The map view's dispatcher arms for `0x40e` and `0x40f` each call one of two AddChild/RemoveChild toggle routines, gated on the same child-existence predicates `TOWN-347`/`TOWN-351` already read for the widget's own paint logic, and the two arms are the same shape with two structural asymmetries.** `AI-CURSOR-177` resolves `0x40e` to `0x0040ed5d` and `0x40f` to `0x0040edd9`, inside the map view's private dispatcher (`0x0040ec30`-`0x0040ee91`, its own `PUSH EBP`/`RET`, byte table at `0x0040eeb8`, dword table at `0x0040ee94`); this experiment reads both arms and the four routines they call to each routine's own `RET`. Arm `0x40e`: `CALL FUN_0041ddcf(this,2)` (`0040ed60`) tests presence of `[sess+0xe8]` as child id 2 of `this`; TRUE calls `FUN_0041dd1f` (`0040ed6c`, removes it), FALSE calls `FUN_0041dc6a` (`0040ed76`, adds it). Arm `0x40f`: `CALL FUN_0041ddea(this,3)` (`0040eddc`) tests presence of `[sess+0xec]` as child id 3, through the host redirect `TOWN-347`/`AI-CURSOR-229` already document (`[sess+0xf0]` when `sess+0x3dc & 2`, else `this`); TRUE calls `FUN_0041dfa6` (`0040ede8`, removes it), FALSE calls `FUN_0041de5d` (`0040edf2`, adds it). `FUN_0041ddcf` and `FUN_0041ddea` are the same two routines `TOWN-347` names for the widget's own art-switch predicates: one predicate pair drives both the widget's paint state and the map view's own child presence. Each of the four toggle routines: gets the session object via `FUN_00573196`+`CALL [EAX+0x7c]` (`SESS-OBJ-001`); adds or removes `[sess+0xe8]` as a child of `this` (`0x40e` pair) or `[sess+0xec]` as a child of `this` or of `[sess+0xf0]` under the same host-redirect test (`0x40f` pair), through `FUN_004bcd19`/`FUN_004bccf4` (`RemoveChild`/`AddChild`, `TOWN-344`); **amended by [EXP-0230], selects registry slot 7 (`ibook`) through `[0x005f1b84][7]` and passes that sample in `ECX` to `FUN_00453b08`; the separately pushed `0xdc` is the terminal's priority/category byte, and rects D/E/F instead select slot 1**; and calls `FUN_0041e06d` (`SESS-VIEW-028`'s viewport-row recompute for child id 2 or 3). Three of the four also reposition `[sess+0xec]` (child id 3) via `FUN_004bcba8` (the `SetRect` forwarder `TOWN-184`/`TOWN-222`/`TOWN-232`/`TOWN-280` establish), computing the destination from `this+0x10`/`this+0x14` offset by one of three constants (`0x55`, `0x5a`, `0xaf`); `FUN_0041dfa6` does not reposition anything. In `FUN_0041dd1f` and `FUN_0041dc6a` (the `0x40e` pair) the reposition is gated on `FUN_0041ddea` returning non-zero and is skipped when it returns zero. In `FUN_0041de5d` (the `0x40f` "absent" routine) the `FUN_0041ddcf` test at `0041de97` selects only which offset constant applies (`0x5a`/`0xaf` versus `0x55`); the reposition itself runs on both of that test's branches. That row's own four callers, named only generically ("the open/close pairs the WindowProc runs for messages 0x40e/0x40f"), are exactly `0041ddc6`, `0041dd16`, `0041e064` and `0041df9d`, one per toggle routine. Two asymmetries beyond the toggle direction. First, only the `0x40f` "present" routine (`FUN_0041dfa6`) applies the host redirect to its own RemoveChild target; the "absent" routine (`FUN_0041de5d`) has one `[sess+0x3dc]&2` test (`0041defa`-`0041df05`), whose single taken branch both forces `[sess+0xec]`'s own rect to a fixed literal (operands `0x131`, `0x1e0`, `0x186`, `0`, not computed from `this`) and adds it under `[sess+0xf0]`; the not-taken branch adds it under `this` directly, with no literal-rect force; the `0x40e` pair applies no host redirect anywhere. Second, only the two `0x40f` routines additionally post message `0x408` to `[sess+0xd4]` through `vt+0x48` (`0041e045`-`0041e05e` and `0041df7e`-`0041df97`); neither `0x40e` routine posts anything past `FUN_0041e06d`. `sess+0xd4` is stated here only as the mechanical post target; `TOWN-086` names `campaign+0xd4` as the right-hand column's own container (a different constructor than the widget), and reconciling that with `FUN_0041e06d` reading session child-presence off the same object is outside this experiment | High for every call site, predicate identity, toggle target, reposition mechanism, `FUN_0041e06d`'s four callers and the two structural asymmetries -- all instructions are read on both roots (byte-identical) to each routine's own `RET`, and the alternative that the two pairs differ only in operand values is excluded by the added `PUSH 0x408`/`CALL [vt+0x48]` sequence and the doubled host-redirect test, both control-flow differences rather than value differences. This raises `TOWN-347`'s own Medium clause ("0x40e and 0x40f toggling the presence of child id 2 and child id 3... inferred from the widget's own paint predicate and not read from the map view's handler") to High for the toggling itself, now read from the handler directly. **Medium** for the game identity of `sess+0xe8`/`sess+0xec`: `TOWN-356` reads rect A's (`0x40e`, child id 2) icon pair as the open/closed backpack and rect B's (`0x40f`, child id 3) as the open/closed journal, and `SESS-VIEW-028` names its own four callers as these same four toggle routines, and separately states that mission entry itself invokes those routines through two other addresses (`004781ae`, `004781c4`) to restore "the store's `Inventory/IsOpen` and `SpellBook/IsOpen`"; the two published rows use different words for what is structurally the same object pair, and neither decodes the child class itself (`SESS-VIEW-028`'s own Medium). Unknown for what `this+0x10`/`this+0x14` are and for what message `0x408` does at `sess+0xd4`; neither was read past its own use site here | ● active (amended) | [EXP-0219](../experiments/EXP-0219-mapview-panel-messages/), **[EXP-0230](../experiments/EXP-0230-non-music-sfx-events/)** |
| AI-CURSOR-235 | **`FUN_0040eaee` is a distinct routine from the map view's private dispatcher, not part of it, and it writes `this+0xdc=1` and `this+0xe0=1` unconditionally at its own entry, roughly once every 32 ticks of message `0x401`. This corrects `AI-CURSOR-233`'s attribution of two call sites to `FUN_0040eaee`.** A scan of the declared PE sections for the dword `0x000000E0` (`corpus-scan-e0.txt`) gives 278 hits; over the complete file, including header, padding and overlay bytes, the same dword occurs 287 times (`-mode rawfull`), the 9 extra being bytes that cannot be an operand. Of the 278, 143 carry a preceding byte in the ModRM mod=10 range (`0x80`-`0xBF`), the disp32 register-indirect form; 20 of those 143 are the `MOV r32,imm32` opcode byte (`0xB8`/`0xB9`/`0xBA`/`0xBB`/`0xBD`/`0xBE`, a false positive, not a genuine ModRM byte), leaving 123. One of those 123 is `TOWN-347`'s own already-published write, whose ModRM byte is at `0x004915c4` (`[EBX+0xe0]=1`; the dword hit itself, its disp32, is at `0x004915c5`). A second, `0x0040eb0c`, sits inside the address range `AI-CURSOR-233` attributes to `FUN_0040eaee`. Read from its own `PUSH EBP` at `0x0040eaee`, the routine caches `this` at `[EBP-0x2c]` and, with no message-id test first, executes `MOV dword ptr [EAX+0xdc],0x1` (`0040eafd`) and `MOV dword ptr [ECX+0xe0],0x1` (`0040eb0a`), both through `this`. It continues: a loop clearing bit `0x4000` from every word of the tile plane `[[this+0x80]+0xc]` over `[this+0x84] * [this+0x88]` entries (`0040eb39`-`0040eb4c`; `this+0x80`/`this+0x84` are `AI-CURSOR-232`'s tile plane and row stride); a conditional walk of a list at `this+0x9b8`, gated on `this+0x9c4 != 0`, calling `[[entry]+0xc]`'s `vt+0x48` on each non-null entry and looping back to its own gating test at `0x0040eb5e` (`JMP 0x0040eb5e` at `0040ec24`), not to the tile-clear loop, which is self-contained at `0040eb44`-`0040eb4c` and is never re-entered; and its own `POP EDI/ESI/EBX; MOV ESP,EBP; POP EBP; RET` at `0x0040ec29`-`0x0040ec2f`. **This `RET` ends the routine.** The very next instruction, `0x0040ec30`, is a separate `PUSH EBP` beginning the map view's private dispatcher `AI-CURSOR-177` documents (byte table `0x0040eeb8`, dword table `0x0040ee94`, own `RET` at `0x0040ee91`, already read in full by this experiment for the `0x40e`/`0x40f` arms, `AI-CURSOR-234`). `0x0040ed9f` and `0x0040ee1b`, the two addresses `AI-CURSOR-233` gives as call sites of `FUN_0040d1f1` "inside `FUN_0040eaee`", are inside this second routine, not inside `FUN_0040eaee`: both lie past `0x0040ec30`'s `RET`-terminated boundary and are read directly in `disasm-dispatch-arms.txt`. `-mode callto` gives `FUN_0040eaee` exactly one direct `rel32` caller, `0x0040dd9a`, and 0 raw dword occurrences; the same query on `0x0040ec30` gives 0 `rel32` callers and one raw dword occurrence, an `.rdata` slot at `0x005970d0` -- the dispatcher is reached only through that slot, never by a direct `CALL`. `AI-CURSOR-233`'s own tool (`-fnof`, nearest directly-called start) has no entry for `0x0040ec30` and reports the nearest routine that IS directly called, `FUN_0040eaee`, across the clean `RET` boundary between them -- the same false-boundary class `AI-CURSOR-233` names for `FUN_00407b1a`/`0x0040cc51`. `0x0040dd9a` is inside `FUN_0040dcdd`, msg `0x401`'s own dispatcher arm (`AI-CURSOR-177`'s base message): it increments a tick counter at `[this+0xa70]`, and calls `FUN_0040eaee` only when `[0x005eb588] == 0` and `([this+0xa70] & 0x1f) == 0` -- once every 32 calls to that arm. `0x0040f10e`, `AI-CURSOR-233`'s third address in the same sentence, is not inside `FUN_0040eaee`: it is past `FUN_0040eaee`'s own `0x0040ec2f` `RET`. It is inside a separate routine, `0x0040f0a0`-`0x0040f2db`, its own `PUSH EBP`/`RET 0xc` pair, read with no intervening `RET` before `0x0040f10e`; this experiment did not read that routine's body past locating its boundary | High for `FUN_0040eaee`'s own span, its two unconditional writes, its one direct caller, the dispatcher's separate span and its own `RET`, the correction to `AI-CURSOR-233` for `0x0040ed9f`/`0x0040ee1b`, and that `0x0040f10e` is not inside `FUN_0040eaee` -- every boundary is a `PUSH EBP`/`RET` pair read at instruction level on both roots (byte-identical), and the `callto` asymmetry (one direct caller for `FUN_0040eaee`, zero plus one vtable slot for the dispatcher) is the same instrument used for the sibling case in `AI-CURSOR-233`. High for the msg-`0x401`/32-tick trigger, read directly in `FUN_0040dcdd`. Medium that `0x0040f10e`'s containing routine is `0x0040f0a0`-`0x0040f2db`: its own `PUSH EBP`/`RET 0xc` pair is read with no intervening `RET` before `0x0040f10e`, but its body was not read past locating the boundary. Unknown for the tile-clear loop's and the list-walk's game effect, and for what `this+0xdc` and `this+0xe0` mean together as a pair given this second, periodic writer -- not read past the instructions cited | ● active | [EXP-0219](../experiments/EXP-0219-mapview-panel-messages/) |
| AI-CURSOR-236 | **No reader of the map view's own `+0xe0` field was found. Two writers are now known -- `TOWN-347`'s rect C (`0x004915c3`) and `FUN_0040eaee`'s unconditional entry write (`0x0040eb0a`, `AI-CURSOR-235`) -- and this experiment traced neither writer to a consumer.** Searched: the map view's private dispatcher, `0x0040ed5d`-`0x0040ee91` (`disasm-dispatch-arms.txt`); this listing does not reach the dispatcher's own `0x0040ec30` start, and 86 bytes inside the dispatcher, `0x0040ed07`-`0x0040ed5c` (the `0x406` arm's tail and the whole `0x40c` arm), are in no committed listing -- the dword scan below still covers that gap; `FUN_0040eaee`, `0x0040eaee`-`0x0040ec2f` (`disasm-toggle-and-eaee.txt`, `AI-CURSOR-235`); the four `0x40e`/`0x40f` toggle routines and `FUN_0041e06d` (`disasm-toggle-routines.txt`, `disasm-predicates-and-tail.txt`, `AI-CURSOR-234`) -- `FUN_0041e06d`'s own listing stops at instruction 80 of 84 (instruction-budget exhaustion) and does not reach its own `RET`; the widget's own paint routine and the base Control dispatcher, both already committed by `EXP-0210` (`experiments/EXP-0210-figure-panel-controls/evidence/listing-00491930-paint.txt`, `listing-004bd9dc-dispatch.txt`); and the first 250 instructions of the map view's own constructor, `0x004023c6`, which does not reach its own `RET` (`disasm-e0-search.txt`) -- so the constructor's own tail past that budget is not covered. A case-insensitive scan of every disassembly listing this experiment produced for the byte sequence `e0]` (a `+0xe0` memory operand written out by the tool) finds exactly two occurrences: the write at `0x0040eb0a`, and `[ESI+0xe0]` at `0x0047664e`, which is `sess+0xe0`, a different object -- `ESI` in that routine also reads `sess+0x3dc`, `sess+0xcc`, `sess+0x35c` and `sess+0x41c` -- not a read of the map view's field. Beyond the disassembly, a scan of the declared PE sections for the dword `0x000000E0` gives 278 hits (287 over the complete file, `-mode rawfull`; the difference is header, padding and overlay bytes), 123 with a preceding byte in the ModRM mod=10 range after excluding 20 `MOV r32,imm32` opcode bytes; three were traced to completion (`TOWN-347`'s write, `FUN_0040eaee`'s write, and the `sess+0xe0` false lead above); the remaining 120 were not traced (`corpus-scan-e0.txt`). `go run ./tools/claim -k` was run for `mapview\+0xe0`, `CMapView\+0xe0`, `sess\+0xe8` and `sess\+0xec` before this statement; none returns a row naming a reader of `+0xe0` (`corpus-searches.txt`) | Unknown, scoped to the search stated. The population is not exhausted: the map view's constructor tail past instruction 250 and 120 of 123 raw-scan candidates were not read, so a reader may exist outside the routines this experiment checked | ● active | [EXP-0219](../experiments/EXP-0219-mapview-panel-messages/) |
| AI-CURSOR-242 | **Both `pickup` loads in `FUN_0040d1f1`'s ordinary-hover cascade are gated by the identical stack slot, `[EBP-0x68]`, and that slot's own construction reads no dedicated "sack" bit: three ANDed tests ending in a two-armed OR over an already-named mask bit and an already-named hit-object field.** The `mask & 0x23` arm's `pickup` load, `AI-CURSOR-226`'s `0x0040da2f`, is reached from the arm's own entry `0x0040da01` through two sibling tests: `CMP [EBP-0x20],0x0` / `JZ 0x0040da18` at `0x0040da08` (Alt, shared with this arm's own `move` pick, `0x0040da0e`), then `CMP [EBP-0x30],0x0` / `JZ 0x0040da29` at `0x0040da18` (Ctrl, shared with this arm's own `attack` pick, `0x0040da1e`), then `CMP [EBP-0x68],0x0` / `JZ 0x0040da3a` at `0x0040da29`, whose non-zero side loads `pickup` (slot 8, `0x005ef9d4`, `SPR16A-CURSOR-067`) at `0x0040da2f` and stores it at `0x0040da35`. `AI-CURSOR-226` also names `0x0040da55` for the routine's other `pickup` arm, "empty ground"; that address is the arm's own entry (`CMP [EBP-0x30],0x0`, Ctrl tested first here, the opposite order from the `mask & 0x23` arm), not a `pickup` load. That arm's own `pickup` load is `0x0040da7c` (`MOV ECX,[0x5ef9d4]`, stored `0x0040da82`), reached from `0x0040da55` through `CMP [EBP-0x30],0x0` / `JZ 0x0040da66` (Ctrl, this arm's own `swarm` pick, `0x005ef9bc`, `0x0040da5b`), then `CMP [EBP-0x20],0x0` / `JZ 0x0040da76` at `0x0040da66` (Alt, this arm's own `move` pick, `0x0040da6c`), then `CMP [EBP-0x68],0x0` / `JZ 0x0040da87` at `0x0040da76`. Both `pickup` tests read the identical `EBP-0x68` offset: the committed listing gives exactly eight accesses to that offset across this range -- three writes (`0x0040d900`, `0x0040d924`, `0x0040d973`) and five reads (`0x0040d903`, `0x0040d909`, `0x0040d927`, `0x0040da29`, `0x0040da76`) -- with no writer between the last write and either arm's `CMP`, and no `LEA` instruction anywhere in the committed listing that could alias a pointer into it, so one computed value governs `pickup` in both arms; only the Alt/Ctrl priority order and their own non-`pickup` targets differ. `[EBP-0x68]`'s own construction, `0x0040d8c2`-`0x0040d973`, runs once before either arm. It starts as a copy of `[EBP-0x104]` = `[view+0x140]==1` (`0x0040d8c8`) AND `[view+0x144]&0x1` (`0x0040d8dd`) -- exactly one object selected and that object's class name is `CUnit` (`AI-PANEL-061`) -- stored at `0x0040d8fa`/`0x0040d900` (`AI-CURSOR-209` reads the same `[EBP-0x104]` copied one instruction later into its own composite `[EBP-0x64]`, `0x0040d906`). If that copy is zero, `CMP [EBP-0x68],0x0` / `JZ 0x0040d976` at `0x0040d909` leaves `[EBP-0x68]` at zero. Otherwise `[EBP-0x68]` is overwritten with `[[view+0x138]+0x18c]&0x1` (`0x0040d915`-`0x0040d921`, stored `0x0040d924`) -- the selected object's own field, the same field and bit `AI-CURSOR-209` cites for its `town` composite. If that is zero, `CMP [EBP-0x68],0x0` / `JZ 0x0040d976` at `0x0040d927` again leaves `[EBP-0x68]` at zero. Otherwise `[EBP-0x68]` takes a third value built at `0x0040d92d`-`0x0040d973`: `CMP [EBP-0x60],0x40` / `JZ 0x0040d963` sets it when the whole hover hit-test mask (`AI-CURSOR-231`'s `[EBP-0x60]`) equals exactly `0x40` -- no other of its seven bits set, so no `CUnit`/`CAirUnit`/`CStructure` was hit; otherwise it sets `[EBP-0x108]` (stored into `[EBP-0x68]` at `0x0040d973`) when `[view+0x98c] == [view+0x138]` (`0x0040d93f`-`0x0040d94b`, the hover hit-test's own hit-object pointer, `UNIT-HOVER-020`/`AI-CURSOR-231`, equals the selected object) AND `[EBP-0x60]&0x40` (`0x0040d94d`-`0x0040d955`), else zero. So `pickup`, in either arm, requires exactly one selected `CUnit`, that object's own `+0x18c` bit `0x1` set, and either the hover mask equal to exactly `0x40`, or the hit object is the selection itself with mask bit `0x40` also set -- on top of the two gates `AI-CURSOR-226` publishes that this whole cascade already requires to be reached at all: `[view+0x144]&0x24` clear (`0x0040d89d JZ 0040d8c2`; set, it picks `select`/`default` and leaves for `0x0040da90`) and mask bit `0x4` clear (`0x0040d9c9 JZ 0040d9fe`; set, it routes to the `move`/`select`/`attack` block instead). The mask-equal-`0x40` sub-case fires only in the empty-ground arm (it requires `mask&0x23==0`, that arm's own gate); the `mask & 0x23` arm's `0x0040da2f` load can only be reached through the hit-object-identity sub-case, which requires `[view+0x98c]` -- `UNIT-HOVER-020`'s own "the hit", singular -- to equal `[view+0x138]`, the object this composite's own first refinement already confirms a `CUnit`. `AI-CURSOR-209`'s `town` composite `[EBP-0x64]` instead requires mask bit `0x800`, which `AI-CURSOR-231` sets only inside the class test's `CStructure` arm of `FUN_0041ab8e`. Whether that arm's own object-class assignment (`0x0041aee0`-`0x0041afe8`) excludes `0x800` from ever coexisting with a `CUnit` identity match is not read by this experiment, so the `0x0040da29`-before-`0x0040da3a` test order's freedom from a real conflict between `pickup` and `town` rests on `UNIT-HOVER-020`'s singular-hit wording rather than on an excluded alternative. Bit `0x40` and the field `[view+0x98c]` are both already named (`AI-CURSOR-231`, `UNIT-HOVER-020`); the routine reads no bit and no field beyond what those rows already name, and has no dedicated "sack" test. `PARTY-FLAG-003` ([EXP-0077](../experiments/EXP-0077-party-and-save/)) reads `CUnit+0x18c` bit `0x1`, the same field `AI-CURSOR-209` cites for its `town` composite, as the player-character flag, at Medium; the earlier "hero" gloss for the same bit is itself refuted in that row | High for the shared-slot fact, the `[EBP-0x68]` construction and both arms' full instruction sequences: one continuous listing, `evidence/disasm-listings.txt`, `0x0040d84d`-`0x0040da90`, extracted independently for this experiment and byte-identical to `EXP-0218`'s own committed listing over the same range; the manifest gives both preserved roots the same `rom.exe` image (`EXP-0123`), so the bytes disassembled do not depend on which root is scanned. The live alternative excluded for the shared-slot High is that the two `CMP [EBP-0x68],0x0` tests read two different locals sharing a coincidental offset; excluded because the committed listing gives a complete, continuous account of every access to that offset across the whole span -- eight accesses, three writes and five reads, with no writer between the composite's last write and either arm's `CMP` -- and contains no `LEA` instruction anywhere that could alias a pointer into the slot. This experiment extracted no predecessor set of its own, so an edge entering the span from outside `0x0040d84d`-`0x0040da90` is not excluded by this evidence. Medium for the `pickup`/`town` mutual-exclusion argument: `UNIT-HOVER-020` states the routine stores "the hit" in `view+0x98c`, singular, and `AI-CURSOR-231` sets mask bit `0x800` only inside the class test's `CStructure` arm, so a hit `CStructure` and the identity sub-case's required `CUnit` match are unlikely to be the same evaluation, but this experiment does not read the class-assignment block (`0x0041aee0`-`0x0041afe8`) that would exclude the alternative outright -- a second fitting model, that the walk sets `0x800` from one object while `view+0x98c` is later read as a different one, is not excluded by evidence this experiment commits. Medium for the `+0x18c` bit `0x1` reading, inherited from `PARTY-FLAG-003`'s own grade. Unknown for whether the mask-`0x40` drawable is specifically a sack: `AI-CURSOR-231` already leaves this open and this experiment does not close it | ● active | [EXP-0220](../experiments/EXP-0220-pickup-cursor-route/) |
| AI-CURSOR-243 | **`AI-CURSOR-226`'s `town` load at `0x0040da40`, in the same `mask & 0x23` arm as the first `pickup` site, is one of `AI-CURSOR-209`'s two cited `town` sites; the other is a different address, in the routine's other cascade.** `AI-CURSOR-209` cites two operand addresses for the `town` slot load: `0x0040da41` (the operand of `MOV EAX,[0x5efa14]` at `0x0040da40`) and `0x0040d833` (the operand of `MOV EDX,[0x5efa14]` at `0x0040d831`). `AI-CURSOR-226` places its own `town` load, in the ordinary-hover cascade, at `0x0040da40` -- the same instruction address and the same operand address as the first of the two. `AI-CURSOR-209`'s other site, `0x0040d831`, is not in the ordinary-hover cascade: `AI-CURSOR-226` gives that cascade's entry as `0x0040d84d`, and `0x0040d831` sits earlier, inside the routine's other cascade, in the tail `AI-CURSOR-230` gives as ending `0x0040d848 JMP 0x0040dabb`. One of `AI-CURSOR-209`'s two sites is `0x0040da40`; the other is a distinct address in a distinct cascade of the same routine | High. Both addresses are cited verbatim by the two rows compared, `AI-CURSOR-226`'s cascade boundary at `0x0040d84d` is itself graded High, and `AI-CURSOR-230`'s independently cited `0x0040d848` tail address corroborates it; the comparison needs no new disassembly, only the four already-published addresses | ● active | [EXP-0220](../experiments/EXP-0220-pickup-cursor-route/) |

## Open, written out

1. ~~**The AI state machine.**~~ — *closed by [EXP-0082]*: the switch has **27** arms, not twenty,
   and all of them are named (`AI-STATE-011`). ~~Still open from that item: how `order+8` states
   1/5/6/0xb are consumed — the pursuit itself — is unread~~ — *closed by [EXP-0099]*: all twelve
   live `ord+0x08` arms are read (`AI-ORDER-039`), 5 and 6 are the pursuit pair (`AI-PURSUE-040`)
   and `0xb` is the idle turn. It was never a `claims/move.md` area — the pursuit is decided in the
   AI module and only *executed* by the mover.
2. ~~**The group layer.**~~ — *closed by [EXP-0082]* for the parts that decide behaviour
   (`AI-TICK-008`, `AI-GROUP-009`, `AI-ORDER-010`). ~~Still open: group orders **2, 4, 5 and
   0x11**~~ — *all four read by [EXP-0087]* (`AI-SWARM-022`…`AI-ROAM-025`). Still open there: the
   candidate scorer `FUN_00535e90`, and `FUN_00535d30`, the *other* scorer~~ — *both read by
   [EXP-0112]* (`AI-COST-071`, `AI-REACH-072`), together with the builder `FUN_005365e0`
   (`AI-GROUPSEE-068`), the assigner `FUN_00536c10`/`FUN_00536d50` (`AI-SCORE-069`), the matrix they
   index (`AI-PREF-070`) and group order 1 end to end (`AI-GRPGUARD-074`). ~~**Still open there:**
   group orders 2 and 5 were read by [EXP-0087] before the scorer was, so what `Swarm` and `Swarm 2`
   do with a `0xffffff` veto was never stated; and `FUN_0053ddd0`, the fifth caller of the builder,
   is outside the AI module and unread.~~ — *order 5 closed by [EXP-0126]*: `AImgr+0xbb4` is the
   candidate collection's element count, so an empty list takes the arm out to order 4 and the
   members walk, while a list the matrix vetoes keeps it in its own body, whose two zero-target
   branches do not move (`AI-CANDCOUNT-105`, `AI-SWARM2GATE-107`). *`FUN_0053ddd0` closed by
   [EXP-0114]* (`AI-DIPLO-084`): it is the spell-cast twin of `FUN_00531ac0`, and the only writer
   of the matrix that re-scans rather than waiting for the next AI tick. **Still open there: group
   order 2**, which has no gate and is the cheaper of the two to read.
3. ~~**The line-of-sight test** `FUN_00546c70`~~ — *read whole by [EXP-0112]* (`AI-LOS-081`): it is
   an accumulator march, not a predicate on altitude. ~~Open in its place: what fills the per-cell
   step grid at `fog+0x22000` and the per-cell cost grid at `fog+0x28000` — both are read by it and
   neither was traced to a writer.~~ — *closed by [EXP-0117]*: one writer, `FUN_00546790`, run once
   from the sight object's init at world construction, from geometry and `ScanShift` alone
   (`AI-LOS-087`…`AI-LOS-091`). **Open in its place, and smaller:** `FUN_00546fa0` and
   `FUN_005470a0` — the two routines that consume the *other* grid the same init builds — were
   placed but not read, so what the per-player `u16` mask they OR into `fog+0x00000` is used for is
   unknown; ~~and the flat-ground region measured here (145 cells at sight 6) does not equal the 127
   `TERR-FOG-080` publishes for the structurally parallel client implementation at the same `k`,
   which is either a real divergence between the two or a defect in one of the two readings.~~ —
   *adjudicated by [EXP-0120]*: 145 is right and the two are one algorithm; 127 is
   `TERR-FOG-080`'s transcription stopping four instructions short of the end of `FUN_00403718`
   (`TERR-FOG-117`, `AI-SIGHT-093`).
4. ~~`actor+0x4c` bit 2 and `actor+0x0e`~~ — *both settled by [EXP-0112]* (`AI-GATE-079`); bit 2 had
   been answered in `claims/hero.md` for two experiments and no AI row cited it.
5. Whether `FUN_00532e60`'s reach overwrite is reachable outside a multiplayer session — *narrowed
   by [EXP-0112] rather than closed: its own `actor+0x0e == 0x18` gate names 57 shipped `Humans`
   rows, so the gate is not the thing that makes the arm unreachable; `AI-STATE-043`'s missing
   writer for state `0x17` is.*
6. **`grpAI`'s unread fields.** `+0x30`/`+0x34` are a latch and a counter the guard arm advances;
   `+0x08`, `+0x09`, `+0x0a`, `+0x3c` are written but their consumers were not traced; nothing was
   found that ever clears `+0x45`. ~~`+0x24`, `+0x2a`, `+0x2b`~~ — *sourced by [EXP-0094]*: they
   are `FUN_00533210`'s outputs, the group's **fine centroid** (`+0x24`), its **packed centroid
   cell** (`+0x28`), the **maximum Chebyshev spread** from a member to it (`+0x2a`), the maximum
   member `actor+0xa5` sight (`+0x2b`) and the maximum of the two summed (`+0x2c`). Only the
   centroid pair has a traced consumer — the formation gate (`MOVE-GATE-035`) — so what reads the
   three maxima is still open.
7. ~~**`actor+0x54`.** … The full value set is unknown.~~ — *closed by [EXP-0090]*: the actor tick
   `FUN_004f37be` dispatches on `actor+0x54 − 1` over a **15**-byte index at `0x4f41d9` into 6
   dwords at `0x4f41c1`, so the consumed set is `1` → `004f39bf`, **`2` → the pick-up**
   (`ITEM-PICK-016`), `3`/`0xd`/`0xe` → `004f39e8`, `0xf` → `004f3ec5`, and `4..0xc` on the
   default — nine of fifteen empty. Beside those, `0x10` is death (`ITEM-DEATH-012`, and
   `SESS-TICK-006`'s skip) and `0x1a` this machine's own arm `0x1a`. `FUN_005310e0` **preserves**
   only 2 and `0xf` across a tick and clears everything else (`00531136`…`00531147`). Still open:
   what the arms for 1, 3/0xd/0xe and 0xf do.
8. ~~**The mission description file.** `World\Mission\<n>.ini` is the only surface that authors a
   behaviour~~ — *closed the wrong way by [EXP-0083]: it is not the only surface, and it is the one
   that does not ship. The `.alm`'s own type-7 script authors group behaviour on 20 campaign maps
   (`AI-GROUPCMD-020`) and patrol on 8 of them (`AI-PATROL-017`).* Still open from that item: the
   `.ini`'s grammar past `Patrol` / `StandGround`, which cannot be measured because none ships.
9. ~~**The four unread group arms.**~~ — *closed by [EXP-0087]*: `AI-SWARM-022`, `AI-MOVE-023`,
   `AI-SWARM2-024`, `AI-ROAM-025`. Opened in their place, each narrow: ~~what fills
   `AImanager+0xbb4`, the gate that decides whether order 5 is `Swarm 2` or `Move`~~ — *closed by
   [EXP-0126]: it is the candidate collection's element count, so the gate is "did this group see
   anything" and the fallback is the ordinary path (`AI-CANDCOUNT-105`, `AI-SWARM2GATE-107`). Still
   open from that item: how often the branch is taken in a shipped mission, which only a running
   original can say — the prediction is written out in that experiment*; whether
   `Roam`'s re-roll loop can fail to terminate for a group cornered against the map rectangle;
   what fills `pth+0x08`, the `Wimpy` radius; ~~whether `actor+0x50 = 0x16` — the withdraw's own
   state-machine arm, table slot 22 — has a register-form writer~~ — *closed by [EXP-0099]:
   exactly one, `00534f2d` with `EBP = 0x16` from `00534ea1`, inside `FUN_00534e20`, the player's
   order `0x14` (`AI-STATE-043`)*; and the entry point and reachability of the orphan routine at
   `005382ed..005384b2`, which writes `grpAI+0x20 = 2` (`AI-ORDER-031`).
10. **`Target_Group` when one id has two owners.** `AI-GROUP-009` keys a runtime group on
    (owner, groupId); the script names a group id alone. One shipped patrol node (`scn:100` group 25)
    names an id carried by two owners, and which runtime group the resolver returns was not read.
11. **`order+0x00` for a group-guard member.** `AI-GUARD-012` reads the per-actor arm, which sets the
    post to the current cell when it is 0; the group arm `FUN_00536ef0` has no such initialisation and
    its walk-home writes `order+0x0a = order+0x00` (`005370a5`). Where that sends a member whose post
    was never set is unread.
12. **The order object's own bytes.** `FUN_005310e0` dispatches on `ord+0x09` and `ord+0x08`
    (`AI-PROGRESS-034`) and the whole object moves as a raw `0x94`-byte block in a save
    (`FUN_005390c0`). ~~What the other bytes of that block are — and which of `ord+0x09`'s four live
    values means what — is unread; the twelve live `ord+0x08` arms were classified by their calls,
    not read.~~ — *both switches read by [EXP-0099]* (`AI-ORDER-039`), and the fields they consume
    are named: `+0x00` post, `+0x02` patrol waypoint, `+0x04` patrol re-anchor latch, `+0x0a` cell,
    `+0x0c` target, `+0x14` stop distance, `+0x15` attack counter, `+0x18` followed actor, `+0x20`
    group-assigned target, `+0x28`/`+0x30` spell target and spell, `+0x3c` spell cell, `+0x40`/
    `+0x44` the withdraw thresholds, `+0x70` follow leash, `+0x78`..`+0x8c` the threat cache
    (`AI-THREAT-044`), `+0x90` the patrol ring. Still open: the remainder of the `0x94` block, and
    which event fills the threat cache.
13. ~~**What enqueues a command.**~~ — *closed for the **order** space by [EXP-0109], and not by
    sweeping the pool: the producers are found from the other end, by the opcode store
    `re:MOV byte ptr \[E.. \+ 0x9\],0x`, which lands a one-routine-per-opcode builder family in
    `0x0041b620..0x0041c98f`. Every order opcode has exactly one builder, the map click chooses
    among them by cursor (`AI-CLICK-050`) and the command panel issues three of them directly
    (`AI-PANEL-053`).* Still open: the **session** space, and with it whether the source-3 pick-up
    is ever emitted; and `FUN_0048fdb0`, a second surface reaching five of the same builders, which
    was not read.
14. **A fault, stated not observed.** `FUN_0052d940`'s ring search ends `0052d9d6 XOR EAX,EAX` /
    `0052d9d8 MOV EAX,dword ptr [EAX]`, a null dereference when `order+0x02` is not a member of
    `order+0x90`. Neither live setter can produce that state; the one that would is dead
    (`AI-PATROL-019`).
15. **The formation mode's third state.** `AI-FORM-037` shows that a value other than `0` or `2`
    puts a group in formation with **no** spread test, and that the trigger instant can author one.
    Whether the game's own interface can reach it — and what the shipped catalogue's `Par0`/`Par1`
    labels mean against a runtime that takes the value from `Par0` — are interface- and
    editor-layer questions, not questions about these routines.
16. **What reads `grpAI+0x2a`/`+0x2b`/`+0x2c`.** `FUN_00533210` publishes a group's maximum spread,
    maximum member sight and their sum on every call, and `EXP-0094` traced a consumer only for the
    centroid pair.
17. **What fills the threat cache.** `AI-THREAT-044` reads `ord+0x78`/`+0x7c`/`+0x80` and their
    companion dwords `ord+0x84`/`+0x88`/`+0x8c` on the *consume* side only. Which event writes a
    slot, what value it sets, and whether a slot is ever cleared early, are unread — and they
    decide whether an actor that has just been sent home can turn round on the next tick.
    *Narrowed twice by [EXP-0109] and closed by neither: the companion dwords are **not** expiry
    stamps but thresholds against `rand()` (`AI-RAND-058`), and whole-image `disp:78` / `disp:84`
    find no access to either field anywhere in the AI module — so the writer, if there is one,
    reaches the block through a pointer or a wholesale copy.*
18. **The five live `actor+0x50` arms with no writer.** `4`, `0x17`, `0x18`, `0x19`, `0x1a`
    (`AI-STATE-043`), `4` being written only from inside arm `0x18`. Two of them write hard-coded
    map cells (`AI-GUARD-012`), which reads as leftover test code — but the sweep is store-form and
    cannot see a state arriving from a save, so *argued* unreachable, not *shown*.
19. **Tick order inside one full tick.** `FUN_0052ce50` runs from the group AI slot and
    `FUN_005310e0` from the actor tick. Which comes first decides whether a rewritten `ord+0x08`
    is executed on the same tick or the next, and therefore how many cells a creature overshoots
    before it turns round. `SESS-TICK-006` fixes the slot; the relative order was not read.
20. ~~**Whether the per-actor and group leashes can both be live.**~~ — *the corpus half closed by
    [EXP-0102]*. A group at order 0 runs `AI-BREAK-041`'s 5-cell post block; one at order 1 runs
    `AI-RADIUS-014`'s jittered radius about the centroid and never enters the per-actor machine
    (`AI-ORDER-010`). The census now exists: **at load the byte is never 0** — 8085 of 8094 EN
    placements are at 1 and the other 9 at 3 — and even assuming every shipped group command fires,
    the post rule reaches at most **68 of 8094** (`AI-CENSUS-046`, `AI-CENSUS-047`). A player meets
    the **centre** rule. Still open, and now load-bearing rather than a detail: **whether
    `FUN_004e3591`'s `server+0x0c == 0` gate wraps the whole walk or only its aggressive branch** —
    if the whole walk, a multiplayer load leaves every group at the constructor's 0 and the answer
    inverts.
21. **The three cursor gates.** `[0x005eb558]`, `[0x005eb55c]` and `[0x005eb560]` decide, before any
    property of the hovered object is consulted, whether the attack, select and move cursors are
    available at all (`AI-CURSOR-052`). `FUN_00475780` writes one value to all three;
    `FUN_00472b80` sets two and `FUN_00473070` clears all three. No caller of any of them was read,
    so whether they are a screen mode, a key state or a lobby flag is unknown — and every clause in
    this ledger that stands on the cursor is capped at Medium until one is.
22. **The selection capability mask** `FUN_00419e7b`. It decides which of the eight armed modes a
    given selection may enter (`AI-PANEL-053`), so it is what actually answers "can this unit be
    ordered to attack". It was located, not read.
23. **`view+0x9b4` against `actor+0x14`.** `FUN_0041a2d5` compares an actor's owning `Player`
    (`0x70` bytes) with the view-side player record (`0x48` bytes) at `0041a4bb`. Both readings are
    established at instruction level and they cannot both be the same object
    (`UNIT-VPLAYER-021`); which one that comparison is really made against is unread.

Items 24 to 28 were filed on 2026-08-15 from the *AI's own machine* door in
`docs/DOORS-OPEN.md`, when that door was retired to `docs/DOORS-CLOSED.md`. The wording is the
door's own.

24. **What fills `actor+0x12c`.** The field decides which branch of both scorers a creature takes,
    the melee row `[member][cand]` or ranged row 0, and only the melee rows carry the flier veto
    (`AI-PREF-070`, `AI-REACH-072`). It was never traced, so `AI-FLIER-073`'s reach is bounded by a
    field whose value space is unknown. One write is known, `00532e92` in `FUN_00532e60`
    (`AI-GUARD-007`), and it sits in a state arm `AI-STATE-043` finds no writer for, so it is not
    the ordinary source.
25. **Whether `FUN_004d214b` runs in ordinary play.** It calls the AI walk with no `% 16` gate in
    front of it, which is the one thing that would make `AI-TICK-008`'s cadence wrong. Its callers
    were not read.
26. **Which spell carries id 20.** `AI-COST-071` shows that a candidate bearing it costs a flat
    +127 to target, which removes it from selection at any distance. Which authored spell holds
    that id is unread.
27. **How often group order 5's fallback fires in a shipped mission.** [EXP-0126] separates a
    vetoed group from a blind one (`AI-CANDCOUNT-105`), and the frequency of the walking arm in
    play is not a property of the image. Only a running original can answer it.
28. **Whether a player-controlled hero is ever the subject of a server sight stamp.** That is what
    bounds the hero-truncation divergence `AI-SIGHT-094` ([EXP-0120]).
29. **What the array at minimap `this+0x9bc`/`+0x9c0` holds.** `AI-MINIMAP-156` reads the loop that
    walks it and blits one entry per iteration through the same external-object convention used for
    the terrain layers; the array's own contents were not identified.
30. **The call path from the command panel's own `0x40c` post to `FUN_0041b439`.** `AI-PANEL-158`
    shows the two routines are distinct and neither calls the other directly; `FUN_0041b439` has 14
    direct call sites in `.text` and none was traced back to a `0x40c` handler.
31. **What sets the cursor outside the mission map.** `AI-CURSOR-175`'s classify-setters census
    shows several set-cursor call sites (e.g. `0049094f`, `004922cf`, `004929b9`) in the
    `0x490000`-`0x4b9000` address range this ledger's own town/shop rows (`AI-MINIMAP-156` and
    neighbours) already work in, and a second `[current-cursor]` access cluster sits in that same
    range. Neither the containing functions nor the surface each belongs to (town, shop, school,
    tavern, main menu, dialogue, an inventory or character panel, any modal) was read in this
    experiment. A keyword search of every ledger for "town cursor"/"shop cursor" returned nothing
    at the time this item was written.
32. **The idle rule: what selects the cursor with nothing armed and the pointer over no actor.**
    `default` is registry slot 0 and the single most common target among `AI-CURSOR-175`'s 69
    classified set-cursor calls (36 of 69), which is suggestive but was not traced to a specific
    "no hit" code path. Whether a reachable no-cursor state exists, and what it shows if so, was
    not investigated.
33. **The tail-jump target `0x0042a510`.** `AI-CURSOR-172`'s decrement helper `FUN_0042a410`
    reaches it on the manager's refcount going `1->0` with `mgr+0x4` non-zero, instead of calling
    the frame-advance routine `FUN_0042a450` the increment helper calls on the opposite
    transition. Not disassembled in this experiment.
34. **The 11 untraced callers of the frame-advance gate `FUN_0042a430`, outside `FUN_0042a300`.**
    `0042a678`, `0042a91b`, `0042b129`, `0042b151`, `0042b4f9`, `0042b8ea`, `004381fd`, `00471dbe`,
    `0047a294`, `0047a4a5`, `0047a5b8` (`AI-CURSOR-172`). Whether any of these runs on a cadence
    that does not immediately follow a `FUN_0042a300` reset decides whether the cursor animator is
    ever observed to advance past frame index 1 in play.

`EXP-0213` was allocated `AI-CURSOR-172`..`AI-CURSOR-187` (16 ids) and spent
`AI-CURSOR-172`..`AI-CURSOR-178`, 7 ids. **`AI-CURSOR-179`..`AI-CURSOR-187` (9 ids) are
returned unused**, none ever reissued. The next free `ai.md` id is therefore `AI-CURSOR-188`.

`EXP-0214` was allocated `AI-CURSOR-188`..`AI-CURSOR-201` (14 ids) and spent
`AI-CURSOR-188`..`AI-CURSOR-196`, 9 ids. **`AI-CURSOR-197`..`AI-CURSOR-201` (5 ids) are
returned unused**, none ever reissued. The next free `ai.md` id is therefore
`AI-CURSOR-202`.

`EXP-0216` was allocated ids `202`..`217` of `claims/ai.md` (16 ids, one number space across
all stems) and spent `AI-CURSOR-202`..`AI-CURSOR-209`, 8 ids. **`AI-CURSOR-210`..`AI-CURSOR-217`
(8 ids) are returned unused**, none ever reissued. The next free `ai.md` id is therefore
`AI-CURSOR-218`.

`EXP-0217` was allocated ids `218`..`225` of `claims/ai.md` (8 ids) and spent all eight,
`AI-CURSOR-218`..`AI-CURSOR-225`. **No id is returned unused.** The next free `ai.md` id is
therefore `AI-CURSOR-226`.

`EXP-0218` was allocated ids `226`..`233` of `claims/ai.md` (8 ids, one number space across
all stems) and spent `AI-CURSOR-226`..`AI-CURSOR-233`, all 8. None are returned. The next free
`ai.md` id is therefore `AI-CURSOR-234`.

`EXP-0219` was allocated ids `234`..`241` of `claims/ai.md` (8 ids) and spent
`AI-CURSOR-234`..`AI-CURSOR-236`, 3 ids. **`AI-CURSOR-237`..`AI-CURSOR-241` (5 ids) are
returned unused**, none ever reissued. A returned range stays retired; the next free
`ai.md` id is therefore past it, at `AI-CURSOR-242`.
