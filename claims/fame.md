# Claim registry — FAME (famehall.dat)

Level 2 ledger. Index: [registry.md](registry.md) · spec: [`formats/fame/format.md`](../formats/fame/format.md). Legend: `✔ promoted` (also in the spec) · `● active` · `✖ retracted`. IDs are permanent.

> **Confidence re-graded by the standing sweep (2026-07-25).** Everything here rests on **one
> shipped file of ten records**, with no `rom.exe` reader read — which the scale calls a
> single-file observation. The exact self-tiling is what keeps these rows at Medium rather than
> Low. **EXP-0086 (2026-08-02) read the writer and ran the round trip the single sample could not supply**:
> `FUN_00489de0`, its `WM_DESTROY` trigger, and a play session after which the game rewrote the file
> **byte-identically**. `FAME-HDR-001`, `FAME-REC-002` and `FAME-NAME-003` are raised; see `FAME-WRITE-007`. Note also that this ledger's "6 mutants rejected" means rejected by **our own
> `validate()` contract**, the same construction as `SAV-HDR-001`: it shows the invariants are
> non-vacuous, not that a second model fails.

| ID | Claim | Confidence | Status | Evidence |
|----|-------|-----------|--------|----------|
| FAME-HDR-001 | `famehall.dat` begins with a 4-byte LE header `u32@0 = record count` (= 10). No ASCII magic, no separate version field; the record stream starts at offset 4. The count equals the 10 records that tile the file; survives count±1/zero mutants **Raised by `FAME-WRITE-007`:** `FUN_00489de0` writes `obj+0x138` — the in-memory record count — as its first four bytes (`00489df1`…`00489e03`), before the loop, and bounds the loop with the same field | **High** (the writer's own first `Write`; the count is a variable, not the constant 10) | ● active | [EXP-0012](../experiments/EXP-0012-famehall/) |
| FAME-REC-002 | Body = **variable-length, length-prefixed record stream** (not a fixed array). Record = `[u32 nameLen][name: nameLen bytes][u32 score][u32][u32]`; size `= 4 + nameLen + 12`. Stream **tiles the file exactly**: `4 + Σ(4 + nameLen + 12) == 228` (0 unaccounted, no overrun); 10/10 parse; 6 mutants rejected **Raised by `FAME-WRITE-007`:** the writer emits exactly this per record and its in-memory record is 16 bytes (`{CString* , u32, u32, u32}`, with a 16-byte record stride established at `00489e82`) — so the three trailing dwords are the record's own fields, not padding, and the variable-length shape is the writer's, not this file's | High (the tiling **arithmetic**: `4 + Σ(4 + nameLen + 12) == 228` with 0 bytes unaccounted — a closed measurement of this file) / **Medium** (that this is the *format* rather than one file's arrangement — single sample, no reader read; and the mutants test our own contract) | ● active | [EXP-0012](../experiments/EXP-0012-famehall/) |
| FAME-NAME-003 | Name field is **length-prefixed and NUL-terminated** ASCII — `nameLen = strlen + 1` (includes the trailing `\0`) in all 10 records; the literal names are omitted in this public edition, with their source-table correspondence retained in `FAME-DEFAULT-008` **Raised by `FAME-WRITE-007`:** the writer takes the MFC `CString`'s own `nDataLength` from `[ptr-8]` and **increments it** (the retained writer analysis at `00489e25`–`00489e2a` establishes this length-plus-one operation) before writing it as the prefix, then writes that many bytes — so `nameLen = strlen + 1` including the NUL is the writer's arithmetic, read off two instructions | **High** for `nameLen = strlen + 1` (the writer's own length increment) / Medium was: (the 2026-07-25 sweep's reasoning, the observation `nameLen = strlen + 1` is exact on 10/10 records, but ten records of **one file** is a single-file observation, and no consumer read shows the loader treats the prefix that way. A second `famehall.dat` — or the reader — settles it) | ● active | [EXP-0012](../experiments/EXP-0012-famehall/) |
| FAME-SCORE-004 | The `u32` after each name is a **score**; the table is sorted **strictly descending** by it (70006 → 0). Score spans **> 16 bits** (70006, 65817 exceed 65535), refuting a `u16` reading; LE `u32` read is exact | High (the **width**: two values exceed 65535, so the `u16` alternative is refuted by the bytes themselves — the one genuinely discriminating fact in this ledger) / **Medium** (that the field is a *score* and the order a ranking — an inference from monotonicity in a single, apparently untouched default table) | ● active | [EXP-0012](../experiments/EXP-0012-famehall/) |
| FAME-UNK-005 | The 8 bytes after each score (`[u32][u32]`) are all zero in this sample (80/80 bytes); a reserved/unknown block, meaning not decodable from an all-zero sample | Unknown | ● active | [EXP-0012](../experiments/EXP-0012-famehall/) |
| FAME-DEFAULT-006 | The sample is consistent with the shipped **default** table (10 seeded names, clean descending ladder ending at a `0` entry, all-zero trailing fields) — i.e. not yet modified by play. Context, not a structural claim | Low | ● active | [EXP-0012](../experiments/EXP-0012-famehall/) |
| FAME-WRITE-007 | **`famehall.dat` is written by a raw `CFile`, on `WM_DESTROY`, and shares nothing with the save path.** `FUN_00489de0(this, CFile*)` loads `[[file]+0x40]` — the `CFile::Write` slot — into `EBP` once and calls it: first `obj+0x138` as 4 bytes (the count), then per record `strlen+1` as 4 bytes, the string that many bytes, and `+0x04`/`+0x08`/`+0x0c` as 4 bytes each, striding `0x10` through the array at `obj+0x134`. **No `Asg&` header, no `CArchive`, no class record, no tag, no compression** — every one of which the save writer `FUN_004cee8f` uses. Its **only** caller is `FUN_00471ee0` (`EnumRefs callto:489de0`: 1 hit, 1 owner, 0 orphan), which opens the file `CFile::Open("famehall.dat", 0x1001, NULL)` = `modeCreate\|modeWrite` and is itself reached only through `0x00599bec` (`EnumRefs callto:471ee0`: 1 hit, 0 owners, 0 orphan) — a `pfn` slot in the six-dword message-map entry array at `0x00599bd8` whose `nMessage` dword is **2**, `WM_DESTROY`. So “written in the same minute as the last save” is the application exiting, not a shared writer. **And the round trip is byte-exact, twice**: the game rewrote the file at 00:24 and again at 00:40 with fresh mtimes, and both results hash `1845f772…1cbe9d`, **identical to the pristine EN root and to the pristine RU root** (the 00:40 copy was compared with `cmp` — 0 differing offsets over 228 bytes — and then discarded, per the corpus manifest's own addendum). That addendum asks what the game did at 00:40 that rewrites the hall of fame without changing it; this row answers it — **it exited** — a read‑modify‑write reproducing its own input, which is the test a single shipped sample could never give | **High** for the writer, the mode, the trigger and the record arithmetic (one routine read whole; the `CFile::Write` slot is loaded once and used for every field; both caller sets are `EnumRefs callto:` with 0 orphan; the message-map record is read out of `.rdata`) / **High** that the shipped bytes survive a round trip (three sha256 over three copies, one of them written by the game) / **Unknown** what the three per-record dwords mean beyond `FAME-SCORE-004`'s width — the writer names nothing, and nothing in this session changed the table | ● active | [EXP-0086](../experiments/EXP-0086-saves/) |
| FAME-DEFAULT-008 | **The shipped `famehall.dat` is byte-identical on both roots, and its ten names are ten lines of the localised UI string table that the Russian build left in English.** The file is **228 bytes with the same sha256 on `en` and `ru`**, a leading u32 record count of **10**, and ten length-prefixed names on which `nameLen = strlen + 1` holds 10 of 10 (`evidence/fame-roots.txt`) — an independent second file for `FAME-NAME-003`, which had only one. No name carries a byte >= 0x80. The ten names are **exactly** lines 263..272 of `main.res::text/main.txt`, same set and same order (`TEXT-STRTAB-023`); and those ten lines are ten of the **seventeen** lines out of 274 that the RU table does not change, the other seven being empty or structural. So the hall of fame is not localised on either side of the boundary, and one of the two copies is redundant. Two models fit: the string table seeds the file when none exists, or the table's copy draws the ten rows and the shipped file merely agrees. Nothing read this round separates them | **High** for the byte-identity, the record count, the `nameLen` invariant on a second file and the exact correspondence with lines 263..272 (all four are whole-file measurements over both roots) / **Unknown** which of the two copies the engine actually draws, and therefore whether deleting `famehall.dat` reproduces the same ten names | ● active | [EXP-0143](../experiments/EXP-0143-actor-names/) |

Publication editing: the literal name list and selected instruction excerpts are omitted or restated; the original evidence remains at the research revision in [SOURCE.md](../SOURCE.md). Confidence levels and correction scope are unchanged.
